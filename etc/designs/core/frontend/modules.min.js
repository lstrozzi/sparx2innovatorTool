!function(t, e) {
    "object" == typeof module && "object" == typeof module.exports ? module.exports = t.document ? e(t, !0) : function(t) {
        if (t.document)
            return e(t);
        throw new Error("jQuery requires a window with a document")
    }
    : e(t)
}("undefined" != typeof window ? window : this, function(w, R) {
    function L(t, e) {
        return e.toUpperCase()
    }
    var d = []
      , f = w.document
      , u = d.slice
      , j = d.concat
      , H = d.push
      , F = d.indexOf
      , W = {}
      , Y = W.toString
      , g = W.hasOwnProperty
      , m = {}
      , t = "1.12.4"
      , x = function(t, e) {
        return new x.fn.init(t,e)
    }
      , q = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g
      , z = /^-ms-/
      , B = /-([\da-z])/gi;
    function V(t) {
        var e = !!t && "length"in t && t.length
          , n = x.type(t);
        return "function" !== n && !x.isWindow(t) && ("array" === n || 0 === e || "number" == typeof e && 0 < e && e - 1 in t)
    }
    x.fn = x.prototype = {
        jquery: t,
        constructor: x,
        selector: "",
        length: 0,
        toArray: function() {
            return u.call(this)
        },
        get: function(t) {
            return null != t ? t < 0 ? this[t + this.length] : this[t] : u.call(this)
        },
        pushStack: function(t) {
            t = x.merge(this.constructor(), t);
            return t.prevObject = this,
            t.context = this.context,
            t
        },
        each: function(t) {
            return x.each(this, t)
        },
        map: function(n) {
            return this.pushStack(x.map(this, function(t, e) {
                return n.call(t, e, t)
            }))
        },
        slice: function() {
            return this.pushStack(u.apply(this, arguments))
        },
        first: function() {
            return this.eq(0)
        },
        last: function() {
            return this.eq(-1)
        },
        eq: function(t) {
            var e = this.length
              , t = +t + (t < 0 ? e : 0);
            return this.pushStack(0 <= t && t < e ? [this[t]] : [])
        },
        end: function() {
            return this.prevObject || this.constructor()
        },
        push: H,
        sort: d.sort,
        splice: d.splice
    },
    x.extend = x.fn.extend = function() {
        var t, e, n, i, s, r = arguments[0] || {}, o = 1, a = arguments.length, l = !1;
        for ("boolean" == typeof r && (l = r,
        r = arguments[o] || {},
        o++),
        "object" == typeof r || x.isFunction(r) || (r = {}),
        o === a && (r = this,
        o--); o < a; o++)
            if (null != (i = arguments[o]))
                for (n in i)
                    s = r[n],
                    r !== (e = i[n]) && (l && e && (x.isPlainObject(e) || (t = x.isArray(e))) ? (s = t ? (t = !1,
                    s && x.isArray(s) ? s : []) : s && x.isPlainObject(s) ? s : {},
                    r[n] = x.extend(l, s, e)) : void 0 !== e && (r[n] = e));
        return r
    }
    ,
    x.extend({
        expando: "jQuery" + (t + Math.random()).replace(/\D/g, ""),
        isReady: !0,
        error: function(t) {
            throw new Error(t)
        },
        noop: function() {},
        isFunction: function(t) {
            return "function" === x.type(t)
        },
        isArray: Array.isArray || function(t) {
            return "array" === x.type(t)
        }
        ,
        isWindow: function(t) {
            return null != t && t == t.window
        },
        isNumeric: function(t) {
            var e = t && t.toString();
            return !x.isArray(t) && 0 <= e - parseFloat(e) + 1
        },
        isEmptyObject: function(t) {
            for (var e in t)
                return !1;
            return !0
        },
        isPlainObject: function(t) {
            if (!t || "object" !== x.type(t) || t.nodeType || x.isWindow(t))
                return !1;
            try {
                if (t.constructor && !g.call(t, "constructor") && !g.call(t.constructor.prototype, "isPrototypeOf"))
                    return !1
            } catch (t) {
                return !1
            }
            if (!m.ownFirst)
                for (var e in t)
                    return g.call(t, e);
            for (e in t)
                ;
            return void 0 === e || g.call(t, e)
        },
        type: function(t) {
            return null == t ? t + "" : "object" == typeof t || "function" == typeof t ? W[Y.call(t)] || "object" : typeof t
        },
        globalEval: function(t) {
            t && x.trim(t) && (w.execScript || function(t) {
                w.eval.call(w, t)
            }
            )(t)
        },
        camelCase: function(t) {
            return t.replace(z, "ms-").replace(B, L)
        },
        nodeName: function(t, e) {
            return t.nodeName && t.nodeName.toLowerCase() === e.toLowerCase()
        },
        each: function(t, e) {
            var n, i = 0;
            if (V(t))
                for (n = t.length; i < n && !1 !== e.call(t[i], i, t[i]); i++)
                    ;
            else
                for (i in t)
                    if (!1 === e.call(t[i], i, t[i]))
                        break;
            return t
        },
        trim: function(t) {
            return null == t ? "" : (t + "").replace(q, "")
        },
        makeArray: function(t, e) {
            e = e || [];
            return null != t && (V(Object(t)) ? x.merge(e, "string" == typeof t ? [t] : t) : H.call(e, t)),
            e
        },
        inArray: function(t, e, n) {
            var i;
            if (e) {
                if (F)
                    return F.call(e, t, n);
                for (i = e.length,
                n = n ? n < 0 ? Math.max(0, i + n) : n : 0; n < i; n++)
                    if (n in e && e[n] === t)
                        return n
            }
            return -1
        },
        merge: function(t, e) {
            for (var n = +e.length, i = 0, s = t.length; i < n; )
                t[s++] = e[i++];
            if (n != n)
                for (; void 0 !== e[i]; )
                    t[s++] = e[i++];
            return t.length = s,
            t
        },
        grep: function(t, e, n) {
            for (var i = [], s = 0, r = t.length, o = !n; s < r; s++)
                !e(t[s], s) != o && i.push(t[s]);
            return i
        },
        map: function(t, e, n) {
            var i, s, r = 0, o = [];
            if (V(t))
                for (i = t.length; r < i; r++)
                    null != (s = e(t[r], r, n)) && o.push(s);
            else
                for (r in t)
                    null != (s = e(t[r], r, n)) && o.push(s);
            return j.apply([], o)
        },
        guid: 1,
        proxy: function(t, e) {
            var n, i;
            if ("string" == typeof e && (i = t[e],
            e = t,
            t = i),
            x.isFunction(t))
                return n = u.call(arguments, 2),
                (i = function() {
                    return t.apply(e || this, n.concat(u.call(arguments)))
                }
                ).guid = t.guid = t.guid || x.guid++,
                i
        },
        now: function() {
            return +new Date
        },
        support: m
    }),
    "function" == typeof Symbol && (x.fn[Symbol.iterator] = d[Symbol.iterator]),
    x.each("Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "), function(t, e) {
        W["[object " + e + "]"] = e.toLowerCase()
    });
    function i(t, e, n) {
        for (var i = [], s = void 0 !== n; (t = t[e]) && 9 !== t.nodeType; )
            if (1 === t.nodeType) {
                if (s && x(t).is(n))
                    break;
                i.push(t)
            }
        return i
    }
    function U(t, e) {
        for (var n = []; t; t = t.nextSibling)
            1 === t.nodeType && t !== e && n.push(t);
        return n
    }
    var t = function(R) {
        function d(t, e, n) {
            var i = "0x" + e - 65536;
            return i != i || n ? e : i < 0 ? String.fromCharCode(65536 + i) : String.fromCharCode(i >> 10 | 55296, 1023 & i | 56320)
        }
        function L() {
            x()
        }
        var t, f, _, r, j, g, H, F, w, l, c, x, C, e, k, m, i, s, y, S = "sizzle" + +new Date, v = R.document, T = 0, W = 0, Y = ct(), q = ct(), b = ct(), z = function(t, e) {
            return t === e && (c = !0),
            0
        }, B = {}.hasOwnProperty, n = [], V = n.pop, U = n.push, D = n.push, G = n.slice, E = function(t, e) {
            for (var n = 0, i = t.length; n < i; n++)
                if (t[n] === e)
                    return n;
            return -1
        }, Q = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", o = "[\\x20\\t\\r\\n\\f]", a = "(?:\\\\.|[\\w-]|[^\\x00-\\xa0])+", K = "\\[" + o + "*(" + a + ")(?:" + o + "*([*^$|!~]?=)" + o + "*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(" + a + "))|)" + o + "*\\]", X = ":(" + a + ")(?:\\((('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|((?:\\\\.|[^\\\\()[\\]]|" + K + ")*)|.*)\\)|)", Z = new RegExp(o + "+","g"), N = new RegExp("^" + o + "+|((?:^|[^\\\\])(?:\\\\.)*)" + o + "+$","g"), J = new RegExp("^" + o + "*," + o + "*"), tt = new RegExp("^" + o + "*([>+~]|" + o + ")" + o + "*"), et = new RegExp("=" + o + "*([^\\]'\"]*?)" + o + "*\\]","g"), nt = new RegExp(X), it = new RegExp("^" + a + "$"), h = {
            ID: new RegExp("^#(" + a + ")"),
            CLASS: new RegExp("^\\.(" + a + ")"),
            TAG: new RegExp("^(" + a + "|[*])"),
            ATTR: new RegExp("^" + K),
            PSEUDO: new RegExp("^" + X),
            CHILD: new RegExp("^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + o + "*(even|odd|(([+-]|)(\\d*)n|)" + o + "*(?:([+-]|)" + o + "*(\\d+)|))" + o + "*\\)|)","i"),
            bool: new RegExp("^(?:" + Q + ")$","i"),
            needsContext: new RegExp("^" + o + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + o + "*((?:-\\d)?\\d*)" + o + "*\\)|)(?=[^-]|$)","i")
        }, st = /^(?:input|select|textarea|button)$/i, rt = /^h\d$/i, u = /^[^{]+\{\s*\[native \w/, ot = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, at = /[+~]/, lt = /'|\\/g, p = new RegExp("\\\\([\\da-f]{1,6}" + o + "?|(" + o + ")|.)","ig");
        try {
            D.apply(n = G.call(v.childNodes), v.childNodes),
            n[v.childNodes.length].nodeType
        } catch (t) {
            D = {
                apply: n.length ? function(t, e) {
                    U.apply(t, G.call(e))
                }
                : function(t, e) {
                    for (var n = t.length, i = 0; t[n++] = e[i++]; )
                        ;
                    t.length = n - 1
                }
            }
        }
        function $(t, e, n, i) {
            var s, r, o, a, l, c, u, d, h = e && e.ownerDocument, p = e ? e.nodeType : 9;
            if (n = n || [],
            "string" != typeof t || !t || 1 !== p && 9 !== p && 11 !== p)
                return n;
            if (!i && ((e ? e.ownerDocument || e : v) !== C && x(e),
            e = e || C,
            k)) {
                if (11 !== p && (c = ot.exec(t)))
                    if (s = c[1]) {
                        if (9 === p) {
                            if (!(o = e.getElementById(s)))
                                return n;
                            if (o.id === s)
                                return n.push(o),
                                n
                        } else if (h && (o = h.getElementById(s)) && y(e, o) && o.id === s)
                            return n.push(o),
                            n
                    } else {
                        if (c[2])
                            return D.apply(n, e.getElementsByTagName(t)),
                            n;
                        if ((s = c[3]) && f.getElementsByClassName && e.getElementsByClassName)
                            return D.apply(n, e.getElementsByClassName(s)),
                            n
                    }
                if (f.qsa && !b[t + " "] && (!m || !m.test(t))) {
                    if (1 !== p)
                        h = e,
                        d = t;
                    else if ("object" !== e.nodeName.toLowerCase()) {
                        for ((a = e.getAttribute("id")) ? a = a.replace(lt, "\\$&") : e.setAttribute("id", a = S),
                        r = (u = g(t)).length,
                        l = it.test(a) ? "#" + a : "[id='" + a + "']"; r--; )
                            u[r] = l + " " + O(u[r]);
                        d = u.join(","),
                        h = at.test(t) && ht(e.parentNode) || e
                    }
                    if (d)
                        try {
                            return D.apply(n, h.querySelectorAll(d)),
                            n
                        } catch (t) {} finally {
                            a === S && e.removeAttribute("id")
                        }
                }
            }
            return F(t.replace(N, "$1"), e, n, i)
        }
        function ct() {
            var n = [];
            function i(t, e) {
                return n.push(t + " ") > _.cacheLength && delete i[n.shift()],
                i[t + " "] = e
            }
            return i
        }
        function A(t) {
            return t[S] = !0,
            t
        }
        function M(t) {
            var e = C.createElement("div");
            try {
                return !!t(e)
            } catch (t) {
                return !1
            } finally {
                e.parentNode && e.parentNode.removeChild(e)
            }
        }
        function ut(t, e) {
            for (var n = t.split("|"), i = n.length; i--; )
                _.attrHandle[n[i]] = e
        }
        function dt(t, e) {
            var n = e && t
              , i = n && 1 === t.nodeType && 1 === e.nodeType && (~e.sourceIndex || 1 << 31) - (~t.sourceIndex || 1 << 31);
            if (i)
                return i;
            if (n)
                for (; n = n.nextSibling; )
                    if (n === e)
                        return -1;
            return t ? 1 : -1
        }
        function I(o) {
            return A(function(r) {
                return r = +r,
                A(function(t, e) {
                    for (var n, i = o([], t.length, r), s = i.length; s--; )
                        t[n = i[s]] && (t[n] = !(e[n] = t[n]))
                })
            })
        }
        function ht(t) {
            return t && void 0 !== t.getElementsByTagName && t
        }
        for (t in f = $.support = {},
        j = $.isXML = function(t) {
            t = t && (t.ownerDocument || t).documentElement;
            return !!t && "HTML" !== t.nodeName
        }
        ,
        x = $.setDocument = function(t) {
            var t = t ? t.ownerDocument || t : v;
            return t !== C && 9 === t.nodeType && t.documentElement && (e = (C = t).documentElement,
            k = !j(C),
            (t = C.defaultView) && t.top !== t && (t.addEventListener ? t.addEventListener("unload", L, !1) : t.attachEvent && t.attachEvent("onunload", L)),
            f.attributes = M(function(t) {
                return t.className = "i",
                !t.getAttribute("className")
            }),
            f.getElementsByTagName = M(function(t) {
                return t.appendChild(C.createComment("")),
                !t.getElementsByTagName("*").length
            }),
            f.getElementsByClassName = u.test(C.getElementsByClassName),
            f.getById = M(function(t) {
                return e.appendChild(t).id = S,
                !C.getElementsByName || !C.getElementsByName(S).length
            }),
            f.getById ? (_.find.ID = function(t, e) {
                if (void 0 !== e.getElementById && k)
                    return (e = e.getElementById(t)) ? [e] : []
            }
            ,
            _.filter.ID = function(t) {
                var e = t.replace(p, d);
                return function(t) {
                    return t.getAttribute("id") === e
                }
            }
            ) : (delete _.find.ID,
            _.filter.ID = function(t) {
                var e = t.replace(p, d);
                return function(t) {
                    t = void 0 !== t.getAttributeNode && t.getAttributeNode("id");
                    return t && t.value === e
                }
            }
            ),
            _.find.TAG = f.getElementsByTagName ? function(t, e) {
                return void 0 !== e.getElementsByTagName ? e.getElementsByTagName(t) : f.qsa ? e.querySelectorAll(t) : void 0
            }
            : function(t, e) {
                var n, i = [], s = 0, r = e.getElementsByTagName(t);
                if ("*" !== t)
                    return r;
                for (; n = r[s++]; )
                    1 === n.nodeType && i.push(n);
                return i
            }
            ,
            _.find.CLASS = f.getElementsByClassName && function(t, e) {
                if (void 0 !== e.getElementsByClassName && k)
                    return e.getElementsByClassName(t)
            }
            ,
            i = [],
            m = [],
            (f.qsa = u.test(C.querySelectorAll)) && (M(function(t) {
                e.appendChild(t).innerHTML = "<a id='" + S + "'></a><select id='" + S + "-\r\\' msallowcapture=''><option selected=''></option></select>",
                t.querySelectorAll("[msallowcapture^='']").length && m.push("[*^$]=" + o + "*(?:''|\"\")"),
                t.querySelectorAll("[selected]").length || m.push("\\[" + o + "*(?:value|" + Q + ")"),
                t.querySelectorAll("[id~=" + S + "-]").length || m.push("~="),
                t.querySelectorAll(":checked").length || m.push(":checked"),
                t.querySelectorAll("a#" + S + "+*").length || m.push(".#.+[+~]")
            }),
            M(function(t) {
                var e = C.createElement("input");
                e.setAttribute("type", "hidden"),
                t.appendChild(e).setAttribute("name", "D"),
                t.querySelectorAll("[name=d]").length && m.push("name" + o + "*[*^$|!~]?="),
                t.querySelectorAll(":enabled").length || m.push(":enabled", ":disabled"),
                t.querySelectorAll("*,:x"),
                m.push(",.*:")
            })),
            (f.matchesSelector = u.test(s = e.matches || e.webkitMatchesSelector || e.mozMatchesSelector || e.oMatchesSelector || e.msMatchesSelector)) && M(function(t) {
                f.disconnectedMatch = s.call(t, "div"),
                s.call(t, "[s!='']:x"),
                i.push("!=", X)
            }),
            m = m.length && new RegExp(m.join("|")),
            i = i.length && new RegExp(i.join("|")),
            t = u.test(e.compareDocumentPosition),
            y = t || u.test(e.contains) ? function(t, e) {
                var n = 9 === t.nodeType ? t.documentElement : t
                  , e = e && e.parentNode;
                return t === e || !(!e || 1 !== e.nodeType || !(n.contains ? n.contains(e) : t.compareDocumentPosition && 16 & t.compareDocumentPosition(e)))
            }
            : function(t, e) {
                if (e)
                    for (; e = e.parentNode; )
                        if (e === t)
                            return !0;
                return !1
            }
            ,
            z = t ? function(t, e) {
                if (t === e)
                    return c = !0,
                    0;
                var n = !t.compareDocumentPosition - !e.compareDocumentPosition;
                return n || (1 & (n = (t.ownerDocument || t) === (e.ownerDocument || e) ? t.compareDocumentPosition(e) : 1) || !f.sortDetached && e.compareDocumentPosition(t) === n ? t === C || t.ownerDocument === v && y(v, t) ? -1 : e === C || e.ownerDocument === v && y(v, e) ? 1 : l ? E(l, t) - E(l, e) : 0 : 4 & n ? -1 : 1)
            }
            : function(t, e) {
                if (t === e)
                    return c = !0,
                    0;
                var n, i = 0, s = t.parentNode, r = e.parentNode, o = [t], a = [e];
                if (!s || !r)
                    return t === C ? -1 : e === C ? 1 : s ? -1 : r ? 1 : l ? E(l, t) - E(l, e) : 0;
                if (s === r)
                    return dt(t, e);
                for (n = t; n = n.parentNode; )
                    o.unshift(n);
                for (n = e; n = n.parentNode; )
                    a.unshift(n);
                for (; o[i] === a[i]; )
                    i++;
                return i ? dt(o[i], a[i]) : o[i] === v ? -1 : a[i] === v ? 1 : 0
            }
            ),
            C
        }
        ,
        $.matches = function(t, e) {
            return $(t, null, null, e)
        }
        ,
        $.matchesSelector = function(t, e) {
            if ((t.ownerDocument || t) !== C && x(t),
            e = e.replace(et, "='$1']"),
            f.matchesSelector && k && !b[e + " "] && (!i || !i.test(e)) && (!m || !m.test(e)))
                try {
                    var n = s.call(t, e);
                    if (n || f.disconnectedMatch || t.document && 11 !== t.document.nodeType)
                        return n
                } catch (t) {}
            return 0 < $(e, C, null, [t]).length
        }
        ,
        $.contains = function(t, e) {
            return (t.ownerDocument || t) !== C && x(t),
            y(t, e)
        }
        ,
        $.attr = function(t, e) {
            (t.ownerDocument || t) !== C && x(t);
            var n = _.attrHandle[e.toLowerCase()]
              , n = n && B.call(_.attrHandle, e.toLowerCase()) ? n(t, e, !k) : void 0;
            return void 0 !== n ? n : f.attributes || !k ? t.getAttribute(e) : (n = t.getAttributeNode(e)) && n.specified ? n.value : null
        }
        ,
        $.error = function(t) {
            throw new Error("Syntax error, unrecognized expression: " + t)
        }
        ,
        $.uniqueSort = function(t) {
            var e, n = [], i = 0, s = 0;
            if (c = !f.detectDuplicates,
            l = !f.sortStable && t.slice(0),
            t.sort(z),
            c) {
                for (; e = t[s++]; )
                    e === t[s] && (i = n.push(s));
                for (; i--; )
                    t.splice(n[i], 1)
            }
            return l = null,
            t
        }
        ,
        r = $.getText = function(t) {
            var e, n = "", i = 0, s = t.nodeType;
            if (s) {
                if (1 === s || 9 === s || 11 === s) {
                    if ("string" == typeof t.textContent)
                        return t.textContent;
                    for (t = t.firstChild; t; t = t.nextSibling)
                        n += r(t)
                } else if (3 === s || 4 === s)
                    return t.nodeValue
            } else
                for (; e = t[i++]; )
                    n += r(e);
            return n
        }
        ,
        (_ = $.selectors = {
            cacheLength: 50,
            createPseudo: A,
            match: h,
            attrHandle: {},
            find: {},
            relative: {
                ">": {
                    dir: "parentNode",
                    first: !0
                },
                " ": {
                    dir: "parentNode"
                },
                "+": {
                    dir: "previousSibling",
                    first: !0
                },
                "~": {
                    dir: "previousSibling"
                }
            },
            preFilter: {
                ATTR: function(t) {
                    return t[1] = t[1].replace(p, d),
                    t[3] = (t[3] || t[4] || t[5] || "").replace(p, d),
                    "~=" === t[2] && (t[3] = " " + t[3] + " "),
                    t.slice(0, 4)
                },
                CHILD: function(t) {
                    return t[1] = t[1].toLowerCase(),
                    "nth" === t[1].slice(0, 3) ? (t[3] || $.error(t[0]),
                    t[4] = +(t[4] ? t[5] + (t[6] || 1) : 2 * ("even" === t[3] || "odd" === t[3])),
                    t[5] = +(t[7] + t[8] || "odd" === t[3])) : t[3] && $.error(t[0]),
                    t
                },
                PSEUDO: function(t) {
                    var e, n = !t[6] && t[2];
                    return h.CHILD.test(t[0]) ? null : (t[3] ? t[2] = t[4] || t[5] || "" : n && nt.test(n) && (e = g(n, !0)) && (e = n.indexOf(")", n.length - e) - n.length) && (t[0] = t[0].slice(0, e),
                    t[2] = n.slice(0, e)),
                    t.slice(0, 3))
                }
            },
            filter: {
                TAG: function(t) {
                    var e = t.replace(p, d).toLowerCase();
                    return "*" === t ? function() {
                        return !0
                    }
                    : function(t) {
                        return t.nodeName && t.nodeName.toLowerCase() === e
                    }
                },
                CLASS: function(t) {
                    var e = Y[t + " "];
                    return e || (e = new RegExp("(^|" + o + ")" + t + "(" + o + "|$)")) && Y(t, function(t) {
                        return e.test("string" == typeof t.className && t.className || void 0 !== t.getAttribute && t.getAttribute("class") || "")
                    })
                },
                ATTR: function(e, n, i) {
                    return function(t) {
                        t = $.attr(t, e);
                        return null == t ? "!=" === n : !n || (t += "",
                        "=" === n ? t === i : "!=" === n ? t !== i : "^=" === n ? i && 0 === t.indexOf(i) : "*=" === n ? i && -1 < t.indexOf(i) : "$=" === n ? i && t.slice(-i.length) === i : "~=" === n ? -1 < (" " + t.replace(Z, " ") + " ").indexOf(i) : "|=" === n && (t === i || t.slice(0, i.length + 1) === i + "-"))
                    }
                },
                CHILD: function(f, t, e, g, m) {
                    var y = "nth" !== f.slice(0, 3)
                      , v = "last" !== f.slice(-4)
                      , b = "of-type" === t;
                    return 1 === g && 0 === m ? function(t) {
                        return !!t.parentNode
                    }
                    : function(t, e, n) {
                        var i, s, r, o, a, l, c = y != v ? "nextSibling" : "previousSibling", u = t.parentNode, d = b && t.nodeName.toLowerCase(), h = !n && !b, p = !1;
                        if (u) {
                            if (y) {
                                for (; c; ) {
                                    for (o = t; o = o[c]; )
                                        if (b ? o.nodeName.toLowerCase() === d : 1 === o.nodeType)
                                            return !1;
                                    l = c = "only" === f && !l && "nextSibling"
                                }
                                return !0
                            }
                            if (l = [v ? u.firstChild : u.lastChild],
                            v && h) {
                                for (p = (a = (i = (s = (r = (o = u)[S] || (o[S] = {}))[o.uniqueID] || (r[o.uniqueID] = {}))[f] || [])[0] === T && i[1]) && i[2],
                                o = a && u.childNodes[a]; o = ++a && o && o[c] || (p = a = 0) || l.pop(); )
                                    if (1 === o.nodeType && ++p && o === t) {
                                        s[f] = [T, a, p];
                                        break
                                    }
                            } else if (!1 === (p = h ? a = (i = (s = (r = (o = t)[S] || (o[S] = {}))[o.uniqueID] || (r[o.uniqueID] = {}))[f] || [])[0] === T && i[1] : p))
                                for (; (o = ++a && o && o[c] || (p = a = 0) || l.pop()) && ((b ? o.nodeName.toLowerCase() !== d : 1 !== o.nodeType) || !++p || (h && ((s = (r = o[S] || (o[S] = {}))[o.uniqueID] || (r[o.uniqueID] = {}))[f] = [T, p]),
                                o !== t)); )
                                    ;
                            return (p -= m) === g || p % g == 0 && 0 <= p / g
                        }
                    }
                },
                PSEUDO: function(t, r) {
                    var e, o = _.pseudos[t] || _.setFilters[t.toLowerCase()] || $.error("unsupported pseudo: " + t);
                    return o[S] ? o(r) : 1 < o.length ? (e = [t, t, "", r],
                    _.setFilters.hasOwnProperty(t.toLowerCase()) ? A(function(t, e) {
                        for (var n, i = o(t, r), s = i.length; s--; )
                            t[n = E(t, i[s])] = !(e[n] = i[s])
                    }) : function(t) {
                        return o(t, 0, e)
                    }
                    ) : o
                }
            },
            pseudos: {
                not: A(function(t) {
                    var i = []
                      , s = []
                      , a = H(t.replace(N, "$1"));
                    return a[S] ? A(function(t, e, n, i) {
                        for (var s, r = a(t, null, i, []), o = t.length; o--; )
                            (s = r[o]) && (t[o] = !(e[o] = s))
                    }) : function(t, e, n) {
                        return i[0] = t,
                        a(i, null, n, s),
                        i[0] = null,
                        !s.pop()
                    }
                }),
                has: A(function(e) {
                    return function(t) {
                        return 0 < $(e, t).length
                    }
                }),
                contains: A(function(e) {
                    return e = e.replace(p, d),
                    function(t) {
                        return -1 < (t.textContent || t.innerText || r(t)).indexOf(e)
                    }
                }),
                lang: A(function(n) {
                    return it.test(n || "") || $.error("unsupported lang: " + n),
                    n = n.replace(p, d).toLowerCase(),
                    function(t) {
                        var e;
                        do {
                            if (e = k ? t.lang : t.getAttribute("xml:lang") || t.getAttribute("lang"))
                                return (e = e.toLowerCase()) === n || 0 === e.indexOf(n + "-")
                        } while ((t = t.parentNode) && 1 === t.nodeType);
                        return !1
                    }
                }),
                target: function(t) {
                    var e = R.location && R.location.hash;
                    return e && e.slice(1) === t.id
                },
                root: function(t) {
                    return t === e
                },
                focus: function(t) {
                    return t === C.activeElement && (!C.hasFocus || C.hasFocus()) && !!(t.type || t.href || ~t.tabIndex)
                },
                enabled: function(t) {
                    return !1 === t.disabled
                },
                disabled: function(t) {
                    return !0 === t.disabled
                },
                checked: function(t) {
                    var e = t.nodeName.toLowerCase();
                    return "input" === e && !!t.checked || "option" === e && !!t.selected
                },
                selected: function(t) {
                    return t.parentNode && t.parentNode.selectedIndex,
                    !0 === t.selected
                },
                empty: function(t) {
                    for (t = t.firstChild; t; t = t.nextSibling)
                        if (t.nodeType < 6)
                            return !1;
                    return !0
                },
                parent: function(t) {
                    return !_.pseudos.empty(t)
                },
                header: function(t) {
                    return rt.test(t.nodeName)
                },
                input: function(t) {
                    return st.test(t.nodeName)
                },
                button: function(t) {
                    var e = t.nodeName.toLowerCase();
                    return "input" === e && "button" === t.type || "button" === e
                },
                text: function(t) {
                    return "input" === t.nodeName.toLowerCase() && "text" === t.type && (null == (t = t.getAttribute("type")) || "text" === t.toLowerCase())
                },
                first: I(function() {
                    return [0]
                }),
                last: I(function(t, e) {
                    return [e - 1]
                }),
                eq: I(function(t, e, n) {
                    return [n < 0 ? n + e : n]
                }),
                even: I(function(t, e) {
                    for (var n = 0; n < e; n += 2)
                        t.push(n);
                    return t
                }),
                odd: I(function(t, e) {
                    for (var n = 1; n < e; n += 2)
                        t.push(n);
                    return t
                }),
                lt: I(function(t, e, n) {
                    for (var i = n < 0 ? n + e : n; 0 <= --i; )
                        t.push(i);
                    return t
                }),
                gt: I(function(t, e, n) {
                    for (var i = n < 0 ? n + e : n; ++i < e; )
                        t.push(i);
                    return t
                })
            }
        }).pseudos.nth = _.pseudos.eq,
        {
            radio: !0,
            checkbox: !0,
            file: !0,
            password: !0,
            image: !0
        })
            _.pseudos[t] = function(e) {
                return function(t) {
                    return "input" === t.nodeName.toLowerCase() && t.type === e
                }
            }(t);
        for (t in {
            submit: !0,
            reset: !0
        })
            _.pseudos[t] = function(n) {
                return function(t) {
                    var e = t.nodeName.toLowerCase();
                    return ("input" === e || "button" === e) && t.type === n
                }
            }(t);
        function pt() {}
        function O(t) {
            for (var e = 0, n = t.length, i = ""; e < n; e++)
                i += t[e].value;
            return i
        }
        function ft(o, t, e) {
            var a = t.dir
              , l = e && "parentNode" === a
              , c = W++;
            return t.first ? function(t, e, n) {
                for (; t = t[a]; )
                    if (1 === t.nodeType || l)
                        return o(t, e, n)
            }
            : function(t, e, n) {
                var i, s, r = [T, c];
                if (n) {
                    for (; t = t[a]; )
                        if ((1 === t.nodeType || l) && o(t, e, n))
                            return !0
                } else
                    for (; t = t[a]; )
                        if (1 === t.nodeType || l) {
                            if ((i = (s = (s = t[S] || (t[S] = {}))[t.uniqueID] || (s[t.uniqueID] = {}))[a]) && i[0] === T && i[1] === c)
                                return r[2] = i[2];
                            if ((s[a] = r)[2] = o(t, e, n))
                                return !0
                        }
            }
        }
        function gt(s) {
            return 1 < s.length ? function(t, e, n) {
                for (var i = s.length; i--; )
                    if (!s[i](t, e, n))
                        return !1;
                return !0
            }
            : s[0]
        }
        function P(t, e, n, i, s) {
            for (var r, o = [], a = 0, l = t.length, c = null != e; a < l; a++)
                !(r = t[a]) || n && !n(r, i, s) || (o.push(r),
                c && e.push(a));
            return o
        }
        function mt(p, f, g, m, y, t) {
            return m && !m[S] && (m = mt(m)),
            y && !y[S] && (y = mt(y, t)),
            A(function(t, e, n, i) {
                var s, r, o, a = [], l = [], c = e.length, u = t || function(t, e, n) {
                    for (var i = 0, s = e.length; i < s; i++)
                        $(t, e[i], n);
                    return n
                }(f || "*", n.nodeType ? [n] : n, []), d = !p || !t && f ? u : P(u, a, p, n, i), h = g ? y || (t ? p : c || m) ? [] : e : d;
                if (g && g(d, h, n, i),
                m)
                    for (s = P(h, l),
                    m(s, [], n, i),
                    r = s.length; r--; )
                        (o = s[r]) && (h[l[r]] = !(d[l[r]] = o));
                if (t) {
                    if (y || p) {
                        if (y) {
                            for (s = [],
                            r = h.length; r--; )
                                (o = h[r]) && s.push(d[r] = o);
                            y(null, h = [], s, i)
                        }
                        for (r = h.length; r--; )
                            (o = h[r]) && -1 < (s = y ? E(t, o) : a[r]) && (t[s] = !(e[s] = o))
                    }
                } else
                    h = P(h === e ? h.splice(c, h.length) : h),
                    y ? y(null, e, h, i) : D.apply(e, h)
            })
        }
        function yt(m, y) {
            function t(t, e, n, i, s) {
                var r, o, a, l = 0, c = "0", u = t && [], d = [], h = w, p = t || b && _.find.TAG("*", s), f = T += null == h ? 1 : Math.random() || .1, g = p.length;
                for (s && (w = e === C || e || s); c !== g && null != (r = p[c]); c++) {
                    if (b && r) {
                        for (o = 0,
                        e || r.ownerDocument === C || (x(r),
                        n = !k); a = m[o++]; )
                            if (a(r, e || C, n)) {
                                i.push(r);
                                break
                            }
                        s && (T = f)
                    }
                    v && ((r = !a && r) && l--,
                    t && u.push(r))
                }
                if (l += c,
                v && c !== l) {
                    for (o = 0; a = y[o++]; )
                        a(u, d, e, n);
                    if (t) {
                        if (0 < l)
                            for (; c--; )
                                u[c] || d[c] || (d[c] = V.call(i));
                        d = P(d)
                    }
                    D.apply(i, d),
                    s && !t && 0 < d.length && 1 < l + y.length && $.uniqueSort(i)
                }
                return s && (T = f,
                w = h),
                u
            }
            var v = 0 < y.length
              , b = 0 < m.length;
            return v ? A(t) : t
        }
        return pt.prototype = _.filters = _.pseudos,
        _.setFilters = new pt,
        g = $.tokenize = function(t, e) {
            var n, i, s, r, o, a, l, c = q[t + " "];
            if (c)
                return e ? 0 : c.slice(0);
            for (o = t,
            a = [],
            l = _.preFilter; o; ) {
                for (r in n && !(i = J.exec(o)) || (i && (o = o.slice(i[0].length) || o),
                a.push(s = [])),
                n = !1,
                (i = tt.exec(o)) && (n = i.shift(),
                s.push({
                    value: n,
                    type: i[0].replace(N, " ")
                }),
                o = o.slice(n.length)),
                _.filter)
                    !(i = h[r].exec(o)) || l[r] && !(i = l[r](i)) || (n = i.shift(),
                    s.push({
                        value: n,
                        type: r,
                        matches: i
                    }),
                    o = o.slice(n.length));
                if (!n)
                    break
            }
            return e ? o.length : o ? $.error(t) : q(t, a).slice(0)
        }
        ,
        H = $.compile = function(t, e) {
            var n, i = [], s = [], r = b[t + " "];
            if (!r) {
                for (n = (e = e || g(t)).length; n--; )
                    ((r = function t(e) {
                        for (var i, n, s, r = e.length, o = _.relative[e[0].type], a = o || _.relative[" "], l = o ? 1 : 0, c = ft(function(t) {
                            return t === i
                        }, a, !0), u = ft(function(t) {
                            return -1 < E(i, t)
                        }, a, !0), d = [function(t, e, n) {
                            return t = !o && (n || e !== w) || ((i = e).nodeType ? c : u)(t, e, n),
                            i = null,
                            t
                        }
                        ]; l < r; l++)
                            if (n = _.relative[e[l].type])
                                d = [ft(gt(d), n)];
                            else {
                                if ((n = _.filter[e[l].type].apply(null, e[l].matches))[S]) {
                                    for (s = ++l; s < r && !_.relative[e[s].type]; s++)
                                        ;
                                    return mt(1 < l && gt(d), 1 < l && O(e.slice(0, l - 1).concat({
                                        value: " " === e[l - 2].type ? "*" : ""
                                    })).replace(N, "$1"), n, l < s && t(e.slice(l, s)), s < r && t(e = e.slice(s)), s < r && O(e))
                                }
                                d.push(n)
                            }
                        return gt(d)
                    }(e[n]))[S] ? i : s).push(r);
                (r = b(t, yt(s, i))).selector = t
            }
            return r
        }
        ,
        F = $.select = function(t, e, n, i) {
            var s, r, o, a, l, c = "function" == typeof t && t, u = !i && g(t = c.selector || t);
            if (n = n || [],
            1 === u.length) {
                if (2 < (r = u[0] = u[0].slice(0)).length && "ID" === (o = r[0]).type && f.getById && 9 === e.nodeType && k && _.relative[r[1].type]) {
                    if (!(e = (_.find.ID(o.matches[0].replace(p, d), e) || [])[0]))
                        return n;
                    c && (e = e.parentNode),
                    t = t.slice(r.shift().value.length)
                }
                for (s = h.needsContext.test(t) ? 0 : r.length; s-- && (o = r[s],
                !_.relative[a = o.type]); )
                    if ((l = _.find[a]) && (i = l(o.matches[0].replace(p, d), at.test(r[0].type) && ht(e.parentNode) || e))) {
                        if (r.splice(s, 1),
                        t = i.length && O(r))
                            break;
                        return D.apply(n, i),
                        n
                    }
            }
            return (c || H(t, u))(i, e, !k, n, !e || at.test(t) && ht(e.parentNode) || e),
            n
        }
        ,
        f.sortStable = S.split("").sort(z).join("") === S,
        f.detectDuplicates = !!c,
        x(),
        f.sortDetached = M(function(t) {
            return 1 & t.compareDocumentPosition(C.createElement("div"))
        }),
        M(function(t) {
            return t.innerHTML = "<a href='#'></a>",
            "#" === t.firstChild.getAttribute("href")
        }) || ut("type|href|height|width", function(t, e, n) {
            if (!n)
                return t.getAttribute(e, "type" === e.toLowerCase() ? 1 : 2)
        }),
        f.attributes && M(function(t) {
            return t.innerHTML = "<input/>",
            t.firstChild.setAttribute("value", ""),
            "" === t.firstChild.getAttribute("value")
        }) || ut("value", function(t, e, n) {
            if (!n && "input" === t.nodeName.toLowerCase())
                return t.defaultValue
        }),
        M(function(t) {
            return null == t.getAttribute("disabled")
        }) || ut(Q, function(t, e, n) {
            if (!n)
                return !0 === t[e] ? e.toLowerCase() : (n = t.getAttributeNode(e)) && n.specified ? n.value : null
        }),
        $
    }(w)
      , G = (x.find = t,
    x.expr = t.selectors,
    x.expr[":"] = x.expr.pseudos,
    x.uniqueSort = x.unique = t.uniqueSort,
    x.text = t.getText,
    x.isXMLDoc = t.isXML,
    x.contains = t.contains,
    x.expr.match.needsContext)
      , Q = /^<([\w-]+)\s*\/?>(?:<\/\1>|)$/
      , K = /^.[^:#\[\.,]*$/;
    function X(t, n, i) {
        if (x.isFunction(n))
            return x.grep(t, function(t, e) {
                return !!n.call(t, e, t) !== i
            });
        if (n.nodeType)
            return x.grep(t, function(t) {
                return t === n !== i
            });
        if ("string" == typeof n) {
            if (K.test(n))
                return x.filter(n, t, i);
            n = x.filter(n, t)
        }
        return x.grep(t, function(t) {
            return -1 < x.inArray(t, n) !== i
        })
    }
    x.filter = function(t, e, n) {
        var i = e[0];
        return n && (t = ":not(" + t + ")"),
        1 === e.length && 1 === i.nodeType ? x.find.matchesSelector(i, t) ? [i] : [] : x.find.matches(t, x.grep(e, function(t) {
            return 1 === t.nodeType
        }))
    }
    ,
    x.fn.extend({
        find: function(t) {
            var e, n = [], i = this, s = i.length;
            if ("string" != typeof t)
                return this.pushStack(x(t).filter(function() {
                    for (e = 0; e < s; e++)
                        if (x.contains(i[e], this))
                            return !0
                }));
            for (e = 0; e < s; e++)
                x.find(t, i[e], n);
            return (n = this.pushStack(1 < s ? x.unique(n) : n)).selector = this.selector ? this.selector + " " + t : t,
            n
        },
        filter: function(t) {
            return this.pushStack(X(this, t || [], !1))
        },
        not: function(t) {
            return this.pushStack(X(this, t || [], !0))
        },
        is: function(t) {
            return !!X(this, "string" == typeof t && G.test(t) ? x(t) : t || [], !1).length
        }
    });
    var Z, J = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]*))$/, tt = ((x.fn.init = function(t, e, n) {
        if (!t)
            return this;
        if (n = n || Z,
        "string" != typeof t)
            return t.nodeType ? (this.context = this[0] = t,
            this.length = 1,
            this) : x.isFunction(t) ? void 0 !== n.ready ? n.ready(t) : t(x) : (void 0 !== t.selector && (this.selector = t.selector,
            this.context = t.context),
            x.makeArray(t, this));
        if (!(i = "<" === t.charAt(0) && ">" === t.charAt(t.length - 1) && 3 <= t.length ? [null, t, null] : J.exec(t)) || !i[1] && e)
            return (!e || e.jquery ? e || n : this.constructor(e)).find(t);
        if (i[1]) {
            if (e = e instanceof x ? e[0] : e,
            x.merge(this, x.parseHTML(i[1], e && e.nodeType ? e.ownerDocument || e : f, !0)),
            Q.test(i[1]) && x.isPlainObject(e))
                for (var i in e)
                    x.isFunction(this[i]) ? this[i](e[i]) : this.attr(i, e[i]);
            return this
        }
        if ((n = f.getElementById(i[2])) && n.parentNode) {
            if (n.id !== i[2])
                return Z.find(t);
            this.length = 1,
            this[0] = n
        }
        return this.context = f,
        this.selector = t,
        this
    }
    ).prototype = x.fn,
    Z = x(f),
    /^(?:parents|prev(?:Until|All))/), et = {
        children: !0,
        contents: !0,
        next: !0,
        prev: !0
    };
    function nt(t, e) {
        for (; (t = t[e]) && 1 !== t.nodeType; )
            ;
        return t
    }
    x.fn.extend({
        has: function(t) {
            var e, n = x(t, this), i = n.length;
            return this.filter(function() {
                for (e = 0; e < i; e++)
                    if (x.contains(this, n[e]))
                        return !0
            })
        },
        closest: function(t, e) {
            for (var n, i = 0, s = this.length, r = [], o = G.test(t) || "string" != typeof t ? x(t, e || this.context) : 0; i < s; i++)
                for (n = this[i]; n && n !== e; n = n.parentNode)
                    if (n.nodeType < 11 && (o ? -1 < o.index(n) : 1 === n.nodeType && x.find.matchesSelector(n, t))) {
                        r.push(n);
                        break
                    }
            return this.pushStack(1 < r.length ? x.uniqueSort(r) : r)
        },
        index: function(t) {
            return t ? "string" == typeof t ? x.inArray(this[0], x(t)) : x.inArray(t.jquery ? t[0] : t, this) : this[0] && this[0].parentNode ? this.first().prevAll().length : -1
        },
        add: function(t, e) {
            return this.pushStack(x.uniqueSort(x.merge(this.get(), x(t, e))))
        },
        addBack: function(t) {
            return this.add(null == t ? this.prevObject : this.prevObject.filter(t))
        }
    }),
    x.each({
        parent: function(t) {
            t = t.parentNode;
            return t && 11 !== t.nodeType ? t : null
        },
        parents: function(t) {
            return i(t, "parentNode")
        },
        parentsUntil: function(t, e, n) {
            return i(t, "parentNode", n)
        },
        next: function(t) {
            return nt(t, "nextSibling")
        },
        prev: function(t) {
            return nt(t, "previousSibling")
        },
        nextAll: function(t) {
            return i(t, "nextSibling")
        },
        prevAll: function(t) {
            return i(t, "previousSibling")
        },
        nextUntil: function(t, e, n) {
            return i(t, "nextSibling", n)
        },
        prevUntil: function(t, e, n) {
            return i(t, "previousSibling", n)
        },
        siblings: function(t) {
            return U((t.parentNode || {}).firstChild, t)
        },
        children: function(t) {
            return U(t.firstChild)
        },
        contents: function(t) {
            return x.nodeName(t, "iframe") ? t.contentDocument || t.contentWindow.document : x.merge([], t.childNodes)
        }
    }, function(i, s) {
        x.fn[i] = function(t, e) {
            var n = x.map(this, s, t);
            return (e = "Until" !== i.slice(-5) ? t : e) && "string" == typeof e && (n = x.filter(e, n)),
            1 < this.length && (et[i] || (n = x.uniqueSort(n)),
            tt.test(i) && (n = n.reverse())),
            this.pushStack(n)
        }
    });
    var it, st, C = /\S+/g;
    function rt() {
        f.addEventListener ? (f.removeEventListener("DOMContentLoaded", e),
        w.removeEventListener("load", e)) : (f.detachEvent("onreadystatechange", e),
        w.detachEvent("onload", e))
    }
    function e() {
        !f.addEventListener && "load" !== w.event.type && "complete" !== f.readyState || (rt(),
        x.ready())
    }
    for (st in x.Callbacks = function(i) {
        var t, n;
        i = "string" == typeof i ? (t = i,
        n = {},
        x.each(t.match(C) || [], function(t, e) {
            n[e] = !0
        }),
        n) : x.extend({}, i);
        function s() {
            for (a = i.once,
            o = r = !0; c.length; u = -1)
                for (e = c.shift(); ++u < l.length; )
                    !1 === l[u].apply(e[0], e[1]) && i.stopOnFalse && (u = l.length,
                    e = !1);
            i.memory || (e = !1),
            r = !1,
            a && (l = e ? [] : "")
        }
        var r, e, o, a, l = [], c = [], u = -1, d = {
            add: function() {
                return l && (e && !r && (u = l.length - 1,
                c.push(e)),
                function n(t) {
                    x.each(t, function(t, e) {
                        x.isFunction(e) ? i.unique && d.has(e) || l.push(e) : e && e.length && "string" !== x.type(e) && n(e)
                    })
                }(arguments),
                e && !r && s()),
                this
            },
            remove: function() {
                return x.each(arguments, function(t, e) {
                    for (var n; -1 < (n = x.inArray(e, l, n)); )
                        l.splice(n, 1),
                        n <= u && u--
                }),
                this
            },
            has: function(t) {
                return t ? -1 < x.inArray(t, l) : 0 < l.length
            },
            empty: function() {
                return l = l && [],
                this
            },
            disable: function() {
                return a = c = [],
                l = e = "",
                this
            },
            disabled: function() {
                return !l
            },
            lock: function() {
                return a = !0,
                e || d.disable(),
                this
            },
            locked: function() {
                return !!a
            },
            fireWith: function(t, e) {
                return a || (e = [t, (e = e || []).slice ? e.slice() : e],
                c.push(e),
                r || s()),
                this
            },
            fire: function() {
                return d.fireWith(this, arguments),
                this
            },
            fired: function() {
                return !!o
            }
        };
        return d
    }
    ,
    x.extend({
        Deferred: function(t) {
            var r = [["resolve", "done", x.Callbacks("once memory"), "resolved"], ["reject", "fail", x.Callbacks("once memory"), "rejected"], ["notify", "progress", x.Callbacks("memory")]]
              , s = "pending"
              , o = {
                state: function() {
                    return s
                },
                always: function() {
                    return a.done(arguments).fail(arguments),
                    this
                },
                then: function() {
                    var s = arguments;
                    return x.Deferred(function(i) {
                        x.each(r, function(t, e) {
                            var n = x.isFunction(s[t]) && s[t];
                            a[e[1]](function() {
                                var t = n && n.apply(this, arguments);
                                t && x.isFunction(t.promise) ? t.promise().progress(i.notify).done(i.resolve).fail(i.reject) : i[e[0] + "With"](this === o ? i.promise() : this, n ? [t] : arguments)
                            })
                        }),
                        s = null
                    }).promise()
                },
                promise: function(t) {
                    return null != t ? x.extend(t, o) : o
                }
            }
              , a = {};
            return o.pipe = o.then,
            x.each(r, function(t, e) {
                var n = e[2]
                  , i = e[3];
                o[e[1]] = n.add,
                i && n.add(function() {
                    s = i
                }, r[1 ^ t][2].disable, r[2][2].lock),
                a[e[0]] = function() {
                    return a[e[0] + "With"](this === a ? o : this, arguments),
                    this
                }
                ,
                a[e[0] + "With"] = n.fireWith
            }),
            o.promise(a),
            t && t.call(a, a),
            a
        },
        when: function(t) {
            function e(e, n, i) {
                return function(t) {
                    n[e] = this,
                    i[e] = 1 < arguments.length ? u.call(arguments) : t,
                    i === s ? c.notifyWith(n, i) : --l || c.resolveWith(n, i)
                }
            }
            var s, n, i, r = 0, o = u.call(arguments), a = o.length, l = 1 !== a || t && x.isFunction(t.promise) ? a : 0, c = 1 === l ? t : x.Deferred();
            if (1 < a)
                for (s = new Array(a),
                n = new Array(a),
                i = new Array(a); r < a; r++)
                    o[r] && x.isFunction(o[r].promise) ? o[r].promise().progress(e(r, n, s)).done(e(r, i, o)).fail(c.reject) : --l;
            return l || c.resolveWith(i, o),
            c.promise()
        }
    }),
    x.fn.ready = function(t) {
        return x.ready.promise().done(t),
        this
    }
    ,
    x.extend({
        isReady: !1,
        readyWait: 1,
        holdReady: function(t) {
            t ? x.readyWait++ : x.ready(!0)
        },
        ready: function(t) {
            (!0 === t ? --x.readyWait : x.isReady) || (x.isReady = !0) !== t && 0 < --x.readyWait || (it.resolveWith(f, [x]),
            x.fn.triggerHandler && (x(f).triggerHandler("ready"),
            x(f).off("ready")))
        }
    }),
    x.ready.promise = function(t) {
        if (!it)
            if (it = x.Deferred(),
            "complete" === f.readyState || "loading" !== f.readyState && !f.documentElement.doScroll)
                w.setTimeout(x.ready);
            else if (f.addEventListener)
                f.addEventListener("DOMContentLoaded", e),
                w.addEventListener("load", e);
            else {
                f.attachEvent("onreadystatechange", e),
                w.attachEvent("onload", e);
                var n = !1;
                try {
                    n = null == w.frameElement && f.documentElement
                } catch (t) {}
                n && n.doScroll && !function e() {
                    if (!x.isReady) {
                        try {
                            n.doScroll("left")
                        } catch (t) {
                            return w.setTimeout(e, 50)
                        }
                        rt(),
                        x.ready()
                    }
                }()
            }
        return it.promise(t)
    }
    ,
    x.ready.promise(),
    x(m))
        break;
    m.ownFirst = "0" === st,
    m.inlineBlockNeedsLayout = !1,
    x(function() {
        var t, e, n = f.getElementsByTagName("body")[0];
        n && n.style && (t = f.createElement("div"),
        (e = f.createElement("div")).style.cssText = "position:absolute;border:0;width:0;height:0;top:0;left:-9999px",
        n.appendChild(e).appendChild(t),
        void 0 !== t.style.zoom && (t.style.cssText = "display:inline;margin:0;border:0;padding:1px;width:1px;zoom:1",
        m.inlineBlockNeedsLayout = t = 3 === t.offsetWidth,
        t && (n.style.zoom = 1)),
        n.removeChild(e))
    });
    t = f.createElement("div");
    m.deleteExpando = !0;
    try {
        delete t.test
    } catch (t) {
        m.deleteExpando = !1
    }
    function y(t) {
        var e = x.noData[(t.nodeName + " ").toLowerCase()]
          , n = +t.nodeType || 1;
        return (1 === n || 9 === n) && (!e || !0 !== e && t.getAttribute("classid") === e)
    }
    var s, ot = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, at = /([A-Z])/g;
    function lt(t, e, n) {
        if (void 0 === n && 1 === t.nodeType) {
            var i = "data-" + e.replace(at, "-$1").toLowerCase();
            if ("string" == typeof (n = t.getAttribute(i))) {
                try {
                    n = "true" === n || "false" !== n && ("null" === n ? null : +n + "" === n ? +n : ot.test(n) ? x.parseJSON(n) : n)
                } catch (t) {}
                x.data(t, e, n)
            } else
                n = void 0
        }
        return n
    }
    function ct(t) {
        for (var e in t)
            if (("data" !== e || !x.isEmptyObject(t[e])) && "toJSON" !== e)
                return;
        return 1
    }
    function ut(t, e, n, i) {
        if (y(t)) {
            var s, r = x.expando, o = t.nodeType, a = o ? x.cache : t, l = o ? t[r] : t[r] && r;
            if (l && a[l] && (i || a[l].data) || void 0 !== n || "string" != typeof e)
                return a[l = l || (o ? t[r] = d.pop() || x.guid++ : r)] || (a[l] = o ? {} : {
                    toJSON: x.noop
                }),
                "object" != typeof e && "function" != typeof e || (i ? a[l] = x.extend(a[l], e) : a[l].data = x.extend(a[l].data, e)),
                t = a[l],
                i || (t.data || (t.data = {}),
                t = t.data),
                void 0 !== n && (t[x.camelCase(e)] = n),
                "string" == typeof e ? null == (s = t[e]) && (s = t[x.camelCase(e)]) : s = t,
                s
        }
    }
    function dt(t, e, n) {
        if (y(t)) {
            var i, s, r = t.nodeType, o = r ? x.cache : t, a = r ? t[x.expando] : x.expando;
            if (o[a]) {
                if (e && (i = n ? o[a] : o[a].data)) {
                    s = (e = x.isArray(e) ? e.concat(x.map(e, x.camelCase)) : e in i || (e = x.camelCase(e))in i ? [e] : e.split(" ")).length;
                    for (; s--; )
                        delete i[e[s]];
                    if (n ? !ct(i) : !x.isEmptyObject(i))
                        return
                }
                (n || (delete o[a].data,
                ct(o[a]))) && (r ? x.cleanData([t], !0) : m.deleteExpando || o != o.window ? delete o[a] : o[a] = void 0)
            }
        }
    }
    x.extend({
        cache: {},
        noData: {
            "applet ": !0,
            "embed ": !0,
            "object ": "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000"
        },
        hasData: function(t) {
            return !!(t = t.nodeType ? x.cache[t[x.expando]] : t[x.expando]) && !ct(t)
        },
        data: function(t, e, n) {
            return ut(t, e, n)
        },
        removeData: function(t, e) {
            return dt(t, e)
        },
        _data: function(t, e, n) {
            return ut(t, e, n, !0)
        },
        _removeData: function(t, e) {
            return dt(t, e, !0)
        }
    }),
    x.fn.extend({
        data: function(t, e) {
            var n, i, s, r = this[0], o = r && r.attributes;
            if (void 0 !== t)
                return "object" == typeof t ? this.each(function() {
                    x.data(this, t)
                }) : 1 < arguments.length ? this.each(function() {
                    x.data(this, t, e)
                }) : r ? lt(r, t, x.data(r, t)) : void 0;
            if (this.length && (s = x.data(r),
            1 === r.nodeType && !x._data(r, "parsedAttrs"))) {
                for (n = o.length; n--; )
                    o[n] && 0 === (i = o[n].name).indexOf("data-") && lt(r, i = x.camelCase(i.slice(5)), s[i]);
                x._data(r, "parsedAttrs", !0)
            }
            return s
        },
        removeData: function(t) {
            return this.each(function() {
                x.removeData(this, t)
            })
        }
    }),
    x.extend({
        queue: function(t, e, n) {
            var i;
            if (t)
                return i = x._data(t, e = (e || "fx") + "queue"),
                n && (!i || x.isArray(n) ? i = x._data(t, e, x.makeArray(n)) : i.push(n)),
                i || []
        },
        dequeue: function(t, e) {
            e = e || "fx";
            var n = x.queue(t, e)
              , i = n.length
              , s = n.shift()
              , r = x._queueHooks(t, e);
            "inprogress" === s && (s = n.shift(),
            i--),
            s && ("fx" === e && n.unshift("inprogress"),
            delete r.stop,
            s.call(t, function() {
                x.dequeue(t, e)
            }, r)),
            !i && r && r.empty.fire()
        },
        _queueHooks: function(t, e) {
            var n = e + "queueHooks";
            return x._data(t, n) || x._data(t, n, {
                empty: x.Callbacks("once memory").add(function() {
                    x._removeData(t, e + "queue"),
                    x._removeData(t, n)
                })
            })
        }
    }),
    x.fn.extend({
        queue: function(e, n) {
            var t = 2;
            return "string" != typeof e && (n = e,
            e = "fx",
            t--),
            arguments.length < t ? x.queue(this[0], e) : void 0 === n ? this : this.each(function() {
                var t = x.queue(this, e, n);
                x._queueHooks(this, e),
                "fx" === e && "inprogress" !== t[0] && x.dequeue(this, e)
            })
        },
        dequeue: function(t) {
            return this.each(function() {
                x.dequeue(this, t)
            })
        },
        clearQueue: function(t) {
            return this.queue(t || "fx", [])
        },
        promise: function(t, e) {
            function n() {
                --s || r.resolveWith(o, [o])
            }
            var i, s = 1, r = x.Deferred(), o = this, a = this.length;
            for ("string" != typeof t && (e = t,
            t = void 0),
            t = t || "fx"; a--; )
                (i = x._data(o[a], t + "queueHooks")) && i.empty && (s++,
                i.empty.add(n));
            return n(),
            r.promise(e)
        }
    }),
    m.shrinkWrapBlocks = function() {
        return null != s ? s : (s = !1,
        (e = f.getElementsByTagName("body")[0]) && e.style ? (t = f.createElement("div"),
        (n = f.createElement("div")).style.cssText = "position:absolute;border:0;width:0;height:0;top:0;left:-9999px",
        e.appendChild(n).appendChild(t),
        void 0 !== t.style.zoom && (t.style.cssText = "-webkit-box-sizing:content-box;-moz-box-sizing:content-box;box-sizing:content-box;display:block;margin:0;border:0;padding:1px;width:1px;zoom:1",
        t.appendChild(f.createElement("div")).style.width = "5px",
        s = 3 !== t.offsetWidth),
        e.removeChild(n),
        s) : void 0);
        var t, e, n
    }
    ;
    function ht(t, e) {
        return "none" === x.css(t = e || t, "display") || !x.contains(t.ownerDocument, t)
    }
    var t = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source
      , pt = new RegExp("^(?:([+-])=|)(" + t + ")([a-z%]*)$","i")
      , a = ["Top", "Right", "Bottom", "Left"];
    function ft(t, e, n, i) {
        var s, r = 1, o = 20, a = i ? function() {
            return i.cur()
        }
        : function() {
            return x.css(t, e, "")
        }
        , l = a(), c = n && n[3] || (x.cssNumber[e] ? "" : "px"), u = (x.cssNumber[e] || "px" !== c && +l) && pt.exec(x.css(t, e));
        if (u && u[3] !== c)
            for (c = c || u[3],
            n = n || [],
            u = +l || 1; u /= r = r || ".5",
            x.style(t, e, u + c),
            r !== (r = a() / l) && 1 !== r && --o; )
                ;
        return n && (u = +u || +l || 0,
        s = n[1] ? u + (n[1] + 1) * n[2] : +n[2],
        i && (i.unit = c,
        i.start = u,
        i.end = s)),
        s
    }
    function h(t, e, n, i, s, r, o) {
        var a = 0
          , l = t.length
          , c = null == n;
        if ("object" === x.type(n))
            for (a in s = !0,
            n)
                h(t, e, a, n[a], !0, r, o);
        else if (void 0 !== i && (s = !0,
        x.isFunction(i) || (o = !0),
        e = c ? o ? (e.call(t, i),
        null) : (c = e,
        function(t, e, n) {
            return c.call(x(t), n)
        }
        ) : e))
            for (; a < l; a++)
                e(t[a], n, o ? i : i.call(t[a], a, e(t[a], n)));
        return s ? t : c ? e.call(t) : l ? e(t[0], n) : r
    }
    var gt = /^(?:checkbox|radio)$/i
      , mt = /<([\w:-]+)/
      , yt = /^$|\/(?:java|ecma)script/i
      , vt = /^\s+/
      , bt = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|dialog|figcaption|figure|footer|header|hgroup|main|mark|meter|nav|output|picture|progress|section|summary|template|time|video";
    function _t(t) {
        var e = bt.split("|")
          , n = t.createDocumentFragment();
        if (n.createElement)
            for (; e.length; )
                n.createElement(e.pop());
        return n
    }
    D = f.createElement("div"),
    T = f.createDocumentFragment(),
    I = f.createElement("input"),
    D.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>",
    m.leadingWhitespace = 3 === D.firstChild.nodeType,
    m.tbody = !D.getElementsByTagName("tbody").length,
    m.htmlSerialize = !!D.getElementsByTagName("link").length,
    m.html5Clone = "<:nav></:nav>" !== f.createElement("nav").cloneNode(!0).outerHTML,
    I.type = "checkbox",
    I.checked = !0,
    T.appendChild(I),
    m.appendChecked = I.checked,
    D.innerHTML = "<textarea>x</textarea>",
    m.noCloneChecked = !!D.cloneNode(!0).lastChild.defaultValue,
    T.appendChild(D),
    (I = f.createElement("input")).setAttribute("type", "radio"),
    I.setAttribute("checked", "checked"),
    I.setAttribute("name", "t"),
    D.appendChild(I),
    m.checkClone = D.cloneNode(!0).cloneNode(!0).lastChild.checked,
    m.noCloneEvent = !!D.addEventListener,
    D[x.expando] = 1,
    m.attributes = !D.getAttribute(x.expando);
    var v = {
        option: [1, "<select multiple='multiple'>", "</select>"],
        legend: [1, "<fieldset>", "</fieldset>"],
        area: [1, "<map>", "</map>"],
        param: [1, "<object>", "</object>"],
        thead: [1, "<table>", "</table>"],
        tr: [2, "<table><tbody>", "</tbody></table>"],
        col: [2, "<table><tbody></tbody><colgroup>", "</colgroup></table>"],
        td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
        _default: m.htmlSerialize ? [0, "", ""] : [1, "X<div>", "</div>"]
    };
    function b(t, e) {
        var n, i, s = 0, r = void 0 !== t.getElementsByTagName ? t.getElementsByTagName(e || "*") : void 0 !== t.querySelectorAll ? t.querySelectorAll(e || "*") : void 0;
        if (!r)
            for (r = [],
            n = t.childNodes || t; null != (i = n[s]); s++)
                !e || x.nodeName(i, e) ? r.push(i) : x.merge(r, b(i, e));
        return void 0 === e || e && x.nodeName(t, e) ? x.merge([t], r) : r
    }
    function wt(t, e) {
        for (var n, i = 0; null != (n = t[i]); i++)
            x._data(n, "globalEval", !e || x._data(e[i], "globalEval"))
    }
    v.optgroup = v.option,
    v.tbody = v.tfoot = v.colgroup = v.caption = v.thead,
    v.th = v.td;
    var xt = /<|&#?\w+;/
      , Ct = /<tbody/i;
    function kt(t) {
        gt.test(t.type) && (t.defaultChecked = t.checked)
    }
    function St(t, e, n, i, s) {
        for (var r, o, a, l, c, u, d, h = t.length, p = _t(e), f = [], g = 0; g < h; g++)
            if ((o = t[g]) || 0 === o)
                if ("object" === x.type(o))
                    x.merge(f, o.nodeType ? [o] : o);
                else if (xt.test(o)) {
                    for (l = l || p.appendChild(e.createElement("div")),
                    c = (mt.exec(o) || ["", ""])[1].toLowerCase(),
                    d = v[c] || v._default,
                    l.innerHTML = d[1] + x.htmlPrefilter(o) + d[2],
                    r = d[0]; r--; )
                        l = l.lastChild;
                    if (!m.leadingWhitespace && vt.test(o) && f.push(e.createTextNode(vt.exec(o)[0])),
                    !m.tbody)
                        for (r = (o = "table" !== c || Ct.test(o) ? "<table>" !== d[1] || Ct.test(o) ? 0 : l : l.firstChild) && o.childNodes.length; r--; )
                            x.nodeName(u = o.childNodes[r], "tbody") && !u.childNodes.length && o.removeChild(u);
                    for (x.merge(f, l.childNodes),
                    l.textContent = ""; l.firstChild; )
                        l.removeChild(l.firstChild);
                    l = p.lastChild
                } else
                    f.push(e.createTextNode(o));
        for (l && p.removeChild(l),
        m.appendChecked || x.grep(b(f, "input"), kt),
        g = 0; o = f[g++]; )
            if (i && -1 < x.inArray(o, i))
                s && s.push(o);
            else if (a = x.contains(o.ownerDocument, o),
            l = b(p.appendChild(o), "script"),
            a && wt(l),
            n)
                for (r = 0; o = l[r++]; )
                    yt.test(o.type || "") && n.push(o);
        return l = null,
        p
    }
    var Tt, Dt, Et = f.createElement("div");
    for (Tt in {
        submit: !0,
        change: !0,
        focusin: !0
    })
        (m[Tt] = (Dt = "on" + Tt)in w) || (Et.setAttribute(Dt, "t"),
        m[Tt] = !1 === Et.attributes[Dt].expando);
    var Nt = /^(?:input|select|textarea)$/i
      , $t = /^key/
      , At = /^(?:mouse|pointer|contextmenu|drag|drop)|click/
      , Mt = /^(?:focusinfocus|focusoutblur)$/
      , It = /^([^.]*)(?:\.(.+)|)/;
    function Ot() {
        return !0
    }
    function l() {
        return !1
    }
    function Pt() {
        try {
            return f.activeElement
        } catch (t) {}
    }
    function Rt(t, e, n, i, s, r) {
        var o, a;
        if ("object" == typeof e) {
            for (a in "string" != typeof n && (i = i || n,
            n = void 0),
            e)
                Rt(t, a, n, i, e[a], r);
            return t
        }
        if (null == i && null == s ? (s = n,
        i = n = void 0) : null == s && ("string" == typeof n ? (s = i,
        i = void 0) : (s = i,
        i = n,
        n = void 0)),
        !1 === s)
            s = l;
        else if (!s)
            return t;
        return 1 === r && (o = s,
        (s = function(t) {
            return x().off(t),
            o.apply(this, arguments)
        }
        ).guid = o.guid || (o.guid = x.guid++)),
        t.each(function() {
            x.event.add(this, e, s, i, n)
        })
    }
    x.event = {
        global: {},
        add: function(t, e, n, i, s) {
            var r, o, a, l, c, u, d, h, p, f = x._data(t);
            if (f)
                for (n.handler && (n = (a = n).handler,
                s = a.selector),
                n.guid || (n.guid = x.guid++),
                (r = f.events) || (r = f.events = {}),
                (c = f.handle) || ((c = f.handle = function(t) {
                    return void 0 === x || t && x.event.triggered === t.type ? void 0 : x.event.dispatch.apply(c.elem, arguments)
                }
                ).elem = t),
                o = (e = (e || "").match(C) || [""]).length; o--; )
                    d = p = (h = It.exec(e[o]) || [])[1],
                    h = (h[2] || "").split(".").sort(),
                    d && (l = x.event.special[d] || {},
                    d = (s ? l.delegateType : l.bindType) || d,
                    l = x.event.special[d] || {},
                    p = x.extend({
                        type: d,
                        origType: p,
                        data: i,
                        handler: n,
                        guid: n.guid,
                        selector: s,
                        needsContext: s && x.expr.match.needsContext.test(s),
                        namespace: h.join(".")
                    }, a),
                    (u = r[d]) || ((u = r[d] = []).delegateCount = 0,
                    l.setup && !1 !== l.setup.call(t, i, h, c) || (t.addEventListener ? t.addEventListener(d, c, !1) : t.attachEvent && t.attachEvent("on" + d, c))),
                    l.add && (l.add.call(t, p),
                    p.handler.guid || (p.handler.guid = n.guid)),
                    s ? u.splice(u.delegateCount++, 0, p) : u.push(p),
                    x.event.global[d] = !0)
        },
        remove: function(t, e, n, i, s) {
            var r, o, a, l, c, u, d, h, p, f, g, m = x.hasData(t) && x._data(t);
            if (m && (u = m.events)) {
                for (c = (e = (e || "").match(C) || [""]).length; c--; )
                    if (p = g = (a = It.exec(e[c]) || [])[1],
                    f = (a[2] || "").split(".").sort(),
                    p) {
                        for (d = x.event.special[p] || {},
                        h = u[p = (i ? d.delegateType : d.bindType) || p] || [],
                        a = a[2] && new RegExp("(^|\\.)" + f.join("\\.(?:.*\\.|)") + "(\\.|$)"),
                        l = r = h.length; r--; )
                            o = h[r],
                            !s && g !== o.origType || n && n.guid !== o.guid || a && !a.test(o.namespace) || i && i !== o.selector && ("**" !== i || !o.selector) || (h.splice(r, 1),
                            o.selector && h.delegateCount--,
                            d.remove && d.remove.call(t, o));
                        l && !h.length && (d.teardown && !1 !== d.teardown.call(t, f, m.handle) || x.removeEvent(t, p, m.handle),
                        delete u[p])
                    } else
                        for (p in u)
                            x.event.remove(t, p + e[c], n, i, !0);
                x.isEmptyObject(u) && (delete m.handle,
                x._removeData(t, "events"))
            }
        },
        trigger: function(t, e, n, i) {
            var s, r, o, a, l, c, u = [n || f], d = g.call(t, "type") ? t.type : t, h = g.call(t, "namespace") ? t.namespace.split(".") : [], p = l = n = n || f;
            if (3 !== n.nodeType && 8 !== n.nodeType && !Mt.test(d + x.event.triggered) && (-1 < d.indexOf(".") && (d = (h = d.split(".")).shift(),
            h.sort()),
            r = d.indexOf(":") < 0 && "on" + d,
            (t = t[x.expando] ? t : new x.Event(d,"object" == typeof t && t)).isTrigger = i ? 2 : 3,
            t.namespace = h.join("."),
            t.rnamespace = t.namespace ? new RegExp("(^|\\.)" + h.join("\\.(?:.*\\.|)") + "(\\.|$)") : null,
            t.result = void 0,
            t.target || (t.target = n),
            e = null == e ? [t] : x.makeArray(e, [t]),
            a = x.event.special[d] || {},
            i || !a.trigger || !1 !== a.trigger.apply(n, e))) {
                if (!i && !a.noBubble && !x.isWindow(n)) {
                    for (o = a.delegateType || d,
                    Mt.test(o + d) || (p = p.parentNode); p; p = p.parentNode)
                        u.push(p),
                        l = p;
                    l === (n.ownerDocument || f) && u.push(l.defaultView || l.parentWindow || w)
                }
                for (c = 0; (p = u[c++]) && !t.isPropagationStopped(); )
                    t.type = 1 < c ? o : a.bindType || d,
                    (s = (x._data(p, "events") || {})[t.type] && x._data(p, "handle")) && s.apply(p, e),
                    (s = r && p[r]) && s.apply && y(p) && (t.result = s.apply(p, e),
                    !1 === t.result && t.preventDefault());
                if (t.type = d,
                !i && !t.isDefaultPrevented() && (!a._default || !1 === a._default.apply(u.pop(), e)) && y(n) && r && n[d] && !x.isWindow(n)) {
                    (l = n[r]) && (n[r] = null),
                    x.event.triggered = d;
                    try {
                        n[d]()
                    } catch (t) {}
                    x.event.triggered = void 0,
                    l && (n[r] = l)
                }
                return t.result
            }
        },
        dispatch: function(t) {
            t = x.event.fix(t);
            var e, n, i, s, r, o = u.call(arguments), a = (x._data(this, "events") || {})[t.type] || [], l = x.event.special[t.type] || {};
            if ((o[0] = t).delegateTarget = this,
            !l.preDispatch || !1 !== l.preDispatch.call(this, t)) {
                for (r = x.event.handlers.call(this, t, a),
                e = 0; (i = r[e++]) && !t.isPropagationStopped(); )
                    for (t.currentTarget = i.elem,
                    n = 0; (s = i.handlers[n++]) && !t.isImmediatePropagationStopped(); )
                        t.rnamespace && !t.rnamespace.test(s.namespace) || (t.handleObj = s,
                        t.data = s.data,
                        void 0 !== (s = ((x.event.special[s.origType] || {}).handle || s.handler).apply(i.elem, o)) && !1 === (t.result = s) && (t.preventDefault(),
                        t.stopPropagation()));
                return l.postDispatch && l.postDispatch.call(this, t),
                t.result
            }
        },
        handlers: function(t, e) {
            var n, i, s, r, o = [], a = e.delegateCount, l = t.target;
            if (a && l.nodeType && ("click" !== t.type || isNaN(t.button) || t.button < 1))
                for (; l != this; l = l.parentNode || this)
                    if (1 === l.nodeType && (!0 !== l.disabled || "click" !== t.type)) {
                        for (i = [],
                        n = 0; n < a; n++)
                            void 0 === i[s = (r = e[n]).selector + " "] && (i[s] = r.needsContext ? -1 < x(s, this).index(l) : x.find(s, this, null, [l]).length),
                            i[s] && i.push(r);
                        i.length && o.push({
                            elem: l,
                            handlers: i
                        })
                    }
            return a < e.length && o.push({
                elem: this,
                handlers: e.slice(a)
            }),
            o
        },
        fix: function(t) {
            if (t[x.expando])
                return t;
            var e, n, i, s = t.type, r = t, o = this.fixHooks[s];
            for (o || (this.fixHooks[s] = o = At.test(s) ? this.mouseHooks : $t.test(s) ? this.keyHooks : {}),
            i = o.props ? this.props.concat(o.props) : this.props,
            t = new x.Event(r),
            e = i.length; e--; )
                t[n = i[e]] = r[n];
            return t.target || (t.target = r.srcElement || f),
            3 === t.target.nodeType && (t.target = t.target.parentNode),
            t.metaKey = !!t.metaKey,
            o.filter ? o.filter(t, r) : t
        },
        props: "altKey bubbles cancelable ctrlKey currentTarget detail eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),
        fixHooks: {},
        keyHooks: {
            props: "char charCode key keyCode".split(" "),
            filter: function(t, e) {
                return null == t.which && (t.which = null != e.charCode ? e.charCode : e.keyCode),
                t
            }
        },
        mouseHooks: {
            props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),
            filter: function(t, e) {
                var n, i, s = e.button, r = e.fromElement;
                return null == t.pageX && null != e.clientX && (i = (n = t.target.ownerDocument || f).documentElement,
                n = n.body,
                t.pageX = e.clientX + (i && i.scrollLeft || n && n.scrollLeft || 0) - (i && i.clientLeft || n && n.clientLeft || 0),
                t.pageY = e.clientY + (i && i.scrollTop || n && n.scrollTop || 0) - (i && i.clientTop || n && n.clientTop || 0)),
                !t.relatedTarget && r && (t.relatedTarget = r === t.target ? e.toElement : r),
                t.which || void 0 === s || (t.which = 1 & s ? 1 : 2 & s ? 3 : 4 & s ? 2 : 0),
                t
            }
        },
        special: {
            load: {
                noBubble: !0
            },
            focus: {
                trigger: function() {
                    if (this !== Pt() && this.focus)
                        try {
                            return this.focus(),
                            !1
                        } catch (t) {}
                },
                delegateType: "focusin"
            },
            blur: {
                trigger: function() {
                    if (this === Pt() && this.blur)
                        return this.blur(),
                        !1
                },
                delegateType: "focusout"
            },
            click: {
                trigger: function() {
                    if (x.nodeName(this, "input") && "checkbox" === this.type && this.click)
                        return this.click(),
                        !1
                },
                _default: function(t) {
                    return x.nodeName(t.target, "a")
                }
            },
            beforeunload: {
                postDispatch: function(t) {
                    void 0 !== t.result && t.originalEvent && (t.originalEvent.returnValue = t.result)
                }
            }
        },
        simulate: function(t, e, n) {
            t = x.extend(new x.Event, n, {
                type: t,
                isSimulated: !0
            });
            x.event.trigger(t, null, e),
            t.isDefaultPrevented() && n.preventDefault()
        }
    },
    x.removeEvent = f.removeEventListener ? function(t, e, n) {
        t.removeEventListener && t.removeEventListener(e, n)
    }
    : function(t, e, n) {
        e = "on" + e;
        t.detachEvent && (void 0 === t[e] && (t[e] = null),
        t.detachEvent(e, n))
    }
    ,
    x.Event = function(t, e) {
        if (!(this instanceof x.Event))
            return new x.Event(t,e);
        t && t.type ? (this.originalEvent = t,
        this.type = t.type,
        this.isDefaultPrevented = t.defaultPrevented || void 0 === t.defaultPrevented && !1 === t.returnValue ? Ot : l) : this.type = t,
        e && x.extend(this, e),
        this.timeStamp = t && t.timeStamp || x.now(),
        this[x.expando] = !0
    }
    ,
    x.Event.prototype = {
        constructor: x.Event,
        isDefaultPrevented: l,
        isPropagationStopped: l,
        isImmediatePropagationStopped: l,
        preventDefault: function() {
            var t = this.originalEvent;
            this.isDefaultPrevented = Ot,
            t && (t.preventDefault ? t.preventDefault() : t.returnValue = !1)
        },
        stopPropagation: function() {
            var t = this.originalEvent;
            this.isPropagationStopped = Ot,
            t && !this.isSimulated && (t.stopPropagation && t.stopPropagation(),
            t.cancelBubble = !0)
        },
        stopImmediatePropagation: function() {
            var t = this.originalEvent;
            this.isImmediatePropagationStopped = Ot,
            t && t.stopImmediatePropagation && t.stopImmediatePropagation(),
            this.stopPropagation()
        }
    },
    x.each({
        mouseenter: "mouseover",
        mouseleave: "mouseout",
        pointerenter: "pointerover",
        pointerleave: "pointerout"
    }, function(t, s) {
        x.event.special[t] = {
            delegateType: s,
            bindType: s,
            handle: function(t) {
                var e, n = t.relatedTarget, i = t.handleObj;
                return n && (n === this || x.contains(this, n)) || (t.type = i.origType,
                e = i.handler.apply(this, arguments),
                t.type = s),
                e
            }
        }
    }),
    m.submit || (x.event.special.submit = {
        setup: function() {
            if (x.nodeName(this, "form"))
                return !1;
            x.event.add(this, "click._submit keypress._submit", function(t) {
                t = t.target,
                t = x.nodeName(t, "input") || x.nodeName(t, "button") ? x.prop(t, "form") : void 0;
                t && !x._data(t, "submit") && (x.event.add(t, "submit._submit", function(t) {
                    t._submitBubble = !0
                }),
                x._data(t, "submit", !0))
            })
        },
        postDispatch: function(t) {
            t._submitBubble && (delete t._submitBubble,
            this.parentNode && !t.isTrigger && x.event.simulate("submit", this.parentNode, t))
        },
        teardown: function() {
            if (x.nodeName(this, "form"))
                return !1;
            x.event.remove(this, "._submit")
        }
    }),
    m.change || (x.event.special.change = {
        setup: function() {
            if (Nt.test(this.nodeName))
                return "checkbox" !== this.type && "radio" !== this.type || (x.event.add(this, "propertychange._change", function(t) {
                    "checked" === t.originalEvent.propertyName && (this._justChanged = !0)
                }),
                x.event.add(this, "click._change", function(t) {
                    this._justChanged && !t.isTrigger && (this._justChanged = !1),
                    x.event.simulate("change", this, t)
                })),
                !1;
            x.event.add(this, "beforeactivate._change", function(t) {
                t = t.target;
                Nt.test(t.nodeName) && !x._data(t, "change") && (x.event.add(t, "change._change", function(t) {
                    !this.parentNode || t.isSimulated || t.isTrigger || x.event.simulate("change", this.parentNode, t)
                }),
                x._data(t, "change", !0))
            })
        },
        handle: function(t) {
            var e = t.target;
            if (this !== e || t.isSimulated || t.isTrigger || "radio" !== e.type && "checkbox" !== e.type)
                return t.handleObj.handler.apply(this, arguments)
        },
        teardown: function() {
            return x.event.remove(this, "._change"),
            !Nt.test(this.nodeName)
        }
    }),
    m.focusin || x.each({
        focus: "focusin",
        blur: "focusout"
    }, function(n, i) {
        function s(t) {
            x.event.simulate(i, t.target, x.event.fix(t))
        }
        x.event.special[i] = {
            setup: function() {
                var t = this.ownerDocument || this
                  , e = x._data(t, i);
                e || t.addEventListener(n, s, !0),
                x._data(t, i, (e || 0) + 1)
            },
            teardown: function() {
                var t = this.ownerDocument || this
                  , e = x._data(t, i) - 1;
                e ? x._data(t, i, e) : (t.removeEventListener(n, s, !0),
                x._removeData(t, i))
            }
        }
    }),
    x.fn.extend({
        on: function(t, e, n, i) {
            return Rt(this, t, e, n, i)
        },
        one: function(t, e, n, i) {
            return Rt(this, t, e, n, i, 1)
        },
        off: function(t, e, n) {
            var i, s;
            if (t && t.preventDefault && t.handleObj)
                return i = t.handleObj,
                x(t.delegateTarget).off(i.namespace ? i.origType + "." + i.namespace : i.origType, i.selector, i.handler),
                this;
            if ("object" != typeof t)
                return !1 !== e && "function" != typeof e || (n = e,
                e = void 0),
                !1 === n && (n = l),
                this.each(function() {
                    x.event.remove(this, t, n, e)
                });
            for (s in t)
                this.off(s, e, t[s]);
            return this
        },
        trigger: function(t, e) {
            return this.each(function() {
                x.event.trigger(t, e, this)
            })
        },
        triggerHandler: function(t, e) {
            var n = this[0];
            if (n)
                return x.event.trigger(t, e, n, !0)
        }
    });
    var Lt = / jQuery\d+="(?:null|\d+)"/g
      , jt = new RegExp("<(?:" + bt + ")[\\s/>]","i")
      , Ht = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:-]+)[^>]*)\/>/gi
      , Ft = /<script|<style|<link/i
      , Wt = /checked\s*(?:[^=]|=\s*.checked.)/i
      , Yt = /^true\/(.*)/
      , qt = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g
      , zt = _t(f).appendChild(f.createElement("div"));
    function Bt(t, e) {
        return x.nodeName(t, "table") && x.nodeName(11 !== e.nodeType ? e : e.firstChild, "tr") ? t.getElementsByTagName("tbody")[0] || t.appendChild(t.ownerDocument.createElement("tbody")) : t
    }
    function Vt(t) {
        return t.type = (null !== x.find.attr(t, "type")) + "/" + t.type,
        t
    }
    function Ut(t) {
        var e = Yt.exec(t.type);
        return e ? t.type = e[1] : t.removeAttribute("type"),
        t
    }
    function Gt(t, e) {
        if (1 === e.nodeType && x.hasData(t)) {
            var n, i, s, t = x._data(t), r = x._data(e, t), o = t.events;
            if (o)
                for (n in delete r.handle,
                r.events = {},
                o)
                    for (i = 0,
                    s = o[n].length; i < s; i++)
                        x.event.add(e, n, o[n][i]);
            r.data && (r.data = x.extend({}, r.data))
        }
    }
    function _(n, i, s, r) {
        i = j.apply([], i);
        var t, e, o, a, l, c, u = 0, d = n.length, h = d - 1, p = i[0], f = x.isFunction(p);
        if (f || 1 < d && "string" == typeof p && !m.checkClone && Wt.test(p))
            return n.each(function(t) {
                var e = n.eq(t);
                f && (i[0] = p.call(this, t, e.html())),
                _(e, i, s, r)
            });
        if (d && (t = (c = St(i, n[0].ownerDocument, !1, n, r)).firstChild,
        1 === c.childNodes.length && (c = t),
        t || r)) {
            for (o = (a = x.map(b(c, "script"), Vt)).length; u < d; u++)
                e = c,
                u !== h && (e = x.clone(e, !0, !0),
                o && x.merge(a, b(e, "script"))),
                s.call(n[u], e, u);
            if (o)
                for (l = a[a.length - 1].ownerDocument,
                x.map(a, Ut),
                u = 0; u < o; u++)
                    e = a[u],
                    yt.test(e.type || "") && !x._data(e, "globalEval") && x.contains(l, e) && (e.src ? x._evalUrl && x._evalUrl(e.src) : x.globalEval((e.text || e.textContent || e.innerHTML || "").replace(qt, "")));
            c = t = null
        }
        return n
    }
    function Qt(t, e, n) {
        for (var i, s = e ? x.filter(e, t) : t, r = 0; null != (i = s[r]); r++)
            n || 1 !== i.nodeType || x.cleanData(b(i)),
            i.parentNode && (n && x.contains(i.ownerDocument, i) && wt(b(i, "script")),
            i.parentNode.removeChild(i));
        return t
    }
    x.extend({
        htmlPrefilter: function(t) {
            return t.replace(Ht, "<$1></$2>")
        },
        clone: function(t, e, n) {
            var i, s, r, o, a, l = x.contains(t.ownerDocument, t);
            if (m.html5Clone || x.isXMLDoc(t) || !jt.test("<" + t.nodeName + ">") ? r = t.cloneNode(!0) : (zt.innerHTML = t.outerHTML,
            zt.removeChild(r = zt.firstChild)),
            !(m.noCloneEvent && m.noCloneChecked || 1 !== t.nodeType && 11 !== t.nodeType || x.isXMLDoc(t)))
                for (i = b(r),
                a = b(t),
                o = 0; null != (s = a[o]); ++o)
                    if (i[o]) {
                        d = u = c = p = h = void 0;
                        var c, u, d, h = s, p = i[o];
                        if (1 === p.nodeType) {
                            if (c = p.nodeName.toLowerCase(),
                            !m.noCloneEvent && p[x.expando]) {
                                for (u in (d = x._data(p)).events)
                                    x.removeEvent(p, u, d.handle);
                                p.removeAttribute(x.expando)
                            }
                            "script" === c && p.text !== h.text ? (Vt(p).text = h.text,
                            Ut(p)) : "object" === c ? (p.parentNode && (p.outerHTML = h.outerHTML),
                            m.html5Clone && h.innerHTML && !x.trim(p.innerHTML) && (p.innerHTML = h.innerHTML)) : "input" === c && gt.test(h.type) ? (p.defaultChecked = p.checked = h.checked,
                            p.value !== h.value && (p.value = h.value)) : "option" === c ? p.defaultSelected = p.selected = h.defaultSelected : "input" !== c && "textarea" !== c || (p.defaultValue = h.defaultValue)
                        }
                    }
            if (e)
                if (n)
                    for (a = a || b(t),
                    i = i || b(r),
                    o = 0; null != (s = a[o]); o++)
                        Gt(s, i[o]);
                else
                    Gt(t, r);
            return 0 < (i = b(r, "script")).length && wt(i, !l && b(t, "script")),
            i = a = s = null,
            r
        },
        cleanData: function(t, e) {
            for (var n, i, s, r, o = 0, a = x.expando, l = x.cache, c = m.attributes, u = x.event.special; null != (n = t[o]); o++)
                if ((e || y(n)) && (r = (s = n[a]) && l[s])) {
                    if (r.events)
                        for (i in r.events)
                            u[i] ? x.event.remove(n, i) : x.removeEvent(n, i, r.handle);
                    l[s] && (delete l[s],
                    c || void 0 === n.removeAttribute ? n[a] = void 0 : n.removeAttribute(a),
                    d.push(s))
                }
        }
    }),
    x.fn.extend({
        domManip: _,
        detach: function(t) {
            return Qt(this, t, !0)
        },
        remove: function(t) {
            return Qt(this, t)
        },
        text: function(t) {
            return h(this, function(t) {
                return void 0 === t ? x.text(this) : this.empty().append((this[0] && this[0].ownerDocument || f).createTextNode(t))
            }, null, t, arguments.length)
        },
        append: function() {
            return _(this, arguments, function(t) {
                1 !== this.nodeType && 11 !== this.nodeType && 9 !== this.nodeType || Bt(this, t).appendChild(t)
            })
        },
        prepend: function() {
            return _(this, arguments, function(t) {
                var e;
                1 !== this.nodeType && 11 !== this.nodeType && 9 !== this.nodeType || (e = Bt(this, t)).insertBefore(t, e.firstChild)
            })
        },
        before: function() {
            return _(this, arguments, function(t) {
                this.parentNode && this.parentNode.insertBefore(t, this)
            })
        },
        after: function() {
            return _(this, arguments, function(t) {
                this.parentNode && this.parentNode.insertBefore(t, this.nextSibling)
            })
        },
        empty: function() {
            for (var t, e = 0; null != (t = this[e]); e++) {
                for (1 === t.nodeType && x.cleanData(b(t, !1)); t.firstChild; )
                    t.removeChild(t.firstChild);
                t.options && x.nodeName(t, "select") && (t.options.length = 0)
            }
            return this
        },
        clone: function(t, e) {
            return t = null != t && t,
            e = null == e ? t : e,
            this.map(function() {
                return x.clone(this, t, e)
            })
        },
        html: function(t) {
            return h(this, function(t) {
                var e = this[0] || {}
                  , n = 0
                  , i = this.length;
                if (void 0 === t)
                    return 1 === e.nodeType ? e.innerHTML.replace(Lt, "") : void 0;
                if ("string" == typeof t && !Ft.test(t) && (m.htmlSerialize || !jt.test(t)) && (m.leadingWhitespace || !vt.test(t)) && !v[(mt.exec(t) || ["", ""])[1].toLowerCase()]) {
                    t = x.htmlPrefilter(t);
                    try {
                        for (; n < i; n++)
                            1 === (e = this[n] || {}).nodeType && (x.cleanData(b(e, !1)),
                            e.innerHTML = t);
                        e = 0
                    } catch (t) {}
                }
                e && this.empty().append(t)
            }, null, t, arguments.length)
        },
        replaceWith: function() {
            var n = [];
            return _(this, arguments, function(t) {
                var e = this.parentNode;
                x.inArray(this, n) < 0 && (x.cleanData(b(this)),
                e && e.replaceChild(t, this))
            }, n)
        }
    }),
    x.each({
        appendTo: "append",
        prependTo: "prepend",
        insertBefore: "before",
        insertAfter: "after",
        replaceAll: "replaceWith"
    }, function(t, o) {
        x.fn[t] = function(t) {
            for (var e, n = 0, i = [], s = x(t), r = s.length - 1; n <= r; n++)
                e = n === r ? this : this.clone(!0),
                x(s[n])[o](e),
                H.apply(i, e.get());
            return this.pushStack(i)
        }
    });
    var Kt, Xt = {
        HTML: "block",
        BODY: "block"
    };
    function Zt(t, e) {
        t = x(e.createElement(t)).appendTo(e.body),
        e = x.css(t[0], "display");
        return t.detach(),
        e
    }
    function Jt(t) {
        var e = f
          , n = Xt[t];
        return n || ("none" !== (n = Zt(t, e)) && n || ((e = ((Kt = (Kt || x("<iframe frameborder='0' width='0' height='0'/>")).appendTo(e.documentElement))[0].contentWindow || Kt[0].contentDocument).document).write(),
        e.close(),
        n = Zt(t, e),
        Kt.detach()),
        Xt[t] = n),
        n
    }
    function te(t, e, n, i) {
        var s, r = {};
        for (s in e)
            r[s] = t.style[s],
            t.style[s] = e[s];
        for (s in n = n.apply(t, i || []),
        e)
            t.style[s] = r[s];
        return n
    }
    var n, ee, ne, ie, se, re, oe, r, ae = /^margin/, le = new RegExp("^(" + t + ")(?!px)[a-z%]+$","i"), ce = f.documentElement;
    function o() {
        var t, e = f.documentElement;
        e.appendChild(oe),
        r.style.cssText = "-webkit-box-sizing:border-box;box-sizing:border-box;position:relative;display:block;margin:auto;border:1px;padding:1px;top:1%;width:50%",
        n = ne = re = !1,
        ee = se = !0,
        w.getComputedStyle && (t = w.getComputedStyle(r),
        n = "1%" !== (t || {}).top,
        re = "2px" === (t || {}).marginLeft,
        ne = "4px" === (t || {
            width: "4px"
        }).width,
        r.style.marginRight = "50%",
        ee = "4px" === (t || {
            marginRight: "4px"
        }).marginRight,
        (t = r.appendChild(f.createElement("div"))).style.cssText = r.style.cssText = "-webkit-box-sizing:content-box;-moz-box-sizing:content-box;box-sizing:content-box;display:block;margin:0;border:0;padding:0",
        t.style.marginRight = t.style.width = "0",
        r.style.width = "1px",
        se = !parseFloat((w.getComputedStyle(t) || {}).marginRight),
        r.removeChild(t)),
        r.style.display = "none",
        (ie = 0 === r.getClientRects().length) && (r.style.display = "",
        r.innerHTML = "<table><tr><td></td><td>t</td></tr></table>",
        r.childNodes[0].style.borderCollapse = "separate",
        (t = r.getElementsByTagName("td"))[0].style.cssText = "margin:0;border:0;padding:0;display:none",
        (ie = 0 === t[0].offsetHeight) && (t[0].style.display = "",
        t[1].style.display = "none",
        ie = 0 === t[0].offsetHeight)),
        e.removeChild(oe)
    }
    oe = f.createElement("div"),
    (r = f.createElement("div")).style && (r.style.cssText = "float:left;opacity:.5",
    m.opacity = "0.5" === r.style.opacity,
    m.cssFloat = !!r.style.cssFloat,
    r.style.backgroundClip = "content-box",
    r.cloneNode(!0).style.backgroundClip = "",
    m.clearCloneStyle = "content-box" === r.style.backgroundClip,
    (oe = f.createElement("div")).style.cssText = "border:0;width:8px;height:0;top:0;left:-9999px;padding:0;margin-top:1px;position:absolute",
    r.innerHTML = "",
    oe.appendChild(r),
    m.boxSizing = "" === r.style.boxSizing || "" === r.style.MozBoxSizing || "" === r.style.WebkitBoxSizing,
    x.extend(m, {
        reliableHiddenOffsets: function() {
            return null == n && o(),
            ie
        },
        boxSizingReliable: function() {
            return null == n && o(),
            ne
        },
        pixelMarginRight: function() {
            return null == n && o(),
            ee
        },
        pixelPosition: function() {
            return null == n && o(),
            n
        },
        reliableMarginRight: function() {
            return null == n && o(),
            se
        },
        reliableMarginLeft: function() {
            return null == n && o(),
            re
        }
    }));
    var c, p, ue = /^(top|right|bottom|left)$/;
    function de(t, e) {
        return {
            get: function() {
                if (!t())
                    return (this.get = e).apply(this, arguments);
                delete this.get
            }
        }
    }
    w.getComputedStyle ? (c = function(t) {
        var e = t.ownerDocument.defaultView;
        return (e = e && e.opener ? e : w).getComputedStyle(t)
    }
    ,
    p = function(t, e, n) {
        var i, s, r = t.style;
        return "" !== (s = (n = n || c(t)) ? n.getPropertyValue(e) || n[e] : void 0) && void 0 !== s || x.contains(t.ownerDocument, t) || (s = x.style(t, e)),
        n && !m.pixelMarginRight() && le.test(s) && ae.test(e) && (t = r.width,
        e = r.minWidth,
        i = r.maxWidth,
        r.minWidth = r.maxWidth = r.width = s,
        s = n.width,
        r.width = t,
        r.minWidth = e,
        r.maxWidth = i),
        void 0 === s ? s : s + ""
    }
    ) : ce.currentStyle && (c = function(t) {
        return t.currentStyle
    }
    ,
    p = function(t, e, n) {
        var i, s, r, o = t.style;
        return null == (n = (n = n || c(t)) ? n[e] : void 0) && o && o[e] && (n = o[e]),
        le.test(n) && !ue.test(e) && (i = o.left,
        (r = (s = t.runtimeStyle) && s.left) && (s.left = t.currentStyle.left),
        o.left = "fontSize" === e ? "1em" : n,
        n = o.pixelLeft + "px",
        o.left = i,
        r && (s.left = r)),
        void 0 === n ? n : n + "" || "auto"
    }
    );
    var he = /alpha\([^)]*\)/i
      , pe = /opacity\s*=\s*([^)]*)/i
      , fe = /^(none|table(?!-c[ea]).+)/
      , ge = new RegExp("^(" + t + ")(.*)$","i")
      , me = {
        position: "absolute",
        visibility: "hidden",
        display: "block"
    }
      , ye = {
        letterSpacing: "0",
        fontWeight: "400"
    }
      , ve = ["Webkit", "O", "Moz", "ms"]
      , be = f.createElement("div").style;
    function _e(t) {
        if (t in be)
            return t;
        for (var e = t.charAt(0).toUpperCase() + t.slice(1), n = ve.length; n--; )
            if ((t = ve[n] + e)in be)
                return t
    }
    function we(t, e) {
        for (var n, i, s, r = [], o = 0, a = t.length; o < a; o++)
            (i = t[o]).style && (r[o] = x._data(i, "olddisplay"),
            n = i.style.display,
            e ? (r[o] || "none" !== n || (i.style.display = ""),
            "" === i.style.display && ht(i) && (r[o] = x._data(i, "olddisplay", Jt(i.nodeName)))) : (s = ht(i),
            (n && "none" !== n || !s) && x._data(i, "olddisplay", s ? n : x.css(i, "display"))));
        for (o = 0; o < a; o++)
            !(i = t[o]).style || e && "none" !== i.style.display && "" !== i.style.display || (i.style.display = e ? r[o] || "" : "none");
        return t
    }
    function xe(t, e, n) {
        var i = ge.exec(e);
        return i ? Math.max(0, i[1] - (n || 0)) + (i[2] || "px") : e
    }
    function Ce(t, e, n, i, s) {
        for (var r = n === (i ? "border" : "content") ? 4 : "width" === e ? 1 : 0, o = 0; r < 4; r += 2)
            "margin" === n && (o += x.css(t, n + a[r], !0, s)),
            i ? ("content" === n && (o -= x.css(t, "padding" + a[r], !0, s)),
            "margin" !== n && (o -= x.css(t, "border" + a[r] + "Width", !0, s))) : (o += x.css(t, "padding" + a[r], !0, s),
            "padding" !== n && (o += x.css(t, "border" + a[r] + "Width", !0, s)));
        return o
    }
    function ke(t, e, n) {
        var i = !0
          , s = "width" === e ? t.offsetWidth : t.offsetHeight
          , r = c(t)
          , o = m.boxSizing && "border-box" === x.css(t, "boxSizing", !1, r);
        if (s <= 0 || null == s) {
            if (((s = p(t, e, r)) < 0 || null == s) && (s = t.style[e]),
            le.test(s))
                return s;
            i = o && (m.boxSizingReliable() || s === t.style[e]),
            s = parseFloat(s) || 0
        }
        return s + Ce(t, e, n || (o ? "border" : "content"), i, r) + "px"
    }
    function k(t, e, n, i, s) {
        return new k.prototype.init(t,e,n,i,s)
    }
    x.extend({
        cssHooks: {
            opacity: {
                get: function(t, e) {
                    if (e)
                        return "" === (e = p(t, "opacity")) ? "1" : e
                }
            }
        },
        cssNumber: {
            animationIterationCount: !0,
            columnCount: !0,
            fillOpacity: !0,
            flexGrow: !0,
            flexShrink: !0,
            fontWeight: !0,
            lineHeight: !0,
            opacity: !0,
            order: !0,
            orphans: !0,
            widows: !0,
            zIndex: !0,
            zoom: !0
        },
        cssProps: {
            float: m.cssFloat ? "cssFloat" : "styleFloat"
        },
        style: function(t, e, n, i) {
            if (t && 3 !== t.nodeType && 8 !== t.nodeType && t.style) {
                var s, r, o, a = x.camelCase(e), l = t.style;
                if (e = x.cssProps[a] || (x.cssProps[a] = _e(a) || a),
                o = x.cssHooks[e] || x.cssHooks[a],
                void 0 === n)
                    return o && "get"in o && void 0 !== (s = o.get(t, !1, i)) ? s : l[e];
                if ("string" === (r = typeof n) && (s = pt.exec(n)) && s[1] && (n = ft(t, e, s),
                r = "number"),
                null != n && n == n && ("number" === r && (n += s && s[3] || (x.cssNumber[a] ? "" : "px")),
                m.clearCloneStyle || "" !== n || 0 !== e.indexOf("background") || (l[e] = "inherit"),
                !(o && "set"in o && void 0 === (n = o.set(t, n, i)))))
                    try {
                        l[e] = n
                    } catch (t) {}
            }
        },
        css: function(t, e, n, i) {
            var s, r = x.camelCase(e);
            return e = x.cssProps[r] || (x.cssProps[r] = _e(r) || r),
            "normal" === (s = void 0 === (s = (r = x.cssHooks[e] || x.cssHooks[r]) && "get"in r ? r.get(t, !0, n) : s) ? p(t, e, i) : s) && e in ye && (s = ye[e]),
            "" === n || n ? (r = parseFloat(s),
            !0 === n || isFinite(r) ? r || 0 : s) : s
        }
    }),
    x.each(["height", "width"], function(t, s) {
        x.cssHooks[s] = {
            get: function(t, e, n) {
                if (e)
                    return fe.test(x.css(t, "display")) && 0 === t.offsetWidth ? te(t, me, function() {
                        return ke(t, s, n)
                    }) : ke(t, s, n)
            },
            set: function(t, e, n) {
                var i = n && c(t);
                return xe(0, e, n ? Ce(t, s, n, m.boxSizing && "border-box" === x.css(t, "boxSizing", !1, i), i) : 0)
            }
        }
    }),
    m.opacity || (x.cssHooks.opacity = {
        get: function(t, e) {
            return pe.test((e && t.currentStyle ? t.currentStyle : t.style).filter || "") ? .01 * parseFloat(RegExp.$1) + "" : e ? "1" : ""
        },
        set: function(t, e) {
            var n = t.style
              , t = t.currentStyle
              , i = x.isNumeric(e) ? "alpha(opacity=" + 100 * e + ")" : ""
              , s = t && t.filter || n.filter || "";
            ((n.zoom = 1) <= e || "" === e) && "" === x.trim(s.replace(he, "")) && n.removeAttribute && (n.removeAttribute("filter"),
            "" === e || t && !t.filter) || (n.filter = he.test(s) ? s.replace(he, i) : s + " " + i)
        }
    }),
    x.cssHooks.marginRight = de(m.reliableMarginRight, function(t, e) {
        if (e)
            return te(t, {
                display: "inline-block"
            }, p, [t, "marginRight"])
    }),
    x.cssHooks.marginLeft = de(m.reliableMarginLeft, function(t, e) {
        if (e)
            return (parseFloat(p(t, "marginLeft")) || (x.contains(t.ownerDocument, t) ? t.getBoundingClientRect().left - te(t, {
                marginLeft: 0
            }, function() {
                return t.getBoundingClientRect().left
            }) : 0)) + "px"
    }),
    x.each({
        margin: "",
        padding: "",
        border: "Width"
    }, function(s, r) {
        x.cssHooks[s + r] = {
            expand: function(t) {
                for (var e = 0, n = {}, i = "string" == typeof t ? t.split(" ") : [t]; e < 4; e++)
                    n[s + a[e] + r] = i[e] || i[e - 2] || i[0];
                return n
            }
        },
        ae.test(s) || (x.cssHooks[s + r].set = xe)
    }),
    x.fn.extend({
        css: function(t, e) {
            return h(this, function(t, e, n) {
                var i, s, r = {}, o = 0;
                if (x.isArray(e)) {
                    for (i = c(t),
                    s = e.length; o < s; o++)
                        r[e[o]] = x.css(t, e[o], !1, i);
                    return r
                }
                return void 0 !== n ? x.style(t, e, n) : x.css(t, e)
            }, t, e, 1 < arguments.length)
        },
        show: function() {
            return we(this, !0)
        },
        hide: function() {
            return we(this)
        },
        toggle: function(t) {
            return "boolean" == typeof t ? t ? this.show() : this.hide() : this.each(function() {
                ht(this) ? x(this).show() : x(this).hide()
            })
        }
    }),
    ((x.Tween = k).prototype = {
        constructor: k,
        init: function(t, e, n, i, s, r) {
            this.elem = t,
            this.prop = n,
            this.easing = s || x.easing._default,
            this.options = e,
            this.start = this.now = this.cur(),
            this.end = i,
            this.unit = r || (x.cssNumber[n] ? "" : "px")
        },
        cur: function() {
            var t = k.propHooks[this.prop];
            return (t && t.get ? t : k.propHooks._default).get(this)
        },
        run: function(t) {
            var e, n = k.propHooks[this.prop];
            return this.options.duration ? this.pos = e = x.easing[this.easing](t, this.options.duration * t, 0, 1, this.options.duration) : this.pos = e = t,
            this.now = (this.end - this.start) * e + this.start,
            this.options.step && this.options.step.call(this.elem, this.now, this),
            (n && n.set ? n : k.propHooks._default).set(this),
            this
        }
    }).init.prototype = k.prototype,
    (k.propHooks = {
        _default: {
            get: function(t) {
                return 1 !== t.elem.nodeType || null != t.elem[t.prop] && null == t.elem.style[t.prop] ? t.elem[t.prop] : (t = x.css(t.elem, t.prop, "")) && "auto" !== t ? t : 0
            },
            set: function(t) {
                x.fx.step[t.prop] ? x.fx.step[t.prop](t) : 1 !== t.elem.nodeType || null == t.elem.style[x.cssProps[t.prop]] && !x.cssHooks[t.prop] ? t.elem[t.prop] = t.now : x.style(t.elem, t.prop, t.now + t.unit)
            }
        }
    }).scrollTop = k.propHooks.scrollLeft = {
        set: function(t) {
            t.elem.nodeType && t.elem.parentNode && (t.elem[t.prop] = t.now)
        }
    },
    x.easing = {
        linear: function(t) {
            return t
        },
        swing: function(t) {
            return .5 - Math.cos(t * Math.PI) / 2
        },
        _default: "swing"
    },
    x.fx = k.prototype.init,
    x.fx.step = {};
    var S, Se, T, D, Te = /^(?:toggle|show|hide)$/, De = /queueHooks$/;
    function Ee() {
        return w.setTimeout(function() {
            S = void 0
        }),
        S = x.now()
    }
    function Ne(t, e) {
        var n, i = {
            height: t
        }, s = 0;
        for (e = e ? 1 : 0; s < 4; s += 2 - e)
            i["margin" + (n = a[s])] = i["padding" + n] = t;
        return e && (i.opacity = i.width = t),
        i
    }
    function $e(t, e, n) {
        for (var i, s = (E.tweeners[e] || []).concat(E.tweeners["*"]), r = 0, o = s.length; r < o; r++)
            if (i = s[r].call(n, e, t))
                return i
    }
    function E(s, t, e) {
        var n, r, i, o, a, l, c, u = 0, d = E.prefilters.length, h = x.Deferred().always(function() {
            delete p.elem
        }), p = function() {
            if (r)
                return !1;
            for (var t = S || Ee(), t = Math.max(0, f.startTime + f.duration - t), e = 1 - (t / f.duration || 0), n = 0, i = f.tweens.length; n < i; n++)
                f.tweens[n].run(e);
            return h.notifyWith(s, [f, e, t]),
            e < 1 && i ? t : (h.resolveWith(s, [f]),
            !1)
        }, f = h.promise({
            elem: s,
            props: x.extend({}, t),
            opts: x.extend(!0, {
                specialEasing: {},
                easing: x.easing._default
            }, e),
            originalProperties: t,
            originalOptions: e,
            startTime: S || Ee(),
            duration: e.duration,
            tweens: [],
            createTween: function(t, e) {
                e = x.Tween(s, f.opts, t, e, f.opts.specialEasing[t] || f.opts.easing);
                return f.tweens.push(e),
                e
            },
            stop: function(t) {
                var e = 0
                  , n = t ? f.tweens.length : 0;
                if (r)
                    return this;
                for (r = !0; e < n; e++)
                    f.tweens[e].run(1);
                return t ? (h.notifyWith(s, [f, 1, 0]),
                h.resolveWith(s, [f, t])) : h.rejectWith(s, [f, t]),
                this
            }
        }), g = f.props, m = g, y = f.opts.specialEasing;
        for (i in m)
            if (a = y[o = x.camelCase(i)],
            l = m[i],
            x.isArray(l) && (a = l[1],
            l = m[i] = l[0]),
            i !== o && (m[o] = l,
            delete m[i]),
            (c = x.cssHooks[o]) && "expand"in c)
                for (i in l = c.expand(l),
                delete m[o],
                l)
                    i in m || (m[i] = l[i],
                    y[i] = a);
            else
                y[o] = a;
        for (; u < d; u++)
            if (n = E.prefilters[u].call(f, s, g, f.opts))
                return x.isFunction(n.stop) && (x._queueHooks(f.elem, f.opts.queue).stop = x.proxy(n.stop, n)),
                n;
        return x.map(g, $e, f),
        x.isFunction(f.opts.start) && f.opts.start.call(s, f),
        x.fx.timer(x.extend(p, {
            elem: s,
            anim: f,
            queue: f.opts.queue
        })),
        f.progress(f.opts.progress).done(f.opts.done, f.opts.complete).fail(f.opts.fail).always(f.opts.always)
    }
    x.Animation = x.extend(E, {
        tweeners: {
            "*": [function(t, e) {
                var n = this.createTween(t, e);
                return ft(n.elem, t, pt.exec(e), n),
                n
            }
            ]
        },
        tweener: function(t, e) {
            for (var n, i = 0, s = (t = x.isFunction(t) ? (e = t,
            ["*"]) : t.match(C)).length; i < s; i++)
                n = t[i],
                E.tweeners[n] = E.tweeners[n] || [],
                E.tweeners[n].unshift(e)
        },
        prefilters: [function(e, t, n) {
            var i, s, r, o, a, l, c, u = this, d = {}, h = e.style, p = e.nodeType && ht(e), f = x._data(e, "fxshow");
            for (i in n.queue || (null == (a = x._queueHooks(e, "fx")).unqueued && (a.unqueued = 0,
            l = a.empty.fire,
            a.empty.fire = function() {
                a.unqueued || l()
            }
            ),
            a.unqueued++,
            u.always(function() {
                u.always(function() {
                    a.unqueued--,
                    x.queue(e, "fx").length || a.empty.fire()
                })
            })),
            1 === e.nodeType && ("height"in t || "width"in t) && (n.overflow = [h.overflow, h.overflowX, h.overflowY],
            "inline" === ("none" === (c = x.css(e, "display")) ? x._data(e, "olddisplay") || Jt(e.nodeName) : c) && "none" === x.css(e, "float") && (m.inlineBlockNeedsLayout && "inline" !== Jt(e.nodeName) ? h.zoom = 1 : h.display = "inline-block")),
            n.overflow && (h.overflow = "hidden",
            m.shrinkWrapBlocks() || u.always(function() {
                h.overflow = n.overflow[0],
                h.overflowX = n.overflow[1],
                h.overflowY = n.overflow[2]
            })),
            t)
                if (s = t[i],
                Te.exec(s)) {
                    if (delete t[i],
                    r = r || "toggle" === s,
                    s === (p ? "hide" : "show")) {
                        if ("show" !== s || !f || void 0 === f[i])
                            continue;
                        p = !0
                    }
                    d[i] = f && f[i] || x.style(e, i)
                } else
                    c = void 0;
            if (x.isEmptyObject(d))
                "inline" === ("none" === c ? Jt(e.nodeName) : c) && (h.display = c);
            else
                for (i in f ? "hidden"in f && (p = f.hidden) : f = x._data(e, "fxshow", {}),
                r && (f.hidden = !p),
                p ? x(e).show() : u.done(function() {
                    x(e).hide()
                }),
                u.done(function() {
                    for (var t in x._removeData(e, "fxshow"),
                    d)
                        x.style(e, t, d[t])
                }),
                d)
                    o = $e(p ? f[i] : 0, i, u),
                    i in f || (f[i] = o.start,
                    p && (o.end = o.start,
                    o.start = "width" === i || "height" === i ? 1 : 0))
        }
        ],
        prefilter: function(t, e) {
            e ? E.prefilters.unshift(t) : E.prefilters.push(t)
        }
    }),
    x.speed = function(t, e, n) {
        var i = t && "object" == typeof t ? x.extend({}, t) : {
            complete: n || !n && e || x.isFunction(t) && t,
            duration: t,
            easing: n && e || e && !x.isFunction(e) && e
        };
        return i.duration = x.fx.off ? 0 : "number" == typeof i.duration ? i.duration : i.duration in x.fx.speeds ? x.fx.speeds[i.duration] : x.fx.speeds._default,
        null != i.queue && !0 !== i.queue || (i.queue = "fx"),
        i.old = i.complete,
        i.complete = function() {
            x.isFunction(i.old) && i.old.call(this),
            i.queue && x.dequeue(this, i.queue)
        }
        ,
        i
    }
    ,
    x.fn.extend({
        fadeTo: function(t, e, n, i) {
            return this.filter(ht).css("opacity", 0).show().end().animate({
                opacity: e
            }, t, n, i)
        },
        animate: function(e, t, n, i) {
            function s() {
                var t = E(this, x.extend({}, e), o);
                (r || x._data(this, "finish")) && t.stop(!0)
            }
            var r = x.isEmptyObject(e)
              , o = x.speed(t, n, i);
            return s.finish = s,
            r || !1 === o.queue ? this.each(s) : this.queue(o.queue, s)
        },
        stop: function(s, t, r) {
            function o(t) {
                var e = t.stop;
                delete t.stop,
                e(r)
            }
            return "string" != typeof s && (r = t,
            t = s,
            s = void 0),
            t && !1 !== s && this.queue(s || "fx", []),
            this.each(function() {
                var t = !0
                  , e = null != s && s + "queueHooks"
                  , n = x.timers
                  , i = x._data(this);
                if (e)
                    i[e] && i[e].stop && o(i[e]);
                else
                    for (e in i)
                        i[e] && i[e].stop && De.test(e) && o(i[e]);
                for (e = n.length; e--; )
                    n[e].elem !== this || null != s && n[e].queue !== s || (n[e].anim.stop(r),
                    t = !1,
                    n.splice(e, 1));
                !t && r || x.dequeue(this, s)
            })
        },
        finish: function(o) {
            return !1 !== o && (o = o || "fx"),
            this.each(function() {
                var t, e = x._data(this), n = e[o + "queue"], i = e[o + "queueHooks"], s = x.timers, r = n ? n.length : 0;
                for (e.finish = !0,
                x.queue(this, o, []),
                i && i.stop && i.stop.call(this, !0),
                t = s.length; t--; )
                    s[t].elem === this && s[t].queue === o && (s[t].anim.stop(!0),
                    s.splice(t, 1));
                for (t = 0; t < r; t++)
                    n[t] && n[t].finish && n[t].finish.call(this);
                delete e.finish
            })
        }
    }),
    x.each(["toggle", "show", "hide"], function(t, i) {
        var s = x.fn[i];
        x.fn[i] = function(t, e, n) {
            return null == t || "boolean" == typeof t ? s.apply(this, arguments) : this.animate(Ne(i, !0), t, e, n)
        }
    }),
    x.each({
        slideDown: Ne("show"),
        slideUp: Ne("hide"),
        slideToggle: Ne("toggle"),
        fadeIn: {
            opacity: "show"
        },
        fadeOut: {
            opacity: "hide"
        },
        fadeToggle: {
            opacity: "toggle"
        }
    }, function(t, i) {
        x.fn[t] = function(t, e, n) {
            return this.animate(i, t, e, n)
        }
    }),
    x.timers = [],
    x.fx.tick = function() {
        var t, e = x.timers, n = 0;
        for (S = x.now(); n < e.length; n++)
            (t = e[n])() || e[n] !== t || e.splice(n--, 1);
        e.length || x.fx.stop(),
        S = void 0
    }
    ,
    x.fx.timer = function(t) {
        x.timers.push(t),
        t() ? x.fx.start() : x.timers.pop()
    }
    ,
    x.fx.interval = 13,
    x.fx.start = function() {
        Se = Se || w.setInterval(x.fx.tick, x.fx.interval)
    }
    ,
    x.fx.stop = function() {
        w.clearInterval(Se),
        Se = null
    }
    ,
    x.fx.speeds = {
        slow: 600,
        fast: 200,
        _default: 400
    },
    x.fn.delay = function(i, t) {
        return i = x.fx && x.fx.speeds[i] || i,
        this.queue(t = t || "fx", function(t, e) {
            var n = w.setTimeout(t, i);
            e.stop = function() {
                w.clearTimeout(n)
            }
        })
    }
    ,
    T = f.createElement("input"),
    I = f.createElement("div"),
    D = f.createElement("select"),
    t = D.appendChild(f.createElement("option")),
    (I = f.createElement("div")).setAttribute("className", "t"),
    I.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>",
    P = I.getElementsByTagName("a")[0],
    T.setAttribute("type", "checkbox"),
    I.appendChild(T),
    (P = I.getElementsByTagName("a")[0]).style.cssText = "top:1px",
    m.getSetAttribute = "t" !== I.className,
    m.style = /top/.test(P.getAttribute("style")),
    m.hrefNormalized = "/a" === P.getAttribute("href"),
    m.checkOn = !!T.value,
    m.optSelected = t.selected,
    m.enctype = !!f.createElement("form").enctype,
    D.disabled = !0,
    m.optDisabled = !t.disabled,
    (T = f.createElement("input")).setAttribute("value", ""),
    m.input = "" === T.getAttribute("value"),
    T.value = "t",
    T.setAttribute("type", "radio"),
    m.radioValue = "t" === T.value;
    var Ae = /\r/g
      , Me = /[\x20\t\r\n\f]+/g;
    x.fn.extend({
        val: function(e) {
            var n, t, i, s = this[0];
            return arguments.length ? (i = x.isFunction(e),
            this.each(function(t) {
                1 === this.nodeType && (null == (t = i ? e.call(this, t, x(this).val()) : e) ? t = "" : "number" == typeof t ? t += "" : x.isArray(t) && (t = x.map(t, function(t) {
                    return null == t ? "" : t + ""
                })),
                (n = x.valHooks[this.type] || x.valHooks[this.nodeName.toLowerCase()]) && "set"in n && void 0 !== n.set(this, t, "value") || (this.value = t))
            })) : s ? (n = x.valHooks[s.type] || x.valHooks[s.nodeName.toLowerCase()]) && "get"in n && void 0 !== (t = n.get(s, "value")) ? t : "string" == typeof (t = s.value) ? t.replace(Ae, "") : null == t ? "" : t : void 0
        }
    }),
    x.extend({
        valHooks: {
            option: {
                get: function(t) {
                    var e = x.find.attr(t, "value");
                    return null != e ? e : x.trim(x.text(t)).replace(Me, " ")
                }
            },
            select: {
                get: function(t) {
                    for (var e, n = t.options, i = t.selectedIndex, s = "select-one" === t.type || i < 0, r = s ? null : [], o = s ? i + 1 : n.length, a = i < 0 ? o : s ? i : 0; a < o; a++)
                        if (((e = n[a]).selected || a === i) && (m.optDisabled ? !e.disabled : null === e.getAttribute("disabled")) && (!e.parentNode.disabled || !x.nodeName(e.parentNode, "optgroup"))) {
                            if (e = x(e).val(),
                            s)
                                return e;
                            r.push(e)
                        }
                    return r
                },
                set: function(t, e) {
                    for (var n, i, s = t.options, r = x.makeArray(e), o = s.length; o--; )
                        if (i = s[o],
                        -1 < x.inArray(x.valHooks.option.get(i), r))
                            try {
                                i.selected = n = !0
                            } catch (t) {
                                i.scrollHeight
                            }
                        else
                            i.selected = !1;
                    return n || (t.selectedIndex = -1),
                    s
                }
            }
        }
    }),
    x.each(["radio", "checkbox"], function() {
        x.valHooks[this] = {
            set: function(t, e) {
                if (x.isArray(e))
                    return t.checked = -1 < x.inArray(x(t).val(), e)
            }
        },
        m.checkOn || (x.valHooks[this].get = function(t) {
            return null === t.getAttribute("value") ? "on" : t.value
        }
        )
    });
    var N, Ie, $ = x.expr.attrHandle, Oe = /^(?:checked|selected)$/i, A = m.getSetAttribute, Pe = m.input, Re = (x.fn.extend({
        attr: function(t, e) {
            return h(this, x.attr, t, e, 1 < arguments.length)
        },
        removeAttr: function(t) {
            return this.each(function() {
                x.removeAttr(this, t)
            })
        }
    }),
    x.extend({
        attr: function(t, e, n) {
            var i, s, r = t.nodeType;
            if (3 !== r && 8 !== r && 2 !== r)
                return void 0 === t.getAttribute ? x.prop(t, e, n) : (1 === r && x.isXMLDoc(t) || (e = e.toLowerCase(),
                s = x.attrHooks[e] || (x.expr.match.bool.test(e) ? Ie : N)),
                void 0 !== n ? null === n ? void x.removeAttr(t, e) : s && "set"in s && void 0 !== (i = s.set(t, n, e)) ? i : (t.setAttribute(e, n + ""),
                n) : !(s && "get"in s && null !== (i = s.get(t, e))) && null == (i = x.find.attr(t, e)) ? void 0 : i)
        },
        attrHooks: {
            type: {
                set: function(t, e) {
                    var n;
                    if (!m.radioValue && "radio" === e && x.nodeName(t, "input"))
                        return n = t.value,
                        t.setAttribute("type", e),
                        n && (t.value = n),
                        e
                }
            }
        },
        removeAttr: function(t, e) {
            var n, i, s = 0, r = e && e.match(C);
            if (r && 1 === t.nodeType)
                for (; n = r[s++]; )
                    i = x.propFix[n] || n,
                    x.expr.match.bool.test(n) ? Pe && A || !Oe.test(n) ? t[i] = !1 : t[x.camelCase("default-" + n)] = t[i] = !1 : x.attr(t, n, ""),
                    t.removeAttribute(A ? n : i)
        }
    }),
    Ie = {
        set: function(t, e, n) {
            return !1 === e ? x.removeAttr(t, n) : Pe && A || !Oe.test(n) ? t.setAttribute(!A && x.propFix[n] || n, n) : t[x.camelCase("default-" + n)] = t[n] = !0,
            n
        }
    },
    x.each(x.expr.match.bool.source.match(/\w+/g), function(t, e) {
        var r = $[e] || x.find.attr;
        Pe && A || !Oe.test(e) ? $[e] = function(t, e, n) {
            var i, s;
            return n || (s = $[e],
            $[e] = i,
            i = null != r(t, e, n) ? e.toLowerCase() : null,
            $[e] = s),
            i
        }
        : $[e] = function(t, e, n) {
            if (!n)
                return t[x.camelCase("default-" + e)] ? e.toLowerCase() : null
        }
    }),
    Pe && A || (x.attrHooks.value = {
        set: function(t, e, n) {
            if (!x.nodeName(t, "input"))
                return N && N.set(t, e, n);
            t.defaultValue = e
        }
    }),
    A || (N = {
        set: function(t, e, n) {
            var i = t.getAttributeNode(n);
            if (i || t.setAttributeNode(i = t.ownerDocument.createAttribute(n)),
            i.value = e += "",
            "value" === n || e === t.getAttribute(n))
                return e
        }
    },
    $.id = $.name = $.coords = function(t, e, n) {
        if (!n)
            return (n = t.getAttributeNode(e)) && "" !== n.value ? n.value : null
    }
    ,
    x.valHooks.button = {
        get: function(t, e) {
            t = t.getAttributeNode(e);
            if (t && t.specified)
                return t.value
        },
        set: N.set
    },
    x.attrHooks.contenteditable = {
        set: function(t, e, n) {
            N.set(t, "" !== e && e, n)
        }
    },
    x.each(["width", "height"], function(t, n) {
        x.attrHooks[n] = {
            set: function(t, e) {
                if ("" === e)
                    return t.setAttribute(n, "auto"),
                    e
            }
        }
    })),
    m.style || (x.attrHooks.style = {
        get: function(t) {
            return t.style.cssText || void 0
        },
        set: function(t, e) {
            return t.style.cssText = e + ""
        }
    }),
    /^(?:input|select|textarea|button|object)$/i), Le = /^(?:a|area)$/i, je = (x.fn.extend({
        prop: function(t, e) {
            return h(this, x.prop, t, e, 1 < arguments.length)
        },
        removeProp: function(t) {
            return t = x.propFix[t] || t,
            this.each(function() {
                try {
                    this[t] = void 0,
                    delete this[t]
                } catch (t) {}
            })
        }
    }),
    x.extend({
        prop: function(t, e, n) {
            var i, s, r = t.nodeType;
            if (3 !== r && 8 !== r && 2 !== r)
                return 1 === r && x.isXMLDoc(t) || (e = x.propFix[e] || e,
                s = x.propHooks[e]),
                void 0 !== n ? s && "set"in s && void 0 !== (i = s.set(t, n, e)) ? i : t[e] = n : s && "get"in s && null !== (i = s.get(t, e)) ? i : t[e]
        },
        propHooks: {
            tabIndex: {
                get: function(t) {
                    var e = x.find.attr(t, "tabindex");
                    return e ? parseInt(e, 10) : Re.test(t.nodeName) || Le.test(t.nodeName) && t.href ? 0 : -1
                }
            }
        },
        propFix: {
            for: "htmlFor",
            class: "className"
        }
    }),
    m.hrefNormalized || x.each(["href", "src"], function(t, e) {
        x.propHooks[e] = {
            get: function(t) {
                return t.getAttribute(e, 4)
            }
        }
    }),
    m.optSelected || (x.propHooks.selected = {
        get: function(t) {
            t = t.parentNode;
            return t && (t.selectedIndex,
            t.parentNode && t.parentNode.selectedIndex),
            null
        },
        set: function(t) {
            t = t.parentNode;
            t && (t.selectedIndex,
            t.parentNode && t.parentNode.selectedIndex)
        }
    }),
    x.each(["tabIndex", "readOnly", "maxLength", "cellSpacing", "cellPadding", "rowSpan", "colSpan", "useMap", "frameBorder", "contentEditable"], function() {
        x.propFix[this.toLowerCase()] = this
    }),
    m.enctype || (x.propFix.enctype = "encoding"),
    /[\t\r\n\f]/g);
    function M(t) {
        return x.attr(t, "class") || ""
    }
    x.fn.extend({
        addClass: function(e) {
            var t, n, i, s, r, o, a = 0;
            if (x.isFunction(e))
                return this.each(function(t) {
                    x(this).addClass(e.call(this, t, M(this)))
                });
            if ("string" == typeof e && e)
                for (t = e.match(C) || []; n = this[a++]; )
                    if (o = M(n),
                    i = 1 === n.nodeType && (" " + o + " ").replace(je, " ")) {
                        for (r = 0; s = t[r++]; )
                            i.indexOf(" " + s + " ") < 0 && (i += s + " ");
                        o !== (o = x.trim(i)) && x.attr(n, "class", o)
                    }
            return this
        },
        removeClass: function(e) {
            var t, n, i, s, r, o, a = 0;
            if (x.isFunction(e))
                return this.each(function(t) {
                    x(this).removeClass(e.call(this, t, M(this)))
                });
            if (!arguments.length)
                return this.attr("class", "");
            if ("string" == typeof e && e)
                for (t = e.match(C) || []; n = this[a++]; )
                    if (o = M(n),
                    i = 1 === n.nodeType && (" " + o + " ").replace(je, " ")) {
                        for (r = 0; s = t[r++]; )
                            for (; -1 < i.indexOf(" " + s + " "); )
                                i = i.replace(" " + s + " ", " ");
                        o !== (o = x.trim(i)) && x.attr(n, "class", o)
                    }
            return this
        },
        toggleClass: function(s, e) {
            var r = typeof s;
            return "boolean" == typeof e && "string" == r ? e ? this.addClass(s) : this.removeClass(s) : x.isFunction(s) ? this.each(function(t) {
                x(this).toggleClass(s.call(this, t, M(this), e), e)
            }) : this.each(function() {
                var t, e, n, i;
                if ("string" == r)
                    for (e = 0,
                    n = x(this),
                    i = s.match(C) || []; t = i[e++]; )
                        n.hasClass(t) ? n.removeClass(t) : n.addClass(t);
                else
                    void 0 !== s && "boolean" != r || ((t = M(this)) && x._data(this, "__className__", t),
                    x.attr(this, "class", !t && !1 !== s && x._data(this, "__className__") || ""))
            })
        },
        hasClass: function(t) {
            for (var e, n = 0, i = " " + t + " "; e = this[n++]; )
                if (1 === e.nodeType && -1 < (" " + M(e) + " ").replace(je, " ").indexOf(i))
                    return !0;
            return !1
        }
    }),
    x.each("blur focus focusin focusout load resize scroll unload click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup error contextmenu".split(" "), function(t, n) {
        x.fn[n] = function(t, e) {
            return 0 < arguments.length ? this.on(n, null, t, e) : this.trigger(n)
        }
    }),
    x.fn.extend({
        hover: function(t, e) {
            return this.mouseenter(t).mouseleave(e || t)
        }
    });
    var I = w.location
      , He = x.now()
      , Fe = /\?/
      , We = /(,)|(\[|{)|(}|])|"(?:[^"\\\r\n]|\\["\\\/bfnrt]|\\u[\da-fA-F]{4})*"\s*:?|true|false|null|-?(?!0\d)\d+(?:\.\d+|)(?:[eE][+-]?\d+|)/g
      , Ye = (x.parseJSON = function(t) {
        if (w.JSON && w.JSON.parse)
            return w.JSON.parse(t + "");
        var s, r = null, e = x.trim(t + "");
        return e && !x.trim(e.replace(We, function(t, e, n, i) {
            return 0 === (r = s && e ? 0 : r) ? t : (s = n || e,
            r += !i - !n,
            "")
        })) ? Function("return " + e)() : x.error("Invalid JSON: " + t)
    }
    ,
    x.parseXML = function(t) {
        var e;
        if (!t || "string" != typeof t)
            return null;
        try {
            w.DOMParser ? e = (new w.DOMParser).parseFromString(t, "text/xml") : ((e = new w.ActiveXObject("Microsoft.XMLDOM")).async = "false",
            e.loadXML(t))
        } catch (t) {
            e = void 0
        }
        return e && e.documentElement && !e.getElementsByTagName("parsererror").length || x.error("Invalid XML: " + t),
        e
    }
    ,
    /#.*$/)
      , qe = /([?&])_=[^&]*/
      , ze = /^(.*?):[ \t]*([^\r\n]*)\r?$/gm
      , Be = /^(?:GET|HEAD)$/
      , Ve = /^\/\//
      , Ue = /^([\w.+-]+:)(?:\/\/(?:[^\/?#]*@|)([^\/?#:]*)(?::(\d+)|)|)/
      , Ge = {}
      , Qe = {}
      , Ke = "*/".concat("*")
      , Xe = I.href
      , O = Ue.exec(Xe.toLowerCase()) || [];
    function Ze(r) {
        return function(t, e) {
            "string" != typeof t && (e = t,
            t = "*");
            var n, i = 0, s = t.toLowerCase().match(C) || [];
            if (x.isFunction(e))
                for (; n = s[i++]; )
                    "+" === n.charAt(0) ? (n = n.slice(1) || "*",
                    (r[n] = r[n] || []).unshift(e)) : (r[n] = r[n] || []).push(e)
        }
    }
    function Je(e, i, s, r) {
        var o = {}
          , a = e === Qe;
        function l(t) {
            var n;
            return o[t] = !0,
            x.each(e[t] || [], function(t, e) {
                e = e(i, s, r);
                return "string" != typeof e || a || o[e] ? a ? !(n = e) : void 0 : (i.dataTypes.unshift(e),
                l(e),
                !1)
            }),
            n
        }
        return l(i.dataTypes[0]) || !o["*"] && l("*")
    }
    function tn(t, e) {
        var n, i, s = x.ajaxSettings.flatOptions || {};
        for (i in e)
            void 0 !== e[i] && ((s[i] ? t : n = n || {})[i] = e[i]);
        return n && x.extend(!0, t, n),
        t
    }
    function en(t) {
        if (!x.contains(t.ownerDocument || f, t))
            return !0;
        for (; t && 1 === t.nodeType; ) {
            if ("none" === ((e = t).style && e.style.display || x.css(e, "display")) || "hidden" === t.type)
                return !0;
            t = t.parentNode
        }
        var e;
        return !1
    }
    x.extend({
        active: 0,
        lastModified: {},
        etag: {},
        ajaxSettings: {
            url: Xe,
            type: "GET",
            isLocal: /^(?:about|app|app-storage|.+-extension|file|res|widget):$/.test(O[1]),
            global: !0,
            processData: !0,
            async: !0,
            contentType: "application/x-www-form-urlencoded; charset=UTF-8",
            accepts: {
                "*": Ke,
                text: "text/plain",
                html: "text/html",
                xml: "application/xml, text/xml",
                json: "application/json, text/javascript"
            },
            contents: {
                xml: /\bxml\b/,
                html: /\bhtml/,
                json: /\bjson\b/
            },
            responseFields: {
                xml: "responseXML",
                text: "responseText",
                json: "responseJSON"
            },
            converters: {
                "* text": String,
                "text html": !0,
                "text json": x.parseJSON,
                "text xml": x.parseXML
            },
            flatOptions: {
                url: !0,
                context: !0
            }
        },
        ajaxSetup: function(t, e) {
            return e ? tn(tn(t, x.ajaxSettings), e) : tn(x.ajaxSettings, t)
        },
        ajaxPrefilter: Ze(Ge),
        ajaxTransport: Ze(Qe),
        ajax: function(t, e) {
            "object" == typeof t && (e = t,
            t = void 0);
            var n, l, c, u, d, h, i, p = x.ajaxSetup({}, e = e || {}), f = p.context || p, g = p.context && (f.nodeType || f.jquery) ? x(f) : x.event, m = x.Deferred(), y = x.Callbacks("once memory"), v = p.statusCode || {}, s = {}, r = {}, b = 0, o = "canceled", _ = {
                readyState: 0,
                getResponseHeader: function(t) {
                    var e;
                    if (2 === b) {
                        if (!i)
                            for (i = {}; e = ze.exec(c); )
                                i[e[1].toLowerCase()] = e[2];
                        e = i[t.toLowerCase()]
                    }
                    return null == e ? null : e
                },
                getAllResponseHeaders: function() {
                    return 2 === b ? c : null
                },
                setRequestHeader: function(t, e) {
                    var n = t.toLowerCase();
                    return b || (t = r[n] = r[n] || t,
                    s[t] = e),
                    this
                },
                overrideMimeType: function(t) {
                    return b || (p.mimeType = t),
                    this
                },
                statusCode: function(t) {
                    if (t)
                        if (b < 2)
                            for (var e in t)
                                v[e] = [v[e], t[e]];
                        else
                            _.always(t[_.status]);
                    return this
                },
                abort: function(t) {
                    t = t || o;
                    return h && h.abort(t),
                    a(0, t),
                    this
                }
            };
            if (m.promise(_).complete = y.add,
            _.success = _.done,
            _.error = _.fail,
            p.url = ((t || p.url || Xe) + "").replace(Ye, "").replace(Ve, O[1] + "//"),
            p.type = e.method || e.type || p.method || p.type,
            p.dataTypes = x.trim(p.dataType || "*").toLowerCase().match(C) || [""],
            null == p.crossDomain && (t = Ue.exec(p.url.toLowerCase()),
            p.crossDomain = !(!t || t[1] === O[1] && t[2] === O[2] && (t[3] || ("http:" === t[1] ? "80" : "443")) === (O[3] || ("http:" === O[1] ? "80" : "443")))),
            p.data && p.processData && "string" != typeof p.data && (p.data = x.param(p.data, p.traditional)),
            Je(Ge, p, e, _),
            2 === b)
                return _;
            for (n in (d = x.event && p.global) && 0 == x.active++ && x.event.trigger("ajaxStart"),
            p.type = p.type.toUpperCase(),
            p.hasContent = !Be.test(p.type),
            l = p.url,
            p.hasContent || (p.data && (l = p.url += (Fe.test(l) ? "&" : "?") + p.data,
            delete p.data),
            !1 === p.cache && (p.url = qe.test(l) ? l.replace(qe, "$1_=" + He++) : l + (Fe.test(l) ? "&" : "?") + "_=" + He++)),
            p.ifModified && (x.lastModified[l] && _.setRequestHeader("If-Modified-Since", x.lastModified[l]),
            x.etag[l] && _.setRequestHeader("If-None-Match", x.etag[l])),
            (p.data && p.hasContent && !1 !== p.contentType || e.contentType) && _.setRequestHeader("Content-Type", p.contentType),
            _.setRequestHeader("Accept", p.dataTypes[0] && p.accepts[p.dataTypes[0]] ? p.accepts[p.dataTypes[0]] + ("*" !== p.dataTypes[0] ? ", " + Ke + "; q=0.01" : "") : p.accepts["*"]),
            p.headers)
                _.setRequestHeader(n, p.headers[n]);
            if (p.beforeSend && (!1 === p.beforeSend.call(f, _, p) || 2 === b))
                return _.abort();
            for (n in o = "abort",
            {
                success: 1,
                error: 1,
                complete: 1
            })
                _[n](p[n]);
            if (h = Je(Qe, p, e, _)) {
                if (_.readyState = 1,
                d && g.trigger("ajaxSend", [_, p]),
                2 === b)
                    return _;
                p.async && 0 < p.timeout && (u = w.setTimeout(function() {
                    _.abort("timeout")
                }, p.timeout));
                try {
                    b = 1,
                    h.send(s, a)
                } catch (t) {
                    if (!(b < 2))
                        throw t;
                    a(-1, t)
                }
            } else
                a(-1, "No Transport");
            function a(t, e, n, i) {
                var s, r, o, a = e;
                2 !== b && (b = 2,
                u && w.clearTimeout(u),
                h = void 0,
                c = i || "",
                _.readyState = 0 < t ? 4 : 0,
                i = 200 <= t && t < 300 || 304 === t,
                n && (o = function(t, e, n) {
                    for (var i, s, r, o, a = t.contents, l = t.dataTypes; "*" === l[0]; )
                        l.shift(),
                        void 0 === s && (s = t.mimeType || e.getResponseHeader("Content-Type"));
                    if (s)
                        for (o in a)
                            if (a[o] && a[o].test(s)) {
                                l.unshift(o);
                                break
                            }
                    if (l[0]in n)
                        r = l[0];
                    else {
                        for (o in n) {
                            if (!l[0] || t.converters[o + " " + l[0]]) {
                                r = o;
                                break
                            }
                            i = i || o
                        }
                        r = r || i
                    }
                    if (r)
                        return r !== l[0] && l.unshift(r),
                        n[r]
                }(p, _, n)),
                o = function(t, e, n, i) {
                    var s, r, o, a, l, c = {}, u = t.dataTypes.slice();
                    if (u[1])
                        for (o in t.converters)
                            c[o.toLowerCase()] = t.converters[o];
                    for (r = u.shift(); r; )
                        if (t.responseFields[r] && (n[t.responseFields[r]] = e),
                        !l && i && t.dataFilter && (e = t.dataFilter(e, t.dataType)),
                        l = r,
                        r = u.shift())
                            if ("*" === r)
                                r = l;
                            else if ("*" !== l && l !== r) {
                                if (!(o = c[l + " " + r] || c["* " + r]))
                                    for (s in c)
                                        if ((a = s.split(" "))[1] === r && (o = c[l + " " + a[0]] || c["* " + a[0]])) {
                                            !0 === o ? o = c[s] : !0 !== c[s] && (r = a[0],
                                            u.unshift(a[1]));
                                            break
                                        }
                                if (!0 !== o)
                                    if (o && t.throws)
                                        e = o(e);
                                    else
                                        try {
                                            e = o(e)
                                        } catch (t) {
                                            return {
                                                state: "parsererror",
                                                error: o ? t : "No conversion from " + l + " to " + r
                                            }
                                        }
                            }
                    return {
                        state: "success",
                        data: e
                    }
                }(p, o, _, i),
                i ? (p.ifModified && ((n = _.getResponseHeader("Last-Modified")) && (x.lastModified[l] = n),
                (n = _.getResponseHeader("etag")) && (x.etag[l] = n)),
                204 === t || "HEAD" === p.type ? a = "nocontent" : 304 === t ? a = "notmodified" : (a = o.state,
                s = o.data,
                i = !(r = o.error))) : (r = a,
                !t && a || (a = "error",
                t < 0 && (t = 0))),
                _.status = t,
                _.statusText = (e || a) + "",
                i ? m.resolveWith(f, [s, a, _]) : m.rejectWith(f, [_, a, r]),
                _.statusCode(v),
                v = void 0,
                d && g.trigger(i ? "ajaxSuccess" : "ajaxError", [_, p, i ? s : r]),
                y.fireWith(f, [_, a]),
                d && (g.trigger("ajaxComplete", [_, p]),
                --x.active || x.event.trigger("ajaxStop")))
            }
            return _
        },
        getJSON: function(t, e, n) {
            return x.get(t, e, n, "json")
        },
        getScript: function(t, e) {
            return x.get(t, void 0, e, "script")
        }
    }),
    x.each(["get", "post"], function(t, s) {
        x[s] = function(t, e, n, i) {
            return x.isFunction(e) && (i = i || n,
            n = e,
            e = void 0),
            x.ajax(x.extend({
                url: t,
                type: s,
                dataType: i,
                data: e,
                success: n
            }, x.isPlainObject(t) && t))
        }
    }),
    x._evalUrl = function(t) {
        return x.ajax({
            url: t,
            type: "GET",
            dataType: "script",
            cache: !0,
            async: !1,
            global: !1,
            throws: !0
        })
    }
    ,
    x.fn.extend({
        wrapAll: function(e) {
            return x.isFunction(e) ? this.each(function(t) {
                x(this).wrapAll(e.call(this, t))
            }) : (this[0] && (t = x(e, this[0].ownerDocument).eq(0).clone(!0),
            this[0].parentNode && t.insertBefore(this[0]),
            t.map(function() {
                for (var t = this; t.firstChild && 1 === t.firstChild.nodeType; )
                    t = t.firstChild;
                return t
            }).append(this)),
            this);
            var t
        },
        wrapInner: function(n) {
            return x.isFunction(n) ? this.each(function(t) {
                x(this).wrapInner(n.call(this, t))
            }) : this.each(function() {
                var t = x(this)
                  , e = t.contents();
                e.length ? e.wrapAll(n) : t.append(n)
            })
        },
        wrap: function(e) {
            var n = x.isFunction(e);
            return this.each(function(t) {
                x(this).wrapAll(n ? e.call(this, t) : e)
            })
        },
        unwrap: function() {
            return this.parent().each(function() {
                x.nodeName(this, "body") || x(this).replaceWith(this.childNodes)
            }).end()
        }
    }),
    x.expr.filters.hidden = function(t) {
        return m.reliableHiddenOffsets() ? t.offsetWidth <= 0 && t.offsetHeight <= 0 && !t.getClientRects().length : en(t)
    }
    ,
    x.expr.filters.visible = function(t) {
        return !x.expr.filters.hidden(t)
    }
    ;
    var nn = /%20/g
      , sn = /\[\]$/
      , rn = /\r?\n/g
      , on = /^(?:submit|button|image|reset|file)$/i
      , an = /^(?:input|select|textarea|keygen)/i;
    x.param = function(t, e) {
        function n(t, e) {
            e = x.isFunction(e) ? e() : null == e ? "" : e,
            s[s.length] = encodeURIComponent(t) + "=" + encodeURIComponent(e)
        }
        var i, s = [];
        if (void 0 === e && (e = x.ajaxSettings && x.ajaxSettings.traditional),
        x.isArray(t) || t.jquery && !x.isPlainObject(t))
            x.each(t, function() {
                n(this.name, this.value)
            });
        else
            for (i in t)
                !function n(i, t, s, r) {
                    if (x.isArray(t))
                        x.each(t, function(t, e) {
                            s || sn.test(i) ? r(i, e) : n(i + "[" + ("object" == typeof e && null != e ? t : "") + "]", e, s, r)
                        });
                    else if (s || "object" !== x.type(t))
                        r(i, t);
                    else
                        for (var e in t)
                            n(i + "[" + e + "]", t[e], s, r)
                }(i, t[i], e, n);
        return s.join("&").replace(nn, "+")
    }
    ,
    x.fn.extend({
        serialize: function() {
            return x.param(this.serializeArray())
        },
        serializeArray: function() {
            return this.map(function() {
                var t = x.prop(this, "elements");
                return t ? x.makeArray(t) : this
            }).filter(function() {
                var t = this.type;
                return this.name && !x(this).is(":disabled") && an.test(this.nodeName) && !on.test(t) && (this.checked || !gt.test(t))
            }).map(function(t, e) {
                var n = x(this).val();
                return null == n ? null : x.isArray(n) ? x.map(n, function(t) {
                    return {
                        name: e.name,
                        value: t.replace(rn, "\r\n")
                    }
                }) : {
                    name: e.name,
                    value: n.replace(rn, "\r\n")
                }
            }).get()
        }
    }),
    x.ajaxSettings.xhr = void 0 !== w.ActiveXObject ? function() {
        return this.isLocal ? dn() : 8 < f.documentMode ? un() : /^(get|post|head|put|delete|options)$/i.test(this.type) && un() || dn()
    }
    : un;
    var ln = 0
      , cn = {}
      , P = x.ajaxSettings.xhr();
    function un() {
        try {
            return new w.XMLHttpRequest
        } catch (t) {}
    }
    function dn() {
        try {
            return new w.ActiveXObject("Microsoft.XMLHTTP")
        } catch (t) {}
    }
    w.attachEvent && w.attachEvent("onunload", function() {
        for (var t in cn)
            cn[t](void 0, !0)
    }),
    m.cors = !!P && "withCredentials"in P,
    (P = m.ajax = !!P) && x.ajaxTransport(function(l) {
        var c;
        if (!l.crossDomain || m.cors)
            return {
                send: function(t, r) {
                    var e, o = l.xhr(), a = ++ln;
                    if (o.open(l.type, l.url, l.async, l.username, l.password),
                    l.xhrFields)
                        for (e in l.xhrFields)
                            o[e] = l.xhrFields[e];
                    for (e in l.mimeType && o.overrideMimeType && o.overrideMimeType(l.mimeType),
                    l.crossDomain || t["X-Requested-With"] || (t["X-Requested-With"] = "XMLHttpRequest"),
                    t)
                        void 0 !== t[e] && o.setRequestHeader(e, t[e] + "");
                    o.send(l.hasContent && l.data || null),
                    c = function(t, e) {
                        var n, i, s;
                        if (c && (e || 4 === o.readyState))
                            if (delete cn[a],
                            c = void 0,
                            o.onreadystatechange = x.noop,
                            e)
                                4 !== o.readyState && o.abort();
                            else {
                                s = {},
                                n = o.status,
                                "string" == typeof o.responseText && (s.text = o.responseText);
                                try {
                                    i = o.statusText
                                } catch (t) {
                                    i = ""
                                }
                                n || !l.isLocal || l.crossDomain ? 1223 === n && (n = 204) : n = s.text ? 200 : 404
                            }
                        s && r(n, i, s, o.getAllResponseHeaders())
                    }
                    ,
                    l.async ? 4 === o.readyState ? w.setTimeout(c) : o.onreadystatechange = cn[a] = c : c()
                },
                abort: function() {
                    c && c(void 0, !0)
                }
            }
    }),
    x.ajaxSetup({
        accepts: {
            script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
        },
        contents: {
            script: /\b(?:java|ecma)script\b/
        },
        converters: {
            "text script": function(t) {
                return x.globalEval(t),
                t
            }
        }
    }),
    x.ajaxPrefilter("script", function(t) {
        void 0 === t.cache && (t.cache = !1),
        t.crossDomain && (t.type = "GET",
        t.global = !1)
    }),
    x.ajaxTransport("script", function(e) {
        var i, s;
        if (e.crossDomain)
            return s = f.head || x("head")[0] || f.documentElement,
            {
                send: function(t, n) {
                    (i = f.createElement("script")).async = !0,
                    e.scriptCharset && (i.charset = e.scriptCharset),
                    i.src = e.url,
                    i.onload = i.onreadystatechange = function(t, e) {
                        !e && i.readyState && !/loaded|complete/.test(i.readyState) || (i.onload = i.onreadystatechange = null,
                        i.parentNode && i.parentNode.removeChild(i),
                        i = null,
                        e || n(200, "success"))
                    }
                    ,
                    s.insertBefore(i, s.firstChild)
                },
                abort: function() {
                    i && i.onload(void 0, !0)
                }
            }
    });
    var hn = []
      , pn = /(=)\?(?=&|$)|\?\?/
      , fn = (x.ajaxSetup({
        jsonp: "callback",
        jsonpCallback: function() {
            var t = hn.pop() || x.expando + "_" + He++;
            return this[t] = !0,
            t
        }
    }),
    x.ajaxPrefilter("json jsonp", function(t, e, n) {
        var i, s, r, o = !1 !== t.jsonp && (pn.test(t.url) ? "url" : "string" == typeof t.data && 0 === (t.contentType || "").indexOf("application/x-www-form-urlencoded") && pn.test(t.data) && "data");
        if (o || "jsonp" === t.dataTypes[0])
            return i = t.jsonpCallback = x.isFunction(t.jsonpCallback) ? t.jsonpCallback() : t.jsonpCallback,
            o ? t[o] = t[o].replace(pn, "$1" + i) : !1 !== t.jsonp && (t.url += (Fe.test(t.url) ? "&" : "?") + t.jsonp + "=" + i),
            t.converters["script json"] = function() {
                return r || x.error(i + " was not called"),
                r[0]
            }
            ,
            t.dataTypes[0] = "json",
            s = w[i],
            w[i] = function() {
                r = arguments
            }
            ,
            n.always(function() {
                void 0 === s ? x(w).removeProp(i) : w[i] = s,
                t[i] && (t.jsonpCallback = e.jsonpCallback,
                hn.push(i)),
                r && x.isFunction(s) && s(r[0]),
                r = s = void 0
            }),
            "script"
    }),
    x.parseHTML = function(t, e, n) {
        if (!t || "string" != typeof t)
            return null;
        "boolean" == typeof e && (n = e,
        e = !1),
        e = e || f;
        var i = Q.exec(t)
          , n = !n && [];
        return i ? [e.createElement(i[1])] : (i = St([t], e, n),
        n && n.length && x(n).remove(),
        x.merge([], i.childNodes))
    }
    ,
    x.fn.load);
    function gn(t) {
        return x.isWindow(t) ? t : 9 === t.nodeType && (t.defaultView || t.parentWindow)
    }
    x.fn.load = function(t, e, n) {
        if ("string" != typeof t && fn)
            return fn.apply(this, arguments);
        var i, s, r, o = this, a = t.indexOf(" ");
        return -1 < a && (i = x.trim(t.slice(a, t.length)),
        t = t.slice(0, a)),
        x.isFunction(e) ? (n = e,
        e = void 0) : e && "object" == typeof e && (s = "POST"),
        0 < o.length && x.ajax({
            url: t,
            type: s || "GET",
            dataType: "html",
            data: e
        }).done(function(t) {
            r = arguments,
            o.html(i ? x("<div>").append(x.parseHTML(t)).find(i) : t)
        }).always(n && function(t, e) {
            o.each(function() {
                n.apply(this, r || [t.responseText, e, t])
            })
        }
        ),
        this
    }
    ,
    x.each(["ajaxStart", "ajaxStop", "ajaxComplete", "ajaxError", "ajaxSuccess", "ajaxSend"], function(t, e) {
        x.fn[e] = function(t) {
            return this.on(e, t)
        }
    }),
    x.expr.filters.animated = function(e) {
        return x.grep(x.timers, function(t) {
            return e === t.elem
        }).length
    }
    ,
    x.offset = {
        setOffset: function(t, e, n) {
            var i, s, r, o, a = x.css(t, "position"), l = x(t), c = {};
            "static" === a && (t.style.position = "relative"),
            r = l.offset(),
            i = x.css(t, "top"),
            o = x.css(t, "left"),
            a = ("absolute" === a || "fixed" === a) && -1 < x.inArray("auto", [i, o]) ? (s = (a = l.position()).top,
            a.left) : (s = parseFloat(i) || 0,
            parseFloat(o) || 0),
            null != (e = x.isFunction(e) ? e.call(t, n, x.extend({}, r)) : e).top && (c.top = e.top - r.top + s),
            null != e.left && (c.left = e.left - r.left + a),
            "using"in e ? e.using.call(t, c) : l.css(c)
        }
    },
    x.fn.extend({
        offset: function(e) {
            if (arguments.length)
                return void 0 === e ? this : this.each(function(t) {
                    x.offset.setOffset(this, e, t)
                });
            var t, n = {
                top: 0,
                left: 0
            }, i = this[0], s = i && i.ownerDocument;
            return s ? (t = s.documentElement,
            x.contains(t, i) ? (void 0 !== i.getBoundingClientRect && (n = i.getBoundingClientRect()),
            i = gn(s),
            {
                top: n.top + (i.pageYOffset || t.scrollTop) - (t.clientTop || 0),
                left: n.left + (i.pageXOffset || t.scrollLeft) - (t.clientLeft || 0)
            }) : n) : void 0
        },
        position: function() {
            var t, e, n, i;
            if (this[0])
                return n = {
                    top: 0,
                    left: 0
                },
                i = this[0],
                "fixed" === x.css(i, "position") ? e = i.getBoundingClientRect() : (t = this.offsetParent(),
                e = this.offset(),
                (n = x.nodeName(t[0], "html") ? n : t.offset()).top += x.css(t[0], "borderTopWidth", !0),
                n.left += x.css(t[0], "borderLeftWidth", !0)),
                {
                    top: e.top - n.top - x.css(i, "marginTop", !0),
                    left: e.left - n.left - x.css(i, "marginLeft", !0)
                }
        },
        offsetParent: function() {
            return this.map(function() {
                for (var t = this.offsetParent; t && !x.nodeName(t, "html") && "static" === x.css(t, "position"); )
                    t = t.offsetParent;
                return t || ce
            })
        }
    }),
    x.each({
        scrollLeft: "pageXOffset",
        scrollTop: "pageYOffset"
    }, function(e, s) {
        var r = /Y/.test(s);
        x.fn[e] = function(t) {
            return h(this, function(t, e, n) {
                var i = gn(t);
                if (void 0 === n)
                    return i ? s in i ? i[s] : i.document.documentElement[e] : t[e];
                i ? i.scrollTo(r ? x(i).scrollLeft() : n, r ? n : x(i).scrollTop()) : t[e] = n
            }, e, t, arguments.length, null)
        }
    }),
    x.each(["top", "left"], function(t, n) {
        x.cssHooks[n] = de(m.pixelPosition, function(t, e) {
            if (e)
                return e = p(t, n),
                le.test(e) ? x(t).position()[n] + "px" : e
        })
    }),
    x.each({
        Height: "height",
        Width: "width"
    }, function(r, o) {
        x.each({
            padding: "inner" + r,
            content: o,
            "": "outer" + r
        }, function(i, t) {
            x.fn[t] = function(t, e) {
                var n = arguments.length && (i || "boolean" != typeof t)
                  , s = i || (!0 === t || !0 === e ? "margin" : "border");
                return h(this, function(t, e, n) {
                    var i;
                    return x.isWindow(t) ? t.document.documentElement["client" + r] : 9 === t.nodeType ? (i = t.documentElement,
                    Math.max(t.body["scroll" + r], i["scroll" + r], t.body["offset" + r], i["offset" + r], i["client" + r])) : void 0 === n ? x.css(t, e, s) : x.style(t, e, n, s)
                }, o, n ? t : void 0, n, null)
            }
        })
    }),
    x.fn.extend({
        bind: function(t, e, n) {
            return this.on(t, null, e, n)
        },
        unbind: function(t, e) {
            return this.off(t, null, e)
        },
        delegate: function(t, e, n, i) {
            return this.on(e, t, n, i)
        },
        undelegate: function(t, e, n) {
            return 1 === arguments.length ? this.off(t, "**") : this.off(e, t || "**", n)
        }
    }),
    x.fn.size = function() {
        return this.length
    }
    ,
    x.fn.andSelf = x.fn.addBack,
    "function" == typeof define && define.amd && define("jquery", [], function() {
        return x
    });
    var mn = w.jQuery
      , yn = w.$;
    return x.noConflict = function(t) {
        return w.$ === x && (w.$ = yn),
        t && w.jQuery === x && (w.jQuery = mn),
        x
    }
    ,
    R || (w.jQuery = w.$ = x),
    x
}),
function() {
    var a, r, s, o, l = {}.hasOwnProperty;
    function t() {
        this.options_index = 0,
        this.parsed = []
    }
    function n(t, e) {
        this.form_field = t,
        this.options = null != e ? e : {},
        n.browser_is_supported() && (this.is_multiple = this.form_field.multiple,
        this.set_default_text(),
        this.set_default_values(),
        this.setup(),
        this.set_up_html(),
        this.register_observers())
    }
    t.prototype.add_node = function(t) {
        return "OPTGROUP" === t.nodeName.toUpperCase() ? this.add_group(t) : this.add_option(t)
    }
    ,
    t.prototype.add_group = function(t) {
        var e, n, i, s, r, o = this.parsed.length;
        for (this.parsed.push({
            array_index: o,
            group: !0,
            label: this.escapeExpression(t.label),
            children: 0,
            disabled: t.disabled
        }),
        r = [],
        n = 0,
        i = (s = t.childNodes).length; n < i; n++)
            e = s[n],
            r.push(this.add_option(e, o, t.disabled));
        return r
    }
    ,
    t.prototype.add_option = function(t, e, n) {
        if ("OPTION" === t.nodeName.toUpperCase())
            return "" !== t.text ? (null != e && (this.parsed[e].children += 1),
            this.parsed.push({
                array_index: this.parsed.length,
                options_index: this.options_index,
                value: t.value,
                text: t.text,
                html: t.innerHTML,
                selected: t.selected,
                disabled: !0 === n ? n : t.disabled,
                group_array_index: e,
                classes: t.className,
                style: t.style.cssText
            })) : this.parsed.push({
                array_index: this.parsed.length,
                options_index: this.options_index,
                empty: !0
            }),
            this.options_index += 1
    }
    ,
    t.prototype.escapeExpression = function(t) {
        var e;
        return null == t || !1 === t ? "" : /[\&\<\>\"\'\`]/.test(t) ? (e = {
            "<": "&lt;",
            ">": "&gt;",
            '"': "&quot;",
            "'": "&#x27;",
            "`": "&#x60;"
        },
        t.replace(/&(?!\w+;)|[\<\>\"\'\`]/g, function(t) {
            return e[t] || "&amp;"
        })) : t
    }
    ,
    (o = t).select_to_array = function(t) {
        for (var e, n = new o, i = t.childNodes, s = 0, r = i.length; s < r; s++)
            e = i[s],
            n.add_node(e);
        return n.parsed
    }
    ,
    n.prototype.set_default_values = function() {
        var e = this;
        return this.click_test_action = function(t) {
            return e.test_active_click(t)
        }
        ,
        this.activate_action = function(t) {
            return e.activate_field(t)
        }
        ,
        this.active_field = !1,
        this.mouse_on_container = !1,
        this.results_showing = !1,
        this.result_highlighted = null,
        this.allow_single_deselect = null != this.options.allow_single_deselect && null != this.form_field.options[0] && "" === this.form_field.options[0].text && this.options.allow_single_deselect,
        this.disable_search_threshold = this.options.disable_search_threshold || 0,
        this.disable_search = this.options.disable_search || !1,
        this.enable_split_word_search = null == this.options.enable_split_word_search || this.options.enable_split_word_search,
        this.group_search = null == this.options.group_search || this.options.group_search,
        this.search_contains = this.options.search_contains || !1,
        this.single_backstroke_delete = null == this.options.single_backstroke_delete || this.options.single_backstroke_delete,
        this.max_selected_options = this.options.max_selected_options || 1 / 0,
        this.inherit_select_classes = this.options.inherit_select_classes || !1,
        this.display_selected_options = null == this.options.display_selected_options || this.options.display_selected_options,
        this.display_disabled_options = null == this.options.display_disabled_options || this.options.display_disabled_options
    }
    ,
    n.prototype.set_default_text = function() {
        return this.form_field.getAttribute("data-placeholder") ? this.default_text = this.form_field.getAttribute("data-placeholder") : this.is_multiple ? this.default_text = this.options.placeholder_text_multiple || this.options.placeholder_text || n.default_multiple_text : this.default_text = this.options.placeholder_text_single || this.options.placeholder_text || n.default_single_text,
        this.results_none_found = this.form_field.getAttribute("data-no_results_text") || this.options.no_results_text || n.default_no_result_text
    }
    ,
    n.prototype.mouse_enter = function() {
        return this.mouse_on_container = !0
    }
    ,
    n.prototype.mouse_leave = function() {
        return this.mouse_on_container = !1
    }
    ,
    n.prototype.input_focus = function(t) {
        var e = this;
        if (this.is_multiple) {
            if (!this.active_field)
                return setTimeout(function() {
                    return e.container_mousedown()
                }, 50)
        } else if (!this.active_field)
            return this.activate_field()
    }
    ,
    n.prototype.input_blur = function(t) {
        var e = this;
        if (!this.mouse_on_container)
            return this.active_field = !1,
            setTimeout(function() {
                return e.blur_test()
            }, 100)
    }
    ,
    n.prototype.results_option_build = function(t) {
        for (var e, n = "", i = this.results_data, s = 0, r = i.length; s < r; s++)
            (e = i[s]).group ? n += this.result_add_group(e) : n += this.result_add_option(e),
            null != t && t.first && (e.selected && this.is_multiple ? this.choice_build(e) : e.selected && !this.is_multiple && this.single_set_selected_text(e.text));
        return n
    }
    ,
    n.prototype.result_add_option = function(t) {
        var e, n;
        return t.search_match && this.include_option_in_results(t) ? (e = [],
        t.disabled || t.selected && this.is_multiple || e.push("active-result"),
        !t.disabled || t.selected && this.is_multiple || e.push("disabled-result"),
        t.selected && e.push("result-selected"),
        null != t.group_array_index && e.push("group-option"),
        "" !== t.classes && e.push(t.classes),
        (n = document.createElement("li")).className = e.join(" "),
        n.style.cssText = t.style,
        n.setAttribute("data-option-array-index", t.array_index),
        n.innerHTML = t.search_text,
        this.outerHTML(n)) : ""
    }
    ,
    n.prototype.result_add_group = function(t) {
        var e;
        return (t.search_match || t.group_match) && 0 < t.active_options ? ((e = document.createElement("li")).className = "group-result",
        e.innerHTML = t.search_text,
        this.outerHTML(e)) : ""
    }
    ,
    n.prototype.results_update_field = function() {
        if (this.set_default_text(),
        this.is_multiple || this.results_reset_cleanup(),
        this.result_clear_highlight(),
        this.results_build(),
        this.results_showing)
            return this.winnow_results()
    }
    ,
    n.prototype.reset_single_select_options = function() {
        for (var t, e = this.results_data, n = [], i = 0, s = e.length; i < s; i++)
            (t = e[i]).selected ? n.push(t.selected = !1) : n.push(void 0);
        return n
    }
    ,
    n.prototype.results_toggle = function() {
        return this.results_showing ? this.results_hide() : this.results_show()
    }
    ,
    n.prototype.results_search = function(t) {
        return this.results_showing ? this.winnow_results() : this.results_show()
    }
    ,
    n.prototype.winnow_results = function() {
        var t, e, n, i, s, r, o, a, l, c, u, d, h;
        for (this.no_results_clear(),
        s = 0,
        t = (o = this.get_search_text()).replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"),
        i = this.search_contains ? "" : "^",
        n = new RegExp(i + t,"i"),
        c = new RegExp(t,"i"),
        u = 0,
        d = (h = this.results_data).length; u < d; u++)
            (e = h[u]).search_match = !1,
            r = null,
            this.include_option_in_results(e) && (e.group && (e.group_match = !1,
            e.active_options = 0),
            null != e.group_array_index && this.results_data[e.group_array_index] && (0 === (r = this.results_data[e.group_array_index]).active_options && r.search_match && (s += 1),
            r.active_options += 1),
            e.group && !this.group_search || (e.search_text = e.group ? e.label : e.html,
            e.search_match = this.search_string_match(e.search_text, n),
            e.search_match && !e.group && (s += 1),
            e.search_match ? (o.length && (a = e.search_text.search(c),
            l = e.search_text.substr(0, a + o.length) + "</em>" + e.search_text.substr(a + o.length),
            e.search_text = l.substr(0, a) + "<em>" + l.substr(a)),
            null != r && (r.group_match = !0)) : null != e.group_array_index && this.results_data[e.group_array_index].search_match && (e.search_match = !0)));
        return this.result_clear_highlight(),
        s < 1 && o.length ? (this.update_results_content(""),
        this.no_results(o)) : (this.update_results_content(this.results_option_build()),
        this.winnow_results_set_highlight())
    }
    ,
    n.prototype.search_string_match = function(t, e) {
        var n, i, s, r;
        if (e.test(t))
            return !0;
        if (this.enable_split_word_search && (0 <= t.indexOf(" ") || 0 === t.indexOf("[")) && (i = t.replace(/\[|\]/g, "").split(" ")).length)
            for (s = 0,
            r = i.length; s < r; s++)
                if (n = i[s],
                e.test(n))
                    return !0
    }
    ,
    n.prototype.choices_count = function() {
        var t, e, n;
        if (null != this.selected_option_count)
            return this.selected_option_count;
        for (t = this.selected_option_count = 0,
        e = (n = this.form_field.options).length; t < e; t++)
            n[t].selected && (this.selected_option_count += 1);
        return this.selected_option_count
    }
    ,
    n.prototype.choices_click = function(t) {
        if (t.preventDefault(),
        !this.results_showing && !this.is_disabled)
            return this.results_show()
    }
    ,
    n.prototype.keyup_checker = function(t) {
        var e = null != (e = t.which) ? e : t.keyCode;
        switch (this.search_field_scale(),
        e) {
        case 8:
            if (this.is_multiple && this.backstroke_length < 1 && 0 < this.choices_count())
                return this.keydown_backstroke();
            if (this.pending_backstroke)
                break;
            return this.result_clear_highlight(),
            this.results_search();
        case 13:
            if (t.preventDefault(),
            this.results_showing)
                return this.result_select(t);
            break;
        case 27:
            return this.results_showing && this.results_hide(),
            !0;
        case 9:
        case 38:
        case 40:
        case 16:
        case 91:
        case 17:
            break;
        default:
            return this.results_search()
        }
    }
    ,
    n.prototype.clipboard_event_checker = function(t) {
        var e = this;
        return setTimeout(function() {
            return e.results_search()
        }, 50)
    }
    ,
    n.prototype.container_width = function() {
        return null != this.options.width ? this.options.width : this.form_field.offsetWidth + "px"
    }
    ,
    n.prototype.include_option_in_results = function(t) {
        return !(this.is_multiple && !this.display_selected_options && t.selected) && (!(!this.display_disabled_options && t.disabled) && !t.empty)
    }
    ,
    n.prototype.search_results_touchstart = function(t) {
        return this.touch_started = !0,
        this.search_results_mouseover(t)
    }
    ,
    n.prototype.search_results_touchmove = function(t) {
        return this.touch_started = !1,
        this.search_results_mouseout(t)
    }
    ,
    n.prototype.search_results_touchend = function(t) {
        if (this.touch_started)
            return this.search_results_mouseup(t)
    }
    ,
    n.prototype.outerHTML = function(t) {
        var e;
        return t.outerHTML || ((e = document.createElement("div")).appendChild(t),
        e.innerHTML)
    }
    ,
    n.browser_is_supported = function() {
        return "Microsoft Internet Explorer" === window.navigator.appName ? 8 <= document.documentMode : !/iP(od|hone)/i.test(window.navigator.userAgent) && (!/Android/i.test(window.navigator.userAgent) || !/Mobile/i.test(window.navigator.userAgent))
    }
    ,
    n.default_multiple_text = "Select Some Options",
    n.default_single_text = "Select an Option",
    n.default_no_result_text = "No results match",
    r = n,
    (a = jQuery).fn.extend({
        chosen: function(i) {
            return r.browser_is_supported() ? this.each(function(t) {
                var e = a(this)
                  , n = e.data("chosen");
                "destroy" === i && n ? n.destroy() : n || e.data("chosen", new s(this,i))
            }) : this
        }
    }),
    s = function() {
        var t, e = s, n = r;
        for (t in n)
            l.call(n, t) && (e[t] = n[t]);
        function i() {
            this.constructor = e
        }
        function s() {
            return s.__super__.constructor.apply(this, arguments)
        }
        return i.prototype = n.prototype,
        e.prototype = new i,
        e.__super__ = n.prototype,
        s.prototype.setup = function() {
            return this.form_field_jq = a(this.form_field),
            this.current_selectedIndex = this.form_field.selectedIndex,
            this.is_rtl = this.form_field_jq.hasClass("chosen-rtl")
        }
        ,
        s.prototype.set_up_html = function() {
            var t = ["chosen-container"];
            return t.push("chosen-container-" + (this.is_multiple ? "multi" : "single")),
            this.inherit_select_classes && this.form_field.className && t.push(this.form_field.className),
            this.is_rtl && t.push("chosen-rtl"),
            t = {
                class: t.join(" "),
                style: "width: " + this.container_width() + ";",
                title: this.form_field.title
            },
            this.form_field.id.length && (t.id = this.form_field.id.replace(/[^\w]/g, "_") + "_chosen"),
            this.container = a("<div />", t),
            this.is_multiple ? this.container.html('<ul class="chosen-choices"><li class="search-field"><input type="text" value="' + this.default_text + '" class="default" autocomplete="off" style="width:25px;" /></li></ul><div class="chosen-drop"><ul class="chosen-results"></ul></div>') : this.container.html('<a class="chosen-single chosen-default" tabindex="-1"><span>' + this.default_text + '</span><div><b></b></div></a><div class="chosen-drop"><div class="chosen-search"><input type="text" autocomplete="off" /></div><ul class="chosen-results"></ul></div>'),
            this.form_field_jq.hide().after(this.container),
            this.dropdown = this.container.find("div.chosen-drop").first(),
            this.search_field = this.container.find("input").first(),
            this.search_results = this.container.find("ul.chosen-results").first(),
            this.search_field_scale(),
            this.search_no_results = this.container.find("li.no-results").first(),
            this.is_multiple ? (this.search_choices = this.container.find("ul.chosen-choices").first(),
            this.search_container = this.container.find("li.search-field").first()) : (this.search_container = this.container.find("div.chosen-search").first(),
            this.selected_item = this.container.find(".chosen-single").first()),
            this.results_build(),
            this.set_tab_index(),
            this.set_label_behavior(),
            this.form_field_jq.trigger("chosen:ready", {
                chosen: this
            })
        }
        ,
        s.prototype.register_observers = function() {
            var e = this;
            return this.container.bind("mousedown.chosen", function(t) {
                e.container_mousedown(t)
            }),
            this.container.bind("mouseup.chosen", function(t) {
                e.container_mouseup(t)
            }),
            this.container.bind("mouseenter.chosen", function(t) {
                e.mouse_enter(t)
            }),
            this.container.bind("mouseleave.chosen", function(t) {
                e.mouse_leave(t)
            }),
            this.search_results.bind("mouseup.chosen", function(t) {
                e.search_results_mouseup(t)
            }),
            this.search_results.bind("mouseover.chosen", function(t) {
                e.search_results_mouseover(t)
            }),
            this.search_results.bind("mouseout.chosen", function(t) {
                e.search_results_mouseout(t)
            }),
            this.search_results.bind("mousewheel.chosen DOMMouseScroll.chosen", function(t) {
                e.search_results_mousewheel(t)
            }),
            this.search_results.bind("touchstart.chosen", function(t) {
                e.search_results_touchstart(t)
            }),
            this.search_results.bind("touchmove.chosen", function(t) {
                e.search_results_touchmove(t)
            }),
            this.search_results.bind("touchend.chosen", function(t) {
                e.search_results_touchend(t)
            }),
            this.form_field_jq.bind("chosen:updated.chosen", function(t) {
                e.results_update_field(t)
            }),
            this.form_field_jq.bind("chosen:activate.chosen", function(t) {
                e.activate_field(t)
            }),
            this.form_field_jq.bind("chosen:open.chosen", function(t) {
                e.container_mousedown(t)
            }),
            this.form_field_jq.bind("chosen:close.chosen", function(t) {
                e.input_blur(t)
            }),
            this.search_field.bind("blur.chosen", function(t) {
                e.input_blur(t)
            }),
            this.search_field.bind("keyup.chosen", function(t) {
                e.keyup_checker(t)
            }),
            this.search_field.bind("keydown.chosen", function(t) {
                e.keydown_checker(t)
            }),
            this.search_field.bind("focus.chosen", function(t) {
                e.input_focus(t)
            }),
            this.search_field.bind("cut.chosen", function(t) {
                e.clipboard_event_checker(t)
            }),
            this.search_field.bind("paste.chosen", function(t) {
                e.clipboard_event_checker(t)
            }),
            this.is_multiple ? this.search_choices.bind("click.chosen", function(t) {
                e.choices_click(t)
            }) : this.container.bind("click.chosen", function(t) {
                t.preventDefault()
            })
        }
        ,
        s.prototype.destroy = function() {
            return a(this.container[0].ownerDocument).unbind("click.chosen", this.click_test_action),
            this.search_field[0].tabIndex && (this.form_field_jq[0].tabIndex = this.search_field[0].tabIndex),
            this.container.remove(),
            this.form_field_jq.removeData("chosen"),
            this.form_field_jq.show()
        }
        ,
        s.prototype.search_field_disabled = function() {
            return this.is_disabled = this.form_field_jq[0].disabled,
            this.is_disabled ? (this.container.addClass("chosen-disabled"),
            this.search_field[0].disabled = !0,
            this.is_multiple || this.selected_item.unbind("focus.chosen", this.activate_action),
            this.close_field()) : (this.container.removeClass("chosen-disabled"),
            this.search_field[0].disabled = !1,
            this.is_multiple ? void 0 : this.selected_item.bind("focus.chosen", this.activate_action))
        }
        ,
        s.prototype.container_mousedown = function(t) {
            if (!this.is_disabled && (t && "mousedown" === t.type && !this.results_showing && t.preventDefault(),
            null == t || !a(t.target).hasClass("search-choice-close")))
                return this.active_field ? this.is_multiple || !t || a(t.target)[0] !== this.selected_item[0] && !a(t.target).parents("a.chosen-single").length || (t.preventDefault(),
                this.results_toggle()) : (this.is_multiple && this.search_field.val(""),
                a(this.container[0].ownerDocument).bind("click.chosen", this.click_test_action),
                this.results_show()),
                this.activate_field()
        }
        ,
        s.prototype.container_mouseup = function(t) {
            if ("ABBR" === t.target.nodeName && !this.is_disabled)
                return this.results_reset(t)
        }
        ,
        s.prototype.search_results_mousewheel = function(t) {
            var e;
            if (null != (e = t.originalEvent ? -t.originalEvent.wheelDelta || t.originalEvent.detail : e))
                return t.preventDefault(),
                "DOMMouseScroll" === t.type && (e *= 40),
                this.search_results.scrollTop(e + this.search_results.scrollTop())
        }
        ,
        s.prototype.blur_test = function(t) {
            if (!this.active_field && this.container.hasClass("chosen-container-active"))
                return this.close_field()
        }
        ,
        s.prototype.close_field = function() {
            return a(this.container[0].ownerDocument).unbind("click.chosen", this.click_test_action),
            this.active_field = !1,
            this.results_hide(),
            this.container.removeClass("chosen-container-active"),
            this.clear_backstroke(),
            this.show_search_field_default(),
            this.search_field_scale()
        }
        ,
        s.prototype.activate_field = function() {
            return this.container.addClass("chosen-container-active"),
            this.active_field = !0,
            this.search_field.val(this.search_field.val()),
            this.search_field.focus()
        }
        ,
        s.prototype.test_active_click = function(t) {
            t = a(t.target).closest(".chosen-container");
            return t.length && this.container[0] === t[0] ? this.active_field = !0 : this.close_field()
        }
        ,
        s.prototype.results_build = function() {
            return this.parsing = !0,
            this.selected_option_count = null,
            this.results_data = o.select_to_array(this.form_field),
            this.is_multiple ? this.search_choices.find("li.search-choice").remove() : this.is_multiple || (this.single_set_selected_text(),
            this.disable_search || this.form_field.options.length <= this.disable_search_threshold ? (this.search_field[0].readOnly = !0,
            this.container.addClass("chosen-container-single-nosearch")) : (this.search_field[0].readOnly = !1,
            this.container.removeClass("chosen-container-single-nosearch"))),
            this.update_results_content(this.results_option_build({
                first: !0
            })),
            this.search_field_disabled(),
            this.show_search_field_default(),
            this.search_field_scale(),
            this.parsing = !1
        }
        ,
        s.prototype.result_do_highlight = function(t) {
            var e, n, i;
            if (t.length)
                return this.result_clear_highlight(),
                this.result_highlight = t,
                this.result_highlight.addClass("highlighted"),
                (t = parseInt(this.search_results.css("maxHeight"), 10)) + (i = this.search_results.scrollTop()) <= (e = (n = this.result_highlight.position().top + this.search_results.scrollTop()) + this.result_highlight.outerHeight()) ? this.search_results.scrollTop(0 < e - t ? e - t : 0) : n < i ? this.search_results.scrollTop(n) : void 0
        }
        ,
        s.prototype.result_clear_highlight = function() {
            return this.result_highlight && this.result_highlight.removeClass("highlighted"),
            this.result_highlight = null
        }
        ,
        s.prototype.results_show = function() {
            return this.is_multiple && this.max_selected_options <= this.choices_count() ? (this.form_field_jq.trigger("chosen:maxselected", {
                chosen: this
            }),
            !1) : (this.container.addClass("chosen-with-drop"),
            this.results_showing = !0,
            this.search_field.focus(),
            this.search_field.val(this.search_field.val()),
            this.winnow_results(),
            this.form_field_jq.trigger("chosen:showing_dropdown", {
                chosen: this
            }))
        }
        ,
        s.prototype.update_results_content = function(t) {
            return this.search_results.html(t)
        }
        ,
        s.prototype.results_hide = function() {
            return this.results_showing && (this.result_clear_highlight(),
            this.container.removeClass("chosen-with-drop"),
            this.form_field_jq.trigger("chosen:hiding_dropdown", {
                chosen: this
            })),
            this.results_showing = !1
        }
        ,
        s.prototype.set_tab_index = function(t) {
            var e;
            if (this.form_field.tabIndex)
                return e = this.form_field.tabIndex,
                this.form_field.tabIndex = -1,
                this.search_field[0].tabIndex = e
        }
        ,
        s.prototype.set_label_behavior = function() {
            var e = this;
            if (this.form_field_label = this.form_field_jq.parents("label"),
            !this.form_field_label.length && this.form_field.id.length && (this.form_field_label = a("label[for='" + this.form_field.id + "']")),
            0 < this.form_field_label.length)
                return this.form_field_label.bind("click.chosen", function(t) {
                    return e.is_multiple ? e.container_mousedown(t) : e.activate_field()
                })
        }
        ,
        s.prototype.show_search_field_default = function() {
            return this.is_multiple && this.choices_count() < 1 && !this.active_field ? (this.search_field.val(this.default_text),
            this.search_field.addClass("default")) : (this.search_field.val(""),
            this.search_field.removeClass("default"))
        }
        ,
        s.prototype.search_results_mouseup = function(t) {
            var e = a(t.target).hasClass("active-result") ? a(t.target) : a(t.target).parents(".active-result").first();
            if (e.length)
                return this.result_highlight = e,
                this.result_select(t),
                this.search_field.focus()
        }
        ,
        s.prototype.search_results_mouseover = function(t) {
            t = a(t.target).hasClass("active-result") ? a(t.target) : a(t.target).parents(".active-result").first();
            if (t)
                return this.result_do_highlight(t)
        }
        ,
        s.prototype.search_results_mouseout = function(t) {
            if (a(t.target).hasClass("active-result"))
                return this.result_clear_highlight()
        }
        ,
        s.prototype.choice_build = function(t) {
            var e = this
              , n = a("<li />", {
                class: "search-choice"
            }).html("<span>" + t.html + "</span>");
            return t.disabled ? n.addClass("search-choice-disabled") : ((t = a("<a />", {
                class: "search-choice-close",
                "data-option-array-index": t.array_index
            })).bind("click.chosen", function(t) {
                return e.choice_destroy_link_click(t)
            }),
            n.append(t)),
            this.search_container.before(n)
        }
        ,
        s.prototype.choice_destroy_link_click = function(t) {
            if (t.preventDefault(),
            t.stopPropagation(),
            !this.is_disabled)
                return this.choice_destroy(a(t.target))
        }
        ,
        s.prototype.choice_destroy = function(t) {
            if (this.result_deselect(t[0].getAttribute("data-option-array-index")))
                return this.show_search_field_default(),
                this.is_multiple && 0 < this.choices_count() && this.search_field.val().length < 1 && this.results_hide(),
                t.parents("li").first().remove(),
                this.search_field_scale()
        }
        ,
        s.prototype.results_reset = function() {
            if (this.reset_single_select_options(),
            this.form_field.options[0].selected = !0,
            this.single_set_selected_text(),
            this.show_search_field_default(),
            this.results_reset_cleanup(),
            this.form_field_jq.trigger("change"),
            this.active_field)
                return this.results_hide()
        }
        ,
        s.prototype.results_reset_cleanup = function() {
            return this.current_selectedIndex = this.form_field.selectedIndex,
            this.selected_item.find("abbr").remove()
        }
        ,
        s.prototype.result_select = function(t) {
            var e;
            if (this.result_highlight)
                return e = this.result_highlight,
                this.result_clear_highlight(),
                this.is_multiple && this.max_selected_options <= this.choices_count() ? (this.form_field_jq.trigger("chosen:maxselected", {
                    chosen: this
                }),
                !1) : (this.is_multiple ? e.removeClass("active-result") : this.reset_single_select_options(),
                (e = this.results_data[e[0].getAttribute("data-option-array-index")]).selected = !0,
                this.form_field.options[e.options_index].selected = !0,
                this.selected_option_count = null,
                this.is_multiple ? this.choice_build(e) : this.single_set_selected_text(e.text),
                (t.metaKey || t.ctrlKey) && this.is_multiple || this.results_hide(),
                this.search_field.val(""),
                !this.is_multiple && this.form_field.selectedIndex === this.current_selectedIndex || this.form_field_jq.trigger("change", {
                    selected: this.form_field.options[e.options_index].value
                }),
                this.current_selectedIndex = this.form_field.selectedIndex,
                this.search_field_scale())
        }
        ,
        s.prototype.single_set_selected_text = function(t) {
            return (t = null == t ? this.default_text : t) === this.default_text ? this.selected_item.addClass("chosen-default") : (this.single_deselect_control_build(),
            this.selected_item.removeClass("chosen-default")),
            this.selected_item.find("span").text(t)
        }
        ,
        s.prototype.result_deselect = function(t) {
            t = this.results_data[t];
            return !this.form_field.options[t.options_index].disabled && (t.selected = !1,
            this.form_field.options[t.options_index].selected = !1,
            this.selected_option_count = null,
            this.result_clear_highlight(),
            this.results_showing && this.winnow_results(),
            this.form_field_jq.trigger("change", {
                deselected: this.form_field.options[t.options_index].value
            }),
            this.search_field_scale(),
            !0)
        }
        ,
        s.prototype.single_deselect_control_build = function() {
            if (this.allow_single_deselect)
                return this.selected_item.find("abbr").length || this.selected_item.find("span").first().after('<abbr class="search-choice-close"></abbr>'),
                this.selected_item.addClass("chosen-single-with-deselect")
        }
        ,
        s.prototype.get_search_text = function() {
            return this.search_field.val() === this.default_text ? "" : a("<div/>").text(a.trim(this.search_field.val())).html()
        }
        ,
        s.prototype.winnow_results_set_highlight = function() {
            var t = this.is_multiple ? [] : this.search_results.find(".result-selected.active-result")
              , t = (t.length ? t : this.search_results.find(".active-result")).first();
            if (null != t)
                return this.result_do_highlight(t)
        }
        ,
        s.prototype.no_results = function(t) {
            var e = a('<li class="no-results">' + this.results_none_found + ' "<span></span>"</li>');
            return e.find("span").first().html(t),
            this.search_results.append(e),
            this.form_field_jq.trigger("chosen:no_results", {
                chosen: this
            })
        }
        ,
        s.prototype.no_results_clear = function() {
            return this.search_results.find(".no-results").remove()
        }
        ,
        s.prototype.keydown_arrow = function() {
            var t;
            return this.results_showing && this.result_highlight ? (t = this.result_highlight.nextAll("li.active-result").first()) ? this.result_do_highlight(t) : void 0 : this.results_show()
        }
        ,
        s.prototype.keyup_arrow = function() {
            var t;
            return this.results_showing || this.is_multiple ? this.result_highlight ? (t = this.result_highlight.prevAll("li.active-result")).length ? this.result_do_highlight(t.first()) : (0 < this.choices_count() && this.results_hide(),
            this.result_clear_highlight()) : void 0 : this.results_show()
        }
        ,
        s.prototype.keydown_backstroke = function() {
            var t;
            return this.pending_backstroke ? (this.choice_destroy(this.pending_backstroke.find("a").first()),
            this.clear_backstroke()) : (t = this.search_container.siblings("li.search-choice").last()).length && !t.hasClass("search-choice-disabled") ? (this.pending_backstroke = t,
            this.single_backstroke_delete ? this.keydown_backstroke() : this.pending_backstroke.addClass("search-choice-focus")) : void 0
        }
        ,
        s.prototype.clear_backstroke = function() {
            return this.pending_backstroke && this.pending_backstroke.removeClass("search-choice-focus"),
            this.pending_backstroke = null
        }
        ,
        s.prototype.keydown_checker = function(t) {
            var e = null != (e = t.which) ? e : t.keyCode;
            switch (this.search_field_scale(),
            8 !== e && this.pending_backstroke && this.clear_backstroke(),
            e) {
            case 8:
                this.backstroke_length = this.search_field.val().length;
                break;
            case 9:
                this.results_showing && !this.is_multiple && this.result_select(t),
                this.mouse_on_container = !1;
                break;
            case 13:
                t.preventDefault();
                break;
            case 38:
                t.preventDefault(),
                this.keyup_arrow();
                break;
            case 40:
                t.preventDefault(),
                this.keydown_arrow()
            }
        }
        ,
        s.prototype.search_field_scale = function() {
            var t, e, n, i, s, r, o;
            if (this.is_multiple) {
                for (n = "position:absolute; left: -1000px; top: -1000px; display:none;",
                r = s = 0,
                o = (i = ["font-size", "font-style", "font-weight", "font-family", "line-height", "text-transform", "letter-spacing"]).length; r < o; r++)
                    n += (e = i[r]) + ":" + this.search_field.css(e) + ";";
                return (t = a("<div />", {
                    style: n
                })).text(this.search_field.val()),
                a("body").append(t),
                s = t.width() + 25,
                t.remove(),
                t = this.container.outerWidth(),
                this.search_field.css({
                    width: (s = t - 10 < s ? t - 10 : s) + "px"
                })
            }
        }
        ,
        s
    }()
}
.call(this),
function(e, n) {
    "function" == typeof define && define.amd ? define("bloodhound", ["jquery"], function(t) {
        return e.Bloodhound = n()
    }) : "object" == typeof exports ? module.exports = n(require("jquery")) : e.Bloodhound = n(jQuery)
}(this, function() {
    var u = function() {
        "use strict";
        return {
            isMsie: function() {
                return !!/(msie|trident)/i.test(navigator.userAgent) && navigator.userAgent.match(/(msie |rv:)(\d+(.\d+)?)/i)[2]
            },
            isBlankString: function(t) {
                return !t || /^\s*$/.test(t)
            },
            escapeRegExChars: function(t) {
                return t.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&")
            },
            isString: function(t) {
                return "string" == typeof t
            },
            isNumber: function(t) {
                return "number" == typeof t
            },
            isArray: $.isArray,
            isFunction: $.isFunction,
            isObject: $.isPlainObject,
            isUndefined: function(t) {
                return void 0 === t
            },
            isElement: function(t) {
                return !(!t || 1 !== t.nodeType)
            },
            isJQuery: function(t) {
                return t instanceof $
            },
            toStr: function(t) {
                return u.isUndefined(t) || null === t ? "" : t + ""
            },
            bind: $.proxy,
            each: function(t, n) {
                $.each(t, function(t, e) {
                    return n(e, t)
                })
            },
            map: $.map,
            filter: $.grep,
            every: function(n, i) {
                var s = !0;
                return n ? ($.each(n, function(t, e) {
                    if (!(s = i.call(null, e, t, n)))
                        return !1
                }),
                !!s) : s
            },
            some: function(n, i) {
                var s = !1;
                return n ? ($.each(n, function(t, e) {
                    if (s = i.call(null, e, t, n))
                        return !1
                }),
                !!s) : s
            },
            mixin: $.extend,
            identity: function(t) {
                return t
            },
            clone: function(t) {
                return $.extend(!0, {}, t)
            },
            getIdGenerator: function() {
                var t = 0;
                return function() {
                    return t++
                }
            },
            templatify: function(t) {
                return $.isFunction(t) ? t : function() {
                    return String(t)
                }
            },
            defer: function(t) {
                setTimeout(t, 0)
            },
            debounce: function(s, r, o) {
                var a, l;
                return function() {
                    var t = this
                      , e = arguments
                      , n = function() {
                        a = null,
                        o || (l = s.apply(t, e))
                    }
                      , i = o && !a;
                    return clearTimeout(a),
                    a = setTimeout(n, r),
                    l = i ? s.apply(t, e) : l
                }
            },
            throttle: function(n, i) {
                var s, r, o, a, l = 0, c = function() {
                    l = new Date,
                    o = null,
                    a = n.apply(s, r)
                };
                return function() {
                    var t = new Date
                      , e = i - (t - l);
                    return s = this,
                    r = arguments,
                    e <= 0 ? (clearTimeout(o),
                    o = null,
                    l = t,
                    a = n.apply(s, r)) : o = o || setTimeout(c, e),
                    a
                }
            },
            stringify: function(t) {
                return u.isString(t) ? t : JSON.stringify(t)
            },
            noop: function() {}
        }
    }()
      , n = function() {
        "use strict";
        return {
            nonword: e,
            whitespace: t,
            obj: {
                nonword: n(e),
                whitespace: n(t)
            }
        };
        function t(t) {
            return (t = u.toStr(t)) ? t.split(/\s+/) : []
        }
        function e(t) {
            return (t = u.toStr(t)) ? t.split(/\W+/) : []
        }
        function n(i) {
            return function() {
                var t = [].slice.call(arguments, 0);
                return function(e) {
                    var n = [];
                    return u.each(t, function(t) {
                        n = n.concat(i(u.toStr(e[t])))
                    }),
                    n
                }
            }
        }
    }()
      , i = function() {
        "use strict";
        function t(t) {
            this.maxSize = u.isNumber(t) ? t : 100,
            this.reset(),
            this.maxSize <= 0 && (this.set = this.get = $.noop)
        }
        function e() {
            this.head = this.tail = null
        }
        function i(t, e) {
            this.key = t,
            this.val = e,
            this.prev = this.next = null
        }
        return u.mixin(t.prototype, {
            set: function(t, e) {
                var n = this.list.tail;
                this.size >= this.maxSize && (this.list.remove(n),
                delete this.hash[n.key],
                this.size--),
                (n = this.hash[t]) ? (n.val = e,
                this.list.moveToFront(n)) : (n = new i(t,e),
                this.list.add(n),
                this.hash[t] = n,
                this.size++)
            },
            get: function(t) {
                t = this.hash[t];
                if (t)
                    return this.list.moveToFront(t),
                    t.val
            },
            reset: function() {
                this.size = 0,
                this.hash = {},
                this.list = new e
            }
        }),
        u.mixin(e.prototype, {
            add: function(t) {
                this.head && (t.next = this.head,
                this.head.prev = t),
                this.head = t,
                this.tail = this.tail || t
            },
            remove: function(t) {
                t.prev ? t.prev.next = t.next : this.head = t.next,
                t.next ? t.next.prev = t.prev : this.tail = t.prev
            },
            moveToFront: function(t) {
                this.remove(t),
                this.add(t)
            }
        }),
        t
    }()
      , e = function() {
        "use strict";
        var n;
        try {
            (n = window.localStorage).setItem("~~~", "!"),
            n.removeItem("~~~")
        } catch (t) {
            n = null
        }
        function t(t, e) {
            this.prefix = ["__", t, "__"].join(""),
            this.ttlKey = "__ttl__",
            this.keyMatcher = new RegExp("^" + u.escapeRegExChars(this.prefix)),
            this.ls = e || n,
            this.ls && window.JSON || (this.get = this.set = this.remove = this.clear = this.isExpired = u.noop)
        }
        return u.mixin(t.prototype, {
            _prefix: function(t) {
                return this.prefix + t
            },
            _ttlKey: function(t) {
                return this._prefix(t) + this.ttlKey
            },
            get: function(t) {
                return this.isExpired(t) && this.remove(t),
                e(this.ls.getItem(this._prefix(t)))
            },
            set: function(t, e, n) {
                return u.isNumber(n) ? this.ls.setItem(this._ttlKey(t), s(i() + n)) : this.ls.removeItem(this._ttlKey(t)),
                this.ls.setItem(this._prefix(t), s(e))
            },
            remove: function(t) {
                return this.ls.removeItem(this._ttlKey(t)),
                this.ls.removeItem(this._prefix(t)),
                this
            },
            clear: function() {
                for (var t, e = [], n = this.ls.length, i = 0; i < n; i++)
                    (t = this.ls.key(i)).match(this.keyMatcher) && e.push(t.replace(this.keyMatcher, ""));
                for (i = e.length; i--; )
                    this.remove(e[i]);
                return this
            },
            isExpired: function(t) {
                t = e(this.ls.getItem(this._ttlKey(t)));
                return !!(u.isNumber(t) && i() > t)
            }
        }),
        t;
        function i() {
            return (new Date).getTime()
        }
        function s(t) {
            return JSON.stringify(u.isUndefined(t) ? null : t)
        }
        function e(t) {
            return $.parseJSON(t)
        }
    }()
      , s = function() {
        "use strict";
        var a = 0
          , l = {}
          , c = 6
          , e = new i(10);
        function t(t) {
            t = t || {},
            this.cancelled = !1,
            this.lastReq = null,
            this._send = t.transport,
            this._get = t.limiter ? t.limiter(this._get) : this._get,
            this._cache = !1 === t.cache ? new i(0) : e
        }
        return t.setMaxPendingRequests = function(t) {
            c = t
        }
        ,
        t.resetCache = function() {
            e.reset()
        }
        ,
        u.mixin(t.prototype, {
            _fingerprint: function(t) {
                return (t = t || {}).url + t.type + $.param(t.data || {})
            },
            _get: function(t, e) {
                var n, i, s = this;
                function r(t) {
                    e(null, t),
                    s._cache.set(n, t)
                }
                function o() {
                    e(!0)
                }
                n = this._fingerprint(t),
                this.cancelled || n !== this.lastReq || ((i = l[n]) ? i.done(r).fail(o) : a < c ? (a++,
                l[n] = this._send(t).done(r).fail(o).always(function() {
                    a--,
                    delete l[n],
                    s.onDeckRequestArgs && (s._get.apply(s, s.onDeckRequestArgs),
                    s.onDeckRequestArgs = null)
                })) : this.onDeckRequestArgs = [].slice.call(arguments, 0))
            },
            get: function(t, e) {
                var n;
                e = e || $.noop,
                t = u.isString(t) ? {
                    url: t
                } : t || {},
                n = this._fingerprint(t),
                this.cancelled = !1,
                this.lastReq = n,
                (n = this._cache.get(n)) ? e(null, n) : this._get(t, e)
            },
            cancel: function() {
                this.cancelled = !0
            }
        }),
        t
    }()
      , r = window.SearchIndex = function() {
        "use strict";
        function t(t) {
            (t = t || {}).datumTokenizer && t.queryTokenizer || $.error("datumTokenizer and queryTokenizer are both required"),
            this.identify = t.identify || u.stringify,
            this.datumTokenizer = t.datumTokenizer,
            this.queryTokenizer = t.queryTokenizer,
            this.reset()
        }
        return u.mixin(t.prototype, {
            bootstrap: function(t) {
                this.datums = t.datums,
                this.trie = t.trie
            },
            add: function(t) {
                var r = this;
                t = u.isArray(t) ? t : [t],
                u.each(t, function(t) {
                    var s;
                    r.datums[s = r.identify(t)] = t,
                    t = e(r.datumTokenizer(t)),
                    u.each(t, function(t) {
                        for (var e, n = r.trie, i = t.split(""); e = i.shift(); )
                            (n = n.c[e] || (n.c[e] = o())).i.push(s)
                    })
                })
            },
            get: function(t) {
                var e = this;
                return u.map(t, function(t) {
                    return e.datums[t]
                })
            },
            search: function(t) {
                var s, r = this, t = e(this.queryTokenizer(t));
                return u.each(t, function(t) {
                    var e, n, i;
                    if (s && 0 === s.length)
                        return !1;
                    for (e = r.trie,
                    n = t.split(""); e && (i = n.shift()); )
                        e = e.c[i];
                    if (!e || 0 !== n.length)
                        return !(s = []);
                    t = e.i.slice(0),
                    s = s ? function(t, e) {
                        var n = 0
                          , i = 0
                          , s = []
                          , r = (t = t.sort(a),
                        e = e.sort(a),
                        t.length)
                          , o = e.length;
                        for (; n < r && i < o; )
                            t[n] < e[i] ? n++ : (t[n] > e[i] || (s.push(t[n]),
                            n++),
                            i++);
                        return s;
                        function a(t, e) {
                            return t - e
                        }
                    }(s, t) : t
                }),
                s ? u.map(function(t) {
                    for (var e = {}, n = [], i = 0, s = t.length; i < s; i++)
                        e[t[i]] || (e[t[i]] = !0,
                        n.push(t[i]));
                    return n
                }(s), function(t) {
                    return r.datums[t]
                }) : []
            },
            all: function() {
                var t, e = [];
                for (t in this.datums)
                    e.push(this.datums[t]);
                return e
            },
            reset: function() {
                this.datums = {},
                this.trie = o()
            },
            serialize: function() {
                return {
                    datums: this.datums,
                    trie: this.trie
                }
            }
        }),
        t;
        function e(t) {
            return t = u.filter(t, function(t) {
                return !!t
            }),
            t = u.map(t, function(t) {
                return t.toLowerCase()
            })
        }
        function o() {
            var t = {
                i: [],
                c: {}
            };
            return t
        }
    }()
      , o = function() {
        "use strict";
        var n;
        function t(t) {
            this.url = t.url,
            this.ttl = t.ttl,
            this.cache = t.cache,
            this.transform = t.transform,
            this.transport = t.transport,
            this.thumbprint = t.thumbprint,
            this.storage = new e(t.cacheKey)
        }
        return n = {
            data: "data",
            protocol: "protocol",
            thumbprint: "thumbprint"
        },
        u.mixin(t.prototype, {
            _settings: function() {
                return {
                    url: this.url,
                    type: "GET",
                    dataType: "json"
                }
            },
            store: function(t) {
                this.cache && (this.storage.set(n.data, t, this.ttl),
                this.storage.set(n.protocol, location.protocol, this.ttl),
                this.storage.set(n.thumbprint, this.thumbprint, this.ttl))
            },
            fromCache: function() {
                var t, e = {};
                return this.cache ? (e.data = this.storage.get(n.data),
                e.protocol = this.storage.get(n.protocol),
                e.thumbprint = this.storage.get(n.thumbprint),
                t = e.thumbprint !== this.thumbprint || e.protocol !== location.protocol,
                e.data && !t ? e.data : null) : null
            },
            fromNetwork: function(e) {
                var n = this;
                e && this.transport(this._settings()).fail(function() {
                    e(!0)
                }).done(function(t) {
                    e(null, n.transform(t))
                })
            },
            clear: function() {
                return this.storage.clear(),
                this
            }
        }),
        t
    }()
      , a = function() {
        "use strict";
        function t(t) {
            this.url = t.url,
            this.prepare = t.prepare,
            this.transform = t.transform,
            this.transport = new s({
                cache: t.cache,
                limiter: t.limiter,
                transport: t.transport
            })
        }
        return u.mixin(t.prototype, {
            _settings: function() {
                return {
                    url: this.url,
                    type: "GET",
                    dataType: "json"
                }
            },
            get: function(t, n) {
                var i = this;
                if (n)
                    return t = this.prepare(t = t || "", this._settings()),
                    this.transport.get(t, function(t, e) {
                        n(t ? [] : i.transform(e))
                    })
            },
            cancelLastRequest: function() {
                this.transport.cancel()
            }
        }),
        t
    }()
      , l = function() {
        "use strict";
        return function(t) {
            var e, n, i = {
                initialize: !0,
                identify: u.stringify,
                datumTokenizer: null,
                queryTokenizer: null,
                sufficient: 5,
                sorter: null,
                local: [],
                prefetch: null,
                remote: null
            };
            return (t = u.mixin(i, t || {})).datumTokenizer || $.error("datumTokenizer is required"),
            t.queryTokenizer || $.error("queryTokenizer is required"),
            e = t.sorter,
            t.sorter = e ? function(t) {
                return t.sort(e)
            }
            : u.identity,
            t.local = u.isFunction(t.local) ? t.local() : t.local,
            t.prefetch = (i = t.prefetch) ? (n = {
                url: null,
                ttl: 864e5,
                cache: !0,
                cacheKey: null,
                thumbprint: "",
                transform: u.identity,
                transport: null
            },
            i = u.isString(i) ? {
                url: i
            } : i,
            (i = u.mixin(n, i)).url || $.error("prefetch requires url to be set"),
            i.transform = i.filter || i.transform,
            i.cacheKey = i.cacheKey || i.url,
            i.thumbprint = "0.11.0-templating" + i.thumbprint,
            i.transport = i.transport ? s(i.transport) : $.ajax,
            i) : null,
            t.remote = function(t) {
                var e;
                if (t)
                    return e = {
                        url: null,
                        cache: !0,
                        prepare: null,
                        replace: null,
                        wildcard: null,
                        limiter: null,
                        rateLimitBy: "debounce",
                        rateLimitWait: 300,
                        transform: u.identity,
                        transport: null
                    },
                    t = u.isString(t) ? {
                        url: t
                    } : t,
                    (t = u.mixin(e, t)).url || $.error("remote requires url to be set"),
                    t.transform = t.filter || t.transform,
                    t.prepare = function(t) {
                        var e, n, i;
                        if (e = t.prepare,
                        n = t.replace,
                        i = t.wildcard,
                        e)
                            return e;
                        e = n ? function(t, e) {
                            return e.url = n(e.url, t),
                            e
                        }
                        : t.wildcard ? function(t, e) {
                            return e.url = e.url.replace(i, encodeURIComponent(t)),
                            e
                        }
                        : function(t, e) {
                            return e
                        }
                        ;
                        return e
                    }(t),
                    t.limiter = function(t) {
                        var e, n;
                        e = t.limiter,
                        n = t.rateLimitBy,
                        t = t.rateLimitWait,
                        e = e || (/^throttle$/i.test(n) ? function(e) {
                            return function(t) {
                                return u.throttle(t, e)
                            }
                        }
                        : function(e) {
                            return function(t) {
                                return u.debounce(t, e)
                            }
                        }
                        )(t);
                        return e
                    }(t),
                    t.transport = t.transport ? s(t.transport) : $.ajax,
                    delete t.replace,
                    delete t.wildcard,
                    delete t.rateLimitBy,
                    delete t.rateLimitWait,
                    t
            }(t.remote),
            t
        }
        ;
        function s(n) {
            return function(t) {
                var e = $.Deferred();
                return n(t, function(t) {
                    u.defer(function() {
                        e.resolve(t)
                    })
                }, function(t) {
                    u.defer(function() {
                        e.reject(t)
                    })
                }),
                e
            }
        }
    }();
    return function() {
        "use strict";
        var t;
        function e(t) {
            t = l(t),
            this.sorter = t.sorter,
            this.identify = t.identify,
            this.sufficient = t.sufficient,
            this.local = t.local,
            this.remote = t.remote ? new a(t.remote) : null,
            this.prefetch = t.prefetch ? new o(t.prefetch) : null,
            this.index = new r({
                identify: this.identify,
                datumTokenizer: t.datumTokenizer,
                queryTokenizer: t.queryTokenizer
            }),
            !1 !== t.initialize && this.initialize()
        }
        return t = window && window.Bloodhound,
        e.noConflict = function() {
            return window && (window.Bloodhound = t),
            e
        }
        ,
        e.tokenizers = n,
        u.mixin(e.prototype, {
            __ttAdapter: function() {
                var i = this;
                return this.remote ? function(t, e, n) {
                    return i.search(t, e, n)
                }
                : function(t, e) {
                    return i.search(t, e)
                }
            },
            _loadPrefetch: function() {
                var t, n = this, i = $.Deferred();
                return this.prefetch ? (t = this.prefetch.fromCache()) ? (this.index.bootstrap(t),
                i.resolve()) : this.prefetch.fromNetwork(function(t, e) {
                    if (t)
                        return i.reject();
                    n.add(e),
                    n.prefetch.store(n.index.serialize()),
                    i.resolve()
                }) : i.resolve(),
                i.promise()
            },
            _initialize: function() {
                var t = this;
                return this.clear(),
                (this.initPromise = this._loadPrefetch()).done(function() {
                    t.add(t.local)
                }),
                this.initPromise
            },
            initialize: function(t) {
                return !this.initPromise || t ? this._initialize() : this.initPromise
            },
            add: function(t) {
                return this.index.add(t),
                this
            },
            get: function(t) {
                return t = u.isArray(t) ? t : [].slice.call(arguments),
                this.index.get(t)
            },
            search: function(t, e, i) {
                var s = this
                  , r = this.sorter(this.index.search(t));
                return e(this.remote ? r.slice() : r),
                this.remote && r.length < this.sufficient ? this.remote.get(t, function(t) {
                    var n = [];
                    u.each(t, function(e) {
                        u.some(r, function(t) {
                            return s.identify(e) === s.identify(t)
                        }) || n.push(e)
                    }),
                    i && i(n)
                }) : this.remote && this.remote.cancelLastRequest(),
                this
            },
            all: function() {
                return this.index.all()
            },
            clear: function() {
                return this.index.reset(),
                this
            },
            clearPrefetchCache: function() {
                return this.prefetch && this.prefetch.clear(),
                this
            },
            clearRemoteCache: function() {
                return s.resetCache(),
                this
            },
            ttAdapter: function() {
                return this.__ttAdapter()
            }
        }),
        e
    }()
}),
function(e) {
    "function" == typeof define && define.amd ? define("typeahead.js", ["jquery"], function(t) {
        return e()
    }) : "object" == typeof exports ? module.exports = e(require("jquery")) : e(jQuery)
}(function() {
    var h = function() {
        "use strict";
        return {
            isMsie: function() {
                return !!/(msie|trident)/i.test(navigator.userAgent) && navigator.userAgent.match(/(msie |rv:)(\d+(.\d+)?)/i)[2]
            },
            isBlankString: function(t) {
                return !t || /^\s*$/.test(t)
            },
            escapeRegExChars: function(t) {
                return t.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&")
            },
            isString: function(t) {
                return "string" == typeof t
            },
            isNumber: function(t) {
                return "number" == typeof t
            },
            isArray: $.isArray,
            isFunction: $.isFunction,
            isObject: $.isPlainObject,
            isUndefined: function(t) {
                return void 0 === t
            },
            isElement: function(t) {
                return !(!t || 1 !== t.nodeType)
            },
            isJQuery: function(t) {
                return t instanceof $
            },
            toStr: function(t) {
                return h.isUndefined(t) || null === t ? "" : t + ""
            },
            bind: $.proxy,
            each: function(t, n) {
                $.each(t, function(t, e) {
                    return n(e, t)
                })
            },
            map: $.map,
            filter: $.grep,
            every: function(n, i) {
                var s = !0;
                return n ? ($.each(n, function(t, e) {
                    if (!(s = i.call(null, e, t, n)))
                        return !1
                }),
                !!s) : s
            },
            some: function(n, i) {
                var s = !1;
                return n ? ($.each(n, function(t, e) {
                    if (s = i.call(null, e, t, n))
                        return !1
                }),
                !!s) : s
            },
            mixin: $.extend,
            identity: function(t) {
                return t
            },
            clone: function(t) {
                return $.extend(!0, {}, t)
            },
            getIdGenerator: function() {
                var t = 0;
                return function() {
                    return t++
                }
            },
            templatify: function(t) {
                return $.isFunction(t) ? t : function() {
                    return String(t)
                }
            },
            defer: function(t) {
                setTimeout(t, 0)
            },
            debounce: function(s, r, o) {
                var a, l;
                return function() {
                    var t = this
                      , e = arguments
                      , n = function() {
                        a = null,
                        o || (l = s.apply(t, e))
                    }
                      , i = o && !a;
                    return clearTimeout(a),
                    a = setTimeout(n, r),
                    l = i ? s.apply(t, e) : l
                }
            },
            throttle: function(n, i) {
                var s, r, o, a, l = 0, c = function() {
                    l = new Date,
                    o = null,
                    a = n.apply(s, r)
                };
                return function() {
                    var t = new Date
                      , e = i - (t - l);
                    return s = this,
                    r = arguments,
                    e <= 0 ? (clearTimeout(o),
                    o = null,
                    l = t,
                    a = n.apply(s, r)) : o = o || setTimeout(c, e),
                    a
                }
            },
            stringify: function(t) {
                return h.isString(t) ? t : JSON.stringify(t)
            },
            noop: function() {}
        }
    }()
      , n = function() {
        "use strict";
        var s = {
            wrapper: "twitter-typeahead",
            input: "tt-input",
            hint: "tt-hint",
            menu: "tt-menu",
            dataset: "tt-dataset",
            suggestion: "tt-suggestion",
            selectable: "tt-selectable",
            empty: "tt-empty",
            open: "tt-open",
            cursor: "tt-cursor",
            highlight: "tt-highlight"
        }
          , r = {
            wrapper: "<span></span>",
            menu: "<div></div>",
            dataset: "<div></div>"
        };
        return function(t) {
            var e, n, i;
            return t = t || {},
            n = h.mixin({}, s, t.classNames),
            i = h.mixin({}, r, t.templates),
            {
                css: (e = {
                    css: function() {
                        var t = {
                            wrapper: {
                                position: "relative",
                                display: "inline-block"
                            },
                            hint: {
                                position: "absolute",
                                top: "0",
                                left: "0",
                                borderColor: "transparent",
                                boxShadow: "none",
                                opacity: "1"
                            },
                            input: {
                                position: "relative",
                                verticalAlign: "top",
                                backgroundColor: "transparent"
                            },
                            inputWithNoHint: {
                                position: "relative",
                                verticalAlign: "top"
                            },
                            menu: {
                                position: "absolute",
                                top: "100%",
                                left: "0",
                                zIndex: "100",
                                display: "none"
                            },
                            ltr: {
                                left: "0",
                                right: "auto"
                            },
                            rtl: {
                                left: "auto",
                                right: " 0"
                            }
                        };
                        h.isMsie() && h.mixin(t.input, {
                            backgroundImage: "url(data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)"
                        });
                        h.isMsie() && h.isMsie() <= 7 && h.mixin(t.input, {
                            marginTop: "-1px"
                        });
                        return t
                    }(),
                    classes: n,
                    html: {
                        wrapper: function() {
                            return $(i.wrapper).addClass(n.wrapper)
                        },
                        menu: function() {
                            return $(i.menu).addClass(n.menu)
                        },
                        dataset: function() {
                            return $(i.dataset).addClass(n.dataset)
                        }
                    },
                    selectors: function(t) {
                        var n = {};
                        return h.each(t, function(t, e) {
                            n[e] = "." + t
                        }),
                        n
                    }(n)
                }).css,
                html: e.html,
                classes: e.classes,
                selectors: e.selectors,
                mixin: function(t) {
                    h.mixin(t, e)
                }
            }
        }
    }()
      , d = function() {
        "use strict";
        var e;
        function t(t) {
            t && t.el || $.error("EventBus initialized without el"),
            this.$el = $(t.el)
        }
        return e = {
            render: "rendered",
            cursorchange: "cursorchanged",
            select: "selected",
            autocomplete: "autocompleted"
        },
        h.mixin(t.prototype, {
            _trigger: function(t, e) {
                t = $.Event("typeahead:" + t);
                return (e = e || []).unshift(t),
                this.$el.trigger.apply(this.$el, e),
                t
            },
            before: function(t) {
                var e = [].slice.call(arguments, 1);
                return this._trigger("before" + t, e).isDefaultPrevented()
            },
            trigger: function(t) {
                this._trigger(t, [].slice.call(arguments, 1)),
                (t = e[t]) && this._trigger(t, [].slice.call(arguments, 1))
            }
        }),
        t
    }()
      , e = function() {
        "use strict";
        var a = /\s+/
          , r = function() {
            var t;
            t = window.setImmediate ? function(t) {
                setImmediate(function() {
                    t()
                })
            }
            : function(t) {
                setTimeout(function() {
                    t()
                }, 0)
            }
            ;
            return t
        }();
        return {
            onSync: function(t, e, n) {
                return i.call(this, "sync", t, e, n)
            },
            onAsync: function(t, e, n) {
                return i.call(this, "async", t, e, n)
            },
            off: function(t) {
                var e;
                if (!this._callbacks)
                    return this;
                t = t.split(a);
                for (; e = t.shift(); )
                    delete this._callbacks[e];
                return this
            },
            trigger: function(t) {
                var e, n, i, s;
                if (!this._callbacks)
                    return this;
                t = t.split(a),
                n = [].slice.call(arguments, 1);
                for (; (s = t.shift()) && (e = this._callbacks[s]); )
                    i = o(e.sync, this, [s].concat(n)),
                    s = o(e.async, this, [s].concat(n)),
                    i() && r(s);
                return this
            }
        };
        function i(t, e, n, i) {
            var s, r, o;
            if (!n)
                return this;
            for (e = e.split(a),
            n = i ? (o = i,
            (r = n).bind ? r.bind(o) : function() {
                r.apply(o, [].slice.call(arguments, 0))
            }
            ) : n,
            this._callbacks = this._callbacks || {}; s = e.shift(); )
                this._callbacks[s] = this._callbacks[s] || {
                    sync: [],
                    async: []
                },
                this._callbacks[s][t].push(n);
            return this
        }
        function o(i, s, r) {
            return function() {
                for (var t, e = 0, n = i.length; !t && e < n; e += 1)
                    t = !1 === i[e].apply(s, r);
                return !t
            }
        }
    }()
      , o = function(o) {
        "use strict";
        var t = {
            node: null,
            pattern: null,
            tagName: "strong",
            className: null,
            wordsOnly: !1,
            caseSensitive: !1
        };
        return function(s) {
            var r;
            (s = h.mixin({}, t, s)).node && s.pattern && (s.pattern = h.isArray(s.pattern) ? s.pattern : [s.pattern],
            r = function(t, e, n) {
                for (var i = [], s = 0, r = t.length; s < r; s++)
                    i.push(h.escapeRegExChars(t[s]));
                return n = n ? "\\b(" + i.join("|") + ")\\b" : "(" + i.join("|") + ")",
                e ? new RegExp(n) : new RegExp(n,"i")
            }(s.pattern, s.caseSensitive, s.wordsOnly),
            function t(e, n) {
                var i;
                for (var s = 0; s < e.childNodes.length; s++)
                    3 === (i = e.childNodes[s]).nodeType ? s += n(i) ? 1 : 0 : t(i, n)
            }(s.node, function(t) {
                var e, n, i;
                (e = r.exec(t.data)) && (i = o.createElement(s.tagName),
                s.className && (i.className = s.className),
                (n = t.splitText(e.index)).splitText(e[0].length),
                i.appendChild(n.cloneNode(!0)),
                t.parentNode.replaceChild(i, n));
                return !!e
            }))
        }
    }(window.document)
      , p = function() {
        "use strict";
        var r;
        function s(t, e) {
            (t = t || {}).input || $.error("input is missing"),
            e.mixin(this),
            this.$hint = $(t.hint),
            this.$input = $(t.input),
            this.query = this.$input.val(),
            this.queryWhenFocused = this.hasFocus() ? this.query : null,
            this.$overflowHelper = (e = this.$input,
            $('<pre aria-hidden="true"></pre>').css({
                position: "absolute",
                visibility: "hidden",
                whiteSpace: "pre",
                fontFamily: e.css("font-family"),
                fontSize: e.css("font-size"),
                fontStyle: e.css("font-style"),
                fontVariant: e.css("font-variant"),
                fontWeight: e.css("font-weight"),
                wordSpacing: e.css("word-spacing"),
                letterSpacing: e.css("letter-spacing"),
                textIndent: e.css("text-indent"),
                textRendering: e.css("text-rendering"),
                textTransform: e.css("text-transform")
            }).insertAfter(e)),
            this._checkLanguageDirection(),
            0 === this.$hint.length && (this.setHint = this.getHint = this.clearHint = this.clearHintIfInvalid = h.noop)
        }
        return r = {
            9: "tab",
            27: "esc",
            37: "left",
            39: "right",
            13: "enter",
            38: "up",
            40: "down"
        },
        s.normalizeQuery = function(t) {
            return (t || "").replace(/^\s*/g, "").replace(/\s{2,}/g, " ")
        }
        ,
        h.mixin(s.prototype, e, {
            _onBlur: function() {
                this.resetInputValue(),
                this.trigger("blurred")
            },
            _onFocus: function() {
                this.queryWhenFocused = this.query,
                this.trigger("focused")
            },
            _onKeydown: function(t) {
                var e = r[t.which || t.keyCode];
                this._managePreventDefault(e, t),
                e && this._shouldTrigger(e, t) && this.trigger(e + "Keyed", t)
            },
            _onInput: function() {
                this._setQuery(this.getInputValue()),
                this.clearHintIfInvalid(),
                this._checkLanguageDirection()
            },
            _managePreventDefault: function(t, e) {
                var n;
                switch (t) {
                case "up":
                case "down":
                    n = !i(e);
                    break;
                default:
                    n = !1
                }
                n && e.preventDefault()
            },
            _shouldTrigger: function(t, e) {
                t = "tab" !== t || !i(e);
                return t
            },
            _checkLanguageDirection: function() {
                var t = (this.$input.css("direction") || "ltr").toLowerCase();
                this.dir !== t && (this.dir = t,
                this.$hint.attr("dir", t),
                this.trigger("langDirChanged", t))
            },
            _setQuery: function(t, e) {
                var n, i;
                n = t,
                i = this.query,
                i = (n = s.normalizeQuery(n) === s.normalizeQuery(i)) && this.query.length !== t.length,
                this.query = t,
                e || n ? !e && i && this.trigger("whitespaceChanged", this.query) : this.trigger("queryChanged", this.query)
            },
            bind: function() {
                var e = this
                  , t = h.bind(this._onBlur, this)
                  , n = h.bind(this._onFocus, this)
                  , i = h.bind(this._onKeydown, this)
                  , s = h.bind(this._onInput, this);
                return this.$input.on("blur.tt", t).on("focus.tt", n).on("keydown.tt", i),
                !h.isMsie() || 9 < h.isMsie() ? this.$input.on("input.tt", s) : this.$input.on("keydown.tt keypress.tt cut.tt paste.tt", function(t) {
                    r[t.which || t.keyCode] || h.defer(h.bind(e._onInput, e, t))
                }),
                this
            },
            focus: function() {
                this.$input.focus()
            },
            blur: function() {
                this.$input.blur()
            },
            getLangDir: function() {
                return this.dir
            },
            getQuery: function() {
                return this.query || ""
            },
            setQuery: function(t, e) {
                this.setInputValue(t),
                this._setQuery(t, e)
            },
            hasQueryChangedSinceLastFocus: function() {
                return this.query !== this.queryWhenFocused
            },
            getInputValue: function() {
                return this.$input.val()
            },
            setInputValue: function(t) {
                this.$input.val(t),
                this.clearHintIfInvalid(),
                this._checkLanguageDirection()
            },
            resetInputValue: function() {
                this.setInputValue(this.query)
            },
            getHint: function() {
                return this.$hint.val()
            },
            setHint: function(t) {
                this.$hint.val(t)
            },
            clearHint: function() {
                this.setHint("")
            },
            clearHintIfInvalid: function() {
                var t = this.getInputValue()
                  , e = this.getHint()
                  , e = t !== e && 0 === e.indexOf(t);
                "" !== t && e && !this.hasOverflow() || this.clearHint()
            },
            hasFocus: function() {
                return this.$input.is(":focus")
            },
            hasOverflow: function() {
                var t = this.$input.width() - 2;
                return this.$overflowHelper.text(this.getInputValue()),
                this.$overflowHelper.width() >= t
            },
            isCursorAtEnd: function() {
                var t = this.$input.val().length
                  , e = this.$input[0].selectionStart;
                return h.isNumber(e) ? e === t : !document.selection || ((e = document.selection.createRange()).moveStart("character", -t),
                t === e.text.length)
            },
            destroy: function() {
                this.$hint.off(".tt"),
                this.$input.off(".tt"),
                this.$overflowHelper.remove(),
                this.$hint = this.$input = this.$overflowHelper = null
            }
        }),
        s;
        function i(t) {
            return t.altKey || t.ctrlKey || t.metaKey || t.shiftKey
        }
    }()
      , s = function() {
        "use strict";
        var r, s;
        function t(t, e) {
            var n, i;
            (t = t || {}).templates = t.templates || {},
            t.templates.notFound = t.templates.notFound || t.templates.empty,
            t.source || $.error("missing source"),
            t.node || $.error("missing node"),
            t.name && !/^[_a-zA-Z0-9-]+$/.test(t.name) && $.error("invalid dataset name: " + t.name),
            e.mixin(this),
            this.highlight = !!t.highlight,
            this.name = t.name || s(),
            this.limit = t.limit || 5,
            this.displayFn = (n = (n = t.display || t.displayKey) || h.stringify,
            h.isFunction(n) ? n : function(t) {
                return t[n]
            }
            ),
            this.templates = (e = t.templates,
            i = this.displayFn,
            {
                notFound: e.notFound && h.templatify(e.notFound),
                pending: e.pending && h.templatify(e.pending),
                header: e.header && h.templatify(e.header),
                footer: e.footer && h.templatify(e.footer),
                suggestion: e.suggestion || function(t) {
                    return "<div><p>" + i(t) + "</p></div>"
                }
            }),
            this.source = t.source.__ttAdapter ? t.source.__ttAdapter() : t.source,
            this.async = h.isUndefined(t.async) ? 2 < this.source.length : !!t.async,
            this._resetLastSuggestion(),
            this.$el = $(t.node).addClass(this.classes.dataset).addClass(this.classes.dataset + "-" + this.name)
        }
        return r = {
            val: "tt-selectable-display",
            obj: "tt-selectable-object"
        },
        s = h.getIdGenerator(),
        t.extractData = function(t) {
            t = $(t);
            return t.data(r.obj) ? {
                val: t.data(r.val) || "",
                obj: t.data(r.obj) || null
            } : null
        }
        ,
        h.mixin(t.prototype, e, {
            _overwrite: function(t, e) {
                (e = e || []).length ? this._renderSuggestions(t, e) : this.async && this.templates.pending ? this._renderPending(t) : !this.async && this.templates.notFound ? this._renderNotFound(t) : this._empty(),
                this.trigger("rendered", this.name, e, !1)
            },
            _append: function(t, e) {
                (e = e || []).length && this.$lastSuggestion.length ? this._appendSuggestions(t, e) : e.length ? this._renderSuggestions(t, e) : !this.$lastSuggestion.length && this.templates.notFound && this._renderNotFound(t),
                this.trigger("rendered", this.name, e, !0)
            },
            _renderSuggestions: function(t, e) {
                var n = this._getSuggestionsFragment(t, e);
                this.$lastSuggestion = n.children().last(),
                this.$el.html(n).prepend(this._getHeader(t, e)).append(this._getFooter(t, e))
            },
            _appendSuggestions: function(t, e) {
                t = this._getSuggestionsFragment(t, e),
                e = t.children().last();
                this.$lastSuggestion.after(t),
                this.$lastSuggestion = e
            },
            _renderPending: function(t) {
                var e = this.templates.pending;
                this._resetLastSuggestion(),
                e && this.$el.html(e({
                    query: t,
                    dataset: this.name
                }))
            },
            _renderNotFound: function(t) {
                var e = this.templates.notFound;
                this._resetLastSuggestion(),
                e && this.$el.html(e({
                    query: t,
                    dataset: this.name
                }))
            },
            _empty: function() {
                this.$el.empty(),
                this._resetLastSuggestion()
            },
            _getSuggestionsFragment: function(n, t) {
                var i = this
                  , s = document.createDocumentFragment();
                return h.each(t, function(t) {
                    var e = i._injectQuery(n, t)
                      , e = $(i.templates.suggestion(e)).data(r.obj, t).data(r.val, i.displayFn(t)).addClass(i.classes.suggestion + " " + i.classes.selectable);
                    s.appendChild(e[0])
                }),
                this.highlight && o({
                    className: this.classes.highlight,
                    node: s,
                    pattern: n
                }),
                $(s)
            },
            _getFooter: function(t, e) {
                return this.templates.footer ? this.templates.footer({
                    query: t,
                    suggestions: e,
                    dataset: this.name
                }) : null
            },
            _getHeader: function(t, e) {
                return this.templates.header ? this.templates.header({
                    query: t,
                    suggestions: e,
                    dataset: this.name
                }) : null
            },
            _resetLastSuggestion: function() {
                this.$lastSuggestion = $()
            },
            _injectQuery: function(t, e) {
                return h.isObject(e) ? h.mixin({
                    _query: t
                }, e) : e
            },
            update: function(e) {
                var n = this
                  , i = !1
                  , s = !1
                  , r = 0;
                function t(t) {
                    s || (s = !0,
                    t = (t || []).slice(0, n.limit),
                    r = t.length,
                    n._overwrite(e, t),
                    r < n.limit && n.async && n.trigger("asyncRequested", e))
                }
                this.cancel(),
                this.cancel = function() {
                    i = !0,
                    n.cancel = $.noop,
                    n.async && n.trigger("asyncCanceled", e)
                }
                ,
                this.source(e, t, function(t) {
                    t = t || [],
                    !i && r < n.limit && (n.cancel = $.noop,
                    r += t.length,
                    n._append(e, t.slice(0, n.limit - r)),
                    n.async && n.trigger("asyncReceived", e))
                }),
                s || t([])
            },
            cancel: $.noop,
            clear: function() {
                this._empty(),
                this.cancel(),
                this.trigger("cleared")
            },
            isEmpty: function() {
                return this.$el.is(":empty")
            },
            destroy: function() {
                this.$el = null
            }
        }),
        t
    }()
      , f = function() {
        "use strict";
        function t(t, n) {
            var i = this;
            (t = t || {}).node || $.error("node is required"),
            n.mixin(this),
            this.$node = $(t.node),
            this.query = null,
            this.datasets = h.map(t.datasets, function(t) {
                var e = i.$node.find(t.node).first();
                return t.node = e.length ? e : $(t.templates.dataset).appendTo(i.$node),
                new s(t,n)
            })
        }
        return h.mixin(t.prototype, e, {
            _onSelectableClick: function(t) {
                this.trigger("selectableClicked", $(t.currentTarget))
            },
            _onRendered: function(t, e, n, i) {
                this.$node.toggleClass(this.classes.empty, this._allDatasetsEmpty()),
                this.trigger("datasetRendered", e, n, i)
            },
            _onCleared: function() {
                this.$node.toggleClass(this.classes.empty, this._allDatasetsEmpty()),
                this.trigger("datasetCleared")
            },
            _propagate: function() {
                this.trigger.apply(this, arguments)
            },
            _allDatasetsEmpty: function() {
                return h.every(this.datasets, function(t) {
                    return t.isEmpty()
                })
            },
            _getSelectables: function() {
                return this.$node.find(this.selectors.selectable)
            },
            _removeCursor: function() {
                var t = this.getActiveSelectable();
                t && t.removeClass(this.classes.cursor)
            },
            _ensureVisible: function(t) {
                var e = t.position().top
                  , t = e + t.outerHeight(!0)
                  , n = this.$node.scrollTop()
                  , i = this.$node.height() + parseInt(this.$node.css("paddingTop"), 10) + parseInt(this.$node.css("paddingBottom"), 10);
                e < 0 ? this.$node.scrollTop(n + e) : i < t && this.$node.scrollTop(n + (t - i))
            },
            bind: function() {
                var e = this
                  , t = h.bind(this._onSelectableClick, this);
                return this.$node.on("click.tt", this.selectors.selectable, t),
                h.each(this.datasets, function(t) {
                    t.onSync("asyncRequested", e._propagate, e).onSync("asyncCanceled", e._propagate, e).onSync("asyncReceived", e._propagate, e).onSync("rendered", e._onRendered, e).onSync("cleared", e._onCleared, e)
                }),
                this
            },
            isOpen: function() {
                return this.$node.hasClass(this.classes.open)
            },
            open: function() {
                this.$node.addClass(this.classes.open)
            },
            close: function() {
                this.$node.removeClass(this.classes.open),
                this._removeCursor()
            },
            setLanguageDirection: function(t) {
                this.$node.attr("dir", t)
            },
            selectableRelativeToCursor: function(t) {
                var e = this.getActiveSelectable()
                  , n = this._getSelectables()
                  , e = (e ? n.index(e) : -1) + t;
                return -1 === (e = (e = (e + 1) % (n.length + 1) - 1) < -1 ? n.length - 1 : e) ? null : n.eq(e)
            },
            setCursor: function(t) {
                this._removeCursor(),
                (t = t && t.first()) && (t.addClass(this.classes.cursor),
                this._ensureVisible(t))
            },
            getSelectableData: function(t) {
                return t && t.length ? s.extractData(t) : null
            },
            getActiveSelectable: function() {
                var t = this._getSelectables().filter(this.selectors.cursor).first();
                return t.length ? t : null
            },
            getTopSelectable: function() {
                var t = this._getSelectables().first();
                return t.length ? t : null
            },
            update: function(e) {
                var t = e !== this.query;
                return t && (this.query = e,
                h.each(this.datasets, function(t) {
                    t.update(e)
                })),
                t
            },
            empty: function() {
                h.each(this.datasets, function(t) {
                    t.clear()
                }),
                this.query = null,
                this.$node.addClass(this.classes.empty)
            },
            destroy: function() {
                this.$node.off(".tt"),
                this.$node = null,
                h.each(this.datasets, function(t) {
                    t.destroy()
                })
            }
        }),
        t
    }()
      , g = function() {
        "use strict";
        var e = f.prototype;
        function t() {
            f.apply(this, [].slice.call(arguments, 0))
        }
        return h.mixin(t.prototype, f.prototype, {
            open: function() {
                return this._allDatasetsEmpty() || this._show(),
                e.open.apply(this, [].slice.call(arguments, 0))
            },
            close: function() {
                return this._hide(),
                e.close.apply(this, [].slice.call(arguments, 0))
            },
            _onRendered: function() {
                return this._allDatasetsEmpty() ? this._hide() : this.isOpen() && this._show(),
                e._onRendered.apply(this, [].slice.call(arguments, 0))
            },
            _onCleared: function() {
                return this._allDatasetsEmpty() ? this._hide() : this.isOpen() && this._show(),
                e._onCleared.apply(this, [].slice.call(arguments, 0))
            },
            setLanguageDirection: function(t) {
                return this.$node.css("ltr" === t ? this.css.ltr : this.css.rtl),
                e.setLanguageDirection.apply(this, [].slice.call(arguments, 0))
            },
            _hide: function() {
                this.$node.hide()
            },
            _show: function() {
                this.$node.css("display", "block")
            }
        }),
        t
    }()
      , m = function() {
        "use strict";
        function t(t, e) {
            var n, i, s, r, o, a, l, c, u;
            (t = t || {}).input || $.error("missing input"),
            t.menu || $.error("missing menu"),
            t.eventBus || $.error("missing event bus"),
            e.mixin(this),
            this.eventBus = t.eventBus,
            this.minLength = h.isNumber(t.minLength) ? t.minLength : 1,
            this.input = t.input,
            this.menu = t.menu,
            this.enabled = !0,
            this.active = !1,
            this.input.hasFocus() && this.activate(),
            this.dir = this.input.getLangDir(),
            this._hacks(),
            this.menu.bind().onSync("selectableClicked", this._onSelectableClicked, this).onSync("asyncRequested", this._onAsyncRequested, this).onSync("asyncCanceled", this._onAsyncCanceled, this).onSync("asyncReceived", this._onAsyncReceived, this).onSync("datasetRendered", this._onDatasetRendered, this).onSync("datasetCleared", this._onDatasetCleared, this),
            e = d(this, "activate", "open", "_onFocused"),
            t = d(this, "deactivate", "_onBlurred"),
            n = d(this, "isActive", "isOpen", "_onEnterKeyed"),
            i = d(this, "isActive", "isOpen", "_onTabKeyed"),
            s = d(this, "isActive", "_onEscKeyed"),
            r = d(this, "isActive", "open", "_onUpKeyed"),
            o = d(this, "isActive", "open", "_onDownKeyed"),
            a = d(this, "isActive", "isOpen", "_onLeftKeyed"),
            l = d(this, "isActive", "isOpen", "_onRightKeyed"),
            c = d(this, "_openIfActive", "_onQueryChanged"),
            u = d(this, "_openIfActive", "_onWhitespaceChanged"),
            this.input.bind().onSync("focused", e, this).onSync("blurred", t, this).onSync("enterKeyed", n, this).onSync("tabKeyed", i, this).onSync("escKeyed", s, this).onSync("upKeyed", r, this).onSync("downKeyed", o, this).onSync("leftKeyed", a, this).onSync("rightKeyed", l, this).onSync("queryChanged", c, this).onSync("whitespaceChanged", u, this).onSync("langDirChanged", this._onLangDirChanged, this)
        }
        return h.mixin(t.prototype, {
            _hacks: function() {
                var i = this.input.$input || $("<div>")
                  , s = this.menu.$node || $("<div>");
                i.on("blur.tt", function(t) {
                    var e = document.activeElement
                      , n = s.is(e)
                      , e = 0 < s.has(e).length;
                    h.isMsie() && (n || e) && (t.preventDefault(),
                    t.stopImmediatePropagation(),
                    h.defer(function() {
                        i.focus()
                    }))
                }),
                s.on("mousedown.tt", function(t) {
                    t.preventDefault()
                })
            },
            _onSelectableClicked: function(t, e) {
                this.select(e)
            },
            _onDatasetCleared: function() {
                this._updateHint()
            },
            _onDatasetRendered: function(t, e, n, i) {
                this._updateHint(),
                this.eventBus.trigger("render", n, i, e)
            },
            _onAsyncRequested: function(t, e, n) {
                this.eventBus.trigger("asyncrequest", n, e)
            },
            _onAsyncCanceled: function(t, e, n) {
                this.eventBus.trigger("asynccancel", n, e)
            },
            _onAsyncReceived: function(t, e, n) {
                this.eventBus.trigger("asyncreceive", n, e)
            },
            _onFocused: function() {
                this._minLengthMet() && this.menu.update(this.input.getQuery())
            },
            _onBlurred: function() {
                this.input.hasQueryChangedSinceLastFocus() && this.eventBus.trigger("change", this.input.getQuery())
            },
            _onEnterKeyed: function(t, e) {
                var n;
                (n = this.menu.getActiveSelectable()) && this.select(n) && e.preventDefault()
            },
            _onTabKeyed: function(t, e) {
                var n;
                (n = this.menu.getActiveSelectable()) ? this.select(n) && e.preventDefault() : (n = this.menu.getTopSelectable()) && this.autocomplete(n) && e.preventDefault()
            },
            _onEscKeyed: function() {
                this.close()
            },
            _onUpKeyed: function() {
                this.moveCursor(-1)
            },
            _onDownKeyed: function() {
                this.moveCursor(1)
            },
            _onLeftKeyed: function() {
                "rtl" === this.dir && this.input.isCursorAtEnd() && this.autocomplete(this.menu.getTopSelectable())
            },
            _onRightKeyed: function() {
                "ltr" === this.dir && this.input.isCursorAtEnd() && this.autocomplete(this.menu.getTopSelectable())
            },
            _onQueryChanged: function(t, e) {
                this._minLengthMet(e) ? this.menu.update(e) : this.menu.empty()
            },
            _onWhitespaceChanged: function() {
                this._updateHint()
            },
            _onLangDirChanged: function(t, e) {
                this.dir !== e && (this.dir = e,
                this.menu.setLanguageDirection(e))
            },
            _openIfActive: function() {
                this.isActive() && this.open()
            },
            _minLengthMet: function(t) {
                return (t = h.isString(t) ? t : this.input.getQuery() || "").length >= this.minLength
            },
            _updateHint: function() {
                var t, e = this.menu.getTopSelectable(), e = this.menu.getSelectableData(e), n = this.input.getInputValue();
                !e || h.isBlankString(n) || this.input.hasOverflow() ? this.input.clearHint() : (t = p.normalizeQuery(n),
                t = h.escapeRegExChars(t),
                (t = new RegExp("^(?:" + t + ")(.+$)","i").exec(e.val)) && this.input.setHint(n + t[1]))
            },
            isEnabled: function() {
                return this.enabled
            },
            enable: function() {
                this.enabled = !0
            },
            disable: function() {
                this.enabled = !1
            },
            isActive: function() {
                return this.active
            },
            activate: function() {
                return !!this.isActive() || !(!this.isEnabled() || this.eventBus.before("active")) && (this.active = !0,
                this.eventBus.trigger("active"),
                !0)
            },
            deactivate: function() {
                return !this.isActive() || !this.eventBus.before("idle") && (this.active = !1,
                this.close(),
                this.eventBus.trigger("idle"),
                !0)
            },
            isOpen: function() {
                return this.menu.isOpen()
            },
            open: function() {
                return this.isOpen() || this.eventBus.before("open") || (this.menu.open(),
                this._updateHint(),
                this.eventBus.trigger("open")),
                this.isOpen()
            },
            close: function() {
                return this.isOpen() && !this.eventBus.before("close") && (this.menu.close(),
                this.input.clearHint(),
                this.input.resetInputValue(),
                this.eventBus.trigger("close")),
                !this.isOpen()
            },
            setVal: function(t) {
                this.input.setQuery(h.toStr(t))
            },
            getVal: function() {
                return this.input.getQuery()
            },
            select: function(t) {
                t = this.menu.getSelectableData(t);
                return !(!t || this.eventBus.before("select", t.obj)) && (this.input.setQuery(t.val, !0),
                this.close(),
                this.eventBus.trigger("select", t.obj),
                !0)
            },
            autocomplete: function(t) {
                var e = this.input.getQuery()
                  , t = this.menu.getSelectableData(t);
                return !(!t || e === t.val || this.eventBus.before("autocomplete", t.obj)) && (this.input.setQuery(t.val),
                this.eventBus.trigger("autocomplete", t.obj),
                !0)
            },
            moveCursor: function(t) {
                var e = this.input.getQuery()
                  , t = this.menu.selectableRelativeToCursor(t)
                  , n = this.menu.getSelectableData(t)
                  , i = n ? n.obj : null;
                return !(this._minLengthMet() && this.menu.update(e) || this.eventBus.before("cursorchange", i)) && (this.menu.setCursor(t),
                n ? this.input.setInputValue(n.val) : (this.input.resetInputValue(),
                this._updateHint()),
                this.eventBus.trigger("cursorchange", i),
                !0)
            },
            destroy: function() {
                this.input.destroy(),
                this.menu.destroy()
            }
        }),
        t;
        function d(n) {
            var t = [].slice.call(arguments, 1);
            return function() {
                var e = [].slice.call(arguments);
                h.each(t, function(t) {
                    return n[t].apply(n, e)
                })
            }
        }
    }();
    !function() {
        "use strict";
        var t, c, e;
        function i(t, n) {
            t.each(function() {
                var t, e = $(this);
                (t = e.data(c.typeahead)) && n(t, e)
            })
        }
        function u(t) {
            t = h.isJQuery(t) || h.isElement(t) ? $(t).first() : [];
            return t.length ? t : null
        }
        t = $.fn.typeahead,
        c = {
            www: "tt-www",
            attrs: "tt-attrs",
            typeahead: "tt-typeahead"
        },
        e = {
            initialize: function(o, a) {
                var l;
                return a = h.isArray(a) ? a : [].slice.call(arguments, 1),
                l = n(o = o || {}),
                this.each(function() {
                    var t, e, n, i, s, r;
                    h.each(a, function(t) {
                        t.highlight = !!o.highlight,
                        t.templates = h.mixin({
                            dataset: l.html.dataset()
                        }, t.templates)
                    }),
                    t = $(this),
                    r = l.html.wrapper(),
                    s = u(o.hint),
                    e = u(o.menu),
                    n = !1 !== o.hint && !s,
                    i = !1 !== o.menu && !e,
                    n && (s = function(t, e) {
                        return t.clone().addClass(e.classes.hint).removeData().css(e.css.hint).css(function(t) {
                            return {
                                backgroundAttachment: t.css("background-attachment"),
                                backgroundClip: t.css("background-clip"),
                                backgroundColor: t.css("background-color"),
                                backgroundImage: t.css("background-image"),
                                backgroundOrigin: t.css("background-origin"),
                                backgroundPosition: t.css("background-position"),
                                backgroundRepeat: t.css("background-repeat"),
                                backgroundSize: t.css("background-size")
                            }
                        }(t)).prop("readonly", !0).removeAttr("id name placeholder required").attr({
                            autocomplete: "off",
                            spellcheck: "false",
                            tabindex: -1
                        })
                    }(t, l)),
                    i && (e = l.html.menu().css(l.css.menu)),
                    s && s.val(""),
                    t = function(t, e) {
                        t.data(c.attrs, {
                            dir: t.attr("dir"),
                            autocomplete: t.attr("autocomplete"),
                            spellcheck: t.attr("spellcheck"),
                            style: t.attr("style")
                        }),
                        t.addClass(e.classes.input).attr({
                            autocomplete: "off",
                            spellcheck: !1
                        });
                        try {
                            t.attr("dir") || t.attr("dir", "auto")
                        } catch (t) {}
                        return t
                    }(t, l),
                    (n || i) && (r.css(l.css.wrapper),
                    t.css(n ? l.css.input : l.css.inputWithNoHint),
                    t.wrap(r).parent().prepend(n ? s : null).append(i ? e : null));
                    r = i ? g : f,
                    n = new d({
                        el: t
                    }),
                    i = new p({
                        hint: s,
                        input: t
                    },l),
                    s = new r({
                        node: e,
                        datasets: a
                    },l),
                    r = new m({
                        input: i,
                        menu: s,
                        eventBus: n,
                        minLength: o.minLength
                    },l),
                    t.data(c.www, l),
                    t.data(c.typeahead, r)
                })
            },
            isEnabled: function() {
                var e;
                return i(this.first(), function(t) {
                    e = t.isEnabled()
                }),
                e
            },
            enable: function() {
                return i(this, function(t) {
                    t.enable()
                }),
                this
            },
            disable: function() {
                return i(this, function(t) {
                    t.disable()
                }),
                this
            },
            isActive: function() {
                var e;
                return i(this.first(), function(t) {
                    e = t.isActive()
                }),
                e
            },
            activate: function() {
                return i(this, function(t) {
                    t.activate()
                }),
                this
            },
            deactivate: function() {
                return i(this, function(t) {
                    t.deactivate()
                }),
                this
            },
            isOpen: function() {
                var e;
                return i(this.first(), function(t) {
                    e = t.isOpen()
                }),
                e
            },
            open: function() {
                return i(this, function(t) {
                    t.open()
                }),
                this
            },
            close: function() {
                return i(this, function(t) {
                    t.close()
                }),
                this
            },
            select: function(t) {
                var e = !1
                  , n = $(t);
                return i(this.first(), function(t) {
                    e = t.select(n)
                }),
                e
            },
            autocomplete: function(t) {
                var e = !1
                  , n = $(t);
                return i(this.first(), function(t) {
                    e = t.autocomplete(n)
                }),
                e
            },
            moveCursor: function(e) {
                var n = !1;
                return i(this.first(), function(t) {
                    n = t.moveCursor(e)
                }),
                n
            },
            val: function(e) {
                var n;
                return arguments.length ? (i(this, function(t) {
                    t.setVal(e)
                }),
                this) : (i(this.first(), function(t) {
                    n = t.getVal()
                }),
                n)
            },
            destroy: function() {
                return i(this, function(t, e) {
                    var n, i;
                    e = (n = e).data(c.www),
                    i = n.parent().filter(e.selectors.wrapper),
                    h.each(n.data(c.attrs), function(t, e) {
                        h.isUndefined(t) ? n.removeAttr(e) : n.attr(e, t)
                    }),
                    n.removeData(c.typeahead).removeData(c.www).removeData(c.attr).removeClass(e.classes.input),
                    i.length && (n.detach().insertAfter(i),
                    i.remove()),
                    t.destroy()
                }),
                this
            }
        },
        $.fn.typeahead = function(t) {
            return e[t] ? e[t].apply(this, [].slice.call(arguments, 1)) : e.initialize.apply(this, arguments)
        }
        ,
        $.fn.typeahead.noConflict = function() {
            return $.fn.typeahead = t,
            this
        }
    }()
}),
function(o) {
    "use strict";
    function a(t, e) {
        this.options = o.extend({}, a.DEFAULTS, e),
        e = this.options.target === a.DEFAULTS.target ? o(this.options.target) : o(document).find(this.options.target),
        this.$target = e.on("scroll.bs.affix.data-api", o.proxy(this.checkPosition, this)).on("click.bs.affix.data-api", o.proxy(this.checkPositionWithEventLoop, this)),
        this.$element = o(t),
        this.affixed = null,
        this.unpin = null,
        this.pinnedOffset = null,
        this.checkPosition()
    }
    function n(n) {
        return this.each(function() {
            var t = o(this)
              , e = t.data("bs.affix");
            e || t.data("bs.affix", e = new a(this,"object" == typeof n && n)),
            "string" == typeof n && e[n]()
        })
    }
    a.VERSION = "3.4.1",
    a.RESET = "affix affix-top affix-bottom",
    a.DEFAULTS = {
        offset: 0,
        target: window
    },
    a.prototype.getState = function(t, e, n, i) {
        var s = this.$target.scrollTop()
          , r = this.$element.offset()
          , o = this.$target.height();
        if (null != n && "top" == this.affixed)
            return s < n && "top";
        if ("bottom" == this.affixed)
            return null != n ? !(s + this.unpin <= r.top) && "bottom" : !(s + o <= t - i) && "bottom";
        var a = null == this.affixed
          , r = a ? s : r.top;
        return null != n && s <= n ? "top" : null != i && t - i <= r + (a ? o : e) && "bottom"
    }
    ,
    a.prototype.getPinnedOffset = function() {
        if (this.pinnedOffset)
            return this.pinnedOffset;
        this.$element.removeClass(a.RESET).addClass("affix");
        var t = this.$target.scrollTop()
          , e = this.$element.offset();
        return this.pinnedOffset = e.top - t
    }
    ,
    a.prototype.checkPositionWithEventLoop = function() {
        setTimeout(o.proxy(this.checkPosition, this), 1)
    }
    ,
    a.prototype.checkPosition = function() {
        if (this.$element.is(":visible")) {
            var t = this.$element.height()
              , e = this.options.offset
              , n = e.top
              , i = e.bottom
              , s = Math.max(o(document).height(), o(document.body).height())
              , e = ("object" != typeof e && (i = n = e),
            "function" == typeof n && (n = e.top(this.$element)),
            "function" == typeof i && (i = e.bottom(this.$element)),
            this.getState(s, t, n, i));
            if (this.affixed != e) {
                null != this.unpin && this.$element.css("top", "");
                var n = "affix" + (e ? "-" + e : "")
                  , r = o.Event(n + ".bs.affix");
                if (this.$element.trigger(r),
                r.isDefaultPrevented())
                    return;
                this.affixed = e,
                this.unpin = "bottom" == e ? this.getPinnedOffset() : null,
                this.$element.removeClass(a.RESET).addClass(n).trigger(n.replace("affix", "affixed") + ".bs.affix")
            }
            "bottom" == e && this.$element.offset({
                top: s - t - i
            })
        }
    }
    ;
    var t = o.fn.affix;
    o.fn.affix = n,
    o.fn.affix.Constructor = a,
    o.fn.affix.noConflict = function() {
        return o.fn.affix = t,
        this
    }
    ,
    o(window).on("load", function() {
        o('[data-spy="affix"]').each(function() {
            var t = o(this)
              , e = t.data();
            e.offset = e.offset || {},
            null != e.offsetBottom && (e.offset.bottom = e.offsetBottom),
            null != e.offsetTop && (e.offset.top = e.offsetTop),
            n.call(t, e)
        })
    })
}(jQuery),
function(r) {
    "use strict";
    function o(t) {
        r(t).on("click", e, this.close)
    }
    var e = '[data-dismiss="alert"]';
    o.VERSION = "3.4.1",
    o.TRANSITION_DURATION = 150,
    o.prototype.close = function(t) {
        var e = r(this)
          , n = e.attr("data-target")
          , i = (n = "#" === (n = n || (n = e.attr("href")) && n.replace(/.*(?=#[^\s]*$)/, "")) ? [] : n,
        r(document).find(n));
        function s() {
            i.detach().trigger("closed.bs.alert").remove()
        }
        t && t.preventDefault(),
        (i = i.length ? i : e.closest(".alert")).trigger(t = r.Event("close.bs.alert")),
        t.isDefaultPrevented() || (i.removeClass("in"),
        r.support.transition && i.hasClass("fade") ? i.one("bsTransitionEnd", s).emulateTransitionEnd(o.TRANSITION_DURATION) : s())
    }
    ;
    var t = r.fn.alert;
    r.fn.alert = function(n) {
        return this.each(function() {
            var t = r(this)
              , e = t.data("bs.alert");
            e || t.data("bs.alert", e = new o(this)),
            "string" == typeof n && e[n].call(t)
        })
    }
    ,
    r.fn.alert.Constructor = o,
    r.fn.alert.noConflict = function() {
        return r.fn.alert = t,
        this
    }
    ,
    r(document).on("click.bs.alert.data-api", e, o.prototype.close)
}(jQuery),
function(r) {
    "use strict";
    function i(t, e) {
        this.$element = r(t),
        this.options = r.extend({}, i.DEFAULTS, e),
        this.isLoading = !1
    }
    function n(n) {
        return this.each(function() {
            var t = r(this)
              , e = t.data("bs.button");
            e || t.data("bs.button", e = new i(this,"object" == typeof n && n)),
            "toggle" == n ? e.toggle() : n && e.setState(n)
        })
    }
    i.VERSION = "3.4.1",
    i.DEFAULTS = {
        loadingText: "loading..."
    },
    i.prototype.setState = function(t) {
        var e = "disabled"
          , n = this.$element
          , i = n.is("input") ? "val" : "html"
          , s = n.data();
        t += "Text",
        null == s.resetText && n.data("resetText", n[i]()),
        setTimeout(r.proxy(function() {
            n[i]((null == s[t] ? this.options : s)[t]),
            "loadingText" == t ? (this.isLoading = !0,
            n.addClass(e).attr(e, e).prop(e, !0)) : this.isLoading && (this.isLoading = !1,
            n.removeClass(e).removeAttr(e).prop(e, !1))
        }, this), 0)
    }
    ,
    i.prototype.toggle = function() {
        var t, e = !0, n = this.$element.closest('[data-toggle="buttons"]');
        n.length ? ("radio" == (t = this.$element.find("input")).prop("type") ? (t.prop("checked") && (e = !1),
        n.find(".active").removeClass("active"),
        this.$element.addClass("active")) : "checkbox" == t.prop("type") && (t.prop("checked") !== this.$element.hasClass("active") && (e = !1),
        this.$element.toggleClass("active")),
        t.prop("checked", this.$element.hasClass("active")),
        e && t.trigger("change")) : (this.$element.attr("aria-pressed", !this.$element.hasClass("active")),
        this.$element.toggleClass("active"))
    }
    ;
    var t = r.fn.button;
    r.fn.button = n,
    r.fn.button.Constructor = i,
    r.fn.button.noConflict = function() {
        return r.fn.button = t,
        this
    }
    ,
    r(document).on("click.bs.button.data-api", '[data-toggle^="button"]', function(t) {
        var e = r(t.target).closest(".btn");
        n.call(e, "toggle"),
        r(t.target).is('input[type="radio"], input[type="checkbox"]') || (t.preventDefault(),
        (e.is("input,button") ? e : e.find("input:visible,button:visible").first()).trigger("focus"))
    }).on("focus.bs.button.data-api blur.bs.button.data-api", '[data-toggle^="button"]', function(t) {
        r(t.target).closest(".btn").toggleClass("focus", /^focus(in)?$/.test(t.type))
    })
}(jQuery),
function(c) {
    "use strict";
    function u(t, e) {
        this.$element = c(t),
        this.$indicators = this.$element.find(".carousel-indicators"),
        this.options = e,
        this.paused = null,
        this.sliding = null,
        this.interval = null,
        this.$active = null,
        this.$items = null,
        this.options.keyboard && this.$element.on("keydown.bs.carousel", c.proxy(this.keydown, this)),
        "hover" != this.options.pause || "ontouchstart"in document.documentElement || this.$element.on("mouseenter.bs.carousel", c.proxy(this.pause, this)).on("mouseleave.bs.carousel", c.proxy(this.cycle, this))
    }
    function s(s) {
        return this.each(function() {
            var t = c(this)
              , e = t.data("bs.carousel")
              , n = c.extend({}, u.DEFAULTS, t.data(), "object" == typeof s && s)
              , i = "string" == typeof s ? s : n.slide;
            e || t.data("bs.carousel", e = new u(this,n)),
            "number" == typeof s ? e.to(s) : i ? e[i]() : n.interval && e.pause().cycle()
        })
    }
    u.VERSION = "3.4.1",
    u.TRANSITION_DURATION = 600,
    u.DEFAULTS = {
        interval: 5e3,
        pause: "hover",
        wrap: !0,
        keyboard: !0
    },
    u.prototype.keydown = function(t) {
        if (!/input|textarea/i.test(t.target.tagName)) {
            switch (t.which) {
            case 37:
                this.prev();
                break;
            case 39:
                this.next();
                break;
            default:
                return
            }
            t.preventDefault()
        }
    }
    ,
    u.prototype.cycle = function(t) {
        return t || (this.paused = !1),
        this.interval && clearInterval(this.interval),
        this.options.interval && !this.paused && (this.interval = setInterval(c.proxy(this.next, this), this.options.interval)),
        this
    }
    ,
    u.prototype.getItemIndex = function(t) {
        return this.$items = t.parent().children(".item"),
        this.$items.index(t || this.$active)
    }
    ,
    u.prototype.getItemForDirection = function(t, e) {
        var n = this.getItemIndex(e);
        if (("prev" == t && 0 === n || "next" == t && n == this.$items.length - 1) && !this.options.wrap)
            return e;
        e = (n + ("prev" == t ? -1 : 1)) % this.$items.length;
        return this.$items.eq(e)
    }
    ,
    u.prototype.to = function(t) {
        var e = this
          , n = this.getItemIndex(this.$active = this.$element.find(".item.active"));
        if (!(t > this.$items.length - 1 || t < 0))
            return this.sliding ? this.$element.one("slid.bs.carousel", function() {
                e.to(t)
            }) : n == t ? this.pause().cycle() : this.slide(n < t ? "next" : "prev", this.$items.eq(t))
    }
    ,
    u.prototype.pause = function(t) {
        return t || (this.paused = !0),
        this.$element.find(".next, .prev").length && c.support.transition && (this.$element.trigger(c.support.transition.end),
        this.cycle(!0)),
        this.interval = clearInterval(this.interval),
        this
    }
    ,
    u.prototype.next = function() {
        if (!this.sliding)
            return this.slide("next")
    }
    ,
    u.prototype.prev = function() {
        if (!this.sliding)
            return this.slide("prev")
    }
    ,
    u.prototype.slide = function(t, e) {
        var n = this.$element.find(".item.active")
          , i = e || this.getItemForDirection(t, n)
          , e = this.interval
          , s = "next" == t ? "left" : "right"
          , r = this;
        if (i.hasClass("active"))
            return this.sliding = !1;
        var o, a = i[0], l = c.Event("slide.bs.carousel", {
            relatedTarget: a,
            direction: s
        });
        if (this.$element.trigger(l),
        !l.isDefaultPrevented())
            return this.sliding = !0,
            e && this.pause(),
            this.$indicators.length && (this.$indicators.find(".active").removeClass("active"),
            (l = c(this.$indicators.children()[this.getItemIndex(i)])) && l.addClass("active")),
            o = c.Event("slid.bs.carousel", {
                relatedTarget: a,
                direction: s
            }),
            c.support.transition && this.$element.hasClass("slide") ? (i.addClass(t),
            "object" == typeof i && i.length && i[0].offsetWidth,
            n.addClass(s),
            i.addClass(s),
            n.one("bsTransitionEnd", function() {
                i.removeClass([t, s].join(" ")).addClass("active"),
                n.removeClass(["active", s].join(" ")),
                r.sliding = !1,
                setTimeout(function() {
                    r.$element.trigger(o)
                }, 0)
            }).emulateTransitionEnd(u.TRANSITION_DURATION)) : (n.removeClass("active"),
            i.addClass("active"),
            this.sliding = !1,
            this.$element.trigger(o)),
            e && this.cycle(),
            this
    }
    ;
    function t(t) {
        var e, n = c(this), i = (i = n.attr("href")) && i.replace(/.*(?=#[^\s]+$)/, ""), i = n.attr("data-target") || i;
        (i = c(document).find(i)).hasClass("carousel") && (e = c.extend({}, i.data(), n.data()),
        (n = n.attr("data-slide-to")) && (e.interval = !1),
        s.call(i, e),
        n && i.data("bs.carousel").to(n),
        t.preventDefault())
    }
    var e = c.fn.carousel;
    c.fn.carousel = s,
    c.fn.carousel.Constructor = u,
    c.fn.carousel.noConflict = function() {
        return c.fn.carousel = e,
        this
    }
    ;
    c(document).on("click.bs.carousel.data-api", "[data-slide]", t).on("click.bs.carousel.data-api", "[data-slide-to]", t),
    c(window).on("load", function() {
        c('[data-ride="carousel"]').each(function() {
            var t = c(this);
            s.call(t, t.data())
        })
    })
}(jQuery),
function(s) {
    "use strict";
    function r(t, e) {
        this.$element = s(t),
        this.options = s.extend({}, r.DEFAULTS, e),
        this.$trigger = s('[data-toggle="collapse"][href="#' + t.id + '"],[data-toggle="collapse"][data-target="#' + t.id + '"]'),
        this.transitioning = null,
        this.options.parent ? this.$parent = this.getParent() : this.addAriaAndCollapsedClass(this.$element, this.$trigger),
        this.options.toggle && this.toggle()
    }
    function n(t) {
        t = t.attr("data-target") || (t = t.attr("href")) && t.replace(/.*(?=#[^\s]+$)/, "");
        return s(document).find(t)
    }
    function o(i) {
        return this.each(function() {
            var t = s(this)
              , e = t.data("bs.collapse")
              , n = s.extend({}, r.DEFAULTS, t.data(), "object" == typeof i && i);
            !e && n.toggle && /show|hide/.test(i) && (n.toggle = !1),
            e || t.data("bs.collapse", e = new r(this,n)),
            "string" == typeof i && e[i]()
        })
    }
    r.VERSION = "3.4.1",
    r.TRANSITION_DURATION = 350,
    r.DEFAULTS = {
        toggle: !0
    },
    r.prototype.dimension = function() {
        return this.$element.hasClass("width") ? "width" : "height"
    }
    ,
    r.prototype.show = function() {
        if (!this.transitioning && !this.$element.hasClass("in")) {
            var t = this.$parent && this.$parent.children(".panel").children(".in, .collapsing");
            if (!(t && t.length && (i = t.data("bs.collapse")) && i.transitioning)) {
                var e = s.Event("show.bs.collapse");
                if (this.$element.trigger(e),
                !e.isDefaultPrevented()) {
                    t && t.length && (o.call(t, "hide"),
                    i || t.data("bs.collapse", null));
                    var n = this.dimension()
                      , e = (this.$element.removeClass("collapse").addClass("collapsing")[n](0).attr("aria-expanded", !0),
                    this.$trigger.removeClass("collapsed").attr("aria-expanded", !0),
                    this.transitioning = 1,
                    function() {
                        this.$element.removeClass("collapsing").addClass("collapse in")[n](""),
                        this.transitioning = 0,
                        this.$element.trigger("shown.bs.collapse")
                    }
                    );
                    if (!s.support.transition)
                        return e.call(this);
                    var i = s.camelCase(["scroll", n].join("-"));
                    this.$element.one("bsTransitionEnd", s.proxy(e, this)).emulateTransitionEnd(r.TRANSITION_DURATION)[n](this.$element[0][i])
                }
            }
        }
    }
    ,
    r.prototype.hide = function() {
        if (!this.transitioning && this.$element.hasClass("in")) {
            var t = s.Event("hide.bs.collapse");
            if (this.$element.trigger(t),
            !t.isDefaultPrevented()) {
                var t = this.dimension()
                  , e = (this.$element[t](this.$element[t]())[0].offsetHeight,
                this.$element.addClass("collapsing").removeClass("collapse in").attr("aria-expanded", !1),
                this.$trigger.addClass("collapsed").attr("aria-expanded", !1),
                this.transitioning = 1,
                function() {
                    this.transitioning = 0,
                    this.$element.removeClass("collapsing").addClass("collapse").trigger("hidden.bs.collapse")
                }
                );
                if (!s.support.transition)
                    return e.call(this);
                this.$element[t](0).one("bsTransitionEnd", s.proxy(e, this)).emulateTransitionEnd(r.TRANSITION_DURATION)
            }
        }
    }
    ,
    r.prototype.toggle = function() {
        this[this.$element.hasClass("in") ? "hide" : "show"]()
    }
    ,
    r.prototype.getParent = function() {
        return s(document).find(this.options.parent).find('[data-toggle="collapse"][data-parent="' + this.options.parent + '"]').each(s.proxy(function(t, e) {
            e = s(e);
            this.addAriaAndCollapsedClass(n(e), e)
        }, this)).end()
    }
    ,
    r.prototype.addAriaAndCollapsedClass = function(t, e) {
        var n = t.hasClass("in");
        t.attr("aria-expanded", n),
        e.toggleClass("collapsed", !n).attr("aria-expanded", n)
    }
    ;
    var t = s.fn.collapse;
    s.fn.collapse = o,
    s.fn.collapse.Constructor = r,
    s.fn.collapse.noConflict = function() {
        return s.fn.collapse = t,
        this
    }
    ,
    s(document).on("click.bs.collapse.data-api", '[data-toggle="collapse"]', function(t) {
        var e = s(this)
          , t = (e.attr("data-target") || t.preventDefault(),
        n(e))
          , e = t.data("bs.collapse") ? "toggle" : e.data();
        o.call(t, e)
    })
}(jQuery),
function(s) {
    "use strict";
    function i(t) {
        s(t).on("click.bs.dropdown", this.toggle)
    }
    var r = '[data-toggle="dropdown"]';
    function o(t) {
        var e = t.attr("data-target")
          , e = "#" !== (e = e || (e = t.attr("href")) && /#[A-Za-z]/.test(e) && e.replace(/.*(?=#[^\s]*$)/, "")) ? s(document).find(e) : null;
        return e && e.length ? e : t.parent()
    }
    function a(i) {
        i && 3 === i.which || (s(".dropdown-backdrop").remove(),
        s(r).each(function() {
            var t = s(this)
              , e = o(t)
              , n = {
                relatedTarget: this
            };
            !e.hasClass("open") || i && "click" == i.type && /input|textarea/i.test(i.target.tagName) && s.contains(e[0], i.target) || (e.trigger(i = s.Event("hide.bs.dropdown", n)),
            i.isDefaultPrevented() || (t.attr("aria-expanded", "false"),
            e.removeClass("open").trigger(s.Event("hidden.bs.dropdown", n))))
        }))
    }
    i.VERSION = "3.4.1",
    i.prototype.toggle = function(t) {
        var e = s(this);
        if (!e.is(".disabled, :disabled")) {
            var n = o(e)
              , i = n.hasClass("open");
            if (a(),
            !i) {
                "ontouchstart"in document.documentElement && !n.closest(".navbar-nav").length && s(document.createElement("div")).addClass("dropdown-backdrop").insertAfter(s(this)).on("click", a);
                i = {
                    relatedTarget: this
                };
                if (n.trigger(t = s.Event("show.bs.dropdown", i)),
                t.isDefaultPrevented())
                    return;
                e.trigger("focus").attr("aria-expanded", "true"),
                n.toggleClass("open").trigger(s.Event("shown.bs.dropdown", i))
            }
            return !1
        }
    }
    ,
    i.prototype.keydown = function(t) {
        if (/(38|40|27|32)/.test(t.which) && !/input|textarea/i.test(t.target.tagName)) {
            var e = s(this);
            if (t.preventDefault(),
            t.stopPropagation(),
            !e.is(".disabled, :disabled")) {
                var n = o(e)
                  , i = n.hasClass("open");
                if (!i && 27 != t.which || i && 27 == t.which)
                    return 27 == t.which && n.find(r).trigger("focus"),
                    e.trigger("click");
                i = n.find(".dropdown-menu li:not(.disabled):visible a");
                i.length && (e = i.index(t.target),
                38 == t.which && 0 < e && e--,
                40 == t.which && e < i.length - 1 && e++,
                i.eq(e = ~e ? e : 0).trigger("focus"))
            }
        }
    }
    ;
    var t = s.fn.dropdown;
    s.fn.dropdown = function(n) {
        return this.each(function() {
            var t = s(this)
              , e = t.data("bs.dropdown");
            e || t.data("bs.dropdown", e = new i(this)),
            "string" == typeof n && e[n].call(t)
        })
    }
    ,
    s.fn.dropdown.Constructor = i,
    s.fn.dropdown.noConflict = function() {
        return s.fn.dropdown = t,
        this
    }
    ,
    s(document).on("click.bs.dropdown.data-api", a).on("click.bs.dropdown.data-api", ".dropdown form", function(t) {
        t.stopPropagation()
    }).on("click.bs.dropdown.data-api", r, i.prototype.toggle).on("keydown.bs.dropdown.data-api", r, i.prototype.keydown).on("keydown.bs.dropdown.data-api", ".dropdown-menu", i.prototype.keydown)
}(jQuery),
function(r) {
    "use strict";
    function o(t, e) {
        this.options = e,
        this.$body = r(document.body),
        this.$element = r(t),
        this.$dialog = this.$element.find(".modal-dialog"),
        this.$backdrop = null,
        this.isShown = null,
        this.originalBodyPad = null,
        this.scrollbarWidth = 0,
        this.ignoreBackdropClick = !1,
        this.fixedContent = ".navbar-fixed-top, .navbar-fixed-bottom",
        this.options.remote && this.$element.find(".modal-content").load(this.options.remote, r.proxy(function() {
            this.$element.trigger("loaded.bs.modal")
        }, this))
    }
    function a(i, s) {
        return this.each(function() {
            var t = r(this)
              , e = t.data("bs.modal")
              , n = r.extend({}, o.DEFAULTS, t.data(), "object" == typeof i && i);
            e || t.data("bs.modal", e = new o(this,n)),
            "string" == typeof i ? e[i](s) : n.show && e.show(s)
        })
    }
    o.VERSION = "3.4.1",
    o.TRANSITION_DURATION = 300,
    o.BACKDROP_TRANSITION_DURATION = 150,
    o.DEFAULTS = {
        backdrop: !0,
        keyboard: !0,
        show: !0
    },
    o.prototype.toggle = function(t) {
        return this.isShown ? this.hide() : this.show(t)
    }
    ,
    o.prototype.show = function(n) {
        var i = this
          , t = r.Event("show.bs.modal", {
            relatedTarget: n
        });
        this.$element.trigger(t),
        this.isShown || t.isDefaultPrevented() || (this.isShown = !0,
        this.checkScrollbar(),
        this.setScrollbar(),
        this.$body.addClass("modal-open"),
        this.escape(),
        this.resize(),
        this.$element.on("click.dismiss.bs.modal", '[data-dismiss="modal"]', r.proxy(this.hide, this)),
        this.$dialog.on("mousedown.dismiss.bs.modal", function() {
            i.$element.one("mouseup.dismiss.bs.modal", function(t) {
                r(t.target).is(i.$element) && (i.ignoreBackdropClick = !0)
            })
        }),
        this.backdrop(function() {
            var t = r.support.transition && i.$element.hasClass("fade")
              , e = (i.$element.parent().length || i.$element.appendTo(i.$body),
            i.$element.show().scrollTop(0),
            i.adjustDialog(),
            t && i.$element[0].offsetWidth,
            i.$element.addClass("in"),
            i.enforceFocus(),
            r.Event("shown.bs.modal", {
                relatedTarget: n
            }));
            t ? i.$dialog.one("bsTransitionEnd", function() {
                i.$element.trigger("focus").trigger(e)
            }).emulateTransitionEnd(o.TRANSITION_DURATION) : i.$element.trigger("focus").trigger(e)
        }))
    }
    ,
    o.prototype.hide = function(t) {
        t && t.preventDefault(),
        t = r.Event("hide.bs.modal"),
        this.$element.trigger(t),
        this.isShown && !t.isDefaultPrevented() && (this.isShown = !1,
        this.escape(),
        this.resize(),
        r(document).off("focusin.bs.modal"),
        this.$element.removeClass("in").off("click.dismiss.bs.modal").off("mouseup.dismiss.bs.modal"),
        this.$dialog.off("mousedown.dismiss.bs.modal"),
        r.support.transition && this.$element.hasClass("fade") ? this.$element.one("bsTransitionEnd", r.proxy(this.hideModal, this)).emulateTransitionEnd(o.TRANSITION_DURATION) : this.hideModal())
    }
    ,
    o.prototype.enforceFocus = function() {
        r(document).off("focusin.bs.modal").on("focusin.bs.modal", r.proxy(function(t) {
            document === t.target || this.$element[0] === t.target || this.$element.has(t.target).length || this.$element.trigger("focus")
        }, this))
    }
    ,
    o.prototype.escape = function() {
        this.isShown && this.options.keyboard ? this.$element.on("keydown.dismiss.bs.modal", r.proxy(function(t) {
            27 == t.which && this.hide()
        }, this)) : this.isShown || this.$element.off("keydown.dismiss.bs.modal")
    }
    ,
    o.prototype.resize = function() {
        this.isShown ? r(window).on("resize.bs.modal", r.proxy(this.handleUpdate, this)) : r(window).off("resize.bs.modal")
    }
    ,
    o.prototype.hideModal = function() {
        var t = this;
        this.$element.hide(),
        this.backdrop(function() {
            t.$body.removeClass("modal-open"),
            t.resetAdjustments(),
            t.resetScrollbar(),
            t.$element.trigger("hidden.bs.modal")
        })
    }
    ,
    o.prototype.removeBackdrop = function() {
        this.$backdrop && this.$backdrop.remove(),
        this.$backdrop = null
    }
    ,
    o.prototype.backdrop = function(t) {
        var e, n = this, i = this.$element.hasClass("fade") ? "fade" : "";
        this.isShown && this.options.backdrop ? (e = r.support.transition && i,
        this.$backdrop = r(document.createElement("div")).addClass("modal-backdrop " + i).appendTo(this.$body),
        this.$element.on("click.dismiss.bs.modal", r.proxy(function(t) {
            this.ignoreBackdropClick ? this.ignoreBackdropClick = !1 : t.target === t.currentTarget && ("static" == this.options.backdrop ? this.$element[0].focus() : this.hide())
        }, this)),
        e && this.$backdrop[0].offsetWidth,
        this.$backdrop.addClass("in"),
        t && (e ? this.$backdrop.one("bsTransitionEnd", t).emulateTransitionEnd(o.BACKDROP_TRANSITION_DURATION) : t())) : !this.isShown && this.$backdrop ? (this.$backdrop.removeClass("in"),
        i = function() {
            n.removeBackdrop(),
            t && t()
        }
        ,
        r.support.transition && this.$element.hasClass("fade") ? this.$backdrop.one("bsTransitionEnd", i).emulateTransitionEnd(o.BACKDROP_TRANSITION_DURATION) : i()) : t && t()
    }
    ,
    o.prototype.handleUpdate = function() {
        this.adjustDialog()
    }
    ,
    o.prototype.adjustDialog = function() {
        var t = this.$element[0].scrollHeight > document.documentElement.clientHeight;
        this.$element.css({
            paddingLeft: !this.bodyIsOverflowing && t ? this.scrollbarWidth : "",
            paddingRight: this.bodyIsOverflowing && !t ? this.scrollbarWidth : ""
        })
    }
    ,
    o.prototype.resetAdjustments = function() {
        this.$element.css({
            paddingLeft: "",
            paddingRight: ""
        })
    }
    ,
    o.prototype.checkScrollbar = function() {
        var t, e = window.innerWidth;
        e || (e = (t = document.documentElement.getBoundingClientRect()).right - Math.abs(t.left)),
        this.bodyIsOverflowing = document.body.clientWidth < e,
        this.scrollbarWidth = this.measureScrollbar()
    }
    ,
    o.prototype.setScrollbar = function() {
        var t = parseInt(this.$body.css("padding-right") || 0, 10)
          , s = (this.originalBodyPad = document.body.style.paddingRight || "",
        this.scrollbarWidth);
        this.bodyIsOverflowing && (this.$body.css("padding-right", t + s),
        r(this.fixedContent).each(function(t, e) {
            var n = e.style.paddingRight
              , i = r(e).css("padding-right");
            r(e).data("padding-right", n).css("padding-right", parseFloat(i) + s + "px")
        }))
    }
    ,
    o.prototype.resetScrollbar = function() {
        this.$body.css("padding-right", this.originalBodyPad),
        r(this.fixedContent).each(function(t, e) {
            var n = r(e).data("padding-right");
            r(e).removeData("padding-right"),
            e.style.paddingRight = n || ""
        })
    }
    ,
    o.prototype.measureScrollbar = function() {
        var t = document.createElement("div")
          , e = (t.className = "modal-scrollbar-measure",
        this.$body.append(t),
        t.offsetWidth - t.clientWidth);
        return this.$body[0].removeChild(t),
        e
    }
    ;
    var t = r.fn.modal;
    r.fn.modal = a,
    r.fn.modal.Constructor = o,
    r.fn.modal.noConflict = function() {
        return r.fn.modal = t,
        this
    }
    ,
    r(document).on("click.bs.modal.data-api", '[data-toggle="modal"]', function(t) {
        var e = r(this)
          , n = e.attr("href")
          , i = e.attr("data-target") || n && n.replace(/.*(?=#[^\s]+$)/, "")
          , s = r(document).find(i)
          , i = s.data("bs.modal") ? "toggle" : r.extend({
            remote: !/#/.test(n) && n
        }, s.data(), e.data());
        e.is("a") && t.preventDefault(),
        s.one("show.bs.modal", function(t) {
            t.isDefaultPrevented() || s.one("hidden.bs.modal", function() {
                e.is(":visible") && e.trigger("focus")
            })
        }),
        a.call(s, i, this)
    })
}(jQuery),
function(p) {
    "use strict";
    var i = ["sanitize", "whiteList", "sanitizeFn"]
      , f = ["background", "cite", "href", "itemtype", "longdesc", "poster", "src", "xlink:href"]
      , t = {
        "*": ["class", "dir", "id", "lang", "role", /^aria-[\w-]*$/i],
        a: ["target", "href", "title", "rel"],
        area: [],
        b: [],
        br: [],
        col: [],
        code: [],
        div: [],
        em: [],
        hr: [],
        h1: [],
        h2: [],
        h3: [],
        h4: [],
        h5: [],
        h6: [],
        i: [],
        img: ["src", "alt", "title", "width", "height"],
        li: [],
        ol: [],
        p: [],
        pre: [],
        s: [],
        small: [],
        span: [],
        sub: [],
        sup: [],
        strong: [],
        u: [],
        ul: []
    }
      , g = /^(?:(?:https?|mailto|ftp|tel|file):|[^&:/?#]*(?:[/?#]|$))/gi
      , m = /^data:(?:image\/(?:bmp|gif|jpeg|jpg|png|tiff|webp)|video\/(?:mpeg|mp4|ogg|webm)|audio\/(?:mp3|oga|ogg|opus));base64,[a-z0-9+/]+=*$/i;
    function s(t, e, n) {
        if (0 === t.length)
            return t;
        if (n && "function" == typeof n)
            return n(t);
        if (!document.implementation || !document.implementation.createHTMLDocument)
            return t;
        for (var n = document.implementation.createHTMLDocument("sanitization"), i = (n.body.innerHTML = t,
        p.map(e, function(t, e) {
            return e
        })), s = p(n.body).find("*"), r = 0, o = s.length; r < o; r++) {
            var a = s[r]
              , l = a.nodeName.toLowerCase();
            if (-1 === p.inArray(l, i))
                a.parentNode.removeChild(a);
            else
                for (var c = p.map(a.attributes, function(t) {
                    return t
                }), u = [].concat(e["*"] || [], e[l] || []), d = 0, h = c.length; d < h; d++)
                    !function(t, e) {
                        var n = t.nodeName.toLowerCase();
                        if (-1 !== p.inArray(n, e))
                            return -1 === p.inArray(n, f) || Boolean(t.nodeValue.match(g) || t.nodeValue.match(m));
                        for (var i = p(e).filter(function(t, e) {
                            return e instanceof RegExp
                        }), s = 0, r = i.length; s < r; s++)
                            if (n.match(i[s]))
                                return 1
                    }(c[d], u) && a.removeAttribute(c[d].nodeName)
        }
        return n.body.innerHTML
    }
    function l(t, e) {
        this.type = null,
        this.options = null,
        this.enabled = null,
        this.timeout = null,
        this.hoverState = null,
        this.$element = null,
        this.inState = null,
        this.init("tooltip", t, e)
    }
    l.VERSION = "3.4.1",
    l.TRANSITION_DURATION = 150,
    l.DEFAULTS = {
        animation: !0,
        placement: "top",
        selector: !1,
        template: '<div class="tooltip" role="tooltip"><div class="tooltip-arrow"></div><div class="tooltip-inner"></div></div>',
        trigger: "hover focus",
        title: "",
        delay: 0,
        html: !1,
        container: !1,
        viewport: {
            selector: "body",
            padding: 0
        },
        sanitize: !0,
        sanitizeFn: null,
        whiteList: t
    },
    l.prototype.init = function(t, e, n) {
        if (this.enabled = !0,
        this.type = t,
        this.$element = p(e),
        this.options = this.getOptions(n),
        this.$viewport = this.options.viewport && p(document).find(p.isFunction(this.options.viewport) ? this.options.viewport.call(this, this.$element) : this.options.viewport.selector || this.options.viewport),
        this.inState = {
            click: !1,
            hover: !1,
            focus: !1
        },
        this.$element[0]instanceof document.constructor && !this.options.selector)
            throw new Error("`selector` option must be specified when initializing " + this.type + " on the window.document object!");
        for (var i = this.options.trigger.split(" "), s = i.length; s--; ) {
            var r, o = i[s];
            "click" == o ? this.$element.on("click." + this.type, this.options.selector, p.proxy(this.toggle, this)) : "manual" != o && (r = "hover" == o ? "mouseleave" : "focusout",
            this.$element.on(("hover" == o ? "mouseenter" : "focusin") + "." + this.type, this.options.selector, p.proxy(this.enter, this)),
            this.$element.on(r + "." + this.type, this.options.selector, p.proxy(this.leave, this)))
        }
        this.options.selector ? this._options = p.extend({}, this.options, {
            trigger: "manual",
            selector: ""
        }) : this.fixTitle()
    }
    ,
    l.prototype.getDefaults = function() {
        return l.DEFAULTS
    }
    ,
    l.prototype.getOptions = function(t) {
        var e, n = this.$element.data();
        for (e in n)
            n.hasOwnProperty(e) && -1 !== p.inArray(e, i) && delete n[e];
        return (t = p.extend({}, this.getDefaults(), n, t)).delay && "number" == typeof t.delay && (t.delay = {
            show: t.delay,
            hide: t.delay
        }),
        t.sanitize && (t.template = s(t.template, t.whiteList, t.sanitizeFn)),
        t
    }
    ,
    l.prototype.getDelegateOptions = function() {
        var n = {}
          , i = this.getDefaults();
        return this._options && p.each(this._options, function(t, e) {
            i[t] != e && (n[t] = e)
        }),
        n
    }
    ,
    l.prototype.enter = function(t) {
        var e = t instanceof this.constructor ? t : p(t.currentTarget).data("bs." + this.type);
        if (e || (e = new this.constructor(t.currentTarget,this.getDelegateOptions()),
        p(t.currentTarget).data("bs." + this.type, e)),
        t instanceof p.Event && (e.inState["focusin" == t.type ? "focus" : "hover"] = !0),
        e.tip().hasClass("in") || "in" == e.hoverState)
            e.hoverState = "in";
        else {
            if (clearTimeout(e.timeout),
            e.hoverState = "in",
            !e.options.delay || !e.options.delay.show)
                return e.show();
            e.timeout = setTimeout(function() {
                "in" == e.hoverState && e.show()
            }, e.options.delay.show)
        }
    }
    ,
    l.prototype.isInStateTrue = function() {
        for (var t in this.inState)
            if (this.inState[t])
                return !0;
        return !1
    }
    ,
    l.prototype.leave = function(t) {
        var e = t instanceof this.constructor ? t : p(t.currentTarget).data("bs." + this.type);
        if (e || (e = new this.constructor(t.currentTarget,this.getDelegateOptions()),
        p(t.currentTarget).data("bs." + this.type, e)),
        t instanceof p.Event && (e.inState["focusout" == t.type ? "focus" : "hover"] = !1),
        !e.isInStateTrue()) {
            if (clearTimeout(e.timeout),
            e.hoverState = "out",
            !e.options.delay || !e.options.delay.hide)
                return e.hide();
            e.timeout = setTimeout(function() {
                "out" == e.hoverState && e.hide()
            }, e.options.delay.hide)
        }
    }
    ,
    l.prototype.show = function() {
        var e, t, n, i, s, r, o, a = p.Event("show.bs." + this.type);
        this.hasContent() && this.enabled && (this.$element.trigger(a),
        s = p.contains(this.$element[0].ownerDocument.documentElement, this.$element[0]),
        !a.isDefaultPrevented() && s && (a = (e = this).tip(),
        s = this.getUID(this.type),
        this.setContent(),
        a.attr("id", s),
        this.$element.attr("aria-describedby", s),
        this.options.animation && a.addClass("fade"),
        s = "function" == typeof this.options.placement ? this.options.placement.call(this, a[0], this.$element[0]) : this.options.placement,
        (o = (t = /\s?auto?\s?/i).test(s)) && (s = s.replace(t, "") || "top"),
        a.detach().css({
            top: 0,
            left: 0,
            display: "block"
        }).addClass(s).data("bs." + this.type, this),
        this.options.container ? a.appendTo(p(document).find(this.options.container)) : a.insertAfter(this.$element),
        this.$element.trigger("inserted.bs." + this.type),
        t = this.getPosition(),
        n = a[0].offsetWidth,
        i = a[0].offsetHeight,
        o && (o = s,
        r = this.getPosition(this.$viewport),
        s = "bottom" == s && t.bottom + i > r.bottom ? "top" : "top" == s && t.top - i < r.top ? "bottom" : "right" == s && t.right + n > r.width ? "left" : "left" == s && t.left - n < r.left ? "right" : s,
        a.removeClass(o).addClass(s)),
        r = this.getCalculatedOffset(s, t, n, i),
        this.applyPlacement(r, s),
        o = function() {
            var t = e.hoverState;
            e.$element.trigger("shown.bs." + e.type),
            e.hoverState = null,
            "out" == t && e.leave(e)
        }
        ,
        p.support.transition && this.$tip.hasClass("fade") ? a.one("bsTransitionEnd", o).emulateTransitionEnd(l.TRANSITION_DURATION) : o()))
    }
    ,
    l.prototype.applyPlacement = function(t, e) {
        var n = this.tip()
          , i = n[0].offsetWidth
          , s = n[0].offsetHeight
          , r = parseInt(n.css("margin-top"), 10)
          , o = parseInt(n.css("margin-left"), 10)
          , r = (isNaN(r) && (r = 0),
        isNaN(o) && (o = 0),
        t.top += r,
        t.left += o,
        p.offset.setOffset(n[0], p.extend({
            using: function(t) {
                n.css({
                    top: Math.round(t.top),
                    left: Math.round(t.left)
                })
            }
        }, t), 0),
        n.addClass("in"),
        n[0].offsetWidth)
          , o = n[0].offsetHeight
          , a = ("top" == e && o != s && (t.top = t.top + s - o),
        this.getViewportAdjustedDelta(e, t, r, o))
          , e = (a.left ? t.left += a.left : t.top += a.top,
        /top|bottom/.test(e))
          , i = e ? 2 * a.left - i + r : 2 * a.top - s + o
          , r = e ? "offsetWidth" : "offsetHeight";
        n.offset(t),
        this.replaceArrow(i, n[0][r], e)
    }
    ,
    l.prototype.replaceArrow = function(t, e, n) {
        this.arrow().css(n ? "left" : "top", 50 * (1 - t / e) + "%").css(n ? "top" : "left", "")
    }
    ,
    l.prototype.setContent = function() {
        var t = this.tip()
          , e = this.getTitle();
        this.options.html ? (this.options.sanitize && (e = s(e, this.options.whiteList, this.options.sanitizeFn)),
        t.find(".tooltip-inner").html(e)) : t.find(".tooltip-inner").text(e),
        t.removeClass("fade in top bottom left right")
    }
    ,
    l.prototype.hide = function(t) {
        var e = this
          , n = p(this.$tip)
          , i = p.Event("hide.bs." + this.type);
        function s() {
            "in" != e.hoverState && n.detach(),
            e.$element && e.$element.removeAttr("aria-describedby").trigger("hidden.bs." + e.type),
            t && t()
        }
        if (this.$element.trigger(i),
        !i.isDefaultPrevented())
            return n.removeClass("in"),
            p.support.transition && n.hasClass("fade") ? n.one("bsTransitionEnd", s).emulateTransitionEnd(l.TRANSITION_DURATION) : s(),
            this.hoverState = null,
            this
    }
    ,
    l.prototype.fixTitle = function() {
        var t = this.$element;
        !t.attr("title") && "string" == typeof t.attr("data-original-title") || t.attr("data-original-title", t.attr("title") || "").attr("title", "")
    }
    ,
    l.prototype.hasContent = function() {
        return this.getTitle()
    }
    ,
    l.prototype.getPosition = function(t) {
        var e = (t = t || this.$element)[0]
          , n = "BODY" == e.tagName
          , i = e.getBoundingClientRect()
          , e = (null == i.width && (i = p.extend({}, i, {
            width: i.right - i.left,
            height: i.bottom - i.top
        })),
        window.SVGElement && e instanceof window.SVGElement)
          , e = n ? {
            top: 0,
            left: 0
        } : e ? null : t.offset()
          , t = {
            scroll: n ? document.documentElement.scrollTop || document.body.scrollTop : t.scrollTop()
        }
          , n = n ? {
            width: p(window).width(),
            height: p(window).height()
        } : null;
        return p.extend({}, i, t, n, e)
    }
    ,
    l.prototype.getCalculatedOffset = function(t, e, n, i) {
        return "bottom" == t ? {
            top: e.top + e.height,
            left: e.left + e.width / 2 - n / 2
        } : "top" == t ? {
            top: e.top - i,
            left: e.left + e.width / 2 - n / 2
        } : "left" == t ? {
            top: e.top + e.height / 2 - i / 2,
            left: e.left - n
        } : {
            top: e.top + e.height / 2 - i / 2,
            left: e.left + e.width
        }
    }
    ,
    l.prototype.getViewportAdjustedDelta = function(t, e, n, i) {
        var s = {
            top: 0,
            left: 0
        };
        if (!this.$viewport)
            return s;
        var r = this.options.viewport && this.options.viewport.padding || 0
          , o = this.getPosition(this.$viewport);
        return /right|left/.test(t) ? (t = e.top - r - o.scroll,
        i = e.top + r - o.scroll + i,
        t < o.top ? s.top = o.top - t : i > o.top + o.height && (s.top = o.top + o.height - i)) : (t = e.left - r,
        i = e.left + r + n,
        t < o.left ? s.left = o.left - t : i > o.right && (s.left = o.left + o.width - i)),
        s
    }
    ,
    l.prototype.getTitle = function() {
        var t = this.$element
          , e = this.options;
        return t.attr("data-original-title") || ("function" == typeof e.title ? e.title.call(t[0]) : e.title)
    }
    ,
    l.prototype.getUID = function(t) {
        for (; t += ~~(1e6 * Math.random()),
        document.getElementById(t); )
            ;
        return t
    }
    ,
    l.prototype.tip = function() {
        if (this.$tip || (this.$tip = p(this.options.template),
        1 == this.$tip.length))
            return this.$tip;
        throw new Error(this.type + " `template` option must consist of exactly 1 top-level element!")
    }
    ,
    l.prototype.arrow = function() {
        return this.$arrow = this.$arrow || this.tip().find(".tooltip-arrow")
    }
    ,
    l.prototype.enable = function() {
        this.enabled = !0
    }
    ,
    l.prototype.disable = function() {
        this.enabled = !1
    }
    ,
    l.prototype.toggleEnabled = function() {
        this.enabled = !this.enabled
    }
    ,
    l.prototype.toggle = function(t) {
        var e = this;
        t && !(e = p(t.currentTarget).data("bs." + this.type)) && (e = new this.constructor(t.currentTarget,this.getDelegateOptions()),
        p(t.currentTarget).data("bs." + this.type, e)),
        t ? (e.inState.click = !e.inState.click,
        e.isInStateTrue() ? e.enter(e) : e.leave(e)) : e.tip().hasClass("in") ? e.leave(e) : e.enter(e)
    }
    ,
    l.prototype.destroy = function() {
        var t = this;
        clearTimeout(this.timeout),
        this.hide(function() {
            t.$element.off("." + t.type).removeData("bs." + t.type),
            t.$tip && t.$tip.detach(),
            t.$tip = null,
            t.$arrow = null,
            t.$viewport = null,
            t.$element = null
        })
    }
    ,
    l.prototype.sanitizeHtml = function(t) {
        return s(t, this.options.whiteList, this.options.sanitizeFn)
    }
    ;
    var e = p.fn.tooltip;
    p.fn.tooltip = function(i) {
        return this.each(function() {
            var t = p(this)
              , e = t.data("bs.tooltip")
              , n = "object" == typeof i && i;
            !e && /destroy|hide/.test(i) || (e || t.data("bs.tooltip", e = new l(this,n)),
            "string" == typeof i && e[i]())
        })
    }
    ,
    p.fn.tooltip.Constructor = l,
    p.fn.tooltip.noConflict = function() {
        return p.fn.tooltip = e,
        this
    }
}(jQuery),
function(s) {
    "use strict";
    function r(t, e) {
        this.init("popover", t, e)
    }
    if (!s.fn.tooltip)
        throw new Error("Popover requires tooltip.js");
    r.VERSION = "3.4.1",
    r.DEFAULTS = s.extend({}, s.fn.tooltip.Constructor.DEFAULTS, {
        placement: "right",
        trigger: "click",
        content: "",
        template: '<div class="popover" role="tooltip"><div class="arrow"></div><h3 class="popover-title"></h3><div class="popover-content"></div></div>'
    }),
    ((r.prototype = s.extend({}, s.fn.tooltip.Constructor.prototype)).constructor = r).prototype.getDefaults = function() {
        return r.DEFAULTS
    }
    ,
    r.prototype.setContent = function() {
        var t, e = this.tip(), n = this.getTitle(), i = this.getContent();
        this.options.html ? (t = typeof i,
        this.options.sanitize && (n = this.sanitizeHtml(n),
        "string" == t && (i = this.sanitizeHtml(i))),
        e.find(".popover-title").html(n),
        e.find(".popover-content").children().detach().end()["string" == t ? "html" : "append"](i)) : (e.find(".popover-title").text(n),
        e.find(".popover-content").children().detach().end().text(i)),
        e.removeClass("fade top bottom left right in"),
        e.find(".popover-title").html() || e.find(".popover-title").hide()
    }
    ,
    r.prototype.hasContent = function() {
        return this.getTitle() || this.getContent()
    }
    ,
    r.prototype.getContent = function() {
        var t = this.$element
          , e = this.options;
        return t.attr("data-content") || ("function" == typeof e.content ? e.content.call(t[0]) : e.content)
    }
    ,
    r.prototype.arrow = function() {
        return this.$arrow = this.$arrow || this.tip().find(".arrow")
    }
    ;
    var t = s.fn.popover;
    s.fn.popover = function(i) {
        return this.each(function() {
            var t = s(this)
              , e = t.data("bs.popover")
              , n = "object" == typeof i && i;
            !e && /destroy|hide/.test(i) || (e || t.data("bs.popover", e = new r(this,n)),
            "string" == typeof i && e[i]())
        })
    }
    ,
    s.fn.popover.Constructor = r,
    s.fn.popover.noConflict = function() {
        return s.fn.popover = t,
        this
    }
}(jQuery),
function(s) {
    "use strict";
    function i(t, e) {
        this.$body = s(document.body),
        this.$scrollElement = s(t).is(document.body) ? s(window) : s(t),
        this.options = s.extend({}, i.DEFAULTS, e),
        this.selector = (this.options.target || "") + " .nav li > a",
        this.offsets = [],
        this.targets = [],
        this.activeTarget = null,
        this.scrollHeight = 0,
        this.$scrollElement.on("scroll.bs.scrollspy", s.proxy(this.process, this)),
        this.refresh(),
        this.process()
    }
    function e(n) {
        return this.each(function() {
            var t = s(this)
              , e = t.data("bs.scrollspy");
            e || t.data("bs.scrollspy", e = new i(this,"object" == typeof n && n)),
            "string" == typeof n && e[n]()
        })
    }
    i.VERSION = "3.4.1",
    i.DEFAULTS = {
        offset: 10
    },
    i.prototype.getScrollHeight = function() {
        return this.$scrollElement[0].scrollHeight || Math.max(this.$body[0].scrollHeight, document.documentElement.scrollHeight)
    }
    ,
    i.prototype.refresh = function() {
        var t = this
          , n = "offset"
          , i = 0;
        this.offsets = [],
        this.targets = [],
        this.scrollHeight = this.getScrollHeight(),
        s.isWindow(this.$scrollElement[0]) || (n = "position",
        i = this.$scrollElement.scrollTop()),
        this.$body.find(this.selector).map(function() {
            var t = s(this)
              , t = t.data("target") || t.attr("href")
              , e = /^#./.test(t) && s(t);
            return e && e.length && e.is(":visible") ? [[e[n]().top + i, t]] : null
        }).sort(function(t, e) {
            return t[0] - e[0]
        }).each(function() {
            t.offsets.push(this[0]),
            t.targets.push(this[1])
        })
    }
    ,
    i.prototype.process = function() {
        var t, e = this.$scrollElement.scrollTop() + this.options.offset, n = this.getScrollHeight(), i = this.options.offset + n - this.$scrollElement.height(), s = this.offsets, r = this.targets, o = this.activeTarget;
        if (this.scrollHeight != n && this.refresh(),
        i <= e)
            return o != (t = r[r.length - 1]) && this.activate(t);
        if (o && e < s[0])
            return this.activeTarget = null,
            this.clear();
        for (t = s.length; t--; )
            o != r[t] && e >= s[t] && (void 0 === s[t + 1] || e < s[t + 1]) && this.activate(r[t])
    }
    ,
    i.prototype.activate = function(t) {
        this.activeTarget = t,
        this.clear();
        t = this.selector + '[data-target="' + t + '"],' + this.selector + '[href="' + t + '"]',
        t = s(t).parents("li").addClass("active");
        (t = t.parent(".dropdown-menu").length ? t.closest("li.dropdown").addClass("active") : t).trigger("activate.bs.scrollspy")
    }
    ,
    i.prototype.clear = function() {
        s(this.selector).parentsUntil(this.options.target, ".active").removeClass("active")
    }
    ;
    var t = s.fn.scrollspy;
    s.fn.scrollspy = e,
    s.fn.scrollspy.Constructor = i,
    s.fn.scrollspy.noConflict = function() {
        return s.fn.scrollspy = t,
        this
    }
    ,
    s(window).on("load.bs.scrollspy.data-api", function() {
        s('[data-spy="scroll"]').each(function() {
            var t = s(this);
            e.call(t, t.data())
        })
    })
}(jQuery),
function(o) {
    "use strict";
    function a(t) {
        this.element = o(t)
    }
    function e(n) {
        return this.each(function() {
            var t = o(this)
              , e = t.data("bs.tab");
            e || t.data("bs.tab", e = new a(this)),
            "string" == typeof n && e[n]()
        })
    }
    a.VERSION = "3.4.1",
    a.TRANSITION_DURATION = 150,
    a.prototype.show = function() {
        var t, e, n, i = this.element, s = i.closest("ul:not(.dropdown-menu)"), r = (r = i.data("target")) || (r = i.attr("href")) && r.replace(/.*(?=#[^\s]*$)/, "");
        i.parent("li").hasClass("active") || (t = s.find(".active:last a"),
        e = o.Event("hide.bs.tab", {
            relatedTarget: i[0]
        }),
        n = o.Event("show.bs.tab", {
            relatedTarget: t[0]
        }),
        t.trigger(e),
        i.trigger(n),
        n.isDefaultPrevented() || e.isDefaultPrevented() || (n = o(document).find(r),
        this.activate(i.closest("li"), s),
        this.activate(n, n.parent(), function() {
            t.trigger({
                type: "hidden.bs.tab",
                relatedTarget: i[0]
            }),
            i.trigger({
                type: "shown.bs.tab",
                relatedTarget: t[0]
            })
        })))
    }
    ,
    a.prototype.activate = function(t, e, n) {
        var i = e.find("> .active")
          , s = n && o.support.transition && (i.length && i.hasClass("fade") || !!e.find("> .fade").length);
        function r() {
            i.removeClass("active").find("> .dropdown-menu > .active").removeClass("active").end().find('[data-toggle="tab"]').attr("aria-expanded", !1),
            t.addClass("active").find('[data-toggle="tab"]').attr("aria-expanded", !0),
            s ? (t[0].offsetWidth,
            t.addClass("in")) : t.removeClass("fade"),
            t.parent(".dropdown-menu").length && t.closest("li.dropdown").addClass("active").end().find('[data-toggle="tab"]').attr("aria-expanded", !0),
            n && n()
        }
        i.length && s ? i.one("bsTransitionEnd", r).emulateTransitionEnd(a.TRANSITION_DURATION) : r(),
        i.removeClass("in")
    }
    ;
    function t(t) {
        t.preventDefault(),
        e.call(o(this), "show")
    }
    var n = o.fn.tab;
    o.fn.tab = e,
    o.fn.tab.Constructor = a,
    o.fn.tab.noConflict = function() {
        return o.fn.tab = n,
        this
    }
    ;
    o(document).on("click.bs.tab.data-api", '[data-toggle="tab"]', t).on("click.bs.tab.data-api", '[data-toggle="pill"]', t)
}(jQuery),
function(i) {
    "use strict";
    i.fn.emulateTransitionEnd = function(t) {
        var e = !1
          , n = this;
        i(this).one("bsTransitionEnd", function() {
            e = !0
        });
        return setTimeout(function() {
            e || i(n).trigger(i.support.transition.end)
        }, t),
        this
    }
    ,
    i(function() {
        i.support.transition = function() {
            var t, e = document.createElement("bootstrap"), n = {
                WebkitTransition: "webkitTransitionEnd",
                MozTransition: "transitionend",
                OTransition: "oTransitionEnd otransitionend",
                transition: "transitionend"
            };
            for (t in n)
                if (void 0 !== e.style[t])
                    return {
                        end: n[t]
                    };
            return !1
        }(),
        i.support.transition && (i.event.special.bsTransitionEnd = {
            bindType: i.support.transition.end,
            delegateType: i.support.transition.end,
            handle: function(t) {
                if (i(t.target).is(this))
                    return t.handleObj.handler.apply(this, arguments)
            }
        })
    })
}(jQuery),
function(o) {
    "use strict";
    function r(t) {
        return (t || "ui-id") + "-" + Math.floor(1e3 * Math.random() + 1)
    }
    function t(t, e, n) {
        var i = (t.attr(e) || "").split(/\s+/);
        -1 !== (n = o.inArray(n, i)) && i.splice(n, 1),
        (i = o.trim(i.join(" "))) ? t.attr(e, i) : t.removeAttr(e)
    }
    o(".alert").attr("role", "alert"),
    o(".close").removeAttr("aria-hidden").wrapInner('<span aria-hidden="true"></span>').append('<span class="sr-only">Close</span>');
    var n = o.fn.tooltip.Constructor.prototype.show
      , e = o.fn.tooltip.Constructor.prototype.hide
      , i = (o.fn.tooltip.Constructor.prototype.show = function() {
        n.apply(this, arguments);
        var t = this.tip()
          , e = t.attr("id") || r("ui-tooltip");
        t.attr({
            role: "tooltip",
            id: e
        }),
        this.$element.attr("aria-describedby", e)
    }
    ,
    o.fn.tooltip.Constructor.prototype.hide = function() {
        return e.apply(this, arguments),
        t(this.$element, "aria-describedby", this.tip().attr("id")),
        this
    }
    ,
    o.fn.popover.Constructor.prototype.setContent)
      , s = o.fn.popover.Constructor.prototype.hide
      , a = (o.fn.popover.Constructor.prototype.setContent = function() {
        i.apply(this, arguments);
        var t = this.tip()
          , e = t.attr("id") || r("ui-tooltip");
        t.attr({
            role: "alert",
            id: e
        }),
        this.$element.attr("aria-describedby", e),
        this.$element.focus()
    }
    ,
    o.fn.popover.Constructor.prototype.hide = function() {
        return s.apply(this, arguments),
        t(this.$element, "aria-describedby", this.tip().attr("id")),
        this
    }
    ,
    o(".modal-dialog").attr({
        role: "document"
    }),
    o.fn.modal.Constructor.prototype.hide)
      , l = (o.fn.modal.Constructor.prototype.hide = function() {
        var t = this.$element.parent().find('[data-target="#' + this.$element.attr("id") + '"]');
        a.apply(this, arguments),
        t.focus()
    }
    ,
    "[data-toggle=dropdown]")
      , c = (o(l).parent().find("ul").attr("role", "menu").find("li").attr("role", "presentation").find("a").attr({
        role: "menuitem",
        tabIndex: "-1"
    }),
    o(l).attr({
        "aria-haspopup": "true",
        "aria-expanded": "false"
    }),
    o(l).parent().on("shown.bs.dropdown", function(t) {
        o(this).find(l).attr("aria-expanded", "true")
    }),
    o(l).parent().on("hidden.bs.dropdown", function(t) {
        o(this).find(l).attr("aria-expanded", "false")
    }),
    o.fn.dropdown.Constructor.prototype.keydown = function(t) {
        /(32)/.test(t.keyCode) && (o(this).parent(),
        o(this).trigger("click"),
        t.preventDefault() && t.stopPropagation())
    }
    ,
    o(".nav-tabs, .nav-pills"))
      , u = c.children("li")
      , d = c.find('[data-toggle="tab"], [data-toggle="pill"]')
      , h = (c.attr("role", "tablist"),
    u.attr("role", "presentation"),
    d.attr("role", "tab"),
    d.each(function(t) {
        var e = o(o(this).attr("href"))
          , n = o(this)
          , i = n.attr("id") || r("ui-tab");
        n.attr("id", i),
        n.parent().hasClass("active") ? (n.attr({
            tabIndex: "0",
            "aria-selected": "true",
            "aria-controls": n.attr("href").substr(1)
        }),
        e.attr({
            role: "tabpanel",
            "aria-hidden": "false",
            "aria-labelledby": i
        })) : (n.attr({
            tabIndex: "-1",
            "aria-selected": "false",
            "aria-controls": n.attr("href").substr(1)
        }),
        e.attr({
            role: "tabpanel",
            "aria-hidden": "true",
            "aria-labelledby": i
        }))
    }),
    o.fn.tab.Constructor.prototype.keydown = function(t) {
        var e, n = o(this).closest("ul[role=tablist] "), i = t.which || t.keyCode;
        o(this);
        /(37|38|39|40)/.test(i) && (e = (n = n.find("[role=tab]:visible")).index(n.filter(":focus")),
        38 != i && 37 != i || e--,
        39 != i && 40 != i || e++,
        (e = e < 0 ? n.length - 1 : e) == n.length && (e = 0),
        "tab" === (i = n.eq(e)).attr("role") && i.focus(),
        t.preventDefault(),
        t.stopPropagation())
    }
    ,
    o(document).on("keydown.tab.data-api", '[data-toggle="tab"], [data-toggle="pill"]', o.fn.tab.Constructor.prototype.keydown),
    o.fn.tab.Constructor.prototype.activate)
      , c = (o.fn.tab.Constructor.prototype.activate = function(t, e, n) {
        e = e.find("> .active");
        e.find("[data-toggle=tab], [data-toggle=pill]").attr({
            tabIndex: "-1",
            "aria-selected": !1
        }),
        e.filter(".tab-pane").attr({
            "aria-hidden": !0
        }),
        h.apply(this, arguments),
        t.addClass("active"),
        t.find("[data-toggle=tab], [data-toggle=pill]").attr({
            tabIndex: "0",
            "aria-selected": !0
        }),
        t.filter(".tab-pane").attr({
            "aria-hidden": !1
        })
    }
    ,
    o('[data-toggle="collapse"]'))
      , p = (c.attr({
        role: "tab",
        "aria-selected": "false",
        "aria-expanded": "false"
    }),
    c.each(function(t) {
        var e = o(this)
          , n = e.attr("data-target") ? o(e.attr("data-target")) : o(e.attr("href"))
          , i = e.attr("data-parent")
          , i = i && o(i)
          , s = e.attr("id") || r("ui-collapse");
        o(i).find("div:not(.collapse,.panel-body), h4").attr("role", "presentation"),
        e.attr("id", s),
        i && (i.attr({
            role: "tablist",
            "aria-multiselectable": "true"
        }),
        n.hasClass("in") ? (e.attr({
            "aria-controls": e.attr("href").substr(1),
            "aria-selected": "true",
            "aria-expanded": "true",
            tabindex: "0"
        }),
        n.attr({
            role: "tabpanel",
            tabindex: "0",
            "aria-labelledby": s,
            "aria-hidden": "false"
        })) : (e.attr({
            "aria-controls": e.attr("href").substr(1),
            tabindex: "-1"
        }),
        n.attr({
            role: "tabpanel",
            tabindex: "-1",
            "aria-labelledby": s,
            "aria-hidden": "true"
        })))
    }),
    o.fn.collapse.Constructor.prototype.toggle)
      , f = (o.fn.collapse.Constructor.prototype.toggle = function() {
        var t, e, n, i, s = this.$parent && this.$parent.find('[aria-expanded="true"]');
        s ? (t = s.attr("data-target") || (t = s.attr("href")) && t.replace(/.*(?=#[^\s]+$)/, ""),
        e = o(t),
        n = this.$element,
        this.$parent,
        this.$parent && (i = this.$parent.find('[data-toggle=collapse][href="#' + this.$element.attr("id") + '"]')),
        p.apply(this, arguments),
        o.support.transition && this.$element.one(o.support.transition.end, function() {
            s.attr({
                "aria-selected": "false",
                "aria-expanded": "false",
                tabIndex: "-1"
            }),
            e.attr({
                "aria-hidden": "true",
                tabIndex: "-1"
            }),
            i.attr({
                "aria-selected": "true",
                "aria-expanded": "true",
                tabIndex: "0"
            }),
            n.hasClass("in") ? n.attr({
                "aria-hidden": "false",
                tabIndex: "0"
            }) : (i.attr({
                "aria-selected": "false",
                "aria-expanded": "false"
            }),
            n.attr({
                "aria-hidden": "true",
                tabIndex: "-1"
            }))
        })) : p.apply(this, arguments)
    }
    ,
    o.fn.collapse.Constructor.prototype.keydown = function(t) {
        var e = (i = o(this)).closest("div[role=tablist] ")
          , n = t.which || t.keyCode
          , i = o(this);
        /(32|37|38|39|40)/.test(n) && (32 == n && i.click(),
        e = (i = e.find("[role=tab]")).index(i.filter(":focus")),
        38 != n && 37 != n || e--,
        39 != n && 40 != n || e++,
        (e = e < 0 ? i.length - 1 : e) == i.length && (e = 0),
        i.eq(e).focus(),
        t.preventDefault(),
        t.stopPropagation())
    }
    ,
    o(document).on("keydown.collapse.data-api", '[data-toggle="collapse"]', o.fn.collapse.Constructor.prototype.keydown),
    o(".carousel").each(function(t) {
        var e = o(this)
          , n = e.find('[data-slide="prev"]')
          , i = e.find('[data-slide="next"]')
          , s = e.find(".item")
          , r = s.parent()
          , e = (e.attr({
            "data-interval": "false",
            "data-wrap": "false"
        }),
        r.attr("role", "listbox"),
        s.attr("role", "option"),
        document.createElement("span"))
          , r = (e.setAttribute("class", "sr-only"),
        e.innerHTML = "Previous",
        document.createElement("span"));
        r.setAttribute("class", "sr-only"),
        r.innerHTML = "Next",
        n.attr("role", "button"),
        i.attr("role", "button"),
        n.append(e),
        i.append(r),
        s.each(function() {
            var t = o(this);
            t.hasClass("active") ? t.attr({
                "aria-selected": "true",
                tabindex: "0"
            }) : t.attr({
                "aria-selected": "false",
                tabindex: "-1"
            })
        })
    }),
    o.fn.carousel.Constructor.prototype.slide);
    o.fn.carousel.Constructor.prototype.slide = function(t, e) {
        var n = this.$element.find(".item.active")
          , i = e || n[t]();
        f.apply(this, arguments),
        n.one(o.support.transition.end, function() {
            n.attr({
                "aria-selected": !1,
                tabIndex: "-1"
            }),
            i.attr({
                "aria-selected": !0,
                tabIndex: "0"
            })
        })
    }
    ,
    o.fn.carousel.Constructor.prototype.keydown = function(t) {
        var e, n = o(this), i = n.closest("div[role=listbox]"), s = i.find("[role=option]"), i = i.parent(), r = t.which || t.keyCode;
        /(37|38|39|40)/.test(r) && (e = s.index(s.filter(".active")),
        37 != r && 38 != r || (i.carousel("prev"),
        --e < 0 ? e = s.length - 1 : n.prev().focus()),
        39 != r && 40 != r || (i.carousel("next"),
        ++e == s.length ? e = 0 : n.one(o.support.transition.end, function() {
            n.next().focus()
        })),
        t.preventDefault(),
        t.stopPropagation())
    }
    ,
    o(document).on("keydown.carousel.data-api", "div[role=option]", o.fn.carousel.Constructor.prototype.keydown)
}(jQuery),
function(t) {
    "function" == typeof define && define.amd ? define(["jquery"], t) : "object" == typeof module && "object" == typeof module.exports ? module.exports = t(require("jquery")) : t(jQuery)
}(function(t) {
    return function(E) {
        "use strict";
        var N = E.tablesorter = {
            version: "2.31.3",
            parsers: [],
            widgets: [],
            defaults: {
                theme: "default",
                widthFixed: !1,
                showProcessing: !1,
                headerTemplate: "{content}",
                onRenderTemplate: null,
                onRenderHeader: null,
                cancelSelection: !0,
                tabIndex: !0,
                dateFormat: "mmddyyyy",
                sortMultiSortKey: "shiftKey",
                sortResetKey: "ctrlKey",
                usNumberFormat: !0,
                delayInit: !1,
                serverSideSorting: !1,
                resort: !0,
                headers: {},
                ignoreCase: !0,
                sortForce: null,
                sortList: [],
                sortAppend: null,
                sortStable: !1,
                sortInitialOrder: "asc",
                sortLocaleCompare: !1,
                sortReset: !1,
                sortRestart: !1,
                emptyTo: "bottom",
                stringTo: "max",
                duplicateSpan: !0,
                textExtraction: "basic",
                textAttribute: "data-text",
                textSorter: null,
                numberSorter: null,
                initWidgets: !0,
                widgetClass: "widget-{name}",
                widgets: [],
                widgetOptions: {
                    zebra: ["even", "odd"]
                },
                initialized: null,
                tableClass: "",
                cssAsc: "",
                cssDesc: "",
                cssNone: "",
                cssHeader: "",
                cssHeaderRow: "",
                cssProcessing: "",
                cssChildRow: "tablesorter-childRow",
                cssInfoBlock: "tablesorter-infoOnly",
                cssNoSort: "tablesorter-noSort",
                cssIgnoreRow: "tablesorter-ignoreRow",
                cssIcon: "tablesorter-icon",
                cssIconNone: "",
                cssIconAsc: "",
                cssIconDesc: "",
                cssIconDisabled: "",
                pointerClick: "click",
                pointerDown: "mousedown",
                pointerUp: "mouseup",
                selectorHeaders: "> thead th, > thead td",
                selectorSort: "th, td",
                selectorRemove: ".remove-me",
                debug: !1,
                headerList: [],
                empties: {},
                strings: {},
                parsers: [],
                globalize: 0,
                imgAttr: 0
            },
            css: {
                table: "tablesorter",
                cssHasChild: "tablesorter-hasChildRow",
                childRow: "tablesorter-childRow",
                colgroup: "tablesorter-colgroup",
                header: "tablesorter-header",
                headerRow: "tablesorter-headerRow",
                headerIn: "tablesorter-header-inner",
                icon: "tablesorter-icon",
                processing: "tablesorter-processing",
                sortAsc: "tablesorter-headerAsc",
                sortDesc: "tablesorter-headerDesc",
                sortNone: "tablesorter-headerUnSorted"
            },
            language: {
                sortAsc: "Ascending sort applied, ",
                sortDesc: "Descending sort applied, ",
                sortNone: "No sort applied, ",
                sortDisabled: "sorting is disabled",
                nextAsc: "activate to apply an ascending sort",
                nextDesc: "activate to apply a descending sort",
                nextNone: "activate to remove the sort"
            },
            regex: {
                templateContent: /\{content\}/g,
                templateIcon: /\{icon\}/g,
                templateName: /\{name\}/i,
                spaces: /\s+/g,
                nonWord: /\W/g,
                formElements: /(input|select|button|textarea)/i,
                chunk: /(^([+\-]?(?:\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?)?$|^0x[0-9a-f]+$|\d+)/gi,
                chunks: /(^\\0|\\0$)/,
                hex: /^0x[0-9a-f]+$/i,
                comma: /,/g,
                digitNonUS: /[\s|\.]/g,
                digitNegativeTest: /^\s*\([.\d]+\)/,
                digitNegativeReplace: /^\s*\(([.\d]+)\)/,
                digitTest: /^[\-+(]?\d+[)]?$/,
                digitReplace: /[,.'"\s]/g
            },
            string: {
                max: 1,
                min: -1,
                emptymin: 1,
                emptymax: -1,
                zero: 0,
                none: 0,
                null: 0,
                top: !0,
                bottom: !1
            },
            keyCodes: {
                enter: 13
            },
            dates: {},
            instanceMethods: {},
            setup: function(e, n) {
                var t, i, s, r;
                e && e.tHead && 0 !== e.tBodies.length && !0 !== e.hasInitialized ? (t = "",
                i = E(e),
                s = E.metadata,
                e.hasInitialized = !1,
                e.isProcessing = !0,
                e.config = n,
                E.data(e, "tablesorter", n),
                N.debug(n, "core") && (console[console.group ? "group" : "log"]("Initializing tablesorter v" + N.version),
                E.data(e, "startoveralltimer", new Date)),
                n.supportsDataObject = ((r = E.fn.jquery.split("."))[0] = parseInt(r[0], 10),
                1 < r[0] || 1 === r[0] && 4 <= parseInt(r[1], 10)),
                n.emptyTo = n.emptyTo.toLowerCase(),
                n.stringTo = n.stringTo.toLowerCase(),
                n.last = {
                    sortList: [],
                    clickedIndex: -1
                },
                /tablesorter\-/.test(i.attr("class")) || (t = "" !== n.theme ? " tablesorter-" + n.theme : ""),
                n.namespace ? n.namespace = "." + n.namespace.replace(N.regex.nonWord, "") : n.namespace = ".tablesorter" + Math.random().toString(16).slice(2),
                n.table = e,
                n.$table = i.addClass(N.css.table + " " + n.tableClass + t + " " + n.namespace.slice(1)).attr("role", "grid"),
                n.$headers = i.find(n.selectorHeaders),
                n.$table.children().children("tr").attr("role", "row"),
                n.$tbodies = i.children("tbody:not(." + n.cssInfoBlock + ")").attr({
                    "aria-live": "polite",
                    "aria-relevant": "all"
                }),
                n.$table.children("caption").length && ((t = n.$table.children("caption")[0]).id || (t.id = n.namespace.slice(1) + "caption"),
                n.$table.attr("aria-labelledby", t.id)),
                n.widgetInit = {},
                n.textExtraction = n.$table.attr("data-text-extraction") || n.textExtraction || "basic",
                N.buildHeaders(n),
                N.fixColumnWidth(e),
                N.addWidgetFromClass(e),
                N.applyWidgetOptions(e),
                N.setupParsers(n),
                n.totalRows = 0,
                n.debug && N.validateOptions(n),
                n.delayInit || N.buildCache(n),
                N.bindEvents(e, n.$headers, !0),
                N.bindMethods(n),
                n.supportsDataObject && void 0 !== i.data().sortlist ? n.sortList = i.data().sortlist : s && i.metadata() && i.metadata().sortlist && (n.sortList = i.metadata().sortlist),
                N.applyWidget(e, !0),
                0 < n.sortList.length ? (n.last.sortList = n.sortList,
                N.sortOn(n, n.sortList, {}, !n.initWidgets)) : (N.setHeadersCss(n),
                n.initWidgets && N.applyWidget(e, !1)),
                n.showProcessing && i.unbind("sortBegin" + n.namespace + " sortEnd" + n.namespace).bind("sortBegin" + n.namespace + " sortEnd" + n.namespace, function(t) {
                    clearTimeout(n.timerProcessing),
                    N.isProcessing(e),
                    "sortBegin" === t.type && (n.timerProcessing = setTimeout(function() {
                        N.isProcessing(e, !0)
                    }, 500))
                }),
                e.hasInitialized = !0,
                e.isProcessing = !1,
                N.debug(n, "core") && (console.log("Overall initialization time:" + N.benchmark(E.data(e, "startoveralltimer"))),
                N.debug(n, "core") && console.groupEnd && console.groupEnd()),
                i.triggerHandler("tablesorter-initialized", e),
                "function" == typeof n.initialized && n.initialized(e)) : N.debug(n, "core") && (e.hasInitialized ? console.warn("Stopping initialization. Tablesorter has already been initialized") : console.error("Stopping initialization! No table, thead or tbody", e))
            },
            bindMethods: function(e) {
                var t = e.$table
                  , n = e.namespace
                  , i = "sortReset update updateRows updateAll updateHeaders addRows updateCell updateComplete sorton appendCache updateCache applyWidgetId applyWidgets refreshWidgets destroy mouseup mouseleave ".split(" ").join(n + " ");
                t.unbind(i.replace(N.regex.spaces, " ")).bind("sortReset" + n, function(t, e) {
                    t.stopPropagation(),
                    N.sortReset(this.config, function(t) {
                        t.isApplyingWidgets ? setTimeout(function() {
                            N.applyWidget(t, "", e)
                        }, 100) : N.applyWidget(t, "", e)
                    })
                }).bind("updateAll" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.updateAll(this.config, e, n)
                }).bind("update" + n + " updateRows" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.update(this.config, e, n)
                }).bind("updateHeaders" + n, function(t, e) {
                    t.stopPropagation(),
                    N.updateHeaders(this.config, e)
                }).bind("updateCell" + n, function(t, e, n, i) {
                    t.stopPropagation(),
                    N.updateCell(this.config, e, n, i)
                }).bind("addRows" + n, function(t, e, n, i) {
                    t.stopPropagation(),
                    N.addRows(this.config, e, n, i)
                }).bind("updateComplete" + n, function() {
                    this.isUpdating = !1
                }).bind("sorton" + n, function(t, e, n, i) {
                    t.stopPropagation(),
                    N.sortOn(this.config, e, n, i)
                }).bind("appendCache" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.appendCache(this.config, n),
                    E.isFunction(e) && e(this)
                }).bind("updateCache" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.updateCache(this.config, e, n)
                }).bind("applyWidgetId" + n, function(t, e) {
                    t.stopPropagation(),
                    N.applyWidgetId(this, e)
                }).bind("applyWidgets" + n, function(t, e) {
                    t.stopPropagation(),
                    N.applyWidget(this, !1, e)
                }).bind("refreshWidgets" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.refreshWidgets(this, e, n)
                }).bind("removeWidget" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.removeWidget(this, e, n)
                }).bind("destroy" + n, function(t, e, n) {
                    t.stopPropagation(),
                    N.destroy(this, e, n)
                }).bind("resetToLoadState" + n, function(t) {
                    t.stopPropagation(),
                    N.removeWidget(this, !0, !1);
                    t = E.extend(!0, {}, e.originalSettings);
                    (e = E.extend(!0, {}, N.defaults, t)).originalSettings = t,
                    this.hasInitialized = !1,
                    N.setup(this, e)
                })
            },
            bindEvents: function(t, e, n) {
                var i, s = (t = E(t)[0]).config, r = s.namespace, o = null;
                !0 !== n && (e.addClass(r.slice(1) + "_extra_headers"),
                (i = N.getClosest(e, "table")).length && "TABLE" === i[0].nodeName && i[0] !== t && E(i[0]).addClass(r.slice(1) + "_extra_table")),
                i = (s.pointerDown + " " + s.pointerUp + " " + s.pointerClick + " sort keyup ").replace(N.regex.spaces, " ").split(" ").join(r + " "),
                e.find(s.selectorSort).add(e.filter(s.selectorSort)).unbind(i).bind(i, function(t, e) {
                    var n = E(t.target)
                      , i = " " + t.type + " ";
                    if (!(1 !== (t.which || t.button) && !i.match(" " + s.pointerClick + " | sort | keyup ") || " keyup " === i && t.which !== N.keyCodes.enter || i.match(" " + s.pointerClick + " ") && void 0 !== t.which || i.match(" " + s.pointerUp + " ") && o !== t.target && !0 !== e))
                        return i.match(" " + s.pointerDown + " ") ? (o = t.target,
                        void ("1" === (e = n.jquery.split("."))[0] && e[1] < 4 && t.preventDefault())) : (o = null,
                        i = N.getClosest(E(this), "." + N.css.header),
                        N.regex.formElements.test(t.target.nodeName) || n.hasClass(s.cssNoSort) || 0 < n.parents("." + s.cssNoSort).length || i.hasClass("sorter-false") || 0 < n.parents("button").length ? !s.cancelSelection : (s.delayInit && N.isEmptyObject(s.cache) && N.buildCache(s),
                        s.last.clickedIndex = i.attr("data-column") || i.index(),
                        void ((e = s.$headerIndexed[s.last.clickedIndex][0]) && !e.sortDisabled && N.initSort(s, e, t))))
                }),
                s.cancelSelection && e.attr("unselectable", "on").bind("selectstart", !1).css({
                    "user-select": "none",
                    MozUserSelect: "none"
                })
            },
            buildHeaders: function(a) {
                var t, l, e, n;
                for (a.headerList = [],
                a.headerContent = [],
                a.sortVars = [],
                N.debug(a, "core") && (e = new Date),
                a.columns = N.computeColumnIndex(a.$table.children("thead, tfoot").children("tr")),
                l = a.cssIcon ? '<i class="' + (a.cssIcon === N.css.icon ? N.css.icon : a.cssIcon + " " + N.css.icon) + '"></i>' : "",
                a.$headers = E(E.map(a.$table.find(a.selectorHeaders), function(t, e) {
                    var n, i, s, r, o = E(t);
                    if (!N.getClosest(o, "tr").hasClass(a.cssIgnoreRow))
                        return /(th|td)/i.test(t.nodeName) || (r = N.getClosest(o, "th, td"),
                        o.attr("data-column", r.attr("data-column"))),
                        n = N.getColumnData(a.table, a.headers, e, !0),
                        a.headerContent[e] = o.html(),
                        "" === a.headerTemplate || o.find("." + N.css.headerIn).length || (s = a.headerTemplate.replace(N.regex.templateContent, o.html()).replace(N.regex.templateIcon, o.find("." + N.css.icon).length ? "" : l),
                        a.onRenderTemplate && (i = a.onRenderTemplate.apply(o, [e, s])) && "string" == typeof i && (s = i),
                        o.html('<div class="' + N.css.headerIn + '">' + s + "</div>")),
                        a.onRenderHeader && a.onRenderHeader.apply(o, [e, a, a.$table]),
                        i = parseInt(o.attr("data-column"), 10),
                        t.column = i,
                        r = N.getOrder(N.getData(o, n, "sortInitialOrder") || a.sortInitialOrder),
                        a.sortVars[i] = {
                            count: -1,
                            order: r ? a.sortReset ? [1, 0, 2] : [1, 0] : a.sortReset ? [0, 1, 2] : [0, 1],
                            lockedOrder: !1,
                            sortedBy: ""
                        },
                        void 0 !== (r = N.getData(o, n, "lockedOrder") || !1) && !1 !== r && (a.sortVars[i].lockedOrder = !0,
                        a.sortVars[i].order = N.getOrder(r) ? [1, 1] : [0, 0]),
                        a.headerList[e] = t,
                        o.addClass(N.css.header + " " + a.cssHeader),
                        N.getClosest(o, "tr").addClass(N.css.headerRow + " " + a.cssHeaderRow).attr("role", "row"),
                        a.tabIndex && o.attr("tabindex", 0),
                        t
                })),
                a.$headerIndexed = [],
                n = 0; n < a.columns; n++)
                    N.isEmptyObject(a.sortVars[n]) && (a.sortVars[n] = {}),
                    t = a.$headers.filter('[data-column="' + n + '"]'),
                    a.$headerIndexed[n] = t.length ? (t.not(".sorter-false").length ? t.not(".sorter-false") : t).filter(":last") : E();
                a.$table.find(a.selectorHeaders).attr({
                    scope: "col",
                    role: "columnheader"
                }),
                N.updateHeader(a),
                N.debug(a, "core") && (console.log("Built headers:" + N.benchmark(e)),
                console.log(a.$headers))
            },
            addInstanceMethods: function(t) {
                E.extend(N.instanceMethods, t)
            },
            setupParsers: function(t, e) {
                var n, i, s, r, o, a, l, c, u, d, h, p, f, g, m = t.table, y = 0, v = N.debug(t, "core"), b = {};
                if (t.$tbodies = t.$table.children("tbody:not(." + t.cssInfoBlock + ")"),
                0 === (g = (f = void 0 === e ? t.$tbodies : e).length))
                    return v ? console.warn("Warning: *Empty table!* Not building a parser cache") : "";
                for (v && (p = new Date,
                console[console.group ? "group" : "log"]("Detecting parsers for each column")),
                i = {
                    extractors: [],
                    parsers: []
                }; y < g; ) {
                    if ((n = f[y].rows).length)
                        for (r = t.columns,
                        a = o = 0; a < r; a++) {
                            if ((l = t.$headerIndexed[o]) && l.length && (c = N.getColumnData(m, t.headers, o),
                            h = N.getParserById(N.getData(l, c, "extractor")),
                            d = N.getParserById(N.getData(l, c, "sorter")),
                            u = "false" === N.getData(l, c, "parser"),
                            t.empties[o] = (N.getData(l, c, "empty") || t.emptyTo || (t.emptyToBottom ? "bottom" : "top")).toLowerCase(),
                            t.strings[o] = (N.getData(l, c, "string") || t.stringTo || "max").toLowerCase(),
                            h = h || !1,
                            d = (d = u ? N.getParserById("no-parser") : d) || N.detectParserForColumn(t, n, -1, o),
                            v && (b["(" + o + ") " + l.text()] = {
                                parser: d.id,
                                extractor: h ? h.id : "none",
                                string: t.strings[o],
                                empty: t.empties[o]
                            }),
                            i.parsers[o] = d,
                            i.extractors[o] = h,
                            0 < (s = l[0].colSpan - 1)))
                                for (o += s,
                                r += s; 0 < s + 1; )
                                    i.parsers[o - s] = d,
                                    i.extractors[o - s] = h,
                                    s--;
                            o++
                        }
                    y += i.parsers.length ? g : 1
                }
                v && (N.isEmptyObject(b) ? console.warn("  No parsers detected!") : console[console.table ? "table" : "log"](b),
                console.log("Completed detecting parsers" + N.benchmark(p)),
                console.groupEnd && console.groupEnd()),
                t.parsers = i.parsers,
                t.extractors = i.extractors
            },
            addParser: function(t) {
                for (var e = N.parsers.length, n = !0, i = 0; i < e; i++)
                    N.parsers[i].id.toLowerCase() === t.id.toLowerCase() && (n = !1);
                n && (N.parsers[N.parsers.length] = t)
            },
            getParserById: function(t) {
                if ("false" == t)
                    return !1;
                for (var e = N.parsers.length, n = 0; n < e; n++)
                    if (N.parsers[n].id.toLowerCase() === t.toString().toLowerCase())
                        return N.parsers[n];
                return !1
            },
            detectParserForColumn: function(t, e, n, i) {
                for (var s, r, o, a = N.parsers.length, l = !1, c = "", u = N.debug(t, "core"), d = !0; "" === c && d; )
                    (o = e[++n]) && n < 50 ? o.className.indexOf(N.cssIgnoreRow) < 0 && (l = e[n].cells[i],
                    c = N.getElementText(t, l, i),
                    r = E(l),
                    u && console.log("Checking if value was empty on row " + n + ", column: " + i + ': "' + c + '"')) : d = !1;
                for (; 0 <= --a; )
                    if ((s = N.parsers[a]) && "text" !== s.id && s.is && s.is(c, t.table, l, r))
                        return s;
                return N.getParserById("text")
            },
            getElementText: function(t, e, n) {
                if (!e)
                    return "";
                var i, s = t.textExtraction || "", r = e.jquery ? e : E(e);
                return "string" == typeof s ? "basic" === s && void 0 !== (i = r.attr(t.textAttribute)) ? E.trim(i) : E.trim(e.textContent || r.text()) : "function" == typeof s ? E.trim(s(r[0], t.table, n)) : "function" == typeof (i = N.getColumnData(t.table, s, n)) ? E.trim(i(r[0], t.table, n)) : E.trim(r[0].textContent || r.text())
            },
            getParsedText: function(t, e, n, i) {
                var s = "" + (i = void 0 === i ? N.getElementText(t, e, n) : i)
                  , r = t.parsers[n]
                  , o = t.extractors[n];
                return r && (o && "function" == typeof o.format && (i = o.format(i, t.table, e, n)),
                s = "no-parser" === r.id ? "" : r.format("" + i, t.table, e, n),
                t.ignoreCase && "string" == typeof s && (s = s.toLowerCase())),
                s
            },
            buildCache: function(t, e, n) {
                var i, s, r, o, a, l, c, u, d, h, p, f, g, m, y, v, b, _, w, x, C, k, S = t.table, T = t.parsers, D = N.debug(t, "core");
                if (t.$tbodies = t.$table.children("tbody:not(." + t.cssInfoBlock + ")"),
                c = void 0 === n ? t.$tbodies : n,
                t.cache = {},
                t.totalRows = 0,
                !T)
                    return D ? console.warn("Warning: *Empty table!* Not building a cache") : "";
                for (D && (f = new Date),
                t.showProcessing && N.isProcessing(S, !0),
                l = 0; l < c.length; l++) {
                    for (v = [],
                    i = t.cache[l] = {
                        normalized: []
                    },
                    g = c[l] && c[l].rows.length || 0,
                    o = 0; o < g; ++o)
                        if (m = {
                            child: [],
                            raw: []
                        },
                        d = [],
                        !(u = E(c[l].rows[o])).hasClass(t.selectorRemove.slice(1)))
                            if (u.hasClass(t.cssChildRow) && 0 !== o)
                                for (C = i.normalized.length - 1,
                                (y = i.normalized[C][t.columns]).$row = y.$row.add(u),
                                u.prev().hasClass(t.cssChildRow) || u.prev().addClass(N.css.cssHasChild),
                                h = u.children("th, td"),
                                C = y.child.length,
                                y.child[C] = [],
                                x = t.columns,
                                a = _ = 0; a < x; a++)
                                    (p = h[a]) && (y.child[C][a] = N.getParsedText(t, p, a),
                                    0 < (b = h[a].colSpan - 1) && (_ += b,
                                    x += b)),
                                    _++;
                            else {
                                for (m.$row = u,
                                m.order = o,
                                x = t.columns,
                                a = _ = 0; a < x; ++a) {
                                    if ((p = u[0].cells[a]) && _ < t.columns && (!(w = void 0 !== T[_]) && D && console.warn("No parser found for row: " + o + ", column: " + a + '; cell containing: "' + E(p).text() + '"; does it have a header?'),
                                    s = N.getElementText(t, p, _),
                                    m.raw[_] = s,
                                    r = N.getParsedText(t, p, _, s),
                                    d[_] = r,
                                    w && "numeric" === (T[_].type || "").toLowerCase() && (v[_] = Math.max(Math.abs(r) || 0, v[_] || 0)),
                                    0 < (b = p.colSpan - 1))) {
                                        for (k = 0; k <= b; )
                                            r = t.duplicateSpan || 0 === k ? r : "string" != typeof t.textExtraction && N.getElementText(t, p, _ + k) || "",
                                            m.raw[_ + k] = r,
                                            d[_ + k] = r,
                                            k++;
                                        _ += b,
                                        x += b
                                    }
                                    _++
                                }
                                d[t.columns] = m,
                                i.normalized[i.normalized.length] = d
                            }
                    i.colMax = v,
                    t.totalRows += i.normalized.length
                }
                if (t.showProcessing && N.isProcessing(S),
                D) {
                    for (C = Math.min(5, t.cache[0].normalized.length),
                    console[console.group ? "group" : "log"]("Building cache for " + t.totalRows + " rows (showing " + C + " rows in log) and " + t.columns + " columns" + N.benchmark(f)),
                    s = {},
                    a = 0; a < t.columns; a++)
                        for (_ = 0; _ < C; _++)
                            s["row: " + _] || (s["row: " + _] = {}),
                            s["row: " + _][t.$headerIndexed[a].text()] = t.cache[0].normalized[_][a];
                    console[console.table ? "table" : "log"](s),
                    console.groupEnd && console.groupEnd()
                }
                E.isFunction(e) && e(S)
            },
            getColumnText: function(t, e, n, i) {
                var s, r, o, a, l, c, u, d, h, p, f = "function" == typeof n, g = "all" === e, m = {
                    raw: [],
                    parsed: [],
                    $cell: []
                }, y = (t = E(t)[0]).config;
                if (!N.isEmptyObject(y)) {
                    for (l = y.$tbodies.length,
                    s = 0; s < l; s++)
                        for (c = (o = y.cache[s].normalized).length,
                        r = 0; r < c; r++)
                            a = o[r],
                            i && !a[y.columns].$row.is(i) || (p = !0,
                            d = g ? a.slice(0, y.columns) : a[e],
                            a = a[y.columns],
                            u = g ? a.raw : a.raw[e],
                            h = g ? a.$row.children() : a.$row.children().eq(e),
                            !1 !== (p = f ? n({
                                tbodyIndex: s,
                                rowIndex: r,
                                parsed: d,
                                raw: u,
                                $row: a.$row,
                                $cell: h
                            }) : p) && (m.parsed[m.parsed.length] = d,
                            m.raw[m.raw.length] = u,
                            m.$cell[m.$cell.length] = h));
                    return m
                }
                N.debug(y, "core") && console.warn("No cache found - aborting getColumnText function!")
            },
            setHeadersCss: function(s) {
                function t(t, e) {
                    t.removeClass(o).addClass(a[e]).attr("aria-sort", c[e]).find("." + N.css.icon).removeClass(l[2]).addClass(l[e])
                }
                var e, n, i = s.sortList, r = i.length, o = N.css.sortNone + " " + s.cssNone, a = [N.css.sortAsc + " " + s.cssAsc, N.css.sortDesc + " " + s.cssDesc], l = [s.cssIconAsc, s.cssIconDesc, s.cssIconNone], c = ["ascending", "descending"], u = s.$table.find("tfoot tr").children("td, th").add(E(s.namespace + "_extra_headers")).removeClass(a.join(" ")), d = s.$headers.add(E("thead " + s.namespace + "_extra_headers")).removeClass(a.join(" ")).addClass(o).attr("aria-sort", "none").find("." + N.css.icon).removeClass(l.join(" ")).end();
                for (d.not(".sorter-false").find("." + N.css.icon).addClass(l[2]),
                s.cssIconDisabled && d.filter(".sorter-false").find("." + N.css.icon).addClass(s.cssIconDisabled),
                e = 0; e < r; e++)
                    if (2 !== i[e][1]) {
                        if ((d = (d = s.$headers.filter(function(t) {
                            for (var e = !0, t = s.$headers.eq(t), n = parseInt(t.attr("data-column"), 10), i = n + N.getClosest(t, "th, td")[0].colSpan; n < i; n++)
                                e = e || !1;
                            return e
                        })).not(".sorter-false").filter('[data-column="' + i[e][0] + '"]' + (1 === r ? ":last" : ""))).length)
                            for (n = 0; n < d.length; n++)
                                d[n].sortDisabled || t(d.eq(n), i[e][1]);
                        u.length && t(u.filter('[data-column="' + i[e][0] + '"]'), i[e][1])
                    }
                for (r = s.$headers.length,
                e = 0; e < r; e++)
                    N.setColumnAriaLabel(s, s.$headers.eq(e))
            },
            getClosest: function(t, e) {
                return E.fn.closest ? t.closest(e) : t.is(e) ? t : t.parents(e).filter(":first")
            },
            setColumnAriaLabel: function(t, e, n) {
                var i, s;
                e.length && (i = parseInt(e.attr("data-column"), 10),
                t = t.sortVars[i],
                i = e.hasClass(N.css.sortAsc) ? "sortAsc" : e.hasClass(N.css.sortDesc) ? "sortDesc" : "sortNone",
                s = E.trim(e.text()) + ": " + N.language[i],
                e.hasClass("sorter-false") || !1 === n ? s += N.language.sortDisabled : (i = (t.count + 1) % t.order.length,
                n = t.order[i],
                s += N.language[0 === n ? "nextAsc" : 1 === n ? "nextDesc" : "nextNone"]),
                e.attr("aria-label", s),
                t.sortedBy ? e.attr("data-sortedBy", t.sortedBy) : e.removeAttr("data-sortedBy"))
            },
            updateHeader: function(t) {
                for (var e, n, i = t.table, s = t.$headers.length, r = 0; r < s; r++)
                    e = t.$headers.eq(r),
                    n = N.getColumnData(i, t.headers, r, !0),
                    n = "false" === N.getData(e, n, "sorter") || "false" === N.getData(e, n, "parser"),
                    N.setColumnSort(t, e, n)
            },
            setColumnSort: function(t, e, n) {
                var i = t.table.id;
                e[0].sortDisabled = n,
                e[n ? "addClass" : "removeClass"]("sorter-false").attr("aria-disabled", "" + n),
                t.tabIndex && (n ? e.removeAttr("tabindex") : e.attr("tabindex", "0")),
                i && (n ? e.removeAttr("aria-controls") : e.attr("aria-controls", i))
            },
            updateHeaderSortCount: function(t, e) {
                var n, i, s, r, o, a, l, c = e || t.sortList, u = c.length;
                for (t.sortList = [],
                s = 0; s < u; s++)
                    if (a = c[s],
                    (n = parseInt(a[0], 10)) < t.columns) {
                        switch (t.sortVars[n].order || (l = N.getOrder(t.sortInitialOrder) ? t.sortReset ? [1, 0, 2] : [1, 0] : t.sortReset ? [0, 1, 2] : [0, 1],
                        t.sortVars[n].order = l,
                        t.sortVars[n].count = 0),
                        l = t.sortVars[n].order,
                        i = (i = ("" + a[1]).match(/^(1|d|s|o|n)/)) ? i[0] : "") {
                        case "1":
                        case "d":
                            i = 1;
                            break;
                        case "s":
                            i = r || 0;
                            break;
                        case "o":
                            i = 0 === (o = l[(r || 0) % l.length]) ? 1 : 1 === o ? 0 : 2;
                            break;
                        case "n":
                            i = l[++t.sortVars[n].count % l.length];
                            break;
                        default:
                            i = 0
                        }
                        r = 0 === s ? i : r,
                        a = [n, parseInt(i, 10) || 0],
                        t.sortList[t.sortList.length] = a,
                        i = E.inArray(a[1], l),
                        t.sortVars[n].count = 0 <= i ? i : a[1] % l.length
                    }
            },
            updateAll: function(t, e, n) {
                var i = t.table;
                i.isUpdating = !0,
                N.refreshWidgets(i, !0, !0),
                N.buildHeaders(t),
                N.bindEvents(i, t.$headers, !0),
                N.bindMethods(t),
                N.commonUpdate(t, e, n)
            },
            update: function(t, e, n) {
                t.table.isUpdating = !0,
                N.updateHeader(t),
                N.commonUpdate(t, e, n)
            },
            updateHeaders: function(t, e) {
                t.table.isUpdating = !0,
                N.buildHeaders(t),
                N.bindEvents(t.table, t.$headers, !0),
                N.resortComplete(t, e)
            },
            updateCell: function(t, e, n, i) {
                if (E(e).closest("tr").hasClass(t.cssChildRow))
                    console.warn('Tablesorter Warning! "updateCell" for child row content has been disabled, use "update" instead');
                else {
                    if (N.isEmptyObject(t.cache))
                        return N.updateHeader(t),
                        void N.commonUpdate(t, n, i);
                    t.table.isUpdating = !0,
                    t.$table.find(t.selectorRemove).remove();
                    var s, r, o, a, l = t.$tbodies, c = E(e), u = l.index(N.getClosest(c, "tbody")), d = t.cache[u], h = N.getClosest(c, "tr");
                    if (e = c[0],
                    l.length && 0 <= u) {
                        if (l = l.eq(u).find("tr").not("." + t.cssChildRow).index(h),
                        u = d.normalized[l],
                        (a = h[0].cells.length) !== t.columns)
                            for (s = !1,
                            r = o = 0; r < a; r++)
                                s || h[0].cells[r] === e ? s = !0 : o += h[0].cells[r].colSpan;
                        else
                            o = c.index();
                        s = N.getElementText(t, e, o),
                        u[t.columns].raw[o] = s,
                        s = N.getParsedText(t, e, o, s),
                        u[o] = s,
                        "numeric" === (t.parsers[o].type || "").toLowerCase() && (d.colMax[o] = Math.max(Math.abs(s) || 0, d.colMax[o] || 0)),
                        !1 !== (s = "undefined" !== n ? n : t.resort) ? N.checkResort(t, s, i) : N.resortComplete(t, i)
                    } else
                        N.debug(t, "core") && console.error("updateCell aborted, tbody missing or not within the indicated table"),
                        t.table.isUpdating = !1
                }
            },
            addRows: function(t, e, n, i) {
                var s, r, o, a, l, c, u, d, h, p, f, g = "string" == typeof e && 1 === t.$tbodies.length && /<tr/.test(e || ""), m = t.table;
                if (g)
                    e = E(e),
                    t.$tbodies.append(e);
                else if (!(e && e instanceof E && N.getClosest(e, "table")[0] === t.table))
                    return N.debug(t, "core") && console.error("addRows method requires (1) a jQuery selector reference to rows that have already been added to the table, or (2) row HTML string to be added to a table with only one tbody"),
                    !1;
                if (m.isUpdating = !0,
                N.isEmptyObject(t.cache))
                    N.updateHeader(t),
                    N.commonUpdate(t, n, i);
                else {
                    for (o = e.filter("tr").attr("role", "row").length,
                    s = t.$tbodies.index(e.parents("tbody").filter(":first")),
                    t.parsers && t.parsers.length || N.setupParsers(t),
                    r = 0; r < o; r++) {
                        for (u = 0,
                        l = e[r].cells.length,
                        c = t.cache[s].normalized.length,
                        h = [],
                        d = {
                            child: [],
                            raw: [],
                            $row: e.eq(r),
                            order: c
                        },
                        a = 0; a < l; a++)
                            p = e[r].cells[a],
                            f = N.getElementText(t, p, u),
                            d.raw[u] = f,
                            f = N.getParsedText(t, p, u, f),
                            h[u] = f,
                            "numeric" === (t.parsers[u].type || "").toLowerCase() && (t.cache[s].colMax[u] = Math.max(Math.abs(f) || 0, t.cache[s].colMax[u] || 0)),
                            0 < (f = p.colSpan - 1) && (u += f),
                            u++;
                        h[t.columns] = d,
                        t.cache[s].normalized[c] = h
                    }
                    N.checkResort(t, n, i)
                }
            },
            updateCache: function(t, e, n) {
                t.parsers && t.parsers.length || N.setupParsers(t, n),
                N.buildCache(t, e, n)
            },
            appendCache: function(t, e) {
                var n, i, s, r, o, a, l, c = t.table, u = t.$tbodies, d = [], h = t.cache;
                if (N.isEmptyObject(h))
                    return t.appender ? t.appender(c, d) : c.isUpdating ? t.$table.triggerHandler("updateComplete", c) : "";
                for (N.debug(t, "core") && (l = new Date),
                a = 0; a < u.length; a++)
                    if ((s = u.eq(a)).length) {
                        for (r = N.processTbody(c, s, !0),
                        i = (n = h[a].normalized).length,
                        o = 0; o < i; o++)
                            d[d.length] = n[o][t.columns].$row,
                            t.appender && (!t.pager || t.pager.removeRows || t.pager.ajax) || r.append(n[o][t.columns].$row);
                        N.processTbody(c, r, !1)
                    }
                t.appender && t.appender(c, d),
                N.debug(t, "core") && console.log("Rebuilt table" + N.benchmark(l)),
                e || t.appender || N.applyWidget(c),
                c.isUpdating && t.$table.triggerHandler("updateComplete", c)
            },
            commonUpdate: function(t, e, n) {
                t.$table.find(t.selectorRemove).remove(),
                N.setupParsers(t),
                N.buildCache(t),
                N.checkResort(t, e, n)
            },
            initSort: function(e, t, n) {
                if (e.table.isUpdating)
                    return setTimeout(function() {
                        N.initSort(e, t, n)
                    }, 50);
                var i, s, r, o, a, l, c, u = !n[e.sortMultiSortKey], d = e.table, h = e.$headers.length, p = N.getClosest(E(t), "th, td"), f = parseInt(p.attr("data-column"), 10), g = "mouseup" === n.type ? "user" : n.type, m = e.sortVars[f].order, p = p[0];
                if (e.$table.triggerHandler("sortStart", d),
                l = (e.sortVars[f].count + 1) % m.length,
                e.sortVars[f].count = n[e.sortResetKey] ? 2 : l,
                e.sortRestart)
                    for (r = 0; r < h; r++)
                        c = e.$headers.eq(r),
                        f !== (l = parseInt(c.attr("data-column"), 10)) && (u || c.hasClass(N.css.sortNone)) && (e.sortVars[l].count = -1);
                if (u) {
                    if (E.each(e.sortVars, function(t) {
                        e.sortVars[t].sortedBy = ""
                    }),
                    e.sortList = [],
                    e.last.sortList = [],
                    null !== e.sortForce)
                        for (i = e.sortForce,
                        s = 0; s < i.length; s++)
                            i[s][0] !== f && (e.sortList[e.sortList.length] = i[s],
                            e.sortVars[i[s][0]].sortedBy = "sortForce");
                    if ((o = m[e.sortVars[f].count]) < 2 && (e.sortList[e.sortList.length] = [f, o],
                    e.sortVars[f].sortedBy = g,
                    1 < p.colSpan))
                        for (s = 1; s < p.colSpan; s++)
                            e.sortList[e.sortList.length] = [f + s, o],
                            e.sortVars[f + s].count = E.inArray(o, m),
                            e.sortVars[f + s].sortedBy = g
                } else if (e.sortList = E.extend([], e.last.sortList),
                0 <= N.isValueInArray(f, e.sortList))
                    for (e.sortVars[f].sortedBy = g,
                    s = 0; s < e.sortList.length; s++)
                        (l = e.sortList[s])[0] === f && (l[1] = m[e.sortVars[f].count],
                        2 === l[1] && (e.sortList.splice(s, 1),
                        e.sortVars[f].count = -1));
                else if (o = m[e.sortVars[f].count],
                e.sortVars[f].sortedBy = g,
                o < 2 && (e.sortList[e.sortList.length] = [f, o],
                1 < p.colSpan))
                    for (s = 1; s < p.colSpan; s++)
                        e.sortList[e.sortList.length] = [f + s, o],
                        e.sortVars[f + s].count = E.inArray(o, m),
                        e.sortVars[f + s].sortedBy = g;
                if (e.last.sortList = E.extend([], e.sortList),
                e.sortList.length && e.sortAppend && (i = E.isArray(e.sortAppend) ? e.sortAppend : e.sortAppend[e.sortList[0][0]],
                !N.isEmptyObject(i)))
                    for (s = 0; s < i.length; s++)
                        if (i[s][0] !== f && N.isValueInArray(i[s][0], e.sortList) < 0) {
                            if (a = ("" + (o = i[s][1])).match(/^(a|d|s|o|n)/))
                                switch (l = e.sortList[0][1],
                                a[0]) {
                                case "d":
                                    o = 1;
                                    break;
                                case "s":
                                    o = l;
                                    break;
                                case "o":
                                    o = 0 === l ? 1 : 0;
                                    break;
                                case "n":
                                    o = (l + 1) % m.length;
                                    break;
                                default:
                                    o = 0
                                }
                            e.sortList[e.sortList.length] = [i[s][0], o],
                            e.sortVars[i[s][0]].sortedBy = "sortAppend"
                        }
                e.$table.triggerHandler("sortBegin", d),
                setTimeout(function() {
                    N.setHeadersCss(e),
                    N.multisort(e),
                    N.appendCache(e),
                    e.$table.triggerHandler("sortBeforeEnd", d),
                    e.$table.triggerHandler("sortEnd", d)
                }, 1)
            },
            multisort: function(o) {
                var t, e, a, n, l = o.table, c = [], u = 0, d = o.textSorter || "", h = o.sortList, p = h.length, i = o.$tbodies.length;
                if (!o.serverSideSorting && !N.isEmptyObject(o.cache)) {
                    if (N.debug(o, "core") && (e = new Date),
                    "object" == typeof d)
                        for (a = o.columns; a--; )
                            "function" == typeof (n = N.getColumnData(l, d, a)) && (c[a] = n);
                    for (t = 0; t < i; t++)
                        a = o.cache[t].colMax,
                        o.cache[t].normalized.sort(function(t, e) {
                            for (var n, i, s, r = 0; r < p; r++) {
                                if (n = h[r][0],
                                i = h[r][1],
                                u = 0 === i,
                                o.sortStable && t[n] === e[n] && 1 === p)
                                    return t[o.columns].order - e[o.columns].order;
                                if (i = (i = /n/i.test(N.getSortType(o.parsers, n))) && o.strings[n] ? (i = "boolean" == typeof N.string[o.strings[n]] ? (u ? 1 : -1) * (N.string[o.strings[n]] ? -1 : 1) : o.strings[n] && N.string[o.strings[n]] || 0,
                                o.numberSorter ? o.numberSorter(t[n], e[n], u, a[n], l) : N["sortNumeric" + (u ? "Asc" : "Desc")](t[n], e[n], i, a[n], n, o)) : (i = u ? t : e,
                                s = u ? e : t,
                                "function" == typeof d ? d(i[n], s[n], u, n, l) : "function" == typeof c[n] ? c[n](i[n], s[n], u, n, l) : N["sortNatural" + (u ? "Asc" : "Desc")](t[n] || "", e[n] || "", n, o)))
                                    return i
                            }
                            return t[o.columns].order - e[o.columns].order
                        });
                    N.debug(o, "core") && console.log("Applying sort " + h.toString() + N.benchmark(e))
                }
            },
            resortComplete: function(t, e) {
                t.table.isUpdating && t.$table.triggerHandler("updateComplete", t.table),
                E.isFunction(e) && e(t.table)
            },
            checkResort: function(t, e, n) {
                var i = E.isArray(e) ? e : t.sortList;
                !1 === (void 0 === e ? t.resort : e) || t.serverSideSorting || t.table.isProcessing ? (N.resortComplete(t, n),
                N.applyWidget(t.table, !1)) : i.length ? N.sortOn(t, i, function() {
                    N.resortComplete(t, n)
                }, !0) : N.sortReset(t, function() {
                    N.resortComplete(t, n),
                    N.applyWidget(t.table, !1)
                })
            },
            sortOn: function(t, e, n, i) {
                var s, r = t.table;
                for (t.$table.triggerHandler("sortStart", r),
                s = 0; s < t.columns; s++)
                    t.sortVars[s].sortedBy = -1 < N.isValueInArray(s, e) ? "sorton" : "";
                N.updateHeaderSortCount(t, e),
                N.setHeadersCss(t),
                t.delayInit && N.isEmptyObject(t.cache) && N.buildCache(t),
                t.$table.triggerHandler("sortBegin", r),
                N.multisort(t),
                N.appendCache(t, i),
                t.$table.triggerHandler("sortBeforeEnd", r),
                t.$table.triggerHandler("sortEnd", r),
                N.applyWidget(r),
                E.isFunction(n) && n(r)
            },
            sortReset: function(t, e) {
                var n;
                for (t.sortList = [],
                n = 0; n < t.columns; n++)
                    t.sortVars[n].count = -1,
                    t.sortVars[n].sortedBy = "";
                N.setHeadersCss(t),
                N.multisort(t),
                N.appendCache(t),
                E.isFunction(e) && e(t.table)
            },
            getSortType: function(t, e) {
                return t && t[e] && t[e].type || ""
            },
            getOrder: function(t) {
                return /^d/i.test(t) || 1 === t
            },
            sortNatural: function(t, e) {
                if (t === e)
                    return 0;
                t = (t || "").toString(),
                e = (e || "").toString();
                var n, i, s, r, o, a, l = N.regex;
                if (l.hex.test(e)) {
                    if ((n = parseInt(t.match(l.hex), 16)) < (i = parseInt(e.match(l.hex), 16)))
                        return -1;
                    if (i < n)
                        return 1
                }
                for (n = t.replace(l.chunk, "\\0$1\\0").replace(l.chunks, "").split("\\0"),
                i = e.replace(l.chunk, "\\0$1\\0").replace(l.chunks, "").split("\\0"),
                a = Math.max(n.length, i.length),
                o = 0; o < a; o++) {
                    if (s = isNaN(n[o]) ? n[o] || 0 : parseFloat(n[o]) || 0,
                    r = isNaN(i[o]) ? i[o] || 0 : parseFloat(i[o]) || 0,
                    isNaN(s) !== isNaN(r))
                        return isNaN(s) ? 1 : -1;
                    if (typeof s != typeof r && (s += "",
                    r += ""),
                    s < r)
                        return -1;
                    if (r < s)
                        return 1
                }
                return 0
            },
            sortNaturalAsc: function(t, e, n, i) {
                if (t === e)
                    return 0;
                n = N.string[i.empties[n] || i.emptyTo];
                return "" === t && 0 !== n ? "boolean" == typeof n ? n ? -1 : 1 : -n || -1 : "" === e && 0 !== n ? "boolean" == typeof n ? n ? 1 : -1 : n || 1 : N.sortNatural(t, e)
            },
            sortNaturalDesc: function(t, e, n, i) {
                if (t === e)
                    return 0;
                n = N.string[i.empties[n] || i.emptyTo];
                return "" === t && 0 !== n ? "boolean" == typeof n ? n ? -1 : 1 : n || 1 : "" === e && 0 !== n ? "boolean" == typeof n ? n ? 1 : -1 : -n || -1 : N.sortNatural(e, t)
            },
            sortText: function(t, e) {
                return e < t ? 1 : t < e ? -1 : 0
            },
            getTextValue: function(t, e, n) {
                if (n) {
                    for (var i = t ? t.length : 0, s = n + e, r = 0; r < i; r++)
                        s += t.charCodeAt(r);
                    return e * s
                }
                return 0
            },
            sortNumericAsc: function(t, e, n, i, s, r) {
                if (t === e)
                    return 0;
                s = N.string[r.empties[s] || r.emptyTo];
                return "" === t && 0 !== s ? "boolean" == typeof s ? s ? -1 : 1 : -s || -1 : "" === e && 0 !== s ? "boolean" == typeof s ? s ? 1 : -1 : s || 1 : (t = isNaN(t) ? N.getTextValue(t, n, i) : t) - (e = isNaN(e) ? N.getTextValue(e, n, i) : e)
            },
            sortNumericDesc: function(t, e, n, i, s, r) {
                if (t === e)
                    return 0;
                s = N.string[r.empties[s] || r.emptyTo];
                return "" === t && 0 !== s ? "boolean" == typeof s ? s ? -1 : 1 : s || 1 : "" === e && 0 !== s ? "boolean" == typeof s ? s ? 1 : -1 : -s || -1 : (isNaN(t) && (t = N.getTextValue(t, n, i)),
                (e = isNaN(e) ? N.getTextValue(e, n, i) : e) - t)
            },
            sortNumeric: function(t, e) {
                return t - e
            },
            addWidget: function(t) {
                t.id && !N.isEmptyObject(N.getWidgetById(t.id)) && console.warn('"' + t.id + '" widget was loaded more than once!'),
                N.widgets[N.widgets.length] = t
            },
            hasWidget: function(t, e) {
                return (t = E(t)).length && t[0].config && t[0].config.widgetInit[e] || !1
            },
            getWidgetById: function(t) {
                for (var e, n = N.widgets.length, i = 0; i < n; i++)
                    if ((e = N.widgets[i]) && e.id && e.id.toLowerCase() === t.toLowerCase())
                        return e
            },
            applyWidgetOptions: function(t) {
                var e, n, i, s = t.config, r = s.widgets.length;
                if (r)
                    for (e = 0; e < r; e++)
                        (n = N.getWidgetById(s.widgets[e])) && n.options && (i = E.extend(!0, {}, n.options),
                        s.widgetOptions = E.extend(!0, i, s.widgetOptions),
                        E.extend(!0, N.defaults.widgetOptions, n.options))
            },
            addWidgetFromClass: function(t) {
                var e, n, i = t.config, s = "^" + i.widgetClass.replace(N.regex.templateName, "(\\S+)+") + "$", r = new RegExp(s,"g"), o = (t.className || "").split(N.regex.spaces);
                if (o.length)
                    for (e = o.length,
                    n = 0; n < e; n++)
                        o[n].match(r) && (i.widgets[i.widgets.length] = o[n].replace(r, "$1"))
            },
            applyWidgetId: function(t, e, n) {
                var i, s, r, o = (t = E(t)[0]).config, a = o.widgetOptions, l = N.debug(o, "core"), e = N.getWidgetById(e);
                e && (r = e.id,
                i = !1,
                E.inArray(r, o.widgets) < 0 && (o.widgets[o.widgets.length] = r),
                l && (s = new Date),
                !n && o.widgetInit[r] || (o.widgetInit[r] = !0,
                t.hasInitialized && N.applyWidgetOptions(t),
                "function" == typeof e.init && (i = !0,
                l && console[console.group ? "group" : "log"]("Initializing " + r + " widget"),
                e.init(t, e, o, a))),
                n || "function" != typeof e.format || (i = !0,
                l && console[console.group ? "group" : "log"]("Updating " + r + " widget"),
                e.format(t, o, a, !1)),
                l && i && (console.log("Completed " + (n ? "initializing " : "applying ") + r + " widget" + N.benchmark(s)),
                console.groupEnd && console.groupEnd()))
            },
            applyWidget: function(t, e, n) {
                var i, s, r, o, a, l = (t = E(t)[0]).config, c = N.debug(l, "core"), u = [];
                if (!1 === e || !t.hasInitialized || !t.isApplyingWidgets && !t.isUpdating) {
                    if (c && (a = new Date),
                    N.addWidgetFromClass(t),
                    clearTimeout(l.timerReady),
                    l.widgets.length) {
                        for (t.isApplyingWidgets = !0,
                        l.widgets = E.grep(l.widgets, function(t, e) {
                            return E.inArray(t, l.widgets) === e
                        }),
                        s = (r = l.widgets || []).length,
                        i = 0; i < s; i++)
                            (o = N.getWidgetById(r[i])) && o.id ? (o.priority || (o.priority = 10),
                            u[i] = o) : c && console.warn('"' + r[i] + '" was enabled, but the widget code has not been loaded!');
                        for (u.sort(function(t, e) {
                            return t.priority < e.priority ? -1 : t.priority === e.priority ? 0 : 1
                        }),
                        s = u.length,
                        c && console[console.group ? "group" : "log"]("Start " + (e ? "initializing" : "applying") + " widgets"),
                        i = 0; i < s; i++)
                            (o = u[i]) && o.id && N.applyWidgetId(t, o.id, e);
                        c && console.groupEnd && console.groupEnd()
                    }
                    l.timerReady = setTimeout(function() {
                        t.isApplyingWidgets = !1,
                        E.data(t, "lastWidgetApplication", new Date),
                        l.$table.triggerHandler("tablesorter-ready"),
                        e || "function" != typeof n || n(t),
                        c && (o = l.widgets.length,
                        console.log("Completed " + (!0 === e ? "initializing " : "applying ") + o + " widget" + (1 !== o ? "s" : "") + N.benchmark(a)))
                    }, 10)
                }
            },
            removeWidget: function(t, e, n) {
                var i, s, r, o, a = (t = E(t)[0]).config;
                if (!0 === e)
                    for (e = [],
                    o = N.widgets.length,
                    r = 0; r < o; r++)
                        (s = N.widgets[r]) && s.id && (e[e.length] = s.id);
                else
                    e = (E.isArray(e) ? e.join(",") : e || "").toLowerCase().split(/[\s,]+/);
                for (o = e.length,
                i = 0; i < o; i++)
                    s = N.getWidgetById(e[i]),
                    0 <= (r = E.inArray(e[i], a.widgets)) && !0 !== n && a.widgets.splice(r, 1),
                    s && s.remove && (N.debug(a, "core") && console.log((n ? "Refreshing" : "Removing") + ' "' + e[i] + '" widget'),
                    s.remove(t, a, a.widgetOptions, n),
                    a.widgetInit[e[i]] = !1);
                a.$table.triggerHandler("widgetRemoveEnd", t)
            },
            refreshWidgets: function(t, e, n) {
                for (var i, s = (t = E(t)[0]).config.widgets, r = N.widgets, o = r.length, a = [], l = function(t) {
                    E(t).triggerHandler("refreshComplete")
                }, c = 0; c < o; c++)
                    (i = r[c]) && i.id && (e || E.inArray(i.id, s) < 0) && (a[a.length] = i.id);
                N.removeWidget(t, a.join(","), !0),
                !0 !== n ? (N.applyWidget(t, e || !1, l),
                e && N.applyWidget(t, !1, l)) : l(t)
            },
            benchmark: function(t) {
                return " (" + ((new Date).getTime() - t.getTime()) + " ms)"
            },
            log: function() {
                console.log(arguments)
            },
            debug: function(t, e) {
                return t && (!0 === t.debug || "string" == typeof t.debug && -1 < t.debug.indexOf(e))
            },
            isEmptyObject: function(t) {
                for (var e in t)
                    return !1;
                return !0
            },
            isValueInArray: function(t, e) {
                for (var n = e && e.length || 0, i = 0; i < n; i++)
                    if (e[i][0] === t)
                        return i;
                return -1
            },
            formatFloat: function(t, e) {
                return "string" != typeof t || "" === t ? t : (t = (e && e.config ? !1 !== e.config.usNumberFormat : void 0 === e || e) ? t.replace(N.regex.comma, "") : t.replace(N.regex.digitNonUS, "").replace(N.regex.comma, "."),
                N.regex.digitNegativeTest.test(t) && (t = t.replace(N.regex.digitNegativeReplace, "-$1")),
                e = parseFloat(t),
                isNaN(e) ? E.trim(t) : e)
            },
            isDigit: function(t) {
                return isNaN(t) ? N.regex.digitTest.test(t.toString().replace(N.regex.digitReplace, "")) : "" !== t
            },
            computeColumnIndex: function(t, e) {
                for (var n, i, s, r, o, a, l, c, u, d = e && e.columns || 0, h = [], p = new Array(d), f = 0; f < t.length; f++)
                    for (o = t[f].cells,
                    n = 0; n < o.length; n++) {
                        for (a = f,
                        l = (r = o[n]).rowSpan || 1,
                        c = r.colSpan || 1,
                        void 0 === h[a] && (h[a] = []),
                        i = 0; i < h[a].length + 1; i++)
                            if (void 0 === h[a][i]) {
                                u = i;
                                break
                            }
                        for (d && r.cellIndex === u || (r.setAttribute ? r.setAttribute("data-column", u) : E(r).attr("data-column", u)),
                        i = a; i < a + l; i++)
                            for (void 0 === h[i] && (h[i] = []),
                            p = h[i],
                            s = u; s < u + c; s++)
                                p[s] = "x"
                    }
                return N.checkColumnCount(t, h, p.length),
                p.length
            },
            checkColumnCount: function(t, e, n) {
                for (var i, s = !0, r = [], o = 0; o < e.length; o++)
                    if (e[o] && (i = e[o].length,
                    e[o].length !== n)) {
                        s = !1;
                        break
                    }
                s || (t.each(function(t, e) {
                    e = e.parentElement.nodeName;
                    r.indexOf(e) < 0 && r.push(e)
                }),
                console.error("Invalid or incorrect number of columns in the " + r.join(" or ") + "; expected " + n + ", but found " + i + " columns"))
            },
            fixColumnWidth: function(t) {
                var e, n, i, s, r, t = (t = E(t)[0]).config, o = t.$table.children("colgroup");
                if (o.length && o.hasClass(N.css.colgroup) && o.remove(),
                t.widthFixed && 0 === t.$table.children("colgroup").length) {
                    for (o = E('<colgroup class="' + N.css.colgroup + '">'),
                    e = t.$table.width(),
                    s = (i = t.$tbodies.find("tr:first").children(":visible")).length,
                    r = 0; r < s; r++)
                        n = parseInt(i.eq(r).width() / e * 1e3, 10) / 10 + "%",
                        o.append(E("<col>").css("width", n));
                    t.$table.prepend(o)
                }
            },
            getData: function(t, e, n) {
                var i, s, r = "", t = E(t);
                return t.length ? (i = !!E.metadata && t.metadata(),
                s = " " + (t.attr("class") || ""),
                void 0 !== t.data(n) || void 0 !== t.data(n.toLowerCase()) ? r += t.data(n) || t.data(n.toLowerCase()) : i && void 0 !== i[n] ? r += i[n] : e && void 0 !== e[n] ? r += e[n] : " " !== s && s.match(" " + n + "-") && (r = s.match(new RegExp("\\s" + n + "-([\\w-]+)"))[1] || ""),
                E.trim(r)) : ""
            },
            getColumnData: function(t, e, n, i, s) {
                if ("object" != typeof e || null === e)
                    return e;
                var r, t = (t = E(t)[0]).config, s = s || t.$headers, o = t.$headerIndexed && t.$headerIndexed[n] || s.find('[data-column="' + n + '"]:last');
                if (void 0 !== e[n])
                    return i ? e[n] : e[s.index(o)];
                for (r in e)
                    if ("string" == typeof r && o.filter(r).add(o.find(r)).length)
                        return e[r]
            },
            isProcessing: function(t, e, n) {
                var i = (t = E(t))[0].config
                  , s = n || t.find("." + N.css.header);
                e ? (void 0 !== n && 0 < i.sortList.length && (s = s.filter(function() {
                    return !this.sortDisabled && 0 <= N.isValueInArray(parseFloat(E(this).attr("data-column")), i.sortList)
                })),
                t.add(s).addClass(N.css.processing + " " + i.cssProcessing)) : t.add(s).removeClass(N.css.processing + " " + i.cssProcessing)
            },
            processTbody: function(t, e, n) {
                if (t = E(t)[0],
                n)
                    return t.isProcessing = !0,
                    e.before('<colgroup class="tablesorter-savemyplace"/>'),
                    E.fn.detach ? e.detach() : e.remove();
                n = E(t).find("colgroup.tablesorter-savemyplace");
                e.insertAfter(n),
                n.remove(),
                t.isProcessing = !1
            },
            clearTableBody: function(t) {
                E(t)[0].config.$tbodies.children().detach()
            },
            characterEquivalents: {
                a: "áàâãäąå",
                A: "ÁÀÂÃÄĄÅ",
                c: "çćč",
                C: "ÇĆČ",
                e: "éèêëěę",
                E: "ÉÈÊËĚĘ",
                i: "íìİîïı",
                I: "ÍÌİÎÏ",
                o: "óòôõöō",
                O: "ÓÒÔÕÖŌ",
                ss: "ß",
                SS: "ẞ",
                u: "úùûüů",
                U: "ÚÙÛÜŮ"
            },
            replaceAccents: function(t) {
                var e, n = "[", i = N.characterEquivalents;
                if (!N.characterRegex) {
                    for (e in N.characterRegexArray = {},
                    i)
                        "string" == typeof e && (n += i[e],
                        N.characterRegexArray[e] = new RegExp("[" + i[e] + "]","g"));
                    N.characterRegex = new RegExp(n + "]")
                }
                if (N.characterRegex.test(t))
                    for (e in i)
                        "string" == typeof e && (t = t.replace(N.characterRegexArray[e], e));
                return t
            },
            validateOptions: function(t) {
                var e, n, i, s, r = "headers sortForce sortList sortAppend widgets".split(" "), o = t.originalSettings;
                if (o) {
                    for (e in N.debug(t, "core") && (s = new Date),
                    o)
                        if ("undefined" === (i = typeof N.defaults[e]))
                            console.warn('Tablesorter Warning! "table.config.' + e + '" option not recognized');
                        else if ("object" === i)
                            for (n in o[e])
                                i = N.defaults[e] && typeof N.defaults[e][n],
                                E.inArray(e, r) < 0 && "undefined" === i && console.warn('Tablesorter Warning! "table.config.' + e + "." + n + '" option not recognized');
                    N.debug(t, "core") && console.log("validate options time:" + N.benchmark(s))
                }
            },
            restoreHeaders: function(t) {
                for (var e, n = E(t)[0].config, i = n.$table.find(n.selectorHeaders), s = i.length, r = 0; r < s; r++)
                    (e = i.eq(r)).find("." + N.css.headerIn).length && e.html(n.headerContent[r])
            },
            destroy: function(t, e, n) {
                var i, s, r, o, a;
                (t = E(t)[0]).hasInitialized && (N.removeWidget(t, !0, !1),
                i = E(t),
                s = t.config,
                o = (r = i.find("thead:first")).find("tr." + N.css.headerRow).removeClass(N.css.headerRow + " " + s.cssHeaderRow),
                a = i.find("tfoot:first > tr").children("th, td"),
                !1 === e && 0 <= E.inArray("uitheme", s.widgets) && (i.triggerHandler("applyWidgetId", ["uitheme"]),
                i.triggerHandler("applyWidgetId", ["zebra"])),
                r.find("tr").not(o).remove(),
                r = "sortReset update updateRows updateAll updateHeaders updateCell addRows updateComplete sorton appendCache updateCache applyWidgetId applyWidgets refreshWidgets removeWidget destroy mouseup mouseleave " + "keypress sortBegin sortEnd resetToLoadState ".split(" ").join(s.namespace + " "),
                i.removeData("tablesorter").unbind(r.replace(N.regex.spaces, " ")),
                s.$headers.add(a).removeClass([N.css.header, s.cssHeader, s.cssAsc, s.cssDesc, N.css.sortAsc, N.css.sortDesc, N.css.sortNone].join(" ")).removeAttr("data-column").removeAttr("aria-label").attr("aria-disabled", "true"),
                o.find(s.selectorSort).unbind("mousedown mouseup keypress ".split(" ").join(s.namespace + " ").replace(N.regex.spaces, " ")),
                N.restoreHeaders(t),
                i.toggleClass(N.css.table + " " + s.tableClass + " tablesorter-" + s.theme, !1 === e),
                i.removeClass(s.namespace.slice(1)),
                t.hasInitialized = !1,
                delete t.config.cache,
                "function" == typeof n && n(t),
                N.debug(s, "core") && console.log("tablesorter has been removed"))
            }
        };
        E.fn.tablesorter = function(e) {
            return this.each(function() {
                var t = E.extend(!0, {}, N.defaults, e, N.instanceMethods);
                t.originalSettings = e,
                !this.hasInitialized && N.buildTable && "TABLE" !== this.nodeName ? N.buildTable(this, t) : N.setup(this, t)
            })
        }
        ,
        window.console && window.console.log || (N.logs = [],
        (console = {}).log = console.warn = console.error = console.table = function() {
            var t = 1 < arguments.length ? arguments : arguments[0];
            N.logs[N.logs.length] = {
                date: Date.now(),
                log: t
            }
        }
        ),
        N.addParser({
            id: "no-parser",
            is: function() {
                return !1
            },
            format: function() {
                return ""
            },
            type: "text"
        }),
        N.addParser({
            id: "text",
            is: function() {
                return !0
            },
            format: function(t, e) {
                e = e.config;
                return t && (t = E.trim(e.ignoreCase ? t.toLocaleLowerCase() : t),
                t = e.sortLocaleCompare ? N.replaceAccents(t) : t),
                t
            },
            type: "text"
        }),
        N.regex.nondigit = /[^\w,. \-()]/g,
        N.addParser({
            id: "digit",
            is: function(t) {
                return N.isDigit(t)
            },
            format: function(t, e) {
                var n = N.formatFloat((t || "").replace(N.regex.nondigit, ""), e);
                return t && "number" == typeof n ? n : t && E.trim(t && e.config.ignoreCase ? t.toLocaleLowerCase() : t)
            },
            type: "numeric"
        }),
        N.regex.currencyReplace = /[+\-,. ]/g,
        N.regex.currencyTest = /^\(?\d+[\u00a3$\u20ac\u00a4\u00a5\u00a2?.]|[\u00a3$\u20ac\u00a4\u00a5\u00a2?.]\d+\)?$/,
        N.addParser({
            id: "currency",
            is: function(t) {
                return t = (t || "").replace(N.regex.currencyReplace, ""),
                N.regex.currencyTest.test(t)
            },
            format: function(t, e) {
                var n = N.formatFloat((t || "").replace(N.regex.nondigit, ""), e);
                return t && "number" == typeof n ? n : t && E.trim(t && e.config.ignoreCase ? t.toLocaleLowerCase() : t)
            },
            type: "numeric"
        }),
        N.regex.urlProtocolTest = /^(https?|ftp|file):\/\//,
        N.regex.urlProtocolReplace = /(https?|ftp|file):\/\/(www\.)?/,
        N.addParser({
            id: "url",
            is: function(t) {
                return N.regex.urlProtocolTest.test(t)
            },
            format: function(t) {
                return t && E.trim(t.replace(N.regex.urlProtocolReplace, ""))
            },
            type: "text"
        }),
        N.regex.dash = /-/g,
        N.regex.isoDate = /^\d{4}[\/\-]\d{1,2}[\/\-]\d{1,2}/,
        N.addParser({
            id: "isoDate",
            is: function(t) {
                return N.regex.isoDate.test(t)
            },
            format: function(t) {
                var e = t && new Date(t.replace(N.regex.dash, "/"));
                return e instanceof Date && isFinite(e) ? e.getTime() : t
            },
            type: "numeric"
        }),
        N.regex.percent = /%/g,
        N.regex.percentTest = /(\d\s*?%|%\s*?\d)/,
        N.addParser({
            id: "percent",
            is: function(t) {
                return N.regex.percentTest.test(t) && t.length < 15
            },
            format: function(t, e) {
                return t && N.formatFloat(t.replace(N.regex.percent, ""), e)
            },
            type: "numeric"
        }),
        N.addParser({
            id: "image",
            is: function(t, e, n, i) {
                return 0 < i.find("img").length
            },
            format: function(t, e, n) {
                return E(n).find("img").attr(e.config.imgAttr || "alt") || t
            },
            parsed: !0,
            type: "text"
        }),
        N.regex.dateReplace = /(\S)([AP]M)$/i,
        N.regex.usLongDateTest1 = /^[A-Z]{3,10}\.?\s+\d{1,2},?\s+(\d{4})(\s+\d{1,2}:\d{2}(:\d{2})?(\s+[AP]M)?)?$/i,
        N.regex.usLongDateTest2 = /^\d{1,2}\s+[A-Z]{3,10}\s+\d{4}/i,
        N.addParser({
            id: "usLongDate",
            is: function(t) {
                return N.regex.usLongDateTest1.test(t) || N.regex.usLongDateTest2.test(t)
            },
            format: function(t) {
                var e = t && new Date(t.replace(N.regex.dateReplace, "$1 $2"));
                return e instanceof Date && isFinite(e) ? e.getTime() : t
            },
            type: "numeric"
        }),
        N.regex.shortDateTest = /(^\d{1,2}[\/\s]\d{1,2}[\/\s]\d{4})|(^\d{4}[\/\s]\d{1,2}[\/\s]\d{1,2})/,
        N.regex.shortDateReplace = /[\-.,]/g,
        N.regex.shortDateXXY = /(\d{1,2})[\/\s](\d{1,2})[\/\s](\d{4})/,
        N.regex.shortDateYMD = /(\d{4})[\/\s](\d{1,2})[\/\s](\d{1,2})/,
        N.convertFormat = function(t, e) {
            t = (t || "").replace(N.regex.spaces, " ").replace(N.regex.shortDateReplace, "/"),
            "mmddyyyy" === e ? t = t.replace(N.regex.shortDateXXY, "$3/$1/$2") : "ddmmyyyy" === e ? t = t.replace(N.regex.shortDateXXY, "$3/$2/$1") : "yyyymmdd" === e && (t = t.replace(N.regex.shortDateYMD, "$1/$2/$3"));
            e = new Date(t);
            return e instanceof Date && isFinite(e) ? e.getTime() : ""
        }
        ,
        N.addParser({
            id: "shortDate",
            is: function(t) {
                return t = (t || "").replace(N.regex.spaces, " ").replace(N.regex.shortDateReplace, "/"),
                N.regex.shortDateTest.test(t)
            },
            format: function(t, e, n, i) {
                var s, r;
                return t && (e = (r = (s = e.config).$headerIndexed[i]).length && r.data("dateFormat") || N.getData(r, N.getColumnData(e, s.headers, i), "dateFormat") || s.dateFormat,
                r.length && r.data("dateFormat", e),
                N.convertFormat(t, e) || t)
            },
            type: "numeric"
        }),
        N.regex.timeTest = /^(0?[1-9]|1[0-2]):([0-5]\d)(\s[AP]M)$|^((?:[01]\d|[2][0-4]):[0-5]\d)$/i,
        N.regex.timeMatch = /(0?[1-9]|1[0-2]):([0-5]\d)(\s[AP]M)|((?:[01]\d|[2][0-4]):[0-5]\d)/i,
        N.addParser({
            id: "time",
            is: function(t) {
                return N.regex.timeTest.test(t)
            },
            format: function(t) {
                var e = (t || "").match(N.regex.timeMatch)
                  , n = new Date(t)
                  , e = t && (null !== e ? e[0] : "00:00 AM")
                  , e = e && new Date("2000/01/01 " + e.replace(N.regex.dateReplace, "$1 $2"));
                return e instanceof Date && isFinite(e) ? (n instanceof Date && isFinite(n) ? n.getTime() : 0) ? parseFloat(e.getTime() + "." + n.getTime()) : e.getTime() : t
            },
            type: "numeric"
        }),
        N.addParser({
            id: "metadata",
            is: function() {
                return !1
            },
            format: function(t, e, n) {
                e = e.config,
                e = e.parserMetadataName || "sortValue";
                return E(n).metadata()[e]
            },
            type: "numeric"
        }),
        N.addWidget({
            id: "zebra",
            priority: 90,
            format: function(t, e, n) {
                for (var i, s, r, o, a, l = new RegExp(e.cssChildRow,"i"), c = e.$tbodies.add(E(e.namespace + "_extra_table").children("tbody:not(." + e.cssInfoBlock + ")")), u = 0; u < c.length; u++)
                    for (s = 0,
                    a = (i = c.eq(u).children("tr:visible").not(e.selectorRemove)).length,
                    o = 0; o < a; o++)
                        r = i.eq(o),
                        l.test(r[0].className) || s++,
                        r.removeClass(n.zebra[(r = s % 2 == 0) ? 1 : 0]).addClass(n.zebra[r ? 0 : 1])
            },
            remove: function(t, e, n, i) {
                if (!i)
                    for (var s, r = e.$tbodies, o = (n.zebra || ["even", "odd"]).join(" "), a = 0; a < r.length; a++)
                        (s = N.processTbody(t, r.eq(a), !0)).children().removeClass(o),
                        N.processTbody(t, s, !1)
            }
        })
    }(t),
    t.tablesorter
}),
function(t) {
    "function" == typeof define && define.amd ? define(["jquery"], t) : "object" == typeof exports ? module.exports = t(require("jquery")) : t(jQuery)
}(function(i, s) {
    "use strict";
    var r = "drilldown"
      , o = "data-next-parent"
      , a = {
        event: "click",
        selector: "a",
        speed: 100,
        cssClass: {
            container: r + "-container",
            root: r + "-root",
            sub: r + "-sub",
            back: r + "-back"
        }
    }
      , l = (t.prototype = {
        destroy: function() {
            this.reset(),
            this.$element.off(this.options.event + "." + r, this.options.selector)
        },
        reset: function() {
            for (var t = this._history.length; 0 < t; t--)
                c.call(this, {
                    speed: 0
                });
            this._history = [],
            this._css = {
                float: "left",
                width: null
            }
        }
    },
    t);
    function t(t, e) {
        var n = this;
        this._name = r,
        this._defaults = a,
        this.element = t,
        this.$element = i(t),
        this.options = i.extend({}, a, e),
        this._history = [],
        this._css = {
            float: "left",
            width: null
        },
        this.$container = this.$element.find("." + this.options.cssClass.container),
        this.$element.on(this.options.event + "." + r, this.options.selector, function(t) {
            !function(t, e) {
                var n = e.nextAll("." + this.options.cssClass.sub)
                  , i = !0;
                n.length ? function(e, t) {
                    t = (t && t.speed !== s ? t : this.options).speed;
                    e.length && (this._css.width = this.$element.outerWidth(),
                    this.$container.width(2 * this._css.width),
                    e.parent().attr(o, !0),
                    e = e.removeClass(this.options.cssClass.sub).addClass(this.options.cssClass.root),
                    this.$container.append(e),
                    u.call(this, {
                        marginLeft: -1 * this._css.width,
                        speed: t
                    }, function() {
                        var t = e.prev();
                        this._history.push(t.detach()),
                        d.call(this, e)
                    }
                    .bind(this)))
                }
                .call(this, n) : e.closest("." + this.options.cssClass.back).length ? c.call(this) : i = !1;
                i && "A" === e.prop("tagName") && t.preventDefault()
            }
            .call(n, t, i(this))
        })
    }
    function c(t) {
        var t = (t && t.speed !== s ? t : this.options).speed
          , e = this._history.pop();
        this._css.width = this.$element.outerWidth(),
        this.$container.width(2 * this._css.width),
        this.$container.prepend(e),
        u.call(this, {
            marginLeft: 0,
            speed: t
        }, function() {
            var t = e.next();
            t.addClass(this.options.cssClass.sub).removeClass(this.options.cssClass.root),
            this.$container.find("[" + o + "]").last().removeAttr(o).append(t),
            d.call(this, e)
        }
        .bind(this))
    }
    function u(t, e) {
        var n = this.$container.children("." + this.options.cssClass.root);
        n.css(this._css),
        n.first().animate({
            marginLeft: t.marginLeft
        }, t.speed, e)
    }
    function d(t) {
        t.css({
            float: "",
            width: "",
            marginLeft: ""
        }),
        this.$container.css("width", "")
    }
    i.fn[r] = function(n) {
        return this.each(function() {
            var t = i.data(this, r)
              , e = n;
            t ? "string" == typeof e && ("destroy" === e && i.removeData(this, r),
            "function" == typeof t[e] && t[e]()) : i.data(this, r, new l(this,n))
        })
    }
}),
function(i, s, r, o) {
    function a() {
        var t = i(this);
        t[o]() || (t.addClass(s),
        "password" === t.attr("type") && (t.attr("type", "text"),
        t.data(s + "-pwd", !0)),
        t[o](t.attr(r)))
    }
    function l() {
        var t = i(this);
        t.removeClass(s),
        t.data(s + "-pwd") && t.attr("type", "password"),
        t[o]() === t.attr(r) && t[o]("")
    }
    function e() {
        i(this).find("[" + r + "]").each(function() {
            i(this).data(s) && l.call(this)
        })
    }
    i.fn.placeholdr = function() {
        return r in document.createElement("input") || (i(this).find("[" + r + "]").each(function() {
            var t = i(this);
            t.data(s) || (t.data(s, !0),
            a.call(this),
            t.focus(l),
            t.blur(a))
        }),
        i(this).find("form").each(function() {
            var t = i(this);
            t.data(s) || (t.data(s, !0),
            t.submit(e))
        })),
        this
    }
    ,
    i.fn[o] = i.fn.val,
    i.fn.val = function(t) {
        var e = i(this);
        if ("undefined" === i.type(t) && e.data(s) && e[o]() === e.attr(r))
            return "";
        var e = "string" === i.type(t)
          , n = (e && l.call(this),
        i.fn[o].apply(this, arguments));
        return e && !t && a.call(this),
        n
    }
    ,
    i(function() {
        i(document).placeholdr()
    }),
    document.write("<style>.placeholdr{color:#AAA;}</style>")
}(jQuery, "placeholdr", "placeholder", "placeholdrVal"),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["./blueimp-helper"], t) : (window.blueimp = window.blueimp || {},
    window.blueimp.Gallery = t(window.blueimp.helper || window.jQuery))
}(function(u) {
    "use strict";
    function n(t, e) {
        return void 0 === document.body.style.maxHeight ? null : this && this.options === n.prototype.options ? void (t && t.length ? (this.list = t,
        this.num = t.length,
        this.initOptions(e),
        this.initialize()) : this.console.log("blueimp Gallery: No or empty list provided as first argument.", t)) : new n(t,e)
    }
    return u.extend(n.prototype, {
        options: {
            container: "#blueimp-gallery",
            slidesContainer: "div",
            titleElement: "h3",
            displayClass: "blueimp-gallery-display",
            controlsClass: "blueimp-gallery-controls",
            singleClass: "blueimp-gallery-single",
            leftEdgeClass: "blueimp-gallery-left",
            rightEdgeClass: "blueimp-gallery-right",
            playingClass: "blueimp-gallery-playing",
            slideClass: "slide",
            slideLoadingClass: "slide-loading",
            slideErrorClass: "slide-error",
            slideContentClass: "slide-content",
            toggleClass: "toggle",
            prevClass: "prev",
            nextClass: "next",
            closeClass: "close",
            playPauseClass: "play-pause",
            typeProperty: "type",
            titleProperty: "title",
            altTextProperty: "alt",
            urlProperty: "href",
            srcsetProperty: "urlset",
            displayTransition: !0,
            clearSlides: !0,
            stretchImages: !1,
            toggleControlsOnReturn: !0,
            toggleControlsOnSlideClick: !0,
            toggleSlideshowOnSpace: !0,
            enableKeyboardNavigation: !0,
            closeOnEscape: !0,
            closeOnSlideClick: !0,
            closeOnSwipeUpOrDown: !0,
            closeOnHashChange: !0,
            emulateTouchEvents: !0,
            stopTouchEventsPropagation: !1,
            hidePageScrollbars: !0,
            disableScroll: !0,
            carousel: !1,
            continuous: !0,
            unloadElements: !0,
            startSlideshow: !1,
            slideshowInterval: 5e3,
            slideshowDirection: "ltr",
            index: 0,
            preloadRange: 2,
            transitionDuration: 300,
            slideshowTransitionDuration: 500,
            event: void 0,
            onopen: void 0,
            onopened: void 0,
            onslide: void 0,
            onslideend: void 0,
            onslidecomplete: void 0,
            onclose: void 0,
            onclosed: void 0
        },
        carouselOptions: {
            hidePageScrollbars: !1,
            toggleControlsOnReturn: !1,
            toggleSlideshowOnSpace: !1,
            enableKeyboardNavigation: !1,
            closeOnEscape: !1,
            closeOnSlideClick: !1,
            closeOnSwipeUpOrDown: !1,
            disableScroll: !1,
            startSlideshow: !0
        },
        console: window.console && "function" == typeof window.console.log ? window.console : {
            log: function() {}
        },
        support: function(i) {
            var t, s = {
                touch: void 0 !== window.ontouchstart || window.DocumentTouch && document instanceof DocumentTouch
            }, e = {
                webkitTransition: {
                    end: "webkitTransitionEnd",
                    prefix: "-webkit-"
                },
                MozTransition: {
                    end: "transitionend",
                    prefix: "-moz-"
                },
                OTransition: {
                    end: "otransitionend",
                    prefix: "-o-"
                },
                transition: {
                    end: "transitionend",
                    prefix: ""
                }
            };
            for (t in e)
                if (Object.prototype.hasOwnProperty.call(e, t) && void 0 !== i.style[t]) {
                    s.transition = e[t],
                    s.transition.name = t;
                    break
                }
            function n() {
                var t, e, n = s.transition;
                document.body.appendChild(i),
                n && (t = n.name.slice(0, -9) + "ransform",
                void 0 !== i.style[t] && (i.style[t] = "translateZ(0)",
                e = window.getComputedStyle(i).getPropertyValue(n.prefix + "transform"),
                s.transform = {
                    prefix: n.prefix,
                    name: t,
                    translate: !0,
                    translateZ: !!e && "none" !== e
                })),
                void 0 !== i.style.backgroundSize && (s.backgroundSize = {},
                i.style.backgroundSize = "contain",
                s.backgroundSize.contain = "contain" === window.getComputedStyle(i).getPropertyValue("background-size"),
                i.style.backgroundSize = "cover",
                s.backgroundSize.cover = "cover" === window.getComputedStyle(i).getPropertyValue("background-size")),
                document.body.removeChild(i)
            }
            return document.body ? n() : u(document).on("DOMContentLoaded", n),
            s
        }(document.createElement("div")),
        requestAnimationFrame: window.requestAnimationFrame || window.webkitRequestAnimationFrame || window.mozRequestAnimationFrame,
        cancelAnimationFrame: window.cancelAnimationFrame || window.webkitCancelRequestAnimationFrame || window.webkitCancelAnimationFrame || window.mozCancelAnimationFrame,
        initialize: function() {
            if (this.initStartIndex(),
            !1 === this.initWidget())
                return !1;
            this.initEventListeners(),
            this.onslide(this.index),
            this.ontransitionend(),
            this.options.startSlideshow && this.play()
        },
        slide: function(t, e) {
            window.clearTimeout(this.timeout);
            var n, i, s, r = this.index;
            if (r !== t && 1 !== this.num) {
                if (e = e || this.options.transitionDuration,
                this.support.transform) {
                    for (this.options.continuous || (t = this.circle(t)),
                    n = Math.abs(r - t) / (r - t),
                    this.options.continuous && (i = n,
                    (n = -this.positions[this.circle(t)] / this.slideWidth) !== i && (t = -n * this.num + t)),
                    s = Math.abs(r - t) - 1; s; )
                        this.move(this.circle((r < t ? t : r) - --s - 1), this.slideWidth * n, 0);
                    t = this.circle(t),
                    this.move(r, this.slideWidth * n, e),
                    this.move(t, 0, e),
                    this.options.continuous && this.move(this.circle(t - n), -this.slideWidth * n, 0)
                } else
                    t = this.circle(t),
                    this.animate(r * -this.slideWidth, t * -this.slideWidth, e);
                this.onslide(t)
            }
        },
        getIndex: function() {
            return this.index
        },
        getNumber: function() {
            return this.num
        },
        prev: function() {
            (this.options.continuous || this.index) && this.slide(this.index - 1)
        },
        next: function() {
            (this.options.continuous || this.index < this.num - 1) && this.slide(this.index + 1)
        },
        play: function(t) {
            var n = this
              , e = this.index + ("rtl" === this.options.slideshowDirection ? -1 : 1);
            window.clearTimeout(this.timeout),
            this.interval = t || this.options.slideshowInterval,
            1 < this.elements[this.index] && (this.timeout = this.setTimeout(!this.requestAnimationFrame && this.slide || function(t, e) {
                n.animationFrameId = n.requestAnimationFrame.call(window, function() {
                    n.slide(t, e)
                })
            }
            , [e, this.options.slideshowTransitionDuration], this.interval)),
            this.container.addClass(this.options.playingClass)
        },
        pause: function() {
            window.clearTimeout(this.timeout),
            this.interval = null,
            this.cancelAnimationFrame && (this.cancelAnimationFrame.call(window, this.animationFrameId),
            this.animationFrameId = null),
            this.container.removeClass(this.options.playingClass)
        },
        add: function(t) {
            var e;
            for (t.concat || (t = Array.prototype.slice.call(t)),
            this.list.concat || (this.list = Array.prototype.slice.call(this.list)),
            this.list = this.list.concat(t),
            this.num = this.list.length,
            2 < this.num && null === this.options.continuous && (this.options.continuous = !0,
            this.container.removeClass(this.options.leftEdgeClass)),
            this.container.removeClass(this.options.rightEdgeClass).removeClass(this.options.singleClass),
            e = this.num - t.length; e < this.num; e += 1)
                this.addSlide(e),
                this.positionSlide(e);
            this.positions.length = this.num,
            this.initSlides(!0)
        },
        resetSlides: function() {
            this.slidesContainer.empty(),
            this.unloadAllSlides(),
            this.slides = []
        },
        handleClose: function() {
            var t = this.options;
            this.destroyEventListeners(),
            this.pause(),
            this.container[0].style.display = "none",
            this.container.removeClass(t.displayClass).removeClass(t.singleClass).removeClass(t.leftEdgeClass).removeClass(t.rightEdgeClass),
            t.hidePageScrollbars && (document.body.style.overflow = this.bodyOverflowStyle),
            this.options.clearSlides && this.resetSlides(),
            this.options.onclosed && this.options.onclosed.call(this)
        },
        close: function() {
            var n = this;
            this.options.onclose && this.options.onclose.call(this),
            this.support.transition && this.options.displayTransition ? (this.container.on(this.support.transition.end, function t(e) {
                e.target === n.container[0] && (n.container.off(n.support.transition.end, t),
                n.handleClose())
            }),
            this.container.removeClass(this.options.displayClass)) : this.handleClose()
        },
        circle: function(t) {
            return (this.num + t % this.num) % this.num
        },
        move: function(t, e, n) {
            this.translateX(t, e, n),
            this.positions[t] = e
        },
        translate: function(t, e, n, i) {
            var s, r;
            this.slides[t] && (t = this.slides[t].style,
            s = this.support.transition,
            r = this.support.transform,
            t[s.name + "Duration"] = i + "ms",
            t[r.name] = "translate(" + e + "px, " + n + "px)" + (r.translateZ ? " translateZ(0)" : ""))
        },
        translateX: function(t, e, n) {
            this.translate(t, e, 0, n)
        },
        translateY: function(t, e, n) {
            this.translate(t, 0, e, n)
        },
        animate: function(e, n, i) {
            var s, r, o;
            i ? (s = this,
            r = (new Date).getTime(),
            o = window.setInterval(function() {
                var t = (new Date).getTime() - r;
                if (i < t)
                    return s.slidesContainer[0].style.left = n + "px",
                    s.ontransitionend(),
                    void window.clearInterval(o);
                s.slidesContainer[0].style.left = (n - e) * (Math.floor(t / i * 100) / 100) + e + "px"
            }, 4)) : this.slidesContainer[0].style.left = n + "px"
        },
        preventDefault: function(t) {
            t.preventDefault ? t.preventDefault() : t.returnValue = !1
        },
        stopPropagation: function(t) {
            t.stopPropagation ? t.stopPropagation() : t.cancelBubble = !0
        },
        onresize: function() {
            this.initSlides(!0)
        },
        onhashchange: function() {
            this.options.closeOnHashChange && this.close()
        },
        onmousedown: function(t) {
            t.which && 1 === t.which && "VIDEO" !== t.target.nodeName && "AUDIO" !== t.target.nodeName && (t.preventDefault(),
            (t.originalEvent || t).touches = [{
                pageX: t.pageX,
                pageY: t.pageY
            }],
            this.ontouchstart(t))
        },
        onmousemove: function(t) {
            this.touchStart && ((t.originalEvent || t).touches = [{
                pageX: t.pageX,
                pageY: t.pageY
            }],
            this.ontouchmove(t))
        },
        onmouseup: function(t) {
            this.touchStart && (this.ontouchend(t),
            delete this.touchStart)
        },
        onmouseout: function(t) {
            var e, n;
            this.touchStart && (e = t.target,
            (n = t.relatedTarget) && (n === e || u.contains(e, n)) || this.onmouseup(t))
        },
        ontouchstart: function(t) {
            this.options.stopTouchEventsPropagation && this.stopPropagation(t);
            t = (t.originalEvent || t).touches[0];
            this.touchStart = {
                x: t.pageX,
                y: t.pageY,
                time: Date.now()
            },
            this.isScrolling = void 0,
            this.touchDelta = {}
        },
        ontouchmove: function(t) {
            this.options.stopTouchEventsPropagation && this.stopPropagation(t);
            var e, n, i = (t.originalEvent || t).touches, s = i[0], r = (t.originalEvent || t).scale, o = this.index;
            if (!(1 < i.length || r && 1 !== r))
                if (this.options.disableScroll && t.preventDefault(),
                this.touchDelta = {
                    x: s.pageX - this.touchStart.x,
                    y: s.pageY - this.touchStart.y
                },
                e = this.touchDelta.x,
                void 0 === this.isScrolling && (this.isScrolling = this.isScrolling || Math.abs(e) < Math.abs(this.touchDelta.y)),
                this.isScrolling)
                    this.options.carousel || this.translateY(o, this.touchDelta.y + this.positions[o], 0);
                else
                    for (t.preventDefault(),
                    window.clearTimeout(this.timeout),
                    this.options.continuous ? n = [this.circle(o + 1), o, this.circle(o - 1)] : (this.touchDelta.x = e /= !o && 0 < e || o === this.num - 1 && e < 0 ? Math.abs(e) / this.slideWidth + 1 : 1,
                    n = [o],
                    o && n.push(o - 1),
                    o < this.num - 1 && n.unshift(o + 1)); n.length; )
                        o = n.pop(),
                        this.translateX(o, e + this.positions[o], 0)
        },
        ontouchend: function(t) {
            this.options.stopTouchEventsPropagation && this.stopPropagation(t);
            var e, n, t = this.index, i = Math.abs(this.touchDelta.x), s = this.slideWidth, r = Math.ceil(this.options.transitionDuration * (1 - i / s) / 2), i = 20 < i, o = !t && 0 < this.touchDelta.x || t === this.num - 1 && this.touchDelta.x < 0, a = !i && this.options.closeOnSwipeUpOrDown && 20 < Math.abs(this.touchDelta.y);
            this.options.continuous && (o = !1),
            e = this.touchDelta.x < 0 ? -1 : 1,
            this.isScrolling ? a ? this.close() : this.translateY(t, 0, r) : i && !o ? (a = t + e,
            i = t - e,
            o = s * e,
            n = -s * e,
            this.options.continuous ? (this.move(this.circle(a), o, 0),
            this.move(this.circle(t - 2 * e), n, 0)) : 0 <= a && a < this.num && this.move(a, o, 0),
            this.move(t, this.positions[t] + o, r),
            this.move(this.circle(i), this.positions[this.circle(i)] + o, r),
            t = this.circle(i),
            this.onslide(t)) : this.options.continuous ? (this.move(this.circle(t - 1), -s, r),
            this.move(t, 0, r),
            this.move(this.circle(t + 1), s, r)) : (t && this.move(t - 1, -s, r),
            this.move(t, 0, r),
            t < this.num - 1 && this.move(t + 1, s, r))
        },
        ontouchcancel: function(t) {
            this.touchStart && (this.ontouchend(t),
            delete this.touchStart)
        },
        ontransitionend: function(t) {
            var e = this.slides[this.index];
            t && e !== t.target || (this.interval && this.play(),
            this.setTimeout(this.options.onslideend, [this.index, e]))
        },
        oncomplete: function(t) {
            var e, n = t.target || t.srcElement, i = n && n.parentNode;
            n && i && (e = this.getNodeIndex(i),
            u(i).removeClass(this.options.slideLoadingClass),
            "error" === t.type ? (u(i).addClass(this.options.slideErrorClass),
            this.elements[e] = 3) : this.elements[e] = 2,
            n.clientHeight > this.container[0].clientHeight && (n.style.maxHeight = this.container[0].clientHeight),
            this.interval && this.slides[this.index] === i && this.play(),
            this.setTimeout(this.options.onslidecomplete, [e, i]))
        },
        onload: function(t) {
            this.oncomplete(t)
        },
        onerror: function(t) {
            this.oncomplete(t)
        },
        onkeydown: function(t) {
            switch (t.which || t.keyCode) {
            case 13:
                this.options.toggleControlsOnReturn && (this.preventDefault(t),
                this.toggleControls());
                break;
            case 27:
                this.options.closeOnEscape && (this.close(),
                t.stopImmediatePropagation());
                break;
            case 32:
                this.options.toggleSlideshowOnSpace && (this.preventDefault(t),
                this.toggleSlideshow());
                break;
            case 37:
                this.options.enableKeyboardNavigation && (this.preventDefault(t),
                this.prev());
                break;
            case 39:
                this.options.enableKeyboardNavigation && (this.preventDefault(t),
                this.next())
            }
        },
        handleClick: function(t) {
            var e = this.options
              , n = t.target || t.srcElement
              , i = n.parentNode;
            function s(t) {
                return u(n).hasClass(t) || u(i).hasClass(t)
            }
            s(e.toggleClass) ? (this.preventDefault(t),
            this.toggleControls()) : s(e.prevClass) ? (this.preventDefault(t),
            this.prev()) : s(e.nextClass) ? (this.preventDefault(t),
            this.next()) : s(e.closeClass) ? (this.preventDefault(t),
            this.close()) : s(e.playPauseClass) ? (this.preventDefault(t),
            this.toggleSlideshow()) : i === this.slidesContainer[0] ? e.closeOnSlideClick ? (this.preventDefault(t),
            this.close()) : e.toggleControlsOnSlideClick && (this.preventDefault(t),
            this.toggleControls()) : i.parentNode && i.parentNode === this.slidesContainer[0] && e.toggleControlsOnSlideClick && (this.preventDefault(t),
            this.toggleControls())
        },
        onclick: function(t) {
            if (!(this.options.emulateTouchEvents && this.touchDelta && (20 < Math.abs(this.touchDelta.x) || 20 < Math.abs(this.touchDelta.y))))
                return this.handleClick(t);
            delete this.touchDelta
        },
        updateEdgeClasses: function(t) {
            t ? this.container.removeClass(this.options.leftEdgeClass) : this.container.addClass(this.options.leftEdgeClass),
            t === this.num - 1 ? this.container.addClass(this.options.rightEdgeClass) : this.container.removeClass(this.options.rightEdgeClass)
        },
        handleSlide: function(t) {
            this.options.continuous || this.updateEdgeClasses(t),
            this.loadElements(t),
            this.options.unloadElements && this.unloadElements(t),
            this.setTitle(t)
        },
        onslide: function(t) {
            this.index = t,
            this.handleSlide(t),
            this.setTimeout(this.options.onslide, [t, this.slides[t]])
        },
        setTitle: function(t) {
            var t = this.slides[t].firstChild
              , t = t.title || t.alt
              , e = this.titleElement;
            e.length && (this.titleElement.empty(),
            t && e[0].appendChild(document.createTextNode(t)))
        },
        setTimeout: function(t, e, n) {
            var i = this;
            return t && window.setTimeout(function() {
                t.apply(i, e || [])
            }, n || 0)
        },
        imageFactory: function(t, n) {
            var i, s, e, r, o = this, a = this.imagePrototype.cloneNode(!1), l = t, c = this.options.stretchImages;
            return "string" != typeof l && (l = this.getItemProperty(t, this.options.urlProperty),
            e = this.getItemProperty(t, this.options.titleProperty),
            r = this.getItemProperty(t, this.options.altTextProperty) || e),
            !0 === c && (c = "contain"),
            (c = this.support.backgroundSize && this.support.backgroundSize[c] && c) ? s = this.elementPrototype.cloneNode(!1) : (s = a).draggable = !1,
            e && (s.title = e),
            r && (s.alt = r),
            u(a).on("load error", function t(e) {
                if (!i) {
                    if (e = {
                        type: e.type,
                        target: s
                    },
                    !s.parentNode)
                        return o.setTimeout(t, [e]);
                    i = !0,
                    u(a).off("load error", t),
                    c && "load" === e.type && (s.style.background = 'url("' + l + '") center no-repeat',
                    s.style.backgroundSize = c),
                    n(e)
                }
            }),
            a.src = l,
            s
        },
        createElement: function(t, e) {
            var n = t && this.getItemProperty(t, this.options.typeProperty)
              , n = n && this[n.split("/")[0] + "Factory"] || this.imageFactory
              , n = t && n.call(this, t, e)
              , t = this.getItemProperty(t, this.options.srcsetProperty);
            return n || (n = this.elementPrototype.cloneNode(!1),
            this.setTimeout(e, [{
                type: "error",
                target: n
            }])),
            t && n.setAttribute("srcset", t),
            u(n).addClass(this.options.slideContentClass),
            n
        },
        loadElement: function(t) {
            this.elements[t] || (this.slides[t].firstChild ? this.elements[t] = u(this.slides[t]).hasClass(this.options.slideErrorClass) ? 3 : 2 : (this.elements[t] = 1,
            u(this.slides[t]).addClass(this.options.slideLoadingClass),
            this.slides[t].appendChild(this.createElement(this.list[t], this.proxyListener))))
        },
        loadElements: function(t) {
            for (var e = Math.min(this.num, 2 * this.options.preloadRange + 1), n = t, i = 0; i < e; i += 1)
                n = this.circle(n += i * (i % 2 == 0 ? -1 : 1)),
                this.loadElement(n)
        },
        unloadElements: function(t) {
            var e, n;
            for (e in this.elements)
                Object.prototype.hasOwnProperty.call(this.elements, e) && (n = Math.abs(t - e)) > this.options.preloadRange && n + this.options.preloadRange < this.num && (this.unloadSlide(e),
                delete this.elements[e])
        },
        addSlide: function(t) {
            var e = this.slidePrototype.cloneNode(!1);
            e.setAttribute("data-index", t),
            this.slidesContainer[0].appendChild(e),
            this.slides.push(e)
        },
        positionSlide: function(t) {
            var e = this.slides[t];
            e.style.width = this.slideWidth + "px",
            this.support.transform && (e.style.left = t * -this.slideWidth + "px",
            this.move(t, this.index > t ? -this.slideWidth : this.index < t ? this.slideWidth : 0, 0))
        },
        initSlides: function(t) {
            var e, n;
            for (t || (this.positions = [],
            this.positions.length = this.num,
            this.elements = {},
            this.imagePrototype = document.createElement("img"),
            this.elementPrototype = document.createElement("div"),
            this.slidePrototype = document.createElement("div"),
            u(this.slidePrototype).addClass(this.options.slideClass),
            this.slides = this.slidesContainer[0].children,
            e = this.options.clearSlides || this.slides.length !== this.num),
            this.slideWidth = this.container[0].clientWidth,
            this.slideHeight = this.container[0].clientHeight,
            this.slidesContainer[0].style.width = this.num * this.slideWidth + "px",
            e && this.resetSlides(),
            n = 0; n < this.num; n += 1)
                e && this.addSlide(n),
                this.positionSlide(n);
            this.options.continuous && this.support.transform && (this.move(this.circle(this.index - 1), -this.slideWidth, 0),
            this.move(this.circle(this.index + 1), this.slideWidth, 0)),
            this.support.transform || (this.slidesContainer[0].style.left = this.index * -this.slideWidth + "px")
        },
        unloadSlide: function(t) {
            var t = this.slides[t]
              , e = t.firstChild;
            null !== e && t.removeChild(e)
        },
        unloadAllSlides: function() {
            for (var t = 0, e = this.slides.length; t < e; t++)
                this.unloadSlide(t)
        },
        toggleControls: function() {
            var t = this.options.controlsClass;
            this.container.hasClass(t) ? this.container.removeClass(t) : this.container.addClass(t)
        },
        toggleSlideshow: function() {
            this.interval ? this.pause() : this.play()
        },
        getNodeIndex: function(t) {
            return parseInt(t.getAttribute("data-index"), 10)
        },
        getNestedProperty: function(r, t) {
            return t.replace(/\[(?:'([^']+)'|"([^"]+)"|(\d+))\]|(?:(?:^|\.)([^\.\[]+))/g, function(t, e, n, i, s) {
                s = s || e || n || i && parseInt(i, 10);
                t && r && (r = r[s])
            }),
            r
        },
        getDataProperty: function(t, e) {
            var n;
            if (t.dataset ? (n = e.replace(/-([a-z])/g, function(t, e) {
                return e.toUpperCase()
            }),
            n = t.dataset[n]) : t.getAttribute && (n = t.getAttribute("data-" + e.replace(/([A-Z])/g, "-$1").toLowerCase())),
            "string" == typeof n) {
                if (/^(true|false|null|-?\d+(\.\d+)?|\{[\s\S]*\}|\[[\s\S]*\])$/.test(n))
                    try {
                        return u.parseJSON(n)
                    } catch (t) {}
                return n
            }
        },
        getItemProperty: function(t, e) {
            var n = this.getDataProperty(t, e);
            return n = void 0 === (n = void 0 === n ? t[e] : n) ? this.getNestedProperty(t, e) : n
        },
        initStartIndex: function() {
            var t, e = this.options.index, n = this.options.urlProperty;
            if (e && "number" != typeof e)
                for (t = 0; t < this.num; t += 1)
                    if (this.list[t] === e || this.getItemProperty(this.list[t], n) === this.getItemProperty(e, n)) {
                        e = t;
                        break
                    }
            this.index = this.circle(parseInt(e, 10) || 0)
        },
        initEventListeners: function() {
            var n = this
              , t = this.slidesContainer;
            function e(t) {
                var e = n.support.transition && n.support.transition.end === t.type ? "transitionend" : t.type;
                n["on" + e](t)
            }
            u(window).on("resize", e),
            u(window).on("hashchange", e),
            u(document.body).on("keydown", e),
            this.container.on("click", e),
            this.support.touch ? t.on("touchstart touchmove touchend touchcancel", e) : this.options.emulateTouchEvents && this.support.transition && t.on("mousedown mousemove mouseup mouseout", e),
            this.support.transition && t.on(this.support.transition.end, e),
            this.proxyListener = e
        },
        destroyEventListeners: function() {
            var t = this.slidesContainer
              , e = this.proxyListener;
            u(window).off("resize", e),
            u(document.body).off("keydown", e),
            this.container.off("click", e),
            this.support.touch ? t.off("touchstart touchmove touchend touchcancel", e) : this.options.emulateTouchEvents && this.support.transition && t.off("mousedown mousemove mouseup mouseout", e),
            this.support.transition && t.off(this.support.transition.end, e)
        },
        handleOpen: function() {
            this.options.onopened && this.options.onopened.call(this)
        },
        initWidget: function() {
            var n = this;
            return this.container = u(this.options.container),
            this.container.length ? (this.slidesContainer = this.container.find(this.options.slidesContainer).first(),
            this.slidesContainer.length ? (this.titleElement = this.container.find(this.options.titleElement).first(),
            1 === this.num && this.container.addClass(this.options.singleClass),
            this.options.onopen && this.options.onopen.call(this),
            this.support.transition && this.options.displayTransition ? this.container.on(this.support.transition.end, function t(e) {
                e.target === n.container[0] && (n.container.off(n.support.transition.end, t),
                n.handleOpen())
            }) : this.handleOpen(),
            this.options.hidePageScrollbars && (this.bodyOverflowStyle = document.body.style.overflow,
            document.body.style.overflow = "hidden"),
            this.container[0].style.display = "block",
            this.initSlides(),
            void this.container.addClass(this.options.displayClass)) : (this.console.log("blueimp Gallery: Slides container not found.", this.options.slidesContainer),
            !1)) : (this.console.log("blueimp Gallery: Widget container not found.", this.options.container),
            !1)
        },
        initOptions: function(t) {
            this.options = u.extend({}, this.options),
            (t && t.carousel || this.options.carousel && (!t || !1 !== t.carousel)) && u.extend(this.options, this.carouselOptions),
            u.extend(this.options, t),
            this.num < 3 && (this.options.continuous = !!this.options.continuous && null),
            this.support.transition || (this.options.emulateTouchEvents = !1),
            this.options.event && this.preventDefault(this.options.event)
        }
    }),
    n
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["./blueimp-helper", "./blueimp-gallery"], t) : t(window.blueimp.helper || window.jQuery, window.blueimp.Gallery)
}(function(t, e) {
    "use strict";
    var n = e.prototype
      , i = (t.extend(n.options, {
        fullScreen: !1
    }),
    n.initialize)
      , s = n.close;
    return t.extend(n, {
        getFullScreenElement: function() {
            return document.fullscreenElement || document.webkitFullscreenElement || document.mozFullScreenElement || document.msFullscreenElement
        },
        requestFullScreen: function(t) {
            t.requestFullscreen ? t.requestFullscreen() : t.webkitRequestFullscreen ? t.webkitRequestFullscreen() : t.mozRequestFullScreen ? t.mozRequestFullScreen() : t.msRequestFullscreen && t.msRequestFullscreen()
        },
        exitFullScreen: function() {
            document.exitFullscreen ? document.exitFullscreen() : document.webkitCancelFullScreen ? document.webkitCancelFullScreen() : document.mozCancelFullScreen ? document.mozCancelFullScreen() : document.msExitFullscreen && document.msExitFullscreen()
        },
        initialize: function() {
            i.call(this),
            this.options.fullScreen && !this.getFullScreenElement() && this.requestFullScreen(this.container[0])
        },
        close: function() {
            this.getFullScreenElement() === this.container[0] && this.exitFullScreen(),
            s.call(this)
        }
    }),
    e
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["./blueimp-helper", "./blueimp-gallery"], t) : t(window.blueimp.helper || window.jQuery, window.blueimp.Gallery)
}(function(r, t) {
    "use strict";
    var e = t.prototype
      , n = (r.extend(e.options, {
        indicatorContainer: "ol",
        activeIndicatorClass: "active",
        thumbnailProperty: "thumbnail",
        thumbnailIndicators: !0
    }),
    e.initSlides)
      , i = e.addSlide
      , s = e.resetSlides
      , o = e.handleClick
      , a = e.handleSlide
      , l = e.handleClose;
    return r.extend(e, {
        createIndicator: function(t) {
            var e, n = this.indicatorPrototype.cloneNode(!1), i = this.getItemProperty(t, this.options.titleProperty), s = this.options.thumbnailProperty;
            return this.options.thumbnailIndicators && ((e = void 0 === (e = s ? this.getItemProperty(t, s) : e) && (s = t.getElementsByTagName && r(t).find("img")[0]) ? s.src : e) && (n.style.backgroundImage = 'url("' + e + '")')),
            i && (n.title = i),
            n
        },
        addIndicator: function(t) {
            var e;
            this.indicatorContainer.length && ((e = this.createIndicator(this.list[t])).setAttribute("data-index", t),
            this.indicatorContainer[0].appendChild(e),
            this.indicators.push(e))
        },
        setActiveIndicator: function(t) {
            this.indicators && (this.activeIndicator && this.activeIndicator.removeClass(this.options.activeIndicatorClass),
            this.activeIndicator = r(this.indicators[t]),
            this.activeIndicator.addClass(this.options.activeIndicatorClass))
        },
        initSlides: function(t) {
            t || (this.indicatorContainer = this.container.find(this.options.indicatorContainer),
            this.indicatorContainer.length && (this.indicatorPrototype = document.createElement("li"),
            this.indicators = this.indicatorContainer[0].children)),
            n.call(this, t)
        },
        addSlide: function(t) {
            i.call(this, t),
            this.addIndicator(t)
        },
        resetSlides: function() {
            s.call(this),
            this.indicatorContainer.empty(),
            this.indicators = []
        },
        handleClick: function(t) {
            var e = t.target || t.srcElement
              , n = e.parentNode;
            if (n === this.indicatorContainer[0])
                this.preventDefault(t),
                this.slide(this.getNodeIndex(e));
            else {
                if (n.parentNode !== this.indicatorContainer[0])
                    return o.call(this, t);
                this.preventDefault(t),
                this.slide(this.getNodeIndex(n))
            }
        },
        handleSlide: function(t) {
            a.call(this, t),
            this.setActiveIndicator(t)
        },
        handleClose: function() {
            this.activeIndicator && this.activeIndicator.removeClass(this.options.activeIndicatorClass),
            l.call(this)
        }
    }),
    t
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["./blueimp-helper", "./blueimp-gallery"], t) : t(window.blueimp.helper || window.jQuery, window.blueimp.Gallery)
}(function(v, t) {
    "use strict";
    var e = t.prototype
      , n = (v.extend(e.options, {
        videoContentClass: "video-content",
        videoLoadingClass: "video-loading",
        videoPlayingClass: "video-playing",
        videoPosterProperty: "poster",
        videoSourcesProperty: "sources"
    }),
    e.handleSlide);
    return v.extend(e, {
        handleSlide: function(t) {
            n.call(this, t),
            this.playingVideo && this.playingVideo.pause()
        },
        videoFactory: function(t, e, n) {
            var i, s, r, o = this, a = this.options, l = this.elementPrototype.cloneNode(!1), c = v(l), u = [{
                type: "error",
                target: l
            }], d = n || document.createElement("video"), h = this.getItemProperty(t, a.urlProperty), p = this.getItemProperty(t, a.typeProperty), f = this.getItemProperty(t, a.titleProperty), g = this.getItemProperty(t, this.options.altTextProperty) || f, m = this.getItemProperty(t, a.videoPosterProperty), y = this.getItemProperty(t, a.videoSourcesProperty);
            if (c.addClass(a.videoContentClass),
            f && (l.title = f),
            d.canPlayType)
                if (h && p && d.canPlayType(p))
                    d.src = h;
                else if (y)
                    for (; y.length; )
                        if (i = y.shift(),
                        h = this.getItemProperty(i, a.urlProperty),
                        p = this.getItemProperty(i, a.typeProperty),
                        h && p && d.canPlayType(p)) {
                            d.src = h;
                            break
                        }
            return m && (d.poster = m,
            t = this.imagePrototype.cloneNode(!1),
            v(t).addClass(a.toggleClass),
            t.src = m,
            t.draggable = !1,
            t.alt = g,
            l.appendChild(t)),
            (m = document.createElement("a")).setAttribute("target", "_blank"),
            n || m.setAttribute("download", f),
            m.href = h,
            d.src && (d.controls = !0,
            (n || v(d)).on("error", function() {
                o.setTimeout(e, u)
            }).on("pause", function() {
                d.seeking || (s = !1,
                c.removeClass(o.options.videoLoadingClass).removeClass(o.options.videoPlayingClass),
                r && o.container.addClass(o.options.controlsClass),
                delete o.playingVideo,
                o.interval && o.play())
            }).on("playing", function() {
                s = !1,
                c.removeClass(o.options.videoLoadingClass).addClass(o.options.videoPlayingClass),
                o.container.hasClass(o.options.controlsClass) ? (r = !0,
                o.container.removeClass(o.options.controlsClass)) : r = !1
            }).on("play", function() {
                window.clearTimeout(o.timeout),
                s = !0,
                c.addClass(o.options.videoLoadingClass),
                o.playingVideo = d
            }),
            v(m).on("click", function(t) {
                o.preventDefault(t),
                s ? d.pause() : d.play()
            }),
            l.appendChild(n && n.element || d)),
            l.appendChild(m),
            this.setTimeout(e, [{
                type: "load",
                target: l
            }]),
            l
        }
    }),
    t
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["./blueimp-helper", "./blueimp-gallery-video"], t) : t(window.blueimp.helper || window.jQuery, window.blueimp.Gallery)
}(function(a, t) {
    "use strict";
    if (!window.postMessage)
        return t;
    function s(t, e, n, i) {
        this.url = t,
        this.videoId = e,
        this.playerId = n,
        this.clickToPlay = i,
        this.element = document.createElement("div"),
        this.listeners = {}
    }
    var e = t.prototype
      , r = (a.extend(e.options, {
        vimeoVideoIdProperty: "vimeo",
        vimeoPlayerUrl: "//player.vimeo.com/video/VIDEO_ID?api=1&player_id=PLAYER_ID",
        vimeoPlayerIdPrefix: "vimeo-player-",
        vimeoClickToPlay: !0
    }),
    e.textFactory || e.imageFactory)
      , o = 0;
    return a.extend(s.prototype, {
        canPlayType: function() {
            return !0
        },
        on: function(t, e) {
            return this.listeners[t] = e,
            this
        },
        loadAPI: function() {
            var t, e, n = this, i = "//f.vimeocdn.com/js/froogaloop2.min.js", s = document.getElementsByTagName("script"), r = s.length;
            function o() {
                !e && n.playOnReady && n.play(),
                e = !0
            }
            for (; r; )
                if (s[--r].src === i) {
                    t = s[r];
                    break
                }
            t || ((t = document.createElement("script")).src = i),
            a(t).on("load", o),
            s[0].parentNode.insertBefore(t, s[0]),
            /loaded|complete/.test(t.readyState) && o()
        },
        onReady: function() {
            var t = this;
            this.ready = !0,
            this.player.addEvent("play", function() {
                t.hasPlayed = !0,
                t.onPlaying()
            }),
            this.player.addEvent("pause", function() {
                t.onPause()
            }),
            this.player.addEvent("finish", function() {
                t.onPause()
            }),
            this.playOnReady && this.play()
        },
        onPlaying: function() {
            this.playStatus < 2 && (this.listeners.playing(),
            this.playStatus = 2)
        },
        onPause: function() {
            this.listeners.pause(),
            delete this.playStatus
        },
        insertIframe: function() {
            var t = document.createElement("iframe");
            t.src = this.url.replace("VIDEO_ID", this.videoId).replace("PLAYER_ID", this.playerId),
            t.id = this.playerId,
            this.element.parentNode.replaceChild(t, this.element),
            this.element = t
        },
        play: function() {
            var t = this;
            this.playStatus || (this.listeners.play(),
            this.playStatus = 1),
            this.ready ? !this.hasPlayed && (this.clickToPlay || window.navigator && /iP(hone|od|ad)/.test(window.navigator.platform)) ? this.onPlaying() : this.player.api("play") : (this.playOnReady = !0,
            window.$f ? this.player || (this.insertIframe(),
            this.player = $f(this.element),
            this.player.addEvent("ready", function() {
                t.onReady()
            })) : this.loadAPI())
        },
        pause: function() {
            this.ready ? this.player.api("pause") : this.playStatus && (delete this.playOnReady,
            this.listeners.pause(),
            delete this.playStatus)
        }
    }),
    a.extend(e, {
        VimeoPlayer: s,
        textFactory: function(t, e) {
            var n = this.options
              , i = this.getItemProperty(t, n.vimeoVideoIdProperty);
            return i ? (void 0 === this.getItemProperty(t, n.urlProperty) && (t[n.urlProperty] = "//vimeo.com/" + i),
            o += 1,
            this.videoFactory(t, e, new s(n.vimeoPlayerUrl,i,n.vimeoPlayerIdPrefix + o,n.vimeoClickToPlay))) : r.call(this, t, e)
        }
    }),
    t
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["./blueimp-helper", "./blueimp-gallery-video"], t) : t(window.blueimp.helper || window.jQuery, window.blueimp.Gallery)
}(function(t, e) {
    "use strict";
    if (!window.postMessage)
        return e;
    function s(t, e, n) {
        this.videoId = t,
        this.playerVars = e,
        this.clickToPlay = n,
        this.element = document.createElement("div"),
        this.listeners = {}
    }
    var n = e.prototype
      , r = (t.extend(n.options, {
        youTubeVideoIdProperty: "youtube",
        youTubePlayerVars: {
            wmode: "transparent"
        },
        youTubeClickToPlay: !0
    }),
    n.textFactory || n.imageFactory);
    return t.extend(s.prototype, {
        canPlayType: function() {
            return !0
        },
        on: function(t, e) {
            return this.listeners[t] = e,
            this
        },
        loadAPI: function() {
            var t, e = this, n = window.onYouTubeIframeAPIReady, i = "//www.youtube.com/iframe_api", s = document.getElementsByTagName("script"), r = s.length;
            for (window.onYouTubeIframeAPIReady = function() {
                n && n.apply(this),
                e.playOnReady && e.play()
            }
            ; r; )
                if (s[--r].src === i)
                    return;
            (t = document.createElement("script")).src = i,
            s[0].parentNode.insertBefore(t, s[0])
        },
        onReady: function() {
            this.ready = !0,
            this.playOnReady && this.play()
        },
        onPlaying: function() {
            this.playStatus < 2 && (this.listeners.playing(),
            this.playStatus = 2)
        },
        onPause: function() {
            n.setTimeout.call(this, this.checkSeek, null, 2e3)
        },
        checkSeek: function() {
            this.stateChange !== YT.PlayerState.PAUSED && this.stateChange !== YT.PlayerState.ENDED || (this.listeners.pause(),
            delete this.playStatus)
        },
        onStateChange: function(t) {
            switch (t.data) {
            case YT.PlayerState.PLAYING:
                this.hasPlayed = !0,
                this.onPlaying();
                break;
            case YT.PlayerState.PAUSED:
            case YT.PlayerState.ENDED:
                this.onPause()
            }
            this.stateChange = t.data
        },
        onError: function(t) {
            this.listeners.error(t)
        },
        play: function() {
            var e = this;
            this.playStatus || (this.listeners.play(),
            this.playStatus = 1),
            this.ready ? !this.hasPlayed && (this.clickToPlay || window.navigator && /iP(hone|od|ad)/.test(window.navigator.platform)) ? this.onPlaying() : this.player.playVideo() : (this.playOnReady = !0,
            window.YT && YT.Player ? this.player || (this.player = new YT.Player(this.element,{
                videoId: this.videoId,
                playerVars: this.playerVars,
                events: {
                    onReady: function() {
                        e.onReady()
                    },
                    onStateChange: function(t) {
                        e.onStateChange(t)
                    },
                    onError: function(t) {
                        e.onError(t)
                    }
                }
            })) : this.loadAPI())
        },
        pause: function() {
            this.ready ? this.player.pauseVideo() : this.playStatus && (delete this.playOnReady,
            this.listeners.pause(),
            delete this.playStatus)
        }
    }),
    t.extend(n, {
        YouTubePlayer: s,
        textFactory: function(t, e) {
            var n = this.options
              , i = this.getItemProperty(t, n.youTubeVideoIdProperty);
            return i ? (void 0 === this.getItemProperty(t, n.urlProperty) && (t[n.urlProperty] = "//www.youtube.com/watch?v=" + i),
            void 0 === this.getItemProperty(t, n.videoPosterProperty) && (t[n.videoPosterProperty] = "//img.youtube.com/vi/" + i + "/maxresdefault.jpg"),
            this.videoFactory(t, e, new s(i,n.youTubePlayerVars,n.youTubeClickToPlay))) : r.call(this, t, e)
        }
    }),
    e
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["jquery", "./blueimp-gallery"], t) : t(window.jQuery, window.blueimp.Gallery)
}(function(s, r) {
    "use strict";
    s(document).on("click", "[data-gallery]", function(t) {
        var e = s(this).data("gallery")
          , n = s(e)
          , i = n.length && n || s(r.prototype.options.container)
          , n = {
            onopen: function() {
                i.data("gallery", this).trigger("open")
            },
            onopened: function() {
                i.trigger("opened")
            },
            onslide: function() {
                i.trigger("slide", arguments)
            },
            onslideend: function() {
                i.trigger("slideend", arguments)
            },
            onslidecomplete: function() {
                i.trigger("slidecomplete", arguments)
            },
            onclose: function() {
                i.trigger("close")
            },
            onclosed: function() {
                i.trigger("closed").removeData("gallery")
            }
        }
          , t = s.extend(i.data(), {
            container: i[0],
            index: this,
            event: t
        }, n)
          , n = s(this).closest("[data-gallery-group], body").find('[data-gallery="' + e + '"]');
        return t.filter && (n = n.filter(t.filter)),
        new r(n,t)
    })
}),
function(t) {
    "use strict";
    "function" == typeof define && define.amd ? define(["jquery", "./blueimp-gallery"], t) : t(window.jQuery, window.blueimp.Gallery)
}(function(t, e) {
    "use strict";
    t.extend(e.prototype.options, {
        useBootstrapModal: !0
    });
    var n = e.prototype.close
      , i = e.prototype.imageFactory
      , s = e.prototype.videoFactory
      , r = e.prototype.textFactory;
    t.extend(e.prototype, {
        modalFactory: function(t, e, n, i) {
            if (!this.options.useBootstrapModal || n)
                return i.call(this, t, e, n);
            var s = this
              , r = this.container.children(".modal").clone().show().on("click", function(t) {
                t.target !== r[0] && t.target !== r.children()[0] || (t.preventDefault(),
                t.stopPropagation(),
                s.close())
            })
              , i = i.call(this, t, function(t) {
                e({
                    type: t.type,
                    target: r[0]
                }),
                r.addClass("in")
            }, n);
            return r.find(".modal-title").text(i.title || String.fromCharCode(160)),
            r.find(".modal-body").append(i),
            r[0]
        },
        imageFactory: function(t, e, n) {
            return this.modalFactory(t, e, n, i)
        },
        videoFactory: function(t, e, n) {
            return this.modalFactory(t, e, n, s)
        },
        textFactory: function(t, e, n) {
            return this.modalFactory(t, e, n, r)
        },
        close: function() {
            this.container.find(".modal").removeClass("in"),
            n.call(this)
        }
    })
}),
function(t, e) {
    "object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : t.moment = e()
}(this, function() {
    "use strict";
    var R;
    function p() {
        return R.apply(null, arguments)
    }
    function o(t) {
        return t instanceof Array || "[object Array]" === Object.prototype.toString.call(t)
    }
    function L(t) {
        return null != t && "[object Object]" === Object.prototype.toString.call(t)
    }
    function h(t, e) {
        return Object.prototype.hasOwnProperty.call(t, e)
    }
    function j(t) {
        if (Object.getOwnPropertyNames)
            return 0 === Object.getOwnPropertyNames(t).length;
        for (var e in t)
            if (h(t, e))
                return;
        return 1
    }
    function a(t) {
        return void 0 === t
    }
    function l(t) {
        return "number" == typeof t || "[object Number]" === Object.prototype.toString.call(t)
    }
    function H(t) {
        return t instanceof Date || "[object Date]" === Object.prototype.toString.call(t)
    }
    function F(t, e) {
        for (var n = [], i = t.length, s = 0; s < i; ++s)
            n.push(e(t[s], s));
        return n
    }
    function W(t, e) {
        for (var n in e)
            h(e, n) && (t[n] = e[n]);
        return h(e, "toString") && (t.toString = e.toString),
        h(e, "valueOf") && (t.valueOf = e.valueOf),
        t
    }
    function c(t, e, n, i) {
        return Ae(t, e, n, i, !0).utc()
    }
    function f(t) {
        return null == t._pf && (t._pf = {
            empty: !1,
            unusedTokens: [],
            unusedInput: [],
            overflow: -2,
            charsLeftOver: 0,
            nullInput: !1,
            invalidEra: null,
            invalidMonth: null,
            invalidFormat: !1,
            userInvalidated: !1,
            iso: !1,
            parsedDateParts: [],
            era: null,
            meridiem: null,
            rfc2822: !1,
            weekdayMismatch: !1
        }),
        t._pf
    }
    function Y(t) {
        if (null == t._isValid) {
            var e = f(t)
              , n = z.call(e.parsedDateParts, function(t) {
                return null != t
            })
              , n = !isNaN(t._d.getTime()) && e.overflow < 0 && !e.empty && !e.invalidEra && !e.invalidMonth && !e.invalidWeekday && !e.weekdayMismatch && !e.nullInput && !e.invalidFormat && !e.userInvalidated && (!e.meridiem || e.meridiem && n);
            if (t._strict && (n = n && 0 === e.charsLeftOver && 0 === e.unusedTokens.length && void 0 === e.bigHour),
            null != Object.isFrozen && Object.isFrozen(t))
                return n;
            t._isValid = n
        }
        return t._isValid
    }
    function q(t) {
        var e = c(NaN);
        return null != t ? W(f(e), t) : f(e).userInvalidated = !0,
        e
    }
    var z = Array.prototype.some || function(t) {
        for (var e = Object(this), n = e.length >>> 0, i = 0; i < n; i++)
            if (i in e && t.call(this, e[i], i, e))
                return !0;
        return !1
    }
      , B = p.momentProperties = []
      , V = !1;
    function U(t, e) {
        var n, i, s, r = B.length;
        if (a(e._isAMomentObject) || (t._isAMomentObject = e._isAMomentObject),
        a(e._i) || (t._i = e._i),
        a(e._f) || (t._f = e._f),
        a(e._l) || (t._l = e._l),
        a(e._strict) || (t._strict = e._strict),
        a(e._tzm) || (t._tzm = e._tzm),
        a(e._isUTC) || (t._isUTC = e._isUTC),
        a(e._offset) || (t._offset = e._offset),
        a(e._pf) || (t._pf = f(e)),
        a(e._locale) || (t._locale = e._locale),
        0 < r)
            for (n = 0; n < r; n++)
                a(s = e[i = B[n]]) || (t[i] = s);
        return t
    }
    function G(t) {
        U(this, t),
        this._d = new Date(null != t._d ? t._d.getTime() : NaN),
        this.isValid() || (this._d = new Date(NaN)),
        !1 === V && (V = !0,
        p.updateOffset(this),
        V = !1)
    }
    function u(t) {
        return t instanceof G || null != t && null != t._isAMomentObject
    }
    function Q(t) {
        !1 === p.suppressDeprecationWarnings && "undefined" != typeof console && console.warn && console.warn("Deprecation warning: " + t)
    }
    function t(r, o) {
        var a = !0;
        return W(function() {
            if (null != p.deprecationHandler && p.deprecationHandler(null, r),
            a) {
                for (var t, e, n = [], i = arguments.length, s = 0; s < i; s++) {
                    if (t = "",
                    "object" == typeof arguments[s]) {
                        for (e in t += "\n[" + s + "] ",
                        arguments[0])
                            h(arguments[0], e) && (t += e + ": " + arguments[0][e] + ", ");
                        t = t.slice(0, -2)
                    } else
                        t = arguments[s];
                    n.push(t)
                }
                Q(r + "\nArguments: " + Array.prototype.slice.call(n).join("") + "\n" + (new Error).stack),
                a = !1
            }
            return o.apply(this, arguments)
        }, o)
    }
    var K = {};
    function X(t, e) {
        null != p.deprecationHandler && p.deprecationHandler(t, e),
        K[t] || (Q(e),
        K[t] = !0)
    }
    function d(t) {
        return "undefined" != typeof Function && t instanceof Function || "[object Function]" === Object.prototype.toString.call(t)
    }
    function Z(t, e) {
        var n, i = W({}, t);
        for (n in e)
            h(e, n) && (L(t[n]) && L(e[n]) ? (i[n] = {},
            W(i[n], t[n]),
            W(i[n], e[n])) : null != e[n] ? i[n] = e[n] : delete i[n]);
        for (n in t)
            h(t, n) && !h(e, n) && L(t[n]) && (i[n] = W({}, i[n]));
        return i
    }
    function J(t) {
        null != t && this.set(t)
    }
    p.suppressDeprecationWarnings = !1,
    p.deprecationHandler = null;
    var tt = Object.keys || function(t) {
        var e, n = [];
        for (e in t)
            h(t, e) && n.push(e);
        return n
    }
    ;
    function r(t, e, n) {
        var i = "" + Math.abs(t);
        return (0 <= t ? n ? "+" : "" : "-") + Math.pow(10, Math.max(0, e - i.length)).toString().substr(1) + i
    }
    var et = /(\[[^\[]*\])|(\\)?([Hh]mm(ss)?|Mo|MM?M?M?|Do|DDDo|DD?D?D?|ddd?d?|do?|w[o|w]?|W[o|W]?|Qo?|N{1,5}|YYYYYY|YYYYY|YYYY|YY|y{2,4}|yo?|gg(ggg?)?|GG(GGG?)?|e|E|a|A|hh?|HH?|kk?|mm?|ss?|S{1,9}|x|X|zz?|ZZ?|.)/g
      , nt = /(\[[^\[]*\])|(\\)?(LTS|LT|LL?L?L?|l{1,4})/g
      , it = {}
      , st = {};
    function i(t, e, n, i) {
        var s = "string" == typeof i ? function() {
            return this[i]()
        }
        : i;
        t && (st[t] = s),
        e && (st[e[0]] = function() {
            return r(s.apply(this, arguments), e[1], e[2])
        }
        ),
        n && (st[n] = function() {
            return this.localeData().ordinal(s.apply(this, arguments), t)
        }
        )
    }
    function rt(t, e) {
        return t.isValid() ? (e = ot(e, t.localeData()),
        it[e] = it[e] || function(i) {
            for (var t, s = i.match(et), e = 0, r = s.length; e < r; e++)
                st[s[e]] ? s[e] = st[s[e]] : s[e] = (t = s[e]).match(/\[[\s\S]/) ? t.replace(/^\[|\]$/g, "") : t.replace(/\\/g, "");
            return function(t) {
                for (var e = "", n = 0; n < r; n++)
                    e += d(s[n]) ? s[n].call(t, i) : s[n];
                return e
            }
        }(e),
        it[e](t)) : t.localeData().invalidDate()
    }
    function ot(t, e) {
        var n = 5;
        function i(t) {
            return e.longDateFormat(t) || t
        }
        for (nt.lastIndex = 0; 0 <= n && nt.test(t); )
            t = t.replace(nt, i),
            nt.lastIndex = 0,
            --n;
        return t
    }
    var at = {};
    function e(t, e) {
        var n = t.toLowerCase();
        at[n] = at[n + "s"] = at[e] = t
    }
    function g(t) {
        return "string" == typeof t ? at[t] || at[t.toLowerCase()] : void 0
    }
    function lt(t) {
        var e, n, i = {};
        for (n in t)
            h(t, n) && (e = g(n)) && (i[e] = t[n]);
        return i
    }
    var ct = {};
    function n(t, e) {
        ct[t] = e
    }
    function ut(t) {
        return t % 4 == 0 && t % 100 != 0 || t % 400 == 0
    }
    function m(t) {
        return t < 0 ? Math.ceil(t) || 0 : Math.floor(t)
    }
    function y(t) {
        var t = +t
          , e = 0;
        return e = 0 != t && isFinite(t) ? m(t) : e
    }
    function dt(e, n) {
        return function(t) {
            return null != t ? (pt(this, e, t),
            p.updateOffset(this, n),
            this) : ht(this, e)
        }
    }
    function ht(t, e) {
        return t.isValid() ? t._d["get" + (t._isUTC ? "UTC" : "") + e]() : NaN
    }
    function pt(t, e, n) {
        t.isValid() && !isNaN(n) && ("FullYear" === e && ut(t.year()) && 1 === t.month() && 29 === t.date() ? (n = y(n),
        t._d["set" + (t._isUTC ? "UTC" : "") + e](n, t.month(), It(n, t.month()))) : t._d["set" + (t._isUTC ? "UTC" : "") + e](n))
    }
    var s = /\d/
      , v = /\d\d/
      , ft = /\d{3}/
      , gt = /\d{4}/
      , mt = /[+-]?\d{6}/
      , b = /\d\d?/
      , yt = /\d\d\d\d?/
      , vt = /\d\d\d\d\d\d?/
      , bt = /\d{1,3}/
      , _t = /\d{1,4}/
      , wt = /[+-]?\d{1,6}/
      , xt = /\d+/
      , Ct = /[+-]?\d+/
      , kt = /Z|[+-]\d\d:?\d\d/gi
      , St = /Z|[+-]\d\d(?::?\d\d)?/gi
      , _ = /[0-9]{0,256}['a-z\u00A0-\u05FF\u0700-\uD7FF\uF900-\uFDCF\uFDF0-\uFF07\uFF10-\uFFEF]{1,256}|[\u0600-\u06FF\/]{1,256}(\s*?[\u0600-\u06FF]{1,256}){1,2}/i;
    function w(t, n, i) {
        Dt[t] = d(n) ? n : function(t, e) {
            return t && i ? i : n
        }
    }
    function Tt(t, e) {
        return h(Dt, t) ? Dt[t](e._strict, e._locale) : new RegExp(x(t.replace("\\", "").replace(/\\(\[)|\\(\])|\[([^\]\[]*)\]|\\(.)/g, function(t, e, n, i, s) {
            return e || n || i || s
        })))
    }
    function x(t) {
        return t.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&")
    }
    var Dt = {}
      , Et = {};
    function C(t, n) {
        var e, i, s = n;
        for ("string" == typeof t && (t = [t]),
        l(n) && (s = function(t, e) {
            e[n] = y(t)
        }
        ),
        i = t.length,
        e = 0; e < i; e++)
            Et[t[e]] = s
    }
    function Nt(t, s) {
        C(t, function(t, e, n, i) {
            n._w = n._w || {},
            s(t, n._w, n, i)
        })
    }
    var k, S = 0, T = 1, D = 2, E = 3, N = 4, $ = 5, $t = 6, At = 7, Mt = 8;
    function It(t, e) {
        if (isNaN(t) || isNaN(e))
            return NaN;
        var n = (e % (n = 12) + n) % n;
        return t += (e - n) / 12,
        1 == n ? ut(t) ? 29 : 28 : 31 - n % 7 % 2
    }
    k = Array.prototype.indexOf || function(t) {
        for (var e = 0; e < this.length; ++e)
            if (this[e] === t)
                return e;
        return -1
    }
    ,
    i("M", ["MM", 2], "Mo", function() {
        return this.month() + 1
    }),
    i("MMM", 0, 0, function(t) {
        return this.localeData().monthsShort(this, t)
    }),
    i("MMMM", 0, 0, function(t) {
        return this.localeData().months(this, t)
    }),
    e("month", "M"),
    n("month", 8),
    w("M", b),
    w("MM", b, v),
    w("MMM", function(t, e) {
        return e.monthsShortRegex(t)
    }),
    w("MMMM", function(t, e) {
        return e.monthsRegex(t)
    }),
    C(["M", "MM"], function(t, e) {
        e[T] = y(t) - 1
    }),
    C(["MMM", "MMMM"], function(t, e, n, i) {
        i = n._locale.monthsParse(t, i, n._strict);
        null != i ? e[T] = i : f(n).invalidMonth = t
    });
    var Ot = "January_February_March_April_May_June_July_August_September_October_November_December".split("_")
      , Pt = "Jan_Feb_Mar_Apr_May_Jun_Jul_Aug_Sep_Oct_Nov_Dec".split("_")
      , Rt = /D[oD]?(\[[^\[\]]*\]|\s)+MMMM?/
      , Lt = _
      , jt = _;
    function Ht(t, e) {
        var n;
        if (t.isValid()) {
            if ("string" == typeof e)
                if (/^\d+$/.test(e))
                    e = y(e);
                else if (!l(e = t.localeData().monthsParse(e)))
                    return;
            n = Math.min(t.date(), It(t.year(), e)),
            t._d["set" + (t._isUTC ? "UTC" : "") + "Month"](e, n)
        }
    }
    function Ft(t) {
        return null != t ? (Ht(this, t),
        p.updateOffset(this, !0),
        this) : ht(this, "Month")
    }
    function Wt() {
        function t(t, e) {
            return e.length - t.length
        }
        for (var e, n = [], i = [], s = [], r = 0; r < 12; r++)
            e = c([2e3, r]),
            n.push(this.monthsShort(e, "")),
            i.push(this.months(e, "")),
            s.push(this.months(e, "")),
            s.push(this.monthsShort(e, ""));
        for (n.sort(t),
        i.sort(t),
        s.sort(t),
        r = 0; r < 12; r++)
            n[r] = x(n[r]),
            i[r] = x(i[r]);
        for (r = 0; r < 24; r++)
            s[r] = x(s[r]);
        this._monthsRegex = new RegExp("^(" + s.join("|") + ")","i"),
        this._monthsShortRegex = this._monthsRegex,
        this._monthsStrictRegex = new RegExp("^(" + i.join("|") + ")","i"),
        this._monthsShortStrictRegex = new RegExp("^(" + n.join("|") + ")","i")
    }
    function Yt(t) {
        return ut(t) ? 366 : 365
    }
    i("Y", 0, 0, function() {
        var t = this.year();
        return t <= 9999 ? r(t, 4) : "+" + t
    }),
    i(0, ["YY", 2], 0, function() {
        return this.year() % 100
    }),
    i(0, ["YYYY", 4], 0, "year"),
    i(0, ["YYYYY", 5], 0, "year"),
    i(0, ["YYYYYY", 6, !0], 0, "year"),
    e("year", "y"),
    n("year", 1),
    w("Y", Ct),
    w("YY", b, v),
    w("YYYY", _t, gt),
    w("YYYYY", wt, mt),
    w("YYYYYY", wt, mt),
    C(["YYYYY", "YYYYYY"], S),
    C("YYYY", function(t, e) {
        e[S] = 2 === t.length ? p.parseTwoDigitYear(t) : y(t)
    }),
    C("YY", function(t, e) {
        e[S] = p.parseTwoDigitYear(t)
    }),
    C("Y", function(t, e) {
        e[S] = parseInt(t, 10)
    }),
    p.parseTwoDigitYear = function(t) {
        return y(t) + (68 < y(t) ? 1900 : 2e3)
    }
    ;
    var qt = dt("FullYear", !0);
    function zt(t, e, n, i, s, r, o) {
        var a;
        return t < 100 && 0 <= t ? (a = new Date(t + 400,e,n,i,s,r,o),
        isFinite(a.getFullYear()) && a.setFullYear(t)) : a = new Date(t,e,n,i,s,r,o),
        a
    }
    function Bt(t) {
        var e;
        return t < 100 && 0 <= t ? ((e = Array.prototype.slice.call(arguments))[0] = t + 400,
        e = new Date(Date.UTC.apply(null, e)),
        isFinite(e.getUTCFullYear()) && e.setUTCFullYear(t)) : e = new Date(Date.UTC.apply(null, arguments)),
        e
    }
    function Vt(t, e, n) {
        n = 7 + e - n;
        return n - (7 + Bt(t, 0, n).getUTCDay() - e) % 7 - 1
    }
    function Ut(t, e, n, i, s) {
        var r, e = 1 + 7 * (e - 1) + (7 + n - i) % 7 + Vt(t, i, s), n = e <= 0 ? Yt(r = t - 1) + e : e > Yt(t) ? (r = t + 1,
        e - Yt(t)) : (r = t,
        e);
        return {
            year: r,
            dayOfYear: n
        }
    }
    function Gt(t, e, n) {
        var i, s, r = Vt(t.year(), e, n), r = Math.floor((t.dayOfYear() - r - 1) / 7) + 1;
        return r < 1 ? i = r + A(s = t.year() - 1, e, n) : r > A(t.year(), e, n) ? (i = r - A(t.year(), e, n),
        s = t.year() + 1) : (s = t.year(),
        i = r),
        {
            week: i,
            year: s
        }
    }
    function A(t, e, n) {
        var i = Vt(t, e, n)
          , e = Vt(t + 1, e, n);
        return (Yt(t) - i + e) / 7
    }
    i("w", ["ww", 2], "wo", "week"),
    i("W", ["WW", 2], "Wo", "isoWeek"),
    e("week", "w"),
    e("isoWeek", "W"),
    n("week", 5),
    n("isoWeek", 5),
    w("w", b),
    w("ww", b, v),
    w("W", b),
    w("WW", b, v),
    Nt(["w", "ww", "W", "WW"], function(t, e, n, i) {
        e[i.substr(0, 1)] = y(t)
    });
    function Qt(t, e) {
        return t.slice(e, 7).concat(t.slice(0, e))
    }
    i("d", 0, "do", "day"),
    i("dd", 0, 0, function(t) {
        return this.localeData().weekdaysMin(this, t)
    }),
    i("ddd", 0, 0, function(t) {
        return this.localeData().weekdaysShort(this, t)
    }),
    i("dddd", 0, 0, function(t) {
        return this.localeData().weekdays(this, t)
    }),
    i("e", 0, 0, "weekday"),
    i("E", 0, 0, "isoWeekday"),
    e("day", "d"),
    e("weekday", "e"),
    e("isoWeekday", "E"),
    n("day", 11),
    n("weekday", 11),
    n("isoWeekday", 11),
    w("d", b),
    w("e", b),
    w("E", b),
    w("dd", function(t, e) {
        return e.weekdaysMinRegex(t)
    }),
    w("ddd", function(t, e) {
        return e.weekdaysShortRegex(t)
    }),
    w("dddd", function(t, e) {
        return e.weekdaysRegex(t)
    }),
    Nt(["dd", "ddd", "dddd"], function(t, e, n, i) {
        i = n._locale.weekdaysParse(t, i, n._strict);
        null != i ? e.d = i : f(n).invalidWeekday = t
    }),
    Nt(["d", "e", "E"], function(t, e, n, i) {
        e[i] = y(t)
    });
    var Kt = "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_")
      , Xt = "Sun_Mon_Tue_Wed_Thu_Fri_Sat".split("_")
      , Zt = "Su_Mo_Tu_We_Th_Fr_Sa".split("_")
      , Jt = _
      , te = _
      , ee = _;
    function ne() {
        function t(t, e) {
            return e.length - t.length
        }
        for (var e, n, i, s = [], r = [], o = [], a = [], l = 0; l < 7; l++)
            i = c([2e3, 1]).day(l),
            e = x(this.weekdaysMin(i, "")),
            n = x(this.weekdaysShort(i, "")),
            i = x(this.weekdays(i, "")),
            s.push(e),
            r.push(n),
            o.push(i),
            a.push(e),
            a.push(n),
            a.push(i);
        s.sort(t),
        r.sort(t),
        o.sort(t),
        a.sort(t),
        this._weekdaysRegex = new RegExp("^(" + a.join("|") + ")","i"),
        this._weekdaysShortRegex = this._weekdaysRegex,
        this._weekdaysMinRegex = this._weekdaysRegex,
        this._weekdaysStrictRegex = new RegExp("^(" + o.join("|") + ")","i"),
        this._weekdaysShortStrictRegex = new RegExp("^(" + r.join("|") + ")","i"),
        this._weekdaysMinStrictRegex = new RegExp("^(" + s.join("|") + ")","i")
    }
    function ie() {
        return this.hours() % 12 || 12
    }
    function se(t, e) {
        i(t, 0, 0, function() {
            return this.localeData().meridiem(this.hours(), this.minutes(), e)
        })
    }
    function re(t, e) {
        return e._meridiemParse
    }
    i("H", ["HH", 2], 0, "hour"),
    i("h", ["hh", 2], 0, ie),
    i("k", ["kk", 2], 0, function() {
        return this.hours() || 24
    }),
    i("hmm", 0, 0, function() {
        return "" + ie.apply(this) + r(this.minutes(), 2)
    }),
    i("hmmss", 0, 0, function() {
        return "" + ie.apply(this) + r(this.minutes(), 2) + r(this.seconds(), 2)
    }),
    i("Hmm", 0, 0, function() {
        return "" + this.hours() + r(this.minutes(), 2)
    }),
    i("Hmmss", 0, 0, function() {
        return "" + this.hours() + r(this.minutes(), 2) + r(this.seconds(), 2)
    }),
    se("a", !0),
    se("A", !1),
    e("hour", "h"),
    n("hour", 13),
    w("a", re),
    w("A", re),
    w("H", b),
    w("h", b),
    w("k", b),
    w("HH", b, v),
    w("hh", b, v),
    w("kk", b, v),
    w("hmm", yt),
    w("hmmss", vt),
    w("Hmm", yt),
    w("Hmmss", vt),
    C(["H", "HH"], E),
    C(["k", "kk"], function(t, e, n) {
        t = y(t);
        e[E] = 24 === t ? 0 : t
    }),
    C(["a", "A"], function(t, e, n) {
        n._isPm = n._locale.isPM(t),
        n._meridiem = t
    }),
    C(["h", "hh"], function(t, e, n) {
        e[E] = y(t),
        f(n).bigHour = !0
    }),
    C("hmm", function(t, e, n) {
        var i = t.length - 2;
        e[E] = y(t.substr(0, i)),
        e[N] = y(t.substr(i)),
        f(n).bigHour = !0
    }),
    C("hmmss", function(t, e, n) {
        var i = t.length - 4
          , s = t.length - 2;
        e[E] = y(t.substr(0, i)),
        e[N] = y(t.substr(i, 2)),
        e[$] = y(t.substr(s)),
        f(n).bigHour = !0
    }),
    C("Hmm", function(t, e, n) {
        var i = t.length - 2;
        e[E] = y(t.substr(0, i)),
        e[N] = y(t.substr(i))
    }),
    C("Hmmss", function(t, e, n) {
        var i = t.length - 4
          , s = t.length - 2;
        e[E] = y(t.substr(0, i)),
        e[N] = y(t.substr(i, 2)),
        e[$] = y(t.substr(s))
    });
    _ = dt("Hours", !0);
    var oe, ae = {
        calendar: {
            sameDay: "[Today at] LT",
            nextDay: "[Tomorrow at] LT",
            nextWeek: "dddd [at] LT",
            lastDay: "[Yesterday at] LT",
            lastWeek: "[Last] dddd [at] LT",
            sameElse: "L"
        },
        longDateFormat: {
            LTS: "h:mm:ss A",
            LT: "h:mm A",
            L: "MM/DD/YYYY",
            LL: "MMMM D, YYYY",
            LLL: "MMMM D, YYYY h:mm A",
            LLLL: "dddd, MMMM D, YYYY h:mm A"
        },
        invalidDate: "Invalid date",
        ordinal: "%d",
        dayOfMonthOrdinalParse: /\d{1,2}/,
        relativeTime: {
            future: "in %s",
            past: "%s ago",
            s: "a few seconds",
            ss: "%d seconds",
            m: "a minute",
            mm: "%d minutes",
            h: "an hour",
            hh: "%d hours",
            d: "a day",
            dd: "%d days",
            w: "a week",
            ww: "%d weeks",
            M: "a month",
            MM: "%d months",
            y: "a year",
            yy: "%d years"
        },
        months: Ot,
        monthsShort: Pt,
        week: {
            dow: 0,
            doy: 6
        },
        weekdays: Kt,
        weekdaysMin: Zt,
        weekdaysShort: Xt,
        meridiemParse: /[ap]\.?m?\.?/i
    }, M = {}, le = {};
    function ce(t) {
        return t && t.toLowerCase().replace("_", "-")
    }
    function ue(t) {
        for (var e, n, i, s, r = 0; r < t.length; ) {
            for (e = (s = ce(t[r]).split("-")).length,
            n = (n = ce(t[r + 1])) ? n.split("-") : null; 0 < e; ) {
                if (i = de(s.slice(0, e).join("-")))
                    return i;
                if (n && n.length >= e && function(t, e) {
                    for (var n = Math.min(t.length, e.length), i = 0; i < n; i += 1)
                        if (t[i] !== e[i])
                            return i;
                    return n
                }(s, n) >= e - 1)
                    break;
                e--
            }
            r++
        }
        return oe
    }
    function de(e) {
        var t;
        if (void 0 === M[e] && "undefined" != typeof module && module && module.exports && null != e.match("^[^/\\\\]*$"))
            try {
                t = oe._abbr,
                require("./locale/" + e),
                he(t)
            } catch (t) {
                M[e] = null
            }
        return M[e]
    }
    function he(t, e) {
        return t && ((e = a(e) ? fe(t) : pe(t, e)) ? oe = e : "undefined" != typeof console && console.warn && console.warn("Locale " + t + " not found. Did you forget to load it?")),
        oe._abbr
    }
    function pe(t, e) {
        if (null === e)
            return delete M[t],
            null;
        var n, i = ae;
        if (e.abbr = t,
        null != M[t])
            X("defineLocaleOverride", "use moment.updateLocale(localeName, config) to change an existing locale. moment.defineLocale(localeName, config) should only be used for creating a new locale See http://momentjs.com/guides/#/warnings/define-locale/ for more info."),
            i = M[t]._config;
        else if (null != e.parentLocale)
            if (null != M[e.parentLocale])
                i = M[e.parentLocale]._config;
            else {
                if (null == (n = de(e.parentLocale)))
                    return le[e.parentLocale] || (le[e.parentLocale] = []),
                    le[e.parentLocale].push({
                        name: t,
                        config: e
                    }),
                    null;
                i = n._config
            }
        return M[t] = new J(Z(i, e)),
        le[t] && le[t].forEach(function(t) {
            pe(t.name, t.config)
        }),
        he(t),
        M[t]
    }
    function fe(t) {
        var e;
        if (!(t = t && t._locale && t._locale._abbr ? t._locale._abbr : t))
            return oe;
        if (!o(t)) {
            if (e = de(t))
                return e;
            t = [t]
        }
        return ue(t)
    }
    function ge(t) {
        var e = t._a;
        return e && -2 === f(t).overflow && (e = e[T] < 0 || 11 < e[T] ? T : e[D] < 1 || e[D] > It(e[S], e[T]) ? D : e[E] < 0 || 24 < e[E] || 24 === e[E] && (0 !== e[N] || 0 !== e[$] || 0 !== e[$t]) ? E : e[N] < 0 || 59 < e[N] ? N : e[$] < 0 || 59 < e[$] ? $ : e[$t] < 0 || 999 < e[$t] ? $t : -1,
        f(t)._overflowDayOfYear && (e < S || D < e) && (e = D),
        f(t)._overflowWeeks && -1 === e && (e = At),
        f(t)._overflowWeekday && -1 === e && (e = Mt),
        f(t).overflow = e),
        t
    }
    var me = /^\s*((?:[+-]\d{6}|\d{4})-(?:\d\d-\d\d|W\d\d-\d|W\d\d|\d\d\d|\d\d))(?:(T| )(\d\d(?::\d\d(?::\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/
      , ye = /^\s*((?:[+-]\d{6}|\d{4})(?:\d\d\d\d|W\d\d\d|W\d\d|\d\d\d|\d\d|))(?:(T| )(\d\d(?:\d\d(?:\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/
      , ve = /Z|[+-]\d\d(?::?\d\d)?/
      , be = [["YYYYYY-MM-DD", /[+-]\d{6}-\d\d-\d\d/], ["YYYY-MM-DD", /\d{4}-\d\d-\d\d/], ["GGGG-[W]WW-E", /\d{4}-W\d\d-\d/], ["GGGG-[W]WW", /\d{4}-W\d\d/, !1], ["YYYY-DDD", /\d{4}-\d{3}/], ["YYYY-MM", /\d{4}-\d\d/, !1], ["YYYYYYMMDD", /[+-]\d{10}/], ["YYYYMMDD", /\d{8}/], ["GGGG[W]WWE", /\d{4}W\d{3}/], ["GGGG[W]WW", /\d{4}W\d{2}/, !1], ["YYYYDDD", /\d{7}/], ["YYYYMM", /\d{6}/, !1], ["YYYY", /\d{4}/, !1]]
      , _e = [["HH:mm:ss.SSSS", /\d\d:\d\d:\d\d\.\d+/], ["HH:mm:ss,SSSS", /\d\d:\d\d:\d\d,\d+/], ["HH:mm:ss", /\d\d:\d\d:\d\d/], ["HH:mm", /\d\d:\d\d/], ["HHmmss.SSSS", /\d\d\d\d\d\d\.\d+/], ["HHmmss,SSSS", /\d\d\d\d\d\d,\d+/], ["HHmmss", /\d\d\d\d\d\d/], ["HHmm", /\d\d\d\d/], ["HH", /\d\d/]]
      , we = /^\/?Date\((-?\d+)/i
      , xe = /^(?:(Mon|Tue|Wed|Thu|Fri|Sat|Sun),?\s)?(\d{1,2})\s(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\s(\d{2,4})\s(\d\d):(\d\d)(?::(\d\d))?\s(?:(UT|GMT|[ECMP][SD]T)|([Zz])|([+-]\d{4}))$/
      , Ce = {
        UT: 0,
        GMT: 0,
        EDT: -240,
        EST: -300,
        CDT: -300,
        CST: -360,
        MDT: -360,
        MST: -420,
        PDT: -420,
        PST: -480
    };
    function ke(t) {
        var e, n, i, s, r, o, a = t._i, l = me.exec(a) || ye.exec(a), a = be.length, c = _e.length;
        if (l) {
            for (f(t).iso = !0,
            e = 0,
            n = a; e < n; e++)
                if (be[e][1].exec(l[1])) {
                    s = be[e][0],
                    i = !1 !== be[e][2];
                    break
                }
            if (null == s)
                t._isValid = !1;
            else {
                if (l[3]) {
                    for (e = 0,
                    n = c; e < n; e++)
                        if (_e[e][1].exec(l[3])) {
                            r = (l[2] || " ") + _e[e][0];
                            break
                        }
                    if (null == r)
                        return void (t._isValid = !1)
                }
                if (i || null == r) {
                    if (l[4]) {
                        if (!ve.exec(l[4]))
                            return void (t._isValid = !1);
                        o = "Z"
                    }
                    t._f = s + (r || "") + (o || ""),
                    Ne(t)
                } else
                    t._isValid = !1
            }
        } else
            t._isValid = !1
    }
    function Se(t, e, n, i, s, r) {
        t = [function(t) {
            t = parseInt(t, 10);
            {
                if (t <= 49)
                    return 2e3 + t;
                if (t <= 999)
                    return 1900 + t
            }
            return t
        }(t), Pt.indexOf(e), parseInt(n, 10), parseInt(i, 10), parseInt(s, 10)];
        return r && t.push(parseInt(r, 10)),
        t
    }
    function Te(t) {
        var e, n, i, s, r = xe.exec(t._i.replace(/\([^)]*\)|[\n\t]/g, " ").replace(/(\s\s+)/g, " ").replace(/^\s\s*/, "").replace(/\s\s*$/, ""));
        r ? (e = Se(r[4], r[3], r[2], r[5], r[6], r[7]),
        n = r[1],
        i = e,
        s = t,
        n && Xt.indexOf(n) !== new Date(i[0],i[1],i[2]).getDay() ? (f(s).weekdayMismatch = !0,
        s._isValid = !1) : (t._a = e,
        t._tzm = (n = r[8],
        i = r[9],
        s = r[10],
        n ? Ce[n] : i ? 0 : 60 * (((n = parseInt(s, 10)) - (i = n % 100)) / 100) + i),
        t._d = Bt.apply(null, t._a),
        t._d.setUTCMinutes(t._d.getUTCMinutes() - t._tzm),
        f(t).rfc2822 = !0)) : t._isValid = !1
    }
    function De(t, e, n) {
        return null != t ? t : null != e ? e : n
    }
    function Ee(t) {
        var e, n, i, s, r, o, a, l, c, u, d, h = [];
        if (!t._d) {
            for (i = t,
            s = new Date(p.now()),
            n = i._useUTC ? [s.getUTCFullYear(), s.getUTCMonth(), s.getUTCDate()] : [s.getFullYear(), s.getMonth(), s.getDate()],
            t._w && null == t._a[D] && null == t._a[T] && (null != (s = (i = t)._w).GG || null != s.W || null != s.E ? (l = 1,
            c = 4,
            r = De(s.GG, i._a[S], Gt(I(), 1, 4).year),
            o = De(s.W, 1),
            ((a = De(s.E, 1)) < 1 || 7 < a) && (u = !0)) : (l = i._locale._week.dow,
            c = i._locale._week.doy,
            d = Gt(I(), l, c),
            r = De(s.gg, i._a[S], d.year),
            o = De(s.w, d.week),
            null != s.d ? ((a = s.d) < 0 || 6 < a) && (u = !0) : null != s.e ? (a = s.e + l,
            (s.e < 0 || 6 < s.e) && (u = !0)) : a = l),
            o < 1 || o > A(r, l, c) ? f(i)._overflowWeeks = !0 : null != u ? f(i)._overflowWeekday = !0 : (d = Ut(r, o, a, l, c),
            i._a[S] = d.year,
            i._dayOfYear = d.dayOfYear)),
            null != t._dayOfYear && (s = De(t._a[S], n[S]),
            (t._dayOfYear > Yt(s) || 0 === t._dayOfYear) && (f(t)._overflowDayOfYear = !0),
            u = Bt(s, 0, t._dayOfYear),
            t._a[T] = u.getUTCMonth(),
            t._a[D] = u.getUTCDate()),
            e = 0; e < 3 && null == t._a[e]; ++e)
                t._a[e] = h[e] = n[e];
            for (; e < 7; e++)
                t._a[e] = h[e] = null == t._a[e] ? 2 === e ? 1 : 0 : t._a[e];
            24 === t._a[E] && 0 === t._a[N] && 0 === t._a[$] && 0 === t._a[$t] && (t._nextDay = !0,
            t._a[E] = 0),
            t._d = (t._useUTC ? Bt : zt).apply(null, h),
            r = t._useUTC ? t._d.getUTCDay() : t._d.getDay(),
            null != t._tzm && t._d.setUTCMinutes(t._d.getUTCMinutes() - t._tzm),
            t._nextDay && (t._a[E] = 24),
            t._w && void 0 !== t._w.d && t._w.d !== r && (f(t).weekdayMismatch = !0)
        }
    }
    function Ne(t) {
        if (t._f === p.ISO_8601)
            ke(t);
        else if (t._f === p.RFC_2822)
            Te(t);
        else {
            t._a = [],
            f(t).empty = !0;
            for (var e, n, i, s, r, o = "" + t._i, a = o.length, l = 0, c = ot(t._f, t._locale).match(et) || [], u = c.length, d = 0; d < u; d++)
                n = c[d],
                (e = (o.match(Tt(n, t)) || [])[0]) && (0 < (i = o.substr(0, o.indexOf(e))).length && f(t).unusedInput.push(i),
                o = o.slice(o.indexOf(e) + e.length),
                l += e.length),
                st[n] ? (e ? f(t).empty = !1 : f(t).unusedTokens.push(n),
                i = n,
                r = t,
                null != (s = e) && h(Et, i) && Et[i](s, r._a, r, i)) : t._strict && !e && f(t).unusedTokens.push(n);
            f(t).charsLeftOver = a - l,
            0 < o.length && f(t).unusedInput.push(o),
            t._a[E] <= 12 && !0 === f(t).bigHour && 0 < t._a[E] && (f(t).bigHour = void 0),
            f(t).parsedDateParts = t._a.slice(0),
            f(t).meridiem = t._meridiem,
            t._a[E] = function(t, e, n) {
                if (null == n)
                    return e;
                return null != t.meridiemHour ? t.meridiemHour(e, n) : null != t.isPM ? ((t = t.isPM(n)) && e < 12 && (e += 12),
                e = t || 12 !== e ? e : 0) : e
            }(t._locale, t._a[E], t._meridiem),
            null !== (a = f(t).era) && (t._a[S] = t._locale.erasConvertYear(a, t._a[S])),
            Ee(t),
            ge(t)
        }
    }
    function $e(t) {
        var e, n, i, s = t._i, r = t._f;
        if (t._locale = t._locale || fe(t._l),
        null === s || void 0 === r && "" === s)
            return q({
                nullInput: !0
            });
        if ("string" == typeof s && (t._i = s = t._locale.preparse(s)),
        u(s))
            return new G(ge(s));
        if (H(s))
            t._d = s;
        else if (o(r))
            !function(t) {
                var e, n, i, s, r, o, a = !1, l = t._f.length;
                if (0 === l)
                    return f(t).invalidFormat = !0,
                    t._d = new Date(NaN);
                for (s = 0; s < l; s++)
                    r = 0,
                    o = !1,
                    e = U({}, t),
                    null != t._useUTC && (e._useUTC = t._useUTC),
                    e._f = t._f[s],
                    Ne(e),
                    Y(e) && (o = !0),
                    r = (r += f(e).charsLeftOver) + 10 * f(e).unusedTokens.length,
                    f(e).score = r,
                    a ? r < i && (i = r,
                    n = e) : (null == i || r < i || o) && (i = r,
                    n = e,
                    o && (a = !0));
                W(t, n || e)
            }(t);
        else if (r)
            Ne(t);
        else if (a(r = (s = t)._i))
            s._d = new Date(p.now());
        else
            H(r) ? s._d = new Date(r.valueOf()) : "string" == typeof r ? (n = s,
            null !== (e = we.exec(n._i)) ? n._d = new Date(+e[1]) : (ke(n),
            !1 === n._isValid && (delete n._isValid,
            Te(n),
            !1 === n._isValid && (delete n._isValid,
            n._strict ? n._isValid = !1 : p.createFromInputFallback(n))))) : o(r) ? (s._a = F(r.slice(0), function(t) {
                return parseInt(t, 10)
            }),
            Ee(s)) : L(r) ? (e = s)._d || (i = void 0 === (n = lt(e._i)).day ? n.date : n.day,
            e._a = F([n.year, n.month, i, n.hour, n.minute, n.second, n.millisecond], function(t) {
                return t && parseInt(t, 10)
            }),
            Ee(e)) : l(r) ? s._d = new Date(r) : p.createFromInputFallback(s);
        return Y(t) || (t._d = null),
        t
    }
    function Ae(t, e, n, i, s) {
        var r = {};
        return !0 !== e && !1 !== e || (i = e,
        e = void 0),
        !0 !== n && !1 !== n || (i = n,
        n = void 0),
        (L(t) && j(t) || o(t) && 0 === t.length) && (t = void 0),
        r._isAMomentObject = !0,
        r._useUTC = r._isUTC = s,
        r._l = n,
        r._i = t,
        r._f = e,
        r._strict = i,
        (s = new G(ge($e(s = r))))._nextDay && (s.add(1, "d"),
        s._nextDay = void 0),
        s
    }
    function I(t, e, n, i) {
        return Ae(t, e, n, i, !1)
    }
    p.createFromInputFallback = t("value provided is not in a recognized RFC2822 or ISO format. moment construction falls back to js Date(), which is not reliable across all browsers and versions. Non RFC2822/ISO date formats are discouraged. Please refer to http://momentjs.com/guides/#/warnings/js-date/ for more info.", function(t) {
        t._d = new Date(t._i + (t._useUTC ? " UTC" : ""))
    }),
    p.ISO_8601 = function() {}
    ,
    p.RFC_2822 = function() {}
    ;
    yt = t("moment().min is deprecated, use moment.max instead. http://momentjs.com/guides/#/warnings/min-max/", function() {
        var t = I.apply(null, arguments);
        return this.isValid() && t.isValid() ? t < this ? this : t : q()
    }),
    vt = t("moment().max is deprecated, use moment.min instead. http://momentjs.com/guides/#/warnings/min-max/", function() {
        var t = I.apply(null, arguments);
        return this.isValid() && t.isValid() ? this < t ? this : t : q()
    });
    function Me(t, e) {
        var n, i;
        if (!(e = 1 === e.length && o(e[0]) ? e[0] : e).length)
            return I();
        for (n = e[0],
        i = 1; i < e.length; ++i)
            e[i].isValid() && !e[i][t](n) || (n = e[i]);
        return n
    }
    var Ie = ["year", "quarter", "month", "week", "day", "hour", "minute", "second", "millisecond"];
    function Oe(t) {
        var t = lt(t)
          , e = t.year || 0
          , n = t.quarter || 0
          , i = t.month || 0
          , s = t.week || t.isoWeek || 0
          , r = t.day || 0
          , o = t.hour || 0
          , a = t.minute || 0
          , l = t.second || 0
          , c = t.millisecond || 0;
        this._isValid = function(t) {
            var e, n, i = !1, s = Ie.length;
            for (e in t)
                if (h(t, e) && (-1 === k.call(Ie, e) || null != t[e] && isNaN(t[e])))
                    return !1;
            for (n = 0; n < s; ++n)
                if (t[Ie[n]]) {
                    if (i)
                        return !1;
                    parseFloat(t[Ie[n]]) !== y(t[Ie[n]]) && (i = !0)
                }
            return !0
        }(t),
        this._milliseconds = +c + 1e3 * l + 6e4 * a + 1e3 * o * 60 * 60,
        this._days = +r + 7 * s,
        this._months = +i + 3 * n + 12 * e,
        this._data = {},
        this._locale = fe(),
        this._bubble()
    }
    function Pe(t) {
        return t instanceof Oe
    }
    function Re(t) {
        return t < 0 ? -1 * Math.round(-1 * t) : Math.round(t)
    }
    function Le(t, n) {
        i(t, 0, 0, function() {
            var t = this.utcOffset()
              , e = "+";
            return t < 0 && (t = -t,
            e = "-"),
            e + r(~~(t / 60), 2) + n + r(~~t % 60, 2)
        })
    }
    Le("Z", ":"),
    Le("ZZ", ""),
    w("Z", St),
    w("ZZ", St),
    C(["Z", "ZZ"], function(t, e, n) {
        n._useUTC = !0,
        n._tzm = He(St, t)
    });
    var je = /([\+\-]|\d\d)/gi;
    function He(t, e) {
        var e = (e || "").match(t);
        return null === e ? null : 0 === (e = 60 * (t = ((e[e.length - 1] || []) + "").match(je) || ["-", 0, 0])[1] + y(t[2])) ? 0 : "+" === t[0] ? e : -e
    }
    function Fe(t, e) {
        var n;
        return e._isUTC ? (e = e.clone(),
        n = (u(t) || H(t) ? t : I(t)).valueOf() - e.valueOf(),
        e._d.setTime(e._d.valueOf() + n),
        p.updateOffset(e, !1),
        e) : I(t).local()
    }
    function We(t) {
        return -Math.round(t._d.getTimezoneOffset())
    }
    function Ye() {
        return !!this.isValid() && (this._isUTC && 0 === this._offset)
    }
    p.updateOffset = function() {}
    ;
    var qe = /^(-|\+)?(?:(\d*)[. ])?(\d+):(\d+)(?::(\d+)(\.\d*)?)?$/
      , ze = /^(-|\+)?P(?:([-+]?[0-9,.]*)Y)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)W)?(?:([-+]?[0-9,.]*)D)?(?:T(?:([-+]?[0-9,.]*)H)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)S)?)?$/;
    function O(t, e) {
        var n, i = t, s = null;
        return Pe(t) ? i = {
            ms: t._milliseconds,
            d: t._days,
            M: t._months
        } : l(t) || !isNaN(+t) ? (i = {},
        e ? i[e] = +t : i.milliseconds = +t) : (s = qe.exec(t)) ? (n = "-" === s[1] ? -1 : 1,
        i = {
            y: 0,
            d: y(s[D]) * n,
            h: y(s[E]) * n,
            m: y(s[N]) * n,
            s: y(s[$]) * n,
            ms: y(Re(1e3 * s[$t])) * n
        }) : (s = ze.exec(t)) ? (n = "-" === s[1] ? -1 : 1,
        i = {
            y: Be(s[2], n),
            M: Be(s[3], n),
            w: Be(s[4], n),
            d: Be(s[5], n),
            h: Be(s[6], n),
            m: Be(s[7], n),
            s: Be(s[8], n)
        }) : null == i ? i = {} : "object" == typeof i && ("from"in i || "to"in i) && (e = function(t, e) {
            var n;
            if (!t.isValid() || !e.isValid())
                return {
                    milliseconds: 0,
                    months: 0
                };
            e = Fe(e, t),
            t.isBefore(e) ? n = Ve(t, e) : ((n = Ve(e, t)).milliseconds = -n.milliseconds,
            n.months = -n.months);
            return n
        }(I(i.from), I(i.to)),
        (i = {}).ms = e.milliseconds,
        i.M = e.months),
        s = new Oe(i),
        Pe(t) && h(t, "_locale") && (s._locale = t._locale),
        Pe(t) && h(t, "_isValid") && (s._isValid = t._isValid),
        s
    }
    function Be(t, e) {
        t = t && parseFloat(t.replace(",", "."));
        return (isNaN(t) ? 0 : t) * e
    }
    function Ve(t, e) {
        var n = {};
        return n.months = e.month() - t.month() + 12 * (e.year() - t.year()),
        t.clone().add(n.months, "M").isAfter(e) && --n.months,
        n.milliseconds = +e - +t.clone().add(n.months, "M"),
        n
    }
    function Ue(i, s) {
        return function(t, e) {
            var n;
            return null === e || isNaN(+e) || (X(s, "moment()." + s + "(period, number) is deprecated. Please use moment()." + s + "(number, period). See http://momentjs.com/guides/#/warnings/add-inverted-param/ for more info."),
            n = t,
            t = e,
            e = n),
            Ge(this, O(t, e), i),
            this
        }
    }
    function Ge(t, e, n, i) {
        var s = e._milliseconds
          , r = Re(e._days)
          , e = Re(e._months);
        t.isValid() && (i = null == i || i,
        e && Ht(t, ht(t, "Month") + e * n),
        r && pt(t, "Date", ht(t, "Date") + r * n),
        s && t._d.setTime(t._d.valueOf() + s * n),
        i && p.updateOffset(t, r || e))
    }
    O.fn = Oe.prototype,
    O.invalid = function() {
        return O(NaN)
    }
    ;
    Ot = Ue(1, "add"),
    Kt = Ue(-1, "subtract");
    function Qe(t) {
        return "string" == typeof t || t instanceof String
    }
    function Ke(t) {
        return u(t) || H(t) || Qe(t) || l(t) || function(e) {
            var t = o(e)
              , n = !1;
            t && (n = 0 === e.filter(function(t) {
                return !l(t) && Qe(e)
            }).length);
            return t && n
        }(t) || function(t) {
            var e, n, i = L(t) && !j(t), s = !1, r = ["years", "year", "y", "months", "month", "M", "days", "day", "d", "dates", "date", "D", "hours", "hour", "h", "minutes", "minute", "m", "seconds", "second", "s", "milliseconds", "millisecond", "ms"], o = r.length;
            for (e = 0; e < o; e += 1)
                n = r[e],
                s = s || h(t, n);
            return i && s
        }(t) || null == t
    }
    function Xe(t, e) {
        if (t.date() < e.date())
            return -Xe(e, t);
        var n = 12 * (e.year() - t.year()) + (e.month() - t.month())
          , i = t.clone().add(n, "months")
          , e = e - i < 0 ? (e - i) / (i - t.clone().add(n - 1, "months")) : (e - i) / (t.clone().add(1 + n, "months") - i);
        return -(n + e) || 0
    }
    function Ze(t) {
        return void 0 === t ? this._locale._abbr : (null != (t = fe(t)) && (this._locale = t),
        this)
    }
    p.defaultFormat = "YYYY-MM-DDTHH:mm:ssZ",
    p.defaultFormatUtc = "YYYY-MM-DDTHH:mm:ss[Z]";
    Zt = t("moment().lang() is deprecated. Instead, use moment().localeData() to get the language configuration. Use moment().locale() to change languages.", function(t) {
        return void 0 === t ? this.localeData() : this.locale(t)
    });
    function Je() {
        return this._locale
    }
    var tn = 126227808e5;
    function en(t, e) {
        return (t % e + e) % e
    }
    function nn(t, e, n) {
        return t < 100 && 0 <= t ? new Date(t + 400,e,n) - tn : new Date(t,e,n).valueOf()
    }
    function sn(t, e, n) {
        return t < 100 && 0 <= t ? Date.UTC(t + 400, e, n) - tn : Date.UTC(t, e, n)
    }
    function rn(t, e) {
        return e.erasAbbrRegex(t)
    }
    function on() {
        for (var t = [], e = [], n = [], i = [], s = this.eras(), r = 0, o = s.length; r < o; ++r)
            e.push(x(s[r].name)),
            t.push(x(s[r].abbr)),
            n.push(x(s[r].narrow)),
            i.push(x(s[r].name)),
            i.push(x(s[r].abbr)),
            i.push(x(s[r].narrow));
        this._erasRegex = new RegExp("^(" + i.join("|") + ")","i"),
        this._erasNameRegex = new RegExp("^(" + e.join("|") + ")","i"),
        this._erasAbbrRegex = new RegExp("^(" + t.join("|") + ")","i"),
        this._erasNarrowRegex = new RegExp("^(" + n.join("|") + ")","i")
    }
    function an(t, e) {
        i(0, [t, t.length], 0, e)
    }
    function ln(t, e, n, i, s) {
        var r;
        return null == t ? Gt(this, i, s).year : (r = A(t, i, s),
        function(t, e, n, i, s) {
            t = Ut(t, e, n, i, s),
            e = Bt(t.year, 0, t.dayOfYear);
            return this.year(e.getUTCFullYear()),
            this.month(e.getUTCMonth()),
            this.date(e.getUTCDate()),
            this
        }
        .call(this, t, e = r < e ? r : e, n, i, s))
    }
    i("N", 0, 0, "eraAbbr"),
    i("NN", 0, 0, "eraAbbr"),
    i("NNN", 0, 0, "eraAbbr"),
    i("NNNN", 0, 0, "eraName"),
    i("NNNNN", 0, 0, "eraNarrow"),
    i("y", ["y", 1], "yo", "eraYear"),
    i("y", ["yy", 2], 0, "eraYear"),
    i("y", ["yyy", 3], 0, "eraYear"),
    i("y", ["yyyy", 4], 0, "eraYear"),
    w("N", rn),
    w("NN", rn),
    w("NNN", rn),
    w("NNNN", function(t, e) {
        return e.erasNameRegex(t)
    }),
    w("NNNNN", function(t, e) {
        return e.erasNarrowRegex(t)
    }),
    C(["N", "NN", "NNN", "NNNN", "NNNNN"], function(t, e, n, i) {
        i = n._locale.erasParse(t, i, n._strict);
        i ? f(n).era = i : f(n).invalidEra = t
    }),
    w("y", xt),
    w("yy", xt),
    w("yyy", xt),
    w("yyyy", xt),
    w("yo", function(t, e) {
        return e._eraYearOrdinalRegex || xt
    }),
    C(["y", "yy", "yyy", "yyyy"], S),
    C(["yo"], function(t, e, n, i) {
        var s;
        n._locale._eraYearOrdinalRegex && (s = t.match(n._locale._eraYearOrdinalRegex)),
        n._locale.eraYearOrdinalParse ? e[S] = n._locale.eraYearOrdinalParse(t, s) : e[S] = parseInt(t, 10)
    }),
    i(0, ["gg", 2], 0, function() {
        return this.weekYear() % 100
    }),
    i(0, ["GG", 2], 0, function() {
        return this.isoWeekYear() % 100
    }),
    an("gggg", "weekYear"),
    an("ggggg", "weekYear"),
    an("GGGG", "isoWeekYear"),
    an("GGGGG", "isoWeekYear"),
    e("weekYear", "gg"),
    e("isoWeekYear", "GG"),
    n("weekYear", 1),
    n("isoWeekYear", 1),
    w("G", Ct),
    w("g", Ct),
    w("GG", b, v),
    w("gg", b, v),
    w("GGGG", _t, gt),
    w("gggg", _t, gt),
    w("GGGGG", wt, mt),
    w("ggggg", wt, mt),
    Nt(["gggg", "ggggg", "GGGG", "GGGGG"], function(t, e, n, i) {
        e[i.substr(0, 2)] = y(t)
    }),
    Nt(["gg", "GG"], function(t, e, n, i) {
        e[i] = p.parseTwoDigitYear(t)
    }),
    i("Q", 0, "Qo", "quarter"),
    e("quarter", "Q"),
    n("quarter", 7),
    w("Q", s),
    C("Q", function(t, e) {
        e[T] = 3 * (y(t) - 1)
    }),
    i("D", ["DD", 2], "Do", "date"),
    e("date", "D"),
    n("date", 9),
    w("D", b),
    w("DD", b, v),
    w("Do", function(t, e) {
        return t ? e._dayOfMonthOrdinalParse || e._ordinalParse : e._dayOfMonthOrdinalParseLenient
    }),
    C(["D", "DD"], D),
    C("Do", function(t, e) {
        e[D] = y(t.match(b)[0])
    });
    _t = dt("Date", !0);
    i("DDD", ["DDDD", 3], "DDDo", "dayOfYear"),
    e("dayOfYear", "DDD"),
    n("dayOfYear", 4),
    w("DDD", bt),
    w("DDDD", ft),
    C(["DDD", "DDDD"], function(t, e, n) {
        n._dayOfYear = y(t)
    }),
    i("m", ["mm", 2], 0, "minute"),
    e("minute", "m"),
    n("minute", 14),
    w("m", b),
    w("mm", b, v),
    C(["m", "mm"], N);
    var cn, gt = dt("Minutes", !1), wt = (i("s", ["ss", 2], 0, "second"),
    e("second", "s"),
    n("second", 15),
    w("s", b),
    w("ss", b, v),
    C(["s", "ss"], $),
    dt("Seconds", !1));
    for (i("S", 0, 0, function() {
        return ~~(this.millisecond() / 100)
    }),
    i(0, ["SS", 2], 0, function() {
        return ~~(this.millisecond() / 10)
    }),
    i(0, ["SSS", 3], 0, "millisecond"),
    i(0, ["SSSS", 4], 0, function() {
        return 10 * this.millisecond()
    }),
    i(0, ["SSSSS", 5], 0, function() {
        return 100 * this.millisecond()
    }),
    i(0, ["SSSSSS", 6], 0, function() {
        return 1e3 * this.millisecond()
    }),
    i(0, ["SSSSSSS", 7], 0, function() {
        return 1e4 * this.millisecond()
    }),
    i(0, ["SSSSSSSS", 8], 0, function() {
        return 1e5 * this.millisecond()
    }),
    i(0, ["SSSSSSSSS", 9], 0, function() {
        return 1e6 * this.millisecond()
    }),
    e("millisecond", "ms"),
    n("millisecond", 16),
    w("S", bt, s),
    w("SS", bt, v),
    w("SSS", bt, ft),
    cn = "SSSS"; cn.length <= 9; cn += "S")
        w(cn, xt);
    function un(t, e) {
        e[$t] = y(1e3 * ("0." + t))
    }
    for (cn = "S"; cn.length <= 9; cn += "S")
        C(cn, un);
    mt = dt("Milliseconds", !1),
    i("z", 0, 0, "zoneAbbr"),
    i("zz", 0, 0, "zoneName");
    s = G.prototype;
    function dn(t) {
        return t
    }
    s.add = Ot,
    s.calendar = function(t, e) {
        1 === arguments.length && (arguments[0] ? Ke(arguments[0]) ? (t = arguments[0],
        e = void 0) : function(t) {
            for (var e = L(t) && !j(t), n = !1, i = ["sameDay", "nextDay", "lastDay", "nextWeek", "lastWeek", "sameElse"], s = 0; s < i.length; s += 1)
                n = n || h(t, i[s]);
            return e && n
        }(arguments[0]) && (e = arguments[0],
        t = void 0) : e = t = void 0);
        var t = t || I()
          , n = Fe(t, this).startOf("day")
          , n = p.calendarFormat(this, n) || "sameElse"
          , e = e && (d(e[n]) ? e[n].call(this, t) : e[n]);
        return this.format(e || this.localeData().calendar(n, this, I(t)))
    }
    ,
    s.clone = function() {
        return new G(this)
    }
    ,
    s.diff = function(t, e, n) {
        var i, s, r;
        if (!this.isValid())
            return NaN;
        if (!(i = Fe(t, this)).isValid())
            return NaN;
        switch (s = 6e4 * (i.utcOffset() - this.utcOffset()),
        e = g(e)) {
        case "year":
            r = Xe(this, i) / 12;
            break;
        case "month":
            r = Xe(this, i);
            break;
        case "quarter":
            r = Xe(this, i) / 3;
            break;
        case "second":
            r = (this - i) / 1e3;
            break;
        case "minute":
            r = (this - i) / 6e4;
            break;
        case "hour":
            r = (this - i) / 36e5;
            break;
        case "day":
            r = (this - i - s) / 864e5;
            break;
        case "week":
            r = (this - i - s) / 6048e5;
            break;
        default:
            r = this - i
        }
        return n ? r : m(r)
    }
    ,
    s.endOf = function(t) {
        var e, n;
        if (void 0 === (t = g(t)) || "millisecond" === t || !this.isValid())
            return this;
        switch (n = this._isUTC ? sn : nn,
        t) {
        case "year":
            e = n(this.year() + 1, 0, 1) - 1;
            break;
        case "quarter":
            e = n(this.year(), this.month() - this.month() % 3 + 3, 1) - 1;
            break;
        case "month":
            e = n(this.year(), this.month() + 1, 1) - 1;
            break;
        case "week":
            e = n(this.year(), this.month(), this.date() - this.weekday() + 7) - 1;
            break;
        case "isoWeek":
            e = n(this.year(), this.month(), this.date() - (this.isoWeekday() - 1) + 7) - 1;
            break;
        case "day":
        case "date":
            e = n(this.year(), this.month(), this.date() + 1) - 1;
            break;
        case "hour":
            e = this._d.valueOf(),
            e += 36e5 - en(e + (this._isUTC ? 0 : 6e4 * this.utcOffset()), 36e5) - 1;
            break;
        case "minute":
            e = this._d.valueOf(),
            e += 6e4 - en(e, 6e4) - 1;
            break;
        case "second":
            e = this._d.valueOf(),
            e += 1e3 - en(e, 1e3) - 1
        }
        return this._d.setTime(e),
        p.updateOffset(this, !0),
        this
    }
    ,
    s.format = function(t) {
        return t = t || (this.isUtc() ? p.defaultFormatUtc : p.defaultFormat),
        t = rt(this, t),
        this.localeData().postformat(t)
    }
    ,
    s.from = function(t, e) {
        return this.isValid() && (u(t) && t.isValid() || I(t).isValid()) ? O({
            to: this,
            from: t
        }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate()
    }
    ,
    s.fromNow = function(t) {
        return this.from(I(), t)
    }
    ,
    s.to = function(t, e) {
        return this.isValid() && (u(t) && t.isValid() || I(t).isValid()) ? O({
            from: this,
            to: t
        }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate()
    }
    ,
    s.toNow = function(t) {
        return this.to(I(), t)
    }
    ,
    s.get = function(t) {
        return d(this[t = g(t)]) ? this[t]() : this
    }
    ,
    s.invalidAt = function() {
        return f(this).overflow
    }
    ,
    s.isAfter = function(t, e) {
        return t = u(t) ? t : I(t),
        !(!this.isValid() || !t.isValid()) && ("millisecond" === (e = g(e) || "millisecond") ? this.valueOf() > t.valueOf() : t.valueOf() < this.clone().startOf(e).valueOf())
    }
    ,
    s.isBefore = function(t, e) {
        return t = u(t) ? t : I(t),
        !(!this.isValid() || !t.isValid()) && ("millisecond" === (e = g(e) || "millisecond") ? this.valueOf() < t.valueOf() : this.clone().endOf(e).valueOf() < t.valueOf())
    }
    ,
    s.isBetween = function(t, e, n, i) {
        return t = u(t) ? t : I(t),
        e = u(e) ? e : I(e),
        !!(this.isValid() && t.isValid() && e.isValid()) && (("(" === (i = i || "()")[0] ? this.isAfter(t, n) : !this.isBefore(t, n)) && (")" === i[1] ? this.isBefore(e, n) : !this.isAfter(e, n)))
    }
    ,
    s.isSame = function(t, e) {
        var t = u(t) ? t : I(t);
        return !(!this.isValid() || !t.isValid()) && ("millisecond" === (e = g(e) || "millisecond") ? this.valueOf() === t.valueOf() : (t = t.valueOf(),
        this.clone().startOf(e).valueOf() <= t && t <= this.clone().endOf(e).valueOf()))
    }
    ,
    s.isSameOrAfter = function(t, e) {
        return this.isSame(t, e) || this.isAfter(t, e)
    }
    ,
    s.isSameOrBefore = function(t, e) {
        return this.isSame(t, e) || this.isBefore(t, e)
    }
    ,
    s.isValid = function() {
        return Y(this)
    }
    ,
    s.lang = Zt,
    s.locale = Ze,
    s.localeData = Je,
    s.max = vt,
    s.min = yt,
    s.parsingFlags = function() {
        return W({}, f(this))
    }
    ,
    s.set = function(t, e) {
        if ("object" == typeof t)
            for (var n = function(t) {
                var e, n = [];
                for (e in t)
                    h(t, e) && n.push({
                        unit: e,
                        priority: ct[e]
                    });
                return n.sort(function(t, e) {
                    return t.priority - e.priority
                }),
                n
            }(t = lt(t)), i = n.length, s = 0; s < i; s++)
                this[n[s].unit](t[n[s].unit]);
        else if (d(this[t = g(t)]))
            return this[t](e);
        return this
    }
    ,
    s.startOf = function(t) {
        var e, n;
        if (void 0 === (t = g(t)) || "millisecond" === t || !this.isValid())
            return this;
        switch (n = this._isUTC ? sn : nn,
        t) {
        case "year":
            e = n(this.year(), 0, 1);
            break;
        case "quarter":
            e = n(this.year(), this.month() - this.month() % 3, 1);
            break;
        case "month":
            e = n(this.year(), this.month(), 1);
            break;
        case "week":
            e = n(this.year(), this.month(), this.date() - this.weekday());
            break;
        case "isoWeek":
            e = n(this.year(), this.month(), this.date() - (this.isoWeekday() - 1));
            break;
        case "day":
        case "date":
            e = n(this.year(), this.month(), this.date());
            break;
        case "hour":
            e = this._d.valueOf(),
            e -= en(e + (this._isUTC ? 0 : 6e4 * this.utcOffset()), 36e5);
            break;
        case "minute":
            e = this._d.valueOf(),
            e -= en(e, 6e4);
            break;
        case "second":
            e = this._d.valueOf(),
            e -= en(e, 1e3)
        }
        return this._d.setTime(e),
        p.updateOffset(this, !0),
        this
    }
    ,
    s.subtract = Kt,
    s.toArray = function() {
        return [this.year(), this.month(), this.date(), this.hour(), this.minute(), this.second(), this.millisecond()]
    }
    ,
    s.toObject = function() {
        return {
            years: this.year(),
            months: this.month(),
            date: this.date(),
            hours: this.hours(),
            minutes: this.minutes(),
            seconds: this.seconds(),
            milliseconds: this.milliseconds()
        }
    }
    ,
    s.toDate = function() {
        return new Date(this.valueOf())
    }
    ,
    s.toISOString = function(t) {
        if (!this.isValid())
            return null;
        var e = (t = !0 !== t) ? this.clone().utc() : this;
        return e.year() < 0 || 9999 < e.year() ? rt(e, t ? "YYYYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYYYY-MM-DD[T]HH:mm:ss.SSSZ") : d(Date.prototype.toISOString) ? t ? this.toDate().toISOString() : new Date(this.valueOf() + 60 * this.utcOffset() * 1e3).toISOString().replace("Z", rt(e, "Z")) : rt(e, t ? "YYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYY-MM-DD[T]HH:mm:ss.SSSZ")
    }
    ,
    s.inspect = function() {
        if (!this.isValid())
            return "moment.invalid(/* " + this._i + " */)";
        var t, e = "moment", n = "";
        return this.isLocal() || (e = 0 === this.utcOffset() ? "moment.utc" : "moment.parseZone",
        n = "Z"),
        e = "[" + e + '("]',
        t = 0 <= this.year() && this.year() <= 9999 ? "YYYY" : "YYYYYY",
        this.format(e + t + "-MM-DD[T]HH:mm:ss.SSS" + (n + '[")]'))
    }
    ,
    "undefined" != typeof Symbol && null != Symbol.for && (s[Symbol.for("nodejs.util.inspect.custom")] = function() {
        return "Moment<" + this.format() + ">"
    }
    ),
    s.toJSON = function() {
        return this.isValid() ? this.toISOString() : null
    }
    ,
    s.toString = function() {
        return this.clone().locale("en").format("ddd MMM DD YYYY HH:mm:ss [GMT]ZZ")
    }
    ,
    s.unix = function() {
        return Math.floor(this.valueOf() / 1e3)
    }
    ,
    s.valueOf = function() {
        return this._d.valueOf() - 6e4 * (this._offset || 0)
    }
    ,
    s.creationData = function() {
        return {
            input: this._i,
            format: this._f,
            locale: this._locale,
            isUTC: this._isUTC,
            strict: this._strict
        }
    }
    ,
    s.eraName = function() {
        for (var t, e = this.localeData().eras(), n = 0, i = e.length; n < i; ++n) {
            if (t = this.clone().startOf("day").valueOf(),
            e[n].since <= t && t <= e[n].until)
                return e[n].name;
            if (e[n].until <= t && t <= e[n].since)
                return e[n].name
        }
        return ""
    }
    ,
    s.eraNarrow = function() {
        for (var t, e = this.localeData().eras(), n = 0, i = e.length; n < i; ++n) {
            if (t = this.clone().startOf("day").valueOf(),
            e[n].since <= t && t <= e[n].until)
                return e[n].narrow;
            if (e[n].until <= t && t <= e[n].since)
                return e[n].narrow
        }
        return ""
    }
    ,
    s.eraAbbr = function() {
        for (var t, e = this.localeData().eras(), n = 0, i = e.length; n < i; ++n) {
            if (t = this.clone().startOf("day").valueOf(),
            e[n].since <= t && t <= e[n].until)
                return e[n].abbr;
            if (e[n].until <= t && t <= e[n].since)
                return e[n].abbr
        }
        return ""
    }
    ,
    s.eraYear = function() {
        for (var t, e, n = this.localeData().eras(), i = 0, s = n.length; i < s; ++i)
            if (t = n[i].since <= n[i].until ? 1 : -1,
            e = this.clone().startOf("day").valueOf(),
            n[i].since <= e && e <= n[i].until || n[i].until <= e && e <= n[i].since)
                return (this.year() - p(n[i].since).year()) * t + n[i].offset;
        return this.year()
    }
    ,
    s.year = qt,
    s.isLeapYear = function() {
        return ut(this.year())
    }
    ,
    s.weekYear = function(t) {
        return ln.call(this, t, this.week(), this.weekday(), this.localeData()._week.dow, this.localeData()._week.doy)
    }
    ,
    s.isoWeekYear = function(t) {
        return ln.call(this, t, this.isoWeek(), this.isoWeekday(), 1, 4)
    }
    ,
    s.quarter = s.quarters = function(t) {
        return null == t ? Math.ceil((this.month() + 1) / 3) : this.month(3 * (t - 1) + this.month() % 3)
    }
    ,
    s.month = Ft,
    s.daysInMonth = function() {
        return It(this.year(), this.month())
    }
    ,
    s.week = s.weeks = function(t) {
        var e = this.localeData().week(this);
        return null == t ? e : this.add(7 * (t - e), "d")
    }
    ,
    s.isoWeek = s.isoWeeks = function(t) {
        var e = Gt(this, 1, 4).week;
        return null == t ? e : this.add(7 * (t - e), "d")
    }
    ,
    s.weeksInYear = function() {
        var t = this.localeData()._week;
        return A(this.year(), t.dow, t.doy)
    }
    ,
    s.weeksInWeekYear = function() {
        var t = this.localeData()._week;
        return A(this.weekYear(), t.dow, t.doy)
    }
    ,
    s.isoWeeksInYear = function() {
        return A(this.year(), 1, 4)
    }
    ,
    s.isoWeeksInISOWeekYear = function() {
        return A(this.isoWeekYear(), 1, 4)
    }
    ,
    s.date = _t,
    s.day = s.days = function(t) {
        if (!this.isValid())
            return null != t ? this : NaN;
        var e, n, i = this._isUTC ? this._d.getUTCDay() : this._d.getDay();
        return null != t ? (e = t,
        n = this.localeData(),
        t = "string" != typeof e ? e : isNaN(e) ? "number" == typeof (e = n.weekdaysParse(e)) ? e : null : parseInt(e, 10),
        this.add(t - i, "d")) : i
    }
    ,
    s.weekday = function(t) {
        if (!this.isValid())
            return null != t ? this : NaN;
        var e = (this.day() + 7 - this.localeData()._week.dow) % 7;
        return null == t ? e : this.add(t - e, "d")
    }
    ,
    s.isoWeekday = function(t) {
        return this.isValid() ? null != t ? (e = t,
        n = this.localeData(),
        n = "string" == typeof e ? n.weekdaysParse(e) % 7 || 7 : isNaN(e) ? null : e,
        this.day(this.day() % 7 ? n : n - 7)) : this.day() || 7 : null != t ? this : NaN;
        var e, n
    }
    ,
    s.dayOfYear = function(t) {
        var e = Math.round((this.clone().startOf("day") - this.clone().startOf("year")) / 864e5) + 1;
        return null == t ? e : this.add(t - e, "d")
    }
    ,
    s.hour = s.hours = _,
    s.minute = s.minutes = gt,
    s.second = s.seconds = wt,
    s.millisecond = s.milliseconds = mt,
    s.utcOffset = function(t, e, n) {
        var i, s = this._offset || 0;
        if (!this.isValid())
            return null != t ? this : NaN;
        if (null == t)
            return this._isUTC ? s : We(this);
        if ("string" == typeof t) {
            if (null === (t = He(St, t)))
                return this
        } else
            Math.abs(t) < 16 && !n && (t *= 60);
        return !this._isUTC && e && (i = We(this)),
        this._offset = t,
        this._isUTC = !0,
        null != i && this.add(i, "m"),
        s !== t && (!e || this._changeInProgress ? Ge(this, O(t - s, "m"), 1, !1) : this._changeInProgress || (this._changeInProgress = !0,
        p.updateOffset(this, !0),
        this._changeInProgress = null)),
        this
    }
    ,
    s.utc = function(t) {
        return this.utcOffset(0, t)
    }
    ,
    s.local = function(t) {
        return this._isUTC && (this.utcOffset(0, t),
        this._isUTC = !1,
        t && this.subtract(We(this), "m")),
        this
    }
    ,
    s.parseZone = function() {
        var t;
        return null != this._tzm ? this.utcOffset(this._tzm, !1, !0) : "string" == typeof this._i && (null != (t = He(kt, this._i)) ? this.utcOffset(t) : this.utcOffset(0, !0)),
        this
    }
    ,
    s.hasAlignedHourOffset = function(t) {
        return !!this.isValid() && (t = t ? I(t).utcOffset() : 0,
        (this.utcOffset() - t) % 60 == 0)
    }
    ,
    s.isDST = function() {
        return this.utcOffset() > this.clone().month(0).utcOffset() || this.utcOffset() > this.clone().month(5).utcOffset()
    }
    ,
    s.isLocal = function() {
        return !!this.isValid() && !this._isUTC
    }
    ,
    s.isUtcOffset = function() {
        return !!this.isValid() && this._isUTC
    }
    ,
    s.isUtc = Ye,
    s.isUTC = Ye,
    s.zoneAbbr = function() {
        return this._isUTC ? "UTC" : ""
    }
    ,
    s.zoneName = function() {
        return this._isUTC ? "Coordinated Universal Time" : ""
    }
    ,
    s.dates = t("dates accessor is deprecated. Use date instead.", _t),
    s.months = t("months accessor is deprecated. Use month instead", Ft),
    s.years = t("years accessor is deprecated. Use year instead", qt),
    s.zone = t("moment().zone is deprecated, use moment().utcOffset instead. http://momentjs.com/guides/#/warnings/zone/", function(t, e) {
        return null != t ? (this.utcOffset(t = "string" != typeof t ? -t : t, e),
        this) : -this.utcOffset()
    }),
    s.isDSTShifted = t("isDSTShifted is deprecated. See http://momentjs.com/guides/#/warnings/dst-shifted/ for more information", function() {
        if (!a(this._isDSTShifted))
            return this._isDSTShifted;
        var t, e = {};
        return U(e, this),
        (e = $e(e))._a ? (t = (e._isUTC ? c : I)(e._a),
        this._isDSTShifted = this.isValid() && 0 < function(t, e, n) {
            for (var i = Math.min(t.length, e.length), s = Math.abs(t.length - e.length), r = 0, o = 0; o < i; o++)
                (n && t[o] !== e[o] || !n && y(t[o]) !== y(e[o])) && r++;
            return r + s
        }(e._a, t.toArray())) : this._isDSTShifted = !1,
        this._isDSTShifted
    });
    v = J.prototype;
    function hn(t, e, n, i) {
        var s = fe()
          , i = c().set(i, e);
        return s[n](i, t)
    }
    function pn(t, e, n) {
        if (l(t) && (e = t,
        t = void 0),
        t = t || "",
        null != e)
            return hn(t, e, n, "month");
        for (var i = [], s = 0; s < 12; s++)
            i[s] = hn(t, s, n, "month");
        return i
    }
    function fn(t, e, n, i) {
        e = ("boolean" == typeof t ? l(e) && (n = e,
        e = void 0) : (e = t,
        t = !1,
        l(n = e) && (n = e,
        e = void 0)),
        e || "");
        var s, r = fe(), o = t ? r._week.dow : 0, a = [];
        if (null != n)
            return hn(e, (n + o) % 7, i, "day");
        for (s = 0; s < 7; s++)
            a[s] = hn(e, (s + o) % 7, i, "day");
        return a
    }
    v.calendar = function(t, e, n) {
        return d(t = this._calendar[t] || this._calendar.sameElse) ? t.call(e, n) : t
    }
    ,
    v.longDateFormat = function(t) {
        var e = this._longDateFormat[t]
          , n = this._longDateFormat[t.toUpperCase()];
        return e || !n ? e : (this._longDateFormat[t] = n.match(et).map(function(t) {
            return "MMMM" === t || "MM" === t || "DD" === t || "dddd" === t ? t.slice(1) : t
        }).join(""),
        this._longDateFormat[t])
    }
    ,
    v.invalidDate = function() {
        return this._invalidDate
    }
    ,
    v.ordinal = function(t) {
        return this._ordinal.replace("%d", t)
    }
    ,
    v.preparse = dn,
    v.postformat = dn,
    v.relativeTime = function(t, e, n, i) {
        var s = this._relativeTime[n];
        return d(s) ? s(t, e, n, i) : s.replace(/%d/i, t)
    }
    ,
    v.pastFuture = function(t, e) {
        return d(t = this._relativeTime[0 < t ? "future" : "past"]) ? t(e) : t.replace(/%s/i, e)
    }
    ,
    v.set = function(t) {
        var e, n;
        for (n in t)
            h(t, n) && (d(e = t[n]) ? this[n] = e : this["_" + n] = e);
        this._config = t,
        this._dayOfMonthOrdinalParseLenient = new RegExp((this._dayOfMonthOrdinalParse.source || this._ordinalParse.source) + "|" + /\d{1,2}/.source)
    }
    ,
    v.eras = function(t, e) {
        for (var n, i = this._eras || fe("en")._eras, s = 0, r = i.length; s < r; ++s)
            switch ("string" == typeof i[s].since && (n = p(i[s].since).startOf("day"),
            i[s].since = n.valueOf()),
            typeof i[s].until) {
            case "undefined":
                i[s].until = 1 / 0;
                break;
            case "string":
                n = p(i[s].until).startOf("day").valueOf(),
                i[s].until = n.valueOf()
            }
        return i
    }
    ,
    v.erasParse = function(t, e, n) {
        var i, s, r, o, a, l = this.eras();
        for (t = t.toUpperCase(),
        i = 0,
        s = l.length; i < s; ++i)
            if (r = l[i].name.toUpperCase(),
            o = l[i].abbr.toUpperCase(),
            a = l[i].narrow.toUpperCase(),
            n)
                switch (e) {
                case "N":
                case "NN":
                case "NNN":
                    if (o === t)
                        return l[i];
                    break;
                case "NNNN":
                    if (r === t)
                        return l[i];
                    break;
                case "NNNNN":
                    if (a === t)
                        return l[i]
                }
            else if (0 <= [r, o, a].indexOf(t))
                return l[i]
    }
    ,
    v.erasConvertYear = function(t, e) {
        var n = t.since <= t.until ? 1 : -1;
        return void 0 === e ? p(t.since).year() : p(t.since).year() + (e - t.offset) * n
    }
    ,
    v.erasAbbrRegex = function(t) {
        return h(this, "_erasAbbrRegex") || on.call(this),
        t ? this._erasAbbrRegex : this._erasRegex
    }
    ,
    v.erasNameRegex = function(t) {
        return h(this, "_erasNameRegex") || on.call(this),
        t ? this._erasNameRegex : this._erasRegex
    }
    ,
    v.erasNarrowRegex = function(t) {
        return h(this, "_erasNarrowRegex") || on.call(this),
        t ? this._erasNarrowRegex : this._erasRegex
    }
    ,
    v.months = function(t, e) {
        return t ? (o(this._months) ? this._months : this._months[(this._months.isFormat || Rt).test(e) ? "format" : "standalone"])[t.month()] : o(this._months) ? this._months : this._months.standalone
    }
    ,
    v.monthsShort = function(t, e) {
        return t ? (o(this._monthsShort) ? this._monthsShort : this._monthsShort[Rt.test(e) ? "format" : "standalone"])[t.month()] : o(this._monthsShort) ? this._monthsShort : this._monthsShort.standalone
    }
    ,
    v.monthsParse = function(t, e, n) {
        var i, s;
        if (this._monthsParseExact)
            return function(t, e, n) {
                var i, s, r, t = t.toLocaleLowerCase();
                if (!this._monthsParse)
                    for (this._monthsParse = [],
                    this._longMonthsParse = [],
                    this._shortMonthsParse = [],
                    i = 0; i < 12; ++i)
                        r = c([2e3, i]),
                        this._shortMonthsParse[i] = this.monthsShort(r, "").toLocaleLowerCase(),
                        this._longMonthsParse[i] = this.months(r, "").toLocaleLowerCase();
                return n ? "MMM" === e ? -1 !== (s = k.call(this._shortMonthsParse, t)) ? s : null : -1 !== (s = k.call(this._longMonthsParse, t)) ? s : null : "MMM" === e ? -1 !== (s = k.call(this._shortMonthsParse, t)) || -1 !== (s = k.call(this._longMonthsParse, t)) ? s : null : -1 !== (s = k.call(this._longMonthsParse, t)) || -1 !== (s = k.call(this._shortMonthsParse, t)) ? s : null
            }
            .call(this, t, e, n);
        for (this._monthsParse || (this._monthsParse = [],
        this._longMonthsParse = [],
        this._shortMonthsParse = []),
        i = 0; i < 12; i++) {
            if (s = c([2e3, i]),
            n && !this._longMonthsParse[i] && (this._longMonthsParse[i] = new RegExp("^" + this.months(s, "").replace(".", "") + "$","i"),
            this._shortMonthsParse[i] = new RegExp("^" + this.monthsShort(s, "").replace(".", "") + "$","i")),
            n || this._monthsParse[i] || (s = "^" + this.months(s, "") + "|^" + this.monthsShort(s, ""),
            this._monthsParse[i] = new RegExp(s.replace(".", ""),"i")),
            n && "MMMM" === e && this._longMonthsParse[i].test(t))
                return i;
            if (n && "MMM" === e && this._shortMonthsParse[i].test(t))
                return i;
            if (!n && this._monthsParse[i].test(t))
                return i
        }
    }
    ,
    v.monthsRegex = function(t) {
        return this._monthsParseExact ? (h(this, "_monthsRegex") || Wt.call(this),
        t ? this._monthsStrictRegex : this._monthsRegex) : (h(this, "_monthsRegex") || (this._monthsRegex = jt),
        this._monthsStrictRegex && t ? this._monthsStrictRegex : this._monthsRegex)
    }
    ,
    v.monthsShortRegex = function(t) {
        return this._monthsParseExact ? (h(this, "_monthsRegex") || Wt.call(this),
        t ? this._monthsShortStrictRegex : this._monthsShortRegex) : (h(this, "_monthsShortRegex") || (this._monthsShortRegex = Lt),
        this._monthsShortStrictRegex && t ? this._monthsShortStrictRegex : this._monthsShortRegex)
    }
    ,
    v.week = function(t) {
        return Gt(t, this._week.dow, this._week.doy).week
    }
    ,
    v.firstDayOfYear = function() {
        return this._week.doy
    }
    ,
    v.firstDayOfWeek = function() {
        return this._week.dow
    }
    ,
    v.weekdays = function(t, e) {
        return e = o(this._weekdays) ? this._weekdays : this._weekdays[t && !0 !== t && this._weekdays.isFormat.test(e) ? "format" : "standalone"],
        !0 === t ? Qt(e, this._week.dow) : t ? e[t.day()] : e
    }
    ,
    v.weekdaysMin = function(t) {
        return !0 === t ? Qt(this._weekdaysMin, this._week.dow) : t ? this._weekdaysMin[t.day()] : this._weekdaysMin
    }
    ,
    v.weekdaysShort = function(t) {
        return !0 === t ? Qt(this._weekdaysShort, this._week.dow) : t ? this._weekdaysShort[t.day()] : this._weekdaysShort
    }
    ,
    v.weekdaysParse = function(t, e, n) {
        var i, s;
        if (this._weekdaysParseExact)
            return function(t, e, n) {
                var i, s, r, t = t.toLocaleLowerCase();
                if (!this._weekdaysParse)
                    for (this._weekdaysParse = [],
                    this._shortWeekdaysParse = [],
                    this._minWeekdaysParse = [],
                    i = 0; i < 7; ++i)
                        r = c([2e3, 1]).day(i),
                        this._minWeekdaysParse[i] = this.weekdaysMin(r, "").toLocaleLowerCase(),
                        this._shortWeekdaysParse[i] = this.weekdaysShort(r, "").toLocaleLowerCase(),
                        this._weekdaysParse[i] = this.weekdays(r, "").toLocaleLowerCase();
                return n ? "dddd" === e ? -1 !== (s = k.call(this._weekdaysParse, t)) ? s : null : "ddd" === e ? -1 !== (s = k.call(this._shortWeekdaysParse, t)) ? s : null : -1 !== (s = k.call(this._minWeekdaysParse, t)) ? s : null : "dddd" === e ? -1 !== (s = k.call(this._weekdaysParse, t)) || -1 !== (s = k.call(this._shortWeekdaysParse, t)) || -1 !== (s = k.call(this._minWeekdaysParse, t)) ? s : null : "ddd" === e ? -1 !== (s = k.call(this._shortWeekdaysParse, t)) || -1 !== (s = k.call(this._weekdaysParse, t)) || -1 !== (s = k.call(this._minWeekdaysParse, t)) ? s : null : -1 !== (s = k.call(this._minWeekdaysParse, t)) || -1 !== (s = k.call(this._weekdaysParse, t)) || -1 !== (s = k.call(this._shortWeekdaysParse, t)) ? s : null
            }
            .call(this, t, e, n);
        for (this._weekdaysParse || (this._weekdaysParse = [],
        this._minWeekdaysParse = [],
        this._shortWeekdaysParse = [],
        this._fullWeekdaysParse = []),
        i = 0; i < 7; i++) {
            if (s = c([2e3, 1]).day(i),
            n && !this._fullWeekdaysParse[i] && (this._fullWeekdaysParse[i] = new RegExp("^" + this.weekdays(s, "").replace(".", "\\.?") + "$","i"),
            this._shortWeekdaysParse[i] = new RegExp("^" + this.weekdaysShort(s, "").replace(".", "\\.?") + "$","i"),
            this._minWeekdaysParse[i] = new RegExp("^" + this.weekdaysMin(s, "").replace(".", "\\.?") + "$","i")),
            this._weekdaysParse[i] || (s = "^" + this.weekdays(s, "") + "|^" + this.weekdaysShort(s, "") + "|^" + this.weekdaysMin(s, ""),
            this._weekdaysParse[i] = new RegExp(s.replace(".", ""),"i")),
            n && "dddd" === e && this._fullWeekdaysParse[i].test(t))
                return i;
            if (n && "ddd" === e && this._shortWeekdaysParse[i].test(t))
                return i;
            if (n && "dd" === e && this._minWeekdaysParse[i].test(t))
                return i;
            if (!n && this._weekdaysParse[i].test(t))
                return i
        }
    }
    ,
    v.weekdaysRegex = function(t) {
        return this._weekdaysParseExact ? (h(this, "_weekdaysRegex") || ne.call(this),
        t ? this._weekdaysStrictRegex : this._weekdaysRegex) : (h(this, "_weekdaysRegex") || (this._weekdaysRegex = Jt),
        this._weekdaysStrictRegex && t ? this._weekdaysStrictRegex : this._weekdaysRegex)
    }
    ,
    v.weekdaysShortRegex = function(t) {
        return this._weekdaysParseExact ? (h(this, "_weekdaysRegex") || ne.call(this),
        t ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex) : (h(this, "_weekdaysShortRegex") || (this._weekdaysShortRegex = te),
        this._weekdaysShortStrictRegex && t ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex)
    }
    ,
    v.weekdaysMinRegex = function(t) {
        return this._weekdaysParseExact ? (h(this, "_weekdaysRegex") || ne.call(this),
        t ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex) : (h(this, "_weekdaysMinRegex") || (this._weekdaysMinRegex = ee),
        this._weekdaysMinStrictRegex && t ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex)
    }
    ,
    v.isPM = function(t) {
        return "p" === (t + "").toLowerCase().charAt(0)
    }
    ,
    v.meridiem = function(t, e, n) {
        return 11 < t ? n ? "pm" : "PM" : n ? "am" : "AM"
    }
    ,
    he("en", {
        eras: [{
            since: "0001-01-01",
            until: 1 / 0,
            offset: 1,
            name: "Anno Domini",
            narrow: "AD",
            abbr: "AD"
        }, {
            since: "0000-12-31",
            until: -1 / 0,
            offset: 1,
            name: "Before Christ",
            narrow: "BC",
            abbr: "BC"
        }],
        dayOfMonthOrdinalParse: /\d{1,2}(th|st|nd|rd)/,
        ordinal: function(t) {
            var e = t % 10;
            return t + (1 === y(t % 100 / 10) ? "th" : 1 == e ? "st" : 2 == e ? "nd" : 3 == e ? "rd" : "th")
        }
    }),
    p.lang = t("moment.lang is deprecated. Use moment.locale instead.", he),
    p.langData = t("moment.langData is deprecated. Use moment.localeData instead.", fe);
    var gn = Math.abs;
    function mn(t, e, n, i) {
        e = O(e, n);
        return t._milliseconds += i * e._milliseconds,
        t._days += i * e._days,
        t._months += i * e._months,
        t._bubble()
    }
    function yn(t) {
        return t < 0 ? Math.floor(t) : Math.ceil(t)
    }
    function vn(t) {
        return 4800 * t / 146097
    }
    function bn(t) {
        return 146097 * t / 4800
    }
    function _n(t) {
        return function() {
            return this.as(t)
        }
    }
    bt = _n("ms"),
    ft = _n("s"),
    Ot = _n("m"),
    vt = _n("h"),
    yt = _n("d"),
    Kt = _n("w"),
    _ = _n("M"),
    gt = _n("Q"),
    wt = _n("y");
    function wn(t) {
        return function() {
            return this.isValid() ? this._data[t] : NaN
        }
    }
    var mt = wn("milliseconds")
      , _t = wn("seconds")
      , qt = wn("minutes")
      , v = wn("hours")
      , xn = wn("days")
      , Cn = wn("months")
      , kn = wn("years");
    var Sn = Math.round
      , Tn = {
        ss: 44,
        s: 45,
        m: 45,
        h: 22,
        d: 26,
        w: null,
        M: 11
    };
    function Dn(t, e, n, i) {
        var s = O(t).abs()
          , r = Sn(s.as("s"))
          , o = Sn(s.as("m"))
          , a = Sn(s.as("h"))
          , l = Sn(s.as("d"))
          , c = Sn(s.as("M"))
          , u = Sn(s.as("w"))
          , s = Sn(s.as("y"))
          , r = (r <= n.ss ? ["s", r] : r < n.s && ["ss", r]) || o <= 1 && ["m"] || o < n.m && ["mm", o] || a <= 1 && ["h"] || a < n.h && ["hh", a] || l <= 1 && ["d"] || l < n.d && ["dd", l];
        return (r = (r = null != n.w ? r || u <= 1 && ["w"] || u < n.w && ["ww", u] : r) || c <= 1 && ["M"] || c < n.M && ["MM", c] || s <= 1 && ["y"] || ["yy", s])[2] = e,
        r[3] = 0 < +t,
        r[4] = i,
        function(t, e, n, i, s) {
            return s.relativeTime(e || 1, !!n, t, i)
        }
        .apply(null, r)
    }
    var En = Math.abs;
    function Nn(t) {
        return (0 < t) - (t < 0) || +t
    }
    function $n() {
        if (!this.isValid())
            return this.localeData().invalidDate();
        var t, e, n, i, s, r, o, a = En(this._milliseconds) / 1e3, l = En(this._days), c = En(this._months), u = this.asSeconds();
        return u ? (t = m(a / 60),
        e = m(t / 60),
        a %= 60,
        t %= 60,
        n = m(c / 12),
        c %= 12,
        i = a ? a.toFixed(3).replace(/\.?0+$/, "") : "",
        s = Nn(this._months) !== Nn(u) ? "-" : "",
        r = Nn(this._days) !== Nn(u) ? "-" : "",
        o = Nn(this._milliseconds) !== Nn(u) ? "-" : "",
        (u < 0 ? "-" : "") + "P" + (n ? s + n + "Y" : "") + (c ? s + c + "M" : "") + (l ? r + l + "D" : "") + (e || t || a ? "T" : "") + (e ? o + e + "H" : "") + (t ? o + t + "M" : "") + (a ? o + i + "S" : "")) : "P0D"
    }
    var P = Oe.prototype;
    return P.isValid = function() {
        return this._isValid
    }
    ,
    P.abs = function() {
        var t = this._data;
        return this._milliseconds = gn(this._milliseconds),
        this._days = gn(this._days),
        this._months = gn(this._months),
        t.milliseconds = gn(t.milliseconds),
        t.seconds = gn(t.seconds),
        t.minutes = gn(t.minutes),
        t.hours = gn(t.hours),
        t.months = gn(t.months),
        t.years = gn(t.years),
        this
    }
    ,
    P.add = function(t, e) {
        return mn(this, t, e, 1)
    }
    ,
    P.subtract = function(t, e) {
        return mn(this, t, e, -1)
    }
    ,
    P.as = function(t) {
        if (!this.isValid())
            return NaN;
        var e, n, i = this._milliseconds;
        if ("month" === (t = g(t)) || "quarter" === t || "year" === t)
            switch (e = this._days + i / 864e5,
            n = this._months + vn(e),
            t) {
            case "month":
                return n;
            case "quarter":
                return n / 3;
            case "year":
                return n / 12
            }
        else
            switch (e = this._days + Math.round(bn(this._months)),
            t) {
            case "week":
                return e / 7 + i / 6048e5;
            case "day":
                return e + i / 864e5;
            case "hour":
                return 24 * e + i / 36e5;
            case "minute":
                return 1440 * e + i / 6e4;
            case "second":
                return 86400 * e + i / 1e3;
            case "millisecond":
                return Math.floor(864e5 * e) + i;
            default:
                throw new Error("Unknown unit " + t)
            }
    }
    ,
    P.asMilliseconds = bt,
    P.asSeconds = ft,
    P.asMinutes = Ot,
    P.asHours = vt,
    P.asDays = yt,
    P.asWeeks = Kt,
    P.asMonths = _,
    P.asQuarters = gt,
    P.asYears = wt,
    P.valueOf = function() {
        return this.isValid() ? this._milliseconds + 864e5 * this._days + this._months % 12 * 2592e6 + 31536e6 * y(this._months / 12) : NaN
    }
    ,
    P._bubble = function() {
        var t = this._milliseconds
          , e = this._days
          , n = this._months
          , i = this._data;
        return 0 <= t && 0 <= e && 0 <= n || t <= 0 && e <= 0 && n <= 0 || (t += 864e5 * yn(bn(n) + e),
        n = e = 0),
        i.milliseconds = t % 1e3,
        t = m(t / 1e3),
        i.seconds = t % 60,
        t = m(t / 60),
        i.minutes = t % 60,
        t = m(t / 60),
        i.hours = t % 24,
        e += m(t / 24),
        n += t = m(vn(e)),
        e -= yn(bn(t)),
        t = m(n / 12),
        n %= 12,
        i.days = e,
        i.months = n,
        i.years = t,
        this
    }
    ,
    P.clone = function() {
        return O(this)
    }
    ,
    P.get = function(t) {
        return t = g(t),
        this.isValid() ? this[t + "s"]() : NaN
    }
    ,
    P.milliseconds = mt,
    P.seconds = _t,
    P.minutes = qt,
    P.hours = v,
    P.days = xn,
    P.weeks = function() {
        return m(this.days() / 7)
    }
    ,
    P.months = Cn,
    P.years = kn,
    P.humanize = function(t, e) {
        if (!this.isValid())
            return this.localeData().invalidDate();
        var n = !1
          , i = Tn;
        return "object" == typeof t && (e = t,
        t = !1),
        "boolean" == typeof t && (n = t),
        "object" == typeof e && (i = Object.assign({}, Tn, e),
        null != e.s && null == e.ss && (i.ss = e.s - 1)),
        t = this.localeData(),
        e = Dn(this, !n, i, t),
        n && (e = t.pastFuture(+this, e)),
        t.postformat(e)
    }
    ,
    P.toISOString = $n,
    P.toString = $n,
    P.toJSON = $n,
    P.locale = Ze,
    P.localeData = Je,
    P.toIsoString = t("toIsoString() is deprecated. Please use toISOString() instead (notice the capitals)", $n),
    P.lang = Zt,
    i("X", 0, 0, "unix"),
    i("x", 0, 0, "valueOf"),
    w("x", Ct),
    w("X", /[+-]?\d+(\.\d{1,3})?/),
    C("X", function(t, e, n) {
        n._d = new Date(1e3 * parseFloat(t))
    }),
    C("x", function(t, e, n) {
        n._d = new Date(y(t))
    }),
    p.version = "2.29.3",
    R = I,
    p.fn = s,
    p.min = function() {
        return Me("isBefore", [].slice.call(arguments, 0))
    }
    ,
    p.max = function() {
        return Me("isAfter", [].slice.call(arguments, 0))
    }
    ,
    p.now = function() {
        return Date.now ? Date.now() : +new Date
    }
    ,
    p.utc = c,
    p.unix = function(t) {
        return I(1e3 * t)
    }
    ,
    p.months = function(t, e) {
        return pn(t, e, "months")
    }
    ,
    p.isDate = H,
    p.locale = he,
    p.invalid = q,
    p.duration = O,
    p.isMoment = u,
    p.weekdays = function(t, e, n) {
        return fn(t, e, n, "weekdays")
    }
    ,
    p.parseZone = function() {
        return I.apply(null, arguments).parseZone()
    }
    ,
    p.localeData = fe,
    p.isDuration = Pe,
    p.monthsShort = function(t, e) {
        return pn(t, e, "monthsShort")
    }
    ,
    p.weekdaysMin = function(t, e, n) {
        return fn(t, e, n, "weekdaysMin")
    }
    ,
    p.defineLocale = pe,
    p.updateLocale = function(t, e) {
        var n, i;
        return null != e ? (i = ae,
        null != M[t] && null != M[t].parentLocale ? M[t].set(Z(M[t]._config, e)) : (e = Z(i = null != (n = de(t)) ? n._config : i, e),
        null == n && (e.abbr = t),
        (i = new J(e)).parentLocale = M[t],
        M[t] = i),
        he(t)) : null != M[t] && (null != M[t].parentLocale ? (M[t] = M[t].parentLocale,
        t === he() && he(t)) : null != M[t] && delete M[t]),
        M[t]
    }
    ,
    p.locales = function() {
        return tt(M)
    }
    ,
    p.weekdaysShort = function(t, e, n) {
        return fn(t, e, n, "weekdaysShort")
    }
    ,
    p.normalizeUnits = g,
    p.relativeTimeRounding = function(t) {
        return void 0 === t ? Sn : "function" == typeof t && (Sn = t,
        !0)
    }
    ,
    p.relativeTimeThreshold = function(t, e) {
        return void 0 !== Tn[t] && (void 0 === e ? Tn[t] : (Tn[t] = e,
        "s" === t && (Tn.ss = e - 1),
        !0))
    }
    ,
    p.calendarFormat = function(t, e) {
        return (t = t.diff(e, "days", !0)) < -6 ? "sameElse" : t < -1 ? "lastWeek" : t < 0 ? "lastDay" : t < 1 ? "sameDay" : t < 2 ? "nextDay" : t < 7 ? "nextWeek" : "sameElse"
    }
    ,
    p.prototype = s,
    p.HTML5_FMT = {
        DATETIME_LOCAL: "YYYY-MM-DDTHH:mm",
        DATETIME_LOCAL_SECONDS: "YYYY-MM-DDTHH:mm:ss",
        DATETIME_LOCAL_MS: "YYYY-MM-DDTHH:mm:ss.SSS",
        DATE: "YYYY-MM-DD",
        TIME: "HH:mm",
        TIME_SECONDS: "HH:mm:ss",
        TIME_MS: "HH:mm:ss.SSS",
        WEEK: "GGGG-[W]WW",
        MONTH: "YYYY-MM"
    },
    p
}),
function(t, e) {
    "use strict";
    var n;
    if ("object" == typeof exports) {
        try {
            n = require("moment")
        } catch (t) {}
        module.exports = e(n)
    } else
        "function" == typeof define && define.amd ? define(function(t) {
            try {
                n = t("moment")
            } catch (t) {}
            return e(n)
        }) : t.Pikaday = e(t.moment)
}(this, function($) {
    "use strict";
    function s(t, e, n, i) {
        l ? t.addEventListener(e, n, !!i) : t.attachEvent("on" + e, n)
    }
    function e(t, e, n, i) {
        l ? t.removeEventListener(e, n, !!i) : t.detachEvent("on" + e, n)
    }
    function r(t, e) {
        return -1 !== (" " + t.className + " ").indexOf(" " + e + " ")
    }
    function h(t, e) {
        r(t, e) || (t.className = "" === t.className ? e : t.className + " " + e)
    }
    function p(t, e) {
        t.className = (t = (" " + t.className + " ").replace(" " + e + " ", " ")).trim ? t.trim() : t.replace(/^\s+|\s+$/g, "")
    }
    function m(t) {
        return /Array/.test(Object.prototype.toString.call(t))
    }
    function A(t) {
        return /Date/.test(Object.prototype.toString.call(t)) && !isNaN(t.getTime())
    }
    function M(t, e) {
        return [31, (t = t) % 4 == 0 && t % 100 != 0 || t % 400 == 0 ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][e]
    }
    function I(t) {
        A(t) && t.setHours(0, 0, 0, 0)
    }
    function O(t, e) {
        return t.getTime() === e.getTime()
    }
    function o(t, e, n) {
        var i, s;
        for (i in e)
            (s = void 0 !== t[i]) && "object" == typeof e[i] && null !== e[i] && void 0 === e[i].nodeName ? A(e[i]) ? n && (t[i] = new Date(e[i].getTime())) : m(e[i]) ? n && (t[i] = e[i].slice(0)) : t[i] = o({}, e[i], n) : !n && s || (t[i] = e[i]);
        return t
    }
    function a(t, e, n) {
        var i;
        f.createEvent ? ((i = f.createEvent("HTMLEvents")).initEvent(e, !0, !1),
        i = o(i, n),
        t.dispatchEvent(i)) : f.createEventObject && (i = f.createEventObject(),
        i = o(i, n),
        t.fireEvent("on" + e, i))
    }
    function n(t) {
        return t.month < 0 && (t.year -= Math.ceil(Math.abs(t.month) / 12),
        t.month += 12),
        11 < t.month && (t.year += Math.floor(Math.abs(t.month) / 12),
        t.month -= 12),
        t
    }
    function P(t, e) {
        function n(t) {
            return (t + 7 - 1) % 7
        }
        t.setHours(0, 0, 0, 0);
        var i = t.getDate()
          , s = t.getDay()
          , r = e - 1
          , i = (t.setDate(i + r - n(s)),
        new Date(t.getFullYear(),0,e))
          , s = (t.getTime() - i.getTime()) / 864e5;
        return 1 + Math.round((s - r + n(i.getDay())) / 7)
    }
    function R(t, e, n) {
        return '<table cellpadding="0" cellspacing="0" class="pika-table" role="grid" aria-labelledby="' + n + '">' + function(t) {
            var e, n = [];
            for (t.showWeekNumber && n.push("<th></th>"),
            e = 0; e < 7; e++)
                n.push('<th scope="col"><abbr title="' + u(t, e) + '">' + u(t, e, !0) + "</abbr></th>");
            return "<thead><tr>" + (t.isRTL ? n.reverse() : n).join("") + "</tr></thead>"
        }(t) + "<tbody>" + e.join("") + "</tbody></table>"
    }
    function t(t) {
        var n = this
          , i = n.config(t)
          , t = (n._onMouseDown = function(t) {
            if (n._v) {
                var e = (t = t || window.event).target || t.srcElement;
                if (e)
                    if (r(e, "is-disabled") || (!r(e, "pika-button") || r(e, "is-empty") || r(e.parentNode, "is-disabled") ? r(e, "pika-prev") ? n.prevMonth() : r(e, "pika-next") && n.nextMonth() : (n.setDate(new Date(e.getAttribute("data-pika-year"),e.getAttribute("data-pika-month"),e.getAttribute("data-pika-day"))),
                    i.bound && c(function() {
                        n.hide(),
                        i.blurFieldOnSelect && i.field && i.field.blur()
                    }, 100))),
                    r(e, "pika-select"))
                        n._c = !0;
                    else {
                        if (!t.preventDefault)
                            return t.returnValue = !1;
                        t.preventDefault()
                    }
            }
        }
        ,
        n._onChange = function(t) {
            t = (t = t || window.event).target || t.srcElement;
            t && (r(t, "pika-select-month") ? n.gotoMonth(t.value) : r(t, "pika-select-year") && n.gotoYear(t.value))
        }
        ,
        n._onKeyChange = function(t) {
            if (t = t || window.event,
            n.isVisible())
                switch (t.keyCode) {
                case 13:
                case 27:
                    i.field && i.field.blur();
                    break;
                case 37:
                    n.adjustDate("subtract", 1);
                    break;
                case 38:
                    n.adjustDate("subtract", 7);
                    break;
                case 39:
                    n.adjustDate("add", 1);
                    break;
                case 40:
                    n.adjustDate("add", 7);
                    break;
                case 8:
                case 46:
                    n.setDate(null)
                }
        }
        ,
        n._parseFieldValue = function() {
            var t;
            return i.parse ? i.parse(i.field.value, i.format) : L ? (t = $(i.field.value, i.format, i.formatStrict)) && t.isValid() ? t.toDate() : null : new Date(Date.parse(i.field.value))
        }
        ,
        n._onInputChange = function(t) {
            t.firedBy !== n && (t = n._parseFieldValue(),
            A(t) && n.setDate(t),
            n._v || n.show())
        }
        ,
        n._onInputFocus = function() {
            n.show()
        }
        ,
        n._onInputClick = function() {
            n.show()
        }
        ,
        n._onInputBlur = function() {
            var t = f.activeElement;
            do {
                if (r(t, "pika-single"))
                    return
            } while (t = t.parentNode);
            n._c || (n._b = c(function() {
                n.hide()
            }, 50)),
            n._c = !1
        }
        ,
        n._onClick = function(t) {
            var t = (t = t || window.event).target || t.srcElement
              , e = t;
            if (t) {
                l || !r(t, "pika-select") || t.onchange || (t.setAttribute("onchange", "return;"),
                s(t, "change", n._onChange));
                do {
                    if (r(e, "pika-single") || e === i.trigger)
                        return
                } while (e = e.parentNode);
                n._v && t !== i.trigger && e !== i.trigger && n.hide()
            }
        }
        ,
        n.el = f.createElement("div"),
        n.el.className = "pika-single" + (i.isRTL ? " is-rtl" : "") + (i.theme ? " " + i.theme : ""),
        s(n.el, "mousedown", n._onMouseDown, !0),
        s(n.el, "touchend", n._onMouseDown, !0),
        s(n.el, "change", n._onChange),
        i.keyboardInput && s(f, "keydown", n._onKeyChange),
        i.field && (i.container ? i.container.appendChild(n.el) : i.bound ? f.body.appendChild(n.el) : i.field.parentNode.insertBefore(n.el, i.field.nextSibling),
        s(i.field, "change", n._onInputChange),
        i.defaultDate || (i.defaultDate = n._parseFieldValue(),
        i.setDefaultDate = !0)),
        i.defaultDate);
        A(t) ? i.setDefaultDate ? n.setDate(t, !0) : n.gotoDate(t) : n.gotoDate(new Date),
        i.bound ? (this.hide(),
        n.el.className += " is-bound",
        s(i.trigger, "click", n._onInputClick),
        s(i.trigger, "focus", n._onInputFocus),
        s(i.trigger, "blur", n._onInputBlur)) : this.show()
    }
    var L = "function" == typeof $
      , l = !!window.addEventListener
      , f = window.document
      , c = window.setTimeout
      , i = {
        field: null,
        bound: void 0,
        ariaLabel: "Use the arrow keys to pick a date",
        position: "bottom left",
        reposition: !0,
        format: "YYYY-MM-DD",
        toString: null,
        parse: null,
        defaultDate: null,
        setDefaultDate: !1,
        firstDay: 0,
        firstWeekOfYearMinDays: 4,
        formatStrict: !1,
        minDate: null,
        maxDate: null,
        yearRange: 10,
        showWeekNumber: !1,
        pickWholeWeek: !1,
        minYear: 0,
        maxYear: 9999,
        minMonth: void 0,
        maxMonth: void 0,
        startRange: null,
        endRange: null,
        isRTL: !1,
        yearSuffix: "",
        showMonthAfterYear: !1,
        showDaysInNextAndPreviousMonths: !1,
        enableSelectionDaysInNextAndPreviousMonths: !1,
        numberOfMonths: 1,
        mainCalendar: "left",
        container: void 0,
        blurFieldOnSelect: !0,
        i18n: {
            previousMonth: "Previous Month",
            nextMonth: "Next Month",
            months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
            weekdays: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
            weekdaysShort: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
        },
        theme: null,
        events: [],
        onSelect: null,
        onOpen: null,
        onClose: null,
        onDraw: null,
        keyboardInput: !0
    }
      , u = function(t, e, n) {
        for (e += t.firstDay; 7 <= e; )
            e -= 7;
        return (n ? t.i18n.weekdaysShort : t.i18n.weekdays)[e]
    };
    return t.prototype = {
        config: function(t) {
            this._o || (this._o = o({}, i, !0));
            var t = o(this._o, t, !0)
              , e = (t.isRTL = !!t.isRTL,
            t.field = t.field && t.field.nodeName ? t.field : null,
            t.theme = "string" == typeof t.theme && t.theme ? t.theme : null,
            t.bound = !!(void 0 !== t.bound ? t.field && t.bound : t.field),
            t.trigger = t.trigger && t.trigger.nodeName ? t.trigger : t.field,
            t.disableWeekends = !!t.disableWeekends,
            t.disableDayFn = "function" == typeof t.disableDayFn ? t.disableDayFn : null,
            parseInt(t.numberOfMonths, 10) || 1);
            return t.numberOfMonths = 4 < e ? 4 : e,
            A(t.minDate) || (t.minDate = !1),
            A(t.maxDate) || (t.maxDate = !1),
            t.minDate && t.maxDate && t.maxDate < t.minDate && (t.maxDate = t.minDate = !1),
            t.minDate && this.setMinDate(t.minDate),
            t.maxDate && this.setMaxDate(t.maxDate),
            m(t.yearRange) ? (e = (new Date).getFullYear() - 10,
            t.yearRange[0] = parseInt(t.yearRange[0], 10) || e,
            t.yearRange[1] = parseInt(t.yearRange[1], 10) || e) : (t.yearRange = Math.abs(parseInt(t.yearRange, 10)) || i.yearRange,
            100 < t.yearRange && (t.yearRange = 100)),
            t
        },
        toString: function(t) {
            return t = t || this._o.format,
            A(this._d) ? this._o.toString ? this._o.toString(this._d, t) : L ? $(this._d).format(t) : this._d.toDateString() : ""
        },
        getMoment: function() {
            return L ? $(this._d) : null
        },
        setMoment: function(t, e) {
            L && $.isMoment(t) && this.setDate(t.toDate(), e)
        },
        getDate: function() {
            return A(this._d) ? new Date(this._d.getTime()) : null
        },
        setDate: function(t, e) {
            if (!t)
                return this._d = null,
                this._o.field && (this._o.field.value = "",
                a(this._o.field, "change", {
                    firedBy: this
                })),
                this.draw();
            var n, i;
            "string" == typeof t && (t = new Date(Date.parse(t))),
            A(t) && (n = this._o.minDate,
            i = this._o.maxDate,
            A(n) && t < n ? t = n : A(i) && i < t && (t = i),
            this._d = new Date(t.getTime()),
            I(this._d),
            this.gotoDate(this._d),
            this._o.field && (this._o.field.value = this.toString(),
            a(this._o.field, "change", {
                firedBy: this
            })),
            e || "function" != typeof this._o.onSelect || this._o.onSelect.call(this, this.getDate()))
        },
        clear: function() {
            this.setDate(null)
        },
        gotoDate: function(t) {
            var e, n, i, s = !0;
            A(t) && (this.calendars && (e = new Date(this.calendars[0].year,this.calendars[0].month,1),
            n = new Date(this.calendars[this.calendars.length - 1].year,this.calendars[this.calendars.length - 1].month,1),
            i = t.getTime(),
            n.setMonth(n.getMonth() + 1),
            n.setDate(n.getDate() - 1),
            s = i < e.getTime() || n.getTime() < i),
            s && (this.calendars = [{
                month: t.getMonth(),
                year: t.getFullYear()
            }],
            "right" === this._o.mainCalendar && (this.calendars[0].month += 1 - this._o.numberOfMonths)),
            this.adjustCalendars())
        },
        adjustDate: function(t, e) {
            var n, i = this.getDate() || new Date, e = 24 * parseInt(e) * 60 * 60 * 1e3;
            "add" === t ? n = new Date(i.valueOf() + e) : "subtract" === t && (n = new Date(i.valueOf() - e)),
            this.setDate(n)
        },
        adjustCalendars: function() {
            this.calendars[0] = n(this.calendars[0]);
            for (var t = 1; t < this._o.numberOfMonths; t++)
                this.calendars[t] = n({
                    month: this.calendars[0].month + t,
                    year: this.calendars[0].year
                });
            this.draw()
        },
        gotoToday: function() {
            this.gotoDate(new Date)
        },
        gotoMonth: function(t) {
            isNaN(t) || (this.calendars[0].month = parseInt(t, 10),
            this.adjustCalendars())
        },
        nextMonth: function() {
            this.calendars[0].month++,
            this.adjustCalendars()
        },
        prevMonth: function() {
            this.calendars[0].month--,
            this.adjustCalendars()
        },
        gotoYear: function(t) {
            isNaN(t) || (this.calendars[0].year = parseInt(t, 10),
            this.adjustCalendars())
        },
        setMinDate: function(t) {
            t instanceof Date ? (I(t),
            this._o.minDate = t,
            this._o.minYear = t.getFullYear(),
            this._o.minMonth = t.getMonth()) : (this._o.minDate = i.minDate,
            this._o.minYear = i.minYear,
            this._o.minMonth = i.minMonth,
            this._o.startRange = i.startRange),
            this.draw()
        },
        setMaxDate: function(t) {
            t instanceof Date ? (I(t),
            this._o.maxDate = t,
            this._o.maxYear = t.getFullYear(),
            this._o.maxMonth = t.getMonth()) : (this._o.maxDate = i.maxDate,
            this._o.maxYear = i.maxYear,
            this._o.maxMonth = i.maxMonth,
            this._o.endRange = i.endRange),
            this.draw()
        },
        setStartRange: function(t) {
            this._o.startRange = t
        },
        setEndRange: function(t) {
            this._o.endRange = t
        },
        draw: function(t) {
            if (this._v || t) {
                var e, n = this._o, t = n.minYear, i = n.maxYear, s = n.minMonth, r = n.maxMonth, o = "";
                this._y <= t && (this._y = t,
                !isNaN(s) && this._m < s && (this._m = s)),
                this._y >= i && (this._y = i,
                !isNaN(r) && this._m > r && (this._m = r));
                for (var a = 0; a < n.numberOfMonths; a++)
                    e = "pika-title-" + Math.random().toString(36).replace(/[^a-z]+/g, "").substr(0, 2),
                    o += '<div class="pika-lendar">' + function(t, e, n, i, s, r) {
                        for (var o, a, l, c = t._o, u = n === c.minYear, d = n === c.maxYear, r = '<div id="' + r + '" class="pika-title" role="heading" aria-live="assertive">', h = !0, p = !0, f = [], g = 0; g < 12; g++)
                            f.push('<option value="' + (n === s ? g - e : 12 + g - e) + '"' + (g === i ? ' selected="selected"' : "") + (u && g < c.minMonth || d && g > c.maxMonth ? ' disabled="disabled"' : "") + ">" + c.i18n.months[g] + "</option>");
                        for (a = '<div class="pika-label">' + c.i18n.months[i] + '<select class="pika-select pika-select-month" tabindex="-1">' + f.join("") + "</select></div>",
                        o = m(c.yearRange) ? (g = c.yearRange[0],
                        c.yearRange[1] + 1) : (g = n - c.yearRange,
                        1 + n + c.yearRange),
                        f = []; g < o && g <= c.maxYear; g++)
                            g >= c.minYear && f.push('<option value="' + g + '"' + (g === n ? ' selected="selected"' : "") + ">" + g + "</option>");
                        return l = '<div class="pika-label">' + n + c.yearSuffix + '<select class="pika-select pika-select-year" tabindex="-1">' + f.join("") + "</select></div>",
                        c.showMonthAfterYear ? r += l + a : r += a + l,
                        u && (0 === i || c.minMonth >= i) && (h = !1),
                        d && (11 === i || c.maxMonth <= i) && (p = !1),
                        0 === e && (r += '<button class="pika-prev' + (h ? "" : " is-disabled") + '" type="button">' + c.i18n.previousMonth + "</button>"),
                        e === t._o.numberOfMonths - 1 && (r += '<button class="pika-next' + (p ? "" : " is-disabled") + '" type="button">' + c.i18n.nextMonth + "</button>"),
                        r + "</div>"
                    }(this, a, this.calendars[a].year, this.calendars[a].month, this.calendars[0].year, e) + this.render(this.calendars[a].year, this.calendars[a].month, e) + "</div>";
                this.el.innerHTML = o,
                n.bound && "hidden" !== n.field.type && c(function() {
                    n.trigger.focus()
                }, 1),
                "function" == typeof this._o.onDraw && this._o.onDraw(this),
                n.bound && n.field.setAttribute("aria-label", n.ariaLabel)
            }
        },
        adjustPosition: function() {
            var t, e, n, i, s, r, o, a, l, c, u, d;
            if (!this._o.container) {
                if (this.el.style.position = "absolute",
                e = t = this._o.trigger,
                n = this.el.offsetWidth,
                i = this.el.offsetHeight,
                s = window.innerWidth || f.documentElement.clientWidth,
                r = window.innerHeight || f.documentElement.clientHeight,
                o = window.pageYOffset || f.body.scrollTop || f.documentElement.scrollTop,
                d = u = !0,
                "function" == typeof t.getBoundingClientRect)
                    a = (c = t.getBoundingClientRect()).left + window.pageXOffset,
                    l = c.bottom + window.pageYOffset;
                else
                    for (a = e.offsetLeft,
                    l = e.offsetTop + e.offsetHeight; e = e.offsetParent; )
                        a += e.offsetLeft,
                        l += e.offsetTop;
                (this._o.reposition && s < a + n || -1 < this._o.position.indexOf("right") && 0 < a - n + t.offsetWidth) && (a = a - n + t.offsetWidth,
                u = !1),
                (this._o.reposition && r + o < l + i || -1 < this._o.position.indexOf("top") && 0 < l - i - t.offsetHeight) && (l = l - i - t.offsetHeight,
                d = !1),
                this.el.style.left = a + "px",
                this.el.style.top = l + "px",
                h(this.el, u ? "left-aligned" : "right-aligned"),
                h(this.el, d ? "bottom-aligned" : "top-aligned"),
                p(this.el, u ? "right-aligned" : "left-aligned"),
                p(this.el, d ? "top-aligned" : "bottom-aligned")
            }
        },
        render: function(t, e, n) {
            for (var i = this._o, s = new Date, r = M(t, e), o = new Date(t,e,1).getDay(), a = [], l = [], c = (I(s),
            0 < i.firstDay && (o -= i.firstDay) < 0 && (o += 7),
            0 === e ? 11 : e - 1), u = 11 === e ? 0 : e + 1, d = 0 === e ? t - 1 : t, h = 11 === e ? t + 1 : t, p = M(d, c), f = r + o, g = f; 7 < g; )
                g -= 7;
            f += 7 - g;
            for (var m = !1, y = 0, v = 0; y < f; y++) {
                var b = new Date(t,e,y - o + 1)
                  , _ = !!A(this._d) && O(b, this._d)
                  , w = O(b, s)
                  , x = -1 !== i.events.indexOf(b.toDateString())
                  , C = y < o || r + o <= y
                  , k = y - o + 1
                  , S = e
                  , T = t
                  , D = i.startRange && O(i.startRange, b)
                  , E = i.endRange && O(i.endRange, b)
                  , N = i.startRange && i.endRange && i.startRange < b && b < i.endRange
                  , k = (C && (T = y < o ? (k = p + k,
                S = c,
                d) : (k -= r,
                S = u,
                h)),
                {
                    day: k,
                    month: S,
                    year: T,
                    hasEvent: x,
                    isSelected: _,
                    isToday: w,
                    isDisabled: i.minDate && b < i.minDate || i.maxDate && b > i.maxDate || i.disableWeekends && function(t) {
                        t = t.getDay();
                        return 0 === t || 6 === t
                    }(b) || i.disableDayFn && i.disableDayFn(b),
                    isEmpty: C,
                    isStartRange: D,
                    isEndRange: E,
                    isInRange: N,
                    showDaysInNextAndPreviousMonths: i.showDaysInNextAndPreviousMonths,
                    enableSelectionDaysInNextAndPreviousMonths: i.enableSelectionDaysInNextAndPreviousMonths
                });
                i.pickWholeWeek && _ && (m = !0),
                l.push(function(t) {
                    var e = []
                      , n = "false";
                    if (t.isEmpty) {
                        if (!t.showDaysInNextAndPreviousMonths)
                            return '<td class="is-empty"></td>';
                        e.push("is-outside-current-month"),
                        t.enableSelectionDaysInNextAndPreviousMonths || e.push("is-selection-disabled")
                    }
                    return t.isDisabled && e.push("is-disabled"),
                    t.isToday && e.push("is-today"),
                    t.isSelected && (e.push("is-selected"),
                    n = "true"),
                    t.hasEvent && e.push("has-event"),
                    t.isInRange && e.push("is-inrange"),
                    t.isStartRange && e.push("is-startrange"),
                    t.isEndRange && e.push("is-endrange"),
                    '<td data-day="' + t.day + '" class="' + e.join(" ") + '" aria-selected="' + n + '"><button class="pika-button pika-day" type="button" data-pika-year="' + t.year + '" data-pika-month="' + t.month + '" data-pika-day="' + t.day + '">' + t.day + "</button></td>"
                }(k)),
                7 == ++v && (i.showWeekNumber && l.unshift(function(t, e, n, i) {
                    n = new Date(n,e,t);
                    return '<td class="pika-week">' + (L ? $(n).isoWeek() : P(n, i)) + "</td>"
                }(y - o, e, t, i.firstWeekOfYearMinDays)),
                a.push((S = l,
                T = i.isRTL,
                '<tr class="pika-row' + (i.pickWholeWeek ? " pick-whole-week" : "") + (m ? " is-selected" : "") + '">' + (T ? S.reverse() : S).join("") + "</tr>")),
                v = 0,
                m = !(l = []))
            }
            return R(i, a, n)
        },
        isVisible: function() {
            return this._v
        },
        show: function() {
            this.isVisible() || (this._v = !0,
            this.draw(),
            p(this.el, "is-hidden"),
            this._o.bound && (s(f, "click", this._onClick),
            this.adjustPosition()),
            "function" == typeof this._o.onOpen && this._o.onOpen.call(this))
        },
        hide: function() {
            var t = this._v;
            !1 !== t && (this._o.bound && e(f, "click", this._onClick),
            this._o.container || (this.el.style.position = "static",
            this.el.style.left = "auto",
            this.el.style.top = "auto"),
            h(this.el, "is-hidden"),
            this._v = !1,
            void 0 !== t && "function" == typeof this._o.onClose && this._o.onClose.call(this))
        },
        destroy: function() {
            var t = this._o;
            this.hide(),
            e(this.el, "mousedown", this._onMouseDown, !0),
            e(this.el, "touchend", this._onMouseDown, !0),
            e(this.el, "change", this._onChange),
            t.keyboardInput && e(f, "keydown", this._onKeyChange),
            t.field && (e(t.field, "change", this._onInputChange),
            t.bound && (e(t.trigger, "click", this._onInputClick),
            e(t.trigger, "focus", this._onInputFocus),
            e(t.trigger, "blur", this._onInputBlur))),
            this.el.parentNode && this.el.parentNode.removeChild(this.el)
        }
    },
    t
}),
function(n, i, $, d) {
    "use strict";
    function h(t) {
        return t && t.hasOwnProperty && t instanceof $
    }
    function A(t) {
        return p(t) && 0 < t.indexOf("%")
    }
    function M(t, e) {
        var n = parseInt(t, 10) || 0;
        return e && A(t) && (n = O.getViewport()[e] / 100 * n),
        Math.ceil(n)
    }
    function I(t, e) {
        return M(t, e) + "px"
    }
    var s = $("html")
      , r = $(n)
      , c = $(i)
      , O = $.fancybox = function() {
        O.open.apply(this, arguments)
    }
      , o = navigator.userAgent.match(/msie/i)
      , a = null
      , u = i.createTouch !== d
      , p = function(t) {
        return t && "string" === $.type(t)
    };
    $.extend(O, {
        version: "2.1.5",
        defaults: {
            padding: 15,
            margin: 20,
            width: 800,
            height: 600,
            minWidth: 100,
            minHeight: 100,
            maxWidth: 9999,
            maxHeight: 9999,
            pixelRatio: 1,
            autoSize: !0,
            autoHeight: !1,
            autoWidth: !1,
            autoResize: !0,
            autoCenter: !u,
            fitToView: !0,
            aspectRatio: !1,
            topRatio: .5,
            leftRatio: .5,
            scrolling: "auto",
            wrapCSS: "",
            arrows: !0,
            closeBtn: !0,
            closeClick: !1,
            nextClick: !1,
            mouseWheel: !0,
            autoPlay: !1,
            playSpeed: 3e3,
            preload: 3,
            modal: !1,
            loop: !0,
            ajax: {
                dataType: "html",
                headers: {
                    "X-fancyBox": !0
                }
            },
            iframe: {
                scrolling: "auto",
                preload: !0
            },
            swf: {
                wmode: "transparent",
                allowfullscreen: "true",
                allowscriptaccess: "always"
            },
            keys: {
                next: {
                    13: "left",
                    34: "up",
                    39: "left",
                    40: "up"
                },
                prev: {
                    8: "right",
                    33: "down",
                    37: "right",
                    38: "down"
                },
                close: [27],
                play: [32],
                toggle: [70]
            },
            direction: {
                next: "left",
                prev: "right"
            },
            scrollOutside: !0,
            index: 0,
            type: null,
            href: null,
            content: null,
            title: null,
            tpl: {
                wrap: '<div class="fancybox-wrap" tabIndex="-1"><div class="fancybox-skin"><div class="fancybox-outer"><div class="fancybox-inner"></div></div></div></div>',
                image: '<img class="fancybox-image" src="{href}" alt="" />',
                iframe: '<iframe id="fancybox-frame{rnd}" name="fancybox-frame{rnd}" class="fancybox-iframe" frameborder="0" vspace="0" hspace="0" webkitAllowFullScreen mozallowfullscreen allowFullScreen' + (o ? ' allowtransparency="true"' : "") + "></iframe>",
                error: '<p class="fancybox-error">The requested content cannot be loaded.<br/>Please try again later.</p>',
                closeBtn: '<a title="Close" class="fancybox-item fancybox-close" href="javascript:;"></a>',
                next: '<a title="Next" class="fancybox-nav fancybox-next" href="javascript:;"><span></span></a>',
                prev: '<a title="Previous" class="fancybox-nav fancybox-prev" href="javascript:;"><span></span></a>'
            },
            openEffect: "fade",
            openSpeed: 250,
            openEasing: "swing",
            openOpacity: !0,
            openMethod: "zoomIn",
            closeEffect: "fade",
            closeSpeed: 250,
            closeEasing: "swing",
            closeOpacity: !0,
            closeMethod: "zoomOut",
            nextEffect: "elastic",
            nextSpeed: 250,
            nextEasing: "swing",
            nextMethod: "changeIn",
            prevEffect: "elastic",
            prevSpeed: 250,
            prevEasing: "swing",
            prevMethod: "changeOut",
            helpers: {
                overlay: !0,
                title: !0
            },
            onCancel: $.noop,
            beforeLoad: $.noop,
            afterLoad: $.noop,
            beforeShow: $.noop,
            afterShow: $.noop,
            beforeChange: $.noop,
            beforeClose: $.noop,
            afterClose: $.noop
        },
        group: {},
        opts: {},
        previous: null,
        coming: null,
        current: null,
        isActive: !1,
        isOpen: !1,
        isOpened: !1,
        wrap: null,
        skin: null,
        outer: null,
        inner: null,
        player: {
            timer: null,
            isActive: !1
        },
        ajaxLoad: null,
        imgPreload: null,
        transitions: {},
        helpers: {},
        open: function(c, u) {
            if (c && ($.isPlainObject(u) || (u = {}),
            !1 !== O.close(!0)))
                return $.isArray(c) || (c = h(c) ? $(c).get() : [c]),
                $.each(c, function(t, e) {
                    var n, i, s, r, o, a, l = {};
                    "object" === $.type(e) && (e.nodeType && (e = $(e)),
                    h(e) ? (l = {
                        href: e.data("fancybox-href") || e.attr("href"),
                        title: e.data("fancybox-title") || e.attr("title"),
                        isDom: !0,
                        element: e
                    },
                    $.metadata && $.extend(!0, l, e.metadata())) : l = e),
                    n = u.href || l.href || (p(e) ? e : null),
                    i = u.title !== d ? u.title : l.title || "",
                    !(r = (s = u.content || l.content) ? "html" : u.type || l.type) && l.isDom && (r = (r = e.data("fancybox-type")) || ((o = e.prop("class").match(/fancybox\.(\w+)/)) ? o[1] : null)),
                    p(n) && (r || (O.isImage(n) ? r = "image" : O.isSWF(n) ? r = "swf" : "#" === n.charAt(0) ? r = "inline" : p(e) && (r = "html",
                    s = e)),
                    "ajax" === r && (n = (o = n.split(/\s+/, 2)).shift(),
                    a = o.shift())),
                    s || ("inline" === r ? n ? s = $(p(n) ? n.replace(/.*(?=#[^\s]+$)/, "") : n) : l.isDom && (s = e) : "html" === r ? s = n : r || n || !l.isDom || (r = "inline",
                    s = e)),
                    $.extend(l, {
                        href: n,
                        type: r,
                        content: s,
                        title: i,
                        selector: a
                    }),
                    c[t] = l
                }),
                O.opts = $.extend(!0, {}, O.defaults, u),
                u.keys !== d && (O.opts.keys = !!u.keys && $.extend({}, O.defaults.keys, u.keys)),
                O.group = c,
                O._start(O.opts.index)
        },
        cancel: function() {
            var t = O.coming;
            t && !1 !== O.trigger("onCancel") && (O.hideLoading(),
            O.ajaxLoad && O.ajaxLoad.abort(),
            O.ajaxLoad = null,
            O.imgPreload && (O.imgPreload.onload = O.imgPreload.onerror = null),
            t.wrap && t.wrap.stop(!0, !0).trigger("onReset").remove(),
            O.coming = null,
            O.current || O._afterZoomOut(t))
        },
        close: function(t) {
            O.cancel(),
            !1 !== O.trigger("beforeClose") && (O.unbindEvents(),
            O.isActive && (O.isOpen && !0 !== t ? (O.isOpen = O.isOpened = !1,
            O.isClosing = !0,
            $(".fancybox-item, .fancybox-nav").remove(),
            O.wrap.stop(!0, !0).removeClass("fancybox-opened"),
            O.transitions[O.current.closeMethod]()) : ($(".fancybox-wrap").stop(!0).trigger("onReset").remove(),
            O._afterZoomOut())))
        },
        play: function(t) {
            function e() {
                i(),
                O.current && O.player.isActive && (O.player.timer = setTimeout(O.next, O.current.playSpeed))
            }
            function n() {
                i(),
                c.unbind(".player"),
                O.player.isActive = !1,
                O.trigger("onPlayEnd")
            }
            var i = function() {
                clearTimeout(O.player.timer)
            };
            !0 === t || !O.player.isActive && !1 !== t ? O.current && (O.current.loop || O.current.index < O.group.length - 1) && (O.player.isActive = !0,
            c.bind({
                "onCancel.player beforeClose.player": n,
                "onUpdate.player": e,
                "beforeLoad.player": i
            }),
            e(),
            O.trigger("onPlayStart")) : n()
        },
        next: function(t) {
            var e = O.current;
            e && (p(t) || (t = e.direction.next),
            O.jumpto(e.index + 1, t, "next"))
        },
        prev: function(t) {
            var e = O.current;
            e && (p(t) || (t = e.direction.prev),
            O.jumpto(e.index - 1, t, "prev"))
        },
        jumpto: function(t, e, n) {
            var i = O.current;
            i && (t = M(t),
            O.direction = e || i.direction[t >= i.index ? "next" : "prev"],
            O.router = n || "jumpto",
            i.loop && (t < 0 && (t = i.group.length + t % i.group.length),
            t %= i.group.length),
            i.group[t] !== d && (O.cancel(),
            O._start(t)))
        },
        reposition: function(t, e) {
            var n = O.current
              , i = n ? n.wrap : null;
            i && (e = O._getPosition(e),
            t && "scroll" === t.type ? (delete e.position,
            i.stop(!0, !0).animate(e, 200)) : (i.css(e),
            n.pos = $.extend({}, n.dim, e)))
        },
        update: function(e) {
            var n = e && e.type
              , i = !n || "orientationchange" === n;
            i && (clearTimeout(a),
            a = null),
            O.isOpen && !a && (a = setTimeout(function() {
                var t = O.current;
                t && !O.isClosing && (O.wrap.removeClass("fancybox-tmp"),
                (i || "load" === n || "resize" === n && t.autoResize) && O._setDimension(),
                "scroll" === n && t.canShrink || O.reposition(e),
                O.trigger("onUpdate"),
                a = null)
            }, i && !u ? 0 : 300))
        },
        toggle: function(t) {
            O.isOpen && (O.current.fitToView = "boolean" === $.type(t) ? t : !O.current.fitToView,
            u && (O.wrap.removeAttr("style").addClass("fancybox-tmp"),
            O.trigger("onUpdate")),
            O.update())
        },
        hideLoading: function() {
            c.unbind(".loading"),
            $("#fancybox-loading").remove()
        },
        showLoading: function() {
            var t, e;
            O.hideLoading(),
            t = $('<div id="fancybox-loading"><div></div></div>').click(O.cancel).appendTo("body"),
            c.bind("keydown.loading", function(t) {
                27 === (t.which || t.keyCode) && (t.preventDefault(),
                O.cancel())
            }),
            O.defaults.fixed || (e = O.getViewport(),
            t.css({
                position: "absolute",
                top: .5 * e.h + e.y,
                left: .5 * e.w + e.x
            }))
        },
        getViewport: function() {
            var t = O.current && O.current.locked || !1
              , e = {
                x: r.scrollLeft(),
                y: r.scrollTop()
            };
            return t ? (e.w = t[0].clientWidth,
            e.h = t[0].clientHeight) : (e.w = u && n.innerWidth ? n.innerWidth : r.width(),
            e.h = u && n.innerHeight ? n.innerHeight : r.height()),
            e
        },
        unbindEvents: function() {
            O.wrap && h(O.wrap) && O.wrap.unbind(".fb"),
            c.unbind(".fb"),
            r.unbind(".fb")
        },
        bindEvents: function() {
            var e, l = O.current;
            l && (r.bind("orientationchange.fb" + (u ? "" : " resize.fb") + (l.autoCenter && !l.locked ? " scroll.fb" : ""), O.update),
            (e = l.keys) && c.bind("keydown.fb", function(n) {
                var i = n.which || n.keyCode
                  , t = n.target || n.srcElement;
                if (27 === i && O.coming)
                    return !1;
                n.ctrlKey || n.altKey || n.shiftKey || n.metaKey || t && (t.type || $(t).is("[contenteditable]")) || $.each(e, function(t, e) {
                    return 1 < l.group.length && e[i] !== d ? (O[t](e[i]),
                    n.preventDefault(),
                    !1) : -1 < $.inArray(i, e) ? (O[t](),
                    n.preventDefault(),
                    !1) : void 0
                })
            }),
            $.fn.mousewheel && l.mouseWheel && O.wrap.bind("mousewheel.fb", function(t, e, n, i) {
                for (var s, r = t.target || null, o = $(r), a = !1; o.length && !(a || o.is(".fancybox-skin") || o.is(".fancybox-wrap")); )
                    a = (s = o[0]) && !(s.style.overflow && "hidden" === s.style.overflow) && (s.clientWidth && s.scrollWidth > s.clientWidth || s.clientHeight && s.scrollHeight > s.clientHeight),
                    o = $(o).parent();
                0 === e || a || 1 < O.group.length && !l.canShrink && (0 < i || 0 < n ? O.prev(0 < i ? "down" : "left") : (i < 0 || n < 0) && O.next(i < 0 ? "up" : "right"),
                t.preventDefault())
            }))
        },
        trigger: function(n, t) {
            var e, i = t || O.coming || O.current;
            if (i) {
                if (!1 === (e = $.isFunction(i[n]) ? i[n].apply(i, Array.prototype.slice.call(arguments, 1)) : e))
                    return !1;
                i.helpers && $.each(i.helpers, function(t, e) {
                    e && O.helpers[t] && $.isFunction(O.helpers[t][n]) && O.helpers[t][n]($.extend(!0, {}, O.helpers[t].defaults, e), i)
                }),
                c.trigger(n)
            }
        },
        isImage: function(t) {
            return p(t) && t.match(/(^data:image\/.*,)|(\.(jp(e|g|eg)|gif|png|bmp|webp|svg)((\?|#).*)?$)/i)
        },
        isSWF: function(t) {
            return p(t) && t.match(/\.(swf)((\?|#).*)?$/i)
        },
        _start: function(t) {
            var e, n, i = {};
            if (t = M(t),
            !(e = O.group[t] || null))
                return !1;
            if (e = (i = $.extend(!0, {}, O.opts, e)).margin,
            n = i.padding,
            "number" === $.type(e) && (i.margin = [e, e, e, e]),
            "number" === $.type(n) && (i.padding = [n, n, n, n]),
            i.modal && $.extend(!0, i, {
                closeBtn: !1,
                closeClick: !1,
                nextClick: !1,
                arrows: !1,
                mouseWheel: !1,
                keys: null,
                helpers: {
                    overlay: {
                        closeClick: !1
                    }
                }
            }),
            i.autoSize && (i.autoWidth = i.autoHeight = !0),
            "auto" === i.width && (i.autoWidth = !0),
            "auto" === i.height && (i.autoHeight = !0),
            i.group = O.group,
            i.index = t,
            O.coming = i,
            !1 === O.trigger("beforeLoad"))
                O.coming = null;
            else {
                if (e = i.type,
                n = i.href,
                !e)
                    return O.coming = null,
                    !(!O.current || !O.router || "jumpto" === O.router) && (O.current.index = t,
                    O[O.router](O.direction));
                if (O.isActive = !0,
                "image" !== e && "swf" !== e || (i.autoHeight = i.autoWidth = !1,
                i.scrolling = "visible"),
                "image" === e && (i.aspectRatio = !0),
                "iframe" === e && u && (i.scrolling = "scroll"),
                i.wrap = $(i.tpl.wrap).addClass("fancybox-" + (u ? "mobile" : "desktop") + " fancybox-type-" + e + " fancybox-tmp " + i.wrapCSS).appendTo(i.parent || "body"),
                $.extend(i, {
                    skin: $(".fancybox-skin", i.wrap),
                    outer: $(".fancybox-outer", i.wrap),
                    inner: $(".fancybox-inner", i.wrap)
                }),
                $.each(["Top", "Right", "Bottom", "Left"], function(t, e) {
                    i.skin.css("padding" + e, I(i.padding[t]))
                }),
                O.trigger("onReady"),
                "inline" === e || "html" === e) {
                    if (!i.content || !i.content.length)
                        return O._error("content")
                } else if (!n)
                    return O._error("href");
                "image" === e ? O._loadImage() : "ajax" === e ? O._loadAjax() : "iframe" === e ? O._loadIframe() : O._afterLoad()
            }
        },
        _error: function(t) {
            $.extend(O.coming, {
                type: "html",
                autoWidth: !0,
                autoHeight: !0,
                minWidth: 0,
                minHeight: 0,
                scrolling: "no",
                hasError: t,
                content: O.coming.tpl.error
            }),
            O._afterLoad()
        },
        _loadImage: function() {
            var t = O.imgPreload = new Image;
            t.onload = function() {
                this.onload = this.onerror = null,
                O.coming.width = this.width / O.opts.pixelRatio,
                O.coming.height = this.height / O.opts.pixelRatio,
                O._afterLoad()
            }
            ,
            t.onerror = function() {
                this.onload = this.onerror = null,
                O._error("image")
            }
            ,
            t.src = O.coming.href,
            !0 !== t.complete && O.showLoading()
        },
        _loadAjax: function() {
            var n = O.coming;
            O.showLoading(),
            O.ajaxLoad = $.ajax($.extend({}, n.ajax, {
                url: n.href,
                error: function(t, e) {
                    O.coming && "abort" !== e ? O._error("ajax", t) : O.hideLoading()
                },
                success: function(t, e) {
                    "success" === e && (n.content = t,
                    O._afterLoad())
                }
            }))
        },
        _loadIframe: function() {
            var t = O.coming
              , e = $(t.tpl.iframe.replace(/\{rnd\}/g, (new Date).getTime())).attr("scrolling", u ? "auto" : t.iframe.scrolling).attr("src", t.href);
            $(t.wrap).bind("onReset", function() {
                try {
                    $(this).find("iframe").hide().attr("src", "//about:blank").end().empty()
                } catch (t) {}
            }),
            t.iframe.preload && (O.showLoading(),
            e.one("load", function() {
                $(this).data("ready", 1),
                u || $(this).bind("load.fb", O.update),
                $(this).parents(".fancybox-wrap").width("100%").removeClass("fancybox-tmp").show(),
                O._afterLoad()
            })),
            t.content = e.appendTo(t.inner),
            t.iframe.preload || O._afterLoad()
        },
        _preloadImages: function() {
            for (var t, e = O.group, n = O.current, i = e.length, s = n.preload ? Math.min(n.preload, i - 1) : 0, r = 1; r <= s; r += 1)
                "image" === (t = e[(n.index + r) % i]).type && t.href && ((new Image).src = t.href)
        },
        _afterLoad: function() {
            var t, n, e, i, s, r = O.coming, o = O.current, a = "fancybox-placeholder";
            if (O.hideLoading(),
            r && !1 !== O.isActive) {
                if (!1 === O.trigger("afterLoad", r, o))
                    return r.wrap.stop(!0).trigger("onReset").remove(),
                    void (O.coming = null);
                switch (o && (O.trigger("beforeChange", o),
                o.wrap.stop(!0).removeClass("fancybox-opened").find(".fancybox-item, .fancybox-nav").remove()),
                O.unbindEvents(),
                n = (t = r).content,
                e = r.type,
                r = r.scrolling,
                $.extend(O, {
                    wrap: t.wrap,
                    skin: t.skin,
                    outer: t.outer,
                    inner: t.inner,
                    current: t,
                    previous: o
                }),
                i = t.href,
                e) {
                case "inline":
                case "ajax":
                case "html":
                    t.selector ? n = $("<div>").html(n).find(t.selector) : h(n) && (n.data(a) || n.data(a, $('<div class="' + a + '"></div>').insertAfter(n).hide()),
                    n = n.show().detach(),
                    t.wrap.bind("onReset", function() {
                        $(this).find(n).length && n.hide().replaceAll(n.data(a)).data(a, !1)
                    }));
                    break;
                case "image":
                    n = t.tpl.image.replace("{href}", i);
                    break;
                case "swf":
                    n = '<object id="fancybox-swf" classid="clsid:D27CDB6E-AE6D-11cf-96B8-444553540000" width="100%" height="100%"><param name="movie" value="' + i + '"></param>',
                    s = "",
                    $.each(t.swf, function(t, e) {
                        n += '<param name="' + t + '" value="' + e + '"></param>',
                        s += " " + t + '="' + e + '"'
                    }),
                    n += '<embed src="' + i + '" type="application/x-shockwave-flash" width="100%" height="100%"' + s + "></embed></object>"
                }
                h(n) && n.parent().is(t.inner) || t.inner.append(n),
                O.trigger("beforeShow"),
                t.inner.css("overflow", "yes" === r ? "scroll" : "no" === r ? "hidden" : r),
                O._setDimension(),
                O.reposition(),
                O.isOpen = !1,
                O.coming = null,
                O.bindEvents(),
                O.isOpened ? o.prevMethod && O.transitions[o.prevMethod]() : $(".fancybox-wrap").not(t.wrap).stop(!0).trigger("onReset").remove(),
                O.transitions[O.isOpened ? t.nextMethod : t.openMethod](),
                O._preloadImages()
            }
        },
        _setDimension: function() {
            var t, e, n, i, s, r, o, a, l, c, u, d, h, p, f = O.getViewport(), g = 0, m = O.wrap, y = O.skin, v = O.inner, b = O.current, _ = b.width, w = b.height, x = b.minWidth, C = b.minHeight, k = b.maxWidth, S = b.maxHeight, T = b.scrolling, D = b.scrollOutside ? b.scrollbarWidth : 0, E = b.margin, N = M(E[1] + E[3]), E = M(E[0] + E[2]);
            if (m.add(y).add(v).width("auto").height("auto").removeClass("fancybox-tmp"),
            n = N + (t = M(y.outerWidth(!0) - y.width())),
            i = E + (e = M(y.outerHeight(!0) - y.height())),
            s = A(_) ? (f.w - n) * M(_) / 100 : _,
            r = A(w) ? (f.h - i) * M(w) / 100 : w,
            "iframe" === b.type) {
                if (h = b.content,
                b.autoHeight && 1 === h.data("ready"))
                    try {
                        h[0].contentWindow.document.location && (v.width(s).height(9999),
                        p = h.contents().find("body"),
                        D && p.css("overflow-x", "hidden"),
                        r = p.outerHeight(!0))
                    } catch (t) {}
            } else
                (b.autoWidth || b.autoHeight) && (v.addClass("fancybox-tmp"),
                b.autoWidth || v.width(s),
                b.autoHeight || v.height(r),
                b.autoWidth && (s = v.width()),
                b.autoHeight && (r = v.height()),
                v.removeClass("fancybox-tmp"));
            if (_ = M(s),
            w = M(r),
            a = s / r,
            x = M(A(x) ? M(x, "w") - n : x),
            k = M(A(k) ? M(k, "w") - n : k),
            C = M(A(C) ? M(C, "h") - i : C),
            p = k,
            o = S = M(A(S) ? M(S, "h") - i : S),
            b.fitToView && (k = Math.min(f.w - n, k),
            S = Math.min(f.h - i, S)),
            u = f.w - N,
            d = f.h - E,
            b.aspectRatio ? (w = (_ = S < (w = k < _ ? M((_ = k) / a) : w) ? M((w = S) * a) : _) < x ? M((_ = x) / a) : w) < C && (_ = M((w = C) * a)) : (_ = Math.max(x, Math.min(_, k)),
            b.autoHeight && "iframe" !== b.type && (v.width(_),
            w = v.height()),
            w = Math.max(C, Math.min(w, S))),
            b.fitToView)
                if (v.width(_).height(w),
                m.width(_ + t),
                l = m.width(),
                c = m.height(),
                b.aspectRatio)
                    for (; (u < l || d < c) && x < _ && C < w && !(19 < g++); )
                        w = Math.max(C, Math.min(S, w - 10)),
                        (_ = M(w * a)) < x && (w = M((_ = x) / a)),
                        k < _ && (w = M((_ = k) / a)),
                        v.width(_).height(w),
                        m.width(_ + t),
                        l = m.width(),
                        c = m.height();
                else
                    _ = Math.max(x, Math.min(_, _ - (l - u))),
                    w = Math.max(C, Math.min(w, w - (c - d)));
            D && "auto" === T && w < r && _ + t + D < u && (_ += D),
            v.width(_).height(w),
            m.width(_ + t),
            l = m.width(),
            c = m.height(),
            n = (u < l || d < c) && x < _ && C < w,
            i = b.aspectRatio ? _ < p && w < o && _ < s && w < r : (_ < p || w < o) && (_ < s || w < r),
            $.extend(b, {
                dim: {
                    width: I(l),
                    height: I(c)
                },
                origWidth: s,
                origHeight: r,
                canShrink: n,
                canExpand: i,
                wPadding: t,
                hPadding: e,
                wrapSpace: c - y.outerHeight(!0),
                skinSpace: y.height() - w
            }),
            !h && b.autoHeight && C < w && w < S && !i && v.height("auto")
        },
        _getPosition: function(t) {
            var e = O.current
              , n = O.getViewport()
              , i = e.margin
              , s = O.wrap.width() + i[1] + i[3]
              , r = O.wrap.height() + i[0] + i[2]
              , i = {
                position: "absolute",
                top: i[0],
                left: i[3]
            };
            return e.autoCenter && e.fixed && !t && r <= n.h && s <= n.w ? i.position = "fixed" : e.locked || (i.top += n.y,
            i.left += n.x),
            i.top = I(Math.max(i.top, i.top + (n.h - r) * e.topRatio)),
            i.left = I(Math.max(i.left, i.left + (n.w - s) * e.leftRatio)),
            i
        },
        _afterZoomIn: function() {
            var e = O.current;
            e && (O.isOpen = O.isOpened = !0,
            O.wrap.css("overflow", "visible").addClass("fancybox-opened"),
            O.update(),
            (e.closeClick || e.nextClick && 1 < O.group.length) && O.inner.css("cursor", "pointer").bind("click.fb", function(t) {
                $(t.target).is("a") || $(t.target).parent().is("a") || (t.preventDefault(),
                O[e.closeClick ? "close" : "next"]())
            }),
            e.closeBtn && $(e.tpl.closeBtn).appendTo(O.skin).bind("click.fb", function(t) {
                t.preventDefault(),
                O.close()
            }),
            e.arrows && 1 < O.group.length && ((e.loop || 0 < e.index) && $(e.tpl.prev).appendTo(O.outer).bind("click.fb", O.prev),
            (e.loop || e.index < O.group.length - 1) && $(e.tpl.next).appendTo(O.outer).bind("click.fb", O.next)),
            O.trigger("afterShow"),
            e.loop || e.index !== e.group.length - 1 ? O.opts.autoPlay && !O.player.isActive && (O.opts.autoPlay = !1,
            O.play()) : O.play(!1))
        },
        _afterZoomOut: function(t) {
            t = t || O.current,
            $(".fancybox-wrap").trigger("onReset").remove(),
            $.extend(O, {
                group: {},
                opts: {},
                router: !1,
                current: null,
                isActive: !1,
                isOpened: !1,
                isOpen: !1,
                isClosing: !1,
                wrap: null,
                skin: null,
                outer: null,
                inner: null
            }),
            O.trigger("afterClose", t)
        }
    }),
    O.transitions = {
        getOrigPosition: function() {
            var t = O.current
              , e = t.element
              , n = t.orig
              , i = {}
              , s = 50
              , r = 50
              , o = t.hPadding
              , a = t.wPadding
              , l = O.getViewport();
            return !n && t.isDom && e.is(":visible") && !(n = e.find("img:first")).length && (n = e),
            h(n) ? (i = n.offset(),
            n.is("img") && (s = n.outerWidth(),
            r = n.outerHeight())) : (i.top = l.y + (l.h - r) * t.topRatio,
            i.left = l.x + (l.w - s) * t.leftRatio),
            "fixed" !== O.wrap.css("position") && !t.locked || (i.top -= l.y,
            i.left -= l.x),
            i = {
                top: I(i.top - o * t.topRatio),
                left: I(i.left - a * t.leftRatio),
                width: I(s + a),
                height: I(r + o)
            }
        },
        step: function(t, e) {
            var n = e.prop
              , i = O.current
              , s = i.wrapSpace
              , r = i.skinSpace;
            "width" !== n && "height" !== n || (e = e.end === e.start ? 1 : (t - e.start) / (e.end - e.start),
            O.isClosing && (e = 1 - e),
            t = t - ("width" === n ? i.wPadding : i.hPadding),
            O.skin[n](M("width" === n ? t : t - s * e)),
            O.inner[n](M("width" === n ? t : t - s * e - r * e)))
        },
        zoomIn: function() {
            var t = O.current
              , e = t.pos
              , n = t.openEffect
              , i = "elastic" === n
              , s = $.extend({
                opacity: 1
            }, e);
            delete s.position,
            i ? (e = this.getOrigPosition(),
            t.openOpacity && (e.opacity = .1)) : "fade" === n && (e.opacity = .1),
            O.wrap.css(e).animate(s, {
                duration: "none" === n ? 0 : t.openSpeed,
                easing: t.openEasing,
                step: i ? this.step : null,
                complete: O._afterZoomIn
            })
        },
        zoomOut: function() {
            var t = O.current
              , e = t.closeEffect
              , n = "elastic" === e
              , i = {
                opacity: .1
            };
            n && (i = this.getOrigPosition(),
            t.closeOpacity && (i.opacity = .1)),
            O.wrap.animate(i, {
                duration: "none" === e ? 0 : t.closeSpeed,
                easing: t.closeEasing,
                step: n ? this.step : null,
                complete: O._afterZoomOut
            })
        },
        changeIn: function() {
            var t, e = O.current, n = e.nextEffect, i = e.pos, s = {
                opacity: 1
            }, r = O.direction;
            i.opacity = .1,
            "elastic" === n && (t = "down" === r || "up" === r ? "top" : "left",
            "down" === r || "right" === r ? (i[t] = I(M(i[t]) - 200),
            s[t] = "+=200px") : (i[t] = I(M(i[t]) + 200),
            s[t] = "-=200px")),
            "none" === n ? O._afterZoomIn() : O.wrap.css(i).animate(s, {
                duration: e.nextSpeed,
                easing: e.nextEasing,
                complete: O._afterZoomIn
            })
        },
        changeOut: function() {
            var t = O.previous
              , e = t.prevEffect
              , n = {
                opacity: .1
            }
              , i = O.direction;
            "elastic" === e && (n["down" === i || "up" === i ? "top" : "left"] = ("up" === i || "left" === i ? "-" : "+") + "=200px"),
            t.wrap.animate(n, {
                duration: "none" === e ? 0 : t.prevSpeed,
                easing: t.prevEasing,
                complete: function() {
                    $(this).trigger("onReset").remove()
                }
            })
        }
    },
    O.helpers.overlay = {
        defaults: {
            closeClick: !0,
            speedOut: 200,
            showEarly: !0,
            css: {},
            locked: !u,
            fixed: !0
        },
        overlay: null,
        fixed: !1,
        el: $("html"),
        create: function(t) {
            t = $.extend({}, this.defaults, t),
            this.overlay && this.close(),
            this.overlay = $('<div class="fancybox-overlay"></div>').appendTo((O.coming || t).parent),
            this.fixed = !1,
            t.fixed && O.defaults.fixed && (this.overlay.addClass("fancybox-overlay-fixed"),
            this.fixed = !0)
        },
        open: function(t) {
            var e = this;
            t = $.extend({}, this.defaults, t),
            this.overlay ? this.overlay.unbind(".overlay").width("auto").height("auto") : this.create(t),
            this.fixed || (r.bind("resize.overlay", $.proxy(this.update, this)),
            this.update()),
            t.closeClick && this.overlay.bind("click.overlay", function(t) {
                if ($(t.target).hasClass("fancybox-overlay"))
                    return (O.isActive ? O : e).close(),
                    !1
            }),
            this.overlay.css(t.css).show()
        },
        close: function() {
            var t, e;
            r.unbind("resize.overlay"),
            this.el.hasClass("fancybox-lock") && ($(".fancybox-margin").removeClass("fancybox-margin"),
            t = r.scrollTop(),
            e = r.scrollLeft(),
            this.el.removeClass("fancybox-lock"),
            r.scrollTop(t).scrollLeft(e)),
            $(".fancybox-overlay").remove().hide(),
            $.extend(this, {
                overlay: null,
                fixed: !1
            })
        },
        update: function() {
            var t, e = "100%";
            this.overlay.width(e).height("100%"),
            o ? (t = Math.max(i.documentElement.offsetWidth, i.body.offsetWidth),
            c.width() > t && (e = c.width())) : c.width() > r.width() && (e = c.width()),
            this.overlay.width(e).height(c.height())
        },
        onReady: function(t, e) {
            var n = this.overlay;
            $(".fancybox-overlay").stop(!0, !0),
            n || this.create(t),
            t.locked && this.fixed && e.fixed && (n || (this.margin = c.height() > r.height() && $("html").css("margin-right").replace("px", "")),
            e.locked = this.overlay.append(e.wrap),
            e.fixed = !1),
            !0 === t.showEarly && this.beforeShow.apply(this, arguments)
        },
        beforeShow: function(t, e) {
            var n;
            e.locked && (!1 !== this.margin && ($("*").filter(function() {
                return "fixed" === $(this).css("position") && !$(this).hasClass("fancybox-overlay") && !$(this).hasClass("fancybox-wrap")
            }).addClass("fancybox-margin"),
            this.el.addClass("fancybox-margin")),
            e = r.scrollTop(),
            n = r.scrollLeft(),
            this.el.addClass("fancybox-lock"),
            r.scrollTop(e).scrollLeft(n)),
            this.open(t)
        },
        onUpdate: function() {
            this.fixed || this.update()
        },
        afterClose: function(t) {
            this.overlay && !O.coming && this.overlay.fadeOut(t.speedOut, $.proxy(this.close, this))
        }
    },
    O.helpers.title = {
        defaults: {
            type: "float",
            position: "bottom"
        },
        beforeShow: function(t) {
            var e, n, i = O.current, s = i.title, r = t.type;
            if ($.isFunction(s) && (s = s.call(i.element, i)),
            p(s) && "" !== $.trim(s)) {
                switch (e = $('<div class="fancybox-title fancybox-title-' + r + '-wrap">' + s + "</div>"),
                r) {
                case "inside":
                    n = O.skin;
                    break;
                case "outside":
                    n = O.wrap;
                    break;
                case "over":
                    n = O.inner;
                    break;
                default:
                    n = O.skin,
                    e.appendTo("body"),
                    o && e.width(e.width()),
                    e.wrapInner('<span class="child"></span>'),
                    O.current.margin[2] += Math.abs(M(e.css("margin-bottom")))
                }
                e["top" === t.position ? "prependTo" : "appendTo"](n)
            }
        }
    },
    $.fn.fancybox = function(r) {
        function t(t) {
            var e, n, i = $(this).blur(), s = l;
            t.ctrlKey || t.altKey || t.shiftKey || t.metaKey || i.is(".fancybox-wrap") || (e = r.groupAttr || "data-fancybox-group",
            (n = i.attr(e)) || (e = "rel",
            n = i.get(0)[e]),
            n && "" !== n && "nofollow" !== n && (s = (i = (i = a.length ? $(a) : o).filter("[" + e + '="' + n + '"]')).index(this)),
            r.index = s,
            !1 !== O.open(i, r) && t.preventDefault())
        }
        var o = $(this)
          , a = this.selector || ""
          , l = (r = r || {}).index || 0;
        return a && !1 !== r.live ? c.undelegate(a, "click.fb-start").delegate(a + ":not('.fancybox-item, .fancybox-nav')", "click.fb-start", t) : o.unbind("click.fb-start").bind("click.fb-start", t),
        this.filter("[data-fancybox-start=1]").trigger("click"),
        this
    }
    ,
    c.ready(function() {
        var t, e;
        $.scrollbarWidth === d && ($.scrollbarWidth = function() {
            var t = $('<div style="width:50px;height:50px;overflow:auto"><div/></div>').appendTo("body")
              , e = t.children()
              , e = e.innerWidth() - e.height(99).innerWidth();
            return t.remove(),
            e
        }
        ),
        $.support.fixedPosition === d && ($.support.fixedPosition = (t = $('<div style="position:fixed;top:20px;"></div>').appendTo("body"),
        e = 20 === t[0].offsetTop || 15 === t[0].offsetTop,
        t.remove(),
        e)),
        $.extend(O.defaults, {
            scrollbarWidth: $.scrollbarWidth(),
            fixed: $.support.fixedPosition,
            parent: $("body")
        }),
        t = $(n).width(),
        s.addClass("fancybox-lock-test"),
        e = $(n).width(),
        s.removeClass("fancybox-lock-test"),
        $("<style type='text/css'>.fancybox-margin{margin-right:" + (e - t) + "px;}</style>").appendTo("head")
    })
}(window, document, jQuery),
function(c) {
    c.fn.rwdImageMaps = function() {
        var t = this;
        return c(window).resize(function() {
            t.each(function() {
                var e;
                void 0 !== c(this).attr("usemap") && (e = c(this),
                c("<img />").load(function() {
                    var s = e.attr("width")
                      , r = e.attr("height")
                      , o = (s && r || ((t = new Image).src = e.attr("src"),
                    s = s || t.width,
                    r = r || t.height),
                    e.width() / 100)
                      , a = e.height() / 100
                      , t = e.attr("usemap").replace("#", "")
                      , l = "coords";
                    c('map[name="' + t + '"]').find("area").each(function() {
                        for (var t = c(this), e = (t.data(l) || t.data(l, t.attr(l)),
                        t.data(l).split(",")), n = new Array(e.length), i = 0; i < n.length; ++i)
                            n[i] = i % 2 == 0 ? parseInt(e[i] / s * 100 * o) : parseInt(e[i] / r * 100 * a);
                        t.attr(l, n.toString())
                    })
                }).attr("src", e.attr("src")))
            })
        }).trigger("resize"),
        this
    }
}(jQuery);
function subNavInit(e) {
    "use strict";
    var t = e(".drilldown[class*=col-]")
      , e = Math.max(window.innerWidth, e(window).width());
    e <= 767 && !t.hasClass("collapse-enabled") ? t.addClass("collapse-enabled").find(".drilldown-container").addClass("collapse").attr("id", "collapseSubNav") : 767 < e && t.hasClass("collapse-enabled") && t.removeClass("collapse-enabled").find(".drilldown-container").removeClass("collapse in").attr("id", "").css({
        height: "auto"
    })
}
function carouselInit(n) {
    "use strict";
    var e = n(".carousel:not(.carousel-slideshow)");
    n(".carousel .item:first-child").addClass("first"),
    n(".carousel .item:last-child").addClass("last"),
    n(".carousel").each(function() {
        disableControl(n(this))
    }),
    e && e.each(function() {
        var e = 0
          , t = n(this).find("h3:first-child").height()
          , a = n(this).find(".carousel-img").height();
        n(this).find(".carousel-indicators").css("top", t + a + 40),
        n(this).find(".carousel-control").css("top", t + a + 50),
        n(this).find(".item").each(function() {
            n(this).height() >= e && (e = n(this).height())
        }),
        n(this).find(".item").height(e)
    })
}
function disableControl(e) {
    "use strict";
    e.find(".first").hasClass("active") ? e.find(".left").addClass("disabled").attr("aria-disabled", "true") : e.find(".left").removeClass("disabled").attr("aria-disabled", "false"),
    e.find(".last").hasClass("active") ? e.find(".right").addClass("disabled").attr("aria-disabled", "true") : e.find(".right").removeClass("disabled").attr("aria-disabled", "false")
}
!function(e) {
    "use strict";
    e(".collapse:not(tbody)").on("show.bs.collapse", function() {
        e(this).prev().addClass("active icon--root").removeClass("icon--greater").attr({
            "aria-selected": "true",
            "aria-expanded": "true"
        })
    }),
    e(".collapse:not(tbody)").on("hide.bs.collapse", function() {
        e(this).prev().removeClass("active icon--root").addClass("icon--greater").attr({
            "aria-selected": "false",
            "aria-expanded": "false"
        })
    }),
    e("tbody.collapse").on("show.bs.collapse", function() {
        e(this).prev().find("[data-toggle=collapse]").addClass("active").attr({
            "aria-selected": "true",
            "aria-expanded": "true"
        })
    }),
    e("tbody.collapse").on("hide.bs.collapse", function() {
        e(this).prev().find("[data-toggle=collapse]").removeClass("active").attr({
            "aria-selected": "false",
            "aria-expanded": "false"
        })
    }),
    e('[aria-multiselectable="true"]').each(function() {
        e(this).find('[data-toggle="collapse"][data-parent]').first().attr({
            tabindex: 0
        })
    })
}(jQuery),
function() {
    "use strict";
    jQuery(".drilldown").drilldown({
        event: "click",
        selector: "a",
        speed: 100,
        cssClass: {
            container: "drilldown-container",
            root: "nav-page-list",
            sub: "drilldown-sub",
            back: "drilldown-back"
        }
    })
}(),
function(e) {
    "use strict";
    e(window).scroll(function() {
        36 <= e(this).scrollTop() ? e(".nav-mobile").hasClass("fixed") || e(".nav-mobile").addClass("fixed").after('<div class="nav-mobile-spacer" id="spacer" style="height:36px;"></div>') : e(".nav-mobile").hasClass("fixed") && (e(".nav-mobile").removeClass("fixed"),
        e("#spacer").remove())
    })
}(jQuery),
function(a) {
    "use strict";
    var e = a(".yamm")
      , n = a(".yamm-close, .yamm-close-bottom")
      , t = a(".yamm .dropdown")
      , i = a(".yamm .dropdown-toggle");
    e.each(function() {
        var t = a(this);
        t.on("click", ".dropdown-toggle", function() {
            var e;
            a(this).parent().hasClass("open") || (e = a(window).height() - 49,
            t.find(".drilldown-container").height(e))
        })
    }),
    t.find("li a").removeAttr("tabindex"),
    i.on("click", function() {
        a(this).parents(t).trigger("get.hidden")
    }),
    t.on({
        "shown.bs.dropdown": function() {
            this.closable = !1
        },
        "get.hidden": function() {
            this.closable = !0
        }
    }),
    a(".dropdown").on("show.bs.dropdown", function() {
        t.removeClass("open"),
        e.removeClass("nav-open"),
        a(this).parents(e).addClass("nav-open")
    }),
    t.on("hide.bs.dropdown", function() {
        return this.closable && e.removeClass("nav-open"),
        this.closable
    }),
    a(document).on("click", function(e) {
        0 < a(".dropdown.open").length && 0 === a(e.target).parents(".dropdown").length && a(".dropdown.open .dropdown-toggle").trigger("click")
    }),
    t.each(function() {
        var t = a(this);
        t.find(n).click(function(e) {
            e.preventDefault(),
            t.find(i).trigger("click")
        })
    })
}(jQuery),
function(n) {
    "use strict";
    var i = [];
    function e(e) {
        n(".search-input", e).typeahead({
            hint: !1,
            highlight: !0,
            menu: n(".search-results .search-results-list", e),
            classNames: {
                suggestion: "",
                cursor: "active"
            }
        }, i).on("typeahead:selected", function(e, t) {
            e.preventDefault(),
            window.location.replace(t.link)
        }).on("typeahead:open", function() {
            n(this).closest(".global-search").addClass("focused")
        }).on("typeahead:close", function() {
            n(this).closest(".global-search").removeClass("focused")
        }).on("keyup", function(e) {
            27 === e.keyCode ? n(this).closest("form").trigger("reset") : n(this).typeahead("val") ? n(this).closest(".global-search").addClass("has-input") : n(this).closest(".global-search").removeClass("has-input")
        }),
        n("form", e).on("submit", function() {
            return !1
        }).on("reset", function() {
            n(".search-input", this).typeahead("val", ""),
            n(this).closest(".global-search").removeClass("has-input")
        }),
        n(".search-reset", e).on("click", function() {
            n(this).closest("form").trigger("reset"),
            n(".search-input", e).focus()
        })
    }
    n(".dropdown.yamm-fw").each(function() {
        var e = n(".dropdown-toggle", n(this)).html()
          , t = n(".dropdown-menu li a", n(this))
          , a = [];
        t.each(function() {
            a.push({
                title: n(this).html(),
                link: n(this).attr("href")
            })
        }),
        a.length && (t = new Bloodhound({
            initialize: !0,
            local: a,
            identify: function(e) {
                return e.link
            },
            datumTokenizer: Bloodhound.tokenizers.obj.whitespace("title"),
            queryTokenizer: Bloodhound.tokenizers.whitespace
        }),
        i.push({
            display: "title",
            source: t,
            templates: {
                empty: function() {
                    return ['<li class="search-result-header">', e, "</li>", "<li>", window.translations["global-search"]["nothing-found"], "</li>"].join("")
                },
                header: function() {
                    return ['<li class="search-result-header">', e, "</li>"].join("")
                },
                dataset: "<ul><ul>",
                suggestion: function(e) {
                    return '<li><a href="' + e.link + '">' + e.title + "</a></li>"
                }
            }
        }))
    }),
    e(n(".global-search-standard")),
    e(n(".global-search-mobile")),
    n(".nav-mobile .nav-mobile-menu").parent().on("show.bs.dropdown", function() {
        setTimeout(function() {
            n(".nav-mobile .search-input.tt-input").val(null).focus()
        }, 100)
    })
}(jQuery),
function(e) {
    "use strict";
    e(document).ready(function() {
        e("select").chosen({
            disable_search_threshold: 10,
            width: "auto"
        })
    })
}(jQuery),
function(e) {
    "use strict";
    e("a.social-sharing-facebook").click(function() {
        return window.open("https://www.facebook.com/sharer/sharer.php?u=" + encodeURIComponent(location.href), "facebook-share-dialog", "width=626,height=436"),
        !1
    }),
    e("a.social-sharing-twitter").click(function() {
        return window.open("http://twitter.com/share?url=" + encodeURIComponent(location.href), "twitter-share-dialog", "width=626,height=436"),
        !1
    }),
    e("a.social-sharing-google").click(function() {
        return window.open("https://plus.google.com/share?url=" + encodeURIComponent(location.href), "google-share-dialog", "menubar=no,toolbar=no,resizable=yes,scrollbars=yes,height=600,width=600"),
        !1
    }),
    e("a.social-sharing-xing").click(function() {
        return window.open("https://www.xing.com/spi/shares/new?url=" + encodeURIComponent(location.href), "xing-share-dialog", "width=626,height=436"),
        !1
    }),
    e("a.social-sharing-linkedin").click(function() {
        return window.open("https://www.linkedin.com/cws/share?url=" + encodeURIComponent(location.href), "linkedin-share-dialog", "width=626,height=436"),
        !1
    })
}(jQuery),
function(e) {
    "use strict";
    subNavInit(jQuery),
    e(window).resize(function() {
        subNavInit(jQuery)
    }),
    e('a[href="#collapseSubNav"]').on("click", function() {
        e(this).attr("aria-expanded", "true" === e(this).attr("aria-expanded") ? "false" : "true")
    })
}(jQuery),
function() {
    "use strict";
    jQuery(".table-sort").tablesorter({
        sortLocaleCompare: !0
    })
}(),
function(a) {
    "use strict";
    a(".treecrumb").each(function() {
        var t = a(this);
        t.on("hide.bs.dropdown", function() {
            t.find(".dropdown-toggle span").removeClass("icon--bottom"),
            t.find(".dropdown-toggle span").addClass("icon--right")
        }),
        t.on("show.bs.dropdown", function(e) {
            e = e.relatedTarget;
            t.find(".dropdown-toggle span").removeClass("icon--bottom"),
            t.find(".dropdown-toggle span").addClass("icon--right"),
            a(e).find("span").removeClass("icon--right"),
            a(e).find("span").addClass("icon--bottom")
        })
    })
}(jQuery),
function(l) {
    "use strict";
    var e, s = {
        aria: function() {
            l("#ariaGallery").ariaLightbox({
                imageArray: "a.thumbnail-aria",
                altText: function() {
                    return l(this).find("img").attr("alt")
                },
                descText: function() {
                    return l(this).find("img").attr("title")
                },
                useDimmer: !0,
                pos: "auto",
                titleText: function() {
                    return l(this).find("img").attr("alt")
                },
                em: !1
            })
        },
        getBreakPoint: function() {
            return window.getComputedStyle(document.querySelector("body"), ":before").getPropertyValue("content").replace(/\"/g, "")
        },
        thumbnailResolution: function(e) {
            var t = l(e).find("img[data-src]");
            "smartphone" === this.getBreakPoint() && 0 < t.length && (t.each(function(e, t) {
                t.src = t.getAttribute("data-src")
            }),
            l(e).addClass("bigthumbs"))
        },
        waitAndCallThumbnailResolution: function(e, t) {
            for (var a = e, n = 0, i = 0; i < a.length; i++)
                -1 < a[i].className.indexOf("done") && -1 === a[i].className.indexOf("bigthumbs") && (s.thumbnailResolution(a[i]),
                n += 1);
            a.length === n && t()
        },
        on: function() {
            l(".mod.mod-ws_composed_gallery").find(".fancybox-buttons").fancybox({
                type: "image",
                closeBtn: !0,
                arrows: !0,
                nextClick: !0,
                helpers: {
                    title: {
                        type: "inside"
                    },
                    media: {},
                    buttons: {}
                },
                afterLoad: function() {
                    var e = "";
                    e = (e = l(".js-detail-link" + l(this.element).data("id")).html()).replace("##pos##", this.index + 1).replace("##length##", this.group.length).replace("##title##", this.title || "").replace("##url##", l(this.element).data("detail-url") || ""),
                    this.title = e
                }
            })
        },
        after: function() {
            function e(r) {
                var e = l(".mod-ws_composed_gallery figcaption");
                e && e.each(function(e, t) {
                    var t = l(t)
                      , a = t.text()
                      , n = t.attr("text-save")
                      , i = parseInt(t.width())
                      , s = parseInt(t.height())
                      , o = parseFloat(t.css("lineHeight"))
                      , i = i < 100 ? 45 : 53;
                    r ? (t.removeClass("height"),
                    t.text(n),
                    o = parseFloat(t.css("lineHeight")),
                    s = parseInt(t.height()),
                    a = n) : t.attr("text-save", a),
                    s > Math.ceil(4 * o) ? t.html("<h5>" + a.substr(0, i).replace("...", "") + "...</h5>") : t.html("<h5>" + a + "</h5>"),
                    t.addClass("height")
                })
            }
            e(),
            l(window).resize(function() {
                clearTimeout(void 0);
                window.setTimeout(function() {
                    e(!0)
                }, 500)
            })
        }
    }, t = setInterval(function() {
        var e = document.getElementsByClassName("mod-dynamic")[0];
        e && 0 < e.className.indexOf(" done") && (s.on(),
        clearInterval(t))
    }, 100);
    s.after(),
    document.getElementsByClassName("mod-dynamic").length && (e = window.setInterval(function() {
        s.waitAndCallThumbnailResolution(document.getElementsByClassName("mod-dynamic"), function() {
            window.clearInterval(e)
        })
    }, 10))
}(jQuery),
function(a) {
    "use strict";
    function n(e) {
        var t;
        e.preventDefault(),
        "keypress" === e.type && 13 !== e.keyCode || (t = e.target,
        o = document.querySelector(i.aramis + '[data-component-id="' + r(t) + '"]'),
        null != (t = a(e.target, o).data("collaps-id")) && (t = t,
        a(e.target, o).toggleClass(s.legendCollapsable),
        a(i.collapsableArea + '[data-collaps-id="' + t + '"]', o).toggleClass(s.collapsableArea)))
    }
    var i = {
        aramis: ".js-aramis",
        collapsableArea: ".js-collapsable-area",
        attachListener: ".js-attach-listener"
    }
      , s = {
        legendCollapsable: "m-aramis__legend-collapsable--open",
        collapsableArea: "m-aramis__collapsable-area--open"
    }
      , o = null
      , r = function(e) {
        for (; e.parentNode; ) {
            if (e.hasAttribute("data-component-id"))
                return e.getAttribute("data-component-id");
            e = e.parentNode
        }
        return null
    }
      , l = (a(i.attachListener).each(function(e, t) {
        a(t).on("click.aramis", n),
        a(t).on("keypress.aramis", n)
    }),
    new MutationObserver(function(e) {
        e.forEach(function(a) {
            Array.prototype.slice.call(a.addedNodes).forEach(function(e) {
                var t = a.target.lastChild.previousSibling;
                c(t)
            })
        })
    }
    ))
      , c = function(e) {
        var t, a, n;
        e && (e = e,
        n = parseInt(e.lastChild.dataset.optionArrayIndex),
        t = (n = e.parentNode.parentNode.previousSibling.options[n]).dataset.fcolor,
        a = n.dataset.bgcolor,
        (n = n.dataset.abbr || !1) && (e.childNodes[0].innerHTML = n),
        e.style.color = t,
        e.style.backgroundColor = a)
    }
      , d = {
        childList: !0,
        subtree: !1,
        attributes: !1,
        characterData: !1
    };
    window.setTimeout(function() {
        jQuery(".chosen-choices").each(function(e, t) {
            a(t).find(".search-choice").each(function(e, t) {
                c(t)
            }),
            l.observe(t, d)
        })
    }, 300)
}(jQuery),
function(s, o) {
    "use strict";
    s(".form-search, .mod-searchfield-mobile").each(function(e, t) {
        var a, t = s(t), n = s(".js-form-control", t), i = (n.attr("placeholder"),
        n.hasClass("type-ahead"));
        o && i && ((a = new Bloodhound({
            datumTokenizer: Bloodhound.tokenizers.obj.whitespace("value"),
            queryTokenizer: Bloodhound.tokenizers.whitespace,
            local: s.map(o, function(e) {
                return {
                    value: e
                }
            })
        })).initialize(),
        n.typeahead({
            hint: !0,
            highlight: !0,
            minLength: 1
        }, {
            name: "search",
            displayKey: "value",
            source: a.ttAdapter()
        })),
        t.find(".icon--close").click(function() {
            var e = s(this).parent().find(".form-control");
            return e.focus(),
            i ? e.typeahead("val", "dd") : e[0].value = "",
            !1
        })
    })
}(jQuery, "undefined" != typeof searchData && searchData),
function(n) {
    "use strict";
    n(window).load(function() {
        carouselInit(jQuery)
    });
    var i = 0;
    n(".carousel-slideshow").each(function(e, t) {
        i = n(t).find(".item").length,
        n(t).find(".carousel-total").text(i)
    }),
    n(".carousel-slideshow").on("slid.bs.carousel", function() {
        var e = n(this)
          , t = e.find(".carousel-controls")
          , a = e.find(".carousel-inner .item.active")
          , a = (0 === a.nextAll().length ? t.find(".right").addClass("disabled").attr("aria-disabled", "true") : t.find(".right").removeClass("disabled").attr("aria-disabled", "false"),
        0 === a.prevAll().length ? t.find(".left").addClass("disabled").attr("aria-disabled", "true") : t.find(".left").removeClass("disabled").attr("aria-disabled", "false"),
        n(this).find(".carousel-total").text(i),
        e.data("bs.carousel"))
          , t = a.getItemIndex(a.$element.find(".item.active"))
          , a = a.$items.length
          , t = t + 1;
        e.find(".carousel-index").text(t),
        e.find(".carousel-total").text(a),
        disableControl(e)
    })
}(jQuery),
function(a) {
    "use strict";
    function e(e, t) {
        i ? a.addEventListener(e, t) : a.attachEvent("on" + e, t)
    }
    function t(e) {
        n.styleSheet ? n.styleSheet.cssText = e : n.innerHTML = e
    }
    var n = a.createElement("STYLE")
      , i = "addEventListener"in a;
    a.getElementsByTagName("HEAD")[0].appendChild(n),
    e("mousedown", function() {
        t(".chosen-container-active .chosen-single,:focus{outline:0!important}::-moz-focus-inner,.chosen-container-active .chosen-single{border:0!important}")
    }),
    e("mousemove", function() {
        t(".chosen-container-active .chosen-single:focus,:focus{outline:0!important}::-moz-focus-inner,.chosen-container-active .chosen-single:focus {border:0!important}")
    }),
    e("keydown", function() {
        t("")
    })
}(document),
function(r) {
    "use strict";
    var e = r("body")
      , l = null
      , c = r("#content textarea")
      , d = e.find(".nav-mobile, textarea, .drilldown, .nav-main, .header-separator, .nav-service, .nav-lang, .form-search, .yamm--select, footer, .alert, .icon--print, .social-sharing, .nav-process, .carousel-indicators, .carousel-control, .breadcrumb, .pagination-container");
    r.fn.printPreview = function() {
        return this
    }
    ,
    r.printPreview = {
        printPreview: function(e) {
            var t = r("body")
              , a = r(".container-main")
              , n = r("#content")
              , i = ""
              , s = 0
              , o = (l = r('<div class="container container-main print-element"></div>'),
            d.hide(),
            e && (e = r("[data-print=" + e + "]").clone(),
            r("header").clone(),
            o = e.attr("data-title") ? "<h1>" + e.attr("data-title") + "</h1>" : "",
            l.html("").append(o, e),
            l.insertBefore(a),
            a.hide()),
            t.addClass("print-preview"),
            r('<div class="row" id="print-settings"><div class="col-sm-12"><nav class="pagination-container clearfix"><span class="pull-left"><input type="checkbox" id="footnote-links">&nbsp;&nbsp;<label for="footnote-links">Links as footnotes</label>&nbsp;&nbsp;&nbsp;&nbsp;<input type="checkbox" id="text-links">&nbsp;&nbsp;<label for="text-links">Links in text</label></span><ul class="pull-right"><li><button id="print-button" title="print" class="btn"><span class="icon icon--print"></span></button>&nbsp;&nbsp;<button id="close-button" title="close" class="btn btn-secondary"><span class="icon icon--close"></span></button></li></ul></nav></div></div>'));
            a.prepend(o),
            e && l.prepend(o),
            r("#print-button").click(function() {
                r.printPreview.printProcess()
            }),
            r("#close-button").click(function() {
                r.printPreview.printClose()
            }),
            r("#content a").not(".access-keys a").each(function() {
                var e = r(this).attr("href");
                "undefined" !== (e = String(e)) && 0 === e.indexOf("http") && (i += "<li>" + e + "</li>",
                r('<sup class="link-ref print-only">(' + ++s + ")</sup>").insertAfter(this))
            }),
            r("#footnote-links").change(function() {
                this.checked ? (a.append('<div id="footnote-links-wrapper" class="row footnote-links-wrapper"><div class="col-sm-12"><h3>Page Links</h3><hr><ol>' + i + "</ol></div></div>"),
                t.addClass("print-footnotes")) : (r("#footnote-links-wrapper").remove(),
                t.removeClass("print-footnotes"))
            }),
            r("#text-links").change(function() {
                this.checked ? n.addClass("show-link-urls") : n.removeClass("show-link-urls")
            }),
            c.each(function() {
                r('<div class="textarea-style print-only">' + r(this).val() + "</div>").insertBefore(r(this))
            })
        },
        printProcess: function() {
            window.print()
        },
        printClose: function() {
            r("#print-settings, #footnote-links-wrapper").remove(),
            r("#content").removeClass("show-link-urls"),
            e.removeClass("print-preview"),
            e.find(".print-only").remove(),
            l.remove(),
            r(".container-main").css("display", ""),
            d.css("display", "")
        }
    }
}(jQuery),
function(o) {
    "use strict";
    o(document).ready(function() {
        var e, n = !1, i = !1;
        function t() {
            a(),
            s()
        }
        function a() {
            var e, t, i, a = o(".tab-focus"), s = 0;
            a && o(window).width() <= 767 && !n ? (n = !0,
            0 <= (t = (t = document.location.pathname).substring(t.indexOf(".html"))).toLowerCase().indexOf("/teaserfocus") && (e = t.lastIndexOf("/") + 1,
            i = (t = t.substring(e)).substring(0, t.indexOf(".html"))),
            a.each(function() {
                var e, t = o(this), a = -1, n = (s += 1,
                t.attr("id", "tab-focus-" + s),
                t.next(".nav-tabs").hide(),
                t.addClass("carousel slide").removeClass("tab-content tab-border").attr("data-interval", 0),
                t.wrapInner('<div class="carousel-inner"></div>'),
                t.prepend('<ol class="carousel-indicators"></ol>'),
                -1);
                null !== i && (e = t.find('.carousel-inner div[id*="' + i + '"]')).length && 1 === e.length && (n = e.index() - 1),
                t.find(".tab-pane").each(function() {
                    a += 1,
                    o(this).removeClass("tab-pane in active").addClass("item"),
                    -1 !== n && a === n ? t.find(".carousel-indicators").append('<li class="active" data-target="#tab-focus-' + s + '" data-slide-to="' + a + '"></li>') : t.find(".carousel-indicators").append('<li data-target="#tab-focus-' + s + '" data-slide-to="' + a + '"></li>')
                }),
                e.length ? e.addClass("active") : (t.find(".item:first").addClass("active"),
                t.find(".carousel-indicators li:first-child").addClass("active")),
                t.append('<a class="left carousel-control icon icon--before icon--less" href="#tab-focus-' + s + '" data-slide="prev" aria-label="previous"></a><a class="right carousel-control icon icon--before icon--greater" href="#tab-focus-' + s + '" data-slide="next" aria-label="next"></a>')
            })) : a && 767 < o(window).width() && n && (n = !1,
            a.each(function() {
                var e = o(this);
                --s,
                e.attr("id", ""),
                e.next(".nav-tabs-focus").css("display", "flex"),
                e.removeClass("carousel slide").addClass("tab-content tab-border"),
                e.find("ol.carousel-indicators").remove(),
                e.find(".item").each(function() {
                    o(this).addClass("tab-pane").removeClass("item"),
                    o(this).css("height", "auto")
                }),
                e.find(".tab-pane:first-child").addClass("active in"),
                e.find(".tab-pane").parent().hasClass("carousel-inner") && e.find(".tab-pane").unwrap(),
                e.find(".carousel-control").remove()
            }))
        }
        function s() {
            var e = o(".nav-tabs:not(.focus)")
              , t = o(".collapsify")
              , a = 0;
            e && o(window).width() <= 767 && !i ? (i = !0,
            e.not(".tab-focus").each(function() {
                var e = o(this);
                e.removeClass("nav-tabs").addClass("collapsify"),
                e.next(".tab-content").hide(),
                e.find("a").each(function() {
                    var e = o(this).attr("href");
                    a += 1,
                    o(this).unwrap(),
                    o('<div class="collapse" id="collapse-' + a + '">' + o(e).html() + "</div>").insertAfter(this),
                    o(this).attr("data-toggle", "collapse"),
                    o(this).attr("data-target", "#collapse-" + a),
                    o(this).addClass("collapse-closed"),
                    o(this).click(function() {
                        o(this).toggleClass("collapse-closed")
                    })
                })
            })) : t && 767 < o(window).width() && i && (i = !1,
            t.each(function() {
                var e = o(this);
                e.addClass("nav-tabs").removeClass("collapsify"),
                e.next(".tab-content").show(),
                e.find("a").each(function() {
                    --a,
                    o(this).wrap("<li></li>"),
                    o(this).parent().next(".collapse").remove(),
                    o(this).attr("data-toggle", "tab"),
                    o(this).attr("data-target", ""),
                    o(this).removeClass("collapse-closed")
                }),
                e.find("li a").each(function() {
                    var e = o(this).attr("href");
                    o(e).hasClass("active") && o(this).parent().addClass("active")
                })
            }))
        }
        a(),
        s(),
        o(window).resize(function() {
            clearTimeout(e),
            e = setTimeout(t, 500)
        })
    })
}(jQuery),
function(t) {
    "use strict";
    function e(e) {
        this.$wrapper = t(e).parent(),
        this.domNodes = ".tab-focus, .nav-tabs-focus",
        this.delay = t(e).data("interval") || 0,
        this.playing = null,
        this.interval = null,
        this.$wrapper.on("click", ".nav-tabs-focus", function() {
            this.pause(null, !0)
        }
        .bind(this)).on("click", ".tab-focus-control", function() {
            this.playing ? this.pause(null, !0) : this.play(null, !0)
        }
        .bind(this)),
        this.play(null, !0)
    }
    e.prototype = {
        addListeners: function() {
            this.$wrapper.on("mouseenter.tabfocus focus.tabfocus", this.domNodes, this.pause.bind(this)).on("mouseleave.tabfocus blur.tabfocus", this.domNodes, this.play.bind(this))
        },
        removeListeners: function() {
            this.$wrapper.off("mouseenter.tabfocus focus.tabfocus", this.domNodes).off("mouseleave.tabfocus blur.tabfocus", this.domNodes)
        },
        play: function(e, t) {
            this.interval && clearInterval(this.interval),
            0 < this.delay && (this.interval = setInterval(this.slide.bind(this), this.delay)),
            t && (this.playing = !0,
            this.addListeners(),
            this.$wrapper.find(".tab-focus-control .icon").removeClass("icon--play").addClass("icon--pause"))
        },
        pause: function(e, t) {
            clearInterval(this.interval),
            t && (this.playing = !1,
            this.removeListeners(),
            this.$wrapper.find(".tab-focus-control .icon").removeClass("icon--pause").addClass("icon--play"))
        },
        slide: function() {
            var e, t = this.$wrapper.find(".nav-tabs-focus");
            if (t.is(":hidden"))
                return this.pause(null, !0);
            t.find("> li").length && ((e = (t = this.$wrapper.find(".nav-tabs-focus > li")).filter(".active").next("li")).length ? e : t.eq(0)).find("a").tab("show")
        }
    },
    t.fn.tabFocus = function() {
        return this.each(function() {
            t.data(this, "TabFocus") || t.data(this, "TabFocus", new e(this))
        })
    }
    ,
    t(".tab-focus").tabFocus()
}(jQuery),
function(t) {
    "use strict";
    var a = {
        en: {
            previousMonth: "Previous Month",
            nextMonth: "Next Month",
            months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
            weekdays: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
            weekdaysShort: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
        },
        de: {
            previousMonth: "Vorheriger Monat",
            nextMonth: "Nächster Monat",
            months: ["Januar", "Februar", "M&auml;rz", "April", "Mai", "Juni", "Juli", "August", "September", "Oktober", "November", "Dezember"],
            weekdays: ["Sonntag", "Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag", "Samstag"],
            weekdaysShort: ["So.", "Mo.", "Di.", "Mi.", "Do.", "Fr.", "Sa."]
        },
        fr: {
            previousMonth: "Pr&eacute;c&eacute;dent",
            nextMonth: "Suivant",
            months: ["janvier", "f&eacute;vrier", "mars", "avril", "mai", "juin", "juillet", "ao&ucirc;t", "septembre", "octobre", "novembre", "d&eacute;cembre"],
            weekdays: ["dimanche", "lundi", "mardi", "mercredi", "jeudi", "vendredi", "samedi"],
            weekdaysShort: ["dim.", "lun.", "mar.", "mer.", "jeu.", "ven.", "sam."]
        },
        it: {
            previousMonth: "&#x3C;Prec",
            nextMonth: "Succ&#x3E;",
            months: ["Gennaio", "Febbraio", "Marzo", "Aprile", "Maggio", "Giugno", "Luglio", "Agosto", "Settembre", "Ottobre", "Novembre", "Dicembre"],
            weekdays: ["Domenica", "Luned&igrave;", "Marted&igrave;", "Mercoled&igrave;", "Gioved&igrave;", "Venerd&igrave;", "Sabato"],
            weekdaysShort: ["Dom", "Lun", "Mar", "Mer", "Gio", "Ven", "Sab"]
        }
    }
      , e = t("input.datumfeld, .js-datepicker, .js-datepicker-to, .js-datepicker-from")
      , n = t("html").attr("lang") || "en";
    e.each(function() {
        var e = t(this).data("format") || "DD.MM.YYYY";
        t(this).attr("title", "Format " + e.toLowerCase()),
        new Pikaday({
            field: t(this)[0],
            i18n: a[n],
            format: e
        });
        t(this).data("picker-instance")
    })
}(jQuery),
function(l) {
    "use strict";
    l(document).ready(function() {
        l(".js-expandable").each(function(e, t) {
            var a, n, i = l(this), s = i.data("expandable-ellipsis") || "...", o = i.data("expandable-ellipsis-prefix") || "", r = parseInt(i.data("expandable-lines") || 3);
            s = s,
            o = o,
            i = function(e, t) {
                return parseInt(e.css("line-height"), 10) * t
            }(a = i, i = r),
            n = a.attr("style"),
            a.show().dotdotdot({
                ellipsis: o,
                wrap: "word",
                fallbackToLetter: !0,
                height: i,
                after: l('<a href="#" class="expandableClickHandler">' + s + "</a>"),
                callback: function(e, t) {
                    l(".expandableClickHandler", a).on("click", function(e) {
                        e.preventDefault(),
                        a.trigger("destroy.dot"),
                        a.attr("style", n).show(300)
                    })
                }
            })
        })
    })
}(jQuery),
function(e) {
    e(function() {
        e("img[usemap]").length && e("img[usemap]").rwdImageMaps()
    })
}(jQuery),
function(l) {
    "use strict";
    var a = {
        on: function(e) {
            var e = l(e)
              , a = this;
            a.checkRecentIcon(),
            e.find(".table-sort").each(function(e, t) {
                a.removeEmptyCells(t)
            })
        },
        after: function() {},
        checkRecentIcon: function() {
            l(this).find("[data-recent-until]").each(function() {
                var e = l(this)
                  , t = l(this).siblings("span")
                  , a = e.data("recent-until")
                  , n = (new Date).getTime();
                0 !== a.length && a < n && (e.remove(),
                t.remove())
            })
        },
        removeEmptyCells: function(e) {
            for (var e = l(e), t = e.find("thead"), a = e.find("tbody"), n = "", i = "", s = new Array(0), o = 1; o < a.find("tr:nth(0) td").length + 1; o++)
                s[o] = !0;
            a.find("td").each(function() {
                var e = l(this);
                0 === e.html().trim().length && s[e.index()] ? s[e.index()] = !0 : s[e.index()] = !1
            });
            for (var r = 1; r < s.length + 1; r++)
                s[r] && (n += "th:nth-child(" + (parseInt(r, 10) + 1) + "),",
                i += "td:nth-child(" + (parseInt(r, 10) + 1) + "),");
            t.find(n.slice(0, -1)).remove(),
            a.find(i.slice(0, -1)).remove()
        }
    };
    l(".mod.mod-indicator_table").each(function(e, t) {
        a.on(t)
    })
}(jQuery),
jQuery;
/*!
 * jQuery Validation Plugin v1.13.1
 *
 * http://jqueryvalidation.org/
 *
 * Copyright (c) 2014 Jörn Zaefferer
 * Released under the MIT license
 */
(function(factory) {
    if (typeof define === "function" && define.amd) {
        define(["jquery"], factory);
    } else {
        factory(jQuery);
    }
}(function($) {

    $.extend($.fn, {
        // http://jqueryvalidation.org/validate/
        validate: function(options) {

            // if nothing is selected, return nothing; can't chain anyway
            if (!this.length) {
                if (options && options.debug && window.console) {
                    console.warn("Nothing selected, can't validate, returning nothing.");
                }
                return;
            }

            // check if a validator for this form was already created
            var validator = $.data(this[0], "validator");
            if (validator) {
                return validator;
            }

            // Add novalidate tag if HTML5.
            this.attr("novalidate", "novalidate");

            validator = new $.validator(options,this[0]);
            $.data(this[0], "validator", validator);

            if (validator.settings.onsubmit) {

                this.validateDelegate(":submit", "click", function(event) {
                    if (validator.settings.submitHandler) {
                        validator.submitButton = event.target;
                    }
                    // allow suppressing validation by adding a cancel class to the submit button
                    if ($(event.target).hasClass("cancel")) {
                        validator.cancelSubmit = true;
                    }

                    // allow suppressing validation by adding the html5 formnovalidate attribute to the submit button
                    if ($(event.target).attr("formnovalidate") !== undefined) {
                        validator.cancelSubmit = true;
                    }
                });

                // validate the form on submit
                this.submit(function(event) {
                    if (validator.settings.debug) {
                        // prevent form submit to be able to see console output
                        event.preventDefault();
                    }
                    function handle() {
                        var hidden, result;
                        if (validator.settings.submitHandler) {
                            if (validator.submitButton) {
                                // insert a hidden input as a replacement for the missing submit button
                                hidden = $("<input type='hidden'/>").attr("name", validator.submitButton.name).val($(validator.submitButton).val()).appendTo(validator.currentForm);
                            }
                            result = validator.settings.submitHandler.call(validator, validator.currentForm, event);
                            if (validator.submitButton) {
                                // and clean up afterwards; thanks to no-block-scope, hidden can be referenced
                                hidden.remove();
                            }
                            if (result !== undefined) {
                                return result;
                            }
                            return false;
                        }
                        return true;
                    }

                    // prevent submit for invalid forms or custom submit handlers
                    if (validator.cancelSubmit) {
                        validator.cancelSubmit = false;
                        return handle();
                    }
                    if (validator.form()) {
                        if (validator.pendingRequest) {
                            validator.formSubmitted = true;
                            return false;
                        }
                        return handle();
                    } else {
                        validator.focusInvalid();
                        return false;
                    }
                });
            }

            return validator;
        },
        // http://jqueryvalidation.org/valid/
        valid: function() {
            var valid, validator;

            if ($(this[0]).is("form")) {
                valid = this.validate().form();
            } else {
                valid = true;
                validator = $(this[0].form).validate();
                this.each(function() {
                    valid = validator.element(this) && valid;
                });
            }
            return valid;
        },
        // attributes: space separated list of attributes to retrieve and remove
        removeAttrs: function(attributes) {
            var result = {}
              , $element = this;
            $.each(attributes.split(/\s/), function(index, value) {
                result[value] = $element.attr(value);
                $element.removeAttr(value);
            });
            return result;
        },
        // http://jqueryvalidation.org/rules/
        rules: function(command, argument) {
            var element = this[0], settings, staticRules, existingRules, data, param, filtered;

            if (command) {
                settings = $.data(element.form, "validator").settings;
                staticRules = settings.rules;
                existingRules = $.validator.staticRules(element);
                switch (command) {
                case "add":
                    $.extend(existingRules, $.validator.normalizeRule(argument));
                    // remove messages from rules, but allow them to be set separately
                    delete existingRules.messages;
                    staticRules[element.name] = existingRules;
                    if (argument.messages) {
                        settings.messages[element.name] = $.extend(settings.messages[element.name], argument.messages);
                    }
                    break;
                case "remove":
                    if (!argument) {
                        delete staticRules[element.name];
                        return existingRules;
                    }
                    filtered = {};
                    $.each(argument.split(/\s/), function(index, method) {
                        filtered[method] = existingRules[method];
                        delete existingRules[method];
                        if (method === "required") {
                            $(element).removeAttr("aria-required");
                        }
                    });
                    return filtered;
                }
            }

            data = $.validator.normalizeRules($.extend({}, $.validator.classRules(element), $.validator.attributeRules(element), $.validator.dataRules(element), $.validator.staticRules(element)), element);

            // make sure required is at front
            if (data.required) {
                param = data.required;
                delete data.required;
                data = $.extend({
                    required: param
                }, data);
                $(element).attr("aria-required", "true");
            }

            // make sure remote is at back
            if (data.remote) {
                param = data.remote;
                delete data.remote;
                data = $.extend(data, {
                    remote: param
                });
            }

            return data;
        }
    });

    // Custom selectors
    $.extend($.expr[":"], {
        // http://jqueryvalidation.org/blank-selector/
        blank: function(a) {
            return !$.trim("" + $(a).val());
        },
        // http://jqueryvalidation.org/filled-selector/
        filled: function(a) {
            return !!$.trim("" + $(a).val());
        },
        // http://jqueryvalidation.org/unchecked-selector/
        unchecked: function(a) {
            return !$(a).prop("checked");
        }
    });

    // constructor for validator
    $.validator = function(options, form) {
        this.settings = $.extend(true, {}, $.validator.defaults, options);
        this.currentForm = form;
        this.init();
    }
    ;

    // http://jqueryvalidation.org/jQuery.validator.format/
    $.validator.format = function(source, params) {
        if (arguments.length === 1) {
            return function() {
                var args = $.makeArray(arguments);
                args.unshift(source);
                return $.validator.format.apply(this, args);
            }
            ;
        }
        if (arguments.length > 2 && params.constructor !== Array) {
            params = $.makeArray(arguments).slice(1);
        }
        if (params.constructor !== Array) {
            params = [params];
        }
        $.each(params, function(i, n) {
            source = source.replace(new RegExp("\\{" + i + "\\}","g"), function() {
                return n;
            });
        });
        return source;
    }
    ;

    $.extend($.validator, {

        defaults: {
            messages: {},
            groups: {},
            rules: {},
            errorClass: "error",
            validClass: "valid",
            errorElement: "label",
            focusCleanup: false,
            focusInvalid: true,
            errorContainer: $([]),
            errorLabelContainer: $([]),
            onsubmit: true,
            ignore: ":hidden",
            ignoreTitle: false,
            onfocusin: function(element) {
                this.lastActive = element;

                // Hide error label and remove error class on focus if enabled
                if (this.settings.focusCleanup) {
                    if (this.settings.unhighlight) {
                        this.settings.unhighlight.call(this, element, this.settings.errorClass, this.settings.validClass);
                    }
                    this.hideThese(this.errorsFor(element));
                }
            },
            onfocusout: function(element) {
                if (!this.checkable(element) && (element.name in this.submitted || !this.optional(element))) {
                    this.element(element);
                }
            },
            onkeyup: function(element, event) {
                if (event.which === 9 && this.elementValue(element) === "") {
                    return;
                } else if (element.name in this.submitted || element === this.lastElement) {
                    this.element(element);
                }
            },
            onclick: function(element) {
                // click on selects, radiobuttons and checkboxes
                if (element.name in this.submitted) {
                    this.element(element);

                    // or option elements, check parent select in that case
                } else if (element.parentNode.name in this.submitted) {
                    this.element(element.parentNode);
                }
            },
            highlight: function(element, errorClass, validClass) {
                if (element.type === "radio") {
                    this.findByName(element.name).addClass(errorClass).removeClass(validClass);
                } else {
                    $(element).addClass(errorClass).removeClass(validClass);
                }
            },
            unhighlight: function(element, errorClass, validClass) {
                if (element.type === "radio") {
                    this.findByName(element.name).removeClass(errorClass).addClass(validClass);
                } else {
                    $(element).removeClass(errorClass).addClass(validClass);
                }
            }
        },

        // http://jqueryvalidation.org/jQuery.validator.setDefaults/
        setDefaults: function(settings) {
            $.extend($.validator.defaults, settings);
        },

        messages: {
            required: "This field is required.",
            remote: "Please fix this field.",
            email: "Please enter a valid email address.",
            url: "Please enter a valid URL.",
            date: "Please enter a valid date.",
            dateISO: "Please enter a valid date ( ISO ).",
            number: "Please enter a valid number.",
            digits: "Please enter only digits.",
            creditcard: "Please enter a valid credit card number.",
            equalTo: "Please enter the same value again.",
            maxlength: $.validator.format("Please enter no more than {0} characters."),
            minlength: $.validator.format("Please enter at least {0} characters."),
            rangelength: $.validator.format("Please enter a value between {0} and {1} characters long."),
            range: $.validator.format("Please enter a value between {0} and {1}."),
            max: $.validator.format("Please enter a value less than or equal to {0}."),
            min: $.validator.format("Please enter a value greater than or equal to {0}.")
        },

        autoCreateRanges: false,

        prototype: {

            init: function() {
                this.labelContainer = $(this.settings.errorLabelContainer);
                this.errorContext = this.labelContainer.length && this.labelContainer || $(this.currentForm);
                this.containers = $(this.settings.errorContainer).add(this.settings.errorLabelContainer);
                this.submitted = {};
                this.valueCache = {};
                this.pendingRequest = 0;
                this.pending = {};
                this.invalid = {};
                this.reset();

                var groups = (this.groups = {}), rules;
                $.each(this.settings.groups, function(key, value) {
                    if (typeof value === "string") {
                        value = value.split(/\s/);
                    }
                    $.each(value, function(index, name) {
                        groups[name] = key;
                    });
                });
                rules = this.settings.rules;
                $.each(rules, function(key, value) {
                    rules[key] = $.validator.normalizeRule(value);
                });

                function delegate(event) {
                    var validator = $.data(this[0].form, "validator")
                      , eventType = "on" + event.type.replace(/^validate/, "")
                      , settings = validator.settings;
                    if (settings[eventType] && !this.is(settings.ignore)) {
                        settings[eventType].call(validator, this[0], event);
                    }
                }
                $(this.currentForm).validateDelegate(":text, [type='password'], [type='file'], select, textarea, " + "[type='number'], [type='search'] ,[type='tel'], [type='url'], " + "[type='email'], [type='datetime'], [type='date'], [type='month'], " + "[type='week'], [type='time'], [type='datetime-local'], " + "[type='range'], [type='color'], [type='radio'], [type='checkbox']", "focusin focusout keyup", delegate)// Support: Chrome, oldIE
                // "select" is provided as event.target when clicking a option
                .validateDelegate("select, option, [type='radio'], [type='checkbox']", "click", delegate);

                if (this.settings.invalidHandler) {
                    $(this.currentForm).bind("invalid-form.validate", this.settings.invalidHandler);
                }

                // Add aria-required to any Static/Data/Class required fields before first validation
                // Screen readers require this attribute to be present before the initial submission http://www.w3.org/TR/WCAG-TECHS/ARIA2.html
                $(this.currentForm).find("[required], [data-rule-required], .required").attr("aria-required", "true");
            },

            // http://jqueryvalidation.org/Validator.form/
            form: function() {
                this.checkForm();
                $.extend(this.submitted, this.errorMap);
                this.invalid = $.extend({}, this.errorMap);
                if (!this.valid()) {
                    $(this.currentForm).triggerHandler("invalid-form", [this]);
                }
                this.showErrors();
                return this.valid();
            },

            checkForm: function() {
                this.prepareForm();
                for (var i = 0, elements = (this.currentElements = this.elements()); elements[i]; i++) {
                    this.check(elements[i]);
                }
                return this.valid();
            },

            // http://jqueryvalidation.org/Validator.element/
            element: function(element) {
                var cleanElement = this.clean(element)
                  , checkElement = this.validationTargetFor(cleanElement)
                  , result = true;

                this.lastElement = checkElement;

                if (checkElement === undefined) {
                    delete this.invalid[cleanElement.name];
                } else {
                    this.prepareElement(checkElement);
                    this.currentElements = $(checkElement);

                    result = this.check(checkElement) !== false;
                    if (result) {
                        delete this.invalid[checkElement.name];
                    } else {
                        this.invalid[checkElement.name] = true;
                    }
                }
                // Add aria-invalid status for screen readers
                $(element).attr("aria-invalid", !result);

                if (!this.numberOfInvalids()) {
                    // Hide error containers on last error
                    this.toHide = this.toHide.add(this.containers);
                }
                this.showErrors();
                return result;
            },

            // http://jqueryvalidation.org/Validator.showErrors/
            showErrors: function(errors) {
                if (errors) {
                    // add items to error list and map
                    $.extend(this.errorMap, errors);
                    this.errorList = [];
                    for (var name in errors) {
                        this.errorList.push({
                            message: errors[name],
                            element: this.findByName(name)[0]
                        });
                    }
                    // remove items from success list
                    this.successList = $.grep(this.successList, function(element) {
                        return !(element.name in errors);
                    });
                }
                if (this.settings.showErrors) {
                    this.settings.showErrors.call(this, this.errorMap, this.errorList);
                } else {
                    this.defaultShowErrors();
                }
            },

            // http://jqueryvalidation.org/Validator.resetForm/
            resetForm: function() {
                if ($.fn.resetForm) {
                    $(this.currentForm).resetForm();
                }
                this.submitted = {};
                this.lastElement = null;
                this.prepareForm();
                this.hideErrors();
                this.elements().removeClass(this.settings.errorClass).removeData("previousValue").removeAttr("aria-invalid");
            },

            numberOfInvalids: function() {
                return this.objectLength(this.invalid);
            },

            objectLength: function(obj) {
                /* jshint unused: false */
                var count = 0, i;
                for (i in obj) {
                    count++;
                }
                return count;
            },

            hideErrors: function() {
                this.hideThese(this.toHide);
            },

            hideThese: function(errors) {
                errors.not(this.containers).text("");
                this.addWrapper(errors).hide();
            },

            valid: function() {
                return this.size() === 0;
            },

            size: function() {
                return this.errorList.length;
            },

            focusInvalid: function() {
                if (this.settings.focusInvalid) {
                    try {
                        $(this.findLastActive() || this.errorList.length && this.errorList[0].element || []).filter(":visible").focus()// manually trigger focusin event; without it, focusin handler isn't called, findLastActive won't have anything to find
                        .trigger("focusin");
                    } catch (e) {// ignore IE throwing errors when focusing hidden elements
                    }
                }
            },

            findLastActive: function() {
                var lastActive = this.lastActive;
                return lastActive && $.grep(this.errorList, function(n) {
                    return n.element.name === lastActive.name;
                }).length === 1 && lastActive;
            },

            elements: function() {
                var validator = this
                  , rulesCache = {};

                // select all valid inputs inside the form (no submit or reset buttons)
                return $(this.currentForm).find("input, select, textarea").not(":submit, :reset, :image, [disabled], [readonly]").not(this.settings.ignore).filter(function() {
                    if (!this.name && validator.settings.debug && window.console) {
                        console.error("%o has no name assigned", this);
                    }

                    // select only the first element for each name, and only those with rules specified
                    if (this.name in rulesCache || !validator.objectLength($(this).rules())) {
                        return false;
                    }

                    rulesCache[this.name] = true;
                    return true;
                });
            },

            clean: function(selector) {
                return $(selector)[0];
            },

            errors: function() {
                var errorClass = this.settings.errorClass.split(" ").join(".");
                return $(this.settings.errorElement + "." + errorClass, this.errorContext);
            },

            reset: function() {
                this.successList = [];
                this.errorList = [];
                this.errorMap = {};
                this.toShow = $([]);
                this.toHide = $([]);
                this.currentElements = $([]);
            },

            prepareForm: function() {
                this.reset();
                this.toHide = this.errors().add(this.containers);
            },

            prepareElement: function(element) {
                this.reset();
                this.toHide = this.errorsFor(element);
            },

            elementValue: function(element) {
                var val, $element = $(element), type = element.type;

                if (type === "radio" || type === "checkbox") {
                    return $("input[name='" + element.name + "']:checked").val();
                } else if (type === "number" && typeof element.validity !== "undefined") {
                    return element.validity.badInput ? false : $element.val();
                }

                val = $element.val();
                if (typeof val === "string") {
                    return val.replace(/\r/g, "");
                }
                return val;
            },

            check: function(element) {
                element = this.validationTargetFor(this.clean(element));

                var rules = $(element).rules(), rulesCount = $.map(rules, function(n, i) {
                    return i;
                }).length, dependencyMismatch = false, val = this.elementValue(element), result, method, rule;

                for (method in rules) {
                    rule = {
                        method: method,
                        parameters: rules[method]
                    };
                    try {

                        result = $.validator.methods[method].call(this, val, element, rule.parameters);

                        // if a method indicates that the field is optional and therefore valid,
                        // don't mark it as valid when there are no other rules
                        if (result === "dependency-mismatch" && rulesCount === 1) {
                            dependencyMismatch = true;
                            continue;
                        }
                        dependencyMismatch = false;

                        if (result === "pending") {
                            this.toHide = this.toHide.not(this.errorsFor(element));
                            return;
                        }

                        if (!result) {
                            this.formatAndAdd(element, rule);
                            return false;
                        }
                    } catch (e) {
                        if (this.settings.debug && window.console) {
                            console.log("Exception occurred when checking element " + element.id + ", check the '" + rule.method + "' method.", e);
                        }
                        throw e;
                    }
                }
                if (dependencyMismatch) {
                    return;
                }
                if (this.objectLength(rules)) {
                    this.successList.push(element);
                }
                return true;
            },

            // return the custom message for the given element and validation method
            // specified in the element's HTML5 data attribute
            // return the generic message if present and no method specific message is present
            customDataMessage: function(element, method) {
                return $(element).data("msg" + method.charAt(0).toUpperCase() + method.substring(1).toLowerCase()) || $(element).data("msg");
            },

            // return the custom message for the given element name and validation method
            customMessage: function(name, method) {
                var m = this.settings.messages[name];
                return m && (m.constructor === String ? m : m[method]);
            },

            // return the first defined argument, allowing empty strings
            findDefined: function() {
                for (var i = 0; i < arguments.length; i++) {
                    if (arguments[i] !== undefined) {
                        return arguments[i];
                    }
                }
                return undefined;
            },

            defaultMessage: function(element, method) {
                return this.findDefined(this.customMessage(element.name, method), this.customDataMessage(element, method), // title is never undefined, so handle empty string as undefined
                !this.settings.ignoreTitle && element.title || undefined, $.validator.messages[method], "<strong>Warning: No message defined for " + element.name + "</strong>");
            },

            formatAndAdd: function(element, rule) {
                var message = this.defaultMessage(element, rule.method)
                  , theregex = /\$?\{(\d+)\}/g;
                if (typeof message === "function") {
                    message = message.call(this, rule.parameters, element);
                } else if (theregex.test(message)) {
                    message = $.validator.format(message.replace(theregex, "{$1}"), rule.parameters);
                }
                this.errorList.push({
                    message: message,
                    element: element,
                    method: rule.method
                });

                this.errorMap[element.name] = message;
                this.submitted[element.name] = message;
            },

            addWrapper: function(toToggle) {
                if (this.settings.wrapper) {
                    toToggle = toToggle.add(toToggle.parent(this.settings.wrapper));
                }
                return toToggle;
            },

            defaultShowErrors: function() {
                var i, elements, error;
                for (i = 0; this.errorList[i]; i++) {
                    error = this.errorList[i];
                    if (this.settings.highlight) {
                        this.settings.highlight.call(this, error.element, this.settings.errorClass, this.settings.validClass);
                    }
                    this.showLabel(error.element, error.message);
                }
                if (this.errorList.length) {
                    this.toShow = this.toShow.add(this.containers);
                }
                if (this.settings.success) {
                    for (i = 0; this.successList[i]; i++) {
                        this.showLabel(this.successList[i]);
                    }
                }
                if (this.settings.unhighlight) {
                    for (i = 0,
                    elements = this.validElements(); elements[i]; i++) {
                        this.settings.unhighlight.call(this, elements[i], this.settings.errorClass, this.settings.validClass);
                    }
                }
                this.toHide = this.toHide.not(this.toShow);
                this.hideErrors();
                this.addWrapper(this.toShow).show();
            },

            validElements: function() {
                return this.currentElements.not(this.invalidElements());
            },

            invalidElements: function() {
                return $(this.errorList).map(function() {
                    return this.element;
                });
            },

            showLabel: function(element, message) {
                var place, group, errorID, error = this.errorsFor(element), elementID = this.idOrName(element), describedBy = $(element).attr("aria-describedby");
                if (error.length) {
                    // refresh error/success class
                    error.removeClass(this.settings.validClass).addClass(this.settings.errorClass);
                    // replace message on existing label
                    error.html(message);
                } else {
                    // create error element
                    error = $("<" + this.settings.errorElement + ">").attr("id", elementID + "-error").addClass(this.settings.errorClass).html(message || "");

                    // Maintain reference to the element to be placed into the DOM
                    place = error;
                    if (this.settings.wrapper) {
                        // make sure the element is visible, even in IE
                        // actually showing the wrapped element is handled elsewhere
                        place = error.hide().show().wrap("<" + this.settings.wrapper + "/>").parent();
                    }
                    if (this.labelContainer.length) {
                        this.labelContainer.append(place);
                    } else if (this.settings.errorPlacement) {
                        this.settings.errorPlacement(place, $(element));
                    } else {
                        place.insertAfter(element);
                    }

                    // Link error back to the element
                    if (error.is("label")) {
                        // If the error is a label, then associate using 'for'
                        error.attr("for", elementID);
                    } else if (error.parents("label[for='" + elementID + "']").length === 0) {
                        // If the element is not a child of an associated label, then it's necessary
                        // to explicitly apply aria-describedby

                        errorID = error.attr("id").replace(/(:|\.|\[|\])/g, "\\$1");
                        // Respect existing non-error aria-describedby
                        if (!describedBy) {
                            describedBy = errorID;
                        } else if (!describedBy.match(new RegExp("\\b" + errorID + "\\b"))) {
                            // Add to end of list if not already present
                            describedBy += " " + errorID;
                        }
                        $(element).attr("aria-describedby", describedBy);

                        // If this element is grouped, then assign to all elements in the same group
                        group = this.groups[element.name];
                        if (group) {
                            $.each(this.groups, function(name, testgroup) {
                                if (testgroup === group) {
                                    $("[name='" + name + "']", this.currentForm).attr("aria-describedby", error.attr("id"));
                                }
                            });
                        }
                    }
                }
                if (!message && this.settings.success) {
                    error.text("");
                    if (typeof this.settings.success === "string") {
                        error.addClass(this.settings.success);
                    } else {
                        this.settings.success(error, element);
                    }
                }
                this.toShow = this.toShow.add(error);
            },

            errorsFor: function(element) {
                var name = this.idOrName(element)
                  , describer = $(element).attr("aria-describedby")
                  , selector = "label[for='" + name + "'], label[for='" + name + "'] *";

                // aria-describedby should directly reference the error element
                if (describer) {
                    selector = selector + ", #" + describer.replace(/\s+/g, ", #");
                }
                return this.errors().filter(selector);
            },

            idOrName: function(element) {
                return this.groups[element.name] || (this.checkable(element) ? element.name : element.id || element.name);
            },

            validationTargetFor: function(element) {

                // If radio/checkbox, validate first element in group instead
                if (this.checkable(element)) {
                    element = this.findByName(element.name);
                }

                // Always apply ignore filter
                return $(element).not(this.settings.ignore)[0];
            },

            checkable: function(element) {
                return (/radio|checkbox/i).test(element.type);
            },

            findByName: function(name) {
                return $(this.currentForm).find("[name='" + name + "']");
            },

            getLength: function(value, element) {
                switch (element.nodeName.toLowerCase()) {
                case "select":
                    return $("option:selected", element).length;
                case "input":
                    if (this.checkable(element)) {
                        return this.findByName(element.name).filter(":checked").length;
                    }
                }
                return value.length;
            },

            depend: function(param, element) {
                return this.dependTypes[typeof param] ? this.dependTypes[typeof param](param, element) : true;
            },

            dependTypes: {
                "boolean": function(param) {
                    return param;
                },
                "string": function(param, element) {
                    return !!$(param, element.form).length;
                },
                "function": function(param, element) {
                    return param(element);
                }
            },

            optional: function(element) {
                var val = this.elementValue(element);
                return !$.validator.methods.required.call(this, val, element) && "dependency-mismatch";
            },

            startRequest: function(element) {
                if (!this.pending[element.name]) {
                    this.pendingRequest++;
                    this.pending[element.name] = true;
                }
            },

            stopRequest: function(element, valid) {
                this.pendingRequest--;
                // sometimes synchronization fails, make sure pendingRequest is never < 0
                if (this.pendingRequest < 0) {
                    this.pendingRequest = 0;
                }
                delete this.pending[element.name];
                if (valid && this.pendingRequest === 0 && this.formSubmitted && this.form()) {
                    $(this.currentForm).submit();
                    this.formSubmitted = false;
                } else if (!valid && this.pendingRequest === 0 && this.formSubmitted) {
                    $(this.currentForm).triggerHandler("invalid-form", [this]);
                    this.formSubmitted = false;
                }
            },

            previousValue: function(element) {
                return $.data(element, "previousValue") || $.data(element, "previousValue", {
                    old: null,
                    valid: true,
                    message: this.defaultMessage(element, "remote")
                });
            }

        },

        classRuleSettings: {
            required: {
                required: true
            },
            email: {
                email: true
            },
            url: {
                url: true
            },
            date: {
                date: true
            },
            dateISO: {
                dateISO: true
            },
            number: {
                number: true
            },
            digits: {
                digits: true
            },
            creditcard: {
                creditcard: true
            }
        },

        addClassRules: function(className, rules) {
            if (className.constructor === String) {
                this.classRuleSettings[className] = rules;
            } else {
                $.extend(this.classRuleSettings, className);
            }
        },

        classRules: function(element) {
            var rules = {}
              , classes = $(element).attr("class");

            if (classes) {
                $.each(classes.split(" "), function() {
                    if (this in $.validator.classRuleSettings) {
                        $.extend(rules, $.validator.classRuleSettings[this]);
                    }
                });
            }
            return rules;
        },

        attributeRules: function(element) {
            var rules = {}, $element = $(element), type = element.getAttribute("type"), method, value;

            for (method in $.validator.methods) {

                // support for <input required> in both html5 and older browsers
                if (method === "required") {
                    value = element.getAttribute(method);
                    // Some browsers return an empty string for the required attribute
                    // and non-HTML5 browsers might have required="" markup
                    if (value === "") {
                        value = true;
                    }
                    // force non-HTML5 browsers to return bool
                    value = !!value;
                } else {
                    value = $element.attr(method);
                }

                // convert the value to a number for number inputs, and for text for backwards compability
                // allows type="date" and others to be compared as strings
                if (/min|max/.test(method) && (type === null || /number|range|text/.test(type))) {
                    value = Number(value);
                }

                if (value || value === 0) {
                    rules[method] = value;
                } else if (type === method && type !== "range") {
                    // exception: the jquery validate 'range' method
                    // does not test for the html5 'range' type
                    rules[method] = true;
                }
            }

            // maxlength may be returned as -1, 2147483647 ( IE ) and 524288 ( safari ) for text inputs
            if (rules.maxlength && /-1|2147483647|524288/.test(rules.maxlength)) {
                delete rules.maxlength;
            }

            return rules;
        },

        dataRules: function(element) {
            var method, value, rules = {}, $element = $(element);
            for (method in $.validator.methods) {
                value = $element.data("rule" + method.charAt(0).toUpperCase() + method.substring(1).toLowerCase());
                if (value !== undefined) {
                    rules[method] = value;
                }
            }
            return rules;
        },

        staticRules: function(element) {
            var rules = {}
              , validator = $.data(element.form, "validator");

            if (validator.settings.rules) {
                rules = $.validator.normalizeRule(validator.settings.rules[element.name]) || {};
            }
            return rules;
        },

        normalizeRules: function(rules, element) {
            // handle dependency check
            $.each(rules, function(prop, val) {
                // ignore rule when param is explicitly false, eg. required:false
                if (val === false) {
                    delete rules[prop];
                    return;
                }
                if (val.param || val.depends) {
                    var keepRule = true;
                    switch (typeof val.depends) {
                    case "string":
                        keepRule = !!$(val.depends, element.form).length;
                        break;
                    case "function":
                        keepRule = val.depends.call(element, element);
                        break;
                    }
                    if (keepRule) {
                        rules[prop] = val.param !== undefined ? val.param : true;
                    } else {
                        delete rules[prop];
                    }
                }
            });

            // evaluate parameters
            $.each(rules, function(rule, parameter) {
                rules[rule] = $.isFunction(parameter) ? parameter(element) : parameter;
            });

            // clean number parameters
            $.each(["minlength", "maxlength"], function() {
                if (rules[this]) {
                    rules[this] = Number(rules[this]);
                }
            });
            $.each(["rangelength", "range"], function() {
                var parts;
                if (rules[this]) {
                    if ($.isArray(rules[this])) {
                        rules[this] = [Number(rules[this][0]), Number(rules[this][1])];
                    } else if (typeof rules[this] === "string") {
                        parts = rules[this].replace(/[\[\]]/g, "").split(/[\s,]+/);
                        rules[this] = [Number(parts[0]), Number(parts[1])];
                    }
                }
            });

            if ($.validator.autoCreateRanges) {
                // auto-create ranges
                if (rules.min != null && rules.max != null) {
                    rules.range = [rules.min, rules.max];
                    delete rules.min;
                    delete rules.max;
                }
                if (rules.minlength != null && rules.maxlength != null) {
                    rules.rangelength = [rules.minlength, rules.maxlength];
                    delete rules.minlength;
                    delete rules.maxlength;
                }
            }

            return rules;
        },

        // Converts a simple string to a {string: true} rule, e.g., "required" to {required:true}
        normalizeRule: function(data) {
            if (typeof data === "string") {
                var transformed = {};
                $.each(data.split(/\s/), function() {
                    transformed[this] = true;
                });
                data = transformed;
            }
            return data;
        },

        // http://jqueryvalidation.org/jQuery.validator.addMethod/
        addMethod: function(name, method, message) {
            $.validator.methods[name] = method;
            $.validator.messages[name] = message !== undefined ? message : $.validator.messages[name];
            if (method.length < 3) {
                $.validator.addClassRules(name, $.validator.normalizeRule(name));
            }
        },

        methods: {

            // http://jqueryvalidation.org/required-method/
            required: function(value, element, param) {
                // check if dependency is met
                if (!this.depend(param, element)) {
                    return "dependency-mismatch";
                }
                if (element.nodeName.toLowerCase() === "select") {
                    // could be an array for select-multiple or a string, both are fine this way
                    var val = $(element).val();
                    return val && val.length > 0;
                }
                if (this.checkable(element)) {
                    return this.getLength(value, element) > 0;
                }
                return $.trim(value).length > 0;
            },

            // http://jqueryvalidation.org/email-method/
            email: function(value, element) {
                // From http://www.whatwg.org/specs/web-apps/current-work/multipage/states-of-the-type-attribute.html#e-mail-state-%28type=email%29
                // Retrieved 2014-01-14
                // If you have a problem with this implementation, report a bug against the above spec
                // Or use custom methods to implement your own email validation
                return this.optional(element) || /^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/.test(value);
            },

            // http://jqueryvalidation.org/url-method/
            url: function(value, element) {
                // contributed by Scott Gonzalez: http://projects.scottsplayground.com/iri/
                return this.optional(element) || /^(https?|s?ftp):\/\/(((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:)*@)?(((\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5]))|((([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.)+(([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.?)(:\d*)?)(\/((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)+(\/(([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)*)*)?)?(\?((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)|[\uE000-\uF8FF]|\/|\?)*)?(#((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)|\/|\?)*)?$/i.test(value);
            },

            // http://jqueryvalidation.org/date-method/
            date: function(value, element) {
                return this.optional(element) || !/Invalid|NaN/.test(new Date(value).toString());
            },

            // http://jqueryvalidation.org/dateISO-method/
            dateISO: function(value, element) {
                return this.optional(element) || /^\d{4}[\/\-](0?[1-9]|1[012])[\/\-](0?[1-9]|[12][0-9]|3[01])$/.test(value);
            },

            // http://jqueryvalidation.org/number-method/
            number: function(value, element) {
                return this.optional(element) || /^-?(?:\d+|\d{1,3}(?:,\d{3})+)?(?:\.\d+)?$/.test(value);
            },

            // http://jqueryvalidation.org/digits-method/
            digits: function(value, element) {
                return this.optional(element) || /^\d+$/.test(value);
            },

            // http://jqueryvalidation.org/creditcard-method/
            // based on http://en.wikipedia.org/wiki/Luhn/
            creditcard: function(value, element) {
                if (this.optional(element)) {
                    return "dependency-mismatch";
                }
                // accept only spaces, digits and dashes
                if (/[^0-9 \-]+/.test(value)) {
                    return false;
                }
                var nCheck = 0, nDigit = 0, bEven = false, n, cDigit;

                value = value.replace(/\D/g, "");

                // Basing min and max length on
                // http://developer.ean.com/general_info/Valid_Credit_Card_Types
                if (value.length < 13 || value.length > 19) {
                    return false;
                }

                for (n = value.length - 1; n >= 0; n--) {
                    cDigit = value.charAt(n);
                    nDigit = parseInt(cDigit, 10);
                    if (bEven) {
                        if ((nDigit *= 2) > 9) {
                            nDigit -= 9;
                        }
                    }
                    nCheck += nDigit;
                    bEven = !bEven;
                }

                return (nCheck % 10) === 0;
            },

            // http://jqueryvalidation.org/minlength-method/
            minlength: function(value, element, param) {
                var length = $.isArray(value) ? value.length : this.getLength(value, element);
                return this.optional(element) || length >= param;
            },

            // http://jqueryvalidation.org/maxlength-method/
            maxlength: function(value, element, param) {
                var length = $.isArray(value) ? value.length : this.getLength(value, element);
                return this.optional(element) || length <= param;
            },

            // http://jqueryvalidation.org/rangelength-method/
            rangelength: function(value, element, param) {
                var length = $.isArray(value) ? value.length : this.getLength(value, element);
                return this.optional(element) || (length >= param[0] && length <= param[1]);
            },

            // http://jqueryvalidation.org/min-method/
            min: function(value, element, param) {
                return this.optional(element) || value >= param;
            },

            // http://jqueryvalidation.org/max-method/
            max: function(value, element, param) {
                return this.optional(element) || value <= param;
            },

            // http://jqueryvalidation.org/range-method/
            range: function(value, element, param) {
                return this.optional(element) || (value >= param[0] && value <= param[1]);
            },

            // http://jqueryvalidation.org/equalTo-method/
            equalTo: function(value, element, param) {
                // bind to the blur event of the target in order to revalidate whenever the target field is updated
                // TODO find a way to bind the event just once, avoiding the unbind-rebind overhead
                var target = $(param);
                if (this.settings.onfocusout) {
                    target.unbind(".validate-equalTo").bind("blur.validate-equalTo", function() {
                        $(element).valid();
                    });
                }
                return value === target.val();
            },

            // http://jqueryvalidation.org/remote-method/
            remote: function(value, element, param) {
                if (this.optional(element)) {
                    return "dependency-mismatch";
                }

                var previous = this.previousValue(element), validator, data;

                if (!this.settings.messages[element.name]) {
                    this.settings.messages[element.name] = {};
                }
                previous.originalMessage = this.settings.messages[element.name].remote;
                this.settings.messages[element.name].remote = previous.message;

                param = typeof param === "string" && {
                    url: param
                } || param;

                if (previous.old === value) {
                    return previous.valid;
                }

                previous.old = value;
                validator = this;
                this.startRequest(element);
                data = {};
                data[element.name] = value;
                $.ajax($.extend(true, {
                    url: param,
                    mode: "abort",
                    port: "validate" + element.name,
                    dataType: "json",
                    data: data,
                    context: validator.currentForm,
                    success: function(response) {
                        var valid = response === true || response === "true", errors, message, submitted;

                        validator.settings.messages[element.name].remote = previous.originalMessage;
                        if (valid) {
                            submitted = validator.formSubmitted;
                            validator.prepareElement(element);
                            validator.formSubmitted = submitted;
                            validator.successList.push(element);
                            delete validator.invalid[element.name];
                            validator.showErrors();
                        } else {
                            errors = {};
                            message = response || validator.defaultMessage(element, "remote");
                            errors[element.name] = previous.message = $.isFunction(message) ? message(value) : message;
                            validator.invalid[element.name] = true;
                            validator.showErrors(errors);
                        }
                        previous.valid = valid;
                        validator.stopRequest(element, valid);
                    }
                }, param));
                return "pending";
            }

        }

    });

    $.format = function deprecated() {
        throw "$.format has been deprecated. Please use $.validator.format instead.";
    }
    ;

    // ajax mode: abort
    // usage: $.ajax({ mode: "abort"[, port: "uniqueport"]});
    // if mode:"abort" is used, the previous request on that port (port can be undefined) is aborted via XMLHttpRequest.abort()

    var pendingRequests = {}, ajax;
    // Use a prefilter if available (1.5+)
    if ($.ajaxPrefilter) {
        $.ajaxPrefilter(function(settings, _, xhr) {
            var port = settings.port;
            if (settings.mode === "abort") {
                if (pendingRequests[port]) {
                    pendingRequests[port].abort();
                }
                pendingRequests[port] = xhr;
            }
        });
    } else {
        // Proxy ajax
        ajax = $.ajax;
        $.ajax = function(settings) {
            var mode = ("mode"in settings ? settings : $.ajaxSettings).mode
              , port = ("port"in settings ? settings : $.ajaxSettings).port;
            if (mode === "abort") {
                if (pendingRequests[port]) {
                    pendingRequests[port].abort();
                }
                pendingRequests[port] = ajax.apply(this, arguments);
                return pendingRequests[port];
            }
            return ajax.apply(this, arguments);
        }
        ;
    }

    // provides delegate(type: String, delegate: Selector, handler: Callback) plugin for easier event delegation
    // handler is only called when $(event.target).is(delegate), in the scope of the jquery-object for event.target

    $.extend($.fn, {
        validateDelegate: function(delegate, type, handler) {
            return this.bind(type, function(event) {
                var target = $(event.target);
                if (target.is(delegate)) {
                    return handler.apply(target, arguments);
                }
            });
        }
    });

}));
/*
 * jQuery throttle / debounce - v1.1 - 3/7/2010
 * http://benalman.com/projects/jquery-throttle-debounce-plugin/
 * 
 * Copyright (c) 2010 "Cowboy" Ben Alman
 * Dual licensed under the MIT and GPL licenses.
 * http://benalman.com/about/license/
 */
(function(b, c) {
    var $ = b.jQuery || b.Cowboy || (b.Cowboy = {}), a;
    $.throttle = a = function(e, f, j, i) {
        var h, d = 0;
        if (typeof f !== "boolean") {
            i = j;
            j = f;
            f = c
        }
        function g() {
            var o = this
              , m = +new Date() - d
              , n = arguments;
            function l() {
                d = +new Date();
                j.apply(o, n)
            }
            function k() {
                h = c
            }
            if (i && !h) {
                l()
            }
            h && clearTimeout(h);
            if (i === c && m > e) {
                l()
            } else {
                if (f !== true) {
                    h = setTimeout(i ? k : l, i === c ? e - m : e)
                }
            }
        }
        if ($.guid) {
            g.guid = j.guid = j.guid || $.guid++
        }
        return g
    }
    ;
    $.debounce = function(d, e, f) {
        return f === c ? a(d, e, false) : a(d, f, e !== false)
    }
}
)(this);
/*
 *	jQuery dotdotdot 1.7.2
 *
 *	Copyright (c) Fred Heusschen
 *	www.frebsite.nl
 *
 *	Plugin website:
 *	dotdotdot.frebsite.nl
 *
 *	Licensed under the MIT license.
 *	http://en.wikipedia.org/wiki/MIT_License
 */
!function(t, e) {
    function n(t, e, n) {
        var r = t.children()
          , o = !1;
        t.empty();
        for (var i = 0, d = r.length; d > i; i++) {
            var l = r.eq(i);
            if (t.append(l),
            n && t.append(n),
            a(t, e)) {
                l.remove(),
                o = !0;
                break
            }
            n && n.detach()
        }
        return o
    }
    function r(e, n, i, d, l) {
        var s = !1
          , c = "a table, thead, tbody, tfoot, tr, col, colgroup, object, embed, param, ol, ul, dl, blockquote, select, optgroup, option, textarea, script, style"
          , u = "script, .dotdotdot-keep";
        return e.contents().detach().each(function() {
            var f = this
              , h = t(f);
            if ("undefined" == typeof f || 3 == f.nodeType && 0 == t.trim(f.data).length)
                return !0;
            if (h.is(u))
                e.append(h);
            else {
                if (s)
                    return !0;
                e.append(h),
                !l || h.is(d.after) || h.find(d.after).length || e[e.is(c) ? "after" : "append"](l),
                a(i, d) && (s = 3 == f.nodeType ? o(h, n, i, d, l) : r(h, n, i, d, l),
                s || (h.detach(),
                s = !0)),
                s || l && l.detach()
            }
        }),
        s
    }
    function o(e, n, r, o, d) {
        var c = e[0];
        if (!c)
            return !1;
        var f = s(c)
          , h = -1 !== f.indexOf(" ") ? " " : "　"
          , p = "letter" == o.wrap ? "" : h
          , g = f.split(p)
          , v = -1
          , w = -1
          , b = 0
          , y = g.length - 1;
        for (o.fallbackToLetter && 0 == b && 0 == y && (p = "",
        g = f.split(p),
        y = g.length - 1); y >= b && (0 != b || 0 != y); ) {
            var m = Math.floor((b + y) / 2);
            if (m == w)
                break;
            w = m,
            l(c, g.slice(0, w + 1).join(p) + o.ellipsis),
            a(r, o) ? (y = w,
            o.fallbackToLetter && 0 == b && 0 == y && (p = "",
            g = g[0].split(p),
            v = -1,
            w = -1,
            b = 0,
            y = g.length - 1)) : (v = w,
            b = w)
        }
        if (-1 == v || 1 == g.length && 0 == g[0].length) {
            var x = e.parent();
            e.detach();
            var T = d && d.closest(x).length ? d.length : 0;
            x.contents().length > T ? c = u(x.contents().eq(-1 - T), n) : (c = u(x, n, !0),
            T || x.detach()),
            c && (f = i(s(c), o),
            l(c, f),
            T && d && t(c).parent().append(d))
        } else
            f = i(g.slice(0, v + 1).join(p), o),
            l(c, f);
        return !0
    }
    function a(t, e) {
        return t.innerHeight() > e.maxHeight
    }
    function i(e, n) {
        for (; t.inArray(e.slice(-1), n.lastCharacter.remove) > -1; )
            e = e.slice(0, -1);
        return t.inArray(e.slice(-1), n.lastCharacter.noEllipsis) < 0 && (e += n.ellipsis),
        e
    }
    function d(t) {
        return {
            width: t.innerWidth(),
            height: t.innerHeight()
        }
    }
    function l(t, e) {
        t.innerText ? t.innerText = e : t.nodeValue ? t.nodeValue = e : t.textContent && (t.textContent = e)
    }
    function s(t) {
        return t.innerText ? t.innerText : t.nodeValue ? t.nodeValue : t.textContent ? t.textContent : ""
    }
    function c(t) {
        do
            t = t.previousSibling;
        while (t && 1 !== t.nodeType && 3 !== t.nodeType);
        return t
    }
    function u(e, n, r) {
        var o, a = e && e[0];
        if (a) {
            if (!r) {
                if (3 === a.nodeType)
                    return a;
                if (t.trim(e.text()))
                    return u(e.contents().last(), n)
            }
            for (o = c(a); !o; ) {
                if (e = e.parent(),
                e.is(n) || !e.length)
                    return !1;
                o = c(e[0])
            }
            if (o)
                return u(t(o), n)
        }
        return !1
    }
    function f(e, n) {
        return e ? "string" == typeof e ? (e = t(e, n),
        e.length ? e : !1) : e.jquery ? e : !1 : !1
    }
    function h(t) {
        for (var e = t.parent().innerHeight() - t.position().top, n = ["paddingTop", "paddingBottom"], r = 0, o = n.length; o > r; r++) {
            var a = parseInt(t.css(n[r]), 10);
            isNaN(a) && (a = 0),
            e -= a
        }
        return e
    }
    if (!t.fn.dotdotdot) {
        t.fn.dotdotdot = function(e) {
            if (0 == this.length)
                return t.fn.dotdotdot.debug('No element found for "' + this.selector + '".'),
                this;
            if (this.length > 1)
                return this.each(function() {
                    t(this).dotdotdot(e)
                });
            var o = this;
            o.data("dotdotdot") && o.trigger("destroy.dot"),
            o.data("dotdotdot-style", o.attr("style") || ""),
            o.css("word-wrap", "break-word"),
            "nowrap" === o.css("white-space") && o.css("white-space", "normal"),
            o.bind_events = function() {
                return o.bind("update.dot", function(e, d) {
                    e.preventDefault(),
                    e.stopPropagation(),
                    l.maxHeight = "number" == typeof l.height ? l.height : h(o),
                    l.maxHeight += l.tolerance,
                    "undefined" != typeof d && (("string" == typeof d || d instanceof HTMLElement) && (d = t("<div />").append(d).contents()),
                    d instanceof t && (i = d)),
                    g = o.wrapInner('<div class="dotdotdot" />').children(),
                    g.contents().detach().end().append(i.clone(!0)).find("br").replaceWith("  <br />  ").end().css({
                        height: "auto",
                        width: "auto",
                        border: "none",
                        padding: 0,
                        margin: 0
                    });
                    var c = !1
                      , u = !1;
                    return s.afterElement && (c = s.afterElement.clone(!0),
                    c.show(),
                    s.afterElement.detach()),
                    a(g, l) && (u = "children" == l.wrap ? n(g, l, c) : r(g, o, g, l, c)),
                    g.replaceWith(g.contents()),
                    g = null,
                    t.isFunction(l.callback) && l.callback.call(o[0], u, i),
                    s.isTruncated = u,
                    u
                }).bind("isTruncated.dot", function(t, e) {
                    return t.preventDefault(),
                    t.stopPropagation(),
                    "function" == typeof e && e.call(o[0], s.isTruncated),
                    s.isTruncated
                }).bind("originalContent.dot", function(t, e) {
                    return t.preventDefault(),
                    t.stopPropagation(),
                    "function" == typeof e && e.call(o[0], i),
                    i
                }).bind("destroy.dot", function(t) {
                    t.preventDefault(),
                    t.stopPropagation(),
                    o.unwatch().unbind_events().contents().detach().end().append(i).attr("style", o.data("dotdotdot-style") || "").data("dotdotdot", !1)
                }),
                o
            }
            ,
            o.unbind_events = function() {
                return o.unbind(".dot"),
                o
            }
            ,
            o.watch = function() {
                if (o.unwatch(),
                "window" == l.watch) {
                    var e = t(window)
                      , n = e.width()
                      , r = e.height();
                    e.bind("resize.dot" + s.dotId, function() {
                        n == e.width() && r == e.height() && l.windowResizeFix || (n = e.width(),
                        r = e.height(),
                        u && clearInterval(u),
                        u = setTimeout(function() {
                            o.trigger("update.dot")
                        }, 100))
                    })
                } else
                    c = d(o),
                    u = setInterval(function() {
                        if (o.is(":visible")) {
                            var t = d(o);
                            (c.width != t.width || c.height != t.height) && (o.trigger("update.dot"),
                            c = t)
                        }
                    }, 500);
                return o
            }
            ,
            o.unwatch = function() {
                return t(window).unbind("resize.dot" + s.dotId),
                u && clearInterval(u),
                o
            }
            ;
            var i = o.contents()
              , l = t.extend(!0, {}, t.fn.dotdotdot.defaults, e)
              , s = {}
              , c = {}
              , u = null
              , g = null;
            return l.lastCharacter.remove instanceof Array || (l.lastCharacter.remove = t.fn.dotdotdot.defaultArrays.lastCharacter.remove),
            l.lastCharacter.noEllipsis instanceof Array || (l.lastCharacter.noEllipsis = t.fn.dotdotdot.defaultArrays.lastCharacter.noEllipsis),
            s.afterElement = f(l.after, o),
            s.isTruncated = !1,
            s.dotId = p++,
            o.data("dotdotdot", !0).bind_events().trigger("update.dot"),
            l.watch && o.watch(),
            o
        }
        ,
        t.fn.dotdotdot.defaults = {
            ellipsis: "... ",
            wrap: "word",
            fallbackToLetter: !0,
            lastCharacter: {},
            tolerance: 0,
            callback: null,
            after: null,
            height: null,
            watch: !1,
            windowResizeFix: !0
        },
        t.fn.dotdotdot.defaultArrays = {
            lastCharacter: {
                remove: [" ", "　", ",", ";", ".", "!", "?"],
                noEllipsis: []
            }
        },
        t.fn.dotdotdot.debug = function() {}
        ;
        var p = 1
          , g = t.fn.html;
        t.fn.html = function(n) {
            return n != e && !t.isFunction(n) && this.data("dotdotdot") ? this.trigger("update", [n]) : g.apply(this, arguments)
        }
        ;
        var v = t.fn.text;
        t.fn.text = function(n) {
            return n != e && !t.isFunction(n) && this.data("dotdotdot") ? (n = t("<div />").text(n).html(),
            this.trigger("update", [n])) : v.apply(this, arguments)
        }
    }
}(jQuery);
/*
 * jQuery Clipboard :: Fork of zClip :: Uses ZeroClipboard v1.3.2
 *
 * https://github.com/valeriansaliou/jquery.clipboard
 * http://www.steamdev.com/zclip/
 *
 * Copyright 2014, Valerian Saliou
 * Copyright 2011, SteamDev
 *
 * Released under the MIT license.
 * http://www.opensource.org/licenses/mit-license.php
 *
 * Version: v1.4
 * Date: Fri Oct 3, 2014
 */
/* Component: jQuery Clipboard */
(function($) {
    var $clip = null;
    var $is_loaded = false;
    $.fn.clipboard = function(params) {
        if ((typeof params == 'object' && !params.length) || (typeof params == 'undefined')) {
            var settings = $.extend({
                path: 'jquery.clipboard.swf',
                copy: null,
                beforeCopy: null,
                afterCopy: null,
                clickAfter: true
            }, (params || {}));
            return this.each(function() {
                var o = $(this);
                if (o.is(':visible') && (typeof settings.copy == 'string' || $.isFunction(settings.copy))) {
                    if ($.isFunction(settings.copy)) {
                        o.bind('Clipboard_copy', settings.copy);
                    }
                    if ($.isFunction(settings.beforeCopy)) {
                        o.bind('Clipboard_beforeCopy', settings.beforeCopy);
                    }
                    if ($.isFunction(settings.afterCopy)) {
                        o.bind('Clipboard_afterCopy', settings.afterCopy);
                    }
                    if ($clip === null) {
                        ZeroClipboard.config({
                            moviePath: settings.path,
                            trustedDomains: '*',
                            hoverClass: 'hover',
                            activeClass: 'active'
                        });
                        $clip = new ZeroClipboard(null);
                        $clip.on('load', function(client) {
                            client.on('mouseover', function(client) {
                                $(this).trigger('mouseenter');
                            });
                            client.on('mouseout', function(client) {
                                $(this).trigger('mouseleave');
                            });
                            client.on('mousedown', function(client) {
                                $(this).trigger('mousedown');
                                if (!$.isFunction(settings.copy)) {
                                    client.setText(settings.copy);
                                } else {
                                    client.setText($(this).triggerHandler('Clipboard_copy'));
                                }
                                if ($.isFunction(settings.beforeCopy)) {
                                    $(this).trigger('Clipboard_beforeCopy');
                                }
                            });
                            client.on('complete', function(client, args) {
                                if ($.isFunction(settings.afterCopy)) {
                                    $(this).trigger('Clipboard_afterCopy');
                                } else {
                                    $(this).removeClass('hover');
                                }
                                if (settings.clickAfter) {
                                    $(this).trigger('click');
                                }
                            });
                        });
                    }
                    $clip.clip([o[0]]);
                }
            });
        }
    }
    ;
}
)(jQuery);
/* Component: ZeroClipboard */
/*!
 * ZeroClipboard
 * The ZeroClipboard library provides an easy way to copy text to the clipboard using an invisible Adobe Flash movie and a JavaScript interface.
 * Copyright (c) 2014 Jon Rohan, James M. Greene
 * Licensed MIT
 * http://zeroclipboard.org/
 * v1.3.2
 */
(function() {
    "use strict";
    var currentElement;
    var flashState = {
        bridge: null,
        version: "0.0.0",
        disabled: null,
        outdated: null,
        ready: null
    };
    var _clipData = {};
    var clientIdCounter = 0;
    var _clientMeta = {};
    var elementIdCounter = 0;
    var _elementMeta = {};
    var _amdModuleId = null;
    var _cjsModuleId = null;
    var _swfPath = function() {
        var i, jsDir, tmpJsPath, jsPath, swfPath = "ZeroClipboard.swf";
        if (document.currentScript && (jsPath = document.currentScript.src)) {} else {
            var scripts = document.getElementsByTagName("script");
            if ("readyState"in scripts[0]) {
                for (i = scripts.length; i--; ) {
                    if (scripts[i].readyState === "interactive" && (jsPath = scripts[i].src)) {
                        break;
                    }
                }
            } else if (document.readyState === "loading") {
                jsPath = scripts[scripts.length - 1].src;
            } else {
                for (i = scripts.length; i--; ) {
                    tmpJsPath = scripts[i].src;
                    if (!tmpJsPath) {
                        jsDir = null;
                        break;
                    }
                    tmpJsPath = tmpJsPath.split("#")[0].split("?")[0];
                    tmpJsPath = tmpJsPath.slice(0, tmpJsPath.lastIndexOf("/") + 1);
                    if (jsDir === null) {
                        jsDir = tmpJsPath;
                    } else if (jsDir !== tmpJsPath) {
                        jsDir = null;
                        break;
                    }
                }
                if (jsDir !== null) {
                    jsPath = jsDir;
                }
            }
        }
        if (jsPath) {
            jsPath = jsPath.split("#")[0].split("?")[0];
            swfPath = jsPath.slice(0, jsPath.lastIndexOf("/") + 1) + swfPath;
        }
        return swfPath;
    }();
    var _camelizeCssPropName = function() {
        var matcherRegex = /\-([a-z])/g
          , replacerFn = function(match, group) {
            return group.toUpperCase();
        };
        return function(prop) {
            return prop.replace(matcherRegex, replacerFn);
        }
        ;
    }();
    var _getStyle = function(el, prop) {
        var value, camelProp, tagName, possiblePointers, i, len;
        if (window.getComputedStyle) {
            value = window.getComputedStyle(el, null).getPropertyValue(prop);
        } else {
            camelProp = _camelizeCssPropName(prop);
            if (el.currentStyle) {
                value = el.currentStyle[camelProp];
            } else {
                value = el.style[camelProp];
            }
        }
        if (prop === "cursor") {
            if (!value || value === "auto") {
                tagName = el.tagName.toLowerCase();
                if (tagName === "a") {
                    return "pointer";
                }
            }
        }
        return value;
    };
    var _elementMouseOver = function(event) {
        if (!event) {
            event = window.event;
        }
        var target;
        if (this !== window) {
            target = this;
        } else if (event.target) {
            target = event.target;
        } else if (event.srcElement) {
            target = event.srcElement;
        }
        ZeroClipboard.activate(target);
    };
    var _addEventHandler = function(element, method, func) {
        if (!element || element.nodeType !== 1) {
            return;
        }
        if (element.addEventListener) {
            element.addEventListener(method, func, false);
        } else if (element.attachEvent) {
            element.attachEvent("on" + method, func);
        }
    };
    var _removeEventHandler = function(element, method, func) {
        if (!element || element.nodeType !== 1) {
            return;
        }
        if (element.removeEventListener) {
            element.removeEventListener(method, func, false);
        } else if (element.detachEvent) {
            element.detachEvent("on" + method, func);
        }
    };
    var _addClass = function(element, value) {
        if (!element || element.nodeType !== 1) {
            return element;
        }
        if (element.classList) {
            if (!element.classList.contains(value)) {
                element.classList.add(value);
            }
            return element;
        }
        if (value && typeof value === "string") {
            var classNames = (value || "").split(/\s+/);
            if (element.nodeType === 1) {
                if (!element.className) {
                    element.className = value;
                } else {
                    var className = " " + element.className + " "
                      , setClass = element.className;
                    for (var c = 0, cl = classNames.length; c < cl; c++) {
                        if (className.indexOf(" " + classNames[c] + " ") < 0) {
                            setClass += " " + classNames[c];
                        }
                    }
                    element.className = setClass.replace(/^\s+|\s+$/g, "");
                }
            }
        }
        return element;
    };
    var _removeClass = function(element, value) {
        if (!element || element.nodeType !== 1) {
            return element;
        }
        if (element.classList) {
            if (element.classList.contains(value)) {
                element.classList.remove(value);
            }
            return element;
        }
        if (value && typeof value === "string" || value === undefined) {
            var classNames = (value || "").split(/\s+/);
            if (element.nodeType === 1 && element.className) {
                if (value) {
                    var className = (" " + element.className + " ").replace(/[\n\t]/g, " ");
                    for (var c = 0, cl = classNames.length; c < cl; c++) {
                        className = className.replace(" " + classNames[c] + " ", " ");
                    }
                    element.className = className.replace(/^\s+|\s+$/g, "");
                } else {
                    element.className = "";
                }
            }
        }
        return element;
    };
    var _getZoomFactor = function() {
        var rect, physicalWidth, logicalWidth, zoomFactor = 1;
        if (typeof document.body.getBoundingClientRect === "function") {
            rect = document.body.getBoundingClientRect();
            physicalWidth = rect.right - rect.left;
            logicalWidth = document.body.offsetWidth;
            zoomFactor = Math.round(physicalWidth / logicalWidth * 100) / 100;
        }
        return zoomFactor;
    };
    var _getDOMObjectPosition = function(obj, defaultZIndex) {
        var info = {
            left: 0,
            top: 0,
            width: 0,
            height: 0,
            zIndex: _getSafeZIndex(defaultZIndex) - 1
        };
        if (obj.getBoundingClientRect) {
            var rect = obj.getBoundingClientRect();
            var pageXOffset, pageYOffset, zoomFactor;
            if ("pageXOffset"in window && "pageYOffset"in window) {
                pageXOffset = window.pageXOffset;
                pageYOffset = window.pageYOffset;
            } else {
                zoomFactor = _getZoomFactor();
                pageXOffset = Math.round(document.documentElement.scrollLeft / zoomFactor);
                pageYOffset = Math.round(document.documentElement.scrollTop / zoomFactor);
            }
            var leftBorderWidth = document.documentElement.clientLeft || 0;
            var topBorderWidth = document.documentElement.clientTop || 0;
            info.left = rect.left + pageXOffset - leftBorderWidth;
            info.top = rect.top + pageYOffset - topBorderWidth;
            info.width = "width"in rect ? rect.width : rect.right - rect.left;
            info.height = "height"in rect ? rect.height : rect.bottom - rect.top;
        }
        return info;
    };
    var _cacheBust = function(path, options) {
        var cacheBust = options === null || options && options.cacheBust === true && options.useNoCache === true;
        if (cacheBust) {
            return (path.indexOf("?") === -1 ? "?" : "&") + "noCache=" + new Date().getTime();
        } else {
            return "";
        }
    };
    var _vars = function(options) {
        var i, len, domain, str = [], domains = [], trustedOriginsExpanded = [];
        if (options.trustedOrigins) {
            if (typeof options.trustedOrigins === "string") {
                domains.push(options.trustedOrigins);
            } else if (typeof options.trustedOrigins === "object" && "length"in options.trustedOrigins) {
                domains = domains.concat(options.trustedOrigins);
            }
        }
        if (options.trustedDomains) {
            if (typeof options.trustedDomains === "string") {
                domains.push(options.trustedDomains);
            } else if (typeof options.trustedDomains === "object" && "length"in options.trustedDomains) {
                domains = domains.concat(options.trustedDomains);
            }
        }
        if (domains.length) {
            for (i = 0,
            len = domains.length; i < len; i++) {
                if (domains.hasOwnProperty(i) && domains[i] && typeof domains[i] === "string") {
                    domain = _extractDomain(domains[i]);
                    if (!domain) {
                        continue;
                    }
                    if (domain === "*") {
                        trustedOriginsExpanded = [domain];
                        break;
                    }
                    trustedOriginsExpanded.push.apply(trustedOriginsExpanded, [domain, "//" + domain, window.location.protocol + "//" + domain]);
                }
            }
        }
        if (trustedOriginsExpanded.length) {
            str.push("trustedOrigins=" + encodeURIComponent(trustedOriginsExpanded.join(",")));
        }
        if (typeof options.jsModuleId === "string" && options.jsModuleId) {
            str.push("jsModuleId=" + encodeURIComponent(options.jsModuleId));
        }
        return str.join("&");
    };
    var _inArray = function(elem, array, fromIndex) {
        if (typeof array.indexOf === "function") {
            return array.indexOf(elem, fromIndex);
        }
        var i, len = array.length;
        if (typeof fromIndex === "undefined") {
            fromIndex = 0;
        } else if (fromIndex < 0) {
            fromIndex = len + fromIndex;
        }
        for (i = fromIndex; i < len; i++) {
            if (array.hasOwnProperty(i) && array[i] === elem) {
                return i;
            }
        }
        return -1;
    };
    var _prepClip = function(elements) {
        if (typeof elements === "string")
            throw new TypeError("ZeroClipboard doesn't accept query strings.");
        if (!elements.length)
            return [elements];
        return elements;
    };
    var _dispatchCallback = function(func, context, args, async) {
        if (async) {
            window.setTimeout(function() {
                func.apply(context, args);
            }, 0);
        } else {
            func.apply(context, args);
        }
    };
    var _getSafeZIndex = function(val) {
        var zIndex, tmp;
        if (val) {
            if (typeof val === "number" && val > 0) {
                zIndex = val;
            } else if (typeof val === "string" && (tmp = parseInt(val, 10)) && !isNaN(tmp) && tmp > 0) {
                zIndex = tmp;
            }
        }
        if (!zIndex) {
            if (typeof _globalConfig.zIndex === "number" && _globalConfig.zIndex > 0) {
                zIndex = _globalConfig.zIndex;
            } else if (typeof _globalConfig.zIndex === "string" && (tmp = parseInt(_globalConfig.zIndex, 10)) && !isNaN(tmp) && tmp > 0) {
                zIndex = tmp;
            }
        }
        return zIndex || 0;
    };
    var _deprecationWarning = function(deprecatedApiName, debugEnabled) {
        if (deprecatedApiName && debugEnabled !== false && typeof console !== "undefined" && console && (console.warn || console.log)) {
            var deprecationWarning = "`" + deprecatedApiName + "` is deprecated. See docs for more info:\n" + " https://github.com/zeroclipboard/zeroclipboard/blob/master/docs/instructions.md#deprecations";
            if (console.warn) {
                console.warn(deprecationWarning);
            } else {
                console.log(deprecationWarning);
            }
        }
    };
    var _extend = function() {
        var i, len, arg, prop, src, copy, target = arguments[0] || {};
        for (i = 1,
        len = arguments.length; i < len; i++) {
            if ((arg = arguments[i]) !== null) {
                for (prop in arg) {
                    if (arg.hasOwnProperty(prop)) {
                        src = target[prop];
                        copy = arg[prop];
                        if (target === copy) {
                            continue;
                        }
                        if (copy !== undefined) {
                            target[prop] = copy;
                        }
                    }
                }
            }
        }
        return target;
    };
    var _extractDomain = function(originOrUrl) {
        if (originOrUrl === null || originOrUrl === "") {
            return null;
        }
        originOrUrl = originOrUrl.replace(/^\s+|\s+$/g, "");
        if (originOrUrl === "") {
            return null;
        }
        var protocolIndex = originOrUrl.indexOf("//");
        originOrUrl = protocolIndex === -1 ? originOrUrl : originOrUrl.slice(protocolIndex + 2);
        var pathIndex = originOrUrl.indexOf("/");
        originOrUrl = pathIndex === -1 ? originOrUrl : protocolIndex === -1 || pathIndex === 0 ? null : originOrUrl.slice(0, pathIndex);
        if (originOrUrl && originOrUrl.slice(-4).toLowerCase() === ".swf") {
            return null;
        }
        return originOrUrl || null;
    };
    var _determineScriptAccess = function() {
        var _extractAllDomains = function(origins, resultsArray) {
            var i, len, tmp;
            if (origins !== null && resultsArray[0] !== "*") {
                if (typeof origins === "string") {
                    origins = [origins];
                }
                if (typeof origins === "object" && "length"in origins) {
                    for (i = 0,
                    len = origins.length; i < len; i++) {
                        if (origins.hasOwnProperty(i)) {
                            tmp = _extractDomain(origins[i]);
                            if (tmp) {
                                if (tmp === "*") {
                                    resultsArray.length = 0;
                                    resultsArray.push("*");
                                    break;
                                }
                                if (_inArray(tmp, resultsArray) === -1) {
                                    resultsArray.push(tmp);
                                }
                            }
                        }
                    }
                }
            }
        };
        var _accessLevelLookup = {
            always: "always",
            samedomain: "sameDomain",
            never: "never"
        };
        return function(currentDomain, configOptions) {
            var asaLower, allowScriptAccess = configOptions.allowScriptAccess;
            if (typeof allowScriptAccess === "string" && (asaLower = allowScriptAccess.toLowerCase()) && /^always|samedomain|never$/.test(asaLower)) {
                return _accessLevelLookup[asaLower];
            }
            var swfDomain = _extractDomain(configOptions.moviePath);
            if (swfDomain === null) {
                swfDomain = currentDomain;
            }
            var trustedDomains = [];
            _extractAllDomains(configOptions.trustedOrigins, trustedDomains);
            _extractAllDomains(configOptions.trustedDomains, trustedDomains);
            var len = trustedDomains.length;
            if (len > 0) {
                if (len === 1 && trustedDomains[0] === "*") {
                    return "always";
                }
                if (_inArray(currentDomain, trustedDomains) !== -1) {
                    if (len === 1 && currentDomain === swfDomain) {
                        return "sameDomain";
                    }
                    return "always";
                }
            }
            return "never";
        }
        ;
    }();
    var _objectKeys = function(obj) {
        if (obj === null) {
            return [];
        }
        if (Object.keys) {
            return Object.keys(obj);
        }
        var keys = [];
        for (var prop in obj) {
            if (obj.hasOwnProperty(prop)) {
                keys.push(prop);
            }
        }
        return keys;
    };
    var _deleteOwnProperties = function(obj) {
        if (obj) {
            for (var prop in obj) {
                if (obj.hasOwnProperty(prop)) {
                    delete obj[prop];
                }
            }
        }
        return obj;
    };
    var _detectFlashSupport = function() {
        var hasFlash = false;
        if (typeof flashState.disabled === "boolean") {
            hasFlash = flashState.disabled === false;
        } else {
            if (typeof ActiveXObject === "function") {
                try {
                    if (new ActiveXObject("ShockwaveFlash.ShockwaveFlash")) {
                        hasFlash = true;
                    }
                } catch (error) {}
            }
            if (!hasFlash && navigator.mimeTypes["application/x-shockwave-flash"]) {
                hasFlash = true;
            }
        }
        return hasFlash;
    };
    function _parseFlashVersion(flashVersion) {
        return flashVersion.replace(/,/g, ".").replace(/[^0-9\.]/g, "");
    }
    function _isFlashVersionSupported(flashVersion) {
        return parseFloat(_parseFlashVersion(flashVersion)) >= 10;
    }
    var ZeroClipboard = function(elements, options) {
        if (!(this instanceof ZeroClipboard)) {
            return new ZeroClipboard(elements,options);
        }
        this.id = "" + clientIdCounter++;
        _clientMeta[this.id] = {
            instance: this,
            elements: [],
            handlers: {}
        };
        if (elements) {
            this.clip(elements);
        }
        if (typeof options !== "undefined") {
            _deprecationWarning("new ZeroClipboard(elements, options)", _globalConfig.debug);
            ZeroClipboard.config(options);
        }
        this.options = ZeroClipboard.config();
        if (typeof flashState.disabled !== "boolean") {
            flashState.disabled = !_detectFlashSupport();
        }
        if (flashState.disabled === false && flashState.outdated !== true) {
            if (flashState.bridge === null) {
                flashState.outdated = false;
                flashState.ready = false;
                _bridge();
            }
        }
    };
    ZeroClipboard.prototype.setText = function(newText) {
        if (newText && newText !== "") {
            _clipData["text/plain"] = newText;
            if (flashState.ready === true && flashState.bridge) {
                flashState.bridge.setText(newText);
            } else {}
        }
        return this;
    }
    ;
    ZeroClipboard.prototype.setSize = function(width, height) {
        if (flashState.ready === true && flashState.bridge) {
            flashState.bridge.setSize(width, height);
        } else {}
        return this;
    }
    ;
    var _setHandCursor = function(enabled) {
        if (flashState.ready === true && flashState.bridge) {
            flashState.bridge.setHandCursor(enabled);
        } else {}
    };
    ZeroClipboard.prototype.destroy = function() {
        this.unclip();
        this.off();
        delete _clientMeta[this.id];
    }
    ;
    var _getAllClients = function() {
        var i, len, client, clients = [], clientIds = _objectKeys(_clientMeta);
        for (i = 0,
        len = clientIds.length; i < len; i++) {
            client = _clientMeta[clientIds[i]].instance;
            if (client && client instanceof ZeroClipboard) {
                clients.push(client);
            }
        }
        return clients;
    };
    ZeroClipboard.version = "1.3.2";
    var _globalConfig = {
        swfPath: _swfPath,
        trustedDomains: window.location.host ? [window.location.host] : [],
        cacheBust: true,
        forceHandCursor: false,
        zIndex: 999999999,
        debug: true,
        title: null,
        autoActivate: true
    };
    ZeroClipboard.config = function(options) {
        if (typeof options === "object" && options !== null) {
            _extend(_globalConfig, options);
        }
        if (typeof options === "string" && options) {
            if (_globalConfig.hasOwnProperty(options)) {
                return _globalConfig[options];
            }
            return;
        }
        var copy = {};
        for (var prop in _globalConfig) {
            if (_globalConfig.hasOwnProperty(prop)) {
                if (typeof _globalConfig[prop] === "object" && _globalConfig[prop] !== null) {
                    if ("length"in _globalConfig[prop]) {
                        copy[prop] = _globalConfig[prop].slice(0);
                    } else {
                        copy[prop] = _extend({}, _globalConfig[prop]);
                    }
                } else {
                    copy[prop] = _globalConfig[prop];
                }
            }
        }
        return copy;
    }
    ;
    ZeroClipboard.destroy = function() {
        ZeroClipboard.deactivate();
        for (var clientId in _clientMeta) {
            if (_clientMeta.hasOwnProperty(clientId) && _clientMeta[clientId]) {
                var client = _clientMeta[clientId].instance;
                if (client && typeof client.destroy === "function") {
                    client.destroy();
                }
            }
        }
        var htmlBridge = _getHtmlBridge(flashState.bridge);
        if (htmlBridge && htmlBridge.parentNode) {
            htmlBridge.parentNode.removeChild(htmlBridge);
            flashState.ready = null;
            flashState.bridge = null;
        }
    }
    ;
    ZeroClipboard.activate = function(element) {
        if (currentElement) {
            _removeClass(currentElement, _globalConfig.hoverClass);
            _removeClass(currentElement, _globalConfig.activeClass);
        }
        currentElement = element;
        _addClass(element, _globalConfig.hoverClass);
        _reposition();
        var newTitle = _globalConfig.title || element.getAttribute("title");
        if (newTitle) {
            var htmlBridge = _getHtmlBridge(flashState.bridge);
            if (htmlBridge) {
                htmlBridge.setAttribute("title", newTitle);
            }
        }
        var useHandCursor = _globalConfig.forceHandCursor === true || _getStyle(element, "cursor") === "pointer";
        _setHandCursor(useHandCursor);
    }
    ;
    ZeroClipboard.deactivate = function() {
        var htmlBridge = _getHtmlBridge(flashState.bridge);
        if (htmlBridge) {
            htmlBridge.style.left = "0px";
            htmlBridge.style.top = "-9999px";
            htmlBridge.removeAttribute("title");
        }
        if (currentElement) {
            _removeClass(currentElement, _globalConfig.hoverClass);
            _removeClass(currentElement, _globalConfig.activeClass);
            currentElement = null;
        }
    }
    ;
    var _bridge = function() {
        var flashBridge, len;
        var container = document.getElementById("global-zeroclipboard-html-bridge");
        if (!container) {
            var opts = ZeroClipboard.config();
            opts.jsModuleId = typeof _amdModuleId === "string" && _amdModuleId || typeof _cjsModuleId === "string" && _cjsModuleId || null;
            var allowScriptAccess = _determineScriptAccess(window.location.host, _globalConfig);
            var flashvars = _vars(opts);
            var swfUrl = _globalConfig.moviePath + _cacheBust(_globalConfig.moviePath, _globalConfig);
            var html = ' <object classid="clsid:d27cdb6e-ae6d-11cf-96b8-444553540000" id="global-zeroclipboard-flash-bridge" width="100%" height="100%"> <param name="movie" value="' + swfUrl + '"/> <param name="allowScriptAccess" value="' + allowScriptAccess + '"/> <param name="scale" value="exactfit"/> <param name="loop" value="false"/> <param name="menu" value="false"/> <param name="quality" value="best" /> <param name="bgcolor" value="#ffffff"/> <param name="wmode" value="transparent"/> <param name="flashvars" value="' + flashvars + '"/> <embed src="' + swfUrl + '" loop="false" menu="false" quality="best" bgcolor="#ffffff" width="100%" height="100%" name="global-zeroclipboard-flash-bridge" allowScriptAccess="' + allowScriptAccess + '" allowFullScreen="false" type="application/x-shockwave-flash" wmode="transparent" pluginspage="http://www.macromedia.com/go/getflashplayer" flashvars="' + flashvars + '" scale="exactfit"> </embed> </object>';
            container = document.createElement("div");
            container.id = "global-zeroclipboard-html-bridge";
            container.setAttribute("class", "global-zeroclipboard-container");
            container.style.position = "absolute";
            container.style.left = "0px";
            container.style.top = "-9999px";
            container.style.width = "15px";
            container.style.height = "15px";
            container.style.zIndex = "" + _getSafeZIndex(_globalConfig.zIndex);
            document.body.appendChild(container);
            container.innerHTML = html;
        }
        flashBridge = document["global-zeroclipboard-flash-bridge"];
        if (flashBridge && (len = flashBridge.length)) {
            flashBridge = flashBridge[len - 1];
        }
        flashState.bridge = flashBridge || container.children[0].lastElementChild;
    };
    var _getHtmlBridge = function(flashBridge) {
        var isFlashElement = /^OBJECT|EMBED$/;
        var htmlBridge = flashBridge && flashBridge.parentNode;
        while (htmlBridge && isFlashElement.test(htmlBridge.nodeName) && htmlBridge.parentNode) {
            htmlBridge = htmlBridge.parentNode;
        }
        return htmlBridge || null;
    };
    var _reposition = function() {
        if (currentElement) {
            var pos = _getDOMObjectPosition(currentElement, _globalConfig.zIndex);
            var htmlBridge = _getHtmlBridge(flashState.bridge);
            if (htmlBridge) {
                htmlBridge.style.top = pos.top + "px";
                htmlBridge.style.left = pos.left + "px";
                htmlBridge.style.width = pos.width + "px";
                htmlBridge.style.height = pos.height + "px";
                htmlBridge.style.zIndex = pos.zIndex + 1;
            }
            if (flashState.ready === true && flashState.bridge) {
                flashState.bridge.setSize(pos.width, pos.height);
            }
        }
        return this;
    };
    ZeroClipboard.prototype.on = function(eventName, func) {
        var i, len, events, added = {}, handlers = _clientMeta[this.id] && _clientMeta[this.id].handlers;
        if (typeof eventName === "string" && eventName) {
            events = eventName.toLowerCase().split(/\s+/);
        } else if (typeof eventName === "object" && eventName && typeof func === "undefined") {
            for (i in eventName) {
                if (eventName.hasOwnProperty(i) && typeof i === "string" && i && typeof eventName[i] === "function") {
                    this.on(i, eventName[i]);
                }
            }
        }
        if (events && events.length) {
            for (i = 0,
            len = events.length; i < len; i++) {
                eventName = events[i].replace(/^on/, "");
                added[eventName] = true;
                if (!handlers[eventName]) {
                    handlers[eventName] = [];
                }
                handlers[eventName].push(func);
            }
            if (added.noflash && flashState.disabled) {
                _receiveEvent.call(this, "noflash", {});
            }
            if (added.wrongflash && flashState.outdated) {
                _receiveEvent.call(this, "wrongflash", {
                    flashVersion: flashState.version
                });
            }
            if (added.load && flashState.ready) {
                _receiveEvent.call(this, "load", {
                    flashVersion: flashState.version
                });
            }
        }
        return this;
    }
    ;
    ZeroClipboard.prototype.off = function(eventName, func) {
        var i, len, foundIndex, events, perEventHandlers, handlers = _clientMeta[this.id] && _clientMeta[this.id].handlers;
        if (arguments.length === 0) {
            events = _objectKeys(handlers);
        } else if (typeof eventName === "string" && eventName) {
            events = eventName.split(/\s+/);
        } else if (typeof eventName === "object" && eventName && typeof func === "undefined") {
            for (i in eventName) {
                if (eventName.hasOwnProperty(i) && typeof i === "string" && i && typeof eventName[i] === "function") {
                    this.off(i, eventName[i]);
                }
            }
        }
        if (events && events.length) {
            for (i = 0,
            len = events.length; i < len; i++) {
                eventName = events[i].toLowerCase().replace(/^on/, "");
                perEventHandlers = handlers[eventName];
                if (perEventHandlers && perEventHandlers.length) {
                    if (func) {
                        foundIndex = _inArray(func, perEventHandlers);
                        while (foundIndex !== -1) {
                            perEventHandlers.splice(foundIndex, 1);
                            foundIndex = _inArray(func, perEventHandlers, foundIndex);
                        }
                    } else {
                        handlers[eventName].length = 0;
                    }
                }
            }
        }
        return this;
    }
    ;
    ZeroClipboard.prototype.handlers = function(eventName) {
        var prop, copy = null, handlers = _clientMeta[this.id] && _clientMeta[this.id].handlers;
        if (handlers) {
            if (typeof eventName === "string" && eventName) {
                return handlers[eventName] ? handlers[eventName].slice(0) : null;
            }
            copy = {};
            for (prop in handlers) {
                if (handlers.hasOwnProperty(prop) && handlers[prop]) {
                    copy[prop] = handlers[prop].slice(0);
                }
            }
        }
        return copy;
    }
    ;
    var _dispatchClientCallbacks = function(eventName, context, args, async) {
        var handlers = _clientMeta[this.id] && _clientMeta[this.id].handlers[eventName];
        if (handlers && handlers.length) {
            var i, len, func, originalContext = context || this;
            for (i = 0,
            len = handlers.length; i < len; i++) {
                func = handlers[i];
                context = originalContext;
                if (typeof func === "string" && typeof window[func] === "function") {
                    func = window[func];
                }
                if (typeof func === "object" && func && typeof func.handleEvent === "function") {
                    context = func;
                    func = func.handleEvent;
                }
                if (typeof func === "function") {
                    _dispatchCallback(func, context, args, async);
                }
            }
        }
        return this;
    };
    ZeroClipboard.prototype.clip = function(elements) {
        elements = _prepClip(elements);
        for (var i = 0; i < elements.length; i++) {
            if (elements.hasOwnProperty(i) && elements[i] && elements[i].nodeType === 1) {
                if (!elements[i].zcClippingId) {
                    elements[i].zcClippingId = "zcClippingId_" + elementIdCounter++;
                    _elementMeta[elements[i].zcClippingId] = [this.id];
                    if (_globalConfig.autoActivate === true) {
                        _addEventHandler(elements[i], "mouseover", _elementMouseOver);
                    }
                } else if (_inArray(this.id, _elementMeta[elements[i].zcClippingId]) === -1) {
                    _elementMeta[elements[i].zcClippingId].push(this.id);
                }
                var clippedElements = _clientMeta[this.id].elements;
                if (_inArray(elements[i], clippedElements) === -1) {
                    clippedElements.push(elements[i]);
                }
            }
        }
        return this;
    }
    ;
    ZeroClipboard.prototype.unclip = function(elements) {
        var meta = _clientMeta[this.id];
        if (meta) {
            var clippedElements = meta.elements;
            var arrayIndex;
            if (typeof elements === "undefined") {
                elements = clippedElements.slice(0);
            } else {
                elements = _prepClip(elements);
            }
            for (var i = elements.length; i--; ) {
                if (elements.hasOwnProperty(i) && elements[i] && elements[i].nodeType === 1) {
                    arrayIndex = 0;
                    while ((arrayIndex = _inArray(elements[i], clippedElements, arrayIndex)) !== -1) {
                        clippedElements.splice(arrayIndex, 1);
                    }
                    var clientIds = _elementMeta[elements[i].zcClippingId];
                    if (clientIds) {
                        arrayIndex = 0;
                        while ((arrayIndex = _inArray(this.id, clientIds, arrayIndex)) !== -1) {
                            clientIds.splice(arrayIndex, 1);
                        }
                        if (clientIds.length === 0) {
                            if (_globalConfig.autoActivate === true) {
                                _removeEventHandler(elements[i], "mouseover", _elementMouseOver);
                            }
                            delete elements[i].zcClippingId;
                        }
                    }
                }
            }
        }
        return this;
    }
    ;
    ZeroClipboard.prototype.elements = function() {
        var meta = _clientMeta[this.id];
        return meta && meta.elements ? meta.elements.slice(0) : [];
    }
    ;
    var _getAllClientsClippedToElement = function(element) {
        var elementMetaId, clientIds, i, len, client, clients = [];
        if (element && element.nodeType === 1 && (elementMetaId = element.zcClippingId) && _elementMeta.hasOwnProperty(elementMetaId)) {
            clientIds = _elementMeta[elementMetaId];
            if (clientIds && clientIds.length) {
                for (i = 0,
                len = clientIds.length; i < len; i++) {
                    client = _clientMeta[clientIds[i]].instance;
                    if (client && client instanceof ZeroClipboard) {
                        clients.push(client);
                    }
                }
            }
        }
        return clients;
    };
    _globalConfig.hoverClass = "zeroclipboard-is-hover";
    _globalConfig.activeClass = "zeroclipboard-is-active";
    _globalConfig.trustedOrigins = null;
    _globalConfig.allowScriptAccess = null;
    _globalConfig.useNoCache = true;
    _globalConfig.moviePath = "ZeroClipboard.swf";
    ZeroClipboard.detectFlashSupport = function() {
        _deprecationWarning("ZeroClipboard.detectFlashSupport", _globalConfig.debug);
        return _detectFlashSupport();
    }
    ;
    ZeroClipboard.dispatch = function(eventName, args) {
        if (typeof eventName === "string" && eventName) {
            var cleanEventName = eventName.toLowerCase().replace(/^on/, "");
            if (cleanEventName) {
                var clients = currentElement ? _getAllClientsClippedToElement(currentElement) : _getAllClients();
                for (var i = 0, len = clients.length; i < len; i++) {
                    _receiveEvent.call(clients[i], cleanEventName, args);
                }
            }
        }
    }
    ;
    ZeroClipboard.prototype.setHandCursor = function(enabled) {
        _deprecationWarning("ZeroClipboard.prototype.setHandCursor", _globalConfig.debug);
        enabled = typeof enabled === "boolean" ? enabled : !!enabled;
        _setHandCursor(enabled);
        _globalConfig.forceHandCursor = enabled;
        return this;
    }
    ;
    ZeroClipboard.prototype.reposition = function() {
        _deprecationWarning("ZeroClipboard.prototype.reposition", _globalConfig.debug);
        return _reposition();
    }
    ;
    ZeroClipboard.prototype.receiveEvent = function(eventName, args) {
        _deprecationWarning("ZeroClipboard.prototype.receiveEvent", _globalConfig.debug);
        if (typeof eventName === "string" && eventName) {
            var cleanEventName = eventName.toLowerCase().replace(/^on/, "");
            if (cleanEventName) {
                _receiveEvent.call(this, cleanEventName, args);
            }
        }
    }
    ;
    ZeroClipboard.prototype.setCurrent = function(element) {
        _deprecationWarning("ZeroClipboard.prototype.setCurrent", _globalConfig.debug);
        ZeroClipboard.activate(element);
        return this;
    }
    ;
    ZeroClipboard.prototype.resetBridge = function() {
        _deprecationWarning("ZeroClipboard.prototype.resetBridge", _globalConfig.debug);
        ZeroClipboard.deactivate();
        return this;
    }
    ;
    ZeroClipboard.prototype.setTitle = function(newTitle) {
        _deprecationWarning("ZeroClipboard.prototype.setTitle", _globalConfig.debug);
        newTitle = newTitle || _globalConfig.title || currentElement && currentElement.getAttribute("title");
        if (newTitle) {
            var htmlBridge = _getHtmlBridge(flashState.bridge);
            if (htmlBridge) {
                htmlBridge.setAttribute("title", newTitle);
            }
        }
        return this;
    }
    ;
    ZeroClipboard.setDefaults = function(options) {
        _deprecationWarning("ZeroClipboard.setDefaults", _globalConfig.debug);
        ZeroClipboard.config(options);
    }
    ;
    ZeroClipboard.prototype.addEventListener = function(eventName, func) {
        _deprecationWarning("ZeroClipboard.prototype.addEventListener", _globalConfig.debug);
        return this.on(eventName, func);
    }
    ;
    ZeroClipboard.prototype.removeEventListener = function(eventName, func) {
        _deprecationWarning("ZeroClipboard.prototype.removeEventListener", _globalConfig.debug);
        return this.off(eventName, func);
    }
    ;
    ZeroClipboard.prototype.ready = function() {
        _deprecationWarning("ZeroClipboard.prototype.ready", _globalConfig.debug);
        return flashState.ready === true;
    }
    ;
    var _receiveEvent = function(eventName, args) {
        eventName = eventName.toLowerCase().replace(/^on/, "");
        var cleanVersion = args && args.flashVersion && _parseFlashVersion(args.flashVersion) || null;
        var element = currentElement;
        var performCallbackAsync = true;
        switch (eventName) {
        case "load":
            if (cleanVersion) {
                if (!_isFlashVersionSupported(cleanVersion)) {
                    _receiveEvent.call(this, "onWrongFlash", {
                        flashVersion: cleanVersion
                    });
                    return;
                }
                flashState.outdated = false;
                flashState.ready = true;
                flashState.version = cleanVersion;
            }
            break;
        case "wrongflash":
            if (cleanVersion && !_isFlashVersionSupported(cleanVersion)) {
                flashState.outdated = true;
                flashState.ready = false;
                flashState.version = cleanVersion;
            }
            break;
        case "mouseover":
            _addClass(element, _globalConfig.hoverClass);
            break;
        case "mouseout":
            if (_globalConfig.autoActivate === true) {
                ZeroClipboard.deactivate();
            }
            break;
        case "mousedown":
            _addClass(element, _globalConfig.activeClass);
            break;
        case "mouseup":
            _removeClass(element, _globalConfig.activeClass);
            break;
        case "datarequested":
            var targetId = element.getAttribute("data-clipboard-target")
              , targetEl = !targetId ? null : document.getElementById(targetId);
            if (targetEl) {
                var textContent = targetEl.value || targetEl.textContent || targetEl.innerText;
                if (textContent) {
                    this.setText(textContent);
                }
            } else {
                var defaultText = element.getAttribute("data-clipboard-text");
                if (defaultText) {
                    this.setText(defaultText);
                }
            }
            performCallbackAsync = false;
            break;
        case "complete":
            _deleteOwnProperties(_clipData);
            break;
        }
        var context = element;
        var eventArgs = [this, args];
        return _dispatchClientCallbacks.call(this, eventName, context, eventArgs, performCallbackAsync);
    };
    if (typeof define === "function" && define.amd) {
        define(["require", "exports", "module"], function(require, exports, module) {
            _amdModuleId = module && module.id || null;
            return ZeroClipboard;
        });
    } else if (typeof module === "object" && module && typeof module.exports === "object" && module.exports) {
        _cjsModuleId = module.id || null;
        module.exports = ZeroClipboard;
    } else {
        window.ZeroClipboard = ZeroClipboard;
    }
}
)();
/**
 * Terrific JavaScript Framework v2.0.1
 * http://terrifically.org
 *
 * Copyright 2012, Remo Brunschwiler
 * MIT Licensed.
 *
 * Date: Mon, 10 Sep 2012 13:44:44 GMT
 *
 *
 * Includes:
 * Simple JavaScript Inheritance
 * By John Resig http://ejohn.org/
 * MIT Licensed.
 *
 * @module Tc
 *
 */
var Tc = Tc || {};

/*
 * The base library object.
 */
Tc.$ = $;
//Tc.$ = jQuery;// magnolia stk uses jQuery

/*
 * Simple JavaScript Inheritance
 * By John Resig http://ejohn.org/
 * MIT Licensed.
 */
(function() {
    var initializing = false
      , fnTest = /xyz/.test(function() {
        xyz;
    }) ? /\b_super\b/ : /.*/;

    // The base Class implementation (does nothing)
    this.Class = function() {}
    ;

    // Create a new Class that inherits from this class
    Class.extend = function(prop) {
        var _super = this.prototype;

        // Instantiate a base class (but only create the instance,
        // don't run the init constructor)
        initializing = true;
        var prototype = new this();
        initializing = false;

        // Copy the properties over onto the new prototype
        for (var name in prop) {
            // Check if we're overwriting an existing function
            prototype[name] = typeof prop[name] == "function" && typeof _super[name] == "function" && fnTest.test(prop[name]) ? (function(name, fn) {
                return function() {
                    var tmp = this._super;

                    // Add a new ._super() method that is the same method
                    // but on the super-class
                    this._super = _super[name];

                    // The method only need to be bound temporarily, so we
                    // remove it when we're done executing
                    var ret = fn.apply(this, arguments);
                    this._super = tmp;

                    return ret;
                }
                ;
            }
            )(name, prop[name]) : prop[name];
        }

        // The dummy class constructor
        function Class() {
            // All construction is actually done in the init method
            if (!initializing && this.init) {
                this.init.apply(this, arguments);
            }
        }

        // Populate our constructed prototype object
        Class.prototype = prototype;

        // Enforce the constructor to be what we expect
        Class.constructor = Class;

        // And make this class extendable
        Class.extend = arguments.callee;

        return Class;
    }
    ;
}
)();

/**
 * Contains the application base config.
 * The base config can be extended or overwritten either via
 * new Application ($ctx, config) during bootstrapping the application or via
 * overriding the Tc.Config object in your project.
 *
 * @author Remo Brunschwiler
 * @namespace Tc
 * @class Config
 * @static
 */
Tc.Config = {
    /** 
     * The paths for the different types of dependencies.
     *
     * @property dependencies
     * @type Object
     */
    dependencies: {
        css: '/css/dependencies',
        js: '/js/dependencies'
    }
};

(function($) {
    "use strict";

    /**
     * Responsible for application-wide issues such as the creation of modules and establishing connections between them.
     *
     * @author Remo Brunschwiler
     * @namespace Tc
     * @class Application
     */
    Tc.Application = Class.extend({

        /**
         * Initializes the application.
         *
         * @method init
         * @constructor
         * @param {jQuery} $ctx
         *      The jQuery context
         * @param {Object} config
         *      The configuration
         */
        init: function($ctx, config) {
            /**
             * The configuration.
             *
             * @property config
             * @type Object
             */
            this.config = $.extend(Tc.Config, config);

            /**
             * The jQuery context.
             *
             * @property $ctx
             * @type jQuery
             */
            this.$ctx = $ctx || $('body');

            /**
             * Contains references to all modules on the page. This can, for
             * example, be useful when there are interactions between Flash
             * objects and Javascript.
             *
             * @property modules
             * @type Array
             */
            this.modules = [];

            /**
             * Contains references to all connectors on the page.
             *
             * @property connectors
             * @type Object
             */
            this.connectors = {};

            /**
             * The sandbox to get the resources from
             * This sandbox is shared between all modules.
             *
             * @property sandbox
             * @type Sandbox
             */
            this.sandbox = new Tc.Sandbox(this,this.config);
        },

        /**
         * Register modules withing scope
         * Automatically registers all modules within the scope,
         * as long as the modules use the OOCSS naming conventions.
         *
         * @method registerModules
         * @param {jQuery} $ctx
         *      The jQuery context
         * @return {Array}
         *      A list containing the references of the registered modules
         */
        registerModules: function($ctx) {
            var self = this
              , modules = []
              , stringUtils = Tc.Utils.String;

            $ctx = $ctx || this.$ctx;

            $ctx.find('.mod:not([data-ignore="true"])').each(function() {
                var $this = $(this)
                  , classes = $this.attr('class').split(' ');

                /*
                 * A module can have several different classes and data attributes.
                 * See below for possible values.
                 */

                /*
                 * @config .mod
                 *
                 * Indicates that it is a base module, this is the default and
                 * no JavaScript needs to be involved. It must occur excactly
                 * once.
                 */

                /*
                 * @config .mod{moduleName} || .mod-{module-name}
                 *
                 * Indicates that it is a module of type basic, which is
                 * derived from the base module. It can occur at most
                 * once. Example: .modBasic || .mod-basic
                 */

                /*
                 * @config .skin{moduleName}{skinName} || .skin-{module-name}-{skin-name}
                 *
                 * Indicates that the module basic has the submarine skin. It
                 * will be decorated by the skin JS (if it exists). It can occur
                 * arbitrarily. Example: .skinBasicSubmarine || .skin-basic-submarine
                 */

                /*
                 * @config data-connectors
                 *
                 * A module can have a comma-separated list of data connectors.
                 * The list contains the IDs of the connectors in the following
                 * schema: {connectorType}-{connectorId}
                 *
                 * {connectorType} is optional. If only the {connectorId} is given, the
                 * default connector is instantiated.
                 *
                 * The example MasterSlave-Navigation decodes to: type =
                 * MasterSlave, id = Navigation. This instantiates the MasterSlave
                 * connector (as mediator) with the connector id Navigation.
                 * The connector id is used to chain the appropriate (the ones with the same id)
                 * modules together and to improve the reusability of the connector.
                 * It can contain multiple connector ids (e.g. 1,2,MasterSlave-Navigation).
                 */

                if (classes.length > 1) {
                    var modName, skins = [], connectors = [], dataConnectors;

                    for (var i = 0, len = classes.length; i < len; i++) {
                        var part = $.trim(classes[i]);

                        // do nothing for empty parts
                        if (part) {
                            // convert to camel if necessary
                            if (part.indexOf('-') > -1) {
                                part = stringUtils.toCamel(part);
                            }

                            if (part.indexOf('mod') === 0 && part.length > 3) {
                                modName = part.substr(3);
                            } else if (part.indexOf('skin') === 0) {
                                // Remove the mod name part from the skin name
                                skins.push(part.substr(4).replace(modName, ''));
                            }
                        }
                    }

                    /*
                     * This needs to be done via attr() instead of data().
                     * As data() cast a single number-only connector to an integer, the split will fail.
                     */
                    dataConnectors = $this.attr('data-connectors');

                    if (dataConnectors) {
                        connectors = dataConnectors.split(',');
                        for (var i = 0, len = connectors.length; i < len; i++) {
                            var connector = $.trim(connectors[i]);
                            // do nothing for empty connectors
                            if (connector) {
                                connectors[i] = connector;
                            }
                        }
                    }

                    if (modName && Tc.Module[modName]) {
                        modules.push(self.registerModule($this, modName, skins, connectors));
                    }
                }
            });

            return modules;
        },

        /**
         * Unregisters the modules given by the module instances.
         *
         * @method unregisterModule
         * @param {Array} modules
         *      A list containting the module instances to unregister
         */
        unregisterModules: function(modules) {
            var connectors = this.connectors;

            modules = modules || this.modules;

            if (modules === this.modules) {
                // Clear everything if the arrays are equal
                this.connectors = [];
                this.modules = [];
            } else {
                // Unregister the given modules
                for (var i = 0, len = modules.length; i < len; i++) {
                    var module = modules[i], index;

                    // Delete the references in the connectors
                    for (var connectorId in connectors) {
                        if (connectors.hasOwnProperty(connectorId)) {
                            connectors[connectorId].unregisterComponent(module);
                        }
                    }

                    // Delete the module instance itself
                    index = $.inArray(module, this.modules);
                    if (index > -1) {
                        delete this.modules[index];
                    }
                }
            }
        },

        /**
         * Starts (intializes) the registered modules.
         *
         * @method start
         * @param {Array} modules
         *      A list of the modules to start
         */
        start: function(modules) {
            modules = modules || this.modules;

            // Start the modules
            for (var i = 0, len = modules.length; i < len; i++) {
                modules[i].start();
            }
        },

        /**
         * Stops the registered modules.
         *
         * @method stop
         * @param {Array} modules
         *      A list containting the module instances to stop
         */
        stop: function(modules) {
            modules = modules || this.modules;

            // Stop the modules
            for (var i = 0, len = modules.length; i < len; i++) {
                modules[i].stop();
            }
        },

        /**
         * Registers a module.
         *
         * @method registerModule
         * @param {jQuery} $node
         *      The module node
         * @param {String} modName
         *      The module name. It must match the class name of the module
         * @param {Array} skins
         *      A list of skin names. Each entry must match a class name of a skin
         * @param {Array} connectors
         *      A list of connectors identifiers (e.g. MasterSlave-Navigation)
         *      Schema: {connectorName}-{connectorId}
         * @return {Module}
         *      The reference to the registered module
         */
        registerModule: function($node, modName, skins, connectors) {
            var modules = this.modules;

            modName = modName || undefined;
            skins = skins || [];
            connectors = connectors || [];

            if (modName && Tc.Module[modName]) {
                // Generate a unique ID for every module
                var id = modules.length;
                $node.data('id', id);

                // Instantiate module
                modules[id] = new Tc.Module[modName]($node,this.sandbox,id);

                // Decorate it
                for (var i = 0, len = skins.length; i < len; i++) {
                    var skinName = skins[i];

                    if (Tc.Module[modName][skinName]) {
                        modules[id] = modules[id].getDecoratedModule(modName, skinName);
                    }
                }

                // Register connections
                for (var i = 0, len = connectors.length; i < len; i++) {
                    this.registerConnection(connectors[i], modules[id]);
                }

                return modules[id];
            }

            return null;
        },

        /**
         * Registers a connection between a module and a connector.
         *
         * @method registerConnection
         * @param {String} connector
         *      The full connector name (e.g. MasterSlave-Navigation)
         * @param {Module} component
         *      The module instance
         */
        registerConnection: function(connector, component) {
            connector = $.trim(connector);

            var parts = connector.split('-'), connectorType, connectorId, identifier;

            if (parts.length === 1) {
                // default connector
                identifier = connectorId = parts[0];
            } else if (parts.length === 2) {
                // a specific connector type is given
                connectorType = parts[0];
                connectorId = parts[1];
                identifier = connectorType + connectorId;
            }

            if (identifier) {
                var connectors = this.connectors;

                if (!connectors[identifier]) {
                    // Instantiate the appropriate connector if it does not exist yet
                    if (!connectorType) {
                        connectors[identifier] = new Tc.Connector(connectorId);
                    } else if (Tc.Connector[connectorType]) {
                        connectors[identifier] = new Tc.Connector[connectorType](connectorId);
                    }
                }

                if (connectors[identifier]) {
                    /*
                     * The connector observes the component and attaches it as
                     * an observer.
                     */
                    component.attachConnector(connectors[identifier]);

                    /*
                     * The component wants to be informed over state changes.
                     * It registers it as connector member.
                     */
                    connectors[identifier].registerComponent(component);
                }
            }
        },

        /**
         * Unregisters a module from a connector.
         *
         * @method unregisterConnection
         * @param {String} connectorId
         *      The connector channel id (e.g. 2)
         * @param {Module} component
         *      The module instance
         */
        unregisterConnection: function(connectorId, component) {
            var connector = this.connectors[connectorId];

            // Delete the references in the connector and the module
            if (connector) {
                connector.unregisterComponent(component);
                component.detachConnector(connector);
            }
        }
    });
}
)(Tc.$);

(function($) {
    "use strict";

    /**
     * The sandbox is used as a central point to get resources from, grant
     * permissions, etc.  It is shared between all modules.
     *
     * @author Remo Brunschwiler
     * @namespace Tc
     * @class Sandbox
     */
    Tc.Sandbox = Class.extend({

        /**
         * Initializes the Sandbox.
         *
         * @method init
         * @constructor
         * @param {Applicaton} application 
         *      The application reference
         * @param {Object} config 
         *      The configuration
         */
        init: function(application, config) {

            /**
             * The application
             *
             * @property application
             * @type Application
             */
            this.application = application;

            /**
             * The configuration.
             *
             * @property config
             * @type Object
             */
            this.config = config;

            /**
             * Contains the 'after' hook module callbacks.
             *
             * @property afterCallbacks
             * @type Array
             */
            this.afterCallbacks = [];
        },

        /**
         * Adds (register and start) all modules in the given context scope.
         *
         * @method addModules
         * @param {jQuery} $ctx 
         *      The jQuery context
         * @return {Array} 
         *      A list containing the references of the registered modules
         */
        addModules: function($ctx) {
            var modules = []
              , application = this.application;

            if ($ctx) {
                // Register modules
                modules = application.registerModules($ctx);

                // Start modules
                application.start(modules);
            }

            return modules;
        },

        /**
         * Removes a module by module instances.
         * This stops and unregisters a module through a module instance.
         *
         * @method removeModules
         * @param {Array} modules 
         *      A list containting the module instances to remove
         */
        removeModules: function(modules) {
            var application = this.application;

            if (modules) {
                // Stop modules
                application.stop(modules);

                // Unregister modules
                application.unregisterModules(modules);
            }
        },

        /**
         * Subscribes a module to a connector.
         *
         * @method subscribe
         * @param {String} connector The full connector name (e.g. MasterSlave-Navigation)
         * @param {Module} module The module instance
         */
        subscribe: function(connector, module) {
            var application = this.application;

            if (module instanceof Tc.Module && connector) {
                // explicitly cast connector to string
                connector = connector + '';
                application.registerConnection(connector, module);
            }
        },

        /**
         * Unsubscribes a module from a connector.
         *
         * @method unsubscribe
         * @param {String} connectorId The connector channel id (e.g. 2 or Navigation)
         * @param {Module} module The module instance
         */
        unsubscribe: function(connectorId, module) {
            var application = this.application;

            if (module instanceof Tc.Module && connectorId) {
                // explicitly cast connector id to string
                connectorId = connectorId + '';
                application.unregisterConnection(connectorId, module);
            }
        },

        /**
         * Gets the appropriate module for the given ID.
         *
         * @method getModuleById
         * @param {int} id 
         *      The module ID
         * @return {Module} 
         *      The appropriate module
         */
        getModuleById: function(id) {
            var application = this.application;

            if (application.modules[id] !== undefined) {
                return application.modules[id];
            } else {
                throw new Error('the module with the id ' + id + ' does not exist');
            }
        },

        /**
         * Gets the application config.
         *
         * @method getConfig
         * @return {Object} 
         *      The configuration object
         */
        getConfig: function() {
            return this.config;
        },

        /**
         * Gets an application config param.
         *
         * @method getConfigParam
         * @param {String} name 
         *      The param name
         * @return {mixed} 
         *      The appropriate configuration param
         */
        getConfigParam: function(name) {
            var config = this.config;

            if (config[name] !== undefined) {
                return config[name];
            } else {
                throw new Error('the config param ' + name + ' does not exist');
            }
        },

        /**
         * Collects the module status messages and handles the callbacks.
         * This means that it is ready for the 'after' hook.
         *
         * @method ready
         * @param {Function} callback 
         *      The 'after' hook module callback
         */
        ready: function(callback) {
            var afterCallbacks = this.afterCallbacks;

            // Add the callback to the stack
            afterCallbacks.push(callback);

            // Check whether all modules are ready for the 'after' hook
            if (this.application.modules.length === afterCallbacks.length) {
                for (var i = 0; i < afterCallbacks.length; i++) {
                    var afterCallback = afterCallbacks[i];

                    if (typeof afterCallback === "function") {
                        // make sure the callback is only executed once (and is not called during addModules)
                        delete afterCallbacks[i];
                        afterCallback();
                    }
                }
            }
        }
    });
}
)(Tc.$);

(function($) {
    "use strict";

    /**
     * Base class for the different modules.
     *
     * @author Remo Brunschwiler
     * @namespace Tc
     * @class Module
     */
    Tc.Module = Class.extend({

        /**
         * Initializes the Module.
         *
         * @method init
         * @constructor
         * @param {jQuery} $ctx
         *      The jQuery context
         * @param {Sandbox} sandbox
         *      The sandbox to get the resources from
         * @param {String} id
         *      The Unique module ID
         */
        init: function($ctx, sandbox, id) {
            /**
             * Contains the module context.
             *
             * @property $ctx
             * @type jQuery
             */
            this.$ctx = $ctx;

            /**
             * Contains the unique module ID.
             *
             * @property id
             * @type String
             */
            this.id = id;

            /**
             * Contains the attached connectors.
             *
             * @property connectors
             * @type Object
             */
            this.connectors = {};

            /**
             * The sandbox to get the resources from.
             *
             * @property sandbox
             * @type Sandbox
             */
            this.sandbox = sandbox;
        },

        /**
         * Template method to start (i.e. init) the module.
         * This method provides hook functions which can be overridden
         * by the individual instance.
         *
         * @method start
         */
        start: function() {
            var self = this;

            // Call the hook method from the individual instance and provide the appropriate callback
            if (this.on) {
                this.on(function() {
                    self.initAfter();
                });
            }
        },

        /**
         * Template method to stop the module.
         *
         * @method stop
         */
        stop: function() {
            var $ctx = this.$ctx;

            // Remove all bound events and associated jQuery data
            $('*', $ctx).unbind().removeData();
            $ctx.unbind().removeData();
        },

        /**
         * Initialization callback.
         *
         * @method initAfter
         * @protected
         */
        initAfter: function() {
            var self = this;

            this.sandbox.ready(function() {
                /*
                 * Call the 'after' hook method from the individual instance
                 */
                if (self.after) {
                    self.after();
                }
            });
        },

        /**
         * Notifies all attached connectors about changes.
         *
         * @method fire
         * @param {String} state The new state
         * @param {Object} data The data to provide to your connected modules (optional)
         * @param {Array} channels  A list containting the channel ids to send the event to (optional)
         * @param {Function} defaultAction The default action to perform (optinal)
         */
        fire: function(state, data, channels, defaultAction) {
            var self = this
              , connectors = this.connectors
              , shouldBeCalled = true;
            // indicates whether the default handler should be called

            // validate params
            if (channels == null && defaultAction == null) {
                // Max. 2 params
                if (typeof data === 'function') {
                    // (state, defaultAction)
                    defaultAction = data;
                    data = undefined;
                } else if ($.isArray(data)) {
                    // (state, channels)
                    channels = data;
                    data = undefined;
                }
            } else if (defaultAction == null) {
                // 2-3 params
                if (typeof channels === 'function') {
                    // (state, data, defaultAction)
                    defaultAction = channels;
                    channels = undefined;
                }

                if ($.isArray(data)) {
                    // (state, channels, defaultAction)
                    channels = data;
                    data = undefined;
                }
            }

            state = Tc.Utils.String.capitalize(state);
            data = data || {};
            channels = channels || Object.keys(connectors);

            for (var i = 0, len = channels.length; i < len; i++) {
                var connectorId = channels[i];
                if (connectors.hasOwnProperty(connectorId)) {
                    var connector = connectors[connectorId]
                      , proceed = connector.notify(self, 'on' + state, data) || false;

                    if (!proceed) {
                        shouldBeCalled = false;
                    }

                } else {
                    throw new Error('the module #' + self.id + ' is not connected to connector ' + connectorId);
                }
            }

            // Execute default action unless a veto is provided
            if (shouldBeCalled) {
                if (typeof defaultAction === 'function') {
                    defaultAction();
                }
            }
        },

        /**
         * Attaches a connector (observer).
         *
         * @method attachConnector
         * @param {Connector} connector
         *      The connector to attach
         */
        attachConnector: function(connector) {
            this.connectors[connector.connectorId] = connector;
        },

        /**
         * Detaches a connector (observer).
         *
         * @method detachConnector
         * @param {Connector} connector The connector to detach
         */
        detachConnector: function(connector) {
            delete this.connectors[connector.connectorId];
        },

        /**
         * Decorates itself with the given skin.
         *
         * @method getDecoratedModule
         * @param {String} module The name of the module
         * @param {String} skin The name of the skin
         * @return {Module} The decorated module
         */
        getDecoratedModule: function(module, skin) {
            if (Tc.Module[module][skin]) {
                var Decorator = Tc.Module[module][skin];

                /*
                 * Sets the prototype object to the module.
                 * So the "non-decorated" functions will be called on the module
                 * without implementing the whole module interface.
                 */
                Decorator.prototype = this;
                Decorator.prototype.constructor = Tc.Module[module][skin];

                return new Decorator(this);
            }

            return null;
        }
    });
}
)(Tc.$);

(function($) {
    "use strict";

    /**
     * Base class for the different connectors.
     *
     * @author Remo Brunschwiler
     * @namespace Tc
     * @class Connector
     */
    Tc.Connector = Class.extend({

        /**
         * Initializes the Connector.
         *
         * @method init
         * @constructor
         * @param {String} connectorId
         *      The unique connector ID
         */
        init: function(connectorId) {
            this.connectorId = connectorId;
            this.components = {};
        },

        /**
         * Registers a component.
         *
         * @method registerComponent
         * @param {Module} component 
         *      The module to register
         */
        registerComponent: function(component) {
            this.components[component.id] = {
                'component': component
            };
        },

        /**
         * Unregisters a component.
         *
         * @method unregisterComponent
         * @param {Module} component 
         *      The module to unregister
         */
        unregisterComponent: function(component) {
            var components = this.components;

            if (components[component.id]) {
                delete components[component.id];
            }
        },

        /**
         * Notifies all registered components about a state change 
         * This can be be overriden in the specific connectors.
         *
         * @method notify
         * @param {Module} origin
         *      The module that sends the state change
         * @param {String} state 
         *      The component's state
         * @param {Object} data 
         *      Contains the state relevant data (if any)
         * @return {boolean}
         *      Indicates whether the default action should be excuted or not
         */
        notify: function(origin, state, data, callback) {
            /*
             * Gives the components the ability to prevent the default- and
             * after action from the events by returning false in the
             * on {Event}-Handler.
             */
            var proceed = true
              , components = this.components;

            for (var id in components) {
                if (components.hasOwnProperty(id)) {
                    var component = components[id].component;
                    if (component !== origin && component[state]) {
                        if (component[state](data) === false) {
                            proceed = false;
                        }
                    }
                }
            }

            return proceed;
        }
    });
}
)(Tc.$);

/*
 * Contains utility functions for several tasks.
 */
Tc.Utils = {};

// Helper
if (!Object.keys) {
    Object.keys = function(obj) {
        var keys = [], k;
        for (k in obj) {
            if (Object.prototype.hasOwnProperty.call(obj, k)) {
                keys.push(k);
            }
        }
        return keys;
    }
    ;
}
/**
 * Contains utility functions for string concerning tasks.
 *
 * @author Remo Brunschwiler
 * @namespace Tc
 * @class Utils.String
 * @static
 */
(function($) {
    "use strict";

    Tc.Utils.String = {
        /**
         * Capitalizes the first letter of the given string.
         *
         * @method capitalize
         * @param {String} str 
         *      The original string
         * @return {String} 
         *      The capitalized string
         */
        capitalize: function(str) {
            // Capitalize the first letter
            return str.substr(0, 1).toUpperCase().concat(str.substr(1));
        },

        /**
         * Camelizes the given string.
         *
         * @method toCamel
         * @param {String} str 
         *      The original string
         * @return {String} 
         *      The camelized string
         */
        toCamel: function(str) {
            return str.replace(/(\-[A-Za-z])/g, function($1) {
                return $1.toUpperCase().replace('-', '');
            });
        }
    };
}
)(Tc.$);

/**
 * https://github.com/MarcDiethelm/terrificjs-extensions
 * Adds some sugar and enhancements to @brunschgi's excellent Terrificjs frontend framework.
 * @file terrificjs-extensions.js
 * @license MIT
 * @copyright 2014 Marc Diethelm
 */

(function($) {

    'use strict';

    /**
	 * Select elements in the module context. Usage: this.$(selector)
	 * @author Marc Diethelm <marc.diethelm@namics.com>
	 * @param {String} selector
	 * @returns {Object} - jQuery collection
	 */
    Tc.Module.prototype.$ = function $$(selector) {
        return this.$ctx.find(selector);
    }
    ;

    /**
	 * Deprecated. Use Tc.Module.prototype.$
	 * Select elements in the module context. Usage: this.$$(selector)
	 * @deprecated Use Tc.Module.prototype.$
	 * @see Tc.Module.prototype.$
	 * @author Marc Diethelm <marc.diethelm@namics.com>
	 * @param {String} selector
	 * @returns {Object} - jQuery collection
	 */
    Tc.Module.prototype.$$ = Tc.Module.prototype.$;

    /**
	 * Bind methods to Terrific module context. Usage: this.bindAll(funcName [,funcName...])
	 * Inspired by Underscore's bindAll. http://underscorejs.org/#bindAll
	 * @author Marc Diethelm <marc.diethelm@namics.com>
	 * @author Simon Harte <simon.harte@namics.com>
	 * @param {...String} methods - Names of methods each as a param.
	 * @returns {Module} - Returns the module instance for chaining.
	 */
    Tc.Module.prototype.bindAll = function bindAll(methods) {
        var i = 0, args = arguments, argLen = args.length, methodName;

        for (i; i < argLen; i++) {
            methodName = args[i];
            if (typeof this[methodName] === 'function') {
                this[methodName] = $.proxy(this, methodName);
            } else if (typeof methodName === 'string') {
                throw new TypeError('bindAll: Tc.Module.' + this.getName() + '.' + methodName + ' is not a function');
            } else {
                throw new TypeError('Arguments to bindAll must be strings');
            }
        }

        return this;
    }
    ;

    /**
	 * Get the name of the Terrific module
	 * @author Remo Brunschwiler <remo.brunschwiler@namics.com>
	 * @author Mathias Hayoz <mathias.hayoz@namics.com>
	 * @returns {String} - Module name
	 */
    Tc.Module.prototype.getName = function getName() {
        var property;
        if (!this._modName) {
            for (property in Tc.Module) {
                if (Tc.Module.hasOwnProperty(property) && property !== 'constructor' && this instanceof Tc.Module[property]) {
                    this._modName = property;
                    break;
                }
            }
        }

        return this._modName;
    }
    ;

    /**
	 * Simplify connector channel subscription. Usage: this.subscribe(channelName [,channelName...])
	 *
	 * Because the second parameter to sandbox.subscribe (this) is often forgotten.
	 * Additionally this method allows connecting to multiple channels at once.
	 * @author Simon Harte <simon.harte@namics.com>
	 * @param {...String} channels - Connector channels to subscribe to
	 * @returns {Module} - Returns the module instance for chaining
	 */
    Tc.Module.prototype.subscribe = function subscribe(channels) {
        var i = 0, args = arguments, argLen = args.length, channelName;

        for (i; i < argLen; i++) {
            channelName = args[i];
            this.sandbox.subscribe(channelName, this);
        }

        return this;
    }
    ;

    var tplCache = {};
    /**
	 * Micro-templating for modules. Extrapolates {{= foo }} variables in strings from data.
	 * This function is a remix of
	 * - Simple JavaScript Templating – John Resig - http://ejohn.org/ - MIT Licensed
	 * - https://gist.github.com/topliceanu/1537847
	 * - http://weblog.west-wind.com/posts/2008/Oct/13/Client-Templating-with-jQuery
	 * This code incorporates a fix for single-quote usage.
	 * @author Marc Diethelm <marc.diethelm@namics.com>
	 * @param {String} str - Template
	 * @param {Object} [data] - Optional, renders template immediately if present. Data to use as the template context for variable extrapolation.
	 * @returns {Function|String} - Template function, to render template with data, or if data was supplied already the rendered template.
	 */
    Tc.Module.prototype.template = function template(str, data) {

        // Figure out if we're getting a template, or if we need to
        // load the template - and be sure to cache the result.
        var fn = !/\W/.test(str) ? tplCache[str] = tplCache[str] || template(document.getElementById(str).innerHTML) : // Generate a reusable function that will serve as a template
        // generator (and which will be cached).
        /*jshint -W054, -W014 */
        new Function("obj","var p=[],print=function(){p.push.apply(p,arguments);};" + // Introduce the data as local variables using with(){}
        "with(obj){p.push('" + // Convert the template into pure JavaScript
        str.replace(/[\r\t\n]/g, " ").replace(/'(?=[^%]*\}\}>)/g, "\t").split("'").join("\\'").split("\t").join("'").replace(/\{\{=(.+?)\}\}/g, "',$1,'").split("{{").join("');").split("}}").join("p.push('") + "');}return p.join('');");
        /*jshint +W054, +W014 */
        // Provide some basic currying to the user
        return data ? fn(data) : fn;
    }
    ;

}
)(Tc.$);

(function($) {
    $(document).ready(function() {
        var application = new Tc.Application($('html'),{});
        application.registerModules();
        application.start();
    });
}
)(Tc.$);
var Utils = {
    TcSandbox: null
};

/* Called on click of submit button before validation starts */
function BIT_beforeValidateForm(form) {
    if (typeof BIT_ETRACKER !== "undefined") {
        BIT_ETRACKER.trackFormSubmit($(form));
    }
}

/* Called if validation was successful before form is submitted */
function BIT_beforeSubmitForm(form) {
    localStorage.basket = window.location.href;
    if (typeof BIT_ETRACKER !== "undefined") {
        BIT_ETRACKER.trackFormSuccess($(form));
    }
}

/* Called if validation of form failed */
function BIT_validationFailed(form) {
    if (typeof BIT_ETRACKER !== "undefined") {
        BIT_ETRACKER.trackFormError($(form));
    }
}
(function($) {

    'use strict';
    /**
	 *
	 * @static
	 * @namespace Tc.Utils
	 * @class Lib
	 */
    Tc.Utils.Lib = {
        /**
		 * Initializes Datepicker
		 *
		 * @method initDatepicker
		 * @param {Object} $ctx   $context in which datepickers should be loaded
		 * @return {void}
		 */
        initDatepicker: function($ctx) {
            $ctx.find('input[type=date]').each(function(index, element) {
                var $el = $(element);
                var $clone;
                if ($el.is(':not(.has-datepicker, .is-datepicker)')) {
                    $clone = $el.clone(false);
                    $clone.removeAttr('name id value').data('ref', $el.attr('id')).addClass('is-datepicker').datepicker({
                        format: 'dd.mm.yyyy',
                        autoclose: true,
                        weekStart: 1,
                        todayHighlight: true,
                        language: $('html').attr('lang') || 'en'
                    }).on('changeDate', function(ev) {
                        var $el = $(ev.currentTarget), $ref = $('#' + $el.data('ref')), date = Tc.Utils.Lib.convertSwissDatetoDate($el.val()), originalValue;
                        /*
								tried to fix CNGIMPL-1979, does not work correctly yet, because it prevents ALL user inputs
								if(isNaN(date.valueOf())){
									originalValue = $ref[0].getAttribute('value');
									$ref.val(originalValue);
									$el.val(Tc.Utils.Lib.formatSwissDate(Tc.Utils.Lib.convertISOtoDate(originalValue)));
								}else{
									$ref.val(Tc.Utils.Lib.formatISODate(ev.date));
								}
							*/
                        $ref.val(Tc.Utils.Lib.formatISODate(ev.date));
                    }).datepicker('setDate', Tc.Utils.Lib.convertISOtoDate($el.val()));
                    $el.addClass('has-datepicker').hide().after($clone);
                }
            });
        },

        formatISODate: function(date) {
            return date.getFullYear() + '-' + Tc.Utils.Lib.formatTwoDigit(date.getMonth() + 1) + '-' + Tc.Utils.Lib.formatTwoDigit(date.getDate());
        },

        formatSwissDate: function(date) {
            return Tc.Utils.Lib.formatTwoDigit(date.getDate()) + '.' + Tc.Utils.Lib.formatTwoDigit(date.getMonth() + 1) + '.' + date.getFullYear();
        },

        convertISOtoDate: function(iso) {
            var arr = iso.split('-');
            return new Date(arr[0],arr[1] - 1,arr[2]);
        },

        convertSwissDatetoDate: function(swissDate) {
            var arr = swissDate.split('.');
            return new Date(arr[2],arr[1] - 1,arr[0]);
        },

        formatTwoDigit: function(num) {
            return num < 10 ? '0' + num : num;
        },

        /* ******** */
        /* Glossary */
        /* ******** */
        initGlossary: function($ctx) {
            var dataUrl = $('body').data('glossary');
            if (dataUrl) {
                if ($ctx === undefined) {
                    $ctx = $('.js-glossary-context').find('.mod-text p, .mod-teaser p, .mod-textimage p, .mod-table p');
                }
                $.getJSON(dataUrl, function(data) {
                    Tc.Utils.Lib.parseGlossaryJson(data, $ctx)
                });
            }

        },
        parseGlossaryJson: function(data, $ctx) {
            Tc.Utils.Lib.glossaryData = data;
            $ctx.each(Tc.Utils.Lib.replaceGlossaryItems);
        },
        replaceGlossaryItems: function(index, element) {
            var $element = $(element), entry, textnodes = Tc.Utils.Lib.getTextNodesIn($element[0], false, ['abbr']), curNode, curHtml, parentNode, resultNodes;

            function getPlaceholder(i) {
                entry = Tc.Utils.Lib.glossaryData[i];
                return '##' + entry.title.trim() + '##';
            }

            function preventDoubleReplacement() {
                for (var i = 0; i < Tc.Utils.Lib.glossaryData.length; i++) {
                    var placeHolder = getPlaceholder(i);
                    curHtml = curHtml.replace(new RegExp('\\b(' + entry.title.trim() + ')\\b','ig'), placeHolder);
                }
                return i;
            }

            function replacePlaceholderWithEntryTitle() {
                for (var i = 0; i < Tc.Utils.Lib.glossaryData.length; i++) {
                    var placeHolder = getPlaceholder(i);
                    var regExp = new RegExp(placeHolder,"g");
                    curHtml = curHtml.replace(regExp, '<abbr title=\"' + entry.definition.trim().replace(/"/g, "&quot;") + '\">' + entry.title.trim() + '</abbr>');
                }
                return i;
            }

            for (var k = 0; k < textnodes.length; k++) {
                curNode = textnodes[k];
                curHtml = textnodes[k].textContent;
                parentNode = curNode.parentNode;

                preventDoubleReplacement();
                replacePlaceholderWithEntryTitle();
                resultNodes = $('<span>' + curHtml + '</span>').contents();
                //need to place this in some kind of element in order to have valid nodes
                for (var m = 0; m < resultNodes.length; m++) {
                    parentNode.insertBefore(resultNodes[m], curNode);
                }
                parentNode.removeChild(curNode);
            }
        },
        /**
		 * Function to find all textnodes in given html-element, optionaly excluding given nodenames
		 *
		 * @method _getTextNodesIn
		 * @param {Object} node                     plain HTML-Object
		 * @param {Boolean} includeWhitespaceNodes  flag if whitespace nodes should be includes
		 * @param {Array} excludeNodes              Array of lowercase node names to exclude
		 * @return {Array} textnodes
		 */
        getTextNodesIn: function(node, includeWhitespaceNodes, excludeNodes) {
            var textNodes = []
              , nonWhitespaceMatcher = /\S/;
            if (excludeNodes === undefined)
                excludeNodes = [];

            function getTextNodes(node, excludeNodes) {
                if (node.nodeType == 3) {
                    if (includeWhitespaceNodes || nonWhitespaceMatcher.test(node.nodeValue)) {
                        textNodes.push(node);
                    }
                } else {
                    //only move on if tag is not listed in excludeNodes
                    if (excludeNodes.indexOf(node.nodeName.toLocaleLowerCase()) === -1) {
                        for (var i = 0, len = node.childNodes.length; i < len; ++i) {
                            getTextNodes(node.childNodes[i], excludeNodes);
                        }
                    }
                }
            }

            getTextNodes(node, excludeNodes);
            return textNodes;
        },

        /* ********** */
        /* Validation */
        /* ********** */
        /**
		 * Initialises jQuery Validation Plugin with several custom validation methods.
		 * * .js-usepattern will trigger evaluation of pattern-attribute (class is set automatically if pattern-attribute is present)
		 * * .js-validgroup will check if all group members .js-group-element (wrapped in .js-group with data-group="NAME") are valid
		 *
		 * @method initValidation
		 * @param {Object}  jQuery-Object of form
		 * @return {Object} validator
		 */
        initValidation: function($form) {
            $.validator.addMethod('required', function(value, element) {
                if (value === false) {
                    value = '';
                }
                var valid = value.length > 0;
                if (valid) {
                    $(element).closest('.form-group').removeClass('has-warning');
                } else {
                    $(element).closest('.form-group').addClass('has-warning');
                }
                return valid;
            }, 'field is required');

            $.validator.addMethod('js-usepattern', function(value, element) {
                if (value === false) {
                    value = '';
                }
                var valid = (value.search(new RegExp($(element).attr('pattern'),$(element).data('pattern-modifier') || 'i')) !== -1);
                if (valid) {
                    $(element).closest('.form-group').removeClass('has-warning');
                } else {
                    $(element).closest('.form-group').addClass('has-warning');
                }
                return valid;
            }, 'pattern not valid');

            $.validator.addMethod('js-validgroup', function(value, element) {
                var valid = true
                  , filled = 0
                  , $elements = $(element).closest('.js-group').find('.js-group-element');
                $elements.each(function() {
                    var $curEl = $(this)
                      , pattern = $curEl.attr('pattern')
                      , val = $curEl.val();
                    if (val.length > 0) {
                        filled += 1;
                    }
                    if (pattern) {
                        if (val.search(new RegExp(pattern)) === -1) {
                            valid = false;
                            return false;
                        }
                    }
                });
                if (valid) {
                    $elements.removeClass('error');
                }
                if (valid && filled > 0 && filled !== $elements.length) {
                    valid = false;
                }
                return valid;
            }, 'group invalid');

            /* groups */
            var config = {
                groups: {}
            };
            $form.find('.js-group').each(function() {
                var $curEl = $(this);
                $curEl.find('.js-group-element').each(function() {
                    config.groups[$curEl.data('group')] += ' ' + $(this).attr('name');
                });
            });

            $form.find('input[pattern]').each(function() {
                $(this).addClass('js-usepattern');
            });

            return $form.validate($.extend(true, config, {
                ignore: ":hidden, .js-disable-validation",
                errorElement: 'span',
                errorClass: 'error',
                onfocusout: false,
                errorPlacement: function($error, $element) {
                    $error.addClass('help-block');
                    $element.parent().append($error);
                },
                showErrors: function(map, list) {
                    var i;
                    for (i = 0; i < list.length; i += 1) {
                        list[i].message = $(list[i].element).data('validationtext') || list[i].message;
                    }
                    this.defaultShowErrors(list);
                },
                rules: {},
                groups: {}
            }));
        },

        getFileSize: function(uploadField) {
            var _this = this
              , $field = $(uploadField);

            if ($field.attr('type') != "file") {
                return -1;
            }

            if (uploadField.files) {
                // file api available
                if (uploadField.files.length > 0) {
                    // file selected
                    return uploadField.files[0].size;
                } else {
                    return 0;
                }
            } else {
                return -1;
            }
        },

        validateUploadField: function(uploadField, maxSizeAll, maxSizeSingle, message) {
            var _this = this, $field = $(uploadField), $form = $field.parents('form'), formId = $form.attr('id'), $allUploads = $form.find("input[type='file']"), myFileSize, totalFileSize = 0;

            if ($field.attr('type') != "file") {
                return true;
            }

            myFileSize = _this.getFileSize(uploadField);
            $allUploads.each(function(index, element) {
                totalFileSize += _this.getFileSize(element);
            });

            if (myFileSize > maxSizeSingle || totalFileSize > maxSizeAll) {
                cq5forms_showMsg(formId, $field.attr('name'), message);
                return false;
            }

            return true;

        },

        /**
         * see https://www.creativejuiz.fr/blog/en/javascript-en/read-url-get-parameters-with-javascript
         * @param paramName the param to read from the url
         */
        getUrlParameter: function(paramName) {
            var vars = {};
            window.location.href.replace(location.hash, '').replace(/[?&]+([^=&]+)=?([^&]*)?/gi, // regexp
            function(m, key, value) {
                // callback
                vars[key] = value !== undefined ? value : '';
            });

            if (paramName) {
                return vars[paramName] ? vars[paramName] : null;
            }
            return vars;
        },

        /**
         * maximize body on load to fix issue of hidden page dialog
         */
        maximizeBodyInEditMode: function() {
            $(function() {
                var docHeight = $(document).height()
                  , $body = $('body.skin-layout-editmode');
                if ($body.length > 0 && $body.height() < docHeight) {
                    $body.css('min-height', docHeight);
                }
            });

            $(window).on("resize", $.debounce(200, Tc.Utils.Lib.maximizeBodyInEditMode));
        },

        /**
         * String functions
         */

        stringIsEmpty: function(str) {
            return (!str || 0 === str.length);
        },

        stringIsBlank: function(str) {
            return (!str || /^\s*$/.test(str));
        }

    };

    Tc.Utils.Lib.maximizeBodyInEditMode();

}
)(Tc.$);

(function($) {

    'use strict';
    /**
	 *
	 * @static
	 * @namespace Tc.Utils
	 * @class EqualHeight
	 */
    Tc.Utils.EqualHeight = {
        $equals: $(),
        /* public methods */
        addElements: function($elements) {
            Tc.Utils.EqualHeight.$equals = Tc.Utils.EqualHeight.$equals.add($elements);
        },

        set: function() {
            Tc.Utils.EqualHeight.$equals /*.filter(':not([data-equalheight-exclude*='+Tc.Utils.Responsive.getCurrentBPName()+'])')*/
            .each(Tc.Utils.EqualHeight.setHeights);
        },

        reset: function() {
            Tc.Utils.EqualHeight.$equals /*.filter('[data-equalheight-exclude*='+Tc.Utils.Responsive.getCurrentBPName()+']')*/
            .each(Tc.Utils.EqualHeight.resetHeights);
        },
        /* internal methods, only used by this utils */
        getMaxHeight: function($items) {
            var i = 0, max = 0, currHeight;
            for (i; i < $items.length; i += 1) {
                currHeight = $items.eq(i).height();
                max = currHeight > max ? currHeight : max;
            }
            return max;
        },

        setHeights: function(index, item) {
            var $item = $(item), elements = Tc.Utils.EqualHeight.getEqualHeightElements($item), i, $curEl;
            $item.removeClass('equalheight-ready');
            for (i = 0; i < elements.length; i += 1) {
                $curEl = elements[i];
                $curEl.height('');
                $curEl.height(Tc.Utils.EqualHeight.getMaxHeight($curEl));
            }
            $item.addClass('equalheight-ready');
        },

        resetHeights: function(index, item) {
            var $item = $(item), elements = Tc.Utils.EqualHeight.getEqualHeightElements($item), i, $curEl;
            $item.removeClass('equalheight-ready');
            for (i = 0; i < elements.length; i += 1) {
                $curEl = elements[i];
                $curEl.height('');
            }
        },

        getElementsPerLine: function($container) {
            var $childs = $container.children(), lines = [], $el, curOffset, curLineIndex = 0;
            $childs.each(function(i, el) {
                $el = $(el);
                if (i === 0) {
                    lines[curLineIndex] = $();
                    curOffset = $el.offset().top;
                } else if ($el.offset().top !== curOffset) {
                    curLineIndex += 1;
                    lines[curLineIndex] = $();
                    curOffset = $el.offset().top;
                }
                lines[curLineIndex] = lines[curLineIndex].add($el);
            });
            return lines;
        },

        getEqualHeightElements: function($container) {
            var group = $container.data('equalheight-group'), $elements;
            if (group) {
                $elements = $container.find('.js-equalheight-element[data-equalheight-group=' + group + ']');
            } else if ($container.is('[data-equal-height-elements="true"]')) {
                $elements = $container.find('.js-equalheight-element');
            } else if ($container.is('[data-equal-height-selector]')) {
                $elements = $container.find($container.data('equal-height-selector'));
            } else {
                $elements = $container.children();
            }

            if ($container.data('equalheight-line')) {
                return this.getElementsPerLine($elements);
            }

            return [$elements];
        }
    };
}
)(Tc.$);

(function($) {
    'use strict';
    /**
	 *
	 * @static
	 * @namespace Tc.Utils
	 * @class Extensions
	 */
    Tc.Utils.Extensions = {
        isMobileDevice: function() {
            if (/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)) {
                return true;
            }
            return false;
        }
    };
}
)(Tc.$);

(function($) {

    'use strict';

    function AssertionError(sMessage) {
        this.name = "AssertionError";
        this.message = sMessage;
        this.stack = (new Error()).stack;
    }
    AssertionError.prototype = Object.create(Error.prototype);
    AssertionError.prototype.constructor = AssertionError;

    /**
	 *
	 * @static
	 * @namespace Tc.Utils
	 * @class Test
	 */
    Tc.Utils.Test = {

        /**
         *
         * @param expected
         * @param actual
         */
        assertEquals: function(expected, actual) {
            if (expected !== actual) {
                throw new AssertionError("not equal! expected: " + expected + ", actual: " + actual);
            }
        }

    };

}
)(Tc.$);

(function($) {

    'use strict';

    /**
	 *
	 * @static
	 * @namespace Tc.Utils
	 * @class WCM
	 */
    Tc.Utils.WCM = {

        /**
         * Returns the current content window.
		 * This is either the top browser window or the content frame if a page is viewed inside
		 * the content finder.
         */
        getCurrentWindow: function() {

            if (window.location.href.indexOf("cf#") > -1 && document.getElementById("cq-cf-frame")) {
                return document.getElementById("cq-cf-frame").contentWindow;
            } else {
                return window;
            }

        },

        getParsedQueryString: function() {
            var that = this
              , queryString = Tc.Utils.WCM.getCurrentWindow().location.search.replace("?", "")
              , params = {};

            var querystringKeyValueArray = queryString.split("&");
            for (var i = 0; i < querystringKeyValueArray.length; i++) {
                var pair = querystringKeyValueArray[i].split("=");
                if (pair && pair.length === 2) {
                    params[pair[0]] = pair[1];
                }
            }

            return params;
        }

    };

}
)(Tc.$);

/* Modernizr 2.8.3 (Custom Build) | MIT & BSD
 * Build: http://modernizr.com/download/#-inputtypes-load
 */
;window.Modernizr = function(a, b, c) {
    function u(a) {
        i.cssText = a
    }
    function v(a, b) {
        return u(prefixes.join(a + ";") + (b || ""))
    }
    function w(a, b) {
        return typeof a === b
    }
    function x(a, b) {
        return !!~("" + a).indexOf(b)
    }
    function y(a, b, d) {
        for (var e in a) {
            var f = b[a[e]];
            if (f !== c)
                return d === !1 ? a[e] : w(f, "function") ? f.bind(d || b) : f
        }
        return !1
    }
    function z() {
        e.inputtypes = function(a) {
            for (var d = 0, e, g, h, i = a.length; d < i; d++)
                j.setAttribute("type", g = a[d]),
                e = j.type !== "text",
                e && (j.value = k,
                j.style.cssText = "position:absolute;visibility:hidden;",
                /^range$/.test(g) && j.style.WebkitAppearance !== c ? (f.appendChild(j),
                h = b.defaultView,
                e = h.getComputedStyle && h.getComputedStyle(j, null).WebkitAppearance !== "textfield" && j.offsetHeight !== 0,
                f.removeChild(j)) : /^(search|tel)$/.test(g) || (/^(url|email)$/.test(g) ? e = j.checkValidity && j.checkValidity() === !1 : e = j.value != k)),
                n[a[d]] = !!e;
            return n
        }("search tel url email datetime date month week time datetime-local number range color".split(" "))
    }
    var d = "2.8.3", e = {}, f = b.documentElement, g = "modernizr", h = b.createElement(g), i = h.style, j = b.createElement("input"), k = ":)", l = {}.toString, m = {}, n = {}, o = {}, p = [], q = p.slice, r, s = {}.hasOwnProperty, t;
    !w(s, "undefined") && !w(s.call, "undefined") ? t = function(a, b) {
        return s.call(a, b)
    }
    : t = function(a, b) {
        return b in a && w(a.constructor.prototype[b], "undefined")
    }
    ,
    Function.prototype.bind || (Function.prototype.bind = function(b) {
        var c = this;
        if (typeof c != "function")
            throw new TypeError;
        var d = q.call(arguments, 1)
          , e = function() {
            if (this instanceof e) {
                var a = function() {};
                a.prototype = c.prototype;
                var f = new a
                  , g = c.apply(f, d.concat(q.call(arguments)));
                return Object(g) === g ? g : f
            }
            return c.apply(b, d.concat(q.call(arguments)))
        };
        return e
    }
    );
    for (var A in m)
        t(m, A) && (r = A.toLowerCase(),
        e[r] = m[A](),
        p.push((e[r] ? "" : "no-") + r));
    return e.input || z(),
    e.addTest = function(a, b) {
        if (typeof a == "object")
            for (var d in a)
                t(a, d) && e.addTest(d, a[d]);
        else {
            a = a.toLowerCase();
            if (e[a] !== c)
                return e;
            b = typeof b == "function" ? b() : b,
            typeof enableClasses != "undefined" && enableClasses && (f.className += " " + (b ? "" : "no-") + a),
            e[a] = b
        }
        return e
    }
    ,
    u(""),
    h = j = null,
    e._version = d,
    e
}(this, this.document),
function(a, b, c) {
    function d(a) {
        return "[object Function]" == o.call(a)
    }
    function e(a) {
        return "string" == typeof a
    }
    function f() {}
    function g(a) {
        return !a || "loaded" == a || "complete" == a || "uninitialized" == a
    }
    function h() {
        var a = p.shift();
        q = 1,
        a ? a.t ? m(function() {
            ("c" == a.t ? B.injectCss : B.injectJs)(a.s, 0, a.a, a.x, a.e, 1)
        }, 0) : (a(),
        h()) : q = 0
    }
    function i(a, c, d, e, f, i, j) {
        function k(b) {
            if (!o && g(l.readyState) && (u.r = o = 1,
            !q && h(),
            l.onload = l.onreadystatechange = null,
            b)) {
                "img" != a && m(function() {
                    t.removeChild(l)
                }, 50);
                for (var d in y[c])
                    y[c].hasOwnProperty(d) && y[c][d].onload()
            }
        }
        var j = j || B.errorTimeout
          , l = b.createElement(a)
          , o = 0
          , r = 0
          , u = {
            t: d,
            s: c,
            e: f,
            a: i,
            x: j
        };
        1 === y[c] && (r = 1,
        y[c] = []),
        "object" == a ? l.data = c : (l.src = c,
        l.type = a),
        l.width = l.height = "0",
        l.onerror = l.onload = l.onreadystatechange = function() {
            k.call(this, r)
        }
        ,
        p.splice(e, 0, u),
        "img" != a && (r || 2 === y[c] ? (t.insertBefore(l, s ? null : n),
        m(k, j)) : y[c].push(l))
    }
    function j(a, b, c, d, f) {
        return q = 0,
        b = b || "j",
        e(a) ? i("c" == b ? v : u, a, b, this.i++, c, d, f) : (p.splice(this.i++, 0, a),
        1 == p.length && h()),
        this
    }
    function k() {
        var a = B;
        return a.loader = {
            load: j,
            i: 0
        },
        a
    }
    var l = b.documentElement, m = a.setTimeout, n = b.getElementsByTagName("script")[0], o = {}.toString, p = [], q = 0, r = "MozAppearance"in l.style, s = r && !!b.createRange().compareNode, t = s ? l : n.parentNode, l = a.opera && "[object Opera]" == o.call(a.opera), l = !!b.attachEvent && !l, u = r ? "object" : l ? "script" : "img", v = l ? "script" : u, w = Array.isArray || function(a) {
        return "[object Array]" == o.call(a)
    }
    , x = [], y = {}, z = {
        timeout: function(a, b) {
            return b.length && (a.timeout = b[0]),
            a
        }
    }, A, B;
    B = function(a) {
        function b(a) {
            var a = a.split("!"), b = x.length, c = a.pop(), d = a.length, c = {
                url: c,
                origUrl: c,
                prefixes: a
            }, e, f, g;
            for (f = 0; f < d; f++)
                g = a[f].split("="),
                (e = z[g.shift()]) && (c = e(c, g));
            for (f = 0; f < b; f++)
                c = x[f](c);
            return c
        }
        function g(a, e, f, g, h) {
            var i = b(a)
              , j = i.autoCallback;
            i.url.split(".").pop().split("?").shift(),
            i.bypass || (e && (e = d(e) ? e : e[a] || e[g] || e[a.split("/").pop().split("?")[0]]),
            i.instead ? i.instead(a, e, f, g, h) : (y[i.url] ? i.noexec = !0 : y[i.url] = 1,
            f.load(i.url, i.forceCSS || !i.forceJS && "css" == i.url.split(".").pop().split("?").shift() ? "c" : c, i.noexec, i.attrs, i.timeout),
            (d(e) || d(j)) && f.load(function() {
                k(),
                e && e(i.origUrl, h, g),
                j && j(i.origUrl, h, g),
                y[i.url] = 2
            })))
        }
        function h(a, b) {
            function c(a, c) {
                if (a) {
                    if (e(a))
                        c || (j = function() {
                            var a = [].slice.call(arguments);
                            k.apply(this, a),
                            l()
                        }
                        ),
                        g(a, j, b, 0, h);
                    else if (Object(a) === a)
                        for (n in m = function() {
                            var b = 0, c;
                            for (c in a)
                                a.hasOwnProperty(c) && b++;
                            return b
                        }(),
                        a)
                            a.hasOwnProperty(n) && (!c && !--m && (d(j) ? j = function() {
                                var a = [].slice.call(arguments);
                                k.apply(this, a),
                                l()
                            }
                            : j[n] = function(a) {
                                return function() {
                                    var b = [].slice.call(arguments);
                                    a && a.apply(this, b),
                                    l()
                                }
                            }(k[n])),
                            g(a[n], j, b, n, h))
                } else
                    !c && l()
            }
            var h = !!a.test, i = a.load || a.both, j = a.callback || f, k = j, l = a.complete || f, m, n;
            c(h ? a.yep : a.nope, !!i),
            i && c(i)
        }
        var i, j, l = this.yepnope.loader;
        if (e(a))
            g(a, 0, l, 0);
        else if (w(a))
            for (i = 0; i < a.length; i++)
                j = a[i],
                e(j) ? g(j, 0, l, 0) : w(j) ? B(j) : Object(j) === j && h(j, l);
        else
            Object(a) === a && h(a, l)
    }
    ,
    B.addPrefix = function(a, b) {
        z[a] = b
    }
    ,
    B.addFilter = function(a) {
        x.push(a)
    }
    ,
    B.errorTimeout = 1e4,
    null == b.readyState && b.addEventListener && (b.readyState = "loading",
    b.addEventListener("DOMContentLoaded", A = function() {
        b.removeEventListener("DOMContentLoaded", A, 0),
        b.readyState = "complete"
    }
    , 0)),
    a.yepnope = k(),
    a.yepnope.executeStack = h,
    a.yepnope.injectJs = function(a, c, d, e, i, j) {
        var k = b.createElement("script"), l, o, e = e || B.errorTimeout;
        k.src = a;
        for (o in d)
            k.setAttribute(o, d[o]);
        c = j ? h : c || f,
        k.onreadystatechange = k.onload = function() {
            !l && g(k.readyState) && (l = 1,
            c(),
            k.onload = k.onreadystatechange = null)
        }
        ,
        m(function() {
            l || (l = 1,
            c(1))
        }, e),
        i ? k.onload() : n.parentNode.insertBefore(k, n)
    }
    ,
    a.yepnope.injectCss = function(a, c, d, e, g, i) {
        var e = b.createElement("link"), j, c = i ? h : c || f;
        e.href = a,
        e.rel = "stylesheet",
        e.type = "text/css";
        for (j in d)
            e.setAttribute(j, d[j]);
        g || (n.parentNode.insertBefore(e, n),
        m(c, 0))
    }
}(this, document),
Modernizr.load = function() {
    yepnope.apply(window, [].slice.call(arguments, 0))
}
;
/*!
  hey, [be]Lazy.js - v1.8.2 - 2016.10.25
  A fast, small and dependency free lazy load script (https://github.com/dinbror/blazy)
  (c) Bjoern Klinggaard - @bklinggaard - http://dinbror.dk/blazy
*/
(function(q, m) {
    "function" === typeof define && define.amd ? define(m) : "object" === typeof exports ? module.exports = m() : q.Blazy = m()
}
)(this, function() {
    function q(b) {
        var c = b._util;
        c.elements = E(b.options);
        c.count = c.elements.length;
        c.destroyed && (c.destroyed = !1,
        b.options.container && l(b.options.container, function(a) {
            n(a, "scroll", c.validateT)
        }),
        n(window, "resize", c.saveViewportOffsetT),
        n(window, "resize", c.validateT),
        n(window, "scroll", c.validateT));
        m(b)
    }
    function m(b) {
        for (var c = b._util, a = 0; a < c.count; a++) {
            var d = c.elements[a], e;
            a: {
                var g = d;
                e = b.options;
                var p = g.getBoundingClientRect();
                if (e.container && y && (g = g.closest(e.containerClass))) {
                    g = g.getBoundingClientRect();
                    e = r(g, f) ? r(p, {
                        top: g.top - e.offset,
                        right: g.right + e.offset,
                        bottom: g.bottom + e.offset,
                        left: g.left - e.offset
                    }) : !1;
                    break a
                }
                e = r(p, f)
            }
            if (e || t(d, b.options.successClass))
                b.load(d),
                c.elements.splice(a, 1),
                c.count--,
                a--
        }
        0 === c.count && b.destroy()
    }
    function r(b, c) {
        return b.right >= c.left && b.bottom >= c.top && b.left <= c.right && b.top <= c.bottom
    }
    function z(b, c, a) {
        if (!t(b, a.successClass) && (c || a.loadInvisible || 0 < b.offsetWidth && 0 < b.offsetHeight))
            if (c = b.getAttribute(u) || b.getAttribute(a.src)) {
                c = c.split(a.separator);
                var d = c[A && 1 < c.length ? 1 : 0]
                  , e = b.getAttribute(a.srcset)
                  , g = "img" === b.nodeName.toLowerCase()
                  , p = (c = b.parentNode) && "picture" === c.nodeName.toLowerCase();
                if (g || void 0 === b.src) {
                    var h = new Image
                      , w = function() {
                        a.error && a.error(b, "invalid");
                        v(b, a.errorClass);
                        k(h, "error", w);
                        k(h, "load", f)
                    }
                      , f = function() {
                        g ? p || B(b, d, e) : b.style.backgroundImage = 'url("' + d + '")';
                        x(b, a);
                        k(h, "load", f);
                        k(h, "error", w)
                    };
                    p && (h = b,
                    l(c.getElementsByTagName("source"), function(b) {
                        var c = a.srcset
                          , e = b.getAttribute(c);
                        e && (b.setAttribute("srcset", e),
                        b.removeAttribute(c))
                    }));
                    n(h, "error", w);
                    n(h, "load", f);
                    B(h, d, e)
                } else
                    b.src = d,
                    x(b, a)
            } else
                "video" === b.nodeName.toLowerCase() ? (l(b.getElementsByTagName("source"), function(b) {
                    var c = a.src
                      , e = b.getAttribute(c);
                    e && (b.setAttribute("src", e),
                    b.removeAttribute(c))
                }),
                b.load(),
                x(b, a)) : (a.error && a.error(b, "missing"),
                v(b, a.errorClass))
    }
    function x(b, c) {
        v(b, c.successClass);
        c.success && c.success(b);
        b.removeAttribute(c.src);
        b.removeAttribute(c.srcset);
        l(c.breakpoints, function(a) {
            b.removeAttribute(a.src)
        })
    }
    function B(b, c, a) {
        a && b.setAttribute("srcset", a);
        b.src = c
    }
    function t(b, c) {
        return -1 !== (" " + b.className + " ").indexOf(" " + c + " ")
    }
    function v(b, c) {
        t(b, c) || (b.className += " " + c)
    }
    function E(b) {
        var c = [];
        b = b.root.querySelectorAll(b.selector);
        for (var a = b.length; a--; c.unshift(b[a]))
            ;
        return c
    }
    function C(b) {
        f.bottom = (window.innerHeight || document.documentElement.clientHeight) + b;
        f.right = (window.innerWidth || document.documentElement.clientWidth) + b
    }
    function n(b, c, a) {
        b.attachEvent ? b.attachEvent && b.attachEvent("on" + c, a) : b.addEventListener(c, a, {
            capture: !1,
            passive: !0
        })
    }
    function k(b, c, a) {
        b.detachEvent ? b.detachEvent && b.detachEvent("on" + c, a) : b.removeEventListener(c, a, {
            capture: !1,
            passive: !0
        })
    }
    function l(b, c) {
        if (b && c)
            for (var a = b.length, d = 0; d < a && !1 !== c(b[d], d); d++)
                ;
    }
    function D(b, c, a) {
        var d = 0;
        return function() {
            var e = +new Date;
            e - d < c || (d = e,
            b.apply(a, arguments))
        }
    }
    var u, f, A, y;
    return function(b) {
        if (!document.querySelectorAll) {
            var c = document.createStyleSheet();
            document.querySelectorAll = function(a, b, d, h, f) {
                f = document.all;
                b = [];
                a = a.replace(/\[for\b/gi, "[htmlFor").split(",");
                for (d = a.length; d--; ) {
                    c.addRule(a[d], "k:v");
                    for (h = f.length; h--; )
                        f[h].currentStyle.k && b.push(f[h]);
                    c.removeRule(0)
                }
                return b
            }
        }
        var a = this
          , d = a._util = {};
        d.elements = [];
        d.destroyed = !0;
        a.options = b || {};
        a.options.error = a.options.error || !1;
        a.options.offset = a.options.offset || 100;
        a.options.root = a.options.root || document;
        a.options.success = a.options.success || !1;
        a.options.selector = a.options.selector || ".b-lazy";
        a.options.separator = a.options.separator || "|";
        a.options.containerClass = a.options.container;
        a.options.container = a.options.containerClass ? document.querySelectorAll(a.options.containerClass) : !1;
        a.options.errorClass = a.options.errorClass || "b-error";
        a.options.breakpoints = a.options.breakpoints || !1;
        a.options.loadInvisible = a.options.loadInvisible || !1;
        a.options.successClass = a.options.successClass || "b-loaded";
        a.options.validateDelay = a.options.validateDelay || 25;
        a.options.saveViewportOffsetDelay = a.options.saveViewportOffsetDelay || 50;
        a.options.srcset = a.options.srcset || "data-srcset";
        a.options.src = u = a.options.src || "data-src";
        y = Element.prototype.closest;
        A = 1 < window.devicePixelRatio;
        f = {};
        f.top = 0 - a.options.offset;
        f.left = 0 - a.options.offset;
        a.revalidate = function() {
            q(a)
        }
        ;
        a.load = function(a, b) {
            var c = this.options;
            void 0 === a.length ? z(a, b, c) : l(a, function(a) {
                z(a, b, c)
            })
        }
        ;
        a.destroy = function() {
            var a = this._util;
            this.options.container && l(this.options.container, function(b) {
                k(b, "scroll", a.validateT)
            });
            k(window, "scroll", a.validateT);
            k(window, "resize", a.validateT);
            k(window, "resize", a.saveViewportOffsetT);
            a.count = 0;
            a.elements.length = 0;
            a.destroyed = !0
        }
        ;
        d.validateT = D(function() {
            m(a)
        }, a.options.validateDelay, a);
        d.saveViewportOffsetT = D(function() {
            C(a.options.offset)
        }, a.options.saveViewportOffsetDelay, a);
        C(a.options.offset);
        l(a.options.breakpoints, function(a) {
            if (a.width >= window.screen.width)
                return u = a.src,
                !1
        });
        setTimeout(function() {
            q(a)
        })
    }
});
/*!
 * imagesLoaded PACKAGED v3.1.8
 * JavaScript is all like "You images are done yet or what?"
 * MIT License
 */

(function() {
    function e() {}
    function t(e, t) {
        for (var n = e.length; n--; )
            if (e[n].listener === t)
                return n;
        return -1
    }
    function n(e) {
        return function() {
            return this[e].apply(this, arguments)
        }
    }
    var i = e.prototype
      , r = this
      , o = r.EventEmitter;
    i.getListeners = function(e) {
        var t, n, i = this._getEvents();
        if ("object" == typeof e) {
            t = {};
            for (n in i)
                i.hasOwnProperty(n) && e.test(n) && (t[n] = i[n])
        } else
            t = i[e] || (i[e] = []);
        return t
    }
    ,
    i.flattenListeners = function(e) {
        var t, n = [];
        for (t = 0; e.length > t; t += 1)
            n.push(e[t].listener);
        return n
    }
    ,
    i.getListenersAsObject = function(e) {
        var t, n = this.getListeners(e);
        return n instanceof Array && (t = {},
        t[e] = n),
        t || n
    }
    ,
    i.addListener = function(e, n) {
        var i, r = this.getListenersAsObject(e), o = "object" == typeof n;
        for (i in r)
            r.hasOwnProperty(i) && -1 === t(r[i], n) && r[i].push(o ? n : {
                listener: n,
                once: !1
            });
        return this
    }
    ,
    i.on = n("addListener"),
    i.addOnceListener = function(e, t) {
        return this.addListener(e, {
            listener: t,
            once: !0
        })
    }
    ,
    i.once = n("addOnceListener"),
    i.defineEvent = function(e) {
        return this.getListeners(e),
        this
    }
    ,
    i.defineEvents = function(e) {
        for (var t = 0; e.length > t; t += 1)
            this.defineEvent(e[t]);
        return this
    }
    ,
    i.removeListener = function(e, n) {
        var i, r, o = this.getListenersAsObject(e);
        for (r in o)
            o.hasOwnProperty(r) && (i = t(o[r], n),
            -1 !== i && o[r].splice(i, 1));
        return this
    }
    ,
    i.off = n("removeListener"),
    i.addListeners = function(e, t) {
        return this.manipulateListeners(!1, e, t)
    }
    ,
    i.removeListeners = function(e, t) {
        return this.manipulateListeners(!0, e, t)
    }
    ,
    i.manipulateListeners = function(e, t, n) {
        var i, r, o = e ? this.removeListener : this.addListener, s = e ? this.removeListeners : this.addListeners;
        if ("object" != typeof t || t instanceof RegExp)
            for (i = n.length; i--; )
                o.call(this, t, n[i]);
        else
            for (i in t)
                t.hasOwnProperty(i) && (r = t[i]) && ("function" == typeof r ? o.call(this, i, r) : s.call(this, i, r));
        return this
    }
    ,
    i.removeEvent = function(e) {
        var t, n = typeof e, i = this._getEvents();
        if ("string" === n)
            delete i[e];
        else if ("object" === n)
            for (t in i)
                i.hasOwnProperty(t) && e.test(t) && delete i[t];
        else
            delete this._events;
        return this
    }
    ,
    i.removeAllListeners = n("removeEvent"),
    i.emitEvent = function(e, t) {
        var n, i, r, o, s = this.getListenersAsObject(e);
        for (r in s)
            if (s.hasOwnProperty(r))
                for (i = s[r].length; i--; )
                    n = s[r][i],
                    n.once === !0 && this.removeListener(e, n.listener),
                    o = n.listener.apply(this, t || []),
                    o === this._getOnceReturnValue() && this.removeListener(e, n.listener);
        return this
    }
    ,
    i.trigger = n("emitEvent"),
    i.emit = function(e) {
        var t = Array.prototype.slice.call(arguments, 1);
        return this.emitEvent(e, t)
    }
    ,
    i.setOnceReturnValue = function(e) {
        return this._onceReturnValue = e,
        this
    }
    ,
    i._getOnceReturnValue = function() {
        return this.hasOwnProperty("_onceReturnValue") ? this._onceReturnValue : !0
    }
    ,
    i._getEvents = function() {
        return this._events || (this._events = {})
    }
    ,
    e.noConflict = function() {
        return r.EventEmitter = o,
        e
    }
    ,
    "function" == typeof define && define.amd ? define("eventEmitter/EventEmitter", [], function() {
        return e
    }) : "object" == typeof module && module.exports ? module.exports = e : this.EventEmitter = e
}
).call(this),
function(e) {
    function t(t) {
        var n = e.event;
        return n.target = n.target || n.srcElement || t,
        n
    }
    var n = document.documentElement
      , i = function() {};
    n.addEventListener ? i = function(e, t, n) {
        e.addEventListener(t, n, !1)
    }
    : n.attachEvent && (i = function(e, n, i) {
        e[n + i] = i.handleEvent ? function() {
            var n = t(e);
            i.handleEvent.call(i, n)
        }
        : function() {
            var n = t(e);
            i.call(e, n)
        }
        ,
        e.attachEvent("on" + n, e[n + i])
    }
    );
    var r = function() {};
    n.removeEventListener ? r = function(e, t, n) {
        e.removeEventListener(t, n, !1)
    }
    : n.detachEvent && (r = function(e, t, n) {
        e.detachEvent("on" + t, e[t + n]);
        try {
            delete e[t + n]
        } catch (i) {
            e[t + n] = void 0
        }
    }
    );
    var o = {
        bind: i,
        unbind: r
    };
    "function" == typeof define && define.amd ? define("eventie/eventie", o) : e.eventie = o
}(this),
function(e, t) {
    "function" == typeof define && define.amd ? define(["eventEmitter/EventEmitter", "eventie/eventie"], function(n, i) {
        return t(e, n, i)
    }) : "object" == typeof exports ? module.exports = t(e, require("wolfy87-eventemitter"), require("eventie")) : e.imagesLoaded = t(e, e.EventEmitter, e.eventie)
}(window, function(e, t, n) {
    function i(e, t) {
        for (var n in t)
            e[n] = t[n];
        return e
    }
    function r(e) {
        return "[object Array]" === d.call(e)
    }
    function o(e) {
        var t = [];
        if (r(e))
            t = e;
        else if ("number" == typeof e.length)
            for (var n = 0, i = e.length; i > n; n++)
                t.push(e[n]);
        else
            t.push(e);
        return t
    }
    function s(e, t, n) {
        if (!(this instanceof s))
            return new s(e,t);
        "string" == typeof e && (e = document.querySelectorAll(e)),
        this.elements = o(e),
        this.options = i({}, this.options),
        "function" == typeof t ? n = t : i(this.options, t),
        n && this.on("always", n),
        this.getImages(),
        a && (this.jqDeferred = new a.Deferred);
        var r = this;
        setTimeout(function() {
            r.check()
        })
    }
    function f(e) {
        this.img = e
    }
    function c(e) {
        this.src = e,
        v[e] = this
    }
    var a = e.jQuery
      , u = e.console
      , h = u !== void 0
      , d = Object.prototype.toString;
    s.prototype = new t,
    s.prototype.options = {},
    s.prototype.getImages = function() {
        this.images = [];
        for (var e = 0, t = this.elements.length; t > e; e++) {
            var n = this.elements[e];
            "IMG" === n.nodeName && this.addImage(n);
            var i = n.nodeType;
            if (i && (1 === i || 9 === i || 11 === i))
                for (var r = n.querySelectorAll("img"), o = 0, s = r.length; s > o; o++) {
                    var f = r[o];
                    this.addImage(f)
                }
        }
    }
    ,
    s.prototype.addImage = function(e) {
        var t = new f(e);
        this.images.push(t)
    }
    ,
    s.prototype.check = function() {
        function e(e, r) {
            return t.options.debug && h && u.log("confirm", e, r),
            t.progress(e),
            n++,
            n === i && t.complete(),
            !0
        }
        var t = this
          , n = 0
          , i = this.images.length;
        if (this.hasAnyBroken = !1,
        !i)
            return this.complete(),
            void 0;
        for (var r = 0; i > r; r++) {
            var o = this.images[r];
            o.on("confirm", e),
            o.check()
        }
    }
    ,
    s.prototype.progress = function(e) {
        this.hasAnyBroken = this.hasAnyBroken || !e.isLoaded;
        var t = this;
        setTimeout(function() {
            t.emit("progress", t, e),
            t.jqDeferred && t.jqDeferred.notify && t.jqDeferred.notify(t, e)
        })
    }
    ,
    s.prototype.complete = function() {
        var e = this.hasAnyBroken ? "fail" : "done";
        this.isComplete = !0;
        var t = this;
        setTimeout(function() {
            if (t.emit(e, t),
            t.emit("always", t),
            t.jqDeferred) {
                var n = t.hasAnyBroken ? "reject" : "resolve";
                t.jqDeferred[n](t)
            }
        })
    }
    ,
    a && (a.fn.imagesLoaded = function(e, t) {
        var n = new s(this,e,t);
        return n.jqDeferred.promise(a(this))
    }
    ),
    f.prototype = new t,
    f.prototype.check = function() {
        var e = v[this.img.src] || new c(this.img.src);
        if (e.isConfirmed)
            return this.confirm(e.isLoaded, "cached was confirmed"),
            void 0;
        if (this.img.complete && void 0 !== this.img.naturalWidth)
            return this.confirm(0 !== this.img.naturalWidth, "naturalWidth"),
            void 0;
        var t = this;
        e.on("confirm", function(e, n) {
            return t.confirm(e.isLoaded, n),
            !0
        }),
        e.check()
    }
    ,
    f.prototype.confirm = function(e, t) {
        this.isLoaded = e,
        this.emit("confirm", this, t)
    }
    ;
    var v = {};
    return c.prototype = new t,
    c.prototype.check = function() {
        if (!this.isChecked) {
            var e = new Image;
            n.bind(e, "load", this),
            n.bind(e, "error", this),
            e.src = this.src,
            this.isChecked = !0
        }
    }
    ,
    c.prototype.handleEvent = function(e) {
        var t = "on" + e.type;
        this[t] && this[t](e)
    }
    ,
    c.prototype.onload = function(e) {
        this.confirm(!0, "onload"),
        this.unbindProxyEvents(e)
    }
    ,
    c.prototype.onerror = function(e) {
        this.confirm(!1, "onerror"),
        this.unbindProxyEvents(e)
    }
    ,
    c.prototype.confirm = function(e, t) {
        this.isConfirmed = !0,
        this.isLoaded = e,
        this.emit("confirm", this, t)
    }
    ,
    c.prototype.unbindProxyEvents = function(e) {
        n.unbind(e.target, "load", this),
        n.unbind(e.target, "error", this)
    }
    ,
    s
});
/*! matchMedia() polyfill - Test a CSS media type/query in JS. Authors & copyright (c) 2012: Scott Jehl, Paul Irish, Nicholas Zakas, David Knight. Dual MIT/BSD license */

window.matchMedia || (window.matchMedia = function() {
    "use strict";

    // For browsers that support matchMedium api such as IE 9 and webkit
    var styleMedia = (window.styleMedia || window.media);

    // For those that don't support matchMedium
    if (!styleMedia) {
        var style = document.createElement('style')
          , script = document.getElementsByTagName('script')[0]
          , info = null;

        style.type = 'text/css';
        style.id = 'matchmediajs-test';

        script.parentNode.insertBefore(style, script);

        // 'style.currentStyle' is used by IE <= 8 and 'window.getComputedStyle' for all other browsers
        info = ('getComputedStyle'in window) && window.getComputedStyle(style, null) || style.currentStyle;

        styleMedia = {
            matchMedium: function(media) {
                var text = '@media ' + media + '{ #matchmediajs-test { width: 1px; } }';

                // 'style.styleSheet' is used by IE <= 8 and 'style.textContent' for all other browsers
                if (style.styleSheet) {
                    style.styleSheet.cssText = text;
                } else {
                    style.textContent = text;
                }

                // Test if media query is true or false
                return info.width === '1px';
            }
        };
    }

    return function(media) {
        return {
            matches: styleMedia.matchMedium(media || 'all'),
            media: media || 'all'
        };
    }
    ;
}());

/*! matchMedia() polyfill addListener/removeListener extension. Author & copyright (c) 2012: Scott Jehl. Dual MIT/BSD license */
(function() {
    // Bail out for browsers that have addListener support
    if (window.matchMedia && window.matchMedia('all').addListener) {
        return false;
    }

    var localMatchMedia = window.matchMedia
      , hasMediaQueries = localMatchMedia('only all').matches
      , isListening = false
      , timeoutID = 0
      , // setTimeout for debouncing 'handleChange'
    queries = []
      , // Contains each 'mql' and associated 'listeners' if 'addListener' is used
    handleChange = function(evt) {
        // Debounce
        clearTimeout(timeoutID);

        timeoutID = setTimeout(function() {
            for (var i = 0, il = queries.length; i < il; i++) {
                var mql = queries[i].mql
                  , listeners = queries[i].listeners || []
                  , matches = localMatchMedia(mql.media).matches;

                // Update mql.matches value and call listeners
                // Fire listeners only if transitioning to or from matched state
                if (matches !== mql.matches) {
                    mql.matches = matches;

                    for (var j = 0, jl = listeners.length; j < jl; j++) {
                        listeners[j].call(window, mql);
                    }
                }
            }
        }, 30);
    };

    window.matchMedia = function(media) {
        var mql = localMatchMedia(media)
          , listeners = []
          , index = 0;

        mql.addListener = function(listener) {
            // Changes would not occur to css media type so return now (Affects IE <= 8)
            if (!hasMediaQueries) {
                return;
            }

            // Set up 'resize' listener for browsers that support CSS3 media queries (Not for IE <= 8)
            // There should only ever be 1 resize listener running for performance
            if (!isListening) {
                isListening = true;
                window.addEventListener('resize', handleChange, true);
            }

            // Push object only if it has not been pushed already
            if (index === 0) {
                index = queries.push({
                    mql: mql,
                    listeners: listeners
                });
            }

            listeners.push(listener);
        }
        ;

        mql.removeListener = function(listener) {
            for (var i = 0, il = listeners.length; i < il; i++) {
                if (listeners[i] === listener) {
                    listeners.splice(i, 1);
                }
            }
        }
        ;

        return mql;
    }
    ;
}());

"function" != typeof Object.create && (Object.create = function(t) {
    function o() {}
    return o.prototype = t,
    new o
}
),
function(t, o, i, s) {
    "use strict";
    var n = {
        _positionClasses: ["bottom-left", "bottom-right", "top-right", "top-left", "bottom-center", "top-center", "mid-center"],
        _defaultIcons: ["success", "error", "info", "warning"],
        init: function(o, i) {
            this.prepareOptions(o, t.toast.options),
            this.process()
        },
        prepareOptions: function(o, i) {
            var s = {};
            "string" == typeof o || o instanceof Array ? s.text = o : s = o,
            this.options = t.extend({}, i, s)
        },
        process: function() {
            this.setup(),
            this.addToDom(),
            this.position(),
            this.bindToast(),
            this.animate()
        },
        setup: function() {
            var o = "";
            if (this._toastEl = this._toastEl || t("<div></div>", {
                class: "jq-toast-single"
            }),
            o += '<span class="jq-toast-loader"></span>',
            this.options.allowToastClose && (o += '<span class="close-jq-toast-single">×</span>'),
            this.options.text instanceof Array) {
                this.options.heading && (o += '<h2 class="jq-toast-heading">' + this.options.heading + "</h2>"),
                o += '<ul class="jq-toast-ul">';
                for (var i = 0; i < this.options.text.length; i++)
                    o += '<li class="jq-toast-li" id="jq-toast-item-' + i + '">' + this.options.text[i] + "</li>";
                o += "</ul>"
            } else
                this.options.heading && (o += '<h2 class="jq-toast-heading">' + this.options.heading + "</h2>"),
                o += this.options.text;
            this._toastEl.html(o),
            !1 !== this.options.bgColor && this._toastEl.css("background-color", this.options.bgColor),
            !1 !== this.options.textColor && this._toastEl.css("color", this.options.textColor),
            this.options.textAlign && this._toastEl.css("text-align", this.options.textAlign),
            !1 !== this.options.icon && (this._toastEl.addClass("jq-has-icon"),
            -1 !== t.inArray(this.options.icon, this._defaultIcons) && this._toastEl.addClass("jq-icon-" + this.options.icon)),
            !1 !== this.options.class && this._toastEl.addClass(this.options.class)
        },
        position: function() {
            "string" == typeof this.options.position && -1 !== t.inArray(this.options.position, this._positionClasses) ? "bottom-center" === this.options.position ? this._container.css({
                left: t(o).outerWidth() / 2 - this._container.outerWidth() / 2,
                bottom: 20
            }) : "top-center" === this.options.position ? this._container.css({
                left: t(o).outerWidth() / 2 - this._container.outerWidth() / 2,
                top: 20
            }) : "mid-center" === this.options.position ? this._container.css({
                left: t(o).outerWidth() / 2 - this._container.outerWidth() / 2,
                top: t(o).outerHeight() / 2 - this._container.outerHeight() / 2
            }) : this._container.addClass(this.options.position) : "object" == typeof this.options.position ? this._container.css({
                top: this.options.position.top ? this.options.position.top : "auto",
                bottom: this.options.position.bottom ? this.options.position.bottom : "auto",
                left: this.options.position.left ? this.options.position.left : "auto",
                right: this.options.position.right ? this.options.position.right : "auto"
            }) : this._container.addClass("bottom-left")
        },
        bindToast: function() {
            var t = this;
            this._toastEl.on("afterShown", function() {
                t.processLoader()
            }),
            this._toastEl.find(".close-jq-toast-single").on("click", function(o) {
                o.preventDefault(),
                "fade" === t.options.showHideTransition ? (t._toastEl.trigger("beforeHide"),
                t._toastEl.fadeOut(function() {
                    t._toastEl.trigger("afterHidden")
                })) : "slide" === t.options.showHideTransition ? (t._toastEl.trigger("beforeHide"),
                t._toastEl.slideUp(function() {
                    t._toastEl.trigger("afterHidden")
                })) : (t._toastEl.trigger("beforeHide"),
                t._toastEl.hide(function() {
                    t._toastEl.trigger("afterHidden")
                }))
            }),
            "function" == typeof this.options.beforeShow && this._toastEl.on("beforeShow", function() {
                t.options.beforeShow(t._toastEl)
            }),
            "function" == typeof this.options.afterShown && this._toastEl.on("afterShown", function() {
                t.options.afterShown(t._toastEl)
            }),
            "function" == typeof this.options.beforeHide && this._toastEl.on("beforeHide", function() {
                t.options.beforeHide(t._toastEl)
            }),
            "function" == typeof this.options.afterHidden && this._toastEl.on("afterHidden", function() {
                t.options.afterHidden(t._toastEl)
            }),
            "function" == typeof this.options.onClick && this._toastEl.on("click", function() {
                t.options.onClick(t._toastEl)
            })
        },
        addToDom: function() {
            var o = t(".jq-toast-wrap");
            if (0 === o.length ? (o = t("<div></div>", {
                class: "jq-toast-wrap",
                role: "alert",
                "aria-live": "polite"
            }),
            t("body").append(o)) : this.options.stack && !isNaN(parseInt(this.options.stack, 10)) || o.empty(),
            o.find(".jq-toast-single:hidden").remove(),
            o.append(this._toastEl),
            this.options.stack && !isNaN(parseInt(this.options.stack), 10)) {
                var i = o.find(".jq-toast-single").length - this.options.stack;
                i > 0 && t(".jq-toast-wrap").find(".jq-toast-single").slice(0, i).remove()
            }
            this._container = o
        },
        canAutoHide: function() {
            return !1 !== this.options.hideAfter && !isNaN(parseInt(this.options.hideAfter, 10))
        },
        processLoader: function() {
            if (!this.canAutoHide() || !1 === this.options.loader)
                return !1;
            var t = this._toastEl.find(".jq-toast-loader")
              , o = (this.options.hideAfter - 400) / 1e3 + "s"
              , i = this.options.loaderBg
              , s = t.attr("style") || "";
            s = s.substring(0, s.indexOf("-webkit-transition")),
            s += "-webkit-transition: width " + o + " ease-in;                       -o-transition: width " + o + " ease-in;                       transition: width " + o + " ease-in;                       background-color: " + i + ";",
            t.attr("style", s).addClass("jq-toast-loaded")
        },
        animate: function() {
            t = this;
            if (this._toastEl.hide(),
            this._toastEl.trigger("beforeShow"),
            "fade" === this.options.showHideTransition.toLowerCase() ? this._toastEl.fadeIn(function() {
                t._toastEl.trigger("afterShown")
            }) : "slide" === this.options.showHideTransition.toLowerCase() ? this._toastEl.slideDown(function() {
                t._toastEl.trigger("afterShown")
            }) : this._toastEl.show(function() {
                t._toastEl.trigger("afterShown")
            }),
            this.canAutoHide()) {
                var t = this;
                o.setTimeout(function() {
                    "fade" === t.options.showHideTransition.toLowerCase() ? (t._toastEl.trigger("beforeHide"),
                    t._toastEl.fadeOut(function() {
                        t._toastEl.trigger("afterHidden")
                    })) : "slide" === t.options.showHideTransition.toLowerCase() ? (t._toastEl.trigger("beforeHide"),
                    t._toastEl.slideUp(function() {
                        t._toastEl.trigger("afterHidden")
                    })) : (t._toastEl.trigger("beforeHide"),
                    t._toastEl.hide(function() {
                        t._toastEl.trigger("afterHidden")
                    }))
                }, this.options.hideAfter)
            }
        },
        reset: function(o) {
            "all" === o ? t(".jq-toast-wrap").remove() : this._toastEl.remove()
        },
        update: function(t) {
            this.prepareOptions(t, this.options),
            this.setup(),
            this.bindToast()
        },
        close: function() {
            this._toastEl.find(".close-jq-toast-single").click()
        }
    };
    t.toast = function(t) {
        var o = Object.create(n);
        return o.init(t, this),
        {
            reset: function(t) {
                o.reset(t)
            },
            update: function(t) {
                o.update(t)
            },
            close: function() {
                o.close()
            }
        }
    }
    ,
    t.toast.options = {
        text: "",
        heading: "",
        showHideTransition: "fade",
        allowToastClose: !0,
        hideAfter: 3e3,
        loader: !0,
        loaderBg: "#9EC600",
        stack: 5,
        position: "bottom-left",
        bgColor: !1,
        textColor: !1,
        textAlign: "left",
        icon: !1,
        beforeShow: function() {},
        afterShown: function() {},
        beforeHide: function() {},
        afterHidden: function() {},
        onClick: function() {}
    }
}(jQuery, window, document);

/*!
 * Vue.js v2.7.14
 * (c) 2014-2022 Evan You
 * Released under the MIT License.
 */
/*!
 * Vue.js v2.7.14
 * (c) 2014-2022 Evan You
 * Released under the MIT License.
 */
!function(t, e) {
    "object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : (t = "undefined" != typeof globalThis ? globalThis : t || self).Vue = e()
}(this, (function() {
    "use strict";
    var t = Object.freeze({})
      , e = Array.isArray;
    function n(t) {
        return null == t
    }
    function r(t) {
        return null != t
    }
    function o(t) {
        return !0 === t
    }
    function i(t) {
        return "string" == typeof t || "number" == typeof t || "symbol" == typeof t || "boolean" == typeof t
    }
    function a(t) {
        return "function" == typeof t
    }
    function s(t) {
        return null !== t && "object" == typeof t
    }
    var c = Object.prototype.toString;
    function u(t) {
        return "[object Object]" === c.call(t)
    }
    function l(t) {
        var e = parseFloat(String(t));
        return e >= 0 && Math.floor(e) === e && isFinite(t)
    }
    function f(t) {
        return r(t) && "function" == typeof t.then && "function" == typeof t.catch
    }
    function d(t) {
        return null == t ? "" : Array.isArray(t) || u(t) && t.toString === c ? JSON.stringify(t, null, 2) : String(t)
    }
    function p(t) {
        var e = parseFloat(t);
        return isNaN(e) ? t : e
    }
    function v(t, e) {
        for (var n = Object.create(null), r = t.split(","), o = 0; o < r.length; o++)
            n[r[o]] = !0;
        return e ? function(t) {
            return n[t.toLowerCase()]
        }
        : function(t) {
            return n[t]
        }
    }
    var h = v("slot,component", !0)
      , m = v("key,ref,slot,slot-scope,is");
    function g(t, e) {
        var n = t.length;
        if (n) {
            if (e === t[n - 1])
                return void (t.length = n - 1);
            var r = t.indexOf(e);
            if (r > -1)
                return t.splice(r, 1)
        }
    }
    var y = Object.prototype.hasOwnProperty;
    function _(t, e) {
        return y.call(t, e)
    }
    function b(t) {
        var e = Object.create(null);
        return function(n) {
            return e[n] || (e[n] = t(n))
        }
    }
    var $ = /-(\w)/g
      , w = b((function(t) {
        return t.replace($, (function(t, e) {
            return e ? e.toUpperCase() : ""
        }
        ))
    }
    ))
      , x = b((function(t) {
        return t.charAt(0).toUpperCase() + t.slice(1)
    }
    ))
      , C = /\B([A-Z])/g
      , k = b((function(t) {
        return t.replace(C, "-$1").toLowerCase()
    }
    ));
    var S = Function.prototype.bind ? function(t, e) {
        return t.bind(e)
    }
    : function(t, e) {
        function n(n) {
            var r = arguments.length;
            return r ? r > 1 ? t.apply(e, arguments) : t.call(e, n) : t.call(e)
        }
        return n._length = t.length,
        n
    }
    ;
    function O(t, e) {
        e = e || 0;
        for (var n = t.length - e, r = new Array(n); n--; )
            r[n] = t[n + e];
        return r
    }
    function T(t, e) {
        for (var n in e)
            t[n] = e[n];
        return t
    }
    function A(t) {
        for (var e = {}, n = 0; n < t.length; n++)
            t[n] && T(e, t[n]);
        return e
    }
    function j(t, e, n) {}
    var E = function(t, e, n) {
        return !1
    }
      , N = function(t) {
        return t
    };
    function P(t, e) {
        if (t === e)
            return !0;
        var n = s(t)
          , r = s(e);
        if (!n || !r)
            return !n && !r && String(t) === String(e);
        try {
            var o = Array.isArray(t)
              , i = Array.isArray(e);
            if (o && i)
                return t.length === e.length && t.every((function(t, n) {
                    return P(t, e[n])
                }
                ));
            if (t instanceof Date && e instanceof Date)
                return t.getTime() === e.getTime();
            if (o || i)
                return !1;
            var a = Object.keys(t)
              , c = Object.keys(e);
            return a.length === c.length && a.every((function(n) {
                return P(t[n], e[n])
            }
            ))
        } catch (t) {
            return !1
        }
    }
    function D(t, e) {
        for (var n = 0; n < t.length; n++)
            if (P(t[n], e))
                return n;
        return -1
    }
    function M(t) {
        var e = !1;
        return function() {
            e || (e = !0,
            t.apply(this, arguments))
        }
    }
    function I(t, e) {
        return t === e ? 0 === t && 1 / t != 1 / e : t == t || e == e
    }
    var L = "data-server-rendered"
      , R = ["component", "directive", "filter"]
      , F = ["beforeCreate", "created", "beforeMount", "mounted", "beforeUpdate", "updated", "beforeDestroy", "destroyed", "activated", "deactivated", "errorCaptured", "serverPrefetch", "renderTracked", "renderTriggered"]
      , H = {
        optionMergeStrategies: Object.create(null),
        silent: !1,
        productionTip: !1,
        devtools: !1,
        performance: !1,
        errorHandler: null,
        warnHandler: null,
        ignoredElements: [],
        keyCodes: Object.create(null),
        isReservedTag: E,
        isReservedAttr: E,
        isUnknownElement: E,
        getTagNamespace: j,
        parsePlatformTagName: N,
        mustUseProp: E,
        async: !0,
        _lifecycleHooks: F
    }
      , B = /a-zA-Z\u00B7\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u037D\u037F-\u1FFF\u200C-\u200D\u203F-\u2040\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD/;
    function U(t) {
        var e = (t + "").charCodeAt(0);
        return 36 === e || 95 === e
    }
    function z(t, e, n, r) {
        Object.defineProperty(t, e, {
            value: n,
            enumerable: !!r,
            writable: !0,
            configurable: !0
        })
    }
    var V = new RegExp("[^".concat(B.source, ".$_\\d]"));
    var K = "__proto__"in {}
      , J = "undefined" != typeof window
      , q = J && window.navigator.userAgent.toLowerCase()
      , W = q && /msie|trident/.test(q)
      , Z = q && q.indexOf("msie 9.0") > 0
      , G = q && q.indexOf("edge/") > 0;
    q && q.indexOf("android");
    var X = q && /iphone|ipad|ipod|ios/.test(q);
    q && /chrome\/\d+/.test(q),
    q && /phantomjs/.test(q);
    var Y, Q = q && q.match(/firefox\/(\d+)/), tt = {}.watch, et = !1;
    if (J)
        try {
            var nt = {};
            Object.defineProperty(nt, "passive", {
                get: function() {
                    et = !0
                }
            }),
            window.addEventListener("test-passive", null, nt)
        } catch (t) {}
    var rt = function() {
        return void 0 === Y && (Y = !J && "undefined" != typeof global && (global.process && "server" === global.process.env.VUE_ENV)),
        Y
    }
      , ot = J && window.__VUE_DEVTOOLS_GLOBAL_HOOK__;
    function it(t) {
        return "function" == typeof t && /native code/.test(t.toString())
    }
    var at, st = "undefined" != typeof Symbol && it(Symbol) && "undefined" != typeof Reflect && it(Reflect.ownKeys);
    at = "undefined" != typeof Set && it(Set) ? Set : function() {
        function t() {
            this.set = Object.create(null)
        }
        return t.prototype.has = function(t) {
            return !0 === this.set[t]
        }
        ,
        t.prototype.add = function(t) {
            this.set[t] = !0
        }
        ,
        t.prototype.clear = function() {
            this.set = Object.create(null)
        }
        ,
        t
    }();
    var ct = null;
    function ut(t) {
        void 0 === t && (t = null),
        t || ct && ct._scope.off(),
        ct = t,
        t && t._scope.on()
    }
    var lt = function() {
        function t(t, e, n, r, o, i, a, s) {
            this.tag = t,
            this.data = e,
            this.children = n,
            this.text = r,
            this.elm = o,
            this.ns = void 0,
            this.context = i,
            this.fnContext = void 0,
            this.fnOptions = void 0,
            this.fnScopeId = void 0,
            this.key = e && e.key,
            this.componentOptions = a,
            this.componentInstance = void 0,
            this.parent = void 0,
            this.raw = !1,
            this.isStatic = !1,
            this.isRootInsert = !0,
            this.isComment = !1,
            this.isCloned = !1,
            this.isOnce = !1,
            this.asyncFactory = s,
            this.asyncMeta = void 0,
            this.isAsyncPlaceholder = !1
        }
        return Object.defineProperty(t.prototype, "child", {
            get: function() {
                return this.componentInstance
            },
            enumerable: !1,
            configurable: !0
        }),
        t
    }()
      , ft = function(t) {
        void 0 === t && (t = "");
        var e = new lt;
        return e.text = t,
        e.isComment = !0,
        e
    };
    function dt(t) {
        return new lt(void 0,void 0,void 0,String(t))
    }
    function pt(t) {
        var e = new lt(t.tag,t.data,t.children && t.children.slice(),t.text,t.elm,t.context,t.componentOptions,t.asyncFactory);
        return e.ns = t.ns,
        e.isStatic = t.isStatic,
        e.key = t.key,
        e.isComment = t.isComment,
        e.fnContext = t.fnContext,
        e.fnOptions = t.fnOptions,
        e.fnScopeId = t.fnScopeId,
        e.asyncMeta = t.asyncMeta,
        e.isCloned = !0,
        e
    }
    var vt = 0
      , ht = []
      , mt = function() {
        function t() {
            this._pending = !1,
            this.id = vt++,
            this.subs = []
        }
        return t.prototype.addSub = function(t) {
            this.subs.push(t)
        }
        ,
        t.prototype.removeSub = function(t) {
            this.subs[this.subs.indexOf(t)] = null,
            this._pending || (this._pending = !0,
            ht.push(this))
        }
        ,
        t.prototype.depend = function(e) {
            t.target && t.target.addDep(this)
        }
        ,
        t.prototype.notify = function(t) {
            for (var e = this.subs.filter((function(t) {
                return t
            }
            )), n = 0, r = e.length; n < r; n++) {
                e[n].update()
            }
        }
        ,
        t
    }();
    mt.target = null;
    var gt = [];
    function yt(t) {
        gt.push(t),
        mt.target = t
    }
    function _t() {
        gt.pop(),
        mt.target = gt[gt.length - 1]
    }
    var bt = Array.prototype
      , $t = Object.create(bt);
    ["push", "pop", "shift", "unshift", "splice", "sort", "reverse"].forEach((function(t) {
        var e = bt[t];
        z($t, t, (function() {
            for (var n = [], r = 0; r < arguments.length; r++)
                n[r] = arguments[r];
            var o, i = e.apply(this, n), a = this.__ob__;
            switch (t) {
            case "push":
            case "unshift":
                o = n;
                break;
            case "splice":
                o = n.slice(2)
            }
            return o && a.observeArray(o),
            a.dep.notify(),
            i
        }
        ))
    }
    ));
    var wt = Object.getOwnPropertyNames($t)
      , xt = {}
      , Ct = !0;
    function kt(t) {
        Ct = t
    }
    var St = {
        notify: j,
        depend: j,
        addSub: j,
        removeSub: j
    }
      , Ot = function() {
        function t(t, n, r) {
            if (void 0 === n && (n = !1),
            void 0 === r && (r = !1),
            this.value = t,
            this.shallow = n,
            this.mock = r,
            this.dep = r ? St : new mt,
            this.vmCount = 0,
            z(t, "__ob__", this),
            e(t)) {
                if (!r)
                    if (K)
                        t.__proto__ = $t;
                    else
                        for (var o = 0, i = wt.length; o < i; o++) {
                            z(t, s = wt[o], $t[s])
                        }
                n || this.observeArray(t)
            } else {
                var a = Object.keys(t);
                for (o = 0; o < a.length; o++) {
                    var s;
                    At(t, s = a[o], xt, void 0, n, r)
                }
            }
        }
        return t.prototype.observeArray = function(t) {
            for (var e = 0, n = t.length; e < n; e++)
                Tt(t[e], !1, this.mock)
        }
        ,
        t
    }();
    function Tt(t, n, r) {
        return t && _(t, "__ob__") && t.__ob__ instanceof Ot ? t.__ob__ : !Ct || !r && rt() || !e(t) && !u(t) || !Object.isExtensible(t) || t.__v_skip || Ft(t) || t instanceof lt ? void 0 : new Ot(t,n,r)
    }
    function At(t, n, r, o, i, a) {
        var s = new mt
          , c = Object.getOwnPropertyDescriptor(t, n);
        if (!c || !1 !== c.configurable) {
            var u = c && c.get
              , l = c && c.set;
            u && !l || r !== xt && 2 !== arguments.length || (r = t[n]);
            var f = !i && Tt(r, !1, a);
            return Object.defineProperty(t, n, {
                enumerable: !0,
                configurable: !0,
                get: function() {
                    var n = u ? u.call(t) : r;
                    return mt.target && (s.depend(),
                    f && (f.dep.depend(),
                    e(n) && Nt(n))),
                    Ft(n) && !i ? n.value : n
                },
                set: function(e) {
                    var n = u ? u.call(t) : r;
                    if (I(n, e)) {
                        if (l)
                            l.call(t, e);
                        else {
                            if (u)
                                return;
                            if (!i && Ft(n) && !Ft(e))
                                return void (n.value = e);
                            r = e
                        }
                        f = !i && Tt(e, !1, a),
                        s.notify()
                    }
                }
            }),
            s
        }
    }
    function jt(t, n, r) {
        if (!Lt(t)) {
            var o = t.__ob__;
            return e(t) && l(n) ? (t.length = Math.max(t.length, n),
            t.splice(n, 1, r),
            o && !o.shallow && o.mock && Tt(r, !1, !0),
            r) : n in t && !(n in Object.prototype) ? (t[n] = r,
            r) : t._isVue || o && o.vmCount ? r : o ? (At(o.value, n, r, void 0, o.shallow, o.mock),
            o.dep.notify(),
            r) : (t[n] = r,
            r)
        }
    }
    function Et(t, n) {
        if (e(t) && l(n))
            t.splice(n, 1);
        else {
            var r = t.__ob__;
            t._isVue || r && r.vmCount || Lt(t) || _(t, n) && (delete t[n],
            r && r.dep.notify())
        }
    }
    function Nt(t) {
        for (var n = void 0, r = 0, o = t.length; r < o; r++)
            (n = t[r]) && n.__ob__ && n.__ob__.dep.depend(),
            e(n) && Nt(n)
    }
    function Pt(t) {
        return Dt(t, !0),
        z(t, "__v_isShallow", !0),
        t
    }
    function Dt(t, e) {
        Lt(t) || Tt(t, e, rt())
    }
    function Mt(t) {
        return Lt(t) ? Mt(t.__v_raw) : !(!t || !t.__ob__)
    }
    function It(t) {
        return !(!t || !t.__v_isShallow)
    }
    function Lt(t) {
        return !(!t || !t.__v_isReadonly)
    }
    var Rt = "__v_isRef";
    function Ft(t) {
        return !(!t || !0 !== t.__v_isRef)
    }
    function Ht(t, e) {
        if (Ft(t))
            return t;
        var n = {};
        return z(n, Rt, !0),
        z(n, "__v_isShallow", e),
        z(n, "dep", At(n, "value", t, null, e, rt())),
        n
    }
    function Bt(t, e, n) {
        Object.defineProperty(t, n, {
            enumerable: !0,
            configurable: !0,
            get: function() {
                var t = e[n];
                if (Ft(t))
                    return t.value;
                var r = t && t.__ob__;
                return r && r.dep.depend(),
                t
            },
            set: function(t) {
                var r = e[n];
                Ft(r) && !Ft(t) ? r.value = t : e[n] = t
            }
        })
    }
    function Ut(t, e, n) {
        var r = t[e];
        if (Ft(r))
            return r;
        var o = {
            get value() {
                var r = t[e];
                return void 0 === r ? n : r
            },
            set value(n) {
                t[e] = n
            }
        };
        return z(o, Rt, !0),
        o
    }
    function zt(t) {
        return Vt(t, !1)
    }
    function Vt(t, e) {
        if (!u(t))
            return t;
        if (Lt(t))
            return t;
        var n = e ? "__v_rawToShallowReadonly" : "__v_rawToReadonly"
          , r = t[n];
        if (r)
            return r;
        var o = Object.create(Object.getPrototypeOf(t));
        z(t, n, o),
        z(o, "__v_isReadonly", !0),
        z(o, "__v_raw", t),
        Ft(t) && z(o, Rt, !0),
        (e || It(t)) && z(o, "__v_isShallow", !0);
        for (var i = Object.keys(t), a = 0; a < i.length; a++)
            Kt(o, t, i[a], e);
        return o
    }
    function Kt(t, e, n, r) {
        Object.defineProperty(t, n, {
            enumerable: !0,
            configurable: !0,
            get: function() {
                var t = e[n];
                return r || !u(t) ? t : zt(t)
            },
            set: function() {}
        })
    }
    var Jt = b((function(t) {
        var e = "&" === t.charAt(0)
          , n = "~" === (t = e ? t.slice(1) : t).charAt(0)
          , r = "!" === (t = n ? t.slice(1) : t).charAt(0);
        return {
            name: t = r ? t.slice(1) : t,
            once: n,
            capture: r,
            passive: e
        }
    }
    ));
    function qt(t, n) {
        function r() {
            var t = r.fns;
            if (!e(t))
                return dn(t, null, arguments, n, "v-on handler");
            for (var o = t.slice(), i = 0; i < o.length; i++)
                dn(o[i], null, arguments, n, "v-on handler")
        }
        return r.fns = t,
        r
    }
    function Wt(t, e, r, i, a, s) {
        var c, u, l, f;
        for (c in t)
            u = t[c],
            l = e[c],
            f = Jt(c),
            n(u) || (n(l) ? (n(u.fns) && (u = t[c] = qt(u, s)),
            o(f.once) && (u = t[c] = a(f.name, u, f.capture)),
            r(f.name, u, f.capture, f.passive, f.params)) : u !== l && (l.fns = u,
            t[c] = l));
        for (c in e)
            n(t[c]) && i((f = Jt(c)).name, e[c], f.capture)
    }
    function Zt(t, e, i) {
        var a;
        t instanceof lt && (t = t.data.hook || (t.data.hook = {}));
        var s = t[e];
        function c() {
            i.apply(this, arguments),
            g(a.fns, c)
        }
        n(s) ? a = qt([c]) : r(s.fns) && o(s.merged) ? (a = s).fns.push(c) : a = qt([s, c]),
        a.merged = !0,
        t[e] = a
    }
    function Gt(t, e, n, o, i) {
        if (r(e)) {
            if (_(e, n))
                return t[n] = e[n],
                i || delete e[n],
                !0;
            if (_(e, o))
                return t[n] = e[o],
                i || delete e[o],
                !0
        }
        return !1
    }
    function Xt(t) {
        return i(t) ? [dt(t)] : e(t) ? Qt(t) : void 0
    }
    function Yt(t) {
        return r(t) && r(t.text) && !1 === t.isComment
    }
    function Qt(t, a) {
        var s, c, u, l, f = [];
        for (s = 0; s < t.length; s++)
            n(c = t[s]) || "boolean" == typeof c || (l = f[u = f.length - 1],
            e(c) ? c.length > 0 && (Yt((c = Qt(c, "".concat(a || "", "_").concat(s)))[0]) && Yt(l) && (f[u] = dt(l.text + c[0].text),
            c.shift()),
            f.push.apply(f, c)) : i(c) ? Yt(l) ? f[u] = dt(l.text + c) : "" !== c && f.push(dt(c)) : Yt(c) && Yt(l) ? f[u] = dt(l.text + c.text) : (o(t._isVList) && r(c.tag) && n(c.key) && r(a) && (c.key = "__vlist".concat(a, "_").concat(s, "__")),
            f.push(c)));
        return f
    }
    function te(t, n, c, u, l, f) {
        return (e(c) || i(c)) && (l = u,
        u = c,
        c = void 0),
        o(f) && (l = 2),
        function(t, n, o, i, c) {
            if (r(o) && r(o.__ob__))
                return ft();
            r(o) && r(o.is) && (n = o.is);
            if (!n)
                return ft();
            e(i) && a(i[0]) && ((o = o || {}).scopedSlots = {
                default: i[0]
            },
            i.length = 0);
            2 === c ? i = Xt(i) : 1 === c && (i = function(t) {
                for (var n = 0; n < t.length; n++)
                    if (e(t[n]))
                        return Array.prototype.concat.apply([], t);
                return t
            }(i));
            var u, l;
            if ("string" == typeof n) {
                var f = void 0;
                l = t.$vnode && t.$vnode.ns || H.getTagNamespace(n),
                u = H.isReservedTag(n) ? new lt(H.parsePlatformTagName(n),o,i,void 0,void 0,t) : o && o.pre || !r(f = yr(t.$options, "components", n)) ? new lt(n,o,i,void 0,void 0,t) : cr(f, o, t, i, n)
            } else
                u = cr(n, o, t, i);
            return e(u) ? u : r(u) ? (r(l) && ee(u, l),
            r(o) && function(t) {
                s(t.style) && Bn(t.style);
                s(t.class) && Bn(t.class)
            }(o),
            u) : ft()
        }(t, n, c, u, l)
    }
    function ee(t, e, i) {
        if (t.ns = e,
        "foreignObject" === t.tag && (e = void 0,
        i = !0),
        r(t.children))
            for (var a = 0, s = t.children.length; a < s; a++) {
                var c = t.children[a];
                r(c.tag) && (n(c.ns) || o(i) && "svg" !== c.tag) && ee(c, e, i)
            }
    }
    function ne(t, n) {
        var o, i, a, c, u = null;
        if (e(t) || "string" == typeof t)
            for (u = new Array(t.length),
            o = 0,
            i = t.length; o < i; o++)
                u[o] = n(t[o], o);
        else if ("number" == typeof t)
            for (u = new Array(t),
            o = 0; o < t; o++)
                u[o] = n(o + 1, o);
        else if (s(t))
            if (st && t[Symbol.iterator]) {
                u = [];
                for (var l = t[Symbol.iterator](), f = l.next(); !f.done; )
                    u.push(n(f.value, u.length)),
                    f = l.next()
            } else
                for (a = Object.keys(t),
                u = new Array(a.length),
                o = 0,
                i = a.length; o < i; o++)
                    c = a[o],
                    u[o] = n(t[c], c, o);
        return r(u) || (u = []),
        u._isVList = !0,
        u
    }
    function re(t, e, n, r) {
        var o, i = this.$scopedSlots[t];
        i ? (n = n || {},
        r && (n = T(T({}, r), n)),
        o = i(n) || (a(e) ? e() : e)) : o = this.$slots[t] || (a(e) ? e() : e);
        var s = n && n.slot;
        return s ? this.$createElement("template", {
            slot: s
        }, o) : o
    }
    function oe(t) {
        return yr(this.$options, "filters", t) || N
    }
    function ie(t, n) {
        return e(t) ? -1 === t.indexOf(n) : t !== n
    }
    function ae(t, e, n, r, o) {
        var i = H.keyCodes[e] || n;
        return o && r && !H.keyCodes[e] ? ie(o, r) : i ? ie(i, t) : r ? k(r) !== e : void 0 === t
    }
    function se(t, n, r, o, i) {
        if (r)
            if (s(r)) {
                e(r) && (r = A(r));
                var a = void 0
                  , c = function(e) {
                    if ("class" === e || "style" === e || m(e))
                        a = t;
                    else {
                        var s = t.attrs && t.attrs.type;
                        a = o || H.mustUseProp(n, s, e) ? t.domProps || (t.domProps = {}) : t.attrs || (t.attrs = {})
                    }
                    var c = w(e)
                      , u = k(e);
                    c in a || u in a || (a[e] = r[e],
                    i && ((t.on || (t.on = {}))["update:".concat(e)] = function(t) {
                        r[e] = t
                    }
                    ))
                };
                for (var u in r)
                    c(u)
            } else
                ;return t
    }
    function ce(t, e) {
        var n = this._staticTrees || (this._staticTrees = [])
          , r = n[t];
        return r && !e || le(r = n[t] = this.$options.staticRenderFns[t].call(this._renderProxy, this._c, this), "__static__".concat(t), !1),
        r
    }
    function ue(t, e, n) {
        return le(t, "__once__".concat(e).concat(n ? "_".concat(n) : ""), !0),
        t
    }
    function le(t, n, r) {
        if (e(t))
            for (var o = 0; o < t.length; o++)
                t[o] && "string" != typeof t[o] && fe(t[o], "".concat(n, "_").concat(o), r);
        else
            fe(t, n, r)
    }
    function fe(t, e, n) {
        t.isStatic = !0,
        t.key = e,
        t.isOnce = n
    }
    function de(t, e) {
        if (e)
            if (u(e)) {
                var n = t.on = t.on ? T({}, t.on) : {};
                for (var r in e) {
                    var o = n[r]
                      , i = e[r];
                    n[r] = o ? [].concat(o, i) : i
                }
            } else
                ;return t
    }
    function pe(t, n, r, o) {
        n = n || {
            $stable: !r
        };
        for (var i = 0; i < t.length; i++) {
            var a = t[i];
            e(a) ? pe(a, n, r) : a && (a.proxy && (a.fn.proxy = !0),
            n[a.key] = a.fn)
        }
        return o && (n.$key = o),
        n
    }
    function ve(t, e) {
        for (var n = 0; n < e.length; n += 2) {
            var r = e[n];
            "string" == typeof r && r && (t[e[n]] = e[n + 1])
        }
        return t
    }
    function he(t, e) {
        return "string" == typeof t ? e + t : t
    }
    function me(t) {
        t._o = ue,
        t._n = p,
        t._s = d,
        t._l = ne,
        t._t = re,
        t._q = P,
        t._i = D,
        t._m = ce,
        t._f = oe,
        t._k = ae,
        t._b = se,
        t._v = dt,
        t._e = ft,
        t._u = pe,
        t._g = de,
        t._d = ve,
        t._p = he
    }
    function ge(t, e) {
        if (!t || !t.length)
            return {};
        for (var n = {}, r = 0, o = t.length; r < o; r++) {
            var i = t[r]
              , a = i.data;
            if (a && a.attrs && a.attrs.slot && delete a.attrs.slot,
            i.context !== e && i.fnContext !== e || !a || null == a.slot)
                (n.default || (n.default = [])).push(i);
            else {
                var s = a.slot
                  , c = n[s] || (n[s] = []);
                "template" === i.tag ? c.push.apply(c, i.children || []) : c.push(i)
            }
        }
        for (var u in n)
            n[u].every(ye) && delete n[u];
        return n
    }
    function ye(t) {
        return t.isComment && !t.asyncFactory || " " === t.text
    }
    function _e(t) {
        return t.isComment && t.asyncFactory
    }
    function be(e, n, r, o) {
        var i, a = Object.keys(r).length > 0, s = n ? !!n.$stable : !a, c = n && n.$key;
        if (n) {
            if (n._normalized)
                return n._normalized;
            if (s && o && o !== t && c === o.$key && !a && !o.$hasNormal)
                return o;
            for (var u in i = {},
            n)
                n[u] && "$" !== u[0] && (i[u] = $e(e, r, u, n[u]))
        } else
            i = {};
        for (var l in r)
            l in i || (i[l] = we(r, l));
        return n && Object.isExtensible(n) && (n._normalized = i),
        z(i, "$stable", s),
        z(i, "$key", c),
        z(i, "$hasNormal", a),
        i
    }
    function $e(t, n, r, o) {
        var i = function() {
            var n = ct;
            ut(t);
            var r = arguments.length ? o.apply(null, arguments) : o({})
              , i = (r = r && "object" == typeof r && !e(r) ? [r] : Xt(r)) && r[0];
            return ut(n),
            r && (!i || 1 === r.length && i.isComment && !_e(i)) ? void 0 : r
        };
        return o.proxy && Object.defineProperty(n, r, {
            get: i,
            enumerable: !0,
            configurable: !0
        }),
        i
    }
    function we(t, e) {
        return function() {
            return t[e]
        }
    }
    function xe(e) {
        return {
            get attrs() {
                if (!e._attrsProxy) {
                    var n = e._attrsProxy = {};
                    z(n, "_v_attr_proxy", !0),
                    Ce(n, e.$attrs, t, e, "$attrs")
                }
                return e._attrsProxy
            },
            get listeners() {
                e._listenersProxy || Ce(e._listenersProxy = {}, e.$listeners, t, e, "$listeners");
                return e._listenersProxy
            },
            get slots() {
                return function(t) {
                    t._slotsProxy || Se(t._slotsProxy = {}, t.$scopedSlots);
                    return t._slotsProxy
                }(e)
            },
            emit: S(e.$emit, e),
            expose: function(t) {
                t && Object.keys(t).forEach((function(n) {
                    return Bt(e, t, n)
                }
                ))
            }
        }
    }
    function Ce(t, e, n, r, o) {
        var i = !1;
        for (var a in e)
            a in t ? e[a] !== n[a] && (i = !0) : (i = !0,
            ke(t, a, r, o));
        for (var a in t)
            a in e || (i = !0,
            delete t[a]);
        return i
    }
    function ke(t, e, n, r) {
        Object.defineProperty(t, e, {
            enumerable: !0,
            configurable: !0,
            get: function() {
                return n[r][e]
            }
        })
    }
    function Se(t, e) {
        for (var n in e)
            t[n] = e[n];
        for (var n in t)
            n in e || delete t[n]
    }
    function Oe() {
        var t = ct;
        return t._setupContext || (t._setupContext = xe(t))
    }
    var Te, Ae = null;
    function je(t, e) {
        return (t.__esModule || st && "Module" === t[Symbol.toStringTag]) && (t = t.default),
        s(t) ? e.extend(t) : t
    }
    function Ee(t) {
        if (e(t))
            for (var n = 0; n < t.length; n++) {
                var o = t[n];
                if (r(o) && (r(o.componentOptions) || _e(o)))
                    return o
            }
    }
    function Ne(t, e) {
        Te.$on(t, e)
    }
    function Pe(t, e) {
        Te.$off(t, e)
    }
    function De(t, e) {
        var n = Te;
        return function r() {
            var o = e.apply(null, arguments);
            null !== o && n.$off(t, r)
        }
    }
    function Me(t, e, n) {
        Te = t,
        Wt(e, n || {}, Ne, Pe, De, t),
        Te = void 0
    }
    var Ie = null;
    function Le(t) {
        var e = Ie;
        return Ie = t,
        function() {
            Ie = e
        }
    }
    function Re(t) {
        for (; t && (t = t.$parent); )
            if (t._inactive)
                return !0;
        return !1
    }
    function Fe(t, e) {
        if (e) {
            if (t._directInactive = !1,
            Re(t))
                return
        } else if (t._directInactive)
            return;
        if (t._inactive || null === t._inactive) {
            t._inactive = !1;
            for (var n = 0; n < t.$children.length; n++)
                Fe(t.$children[n]);
            Be(t, "activated")
        }
    }
    function He(t, e) {
        if (!(e && (t._directInactive = !0,
        Re(t)) || t._inactive)) {
            t._inactive = !0;
            for (var n = 0; n < t.$children.length; n++)
                He(t.$children[n]);
            Be(t, "deactivated")
        }
    }
    function Be(t, e, n, r) {
        void 0 === r && (r = !0),
        yt();
        var o = ct;
        r && ut(t);
        var i = t.$options[e]
          , a = "".concat(e, " hook");
        if (i)
            for (var s = 0, c = i.length; s < c; s++)
                dn(i[s], t, n || null, t, a);
        t._hasHookEvent && t.$emit("hook:" + e),
        r && ut(o),
        _t()
    }
    var Ue = []
      , ze = []
      , Ve = {}
      , Ke = !1
      , Je = !1
      , qe = 0;
    var We = 0
      , Ze = Date.now;
    if (J && !W) {
        var Ge = window.performance;
        Ge && "function" == typeof Ge.now && Ze() > document.createEvent("Event").timeStamp && (Ze = function() {
            return Ge.now()
        }
        )
    }
    var Xe = function(t, e) {
        if (t.post) {
            if (!e.post)
                return 1
        } else if (e.post)
            return -1;
        return t.id - e.id
    };
    function Ye() {
        var t, e;
        for (We = Ze(),
        Je = !0,
        Ue.sort(Xe),
        qe = 0; qe < Ue.length; qe++)
            (t = Ue[qe]).before && t.before(),
            e = t.id,
            Ve[e] = null,
            t.run();
        var n = ze.slice()
          , r = Ue.slice();
        qe = Ue.length = ze.length = 0,
        Ve = {},
        Ke = Je = !1,
        function(t) {
            for (var e = 0; e < t.length; e++)
                t[e]._inactive = !0,
                Fe(t[e], !0)
        }(n),
        function(t) {
            var e = t.length;
            for (; e--; ) {
                var n = t[e]
                  , r = n.vm;
                r && r._watcher === n && r._isMounted && !r._isDestroyed && Be(r, "updated")
            }
        }(r),
        function() {
            for (var t = 0; t < ht.length; t++) {
                var e = ht[t];
                e.subs = e.subs.filter((function(t) {
                    return t
                }
                )),
                e._pending = !1
            }
            ht.length = 0
        }(),
        ot && H.devtools && ot.emit("flush")
    }
    function Qe(t) {
        var e = t.id;
        if (null == Ve[e] && (t !== mt.target || !t.noRecurse)) {
            if (Ve[e] = !0,
            Je) {
                for (var n = Ue.length - 1; n > qe && Ue[n].id > t.id; )
                    n--;
                Ue.splice(n + 1, 0, t)
            } else
                Ue.push(t);
            Ke || (Ke = !0,
            Cn(Ye))
        }
    }
    var tn = "watcher"
      , en = "".concat(tn, " callback")
      , nn = "".concat(tn, " getter")
      , rn = "".concat(tn, " cleanup");
    function on(t, e) {
        return cn(t, null, {
            flush: "post"
        })
    }
    var an, sn = {};
    function cn(n, r, o) {
        var i = void 0 === o ? t : o
          , s = i.immediate
          , c = i.deep
          , u = i.flush
          , l = void 0 === u ? "pre" : u;
        i.onTrack,
        i.onTrigger;
        var f, d, p = ct, v = function(t, e, n) {
            return void 0 === n && (n = null),
            dn(t, null, n, p, e)
        }, h = !1, m = !1;
        if (Ft(n) ? (f = function() {
            return n.value
        }
        ,
        h = It(n)) : Mt(n) ? (f = function() {
            return n.__ob__.dep.depend(),
            n
        }
        ,
        c = !0) : e(n) ? (m = !0,
        h = n.some((function(t) {
            return Mt(t) || It(t)
        }
        )),
        f = function() {
            return n.map((function(t) {
                return Ft(t) ? t.value : Mt(t) ? Bn(t) : a(t) ? v(t, nn) : void 0
            }
            ))
        }
        ) : f = a(n) ? r ? function() {
            return v(n, nn)
        }
        : function() {
            if (!p || !p._isDestroyed)
                return d && d(),
                v(n, tn, [y])
        }
        : j,
        r && c) {
            var g = f;
            f = function() {
                return Bn(g())
            }
        }
        var y = function(t) {
            d = _.onStop = function() {
                v(t, rn)
            }
        };
        if (rt())
            return y = j,
            r ? s && v(r, en, [f(), m ? [] : void 0, y]) : f(),
            j;
        var _ = new Vn(ct,f,j,{
            lazy: !0
        });
        _.noRecurse = !r;
        var b = m ? [] : sn;
        return _.run = function() {
            if (_.active)
                if (r) {
                    var t = _.get();
                    (c || h || (m ? t.some((function(t, e) {
                        return I(t, b[e])
                    }
                    )) : I(t, b))) && (d && d(),
                    v(r, en, [t, b === sn ? void 0 : b, y]),
                    b = t)
                } else
                    _.get()
        }
        ,
        "sync" === l ? _.update = _.run : "post" === l ? (_.post = !0,
        _.update = function() {
            return Qe(_)
        }
        ) : _.update = function() {
            if (p && p === ct && !p._isMounted) {
                var t = p._preWatchers || (p._preWatchers = []);
                t.indexOf(_) < 0 && t.push(_)
            } else
                Qe(_)
        }
        ,
        r ? s ? _.run() : b = _.get() : "post" === l && p ? p.$once("hook:mounted", (function() {
            return _.get()
        }
        )) : _.get(),
        function() {
            _.teardown()
        }
    }
    var un = function() {
        function t(t) {
            void 0 === t && (t = !1),
            this.detached = t,
            this.active = !0,
            this.effects = [],
            this.cleanups = [],
            this.parent = an,
            !t && an && (this.index = (an.scopes || (an.scopes = [])).push(this) - 1)
        }
        return t.prototype.run = function(t) {
            if (this.active) {
                var e = an;
                try {
                    return an = this,
                    t()
                } finally {
                    an = e
                }
            }
        }
        ,
        t.prototype.on = function() {
            an = this
        }
        ,
        t.prototype.off = function() {
            an = this.parent
        }
        ,
        t.prototype.stop = function(t) {
            if (this.active) {
                var e = void 0
                  , n = void 0;
                for (e = 0,
                n = this.effects.length; e < n; e++)
                    this.effects[e].teardown();
                for (e = 0,
                n = this.cleanups.length; e < n; e++)
                    this.cleanups[e]();
                if (this.scopes)
                    for (e = 0,
                    n = this.scopes.length; e < n; e++)
                        this.scopes[e].stop(!0);
                if (!this.detached && this.parent && !t) {
                    var r = this.parent.scopes.pop();
                    r && r !== this && (this.parent.scopes[this.index] = r,
                    r.index = this.index)
                }
                this.parent = void 0,
                this.active = !1
            }
        }
        ,
        t
    }();
    function ln(t) {
        var e = t._provided
          , n = t.$parent && t.$parent._provided;
        return n === e ? t._provided = Object.create(n) : e
    }
    function fn(t, e, n) {
        yt();
        try {
            if (e)
                for (var r = e; r = r.$parent; ) {
                    var o = r.$options.errorCaptured;
                    if (o)
                        for (var i = 0; i < o.length; i++)
                            try {
                                if (!1 === o[i].call(r, t, e, n))
                                    return
                            } catch (t) {
                                pn(t, r, "errorCaptured hook")
                            }
                }
            pn(t, e, n)
        } finally {
            _t()
        }
    }
    function dn(t, e, n, r, o) {
        var i;
        try {
            (i = n ? t.apply(e, n) : t.call(e)) && !i._isVue && f(i) && !i._handled && (i.catch((function(t) {
                return fn(t, r, o + " (Promise/async)")
            }
            )),
            i._handled = !0)
        } catch (t) {
            fn(t, r, o)
        }
        return i
    }
    function pn(t, e, n) {
        if (H.errorHandler)
            try {
                return H.errorHandler.call(null, t, e, n)
            } catch (e) {
                e !== t && vn(e)
            }
        vn(t)
    }
    function vn(t, e, n) {
        if (!J || "undefined" == typeof console)
            throw t;
        console.error(t)
    }
    var hn, mn = !1, gn = [], yn = !1;
    function _n() {
        yn = !1;
        var t = gn.slice(0);
        gn.length = 0;
        for (var e = 0; e < t.length; e++)
            t[e]()
    }
    if ("undefined" != typeof Promise && it(Promise)) {
        var bn = Promise.resolve();
        hn = function() {
            bn.then(_n),
            X && setTimeout(j)
        }
        ,
        mn = !0
    } else if (W || "undefined" == typeof MutationObserver || !it(MutationObserver) && "[object MutationObserverConstructor]" !== MutationObserver.toString())
        hn = "undefined" != typeof setImmediate && it(setImmediate) ? function() {
            setImmediate(_n)
        }
        : function() {
            setTimeout(_n, 0)
        }
        ;
    else {
        var $n = 1
          , wn = new MutationObserver(_n)
          , xn = document.createTextNode(String($n));
        wn.observe(xn, {
            characterData: !0
        }),
        hn = function() {
            $n = ($n + 1) % 2,
            xn.data = String($n)
        }
        ,
        mn = !0
    }
    function Cn(t, e) {
        var n;
        if (gn.push((function() {
            if (t)
                try {
                    t.call(e)
                } catch (t) {
                    fn(t, e, "nextTick")
                }
            else
                n && n(e)
        }
        )),
        yn || (yn = !0,
        hn()),
        !t && "undefined" != typeof Promise)
            return new Promise((function(t) {
                n = t
            }
            ))
    }
    function kn(t) {
        return function(e, n) {
            if (void 0 === n && (n = ct),
            n)
                return function(t, e, n) {
                    var r = t.$options;
                    r[e] = vr(r[e], n)
                }(n, t, e)
        }
    }
    var Sn = kn("beforeMount")
      , On = kn("mounted")
      , Tn = kn("beforeUpdate")
      , An = kn("updated")
      , jn = kn("beforeDestroy")
      , En = kn("destroyed")
      , Nn = kn("activated")
      , Pn = kn("deactivated")
      , Dn = kn("serverPrefetch")
      , Mn = kn("renderTracked")
      , In = kn("renderTriggered")
      , Ln = kn("errorCaptured");
    var Rn = "2.7.14";
    var Fn = Object.freeze({
        __proto__: null,
        version: Rn,
        defineComponent: function(t) {
            return t
        },
        ref: function(t) {
            return Ht(t, !1)
        },
        shallowRef: function(t) {
            return Ht(t, !0)
        },
        isRef: Ft,
        toRef: Ut,
        toRefs: function(t) {
            var n = e(t) ? new Array(t.length) : {};
            for (var r in t)
                n[r] = Ut(t, r);
            return n
        },
        unref: function(t) {
            return Ft(t) ? t.value : t
        },
        proxyRefs: function(t) {
            if (Mt(t))
                return t;
            for (var e = {}, n = Object.keys(t), r = 0; r < n.length; r++)
                Bt(e, t, n[r]);
            return e
        },
        customRef: function(t) {
            var e = new mt
              , n = t((function() {
                e.depend()
            }
            ), (function() {
                e.notify()
            }
            ))
              , r = n.get
              , o = n.set
              , i = {
                get value() {
                    return r()
                },
                set value(t) {
                    o(t)
                }
            };
            return z(i, Rt, !0),
            i
        },
        triggerRef: function(t) {
            t.dep && t.dep.notify()
        },
        reactive: function(t) {
            return Dt(t, !1),
            t
        },
        isReactive: Mt,
        isReadonly: Lt,
        isShallow: It,
        isProxy: function(t) {
            return Mt(t) || Lt(t)
        },
        shallowReactive: Pt,
        markRaw: function(t) {
            return Object.isExtensible(t) && z(t, "__v_skip", !0),
            t
        },
        toRaw: function t(e) {
            var n = e && e.__v_raw;
            return n ? t(n) : e
        },
        readonly: zt,
        shallowReadonly: function(t) {
            return Vt(t, !0)
        },
        computed: function(t, e) {
            var n, r, o = a(t);
            o ? (n = t,
            r = j) : (n = t.get,
            r = t.set);
            var i = rt() ? null : new Vn(ct,n,j,{
                lazy: !0
            })
              , s = {
                effect: i,
                get value() {
                    return i ? (i.dirty && i.evaluate(),
                    mt.target && i.depend(),
                    i.value) : n()
                },
                set value(t) {
                    r(t)
                }
            };
            return z(s, Rt, !0),
            z(s, "__v_isReadonly", o),
            s
        },
        watch: function(t, e, n) {
            return cn(t, e, n)
        },
        watchEffect: function(t, e) {
            return cn(t, null, e)
        },
        watchPostEffect: on,
        watchSyncEffect: function(t, e) {
            return cn(t, null, {
                flush: "sync"
            })
        },
        EffectScope: un,
        effectScope: function(t) {
            return new un(t)
        },
        onScopeDispose: function(t) {
            an && an.cleanups.push(t)
        },
        getCurrentScope: function() {
            return an
        },
        provide: function(t, e) {
            ct && (ln(ct)[t] = e)
        },
        inject: function(t, e, n) {
            void 0 === n && (n = !1);
            var r = ct;
            if (r) {
                var o = r.$parent && r.$parent._provided;
                if (o && t in o)
                    return o[t];
                if (arguments.length > 1)
                    return n && a(e) ? e.call(r) : e
            }
        },
        h: function(t, e, n) {
            return te(ct, t, e, n, 2, !0)
        },
        getCurrentInstance: function() {
            return ct && {
                proxy: ct
            }
        },
        useSlots: function() {
            return Oe().slots
        },
        useAttrs: function() {
            return Oe().attrs
        },
        useListeners: function() {
            return Oe().listeners
        },
        mergeDefaults: function(t, n) {
            var r = e(t) ? t.reduce((function(t, e) {
                return t[e] = {},
                t
            }
            ), {}) : t;
            for (var o in n) {
                var i = r[o];
                i ? e(i) || a(i) ? r[o] = {
                    type: i,
                    default: n[o]
                } : i.default = n[o] : null === i && (r[o] = {
                    default: n[o]
                })
            }
            return r
        },
        nextTick: Cn,
        set: jt,
        del: Et,
        useCssModule: function(e) {
            return t
        },
        useCssVars: function(t) {
            if (J) {
                var e = ct;
                e && on((function() {
                    var n = e.$el
                      , r = t(e, e._setupProxy);
                    if (n && 1 === n.nodeType) {
                        var o = n.style;
                        for (var i in r)
                            o.setProperty("--".concat(i), r[i])
                    }
                }
                ))
            }
        },
        defineAsyncComponent: function(t) {
            a(t) && (t = {
                loader: t
            });
            var e = t.loader
              , n = t.loadingComponent
              , r = t.errorComponent
              , o = t.delay
              , i = void 0 === o ? 200 : o
              , s = t.timeout;
            t.suspensible;
            var c = t.onError
              , u = null
              , l = 0
              , f = function() {
                var t;
                return u || (t = u = e().catch((function(t) {
                    if (t = t instanceof Error ? t : new Error(String(t)),
                    c)
                        return new Promise((function(e, n) {
                            c(t, (function() {
                                return e((l++,
                                u = null,
                                f()))
                            }
                            ), (function() {
                                return n(t)
                            }
                            ), l + 1)
                        }
                        ));
                    throw t
                }
                )).then((function(e) {
                    return t !== u && u ? u : (e && (e.__esModule || "Module" === e[Symbol.toStringTag]) && (e = e.default),
                    e)
                }
                )))
            };
            return function() {
                return {
                    component: f(),
                    delay: i,
                    timeout: s,
                    error: r,
                    loading: n
                }
            }
        },
        onBeforeMount: Sn,
        onMounted: On,
        onBeforeUpdate: Tn,
        onUpdated: An,
        onBeforeUnmount: jn,
        onUnmounted: En,
        onActivated: Nn,
        onDeactivated: Pn,
        onServerPrefetch: Dn,
        onRenderTracked: Mn,
        onRenderTriggered: In,
        onErrorCaptured: function(t, e) {
            void 0 === e && (e = ct),
            Ln(t, e)
        }
    })
      , Hn = new at;
    function Bn(t) {
        return Un(t, Hn),
        Hn.clear(),
        t
    }
    function Un(t, n) {
        var r, o, i = e(t);
        if (!(!i && !s(t) || t.__v_skip || Object.isFrozen(t) || t instanceof lt)) {
            if (t.__ob__) {
                var a = t.__ob__.dep.id;
                if (n.has(a))
                    return;
                n.add(a)
            }
            if (i)
                for (r = t.length; r--; )
                    Un(t[r], n);
            else if (Ft(t))
                Un(t.value, n);
            else
                for (r = (o = Object.keys(t)).length; r--; )
                    Un(t[o[r]], n)
        }
    }
    var zn = 0
      , Vn = function() {
        function t(t, e, n, r, o) {
            !function(t, e) {
                void 0 === e && (e = an),
                e && e.active && e.effects.push(t)
            }(this, an && !an._vm ? an : t ? t._scope : void 0),
            (this.vm = t) && o && (t._watcher = this),
            r ? (this.deep = !!r.deep,
            this.user = !!r.user,
            this.lazy = !!r.lazy,
            this.sync = !!r.sync,
            this.before = r.before) : this.deep = this.user = this.lazy = this.sync = !1,
            this.cb = n,
            this.id = ++zn,
            this.active = !0,
            this.post = !1,
            this.dirty = this.lazy,
            this.deps = [],
            this.newDeps = [],
            this.depIds = new at,
            this.newDepIds = new at,
            this.expression = "",
            a(e) ? this.getter = e : (this.getter = function(t) {
                if (!V.test(t)) {
                    var e = t.split(".");
                    return function(t) {
                        for (var n = 0; n < e.length; n++) {
                            if (!t)
                                return;
                            t = t[e[n]]
                        }
                        return t
                    }
                }
            }(e),
            this.getter || (this.getter = j)),
            this.value = this.lazy ? void 0 : this.get()
        }
        return t.prototype.get = function() {
            var t;
            yt(this);
            var e = this.vm;
            try {
                t = this.getter.call(e, e)
            } catch (t) {
                if (!this.user)
                    throw t;
                fn(t, e, 'getter for watcher "'.concat(this.expression, '"'))
            } finally {
                this.deep && Bn(t),
                _t(),
                this.cleanupDeps()
            }
            return t
        }
        ,
        t.prototype.addDep = function(t) {
            var e = t.id;
            this.newDepIds.has(e) || (this.newDepIds.add(e),
            this.newDeps.push(t),
            this.depIds.has(e) || t.addSub(this))
        }
        ,
        t.prototype.cleanupDeps = function() {
            for (var t = this.deps.length; t--; ) {
                var e = this.deps[t];
                this.newDepIds.has(e.id) || e.removeSub(this)
            }
            var n = this.depIds;
            this.depIds = this.newDepIds,
            this.newDepIds = n,
            this.newDepIds.clear(),
            n = this.deps,
            this.deps = this.newDeps,
            this.newDeps = n,
            this.newDeps.length = 0
        }
        ,
        t.prototype.update = function() {
            this.lazy ? this.dirty = !0 : this.sync ? this.run() : Qe(this)
        }
        ,
        t.prototype.run = function() {
            if (this.active) {
                var t = this.get();
                if (t !== this.value || s(t) || this.deep) {
                    var e = this.value;
                    if (this.value = t,
                    this.user) {
                        var n = 'callback for watcher "'.concat(this.expression, '"');
                        dn(this.cb, this.vm, [t, e], this.vm, n)
                    } else
                        this.cb.call(this.vm, t, e)
                }
            }
        }
        ,
        t.prototype.evaluate = function() {
            this.value = this.get(),
            this.dirty = !1
        }
        ,
        t.prototype.depend = function() {
            for (var t = this.deps.length; t--; )
                this.deps[t].depend()
        }
        ,
        t.prototype.teardown = function() {
            if (this.vm && !this.vm._isBeingDestroyed && g(this.vm._scope.effects, this),
            this.active) {
                for (var t = this.deps.length; t--; )
                    this.deps[t].removeSub(this);
                this.active = !1,
                this.onStop && this.onStop()
            }
        }
        ,
        t
    }()
      , Kn = {
        enumerable: !0,
        configurable: !0,
        get: j,
        set: j
    };
    function Jn(t, e, n) {
        Kn.get = function() {
            return this[e][n]
        }
        ,
        Kn.set = function(t) {
            this[e][n] = t
        }
        ,
        Object.defineProperty(t, n, Kn)
    }
    function qn(t) {
        var n = t.$options;
        if (n.props && function(t, e) {
            var n = t.$options.propsData || {}
              , r = t._props = Pt({})
              , o = t.$options._propKeys = [];
            t.$parent && kt(!1);
            var i = function(i) {
                o.push(i);
                var a = _r(i, e, n, t);
                At(r, i, a),
                i in t || Jn(t, "_props", i)
            };
            for (var a in e)
                i(a);
            kt(!0)
        }(t, n.props),
        function(t) {
            var e = t.$options
              , n = e.setup;
            if (n) {
                var r = t._setupContext = xe(t);
                ut(t),
                yt();
                var o = dn(n, null, [t._props || Pt({}), r], t, "setup");
                if (_t(),
                ut(),
                a(o))
                    e.render = o;
                else if (s(o))
                    if (t._setupState = o,
                    o.__sfc) {
                        var i = t._setupProxy = {};
                        for (var c in o)
                            "__sfc" !== c && Bt(i, o, c)
                    } else
                        for (var c in o)
                            U(c) || Bt(t, o, c)
            }
        }(t),
        n.methods && function(t, e) {
            for (var n in t.$options.props,
            e)
                t[n] = "function" != typeof e[n] ? j : S(e[n], t)
        }(t, n.methods),
        n.data)
            !function(t) {
                var e = t.$options.data;
                u(e = t._data = a(e) ? function(t, e) {
                    yt();
                    try {
                        return t.call(e, e)
                    } catch (t) {
                        return fn(t, e, "data()"),
                        {}
                    } finally {
                        _t()
                    }
                }(e, t) : e || {}) || (e = {});
                var n = Object.keys(e)
                  , r = t.$options.props;
                t.$options.methods;
                var o = n.length;
                for (; o--; ) {
                    var i = n[o];
                    r && _(r, i) || U(i) || Jn(t, "_data", i)
                }
                var s = Tt(e);
                s && s.vmCount++
            }(t);
        else {
            var r = Tt(t._data = {});
            r && r.vmCount++
        }
        n.computed && function(t, e) {
            var n = t._computedWatchers = Object.create(null)
              , r = rt();
            for (var o in e) {
                var i = e[o]
                  , s = a(i) ? i : i.get;
                r || (n[o] = new Vn(t,s || j,j,Wn)),
                o in t || Zn(t, o, i)
            }
        }(t, n.computed),
        n.watch && n.watch !== tt && function(t, n) {
            for (var r in n) {
                var o = n[r];
                if (e(o))
                    for (var i = 0; i < o.length; i++)
                        Yn(t, r, o[i]);
                else
                    Yn(t, r, o)
            }
        }(t, n.watch)
    }
    var Wn = {
        lazy: !0
    };
    function Zn(t, e, n) {
        var r = !rt();
        a(n) ? (Kn.get = r ? Gn(e) : Xn(n),
        Kn.set = j) : (Kn.get = n.get ? r && !1 !== n.cache ? Gn(e) : Xn(n.get) : j,
        Kn.set = n.set || j),
        Object.defineProperty(t, e, Kn)
    }
    function Gn(t) {
        return function() {
            var e = this._computedWatchers && this._computedWatchers[t];
            if (e)
                return e.dirty && e.evaluate(),
                mt.target && e.depend(),
                e.value
        }
    }
    function Xn(t) {
        return function() {
            return t.call(this, this)
        }
    }
    function Yn(t, e, n, r) {
        return u(n) && (r = n,
        n = n.handler),
        "string" == typeof n && (n = t[n]),
        t.$watch(e, n, r)
    }
    function Qn(t, e) {
        if (t) {
            for (var n = Object.create(null), r = st ? Reflect.ownKeys(t) : Object.keys(t), o = 0; o < r.length; o++) {
                var i = r[o];
                if ("__ob__" !== i) {
                    var s = t[i].from;
                    if (s in e._provided)
                        n[i] = e._provided[s];
                    else if ("default"in t[i]) {
                        var c = t[i].default;
                        n[i] = a(c) ? c.call(e) : c
                    }
                }
            }
            return n
        }
    }
    var tr = 0;
    function er(t) {
        var e = t.options;
        if (t.super) {
            var n = er(t.super);
            if (n !== t.superOptions) {
                t.superOptions = n;
                var r = function(t) {
                    var e, n = t.options, r = t.sealedOptions;
                    for (var o in n)
                        n[o] !== r[o] && (e || (e = {}),
                        e[o] = n[o]);
                    return e
                }(t);
                r && T(t.extendOptions, r),
                (e = t.options = gr(n, t.extendOptions)).name && (e.components[e.name] = t)
            }
        }
        return e
    }
    function nr(n, r, i, a, s) {
        var c, u = this, l = s.options;
        _(a, "_uid") ? (c = Object.create(a))._original = a : (c = a,
        a = a._original);
        var f = o(l._compiled)
          , d = !f;
        this.data = n,
        this.props = r,
        this.children = i,
        this.parent = a,
        this.listeners = n.on || t,
        this.injections = Qn(l.inject, a),
        this.slots = function() {
            return u.$slots || be(a, n.scopedSlots, u.$slots = ge(i, a)),
            u.$slots
        }
        ,
        Object.defineProperty(this, "scopedSlots", {
            enumerable: !0,
            get: function() {
                return be(a, n.scopedSlots, this.slots())
            }
        }),
        f && (this.$options = l,
        this.$slots = this.slots(),
        this.$scopedSlots = be(a, n.scopedSlots, this.$slots)),
        l._scopeId ? this._c = function(t, n, r, o) {
            var i = te(c, t, n, r, o, d);
            return i && !e(i) && (i.fnScopeId = l._scopeId,
            i.fnContext = a),
            i
        }
        : this._c = function(t, e, n, r) {
            return te(c, t, e, n, r, d)
        }
    }
    function rr(t, e, n, r, o) {
        var i = pt(t);
        return i.fnContext = n,
        i.fnOptions = r,
        e.slot && ((i.data || (i.data = {})).slot = e.slot),
        i
    }
    function or(t, e) {
        for (var n in e)
            t[w(n)] = e[n]
    }
    function ir(t) {
        return t.name || t.__name || t._componentTag
    }
    me(nr.prototype);
    var ar = {
        init: function(t, e) {
            if (t.componentInstance && !t.componentInstance._isDestroyed && t.data.keepAlive) {
                var n = t;
                ar.prepatch(n, n)
            } else {
                (t.componentInstance = function(t, e) {
                    var n = {
                        _isComponent: !0,
                        _parentVnode: t,
                        parent: e
                    }
                      , o = t.data.inlineTemplate;
                    r(o) && (n.render = o.render,
                    n.staticRenderFns = o.staticRenderFns);
                    return new t.componentOptions.Ctor(n)
                }(t, Ie)).$mount(e ? t.elm : void 0, e)
            }
        },
        prepatch: function(e, n) {
            var r = n.componentOptions;
            !function(e, n, r, o, i) {
                var a = o.data.scopedSlots
                  , s = e.$scopedSlots
                  , c = !!(a && !a.$stable || s !== t && !s.$stable || a && e.$scopedSlots.$key !== a.$key || !a && e.$scopedSlots.$key)
                  , u = !!(i || e.$options._renderChildren || c)
                  , l = e.$vnode;
                e.$options._parentVnode = o,
                e.$vnode = o,
                e._vnode && (e._vnode.parent = o),
                e.$options._renderChildren = i;
                var f = o.data.attrs || t;
                e._attrsProxy && Ce(e._attrsProxy, f, l.data && l.data.attrs || t, e, "$attrs") && (u = !0),
                e.$attrs = f,
                r = r || t;
                var d = e.$options._parentListeners;
                if (e._listenersProxy && Ce(e._listenersProxy, r, d || t, e, "$listeners"),
                e.$listeners = e.$options._parentListeners = r,
                Me(e, r, d),
                n && e.$options.props) {
                    kt(!1);
                    for (var p = e._props, v = e.$options._propKeys || [], h = 0; h < v.length; h++) {
                        var m = v[h]
                          , g = e.$options.props;
                        p[m] = _r(m, g, n, e)
                    }
                    kt(!0),
                    e.$options.propsData = n
                }
                u && (e.$slots = ge(i, o.context),
                e.$forceUpdate())
            }(n.componentInstance = e.componentInstance, r.propsData, r.listeners, n, r.children)
        },
        insert: function(t) {
            var e, n = t.context, r = t.componentInstance;
            r._isMounted || (r._isMounted = !0,
            Be(r, "mounted")),
            t.data.keepAlive && (n._isMounted ? ((e = r)._inactive = !1,
            ze.push(e)) : Fe(r, !0))
        },
        destroy: function(t) {
            var e = t.componentInstance;
            e._isDestroyed || (t.data.keepAlive ? He(e, !0) : e.$destroy())
        }
    }
      , sr = Object.keys(ar);
    function cr(i, a, c, u, l) {
        if (!n(i)) {
            var d = c.$options._base;
            if (s(i) && (i = d.extend(i)),
            "function" == typeof i) {
                var p;
                if (n(i.cid) && (i = function(t, e) {
                    if (o(t.error) && r(t.errorComp))
                        return t.errorComp;
                    if (r(t.resolved))
                        return t.resolved;
                    var i = Ae;
                    if (i && r(t.owners) && -1 === t.owners.indexOf(i) && t.owners.push(i),
                    o(t.loading) && r(t.loadingComp))
                        return t.loadingComp;
                    if (i && !r(t.owners)) {
                        var a = t.owners = [i]
                          , c = !0
                          , u = null
                          , l = null;
                        i.$on("hook:destroyed", (function() {
                            return g(a, i)
                        }
                        ));
                        var d = function(t) {
                            for (var e = 0, n = a.length; e < n; e++)
                                a[e].$forceUpdate();
                            t && (a.length = 0,
                            null !== u && (clearTimeout(u),
                            u = null),
                            null !== l && (clearTimeout(l),
                            l = null))
                        }
                          , p = M((function(n) {
                            t.resolved = je(n, e),
                            c ? a.length = 0 : d(!0)
                        }
                        ))
                          , v = M((function(e) {
                            r(t.errorComp) && (t.error = !0,
                            d(!0))
                        }
                        ))
                          , h = t(p, v);
                        return s(h) && (f(h) ? n(t.resolved) && h.then(p, v) : f(h.component) && (h.component.then(p, v),
                        r(h.error) && (t.errorComp = je(h.error, e)),
                        r(h.loading) && (t.loadingComp = je(h.loading, e),
                        0 === h.delay ? t.loading = !0 : u = setTimeout((function() {
                            u = null,
                            n(t.resolved) && n(t.error) && (t.loading = !0,
                            d(!1))
                        }
                        ), h.delay || 200)),
                        r(h.timeout) && (l = setTimeout((function() {
                            l = null,
                            n(t.resolved) && v(null)
                        }
                        ), h.timeout)))),
                        c = !1,
                        t.loading ? t.loadingComp : t.resolved
                    }
                }(p = i, d),
                void 0 === i))
                    return function(t, e, n, r, o) {
                        var i = ft();
                        return i.asyncFactory = t,
                        i.asyncMeta = {
                            data: e,
                            context: n,
                            children: r,
                            tag: o
                        },
                        i
                    }(p, a, c, u, l);
                a = a || {},
                er(i),
                r(a.model) && function(t, n) {
                    var o = t.model && t.model.prop || "value"
                      , i = t.model && t.model.event || "input";
                    (n.attrs || (n.attrs = {}))[o] = n.model.value;
                    var a = n.on || (n.on = {})
                      , s = a[i]
                      , c = n.model.callback;
                    r(s) ? (e(s) ? -1 === s.indexOf(c) : s !== c) && (a[i] = [c].concat(s)) : a[i] = c
                }(i.options, a);
                var v = function(t, e, o) {
                    var i = e.options.props;
                    if (!n(i)) {
                        var a = {}
                          , s = t.attrs
                          , c = t.props;
                        if (r(s) || r(c))
                            for (var u in i) {
                                var l = k(u);
                                Gt(a, c, u, l, !0) || Gt(a, s, u, l, !1)
                            }
                        return a
                    }
                }(a, i);
                if (o(i.options.functional))
                    return function(n, o, i, a, s) {
                        var c = n.options
                          , u = {}
                          , l = c.props;
                        if (r(l))
                            for (var f in l)
                                u[f] = _r(f, l, o || t);
                        else
                            r(i.attrs) && or(u, i.attrs),
                            r(i.props) && or(u, i.props);
                        var d = new nr(i,u,s,a,n)
                          , p = c.render.call(null, d._c, d);
                        if (p instanceof lt)
                            return rr(p, i, d.parent, c);
                        if (e(p)) {
                            for (var v = Xt(p) || [], h = new Array(v.length), m = 0; m < v.length; m++)
                                h[m] = rr(v[m], i, d.parent, c);
                            return h
                        }
                    }(i, v, a, c, u);
                var h = a.on;
                if (a.on = a.nativeOn,
                o(i.options.abstract)) {
                    var m = a.slot;
                    a = {},
                    m && (a.slot = m)
                }
                !function(t) {
                    for (var e = t.hook || (t.hook = {}), n = 0; n < sr.length; n++) {
                        var r = sr[n]
                          , o = e[r]
                          , i = ar[r];
                        o === i || o && o._merged || (e[r] = o ? ur(i, o) : i)
                    }
                }(a);
                var y = ir(i.options) || l;
                return new lt("vue-component-".concat(i.cid).concat(y ? "-".concat(y) : ""),a,void 0,void 0,void 0,c,{
                    Ctor: i,
                    propsData: v,
                    listeners: h,
                    tag: l,
                    children: u
                },p)
            }
        }
    }
    function ur(t, e) {
        var n = function(n, r) {
            t(n, r),
            e(n, r)
        };
        return n._merged = !0,
        n
    }
    var lr = j
      , fr = H.optionMergeStrategies;
    function dr(t, e, n) {
        if (void 0 === n && (n = !0),
        !e)
            return t;
        for (var r, o, i, a = st ? Reflect.ownKeys(e) : Object.keys(e), s = 0; s < a.length; s++)
            "__ob__" !== (r = a[s]) && (o = t[r],
            i = e[r],
            n && _(t, r) ? o !== i && u(o) && u(i) && dr(o, i) : jt(t, r, i));
        return t
    }
    function pr(t, e, n) {
        return n ? function() {
            var r = a(e) ? e.call(n, n) : e
              , o = a(t) ? t.call(n, n) : t;
            return r ? dr(r, o) : o
        }
        : e ? t ? function() {
            return dr(a(e) ? e.call(this, this) : e, a(t) ? t.call(this, this) : t)
        }
        : e : t
    }
    function vr(t, n) {
        var r = n ? t ? t.concat(n) : e(n) ? n : [n] : t;
        return r ? function(t) {
            for (var e = [], n = 0; n < t.length; n++)
                -1 === e.indexOf(t[n]) && e.push(t[n]);
            return e
        }(r) : r
    }
    function hr(t, e, n, r) {
        var o = Object.create(t || null);
        return e ? T(o, e) : o
    }
    fr.data = function(t, e, n) {
        return n ? pr(t, e, n) : e && "function" != typeof e ? t : pr(t, e)
    }
    ,
    F.forEach((function(t) {
        fr[t] = vr
    }
    )),
    R.forEach((function(t) {
        fr[t + "s"] = hr
    }
    )),
    fr.watch = function(t, n, r, o) {
        if (t === tt && (t = void 0),
        n === tt && (n = void 0),
        !n)
            return Object.create(t || null);
        if (!t)
            return n;
        var i = {};
        for (var a in T(i, t),
        n) {
            var s = i[a]
              , c = n[a];
            s && !e(s) && (s = [s]),
            i[a] = s ? s.concat(c) : e(c) ? c : [c]
        }
        return i
    }
    ,
    fr.props = fr.methods = fr.inject = fr.computed = function(t, e, n, r) {
        if (!t)
            return e;
        var o = Object.create(null);
        return T(o, t),
        e && T(o, e),
        o
    }
    ,
    fr.provide = function(t, e) {
        return t ? function() {
            var n = Object.create(null);
            return dr(n, a(t) ? t.call(this) : t),
            e && dr(n, a(e) ? e.call(this) : e, !1),
            n
        }
        : e
    }
    ;
    var mr = function(t, e) {
        return void 0 === e ? t : e
    };
    function gr(t, n, r) {
        if (a(n) && (n = n.options),
        function(t, n) {
            var r = t.props;
            if (r) {
                var o, i, a = {};
                if (e(r))
                    for (o = r.length; o--; )
                        "string" == typeof (i = r[o]) && (a[w(i)] = {
                            type: null
                        });
                else if (u(r))
                    for (var s in r)
                        i = r[s],
                        a[w(s)] = u(i) ? i : {
                            type: i
                        };
                t.props = a
            }
        }(n),
        function(t, n) {
            var r = t.inject;
            if (r) {
                var o = t.inject = {};
                if (e(r))
                    for (var i = 0; i < r.length; i++)
                        o[r[i]] = {
                            from: r[i]
                        };
                else if (u(r))
                    for (var a in r) {
                        var s = r[a];
                        o[a] = u(s) ? T({
                            from: a
                        }, s) : {
                            from: s
                        }
                    }
            }
        }(n),
        function(t) {
            var e = t.directives;
            if (e)
                for (var n in e) {
                    var r = e[n];
                    a(r) && (e[n] = {
                        bind: r,
                        update: r
                    })
                }
        }(n),
        !n._base && (n.extends && (t = gr(t, n.extends, r)),
        n.mixins))
            for (var o = 0, i = n.mixins.length; o < i; o++)
                t = gr(t, n.mixins[o], r);
        var s, c = {};
        for (s in t)
            l(s);
        for (s in n)
            _(t, s) || l(s);
        function l(e) {
            var o = fr[e] || mr;
            c[e] = o(t[e], n[e], r, e)
        }
        return c
    }
    function yr(t, e, n, r) {
        if ("string" == typeof n) {
            var o = t[e];
            if (_(o, n))
                return o[n];
            var i = w(n);
            if (_(o, i))
                return o[i];
            var a = x(i);
            return _(o, a) ? o[a] : o[n] || o[i] || o[a]
        }
    }
    function _r(t, e, n, r) {
        var o = e[t]
          , i = !_(n, t)
          , s = n[t]
          , c = xr(Boolean, o.type);
        if (c > -1)
            if (i && !_(o, "default"))
                s = !1;
            else if ("" === s || s === k(t)) {
                var u = xr(String, o.type);
                (u < 0 || c < u) && (s = !0)
            }
        if (void 0 === s) {
            s = function(t, e, n) {
                if (!_(e, "default"))
                    return;
                var r = e.default;
                if (t && t.$options.propsData && void 0 === t.$options.propsData[n] && void 0 !== t._props[n])
                    return t._props[n];
                return a(r) && "Function" !== $r(e.type) ? r.call(t) : r
            }(r, o, t);
            var l = Ct;
            kt(!0),
            Tt(s),
            kt(l)
        }
        return s
    }
    var br = /^\s*function (\w+)/;
    function $r(t) {
        var e = t && t.toString().match(br);
        return e ? e[1] : ""
    }
    function wr(t, e) {
        return $r(t) === $r(e)
    }
    function xr(t, n) {
        if (!e(n))
            return wr(n, t) ? 0 : -1;
        for (var r = 0, o = n.length; r < o; r++)
            if (wr(n[r], t))
                return r;
        return -1
    }
    function Cr(t) {
        this._init(t)
    }
    function kr(t) {
        t.cid = 0;
        var e = 1;
        t.extend = function(t) {
            t = t || {};
            var n = this
              , r = n.cid
              , o = t._Ctor || (t._Ctor = {});
            if (o[r])
                return o[r];
            var i = ir(t) || ir(n.options)
              , a = function(t) {
                this._init(t)
            };
            return (a.prototype = Object.create(n.prototype)).constructor = a,
            a.cid = e++,
            a.options = gr(n.options, t),
            a.super = n,
            a.options.props && function(t) {
                var e = t.options.props;
                for (var n in e)
                    Jn(t.prototype, "_props", n)
            }(a),
            a.options.computed && function(t) {
                var e = t.options.computed;
                for (var n in e)
                    Zn(t.prototype, n, e[n])
            }(a),
            a.extend = n.extend,
            a.mixin = n.mixin,
            a.use = n.use,
            R.forEach((function(t) {
                a[t] = n[t]
            }
            )),
            i && (a.options.components[i] = a),
            a.superOptions = n.options,
            a.extendOptions = t,
            a.sealedOptions = T({}, a.options),
            o[r] = a,
            a
        }
    }
    function Sr(t) {
        return t && (ir(t.Ctor.options) || t.tag)
    }
    function Or(t, n) {
        return e(t) ? t.indexOf(n) > -1 : "string" == typeof t ? t.split(",").indexOf(n) > -1 : (r = t,
        "[object RegExp]" === c.call(r) && t.test(n));
        var r
    }
    function Tr(t, e) {
        var n = t.cache
          , r = t.keys
          , o = t._vnode;
        for (var i in n) {
            var a = n[i];
            if (a) {
                var s = a.name;
                s && !e(s) && Ar(n, i, r, o)
            }
        }
    }
    function Ar(t, e, n, r) {
        var o = t[e];
        !o || r && o.tag === r.tag || o.componentInstance.$destroy(),
        t[e] = null,
        g(n, e)
    }
    !function(e) {
        e.prototype._init = function(e) {
            var n = this;
            n._uid = tr++,
            n._isVue = !0,
            n.__v_skip = !0,
            n._scope = new un(!0),
            n._scope._vm = !0,
            e && e._isComponent ? function(t, e) {
                var n = t.$options = Object.create(t.constructor.options)
                  , r = e._parentVnode;
                n.parent = e.parent,
                n._parentVnode = r;
                var o = r.componentOptions;
                n.propsData = o.propsData,
                n._parentListeners = o.listeners,
                n._renderChildren = o.children,
                n._componentTag = o.tag,
                e.render && (n.render = e.render,
                n.staticRenderFns = e.staticRenderFns)
            }(n, e) : n.$options = gr(er(n.constructor), e || {}, n),
            n._renderProxy = n,
            n._self = n,
            function(t) {
                var e = t.$options
                  , n = e.parent;
                if (n && !e.abstract) {
                    for (; n.$options.abstract && n.$parent; )
                        n = n.$parent;
                    n.$children.push(t)
                }
                t.$parent = n,
                t.$root = n ? n.$root : t,
                t.$children = [],
                t.$refs = {},
                t._provided = n ? n._provided : Object.create(null),
                t._watcher = null,
                t._inactive = null,
                t._directInactive = !1,
                t._isMounted = !1,
                t._isDestroyed = !1,
                t._isBeingDestroyed = !1
            }(n),
            function(t) {
                t._events = Object.create(null),
                t._hasHookEvent = !1;
                var e = t.$options._parentListeners;
                e && Me(t, e)
            }(n),
            function(e) {
                e._vnode = null,
                e._staticTrees = null;
                var n = e.$options
                  , r = e.$vnode = n._parentVnode
                  , o = r && r.context;
                e.$slots = ge(n._renderChildren, o),
                e.$scopedSlots = r ? be(e.$parent, r.data.scopedSlots, e.$slots) : t,
                e._c = function(t, n, r, o) {
                    return te(e, t, n, r, o, !1)
                }
                ,
                e.$createElement = function(t, n, r, o) {
                    return te(e, t, n, r, o, !0)
                }
                ;
                var i = r && r.data;
                At(e, "$attrs", i && i.attrs || t, null, !0),
                At(e, "$listeners", n._parentListeners || t, null, !0)
            }(n),
            Be(n, "beforeCreate", void 0, !1),
            function(t) {
                var e = Qn(t.$options.inject, t);
                e && (kt(!1),
                Object.keys(e).forEach((function(n) {
                    At(t, n, e[n])
                }
                )),
                kt(!0))
            }(n),
            qn(n),
            function(t) {
                var e = t.$options.provide;
                if (e) {
                    var n = a(e) ? e.call(t) : e;
                    if (!s(n))
                        return;
                    for (var r = ln(t), o = st ? Reflect.ownKeys(n) : Object.keys(n), i = 0; i < o.length; i++) {
                        var c = o[i];
                        Object.defineProperty(r, c, Object.getOwnPropertyDescriptor(n, c))
                    }
                }
            }(n),
            Be(n, "created"),
            n.$options.el && n.$mount(n.$options.el)
        }
    }(Cr),
    function(t) {
        var e = {
            get: function() {
                return this._data
            }
        }
          , n = {
            get: function() {
                return this._props
            }
        };
        Object.defineProperty(t.prototype, "$data", e),
        Object.defineProperty(t.prototype, "$props", n),
        t.prototype.$set = jt,
        t.prototype.$delete = Et,
        t.prototype.$watch = function(t, e, n) {
            var r = this;
            if (u(e))
                return Yn(r, t, e, n);
            (n = n || {}).user = !0;
            var o = new Vn(r,t,e,n);
            if (n.immediate) {
                var i = 'callback for immediate watcher "'.concat(o.expression, '"');
                yt(),
                dn(e, r, [o.value], r, i),
                _t()
            }
            return function() {
                o.teardown()
            }
        }
    }(Cr),
    function(t) {
        var n = /^hook:/;
        t.prototype.$on = function(t, r) {
            var o = this;
            if (e(t))
                for (var i = 0, a = t.length; i < a; i++)
                    o.$on(t[i], r);
            else
                (o._events[t] || (o._events[t] = [])).push(r),
                n.test(t) && (o._hasHookEvent = !0);
            return o
        }
        ,
        t.prototype.$once = function(t, e) {
            var n = this;
            function r() {
                n.$off(t, r),
                e.apply(n, arguments)
            }
            return r.fn = e,
            n.$on(t, r),
            n
        }
        ,
        t.prototype.$off = function(t, n) {
            var r = this;
            if (!arguments.length)
                return r._events = Object.create(null),
                r;
            if (e(t)) {
                for (var o = 0, i = t.length; o < i; o++)
                    r.$off(t[o], n);
                return r
            }
            var a, s = r._events[t];
            if (!s)
                return r;
            if (!n)
                return r._events[t] = null,
                r;
            for (var c = s.length; c--; )
                if ((a = s[c]) === n || a.fn === n) {
                    s.splice(c, 1);
                    break
                }
            return r
        }
        ,
        t.prototype.$emit = function(t) {
            var e = this
              , n = e._events[t];
            if (n) {
                n = n.length > 1 ? O(n) : n;
                for (var r = O(arguments, 1), o = 'event handler for "'.concat(t, '"'), i = 0, a = n.length; i < a; i++)
                    dn(n[i], e, r, e, o)
            }
            return e
        }
    }(Cr),
    function(t) {
        t.prototype._update = function(t, e) {
            var n = this
              , r = n.$el
              , o = n._vnode
              , i = Le(n);
            n._vnode = t,
            n.$el = o ? n.__patch__(o, t) : n.__patch__(n.$el, t, e, !1),
            i(),
            r && (r.__vue__ = null),
            n.$el && (n.$el.__vue__ = n);
            for (var a = n; a && a.$vnode && a.$parent && a.$vnode === a.$parent._vnode; )
                a.$parent.$el = a.$el,
                a = a.$parent
        }
        ,
        t.prototype.$forceUpdate = function() {
            this._watcher && this._watcher.update()
        }
        ,
        t.prototype.$destroy = function() {
            var t = this;
            if (!t._isBeingDestroyed) {
                Be(t, "beforeDestroy"),
                t._isBeingDestroyed = !0;
                var e = t.$parent;
                !e || e._isBeingDestroyed || t.$options.abstract || g(e.$children, t),
                t._scope.stop(),
                t._data.__ob__ && t._data.__ob__.vmCount--,
                t._isDestroyed = !0,
                t.__patch__(t._vnode, null),
                Be(t, "destroyed"),
                t.$off(),
                t.$el && (t.$el.__vue__ = null),
                t.$vnode && (t.$vnode.parent = null)
            }
        }
    }(Cr),
    function(t) {
        me(t.prototype),
        t.prototype.$nextTick = function(t) {
            return Cn(t, this)
        }
        ,
        t.prototype._render = function() {
            var t, n = this, r = n.$options, o = r.render, i = r._parentVnode;
            i && n._isMounted && (n.$scopedSlots = be(n.$parent, i.data.scopedSlots, n.$slots, n.$scopedSlots),
            n._slotsProxy && Se(n._slotsProxy, n.$scopedSlots)),
            n.$vnode = i;
            try {
                ut(n),
                Ae = n,
                t = o.call(n._renderProxy, n.$createElement)
            } catch (e) {
                fn(e, n, "render"),
                t = n._vnode
            } finally {
                Ae = null,
                ut()
            }
            return e(t) && 1 === t.length && (t = t[0]),
            t instanceof lt || (t = ft()),
            t.parent = i,
            t
        }
    }(Cr);
    var jr = [String, RegExp, Array]
      , Er = {
        name: "keep-alive",
        abstract: !0,
        props: {
            include: jr,
            exclude: jr,
            max: [String, Number]
        },
        methods: {
            cacheVNode: function() {
                var t = this
                  , e = t.cache
                  , n = t.keys
                  , r = t.vnodeToCache
                  , o = t.keyToCache;
                if (r) {
                    var i = r.tag
                      , a = r.componentInstance
                      , s = r.componentOptions;
                    e[o] = {
                        name: Sr(s),
                        tag: i,
                        componentInstance: a
                    },
                    n.push(o),
                    this.max && n.length > parseInt(this.max) && Ar(e, n[0], n, this._vnode),
                    this.vnodeToCache = null
                }
            }
        },
        created: function() {
            this.cache = Object.create(null),
            this.keys = []
        },
        destroyed: function() {
            for (var t in this.cache)
                Ar(this.cache, t, this.keys)
        },
        mounted: function() {
            var t = this;
            this.cacheVNode(),
            this.$watch("include", (function(e) {
                Tr(t, (function(t) {
                    return Or(e, t)
                }
                ))
            }
            )),
            this.$watch("exclude", (function(e) {
                Tr(t, (function(t) {
                    return !Or(e, t)
                }
                ))
            }
            ))
        },
        updated: function() {
            this.cacheVNode()
        },
        render: function() {
            var t = this.$slots.default
              , e = Ee(t)
              , n = e && e.componentOptions;
            if (n) {
                var r = Sr(n)
                  , o = this.include
                  , i = this.exclude;
                if (o && (!r || !Or(o, r)) || i && r && Or(i, r))
                    return e;
                var a = this.cache
                  , s = this.keys
                  , c = null == e.key ? n.Ctor.cid + (n.tag ? "::".concat(n.tag) : "") : e.key;
                a[c] ? (e.componentInstance = a[c].componentInstance,
                g(s, c),
                s.push(c)) : (this.vnodeToCache = e,
                this.keyToCache = c),
                e.data.keepAlive = !0
            }
            return e || t && t[0]
        }
    }
      , Nr = {
        KeepAlive: Er
    };
    !function(t) {
        var e = {
            get: function() {
                return H
            }
        };
        Object.defineProperty(t, "config", e),
        t.util = {
            warn: lr,
            extend: T,
            mergeOptions: gr,
            defineReactive: At
        },
        t.set = jt,
        t.delete = Et,
        t.nextTick = Cn,
        t.observable = function(t) {
            return Tt(t),
            t
        }
        ,
        t.options = Object.create(null),
        R.forEach((function(e) {
            t.options[e + "s"] = Object.create(null)
        }
        )),
        t.options._base = t,
        T(t.options.components, Nr),
        function(t) {
            t.use = function(t) {
                var e = this._installedPlugins || (this._installedPlugins = []);
                if (e.indexOf(t) > -1)
                    return this;
                var n = O(arguments, 1);
                return n.unshift(this),
                a(t.install) ? t.install.apply(t, n) : a(t) && t.apply(null, n),
                e.push(t),
                this
            }
        }(t),
        function(t) {
            t.mixin = function(t) {
                return this.options = gr(this.options, t),
                this
            }
        }(t),
        kr(t),
        function(t) {
            R.forEach((function(e) {
                t[e] = function(t, n) {
                    return n ? ("component" === e && u(n) && (n.name = n.name || t,
                    n = this.options._base.extend(n)),
                    "directive" === e && a(n) && (n = {
                        bind: n,
                        update: n
                    }),
                    this.options[e + "s"][t] = n,
                    n) : this.options[e + "s"][t]
                }
            }
            ))
        }(t)
    }(Cr),
    Object.defineProperty(Cr.prototype, "$isServer", {
        get: rt
    }),
    Object.defineProperty(Cr.prototype, "$ssrContext", {
        get: function() {
            return this.$vnode && this.$vnode.ssrContext
        }
    }),
    Object.defineProperty(Cr, "FunctionalRenderContext", {
        value: nr
    }),
    Cr.version = Rn;
    var Pr = v("style,class")
      , Dr = v("input,textarea,option,select,progress")
      , Mr = function(t, e, n) {
        return "value" === n && Dr(t) && "button" !== e || "selected" === n && "option" === t || "checked" === n && "input" === t || "muted" === n && "video" === t
    }
      , Ir = v("contenteditable,draggable,spellcheck")
      , Lr = v("events,caret,typing,plaintext-only")
      , Rr = v("allowfullscreen,async,autofocus,autoplay,checked,compact,controls,declare,default,defaultchecked,defaultmuted,defaultselected,defer,disabled,enabled,formnovalidate,hidden,indeterminate,inert,ismap,itemscope,loop,multiple,muted,nohref,noresize,noshade,novalidate,nowrap,open,pauseonexit,readonly,required,reversed,scoped,seamless,selected,sortable,truespeed,typemustmatch,visible")
      , Fr = "http://www.w3.org/1999/xlink"
      , Hr = function(t) {
        return ":" === t.charAt(5) && "xlink" === t.slice(0, 5)
    }
      , Br = function(t) {
        return Hr(t) ? t.slice(6, t.length) : ""
    }
      , Ur = function(t) {
        return null == t || !1 === t
    };
    function zr(t) {
        for (var e = t.data, n = t, o = t; r(o.componentInstance); )
            (o = o.componentInstance._vnode) && o.data && (e = Vr(o.data, e));
        for (; r(n = n.parent); )
            n && n.data && (e = Vr(e, n.data));
        return function(t, e) {
            if (r(t) || r(e))
                return Kr(t, Jr(e));
            return ""
        }(e.staticClass, e.class)
    }
    function Vr(t, e) {
        return {
            staticClass: Kr(t.staticClass, e.staticClass),
            class: r(t.class) ? [t.class, e.class] : e.class
        }
    }
    function Kr(t, e) {
        return t ? e ? t + " " + e : t : e || ""
    }
    function Jr(t) {
        return Array.isArray(t) ? function(t) {
            for (var e, n = "", o = 0, i = t.length; o < i; o++)
                r(e = Jr(t[o])) && "" !== e && (n && (n += " "),
                n += e);
            return n
        }(t) : s(t) ? function(t) {
            var e = "";
            for (var n in t)
                t[n] && (e && (e += " "),
                e += n);
            return e
        }(t) : "string" == typeof t ? t : ""
    }
    var qr = {
        svg: "http://www.w3.org/2000/svg",
        math: "http://www.w3.org/1998/Math/MathML"
    }
      , Wr = v("html,body,base,head,link,meta,style,title,address,article,aside,footer,header,h1,h2,h3,h4,h5,h6,hgroup,nav,section,div,dd,dl,dt,figcaption,figure,picture,hr,img,li,main,ol,p,pre,ul,a,b,abbr,bdi,bdo,br,cite,code,data,dfn,em,i,kbd,mark,q,rp,rt,rtc,ruby,s,samp,small,span,strong,sub,sup,time,u,var,wbr,area,audio,map,track,video,embed,object,param,source,canvas,script,noscript,del,ins,caption,col,colgroup,table,thead,tbody,td,th,tr,button,datalist,fieldset,form,input,label,legend,meter,optgroup,option,output,progress,select,textarea,details,dialog,menu,menuitem,summary,content,element,shadow,template,blockquote,iframe,tfoot")
      , Zr = v("svg,animate,circle,clippath,cursor,defs,desc,ellipse,filter,font-face,foreignobject,g,glyph,image,line,marker,mask,missing-glyph,path,pattern,polygon,polyline,rect,switch,symbol,text,textpath,tspan,use,view", !0)
      , Gr = function(t) {
        return Wr(t) || Zr(t)
    };
    function Xr(t) {
        return Zr(t) ? "svg" : "math" === t ? "math" : void 0
    }
    var Yr = Object.create(null);
    var Qr = v("text,number,password,search,email,tel,url");
    function to(t) {
        if ("string" == typeof t) {
            var e = document.querySelector(t);
            return e || document.createElement("div")
        }
        return t
    }
    var eo = Object.freeze({
        __proto__: null,
        createElement: function(t, e) {
            var n = document.createElement(t);
            return "select" !== t || e.data && e.data.attrs && void 0 !== e.data.attrs.multiple && n.setAttribute("multiple", "multiple"),
            n
        },
        createElementNS: function(t, e) {
            return document.createElementNS(qr[t], e)
        },
        createTextNode: function(t) {
            return document.createTextNode(t)
        },
        createComment: function(t) {
            return document.createComment(t)
        },
        insertBefore: function(t, e, n) {
            t.insertBefore(e, n)
        },
        removeChild: function(t, e) {
            t.removeChild(e)
        },
        appendChild: function(t, e) {
            t.appendChild(e)
        },
        parentNode: function(t) {
            return t.parentNode
        },
        nextSibling: function(t) {
            return t.nextSibling
        },
        tagName: function(t) {
            return t.tagName
        },
        setTextContent: function(t, e) {
            t.textContent = e
        },
        setStyleScope: function(t, e) {
            t.setAttribute(e, "")
        }
    })
      , no = {
        create: function(t, e) {
            ro(e)
        },
        update: function(t, e) {
            t.data.ref !== e.data.ref && (ro(t, !0),
            ro(e))
        },
        destroy: function(t) {
            ro(t, !0)
        }
    };
    function ro(t, n) {
        var o = t.data.ref;
        if (r(o)) {
            var i = t.context
              , s = t.componentInstance || t.elm
              , c = n ? null : s
              , u = n ? void 0 : s;
            if (a(o))
                dn(o, i, [c], i, "template ref function");
            else {
                var l = t.data.refInFor
                  , f = "string" == typeof o || "number" == typeof o
                  , d = Ft(o)
                  , p = i.$refs;
                if (f || d)
                    if (l) {
                        var v = f ? p[o] : o.value;
                        n ? e(v) && g(v, s) : e(v) ? v.includes(s) || v.push(s) : f ? (p[o] = [s],
                        oo(i, o, p[o])) : o.value = [s]
                    } else if (f) {
                        if (n && p[o] !== s)
                            return;
                        p[o] = u,
                        oo(i, o, c)
                    } else if (d) {
                        if (n && o.value !== s)
                            return;
                        o.value = c
                    }
            }
        }
    }
    function oo(t, e, n) {
        var r = t._setupState;
        r && _(r, e) && (Ft(r[e]) ? r[e].value = n : r[e] = n)
    }
    var io = new lt("",{},[])
      , ao = ["create", "activate", "update", "remove", "destroy"];
    function so(t, e) {
        return t.key === e.key && t.asyncFactory === e.asyncFactory && (t.tag === e.tag && t.isComment === e.isComment && r(t.data) === r(e.data) && function(t, e) {
            if ("input" !== t.tag)
                return !0;
            var n, o = r(n = t.data) && r(n = n.attrs) && n.type, i = r(n = e.data) && r(n = n.attrs) && n.type;
            return o === i || Qr(o) && Qr(i)
        }(t, e) || o(t.isAsyncPlaceholder) && n(e.asyncFactory.error))
    }
    function co(t, e, n) {
        var o, i, a = {};
        for (o = e; o <= n; ++o)
            r(i = t[o].key) && (a[i] = o);
        return a
    }
    var uo = {
        create: lo,
        update: lo,
        destroy: function(t) {
            lo(t, io)
        }
    };
    function lo(t, e) {
        (t.data.directives || e.data.directives) && function(t, e) {
            var n, r, o, i = t === io, a = e === io, s = po(t.data.directives, t.context), c = po(e.data.directives, e.context), u = [], l = [];
            for (n in c)
                r = s[n],
                o = c[n],
                r ? (o.oldValue = r.value,
                o.oldArg = r.arg,
                ho(o, "update", e, t),
                o.def && o.def.componentUpdated && l.push(o)) : (ho(o, "bind", e, t),
                o.def && o.def.inserted && u.push(o));
            if (u.length) {
                var f = function() {
                    for (var n = 0; n < u.length; n++)
                        ho(u[n], "inserted", e, t)
                };
                i ? Zt(e, "insert", f) : f()
            }
            l.length && Zt(e, "postpatch", (function() {
                for (var n = 0; n < l.length; n++)
                    ho(l[n], "componentUpdated", e, t)
            }
            ));
            if (!i)
                for (n in s)
                    c[n] || ho(s[n], "unbind", t, t, a)
        }(t, e)
    }
    var fo = Object.create(null);
    function po(t, e) {
        var n, r, o = Object.create(null);
        if (!t)
            return o;
        for (n = 0; n < t.length; n++) {
            if ((r = t[n]).modifiers || (r.modifiers = fo),
            o[vo(r)] = r,
            e._setupState && e._setupState.__sfc) {
                var i = r.def || yr(e, "_setupState", "v-" + r.name);
                r.def = "function" == typeof i ? {
                    bind: i,
                    update: i
                } : i
            }
            r.def = r.def || yr(e.$options, "directives", r.name)
        }
        return o
    }
    function vo(t) {
        return t.rawName || "".concat(t.name, ".").concat(Object.keys(t.modifiers || {}).join("."))
    }
    function ho(t, e, n, r, o) {
        var i = t.def && t.def[e];
        if (i)
            try {
                i(n.elm, t, n, r, o)
            } catch (r) {
                fn(r, n.context, "directive ".concat(t.name, " ").concat(e, " hook"))
            }
    }
    var mo = [no, uo];
    function go(t, e) {
        var i = e.componentOptions;
        if (!(r(i) && !1 === i.Ctor.options.inheritAttrs || n(t.data.attrs) && n(e.data.attrs))) {
            var a, s, c = e.elm, u = t.data.attrs || {}, l = e.data.attrs || {};
            for (a in (r(l.__ob__) || o(l._v_attr_proxy)) && (l = e.data.attrs = T({}, l)),
            l)
                s = l[a],
                u[a] !== s && yo(c, a, s, e.data.pre);
            for (a in (W || G) && l.value !== u.value && yo(c, "value", l.value),
            u)
                n(l[a]) && (Hr(a) ? c.removeAttributeNS(Fr, Br(a)) : Ir(a) || c.removeAttribute(a))
        }
    }
    function yo(t, e, n, r) {
        r || t.tagName.indexOf("-") > -1 ? _o(t, e, n) : Rr(e) ? Ur(n) ? t.removeAttribute(e) : (n = "allowfullscreen" === e && "EMBED" === t.tagName ? "true" : e,
        t.setAttribute(e, n)) : Ir(e) ? t.setAttribute(e, function(t, e) {
            return Ur(e) || "false" === e ? "false" : "contenteditable" === t && Lr(e) ? e : "true"
        }(e, n)) : Hr(e) ? Ur(n) ? t.removeAttributeNS(Fr, Br(e)) : t.setAttributeNS(Fr, e, n) : _o(t, e, n)
    }
    function _o(t, e, n) {
        if (Ur(n))
            t.removeAttribute(e);
        else {
            if (W && !Z && "TEXTAREA" === t.tagName && "placeholder" === e && "" !== n && !t.__ieph) {
                var r = function(e) {
                    e.stopImmediatePropagation(),
                    t.removeEventListener("input", r)
                };
                t.addEventListener("input", r),
                t.__ieph = !0
            }
            t.setAttribute(e, n)
        }
    }
    var bo = {
        create: go,
        update: go
    };
    function $o(t, e) {
        var o = e.elm
          , i = e.data
          , a = t.data;
        if (!(n(i.staticClass) && n(i.class) && (n(a) || n(a.staticClass) && n(a.class)))) {
            var s = zr(e)
              , c = o._transitionClasses;
            r(c) && (s = Kr(s, Jr(c))),
            s !== o._prevClass && (o.setAttribute("class", s),
            o._prevClass = s)
        }
    }
    var wo, xo, Co, ko, So, Oo, To = {
        create: $o,
        update: $o
    }, Ao = /[\w).+\-_$\]]/;
    function jo(t) {
        var e, n, r, o, i, a = !1, s = !1, c = !1, u = !1, l = 0, f = 0, d = 0, p = 0;
        for (r = 0; r < t.length; r++)
            if (n = e,
            e = t.charCodeAt(r),
            a)
                39 === e && 92 !== n && (a = !1);
            else if (s)
                34 === e && 92 !== n && (s = !1);
            else if (c)
                96 === e && 92 !== n && (c = !1);
            else if (u)
                47 === e && 92 !== n && (u = !1);
            else if (124 !== e || 124 === t.charCodeAt(r + 1) || 124 === t.charCodeAt(r - 1) || l || f || d) {
                switch (e) {
                case 34:
                    s = !0;
                    break;
                case 39:
                    a = !0;
                    break;
                case 96:
                    c = !0;
                    break;
                case 40:
                    d++;
                    break;
                case 41:
                    d--;
                    break;
                case 91:
                    f++;
                    break;
                case 93:
                    f--;
                    break;
                case 123:
                    l++;
                    break;
                case 125:
                    l--
                }
                if (47 === e) {
                    for (var v = r - 1, h = void 0; v >= 0 && " " === (h = t.charAt(v)); v--)
                        ;
                    h && Ao.test(h) || (u = !0)
                }
            } else
                void 0 === o ? (p = r + 1,
                o = t.slice(0, r).trim()) : m();
        function m() {
            (i || (i = [])).push(t.slice(p, r).trim()),
            p = r + 1
        }
        if (void 0 === o ? o = t.slice(0, r).trim() : 0 !== p && m(),
        i)
            for (r = 0; r < i.length; r++)
                o = Eo(o, i[r]);
        return o
    }
    function Eo(t, e) {
        var n = e.indexOf("(");
        if (n < 0)
            return '_f("'.concat(e, '")(').concat(t, ")");
        var r = e.slice(0, n)
          , o = e.slice(n + 1);
        return '_f("'.concat(r, '")(').concat(t).concat(")" !== o ? "," + o : o)
    }
    function No(t, e) {
        console.error("[Vue compiler]: ".concat(t))
    }
    function Po(t, e) {
        return t ? t.map((function(t) {
            return t[e]
        }
        )).filter((function(t) {
            return t
        }
        )) : []
    }
    function Do(t, e, n, r, o) {
        (t.props || (t.props = [])).push(zo({
            name: e,
            value: n,
            dynamic: o
        }, r)),
        t.plain = !1
    }
    function Mo(t, e, n, r, o) {
        (o ? t.dynamicAttrs || (t.dynamicAttrs = []) : t.attrs || (t.attrs = [])).push(zo({
            name: e,
            value: n,
            dynamic: o
        }, r)),
        t.plain = !1
    }
    function Io(t, e, n, r) {
        t.attrsMap[e] = n,
        t.attrsList.push(zo({
            name: e,
            value: n
        }, r))
    }
    function Lo(t, e, n, r, o, i, a, s) {
        (t.directives || (t.directives = [])).push(zo({
            name: e,
            rawName: n,
            value: r,
            arg: o,
            isDynamicArg: i,
            modifiers: a
        }, s)),
        t.plain = !1
    }
    function Ro(t, e, n) {
        return n ? "_p(".concat(e, ',"').concat(t, '")') : t + e
    }
    function Fo(e, n, r, o, i, a, s, c) {
        var u;
        (o = o || t).right ? c ? n = "(".concat(n, ")==='click'?'contextmenu':(").concat(n, ")") : "click" === n && (n = "contextmenu",
        delete o.right) : o.middle && (c ? n = "(".concat(n, ")==='click'?'mouseup':(").concat(n, ")") : "click" === n && (n = "mouseup")),
        o.capture && (delete o.capture,
        n = Ro("!", n, c)),
        o.once && (delete o.once,
        n = Ro("~", n, c)),
        o.passive && (delete o.passive,
        n = Ro("&", n, c)),
        o.native ? (delete o.native,
        u = e.nativeEvents || (e.nativeEvents = {})) : u = e.events || (e.events = {});
        var l = zo({
            value: r.trim(),
            dynamic: c
        }, s);
        o !== t && (l.modifiers = o);
        var f = u[n];
        Array.isArray(f) ? i ? f.unshift(l) : f.push(l) : u[n] = f ? i ? [l, f] : [f, l] : l,
        e.plain = !1
    }
    function Ho(t, e, n) {
        var r = Bo(t, ":" + e) || Bo(t, "v-bind:" + e);
        if (null != r)
            return jo(r);
        if (!1 !== n) {
            var o = Bo(t, e);
            if (null != o)
                return JSON.stringify(o)
        }
    }
    function Bo(t, e, n) {
        var r;
        if (null != (r = t.attrsMap[e]))
            for (var o = t.attrsList, i = 0, a = o.length; i < a; i++)
                if (o[i].name === e) {
                    o.splice(i, 1);
                    break
                }
        return n && delete t.attrsMap[e],
        r
    }
    function Uo(t, e) {
        for (var n = t.attrsList, r = 0, o = n.length; r < o; r++) {
            var i = n[r];
            if (e.test(i.name))
                return n.splice(r, 1),
                i
        }
    }
    function zo(t, e) {
        return e && (null != e.start && (t.start = e.start),
        null != e.end && (t.end = e.end)),
        t
    }
    function Vo(t, e, n) {
        var r = n || {}
          , o = r.number
          , i = "$$v"
          , a = i;
        r.trim && (a = "(typeof ".concat(i, " === 'string'") + "? ".concat(i, ".trim()") + ": ".concat(i, ")")),
        o && (a = "_n(".concat(a, ")"));
        var s = Ko(e, a);
        t.model = {
            value: "(".concat(e, ")"),
            expression: JSON.stringify(e),
            callback: "function (".concat(i, ") {").concat(s, "}")
        }
    }
    function Ko(t, e) {
        var n = function(t) {
            if (t = t.trim(),
            wo = t.length,
            t.indexOf("[") < 0 || t.lastIndexOf("]") < wo - 1)
                return (ko = t.lastIndexOf(".")) > -1 ? {
                    exp: t.slice(0, ko),
                    key: '"' + t.slice(ko + 1) + '"'
                } : {
                    exp: t,
                    key: null
                };
            xo = t,
            ko = So = Oo = 0;
            for (; !qo(); )
                Wo(Co = Jo()) ? Go(Co) : 91 === Co && Zo(Co);
            return {
                exp: t.slice(0, So),
                key: t.slice(So + 1, Oo)
            }
        }(t);
        return null === n.key ? "".concat(t, "=").concat(e) : "$set(".concat(n.exp, ", ").concat(n.key, ", ").concat(e, ")")
    }
    function Jo() {
        return xo.charCodeAt(++ko)
    }
    function qo() {
        return ko >= wo
    }
    function Wo(t) {
        return 34 === t || 39 === t
    }
    function Zo(t) {
        var e = 1;
        for (So = ko; !qo(); )
            if (Wo(t = Jo()))
                Go(t);
            else if (91 === t && e++,
            93 === t && e--,
            0 === e) {
                Oo = ko;
                break
            }
    }
    function Go(t) {
        for (var e = t; !qo() && (t = Jo()) !== e; )
            ;
    }
    var Xo, Yo = "__r";
    function Qo(t, e, n) {
        var r = Xo;
        return function o() {
            var i = e.apply(null, arguments);
            null !== i && ni(t, o, n, r)
        }
    }
    var ti = mn && !(Q && Number(Q[1]) <= 53);
    function ei(t, e, n, r) {
        if (ti) {
            var o = We
              , i = e;
            e = i._wrapper = function(t) {
                if (t.target === t.currentTarget || t.timeStamp >= o || t.timeStamp <= 0 || t.target.ownerDocument !== document)
                    return i.apply(this, arguments)
            }
        }
        Xo.addEventListener(t, e, et ? {
            capture: n,
            passive: r
        } : n)
    }
    function ni(t, e, n, r) {
        (r || Xo).removeEventListener(t, e._wrapper || e, n)
    }
    function ri(t, e) {
        if (!n(t.data.on) || !n(e.data.on)) {
            var o = e.data.on || {}
              , i = t.data.on || {};
            Xo = e.elm || t.elm,
            function(t) {
                if (r(t.__r)) {
                    var e = W ? "change" : "input";
                    t[e] = [].concat(t.__r, t[e] || []),
                    delete t.__r
                }
                r(t.__c) && (t.change = [].concat(t.__c, t.change || []),
                delete t.__c)
            }(o),
            Wt(o, i, ei, ni, Qo, e.context),
            Xo = void 0
        }
    }
    var oi, ii = {
        create: ri,
        update: ri,
        destroy: function(t) {
            return ri(t, io)
        }
    };
    function ai(t, e) {
        if (!n(t.data.domProps) || !n(e.data.domProps)) {
            var i, a, s = e.elm, c = t.data.domProps || {}, u = e.data.domProps || {};
            for (i in (r(u.__ob__) || o(u._v_attr_proxy)) && (u = e.data.domProps = T({}, u)),
            c)
                i in u || (s[i] = "");
            for (i in u) {
                if (a = u[i],
                "textContent" === i || "innerHTML" === i) {
                    if (e.children && (e.children.length = 0),
                    a === c[i])
                        continue;
                    1 === s.childNodes.length && s.removeChild(s.childNodes[0])
                }
                if ("value" === i && "PROGRESS" !== s.tagName) {
                    s._value = a;
                    var l = n(a) ? "" : String(a);
                    si(s, l) && (s.value = l)
                } else if ("innerHTML" === i && Zr(s.tagName) && n(s.innerHTML)) {
                    (oi = oi || document.createElement("div")).innerHTML = "<svg>".concat(a, "</svg>");
                    for (var f = oi.firstChild; s.firstChild; )
                        s.removeChild(s.firstChild);
                    for (; f.firstChild; )
                        s.appendChild(f.firstChild)
                } else if (a !== c[i])
                    try {
                        s[i] = a
                    } catch (t) {}
            }
        }
    }
    function si(t, e) {
        return !t.composing && ("OPTION" === t.tagName || function(t, e) {
            var n = !0;
            try {
                n = document.activeElement !== t
            } catch (t) {}
            return n && t.value !== e
        }(t, e) || function(t, e) {
            var n = t.value
              , o = t._vModifiers;
            if (r(o)) {
                if (o.number)
                    return p(n) !== p(e);
                if (o.trim)
                    return n.trim() !== e.trim()
            }
            return n !== e
        }(t, e))
    }
    var ci = {
        create: ai,
        update: ai
    }
      , ui = b((function(t) {
        var e = {}
          , n = /:(.+)/;
        return t.split(/;(?![^(]*\))/g).forEach((function(t) {
            if (t) {
                var r = t.split(n);
                r.length > 1 && (e[r[0].trim()] = r[1].trim())
            }
        }
        )),
        e
    }
    ));
    function li(t) {
        var e = fi(t.style);
        return t.staticStyle ? T(t.staticStyle, e) : e
    }
    function fi(t) {
        return Array.isArray(t) ? A(t) : "string" == typeof t ? ui(t) : t
    }
    var di, pi = /^--/, vi = /\s*!important$/, hi = function(t, e, n) {
        if (pi.test(e))
            t.style.setProperty(e, n);
        else if (vi.test(n))
            t.style.setProperty(k(e), n.replace(vi, ""), "important");
        else {
            var r = gi(e);
            if (Array.isArray(n))
                for (var o = 0, i = n.length; o < i; o++)
                    t.style[r] = n[o];
            else
                t.style[r] = n
        }
    }, mi = ["Webkit", "Moz", "ms"], gi = b((function(t) {
        if (di = di || document.createElement("div").style,
        "filter" !== (t = w(t)) && t in di)
            return t;
        for (var e = t.charAt(0).toUpperCase() + t.slice(1), n = 0; n < mi.length; n++) {
            var r = mi[n] + e;
            if (r in di)
                return r
        }
    }
    ));
    function yi(t, e) {
        var o = e.data
          , i = t.data;
        if (!(n(o.staticStyle) && n(o.style) && n(i.staticStyle) && n(i.style))) {
            var a, s, c = e.elm, u = i.staticStyle, l = i.normalizedStyle || i.style || {}, f = u || l, d = fi(e.data.style) || {};
            e.data.normalizedStyle = r(d.__ob__) ? T({}, d) : d;
            var p = function(t, e) {
                var n, r = {};
                if (e)
                    for (var o = t; o.componentInstance; )
                        (o = o.componentInstance._vnode) && o.data && (n = li(o.data)) && T(r, n);
                (n = li(t.data)) && T(r, n);
                for (var i = t; i = i.parent; )
                    i.data && (n = li(i.data)) && T(r, n);
                return r
            }(e, !0);
            for (s in f)
                n(p[s]) && hi(c, s, "");
            for (s in p)
                (a = p[s]) !== f[s] && hi(c, s, null == a ? "" : a)
        }
    }
    var _i = {
        create: yi,
        update: yi
    }
      , bi = /\s+/;
    function $i(t, e) {
        if (e && (e = e.trim()))
            if (t.classList)
                e.indexOf(" ") > -1 ? e.split(bi).forEach((function(e) {
                    return t.classList.add(e)
                }
                )) : t.classList.add(e);
            else {
                var n = " ".concat(t.getAttribute("class") || "", " ");
                n.indexOf(" " + e + " ") < 0 && t.setAttribute("class", (n + e).trim())
            }
    }
    function wi(t, e) {
        if (e && (e = e.trim()))
            if (t.classList)
                e.indexOf(" ") > -1 ? e.split(bi).forEach((function(e) {
                    return t.classList.remove(e)
                }
                )) : t.classList.remove(e),
                t.classList.length || t.removeAttribute("class");
            else {
                for (var n = " ".concat(t.getAttribute("class") || "", " "), r = " " + e + " "; n.indexOf(r) >= 0; )
                    n = n.replace(r, " ");
                (n = n.trim()) ? t.setAttribute("class", n) : t.removeAttribute("class")
            }
    }
    function xi(t) {
        if (t) {
            if ("object" == typeof t) {
                var e = {};
                return !1 !== t.css && T(e, Ci(t.name || "v")),
                T(e, t),
                e
            }
            return "string" == typeof t ? Ci(t) : void 0
        }
    }
    var Ci = b((function(t) {
        return {
            enterClass: "".concat(t, "-enter"),
            enterToClass: "".concat(t, "-enter-to"),
            enterActiveClass: "".concat(t, "-enter-active"),
            leaveClass: "".concat(t, "-leave"),
            leaveToClass: "".concat(t, "-leave-to"),
            leaveActiveClass: "".concat(t, "-leave-active")
        }
    }
    ))
      , ki = J && !Z
      , Si = "transition"
      , Oi = "animation"
      , Ti = "transition"
      , Ai = "transitionend"
      , ji = "animation"
      , Ei = "animationend";
    ki && (void 0 === window.ontransitionend && void 0 !== window.onwebkittransitionend && (Ti = "WebkitTransition",
    Ai = "webkitTransitionEnd"),
    void 0 === window.onanimationend && void 0 !== window.onwebkitanimationend && (ji = "WebkitAnimation",
    Ei = "webkitAnimationEnd"));
    var Ni = J ? window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : setTimeout : function(t) {
        return t()
    }
    ;
    function Pi(t) {
        Ni((function() {
            Ni(t)
        }
        ))
    }
    function Di(t, e) {
        var n = t._transitionClasses || (t._transitionClasses = []);
        n.indexOf(e) < 0 && (n.push(e),
        $i(t, e))
    }
    function Mi(t, e) {
        t._transitionClasses && g(t._transitionClasses, e),
        wi(t, e)
    }
    function Ii(t, e, n) {
        var r = Ri(t, e)
          , o = r.type
          , i = r.timeout
          , a = r.propCount;
        if (!o)
            return n();
        var s = o === Si ? Ai : Ei
          , c = 0
          , u = function() {
            t.removeEventListener(s, l),
            n()
        }
          , l = function(e) {
            e.target === t && ++c >= a && u()
        };
        setTimeout((function() {
            c < a && u()
        }
        ), i + 1),
        t.addEventListener(s, l)
    }
    var Li = /\b(transform|all)(,|$)/;
    function Ri(t, e) {
        var n, r = window.getComputedStyle(t), o = (r[Ti + "Delay"] || "").split(", "), i = (r[Ti + "Duration"] || "").split(", "), a = Fi(o, i), s = (r[ji + "Delay"] || "").split(", "), c = (r[ji + "Duration"] || "").split(", "), u = Fi(s, c), l = 0, f = 0;
        return e === Si ? a > 0 && (n = Si,
        l = a,
        f = i.length) : e === Oi ? u > 0 && (n = Oi,
        l = u,
        f = c.length) : f = (n = (l = Math.max(a, u)) > 0 ? a > u ? Si : Oi : null) ? n === Si ? i.length : c.length : 0,
        {
            type: n,
            timeout: l,
            propCount: f,
            hasTransform: n === Si && Li.test(r[Ti + "Property"])
        }
    }
    function Fi(t, e) {
        for (; t.length < e.length; )
            t = t.concat(t);
        return Math.max.apply(null, e.map((function(e, n) {
            return Hi(e) + Hi(t[n])
        }
        )))
    }
    function Hi(t) {
        return 1e3 * Number(t.slice(0, -1).replace(",", "."))
    }
    function Bi(t, e) {
        var o = t.elm;
        r(o._leaveCb) && (o._leaveCb.cancelled = !0,
        o._leaveCb());
        var i = xi(t.data.transition);
        if (!n(i) && !r(o._enterCb) && 1 === o.nodeType) {
            for (var c = i.css, u = i.type, l = i.enterClass, f = i.enterToClass, d = i.enterActiveClass, v = i.appearClass, h = i.appearToClass, m = i.appearActiveClass, g = i.beforeEnter, y = i.enter, _ = i.afterEnter, b = i.enterCancelled, $ = i.beforeAppear, w = i.appear, x = i.afterAppear, C = i.appearCancelled, k = i.duration, S = Ie, O = Ie.$vnode; O && O.parent; )
                S = O.context,
                O = O.parent;
            var T = !S._isMounted || !t.isRootInsert;
            if (!T || w || "" === w) {
                var A = T && v ? v : l
                  , j = T && m ? m : d
                  , E = T && h ? h : f
                  , N = T && $ || g
                  , P = T && a(w) ? w : y
                  , D = T && x || _
                  , I = T && C || b
                  , L = p(s(k) ? k.enter : k)
                  , R = !1 !== c && !Z
                  , F = Vi(P)
                  , H = o._enterCb = M((function() {
                    R && (Mi(o, E),
                    Mi(o, j)),
                    H.cancelled ? (R && Mi(o, A),
                    I && I(o)) : D && D(o),
                    o._enterCb = null
                }
                ));
                t.data.show || Zt(t, "insert", (function() {
                    var e = o.parentNode
                      , n = e && e._pending && e._pending[t.key];
                    n && n.tag === t.tag && n.elm._leaveCb && n.elm._leaveCb(),
                    P && P(o, H)
                }
                )),
                N && N(o),
                R && (Di(o, A),
                Di(o, j),
                Pi((function() {
                    Mi(o, A),
                    H.cancelled || (Di(o, E),
                    F || (zi(L) ? setTimeout(H, L) : Ii(o, u, H)))
                }
                ))),
                t.data.show && (e && e(),
                P && P(o, H)),
                R || F || H()
            }
        }
    }
    function Ui(t, e) {
        var o = t.elm;
        r(o._enterCb) && (o._enterCb.cancelled = !0,
        o._enterCb());
        var i = xi(t.data.transition);
        if (n(i) || 1 !== o.nodeType)
            return e();
        if (!r(o._leaveCb)) {
            var a = i.css
              , c = i.type
              , u = i.leaveClass
              , l = i.leaveToClass
              , f = i.leaveActiveClass
              , d = i.beforeLeave
              , v = i.leave
              , h = i.afterLeave
              , m = i.leaveCancelled
              , g = i.delayLeave
              , y = i.duration
              , _ = !1 !== a && !Z
              , b = Vi(v)
              , $ = p(s(y) ? y.leave : y)
              , w = o._leaveCb = M((function() {
                o.parentNode && o.parentNode._pending && (o.parentNode._pending[t.key] = null),
                _ && (Mi(o, l),
                Mi(o, f)),
                w.cancelled ? (_ && Mi(o, u),
                m && m(o)) : (e(),
                h && h(o)),
                o._leaveCb = null
            }
            ));
            g ? g(x) : x()
        }
        function x() {
            w.cancelled || (!t.data.show && o.parentNode && ((o.parentNode._pending || (o.parentNode._pending = {}))[t.key] = t),
            d && d(o),
            _ && (Di(o, u),
            Di(o, f),
            Pi((function() {
                Mi(o, u),
                w.cancelled || (Di(o, l),
                b || (zi($) ? setTimeout(w, $) : Ii(o, c, w)))
            }
            ))),
            v && v(o, w),
            _ || b || w())
        }
    }
    function zi(t) {
        return "number" == typeof t && !isNaN(t)
    }
    function Vi(t) {
        if (n(t))
            return !1;
        var e = t.fns;
        return r(e) ? Vi(Array.isArray(e) ? e[0] : e) : (t._length || t.length) > 1
    }
    function Ki(t, e) {
        !0 !== e.data.show && Bi(e)
    }
    var Ji = function(t) {
        var a, s, c = {}, u = t.modules, l = t.nodeOps;
        for (a = 0; a < ao.length; ++a)
            for (c[ao[a]] = [],
            s = 0; s < u.length; ++s)
                r(u[s][ao[a]]) && c[ao[a]].push(u[s][ao[a]]);
        function f(t) {
            var e = l.parentNode(t);
            r(e) && l.removeChild(e, t)
        }
        function d(t, e, n, i, a, s, u) {
            if (r(t.elm) && r(s) && (t = s[u] = pt(t)),
            t.isRootInsert = !a,
            !function(t, e, n, i) {
                var a = t.data;
                if (r(a)) {
                    var s = r(t.componentInstance) && a.keepAlive;
                    if (r(a = a.hook) && r(a = a.init) && a(t, !1),
                    r(t.componentInstance))
                        return p(t, e),
                        h(n, t.elm, i),
                        o(s) && function(t, e, n, o) {
                            var i, a = t;
                            for (; a.componentInstance; )
                                if (r(i = (a = a.componentInstance._vnode).data) && r(i = i.transition)) {
                                    for (i = 0; i < c.activate.length; ++i)
                                        c.activate[i](io, a);
                                    e.push(a);
                                    break
                                }
                            h(n, t.elm, o)
                        }(t, e, n, i),
                        !0
                }
            }(t, e, n, i)) {
                var f = t.data
                  , d = t.children
                  , v = t.tag;
                r(v) ? (t.elm = t.ns ? l.createElementNS(t.ns, v) : l.createElement(v, t),
                _(t),
                m(t, d, e),
                r(f) && y(t, e),
                h(n, t.elm, i)) : o(t.isComment) ? (t.elm = l.createComment(t.text),
                h(n, t.elm, i)) : (t.elm = l.createTextNode(t.text),
                h(n, t.elm, i))
            }
        }
        function p(t, e) {
            r(t.data.pendingInsert) && (e.push.apply(e, t.data.pendingInsert),
            t.data.pendingInsert = null),
            t.elm = t.componentInstance.$el,
            g(t) ? (y(t, e),
            _(t)) : (ro(t),
            e.push(t))
        }
        function h(t, e, n) {
            r(t) && (r(n) ? l.parentNode(n) === t && l.insertBefore(t, e, n) : l.appendChild(t, e))
        }
        function m(t, n, r) {
            if (e(n))
                for (var o = 0; o < n.length; ++o)
                    d(n[o], r, t.elm, null, !0, n, o);
            else
                i(t.text) && l.appendChild(t.elm, l.createTextNode(String(t.text)))
        }
        function g(t) {
            for (; t.componentInstance; )
                t = t.componentInstance._vnode;
            return r(t.tag)
        }
        function y(t, e) {
            for (var n = 0; n < c.create.length; ++n)
                c.create[n](io, t);
            r(a = t.data.hook) && (r(a.create) && a.create(io, t),
            r(a.insert) && e.push(t))
        }
        function _(t) {
            var e;
            if (r(e = t.fnScopeId))
                l.setStyleScope(t.elm, e);
            else
                for (var n = t; n; )
                    r(e = n.context) && r(e = e.$options._scopeId) && l.setStyleScope(t.elm, e),
                    n = n.parent;
            r(e = Ie) && e !== t.context && e !== t.fnContext && r(e = e.$options._scopeId) && l.setStyleScope(t.elm, e)
        }
        function b(t, e, n, r, o, i) {
            for (; r <= o; ++r)
                d(n[r], i, t, e, !1, n, r)
        }
        function $(t) {
            var e, n, o = t.data;
            if (r(o))
                for (r(e = o.hook) && r(e = e.destroy) && e(t),
                e = 0; e < c.destroy.length; ++e)
                    c.destroy[e](t);
            if (r(e = t.children))
                for (n = 0; n < t.children.length; ++n)
                    $(t.children[n])
        }
        function w(t, e, n) {
            for (; e <= n; ++e) {
                var o = t[e];
                r(o) && (r(o.tag) ? (x(o),
                $(o)) : f(o.elm))
            }
        }
        function x(t, e) {
            if (r(e) || r(t.data)) {
                var n, o = c.remove.length + 1;
                for (r(e) ? e.listeners += o : e = function(t, e) {
                    function n() {
                        0 == --n.listeners && f(t)
                    }
                    return n.listeners = e,
                    n
                }(t.elm, o),
                r(n = t.componentInstance) && r(n = n._vnode) && r(n.data) && x(n, e),
                n = 0; n < c.remove.length; ++n)
                    c.remove[n](t, e);
                r(n = t.data.hook) && r(n = n.remove) ? n(t, e) : e()
            } else
                f(t.elm)
        }
        function C(t, e, n, o) {
            for (var i = n; i < o; i++) {
                var a = e[i];
                if (r(a) && so(t, a))
                    return i
            }
        }
        function k(t, e, i, a, s, u) {
            if (t !== e) {
                r(e.elm) && r(a) && (e = a[s] = pt(e));
                var f = e.elm = t.elm;
                if (o(t.isAsyncPlaceholder))
                    r(e.asyncFactory.resolved) ? T(t.elm, e, i) : e.isAsyncPlaceholder = !0;
                else if (o(e.isStatic) && o(t.isStatic) && e.key === t.key && (o(e.isCloned) || o(e.isOnce)))
                    e.componentInstance = t.componentInstance;
                else {
                    var p, v = e.data;
                    r(v) && r(p = v.hook) && r(p = p.prepatch) && p(t, e);
                    var h = t.children
                      , m = e.children;
                    if (r(v) && g(e)) {
                        for (p = 0; p < c.update.length; ++p)
                            c.update[p](t, e);
                        r(p = v.hook) && r(p = p.update) && p(t, e)
                    }
                    n(e.text) ? r(h) && r(m) ? h !== m && function(t, e, o, i, a) {
                        for (var s, c, u, f = 0, p = 0, v = e.length - 1, h = e[0], m = e[v], g = o.length - 1, y = o[0], _ = o[g], $ = !a; f <= v && p <= g; )
                            n(h) ? h = e[++f] : n(m) ? m = e[--v] : so(h, y) ? (k(h, y, i, o, p),
                            h = e[++f],
                            y = o[++p]) : so(m, _) ? (k(m, _, i, o, g),
                            m = e[--v],
                            _ = o[--g]) : so(h, _) ? (k(h, _, i, o, g),
                            $ && l.insertBefore(t, h.elm, l.nextSibling(m.elm)),
                            h = e[++f],
                            _ = o[--g]) : so(m, y) ? (k(m, y, i, o, p),
                            $ && l.insertBefore(t, m.elm, h.elm),
                            m = e[--v],
                            y = o[++p]) : (n(s) && (s = co(e, f, v)),
                            n(c = r(y.key) ? s[y.key] : C(y, e, f, v)) ? d(y, i, t, h.elm, !1, o, p) : so(u = e[c], y) ? (k(u, y, i, o, p),
                            e[c] = void 0,
                            $ && l.insertBefore(t, u.elm, h.elm)) : d(y, i, t, h.elm, !1, o, p),
                            y = o[++p]);
                        f > v ? b(t, n(o[g + 1]) ? null : o[g + 1].elm, o, p, g, i) : p > g && w(e, f, v)
                    }(f, h, m, i, u) : r(m) ? (r(t.text) && l.setTextContent(f, ""),
                    b(f, null, m, 0, m.length - 1, i)) : r(h) ? w(h, 0, h.length - 1) : r(t.text) && l.setTextContent(f, "") : t.text !== e.text && l.setTextContent(f, e.text),
                    r(v) && r(p = v.hook) && r(p = p.postpatch) && p(t, e)
                }
            }
        }
        function S(t, e, n) {
            if (o(n) && r(t.parent))
                t.parent.data.pendingInsert = e;
            else
                for (var i = 0; i < e.length; ++i)
                    e[i].data.hook.insert(e[i])
        }
        var O = v("attrs,class,staticClass,staticStyle,key");
        function T(t, e, n, i) {
            var a, s = e.tag, c = e.data, u = e.children;
            if (i = i || c && c.pre,
            e.elm = t,
            o(e.isComment) && r(e.asyncFactory))
                return e.isAsyncPlaceholder = !0,
                !0;
            if (r(c) && (r(a = c.hook) && r(a = a.init) && a(e, !0),
            r(a = e.componentInstance)))
                return p(e, n),
                !0;
            if (r(s)) {
                if (r(u))
                    if (t.hasChildNodes())
                        if (r(a = c) && r(a = a.domProps) && r(a = a.innerHTML)) {
                            if (a !== t.innerHTML)
                                return !1
                        } else {
                            for (var l = !0, f = t.firstChild, d = 0; d < u.length; d++) {
                                if (!f || !T(f, u[d], n, i)) {
                                    l = !1;
                                    break
                                }
                                f = f.nextSibling
                            }
                            if (!l || f)
                                return !1
                        }
                    else
                        m(e, u, n);
                if (r(c)) {
                    var v = !1;
                    for (var h in c)
                        if (!O(h)) {
                            v = !0,
                            y(e, n);
                            break
                        }
                    !v && c.class && Bn(c.class)
                }
            } else
                t.data !== e.text && (t.data = e.text);
            return !0
        }
        return function(t, e, i, a) {
            if (!n(e)) {
                var s, u = !1, f = [];
                if (n(t))
                    u = !0,
                    d(e, f);
                else {
                    var p = r(t.nodeType);
                    if (!p && so(t, e))
                        k(t, e, f, null, null, a);
                    else {
                        if (p) {
                            if (1 === t.nodeType && t.hasAttribute(L) && (t.removeAttribute(L),
                            i = !0),
                            o(i) && T(t, e, f))
                                return S(e, f, !0),
                                t;
                            s = t,
                            t = new lt(l.tagName(s).toLowerCase(),{},[],void 0,s)
                        }
                        var v = t.elm
                          , h = l.parentNode(v);
                        if (d(e, f, v._leaveCb ? null : h, l.nextSibling(v)),
                        r(e.parent))
                            for (var m = e.parent, y = g(e); m; ) {
                                for (var _ = 0; _ < c.destroy.length; ++_)
                                    c.destroy[_](m);
                                if (m.elm = e.elm,
                                y) {
                                    for (var b = 0; b < c.create.length; ++b)
                                        c.create[b](io, m);
                                    var x = m.data.hook.insert;
                                    if (x.merged)
                                        for (var C = 1; C < x.fns.length; C++)
                                            x.fns[C]()
                                } else
                                    ro(m);
                                m = m.parent
                            }
                        r(h) ? w([t], 0, 0) : r(t.tag) && $(t)
                    }
                }
                return S(e, f, u),
                e.elm
            }
            r(t) && $(t)
        }
    }({
        nodeOps: eo,
        modules: [bo, To, ii, ci, _i, J ? {
            create: Ki,
            activate: Ki,
            remove: function(t, e) {
                !0 !== t.data.show ? Ui(t, e) : e()
            }
        } : {}].concat(mo)
    });
    Z && document.addEventListener("selectionchange", (function() {
        var t = document.activeElement;
        t && t.vmodel && ta(t, "input")
    }
    ));
    var qi = {
        inserted: function(t, e, n, r) {
            "select" === n.tag ? (r.elm && !r.elm._vOptions ? Zt(n, "postpatch", (function() {
                qi.componentUpdated(t, e, n)
            }
            )) : Wi(t, e, n.context),
            t._vOptions = [].map.call(t.options, Xi)) : ("textarea" === n.tag || Qr(t.type)) && (t._vModifiers = e.modifiers,
            e.modifiers.lazy || (t.addEventListener("compositionstart", Yi),
            t.addEventListener("compositionend", Qi),
            t.addEventListener("change", Qi),
            Z && (t.vmodel = !0)))
        },
        componentUpdated: function(t, e, n) {
            if ("select" === n.tag) {
                Wi(t, e, n.context);
                var r = t._vOptions
                  , o = t._vOptions = [].map.call(t.options, Xi);
                if (o.some((function(t, e) {
                    return !P(t, r[e])
                }
                )))
                    (t.multiple ? e.value.some((function(t) {
                        return Gi(t, o)
                    }
                    )) : e.value !== e.oldValue && Gi(e.value, o)) && ta(t, "change")
            }
        }
    };
    function Wi(t, e, n) {
        Zi(t, e),
        (W || G) && setTimeout((function() {
            Zi(t, e)
        }
        ), 0)
    }
    function Zi(t, e, n) {
        var r = e.value
          , o = t.multiple;
        if (!o || Array.isArray(r)) {
            for (var i, a, s = 0, c = t.options.length; s < c; s++)
                if (a = t.options[s],
                o)
                    i = D(r, Xi(a)) > -1,
                    a.selected !== i && (a.selected = i);
                else if (P(Xi(a), r))
                    return void (t.selectedIndex !== s && (t.selectedIndex = s));
            o || (t.selectedIndex = -1)
        }
    }
    function Gi(t, e) {
        return e.every((function(e) {
            return !P(e, t)
        }
        ))
    }
    function Xi(t) {
        return "_value"in t ? t._value : t.value
    }
    function Yi(t) {
        t.target.composing = !0
    }
    function Qi(t) {
        t.target.composing && (t.target.composing = !1,
        ta(t.target, "input"))
    }
    function ta(t, e) {
        var n = document.createEvent("HTMLEvents");
        n.initEvent(e, !0, !0),
        t.dispatchEvent(n)
    }
    function ea(t) {
        return !t.componentInstance || t.data && t.data.transition ? t : ea(t.componentInstance._vnode)
    }
    var na = {
        bind: function(t, e, n) {
            var r = e.value
              , o = (n = ea(n)).data && n.data.transition
              , i = t.__vOriginalDisplay = "none" === t.style.display ? "" : t.style.display;
            r && o ? (n.data.show = !0,
            Bi(n, (function() {
                t.style.display = i
            }
            ))) : t.style.display = r ? i : "none"
        },
        update: function(t, e, n) {
            var r = e.value;
            !r != !e.oldValue && ((n = ea(n)).data && n.data.transition ? (n.data.show = !0,
            r ? Bi(n, (function() {
                t.style.display = t.__vOriginalDisplay
            }
            )) : Ui(n, (function() {
                t.style.display = "none"
            }
            ))) : t.style.display = r ? t.__vOriginalDisplay : "none")
        },
        unbind: function(t, e, n, r, o) {
            o || (t.style.display = t.__vOriginalDisplay)
        }
    }
      , ra = {
        model: qi,
        show: na
    }
      , oa = {
        name: String,
        appear: Boolean,
        css: Boolean,
        mode: String,
        type: String,
        enterClass: String,
        leaveClass: String,
        enterToClass: String,
        leaveToClass: String,
        enterActiveClass: String,
        leaveActiveClass: String,
        appearClass: String,
        appearActiveClass: String,
        appearToClass: String,
        duration: [Number, String, Object]
    };
    function ia(t) {
        var e = t && t.componentOptions;
        return e && e.Ctor.options.abstract ? ia(Ee(e.children)) : t
    }
    function aa(t) {
        var e = {}
          , n = t.$options;
        for (var r in n.propsData)
            e[r] = t[r];
        var o = n._parentListeners;
        for (var r in o)
            e[w(r)] = o[r];
        return e
    }
    function sa(t, e) {
        if (/\d-keep-alive$/.test(e.tag))
            return t("keep-alive", {
                props: e.componentOptions.propsData
            })
    }
    var ca = function(t) {
        return t.tag || _e(t)
    }
      , ua = function(t) {
        return "show" === t.name
    }
      , la = {
        name: "transition",
        props: oa,
        abstract: !0,
        render: function(t) {
            var e = this
              , n = this.$slots.default;
            if (n && (n = n.filter(ca)).length) {
                var r = this.mode
                  , o = n[0];
                if (function(t) {
                    for (; t = t.parent; )
                        if (t.data.transition)
                            return !0
                }(this.$vnode))
                    return o;
                var a = ia(o);
                if (!a)
                    return o;
                if (this._leaving)
                    return sa(t, o);
                var s = "__transition-".concat(this._uid, "-");
                a.key = null == a.key ? a.isComment ? s + "comment" : s + a.tag : i(a.key) ? 0 === String(a.key).indexOf(s) ? a.key : s + a.key : a.key;
                var c = (a.data || (a.data = {})).transition = aa(this)
                  , u = this._vnode
                  , l = ia(u);
                if (a.data.directives && a.data.directives.some(ua) && (a.data.show = !0),
                l && l.data && !function(t, e) {
                    return e.key === t.key && e.tag === t.tag
                }(a, l) && !_e(l) && (!l.componentInstance || !l.componentInstance._vnode.isComment)) {
                    var f = l.data.transition = T({}, c);
                    if ("out-in" === r)
                        return this._leaving = !0,
                        Zt(f, "afterLeave", (function() {
                            e._leaving = !1,
                            e.$forceUpdate()
                        }
                        )),
                        sa(t, o);
                    if ("in-out" === r) {
                        if (_e(a))
                            return u;
                        var d, p = function() {
                            d()
                        };
                        Zt(c, "afterEnter", p),
                        Zt(c, "enterCancelled", p),
                        Zt(f, "delayLeave", (function(t) {
                            d = t
                        }
                        ))
                    }
                }
                return o
            }
        }
    }
      , fa = T({
        tag: String,
        moveClass: String
    }, oa);
    delete fa.mode;
    var da = {
        props: fa,
        beforeMount: function() {
            var t = this
              , e = this._update;
            this._update = function(n, r) {
                var o = Le(t);
                t.__patch__(t._vnode, t.kept, !1, !0),
                t._vnode = t.kept,
                o(),
                e.call(t, n, r)
            }
        },
        render: function(t) {
            for (var e = this.tag || this.$vnode.data.tag || "span", n = Object.create(null), r = this.prevChildren = this.children, o = this.$slots.default || [], i = this.children = [], a = aa(this), s = 0; s < o.length; s++) {
                (l = o[s]).tag && null != l.key && 0 !== String(l.key).indexOf("__vlist") && (i.push(l),
                n[l.key] = l,
                (l.data || (l.data = {})).transition = a)
            }
            if (r) {
                var c = []
                  , u = [];
                for (s = 0; s < r.length; s++) {
                    var l;
                    (l = r[s]).data.transition = a,
                    l.data.pos = l.elm.getBoundingClientRect(),
                    n[l.key] ? c.push(l) : u.push(l)
                }
                this.kept = t(e, null, c),
                this.removed = u
            }
            return t(e, null, i)
        },
        updated: function() {
            var t = this.prevChildren
              , e = this.moveClass || (this.name || "v") + "-move";
            t.length && this.hasMove(t[0].elm, e) && (t.forEach(pa),
            t.forEach(va),
            t.forEach(ha),
            this._reflow = document.body.offsetHeight,
            t.forEach((function(t) {
                if (t.data.moved) {
                    var n = t.elm
                      , r = n.style;
                    Di(n, e),
                    r.transform = r.WebkitTransform = r.transitionDuration = "",
                    n.addEventListener(Ai, n._moveCb = function t(r) {
                        r && r.target !== n || r && !/transform$/.test(r.propertyName) || (n.removeEventListener(Ai, t),
                        n._moveCb = null,
                        Mi(n, e))
                    }
                    )
                }
            }
            )))
        },
        methods: {
            hasMove: function(t, e) {
                if (!ki)
                    return !1;
                if (this._hasMove)
                    return this._hasMove;
                var n = t.cloneNode();
                t._transitionClasses && t._transitionClasses.forEach((function(t) {
                    wi(n, t)
                }
                )),
                $i(n, e),
                n.style.display = "none",
                this.$el.appendChild(n);
                var r = Ri(n);
                return this.$el.removeChild(n),
                this._hasMove = r.hasTransform
            }
        }
    };
    function pa(t) {
        t.elm._moveCb && t.elm._moveCb(),
        t.elm._enterCb && t.elm._enterCb()
    }
    function va(t) {
        t.data.newPos = t.elm.getBoundingClientRect()
    }
    function ha(t) {
        var e = t.data.pos
          , n = t.data.newPos
          , r = e.left - n.left
          , o = e.top - n.top;
        if (r || o) {
            t.data.moved = !0;
            var i = t.elm.style;
            i.transform = i.WebkitTransform = "translate(".concat(r, "px,").concat(o, "px)"),
            i.transitionDuration = "0s"
        }
    }
    var ma = {
        Transition: la,
        TransitionGroup: da
    };
    Cr.config.mustUseProp = Mr,
    Cr.config.isReservedTag = Gr,
    Cr.config.isReservedAttr = Pr,
    Cr.config.getTagNamespace = Xr,
    Cr.config.isUnknownElement = function(t) {
        if (!J)
            return !0;
        if (Gr(t))
            return !1;
        if (t = t.toLowerCase(),
        null != Yr[t])
            return Yr[t];
        var e = document.createElement(t);
        return t.indexOf("-") > -1 ? Yr[t] = e.constructor === window.HTMLUnknownElement || e.constructor === window.HTMLElement : Yr[t] = /HTMLUnknownElement/.test(e.toString())
    }
    ,
    T(Cr.options.directives, ra),
    T(Cr.options.components, ma),
    Cr.prototype.__patch__ = J ? Ji : j,
    Cr.prototype.$mount = function(t, e) {
        return function(t, e, n) {
            var r;
            t.$el = e,
            t.$options.render || (t.$options.render = ft),
            Be(t, "beforeMount"),
            r = function() {
                t._update(t._render(), n)
            }
            ,
            new Vn(t,r,j,{
                before: function() {
                    t._isMounted && !t._isDestroyed && Be(t, "beforeUpdate")
                }
            },!0),
            n = !1;
            var o = t._preWatchers;
            if (o)
                for (var i = 0; i < o.length; i++)
                    o[i].run();
            return null == t.$vnode && (t._isMounted = !0,
            Be(t, "mounted")),
            t
        }(this, t = t && J ? to(t) : void 0, e)
    }
    ,
    J && setTimeout((function() {
        H.devtools && ot && ot.emit("init", Cr)
    }
    ), 0);
    var ga = /\{\{((?:.|\r?\n)+?)\}\}/g
      , ya = /[-.*+?^${}()|[\]\/\\]/g
      , _a = b((function(t) {
        var e = t[0].replace(ya, "\\$&")
          , n = t[1].replace(ya, "\\$&");
        return new RegExp(e + "((?:.|\\n)+?)" + n,"g")
    }
    ));
    var ba = {
        staticKeys: ["staticClass"],
        transformNode: function(t, e) {
            e.warn;
            var n = Bo(t, "class");
            n && (t.staticClass = JSON.stringify(n.replace(/\s+/g, " ").trim()));
            var r = Ho(t, "class", !1);
            r && (t.classBinding = r)
        },
        genData: function(t) {
            var e = "";
            return t.staticClass && (e += "staticClass:".concat(t.staticClass, ",")),
            t.classBinding && (e += "class:".concat(t.classBinding, ",")),
            e
        }
    };
    var $a, wa = {
        staticKeys: ["staticStyle"],
        transformNode: function(t, e) {
            e.warn;
            var n = Bo(t, "style");
            n && (t.staticStyle = JSON.stringify(ui(n)));
            var r = Ho(t, "style", !1);
            r && (t.styleBinding = r)
        },
        genData: function(t) {
            var e = "";
            return t.staticStyle && (e += "staticStyle:".concat(t.staticStyle, ",")),
            t.styleBinding && (e += "style:(".concat(t.styleBinding, "),")),
            e
        }
    }, xa = function(t) {
        return ($a = $a || document.createElement("div")).innerHTML = t,
        $a.textContent
    }, Ca = v("area,base,br,col,embed,frame,hr,img,input,isindex,keygen,link,meta,param,source,track,wbr"), ka = v("colgroup,dd,dt,li,options,p,td,tfoot,th,thead,tr,source"), Sa = v("address,article,aside,base,blockquote,body,caption,col,colgroup,dd,details,dialog,div,dl,dt,fieldset,figcaption,figure,footer,form,h1,h2,h3,h4,h5,h6,head,header,hgroup,hr,html,legend,li,menuitem,meta,optgroup,option,param,rp,rt,source,style,summary,tbody,td,tfoot,th,thead,title,tr,track"), Oa = /^\s*([^\s"'<>\/=]+)(?:\s*(=)\s*(?:"([^"]*)"+|'([^']*)'+|([^\s"'=<>`]+)))?/, Ta = /^\s*((?:v-[\w-]+:|@|:|#)\[[^=]+?\][^\s"'<>\/=]*)(?:\s*(=)\s*(?:"([^"]*)"+|'([^']*)'+|([^\s"'=<>`]+)))?/, Aa = "[a-zA-Z_][\\-\\.0-9_a-zA-Z".concat(B.source, "]*"), ja = "((?:".concat(Aa, "\\:)?").concat(Aa, ")"), Ea = new RegExp("^<".concat(ja)), Na = /^\s*(\/?)>/, Pa = new RegExp("^<\\/".concat(ja, "[^>]*>")), Da = /^<!DOCTYPE [^>]+>/i, Ma = /^<!\--/, Ia = /^<!\[/, La = v("script,style,textarea", !0), Ra = {}, Fa = {
        "&lt;": "<",
        "&gt;": ">",
        "&quot;": '"',
        "&amp;": "&",
        "&#10;": "\n",
        "&#9;": "\t",
        "&#39;": "'"
    }, Ha = /&(?:lt|gt|quot|amp|#39);/g, Ba = /&(?:lt|gt|quot|amp|#39|#10|#9);/g, Ua = v("pre,textarea", !0), za = function(t, e) {
        return t && Ua(t) && "\n" === e[0]
    };
    function Va(t, e) {
        var n = e ? Ba : Ha;
        return t.replace(n, (function(t) {
            return Fa[t]
        }
        ))
    }
    function Ka(t, e) {
        for (var n, r, o = [], i = e.expectHTML, a = e.isUnaryTag || E, s = e.canBeLeftOpenTag || E, c = 0, u = function() {
            if (n = t,
            r && La(r)) {
                var u = 0
                  , d = r.toLowerCase()
                  , p = Ra[d] || (Ra[d] = new RegExp("([\\s\\S]*?)(</" + d + "[^>]*>)","i"));
                w = t.replace(p, (function(t, n, r) {
                    return u = r.length,
                    La(d) || "noscript" === d || (n = n.replace(/<!\--([\s\S]*?)-->/g, "$1").replace(/<!\[CDATA\[([\s\S]*?)]]>/g, "$1")),
                    za(d, n) && (n = n.slice(1)),
                    e.chars && e.chars(n),
                    ""
                }
                ));
                c += t.length - w.length,
                t = w,
                f(d, c - u, c)
            } else {
                var v = t.indexOf("<");
                if (0 === v) {
                    if (Ma.test(t)) {
                        var h = t.indexOf("--\x3e");
                        if (h >= 0)
                            return e.shouldKeepComment && e.comment && e.comment(t.substring(4, h), c, c + h + 3),
                            l(h + 3),
                            "continue"
                    }
                    if (Ia.test(t)) {
                        var m = t.indexOf("]>");
                        if (m >= 0)
                            return l(m + 2),
                            "continue"
                    }
                    var g = t.match(Da);
                    if (g)
                        return l(g[0].length),
                        "continue";
                    var y = t.match(Pa);
                    if (y) {
                        var _ = c;
                        return l(y[0].length),
                        f(y[1], _, c),
                        "continue"
                    }
                    var b = function() {
                        var e = t.match(Ea);
                        if (e) {
                            var n = {
                                tagName: e[1],
                                attrs: [],
                                start: c
                            };
                            l(e[0].length);
                            for (var r = void 0, o = void 0; !(r = t.match(Na)) && (o = t.match(Ta) || t.match(Oa)); )
                                o.start = c,
                                l(o[0].length),
                                o.end = c,
                                n.attrs.push(o);
                            if (r)
                                return n.unarySlash = r[1],
                                l(r[0].length),
                                n.end = c,
                                n
                        }
                    }();
                    if (b)
                        return function(t) {
                            var n = t.tagName
                              , c = t.unarySlash;
                            i && ("p" === r && Sa(n) && f(r),
                            s(n) && r === n && f(n));
                            for (var u = a(n) || !!c, l = t.attrs.length, d = new Array(l), p = 0; p < l; p++) {
                                var v = t.attrs[p]
                                  , h = v[3] || v[4] || v[5] || ""
                                  , m = "a" === n && "href" === v[1] ? e.shouldDecodeNewlinesForHref : e.shouldDecodeNewlines;
                                d[p] = {
                                    name: v[1],
                                    value: Va(h, m)
                                }
                            }
                            u || (o.push({
                                tag: n,
                                lowerCasedTag: n.toLowerCase(),
                                attrs: d,
                                start: t.start,
                                end: t.end
                            }),
                            r = n);
                            e.start && e.start(n, d, u, t.start, t.end)
                        }(b),
                        za(b.tagName, t) && l(1),
                        "continue"
                }
                var $ = void 0
                  , w = void 0
                  , x = void 0;
                if (v >= 0) {
                    for (w = t.slice(v); !(Pa.test(w) || Ea.test(w) || Ma.test(w) || Ia.test(w) || (x = w.indexOf("<", 1)) < 0); )
                        v += x,
                        w = t.slice(v);
                    $ = t.substring(0, v)
                }
                v < 0 && ($ = t),
                $ && l($.length),
                e.chars && $ && e.chars($, c - $.length, c)
            }
            if (t === n)
                return e.chars && e.chars(t),
                "break"
        }; t; ) {
            if ("break" === u())
                break
        }
        function l(e) {
            c += e,
            t = t.substring(e)
        }
        function f(t, n, i) {
            var a, s;
            if (null == n && (n = c),
            null == i && (i = c),
            t)
                for (s = t.toLowerCase(),
                a = o.length - 1; a >= 0 && o[a].lowerCasedTag !== s; a--)
                    ;
            else
                a = 0;
            if (a >= 0) {
                for (var u = o.length - 1; u >= a; u--)
                    e.end && e.end(o[u].tag, n, i);
                o.length = a,
                r = a && o[a - 1].tag
            } else
                "br" === s ? e.start && e.start(t, [], !0, n, i) : "p" === s && (e.start && e.start(t, [], !1, n, i),
                e.end && e.end(t, n, i))
        }
        f()
    }
    var Ja, qa, Wa, Za, Ga, Xa, Ya, Qa, ts = /^@|^v-on:/, es = /^v-|^@|^:|^#/, ns = /([\s\S]*?)\s+(?:in|of)\s+([\s\S]*)/, rs = /,([^,\}\]]*)(?:,([^,\}\]]*))?$/, os = /^\(|\)$/g, is = /^\[.*\]$/, as = /:(.*)$/, ss = /^:|^\.|^v-bind:/, cs = /\.[^.\]]+(?=[^\]]*$)/g, us = /^v-slot(:|$)|^#/, ls = /[\r\n]/, fs = /[ \f\t\r\n]+/g, ds = b(xa), ps = "_empty_";
    function vs(t, e, n) {
        return {
            type: 1,
            tag: t,
            attrsList: e,
            attrsMap: $s(e),
            rawAttrsMap: {},
            parent: n,
            children: []
        }
    }
    function hs(t, e) {
        Ja = e.warn || No,
        Xa = e.isPreTag || E,
        Ya = e.mustUseProp || E,
        Qa = e.getTagNamespace || E,
        e.isReservedTag,
        Wa = Po(e.modules, "transformNode"),
        Za = Po(e.modules, "preTransformNode"),
        Ga = Po(e.modules, "postTransformNode"),
        qa = e.delimiters;
        var n, r, o = [], i = !1 !== e.preserveWhitespace, a = e.whitespace, s = !1, c = !1;
        function u(t) {
            if (l(t),
            s || t.processed || (t = ms(t, e)),
            o.length || t === n || n.if && (t.elseif || t.else) && ys(n, {
                exp: t.elseif,
                block: t
            }),
            r && !t.forbidden)
                if (t.elseif || t.else)
                    a = t,
                    u = function(t) {
                        for (var e = t.length; e--; ) {
                            if (1 === t[e].type)
                                return t[e];
                            t.pop()
                        }
                    }(r.children),
                    u && u.if && ys(u, {
                        exp: a.elseif,
                        block: a
                    });
                else {
                    if (t.slotScope) {
                        var i = t.slotTarget || '"default"';
                        (r.scopedSlots || (r.scopedSlots = {}))[i] = t
                    }
                    r.children.push(t),
                    t.parent = r
                }
            var a, u;
            t.children = t.children.filter((function(t) {
                return !t.slotScope
            }
            )),
            l(t),
            t.pre && (s = !1),
            Xa(t.tag) && (c = !1);
            for (var f = 0; f < Ga.length; f++)
                Ga[f](t, e)
        }
        function l(t) {
            if (!c)
                for (var e = void 0; (e = t.children[t.children.length - 1]) && 3 === e.type && " " === e.text; )
                    t.children.pop()
        }
        return Ka(t, {
            warn: Ja,
            expectHTML: e.expectHTML,
            isUnaryTag: e.isUnaryTag,
            canBeLeftOpenTag: e.canBeLeftOpenTag,
            shouldDecodeNewlines: e.shouldDecodeNewlines,
            shouldDecodeNewlinesForHref: e.shouldDecodeNewlinesForHref,
            shouldKeepComment: e.comments,
            outputSourceRange: e.outputSourceRange,
            start: function(t, i, a, l, f) {
                var d = r && r.ns || Qa(t);
                W && "svg" === d && (i = function(t) {
                    for (var e = [], n = 0; n < t.length; n++) {
                        var r = t[n];
                        ws.test(r.name) || (r.name = r.name.replace(xs, ""),
                        e.push(r))
                    }
                    return e
                }(i));
                var p, v = vs(t, i, r);
                d && (v.ns = d),
                "style" !== (p = v).tag && ("script" !== p.tag || p.attrsMap.type && "text/javascript" !== p.attrsMap.type) || rt() || (v.forbidden = !0);
                for (var h = 0; h < Za.length; h++)
                    v = Za[h](v, e) || v;
                s || (!function(t) {
                    null != Bo(t, "v-pre") && (t.pre = !0)
                }(v),
                v.pre && (s = !0)),
                Xa(v.tag) && (c = !0),
                s ? function(t) {
                    var e = t.attrsList
                      , n = e.length;
                    if (n)
                        for (var r = t.attrs = new Array(n), o = 0; o < n; o++)
                            r[o] = {
                                name: e[o].name,
                                value: JSON.stringify(e[o].value)
                            },
                            null != e[o].start && (r[o].start = e[o].start,
                            r[o].end = e[o].end);
                    else
                        t.pre || (t.plain = !0)
                }(v) : v.processed || (gs(v),
                function(t) {
                    var e = Bo(t, "v-if");
                    if (e)
                        t.if = e,
                        ys(t, {
                            exp: e,
                            block: t
                        });
                    else {
                        null != Bo(t, "v-else") && (t.else = !0);
                        var n = Bo(t, "v-else-if");
                        n && (t.elseif = n)
                    }
                }(v),
                function(t) {
                    null != Bo(t, "v-once") && (t.once = !0)
                }(v)),
                n || (n = v),
                a ? u(v) : (r = v,
                o.push(v))
            },
            end: function(t, e, n) {
                var i = o[o.length - 1];
                o.length -= 1,
                r = o[o.length - 1],
                u(i)
            },
            chars: function(t, e, n) {
                if (r && (!W || "textarea" !== r.tag || r.attrsMap.placeholder !== t)) {
                    var o, u = r.children;
                    if (t = c || t.trim() ? "script" === (o = r).tag || "style" === o.tag ? t : ds(t) : u.length ? a ? "condense" === a && ls.test(t) ? "" : " " : i ? " " : "" : "") {
                        c || "condense" !== a || (t = t.replace(fs, " "));
                        var l = void 0
                          , f = void 0;
                        !s && " " !== t && (l = function(t, e) {
                            var n = e ? _a(e) : ga;
                            if (n.test(t)) {
                                for (var r, o, i, a = [], s = [], c = n.lastIndex = 0; r = n.exec(t); ) {
                                    (o = r.index) > c && (s.push(i = t.slice(c, o)),
                                    a.push(JSON.stringify(i)));
                                    var u = jo(r[1].trim());
                                    a.push("_s(".concat(u, ")")),
                                    s.push({
                                        "@binding": u
                                    }),
                                    c = o + r[0].length
                                }
                                return c < t.length && (s.push(i = t.slice(c)),
                                a.push(JSON.stringify(i))),
                                {
                                    expression: a.join("+"),
                                    tokens: s
                                }
                            }
                        }(t, qa)) ? f = {
                            type: 2,
                            expression: l.expression,
                            tokens: l.tokens,
                            text: t
                        } : " " === t && u.length && " " === u[u.length - 1].text || (f = {
                            type: 3,
                            text: t
                        }),
                        f && u.push(f)
                    }
                }
            },
            comment: function(t, e, n) {
                if (r) {
                    var o = {
                        type: 3,
                        text: t,
                        isComment: !0
                    };
                    r.children.push(o)
                }
            }
        }),
        n
    }
    function ms(t, e) {
        var n, r;
        (r = Ho(n = t, "key")) && (n.key = r),
        t.plain = !t.key && !t.scopedSlots && !t.attrsList.length,
        function(t) {
            var e = Ho(t, "ref");
            e && (t.ref = e,
            t.refInFor = function(t) {
                var e = t;
                for (; e; ) {
                    if (void 0 !== e.for)
                        return !0;
                    e = e.parent
                }
                return !1
            }(t))
        }(t),
        function(t) {
            var e;
            "template" === t.tag ? (e = Bo(t, "scope"),
            t.slotScope = e || Bo(t, "slot-scope")) : (e = Bo(t, "slot-scope")) && (t.slotScope = e);
            var n = Ho(t, "slot");
            n && (t.slotTarget = '""' === n ? '"default"' : n,
            t.slotTargetDynamic = !(!t.attrsMap[":slot"] && !t.attrsMap["v-bind:slot"]),
            "template" === t.tag || t.slotScope || Mo(t, "slot", n, function(t, e) {
                return t.rawAttrsMap[":" + e] || t.rawAttrsMap["v-bind:" + e] || t.rawAttrsMap[e]
            }(t, "slot")));
            if ("template" === t.tag) {
                if (a = Uo(t, us)) {
                    var r = _s(a)
                      , o = r.name
                      , i = r.dynamic;
                    t.slotTarget = o,
                    t.slotTargetDynamic = i,
                    t.slotScope = a.value || ps
                }
            } else {
                var a;
                if (a = Uo(t, us)) {
                    var s = t.scopedSlots || (t.scopedSlots = {})
                      , c = _s(a)
                      , u = c.name
                      , l = (i = c.dynamic,
                    s[u] = vs("template", [], t));
                    l.slotTarget = u,
                    l.slotTargetDynamic = i,
                    l.children = t.children.filter((function(t) {
                        if (!t.slotScope)
                            return t.parent = l,
                            !0
                    }
                    )),
                    l.slotScope = a.value || ps,
                    t.children = [],
                    t.plain = !1
                }
            }
        }(t),
        function(t) {
            "slot" === t.tag && (t.slotName = Ho(t, "name"))
        }(t),
        function(t) {
            var e;
            (e = Ho(t, "is")) && (t.component = e);
            null != Bo(t, "inline-template") && (t.inlineTemplate = !0)
        }(t);
        for (var o = 0; o < Wa.length; o++)
            t = Wa[o](t, e) || t;
        return function(t) {
            var e, n, r, o, i, a, s, c, u = t.attrsList;
            for (e = 0,
            n = u.length; e < n; e++)
                if (r = o = u[e].name,
                i = u[e].value,
                es.test(r))
                    if (t.hasBindings = !0,
                    (a = bs(r.replace(es, ""))) && (r = r.replace(cs, "")),
                    ss.test(r))
                        r = r.replace(ss, ""),
                        i = jo(i),
                        (c = is.test(r)) && (r = r.slice(1, -1)),
                        a && (a.prop && !c && "innerHtml" === (r = w(r)) && (r = "innerHTML"),
                        a.camel && !c && (r = w(r)),
                        a.sync && (s = Ko(i, "$event"),
                        c ? Fo(t, '"update:"+('.concat(r, ")"), s, null, !1, 0, u[e], !0) : (Fo(t, "update:".concat(w(r)), s, null, !1, 0, u[e]),
                        k(r) !== w(r) && Fo(t, "update:".concat(k(r)), s, null, !1, 0, u[e])))),
                        a && a.prop || !t.component && Ya(t.tag, t.attrsMap.type, r) ? Do(t, r, i, u[e], c) : Mo(t, r, i, u[e], c);
                    else if (ts.test(r))
                        r = r.replace(ts, ""),
                        (c = is.test(r)) && (r = r.slice(1, -1)),
                        Fo(t, r, i, a, !1, 0, u[e], c);
                    else {
                        var l = (r = r.replace(es, "")).match(as)
                          , f = l && l[1];
                        c = !1,
                        f && (r = r.slice(0, -(f.length + 1)),
                        is.test(f) && (f = f.slice(1, -1),
                        c = !0)),
                        Lo(t, r, o, i, f, c, a, u[e])
                    }
                else
                    Mo(t, r, JSON.stringify(i), u[e]),
                    !t.component && "muted" === r && Ya(t.tag, t.attrsMap.type, r) && Do(t, r, "true", u[e])
        }(t),
        t
    }
    function gs(t) {
        var e;
        if (e = Bo(t, "v-for")) {
            var n = function(t) {
                var e = t.match(ns);
                if (!e)
                    return;
                var n = {};
                n.for = e[2].trim();
                var r = e[1].trim().replace(os, "")
                  , o = r.match(rs);
                o ? (n.alias = r.replace(rs, "").trim(),
                n.iterator1 = o[1].trim(),
                o[2] && (n.iterator2 = o[2].trim())) : n.alias = r;
                return n
            }(e);
            n && T(t, n)
        }
    }
    function ys(t, e) {
        t.ifConditions || (t.ifConditions = []),
        t.ifConditions.push(e)
    }
    function _s(t) {
        var e = t.name.replace(us, "");
        return e || "#" !== t.name[0] && (e = "default"),
        is.test(e) ? {
            name: e.slice(1, -1),
            dynamic: !0
        } : {
            name: '"'.concat(e, '"'),
            dynamic: !1
        }
    }
    function bs(t) {
        var e = t.match(cs);
        if (e) {
            var n = {};
            return e.forEach((function(t) {
                n[t.slice(1)] = !0
            }
            )),
            n
        }
    }
    function $s(t) {
        for (var e = {}, n = 0, r = t.length; n < r; n++)
            e[t[n].name] = t[n].value;
        return e
    }
    var ws = /^xmlns:NS\d+/
      , xs = /^NS\d+:/;
    function Cs(t) {
        return vs(t.tag, t.attrsList.slice(), t.parent)
    }
    var ks = [ba, wa, {
        preTransformNode: function(t, e) {
            if ("input" === t.tag) {
                var n = t.attrsMap;
                if (!n["v-model"])
                    return;
                var r = void 0;
                if ((n[":type"] || n["v-bind:type"]) && (r = Ho(t, "type")),
                n.type || r || !n["v-bind"] || (r = "(".concat(n["v-bind"], ").type")),
                r) {
                    var o = Bo(t, "v-if", !0)
                      , i = o ? "&&(".concat(o, ")") : ""
                      , a = null != Bo(t, "v-else", !0)
                      , s = Bo(t, "v-else-if", !0)
                      , c = Cs(t);
                    gs(c),
                    Io(c, "type", "checkbox"),
                    ms(c, e),
                    c.processed = !0,
                    c.if = "(".concat(r, ")==='checkbox'") + i,
                    ys(c, {
                        exp: c.if,
                        block: c
                    });
                    var u = Cs(t);
                    Bo(u, "v-for", !0),
                    Io(u, "type", "radio"),
                    ms(u, e),
                    ys(c, {
                        exp: "(".concat(r, ")==='radio'") + i,
                        block: u
                    });
                    var l = Cs(t);
                    return Bo(l, "v-for", !0),
                    Io(l, ":type", r),
                    ms(l, e),
                    ys(c, {
                        exp: o,
                        block: l
                    }),
                    a ? c.else = !0 : s && (c.elseif = s),
                    c
                }
            }
        }
    }];
    var Ss, Os, Ts = {
        model: function(t, e, n) {
            var r = e.value
              , o = e.modifiers
              , i = t.tag
              , a = t.attrsMap.type;
            if (t.component)
                return Vo(t, r, o),
                !1;
            if ("select" === i)
                !function(t, e, n) {
                    var r = n && n.number
                      , o = 'Array.prototype.filter.call($event.target.options,function(o){return o.selected}).map(function(o){var val = "_value" in o ? o._value : o.value;' + "return ".concat(r ? "_n(val)" : "val", "})")
                      , i = "$event.target.multiple ? $$selectedVal : $$selectedVal[0]"
                      , a = "var $$selectedVal = ".concat(o, ";");
                    a = "".concat(a, " ").concat(Ko(e, i)),
                    Fo(t, "change", a, null, !0)
                }(t, r, o);
            else if ("input" === i && "checkbox" === a)
                !function(t, e, n) {
                    var r = n && n.number
                      , o = Ho(t, "value") || "null"
                      , i = Ho(t, "true-value") || "true"
                      , a = Ho(t, "false-value") || "false";
                    Do(t, "checked", "Array.isArray(".concat(e, ")") + "?_i(".concat(e, ",").concat(o, ")>-1") + ("true" === i ? ":(".concat(e, ")") : ":_q(".concat(e, ",").concat(i, ")"))),
                    Fo(t, "change", "var $$a=".concat(e, ",") + "$$el=$event.target," + "$$c=$$el.checked?(".concat(i, "):(").concat(a, ");") + "if(Array.isArray($$a)){" + "var $$v=".concat(r ? "_n(" + o + ")" : o, ",") + "$$i=_i($$a,$$v);" + "if($$el.checked){$$i<0&&(".concat(Ko(e, "$$a.concat([$$v])"), ")}") + "else{$$i>-1&&(".concat(Ko(e, "$$a.slice(0,$$i).concat($$a.slice($$i+1))"), ")}") + "}else{".concat(Ko(e, "$$c"), "}"), null, !0)
                }(t, r, o);
            else if ("input" === i && "radio" === a)
                !function(t, e, n) {
                    var r = n && n.number
                      , o = Ho(t, "value") || "null";
                    o = r ? "_n(".concat(o, ")") : o,
                    Do(t, "checked", "_q(".concat(e, ",").concat(o, ")")),
                    Fo(t, "change", Ko(e, o), null, !0)
                }(t, r, o);
            else if ("input" === i || "textarea" === i)
                !function(t, e, n) {
                    var r = t.attrsMap.type
                      , o = n || {}
                      , i = o.lazy
                      , a = o.number
                      , s = o.trim
                      , c = !i && "range" !== r
                      , u = i ? "change" : "range" === r ? Yo : "input"
                      , l = "$event.target.value";
                    s && (l = "$event.target.value.trim()");
                    a && (l = "_n(".concat(l, ")"));
                    var f = Ko(e, l);
                    c && (f = "if($event.target.composing)return;".concat(f));
                    Do(t, "value", "(".concat(e, ")")),
                    Fo(t, u, f, null, !0),
                    (s || a) && Fo(t, "blur", "$forceUpdate()")
                }(t, r, o);
            else if (!H.isReservedTag(i))
                return Vo(t, r, o),
                !1;
            return !0
        },
        text: function(t, e) {
            e.value && Do(t, "textContent", "_s(".concat(e.value, ")"), e)
        },
        html: function(t, e) {
            e.value && Do(t, "innerHTML", "_s(".concat(e.value, ")"), e)
        }
    }, As = {
        expectHTML: !0,
        modules: ks,
        directives: Ts,
        isPreTag: function(t) {
            return "pre" === t
        },
        isUnaryTag: Ca,
        mustUseProp: Mr,
        canBeLeftOpenTag: ka,
        isReservedTag: Gr,
        getTagNamespace: Xr,
        staticKeys: function(t) {
            return t.reduce((function(t, e) {
                return t.concat(e.staticKeys || [])
            }
            ), []).join(",")
        }(ks)
    }, js = b((function(t) {
        return v("type,tag,attrsList,attrsMap,plain,parent,children,attrs,start,end,rawAttrsMap" + (t ? "," + t : ""))
    }
    ));
    function Es(t, e) {
        t && (Ss = js(e.staticKeys || ""),
        Os = e.isReservedTag || E,
        Ns(t),
        Ps(t, !1))
    }
    function Ns(t) {
        if (t.static = function(t) {
            if (2 === t.type)
                return !1;
            if (3 === t.type)
                return !0;
            return !(!t.pre && (t.hasBindings || t.if || t.for || h(t.tag) || !Os(t.tag) || function(t) {
                for (; t.parent; ) {
                    if ("template" !== (t = t.parent).tag)
                        return !1;
                    if (t.for)
                        return !0
                }
                return !1
            }(t) || !Object.keys(t).every(Ss)))
        }(t),
        1 === t.type) {
            if (!Os(t.tag) && "slot" !== t.tag && null == t.attrsMap["inline-template"])
                return;
            for (var e = 0, n = t.children.length; e < n; e++) {
                var r = t.children[e];
                Ns(r),
                r.static || (t.static = !1)
            }
            if (t.ifConditions)
                for (e = 1,
                n = t.ifConditions.length; e < n; e++) {
                    var o = t.ifConditions[e].block;
                    Ns(o),
                    o.static || (t.static = !1)
                }
        }
    }
    function Ps(t, e) {
        if (1 === t.type) {
            if ((t.static || t.once) && (t.staticInFor = e),
            t.static && t.children.length && (1 !== t.children.length || 3 !== t.children[0].type))
                return void (t.staticRoot = !0);
            if (t.staticRoot = !1,
            t.children)
                for (var n = 0, r = t.children.length; n < r; n++)
                    Ps(t.children[n], e || !!t.for);
            if (t.ifConditions)
                for (n = 1,
                r = t.ifConditions.length; n < r; n++)
                    Ps(t.ifConditions[n].block, e)
        }
    }
    var Ds = /^([\w$_]+|\([^)]*?\))\s*=>|^function(?:\s+[\w$]+)?\s*\(/
      , Ms = /\([^)]*?\);*$/
      , Is = /^[A-Za-z_$][\w$]*(?:\.[A-Za-z_$][\w$]*|\['[^']*?']|\["[^"]*?"]|\[\d+]|\[[A-Za-z_$][\w$]*])*$/
      , Ls = {
        esc: 27,
        tab: 9,
        enter: 13,
        space: 32,
        up: 38,
        left: 37,
        right: 39,
        down: 40,
        delete: [8, 46]
    }
      , Rs = {
        esc: ["Esc", "Escape"],
        tab: "Tab",
        enter: "Enter",
        space: [" ", "Spacebar"],
        up: ["Up", "ArrowUp"],
        left: ["Left", "ArrowLeft"],
        right: ["Right", "ArrowRight"],
        down: ["Down", "ArrowDown"],
        delete: ["Backspace", "Delete", "Del"]
    }
      , Fs = function(t) {
        return "if(".concat(t, ")return null;")
    }
      , Hs = {
        stop: "$event.stopPropagation();",
        prevent: "$event.preventDefault();",
        self: Fs("$event.target !== $event.currentTarget"),
        ctrl: Fs("!$event.ctrlKey"),
        shift: Fs("!$event.shiftKey"),
        alt: Fs("!$event.altKey"),
        meta: Fs("!$event.metaKey"),
        left: Fs("'button' in $event && $event.button !== 0"),
        middle: Fs("'button' in $event && $event.button !== 1"),
        right: Fs("'button' in $event && $event.button !== 2")
    };
    function Bs(t, e) {
        var n = e ? "nativeOn:" : "on:"
          , r = ""
          , o = "";
        for (var i in t) {
            var a = Us(t[i]);
            t[i] && t[i].dynamic ? o += "".concat(i, ",").concat(a, ",") : r += '"'.concat(i, '":').concat(a, ",")
        }
        return r = "{".concat(r.slice(0, -1), "}"),
        o ? n + "_d(".concat(r, ",[").concat(o.slice(0, -1), "])") : n + r
    }
    function Us(t) {
        if (!t)
            return "function(){}";
        if (Array.isArray(t))
            return "[".concat(t.map((function(t) {
                return Us(t)
            }
            )).join(","), "]");
        var e = Is.test(t.value)
          , n = Ds.test(t.value)
          , r = Is.test(t.value.replace(Ms, ""));
        if (t.modifiers) {
            var o = ""
              , i = ""
              , a = []
              , s = function(e) {
                if (Hs[e])
                    i += Hs[e],
                    Ls[e] && a.push(e);
                else if ("exact" === e) {
                    var n = t.modifiers;
                    i += Fs(["ctrl", "shift", "alt", "meta"].filter((function(t) {
                        return !n[t]
                    }
                    )).map((function(t) {
                        return "$event.".concat(t, "Key")
                    }
                    )).join("||"))
                } else
                    a.push(e)
            };
            for (var c in t.modifiers)
                s(c);
            a.length && (o += function(t) {
                return "if(!$event.type.indexOf('key')&&" + "".concat(t.map(zs).join("&&"), ")return null;")
            }(a)),
            i && (o += i);
            var u = e ? "return ".concat(t.value, ".apply(null, arguments)") : n ? "return (".concat(t.value, ").apply(null, arguments)") : r ? "return ".concat(t.value) : t.value;
            return "function($event){".concat(o).concat(u, "}")
        }
        return e || n ? t.value : "function($event){".concat(r ? "return ".concat(t.value) : t.value, "}")
    }
    function zs(t) {
        var e = parseInt(t, 10);
        if (e)
            return "$event.keyCode!==".concat(e);
        var n = Ls[t]
          , r = Rs[t];
        return "_k($event.keyCode," + "".concat(JSON.stringify(t), ",") + "".concat(JSON.stringify(n), ",") + "$event.key," + "".concat(JSON.stringify(r)) + ")"
    }
    var Vs = {
        on: function(t, e) {
            t.wrapListeners = function(t) {
                return "_g(".concat(t, ",").concat(e.value, ")")
            }
        },
        bind: function(t, e) {
            t.wrapData = function(n) {
                return "_b(".concat(n, ",'").concat(t.tag, "',").concat(e.value, ",").concat(e.modifiers && e.modifiers.prop ? "true" : "false").concat(e.modifiers && e.modifiers.sync ? ",true" : "", ")")
            }
        },
        cloak: j
    }
      , Ks = function(t) {
        this.options = t,
        this.warn = t.warn || No,
        this.transforms = Po(t.modules, "transformCode"),
        this.dataGenFns = Po(t.modules, "genData"),
        this.directives = T(T({}, Vs), t.directives);
        var e = t.isReservedTag || E;
        this.maybeComponent = function(t) {
            return !!t.component || !e(t.tag)
        }
        ,
        this.onceId = 0,
        this.staticRenderFns = [],
        this.pre = !1
    };
    function Js(t, e) {
        var n = new Ks(e)
          , r = t ? "script" === t.tag ? "null" : qs(t, n) : '_c("div")';
        return {
            render: "with(this){return ".concat(r, "}"),
            staticRenderFns: n.staticRenderFns
        }
    }
    function qs(t, e) {
        if (t.parent && (t.pre = t.pre || t.parent.pre),
        t.staticRoot && !t.staticProcessed)
            return Ws(t, e);
        if (t.once && !t.onceProcessed)
            return Zs(t, e);
        if (t.for && !t.forProcessed)
            return Ys(t, e);
        if (t.if && !t.ifProcessed)
            return Gs(t, e);
        if ("template" !== t.tag || t.slotTarget || e.pre) {
            if ("slot" === t.tag)
                return function(t, e) {
                    var n = t.slotName || '"default"'
                      , r = nc(t, e)
                      , o = "_t(".concat(n).concat(r ? ",function(){return ".concat(r, "}") : "")
                      , i = t.attrs || t.dynamicAttrs ? ic((t.attrs || []).concat(t.dynamicAttrs || []).map((function(t) {
                        return {
                            name: w(t.name),
                            value: t.value,
                            dynamic: t.dynamic
                        }
                    }
                    ))) : null
                      , a = t.attrsMap["v-bind"];
                    !i && !a || r || (o += ",null");
                    i && (o += ",".concat(i));
                    a && (o += "".concat(i ? "" : ",null", ",").concat(a));
                    return o + ")"
                }(t, e);
            var n = void 0;
            if (t.component)
                n = function(t, e, n) {
                    var r = e.inlineTemplate ? null : nc(e, n, !0);
                    return "_c(".concat(t, ",").concat(Qs(e, n)).concat(r ? ",".concat(r) : "", ")")
                }(t.component, t, e);
            else {
                var r = void 0
                  , o = e.maybeComponent(t);
                (!t.plain || t.pre && o) && (r = Qs(t, e));
                var i = void 0
                  , a = e.options.bindings;
                o && a && !1 !== a.__isScriptSetup && (i = function(t, e) {
                    var n = w(e)
                      , r = x(n)
                      , o = function(o) {
                        return t[e] === o ? e : t[n] === o ? n : t[r] === o ? r : void 0
                    }
                      , i = o("setup-const") || o("setup-reactive-const");
                    if (i)
                        return i;
                    var a = o("setup-let") || o("setup-ref") || o("setup-maybe-ref");
                    if (a)
                        return a
                }(a, t.tag)),
                i || (i = "'".concat(t.tag, "'"));
                var s = t.inlineTemplate ? null : nc(t, e, !0);
                n = "_c(".concat(i).concat(r ? ",".concat(r) : "").concat(s ? ",".concat(s) : "", ")")
            }
            for (var c = 0; c < e.transforms.length; c++)
                n = e.transforms[c](t, n);
            return n
        }
        return nc(t, e) || "void 0"
    }
    function Ws(t, e) {
        t.staticProcessed = !0;
        var n = e.pre;
        return t.pre && (e.pre = t.pre),
        e.staticRenderFns.push("with(this){return ".concat(qs(t, e), "}")),
        e.pre = n,
        "_m(".concat(e.staticRenderFns.length - 1).concat(t.staticInFor ? ",true" : "", ")")
    }
    function Zs(t, e) {
        if (t.onceProcessed = !0,
        t.if && !t.ifProcessed)
            return Gs(t, e);
        if (t.staticInFor) {
            for (var n = "", r = t.parent; r; ) {
                if (r.for) {
                    n = r.key;
                    break
                }
                r = r.parent
            }
            return n ? "_o(".concat(qs(t, e), ",").concat(e.onceId++, ",").concat(n, ")") : qs(t, e)
        }
        return Ws(t, e)
    }
    function Gs(t, e, n, r) {
        return t.ifProcessed = !0,
        Xs(t.ifConditions.slice(), e, n, r)
    }
    function Xs(t, e, n, r) {
        if (!t.length)
            return r || "_e()";
        var o = t.shift();
        return o.exp ? "(".concat(o.exp, ")?").concat(i(o.block), ":").concat(Xs(t, e, n, r)) : "".concat(i(o.block));
        function i(t) {
            return n ? n(t, e) : t.once ? Zs(t, e) : qs(t, e)
        }
    }
    function Ys(t, e, n, r) {
        var o = t.for
          , i = t.alias
          , a = t.iterator1 ? ",".concat(t.iterator1) : ""
          , s = t.iterator2 ? ",".concat(t.iterator2) : "";
        return t.forProcessed = !0,
        "".concat(r || "_l", "((").concat(o, "),") + "function(".concat(i).concat(a).concat(s, "){") + "return ".concat((n || qs)(t, e)) + "})"
    }
    function Qs(t, e) {
        var n = "{"
          , r = function(t, e) {
            var n = t.directives;
            if (!n)
                return;
            var r, o, i, a, s = "directives:[", c = !1;
            for (r = 0,
            o = n.length; r < o; r++) {
                i = n[r],
                a = !0;
                var u = e.directives[i.name];
                u && (a = !!u(t, i, e.warn)),
                a && (c = !0,
                s += '{name:"'.concat(i.name, '",rawName:"').concat(i.rawName, '"').concat(i.value ? ",value:(".concat(i.value, "),expression:").concat(JSON.stringify(i.value)) : "").concat(i.arg ? ",arg:".concat(i.isDynamicArg ? i.arg : '"'.concat(i.arg, '"')) : "").concat(i.modifiers ? ",modifiers:".concat(JSON.stringify(i.modifiers)) : "", "},"))
            }
            if (c)
                return s.slice(0, -1) + "]"
        }(t, e);
        r && (n += r + ","),
        t.key && (n += "key:".concat(t.key, ",")),
        t.ref && (n += "ref:".concat(t.ref, ",")),
        t.refInFor && (n += "refInFor:true,"),
        t.pre && (n += "pre:true,"),
        t.component && (n += 'tag:"'.concat(t.tag, '",'));
        for (var o = 0; o < e.dataGenFns.length; o++)
            n += e.dataGenFns[o](t);
        if (t.attrs && (n += "attrs:".concat(ic(t.attrs), ",")),
        t.props && (n += "domProps:".concat(ic(t.props), ",")),
        t.events && (n += "".concat(Bs(t.events, !1), ",")),
        t.nativeEvents && (n += "".concat(Bs(t.nativeEvents, !0), ",")),
        t.slotTarget && !t.slotScope && (n += "slot:".concat(t.slotTarget, ",")),
        t.scopedSlots && (n += "".concat(function(t, e, n) {
            var r = t.for || Object.keys(e).some((function(t) {
                var n = e[t];
                return n.slotTargetDynamic || n.if || n.for || tc(n)
            }
            ))
              , o = !!t.if;
            if (!r)
                for (var i = t.parent; i; ) {
                    if (i.slotScope && i.slotScope !== ps || i.for) {
                        r = !0;
                        break
                    }
                    i.if && (o = !0),
                    i = i.parent
                }
            var a = Object.keys(e).map((function(t) {
                return ec(e[t], n)
            }
            )).join(",");
            return "scopedSlots:_u([".concat(a, "]").concat(r ? ",null,true" : "").concat(!r && o ? ",null,false,".concat(function(t) {
                var e = 5381
                  , n = t.length;
                for (; n; )
                    e = 33 * e ^ t.charCodeAt(--n);
                return e >>> 0
            }(a)) : "", ")")
        }(t, t.scopedSlots, e), ",")),
        t.model && (n += "model:{value:".concat(t.model.value, ",callback:").concat(t.model.callback, ",expression:").concat(t.model.expression, "},")),
        t.inlineTemplate) {
            var i = function(t, e) {
                var n = t.children[0];
                if (n && 1 === n.type) {
                    var r = Js(n, e.options);
                    return "inlineTemplate:{render:function(){".concat(r.render, "},staticRenderFns:[").concat(r.staticRenderFns.map((function(t) {
                        return "function(){".concat(t, "}")
                    }
                    )).join(","), "]}")
                }
            }(t, e);
            i && (n += "".concat(i, ","))
        }
        return n = n.replace(/,$/, "") + "}",
        t.dynamicAttrs && (n = "_b(".concat(n, ',"').concat(t.tag, '",').concat(ic(t.dynamicAttrs), ")")),
        t.wrapData && (n = t.wrapData(n)),
        t.wrapListeners && (n = t.wrapListeners(n)),
        n
    }
    function tc(t) {
        return 1 === t.type && ("slot" === t.tag || t.children.some(tc))
    }
    function ec(t, e) {
        var n = t.attrsMap["slot-scope"];
        if (t.if && !t.ifProcessed && !n)
            return Gs(t, e, ec, "null");
        if (t.for && !t.forProcessed)
            return Ys(t, e, ec);
        var r = t.slotScope === ps ? "" : String(t.slotScope)
          , o = "function(".concat(r, "){") + "return ".concat("template" === t.tag ? t.if && n ? "(".concat(t.if, ")?").concat(nc(t, e) || "undefined", ":undefined") : nc(t, e) || "undefined" : qs(t, e), "}")
          , i = r ? "" : ",proxy:true";
        return "{key:".concat(t.slotTarget || '"default"', ",fn:").concat(o).concat(i, "}")
    }
    function nc(t, e, n, r, o) {
        var i = t.children;
        if (i.length) {
            var a = i[0];
            if (1 === i.length && a.for && "template" !== a.tag && "slot" !== a.tag) {
                var s = n ? e.maybeComponent(a) ? ",1" : ",0" : "";
                return "".concat((r || qs)(a, e)).concat(s)
            }
            var c = n ? function(t, e) {
                for (var n = 0, r = 0; r < t.length; r++) {
                    var o = t[r];
                    if (1 === o.type) {
                        if (rc(o) || o.ifConditions && o.ifConditions.some((function(t) {
                            return rc(t.block)
                        }
                        ))) {
                            n = 2;
                            break
                        }
                        (e(o) || o.ifConditions && o.ifConditions.some((function(t) {
                            return e(t.block)
                        }
                        ))) && (n = 1)
                    }
                }
                return n
            }(i, e.maybeComponent) : 0
              , u = o || oc;
            return "[".concat(i.map((function(t) {
                return u(t, e)
            }
            )).join(","), "]").concat(c ? ",".concat(c) : "")
        }
    }
    function rc(t) {
        return void 0 !== t.for || "template" === t.tag || "slot" === t.tag
    }
    function oc(t, e) {
        return 1 === t.type ? qs(t, e) : 3 === t.type && t.isComment ? function(t) {
            return "_e(".concat(JSON.stringify(t.text), ")")
        }(t) : function(t) {
            return "_v(".concat(2 === t.type ? t.expression : ac(JSON.stringify(t.text)), ")")
        }(t)
    }
    function ic(t) {
        for (var e = "", n = "", r = 0; r < t.length; r++) {
            var o = t[r]
              , i = ac(o.value);
            o.dynamic ? n += "".concat(o.name, ",").concat(i, ",") : e += '"'.concat(o.name, '":').concat(i, ",")
        }
        return e = "{".concat(e.slice(0, -1), "}"),
        n ? "_d(".concat(e, ",[").concat(n.slice(0, -1), "])") : e
    }
    function ac(t) {
        return t.replace(/\u2028/g, "\\u2028").replace(/\u2029/g, "\\u2029")
    }
    function sc(t, e) {
        try {
            return new Function(t)
        } catch (n) {
            return e.push({
                err: n,
                code: t
            }),
            j
        }
    }
    function cc(t) {
        var e = Object.create(null);
        return function(n, r, o) {
            (r = T({}, r)).warn,
            delete r.warn;
            var i = r.delimiters ? String(r.delimiters) + n : n;
            if (e[i])
                return e[i];
            var a = t(n, r)
              , s = {}
              , c = [];
            return s.render = sc(a.render, c),
            s.staticRenderFns = a.staticRenderFns.map((function(t) {
                return sc(t, c)
            }
            )),
            e[i] = s
        }
    }
    new RegExp("\\b" + "do,if,for,let,new,try,var,case,else,with,await,break,catch,class,const,super,throw,while,yield,delete,export,import,return,switch,default,extends,finally,continue,debugger,function,arguments".split(",").join("\\b|\\b") + "\\b"),
    new RegExp("\\b" + "delete,typeof,void".split(",").join("\\s*\\([^\\)]*\\)|\\b") + "\\s*\\([^\\)]*\\)");
    var uc, lc, fc = (uc = function(t, e) {
        var n = hs(t.trim(), e);
        !1 !== e.optimize && Es(n, e);
        var r = Js(n, e);
        return {
            ast: n,
            render: r.render,
            staticRenderFns: r.staticRenderFns
        }
    }
    ,
    function(t) {
        function e(e, n) {
            var r = Object.create(t)
              , o = []
              , i = [];
            if (n)
                for (var a in n.modules && (r.modules = (t.modules || []).concat(n.modules)),
                n.directives && (r.directives = T(Object.create(t.directives || null), n.directives)),
                n)
                    "modules" !== a && "directives" !== a && (r[a] = n[a]);
            r.warn = function(t, e, n) {
                (n ? i : o).push(t)
            }
            ;
            var s = uc(e.trim(), r);
            return s.errors = o,
            s.tips = i,
            s
        }
        return {
            compile: e,
            compileToFunctions: cc(e)
        }
    }
    ), dc = fc(As).compileToFunctions;
    function pc(t) {
        return (lc = lc || document.createElement("div")).innerHTML = t ? '<a href="\n"/>' : '<div a="\n"/>',
        lc.innerHTML.indexOf("&#10;") > 0
    }
    var vc = !!J && pc(!1)
      , hc = !!J && pc(!0)
      , mc = b((function(t) {
        var e = to(t);
        return e && e.innerHTML
    }
    ))
      , gc = Cr.prototype.$mount;
    return Cr.prototype.$mount = function(t, e) {
        if ((t = t && to(t)) === document.body || t === document.documentElement)
            return this;
        var n = this.$options;
        if (!n.render) {
            var r = n.template;
            if (r)
                if ("string" == typeof r)
                    "#" === r.charAt(0) && (r = mc(r));
                else {
                    if (!r.nodeType)
                        return this;
                    r = r.innerHTML
                }
            else
                t && (r = function(t) {
                    if (t.outerHTML)
                        return t.outerHTML;
                    var e = document.createElement("div");
                    return e.appendChild(t.cloneNode(!0)),
                    e.innerHTML
                }(t));
            if (r) {
                var o = dc(r, {
                    outputSourceRange: !1,
                    shouldDecodeNewlines: vc,
                    shouldDecodeNewlinesForHref: hc,
                    delimiters: n.delimiters,
                    comments: n.comments
                }, this)
                  , i = o.render
                  , a = o.staticRenderFns;
                n.render = i,
                n.staticRenderFns = a
            }
        }
        return gc.call(this, t, e)
    }
    ,
    Cr.compile = dc,
    T(Cr, Fn),
    Cr.effect = function(t, e) {
        var n = new Vn(ct,t,j,{
            sync: !0
        });
        e && (n.update = function() {
            e((function() {
                return n.run()
            }
            ))
        }
        )
    }
    ,
    Cr
}
));

(function($) {
    "use strict";
    /**
	 * Layout module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Layout
	 * @extends Tc.Module
	 */
    Tc.Module.Layout = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            this.bindAll("_onResizeDebounced");
            this.$body = $("body");
            Utils.TcSandbox = this.sandbox;
            if (localStorage.basket && localStorage.basket !== window.location.href) {
                if (localStorage.orderForm) {
                    localStorage.removeItem("orderForm");
                }
                localStorage.removeItem("basket");
            }
            $('html').removeClass("no-js").addClass("js");
            $.fn.extend({
                scrollToMe: function() {
                    var offset = $(this).offset().top;
                    $('html, body').animate({
                        scrollTop: offset
                    }, 500);
                }
            });
            this._loadDatepicker();
            this._setResizeListener();
            callback();
            // !do not remove
        },

        after: function() {
            Tc.Utils.Lib.initGlossary();
            Tc.Utils.EqualHeight.addElements(this.$$('.js-equalheight'));

            //Initializes LazyLoading of images
            this.bLazy = new Blazy();
        },

        _loadDatepicker: function() {
            var $ctx = this.$ctx;
            Modernizr.load({
                test: Modernizr.inputtypes.date,
                nope: ['/etc/designs/core/frontend/assets/js/dynamic/libraries/bootstrap-datepicker.js', '/etc/designs/core/frontend/assets/js/dynamic/libraries/bootstrap-datepicker.languages.js', '/etc/designs/core/frontend/assets/css/dynamic/libraries/datepicker.css'],
                complete: function() {
                    if (!Modernizr.inputtypes.date) {
                        Tc.Utils.Lib.initDatepicker($ctx);
                    }
                }
            });
        },

        _setResizeListener: function() {
            var _this = this;
            $(window).on("resize viewportchange", $.debounce(200, _this._onResizeDebounced));
        },

        _onResizeDebounced: function() {
            if (!this.$body.hasClass('cq-wcm-edit')) {
                Tc.Utils.EqualHeight.set();
            }
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Accordion module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Accordion
	 * @extends Tc.Module
	 */
    Tc.Module.Accordion = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this
              , autoCollapse = $('> div', _this.$ctx).data('auto-collapse')
              , firstElementOpen = $('> div', _this.$ctx).data('first-element-open')
              , $collapsedElmt = $("a.collapsed", _this.$ctx);

            if (firstElementOpen) {
                $collapsedElmt.first().click();
            }

            if (autoCollapse) {
                $collapsedElmt.unbind('click').on('click', function(e) {
                    var evt = $(e.target);

                    if (!evt.hasClass('active')) {
                        _this.$ctx.find('a.active').click();
                    }
                });
            }

            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;

        }

    });
}(Tc.$));

(function($) {

    var spinnerIcon = '<span><i class="fa fa-spinner fa-spin" style="display: inline-block;"></i></span>';

    function initializeComponentHooks(aramis) {
        var aramisId = aramis.attr('id');
        var aramisForm = aramis.find('form.m-aramis');
        var aramisDetailsView = aramis.find('.js-aramis-detail-view');

        if (aramisForm.length) {
            aramisForm.submit(function() {
                var formData = aramisForm.serialize();
                var isFormChanged = sessionStorage.getItem(aramisId) !== formData;
                if (isFormChanged) {
                    var submitButton = aramisForm.find('[type="submit"]');
                    submitButton.append(spinnerIcon);
                }
                sessionStorage.setItem(aramisId, formData);
            });
        }
        if (aramisDetailsView.length) {
            var aramisBackLink = aramis.find('.js-aramis-details-back-link');
            if (aramisBackLink.length) {
                var href = aramisBackLink.attr('href');
                aramisId = href.substring(href.indexOf("#") + 1);
                var originalRequest = sessionStorage.getItem(aramisId) || '';
                aramisBackLink.attr('href', href.replace("#".concat(aramisId), "?".concat(originalRequest, "#").concat(aramisId)));
                updateLanguageButtons();
            }
        }
    }

    function init() {
        var aramisComponent = $('.mod-aramis');
        if (aramisComponent.length) {
            initializeComponentHooks(aramisComponent);
        }
    }

    init();
}($));

(function($) {
    "use strict";
    /**
	 * Database module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author ewessolek <elena.wessolek@namics.com>
	 * @author sbaur <stefan.baur@namics.com>
	 * @namespace Tc.Module
	 * @class Database
	 * @extends Tc.Module
	 */
    Tc.Module.Database = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            var _this = this;

            _this.initDatabaseFilter();

            callback();
            // !do not remove
        },

        initDatabaseFilter: function() {
            var _this = this
              , $ctx = _this.$ctx;

            _this.$form = $ctx.find('.js-database-form');

            // form submit
            _this.$form.on('submit', function(e) {
                e.preventDefault();
                _this.submitForm();
            });
        },

        initDetails: function() {
            var _this = this
              , $ctx = _this.$ctx
              , $result = $ctx.find('.js-database-result')
              , $detailLink = $result.find('a.database-entry');

            // show details
            $detailLink.on('click', function(e) {
                e.preventDefault();
                var target = $(e.currentTarget)
                  , dataUrl = target.attr('href');

                _this.loadDetails(dataUrl, $result);
            });
        },

        loadDetails: function(dataUrl, $result) {
            var _this = this
              , $ctx = _this.$ctx;

            $result.addClass('loading');

            $.ajax({
                type: 'GET',
                url: dataUrl,
                success: function(data) {
                    $result.html(data).removeClass('loading');

                    // go back to database
                    $ctx.on('click', '.js-database-link', function() {
                        _this.submitForm();
                    });
                }
            });
        },

        submitForm: function() {
            var _this = this
              , attributes = _this.$form.length !== null ? 'pageIndex=' + 0 + '&' + _this.$form.serialize() : 'pageIndex=' + 0;

            this.fire('loadContent', {
                attributes: attributes
            });
        },

        onContentDone: function() {
            var _this = this;

            _this.initDetails();
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
     * Deleteresource module implementation.
     *
     * @author sbaur <stefan.baur@namics.com>
     * @namespace Tc.Module
     * @class Deleteresource
     * @extends Tc.Module
     */
    Tc.Module.Deleteresource = Tc.Module.extend({

        on: function(callback) {
            var $this = this
              , path = this.$ctx.data('delete-path');
            this.$resultContainer = this.$ctx.find('.result').find('.alert');

            $(window).load(function() {
                window.setTimeout(function() {
                    if (path) {
                        $this.askForDeletion(path);
                    } else {
                        $this.showResult('alert-danger', 'core.pages.deleteresource.message.notfound');
                    }
                }, 500);
            });
            callback();
            // !do not remove
        },

        askForDeletion: function(path) {
            var $this = this;
            CQ.Ext.Msg.show({
                "title": CQ.I18n.getMessage("core.pages.deleteresource.title"),
                "msg": CQ.I18n.getMessage("core.pages.deleteresource.message.confirmdeletion"),
                "buttons": CQ.Ext.Msg.YESNO,
                "icon": CQ.Ext.MessageBox.QUESTION,
                "fn": function(btnId) {
                    if (btnId == "yes") {
                        $this.sendDeleteRequest(path)
                    } else if (btnId == "no") {
                        $this.showResult('alert-warning', 'core.pages.deleteresource.message.cancel');
                    }
                },
                "scope": this
            });
        },

        sendDeleteRequest: function(path) {
            var $this = this;
            var data = {
                "path": path,
                "_charset_": "utf-8",
                "cmd": "deletePage",
                "force": false
            };
            $.ajax({
                url: "/bin/wcmcommand",
                type: "POST",
                dataType: "html",
                data: data
            }).done(function(data, textStatus, jqXHR) {
                var message = $this.getMessage(data);
                if (message.indexOf('null') == 0) {
                    $this.showResult('alert-danger', 'core.pages.deleteresource.message.error', 'core.pages.deleteresource.message.notfound');
                    console.log(data);
                } else {
                    $this.showResult('alert-success', 'core.pages.deleteresource.message.success', message);
                }
            }).fail(function(jqXHR, textStatus, errorThrown) {
                var message = $this.getMessage(jqXHR.responseText);
                $this.showResult('alert-danger', 'core.pages.deleteresource.message.error', message);
                console.log(jqXHR.responseText);
            });
        },

        getMessage: function(result) {
            return $(result).find('#Message').html();
        },

        showResult: function(type, message, details) {
            var $this = this
              , i18nMessage = '<p><strong>' + CQ.I18n.getMessage(message) + '</strong></p>'
              , detailsMessage = '';
            if (details) {
                detailsMessage = '<p>' + CQ.I18n.getMessage(details) + '</p>'
            }
            $this.$resultContainer.removeClass().addClass('alert').addClass(type).html(i18nMessage + detailsMessage);
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Contactbox module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Contactbox
	 * @extends Tc.Module
	 */
    Tc.Module.Contactbox = Tc.Module.extend({
        on: function(callback) {
            this.bindAll('onClickPrint');
            this.$$('.js-print-contact').on('click', this.onClickPrint);
            callback();
            // !do not remove
        },

        onClickPrint: function(ev) {
            ev.preventDefault();

            function printMarkup(data) {
                var windowContent = '<html><head><title>Kontakt</title>' + '<link rel="stylesheet" href="/etc/designs/core/frontend/guidelines.min.css" type="text/css" />' + '</head><body class="container container-main" style="padding: 30px;">' + data + '</body></html>';

                return windowContent;
            }

            function Popup(data) {
                if (navigator.userAgent.search(/Windows/) > 0) {
                    var printWin = window.open('', '', 'height=800,width=1000');
                    printWin.document.open();
                    printWin.document.write(printMarkup(data));
                    printWin.document.close();
                    printWin.focus();
                    setTimeout(function() {
                        printWin.print();
                        printWin.close();
                    }, 500);
                } else {
                    var mywindow = window.open('', '', 'height=800,width=1000');
                    mywindow.document.write('<html><head><title>Kontakt</title>');
                    mywindow.document.write('<link rel="stylesheet" href="/etc/designs/core/frontend/guidelines.min.css" type="text/css" />');
                    mywindow.document.write('</head><body class="container container-main" style="padding: 30px;">');
                    mywindow.document.write(data);
                    mywindow.document.write('</body></html>');
                    setTimeout(function() {
                        mywindow.print();
                        mywindow.close();
                    }, 500);
                }
                return true;
            }

            var printContent = this.$$('#contact').html();
            var $contactMap = this.$$("#map");
            if ($contactMap.length) {
                var mapTitleHtml = $contactMap.find("h3").html() || "";
                var $mapImage = $contactMap.find("img").closest('p');
                var mapImageHtml = $mapImage.length ? $mapImage.get(0).outerHTML : "";
                printContent = printContent + mapTitleHtml + mapImageHtml;
            }

            new Popup(printContent);
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
     * Backtooverview
     * https://gitlab.namics.com/snippets/2
     *
     * @author  <>
     * @namespace Tc.Module
     * @class Backtooverview
     * @extends Tc.Module
     */
    Tc.Module.Backtooverview = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this;

            _this.setReferrerToBackButton();

            callback();
            // !do not remove
        },

        setReferrerToBackButton: function() {

            var _this = this
              , $ctx = _this.$ctx
              , backLink = $ctx.find('a')
              , hostPath = location.protocol + '//' + location.host
              , backLinkPath = backLink.eq(0).attr('href')
              , newBackPath = document.referrer.replace(hostPath, '');

            if (document.referrer.indexOf(backLinkPath) > -1) {
                backLink.each(function(i, v) {
                    var newHref = backLinkPath + newBackPath.replace(backLinkPath, '').replace('/content?', '?');
                    $(v).attr('href', newHref);
                });

            }
        },

        after: function() {
            var _this = this;
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Tabs module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Tabs
	 * @extends Tc.Module
	 */
    Tc.Module.Tabs = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            var _this = this;

            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;

            //check if cq editmode class is available
            if ($("body").hasClass("skin-layout-editmode")) {//_this._initEditModeTabs();
            }

        },
        _initEditModeTabs: function() {
            var _this = this;
            $(".tabs-list a", _this.$ctx).first().addClass("current");
            $(".tabbody", _this.$ctx).first().addClass("current");
            $(".tabs-list a", _this.$ctx).click(function(event) {

                var contentId = $(this).attr("href");
                // remove all current classes from tabbody
                $(".tabbody", _this.$ctx).each(function() {
                    $(this).removeClass("current");
                });
                // add current class toactive tab
                $(contentId, _this.$ctx).next(".tabbody").addClass("current");
            });
            //Trigger click on first tab navigation link
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
     * Tabs module implementation.
     * https://gitlab.namics.com/snippets/2
     *
     * @author  <>
     * @namespace Tc.Module
     * @class Tabs
     * @extends Tc.Module
     */
    Tc.Module.DynamicTabs = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            var _this = this;

            callback();
            // !do not remove
        },

        after: function() {
            this._init()
            if (this.$ctx.data("initialload") !== 0) {
                this._selectFirst();
            }

        },

        onLoadTabs: function() {
            var _this = this;
            _this._selectFirst();
        },

        _init: function() {
            var _this = this
              , tabParam = this._getTabParam()
              , selectedTabId = this._getTabId(tabParam);

            $(".nav-tabs a, .collapsify a", _this.$ctx).click(function(event) {

                var tabId = $(this).data("tabid")
                  , dataStr = _this._getParamString(tabId);

                _this.fire("initialLoadContent", {
                    attributes: dataStr
                }, [tabId]);
            });

        },

        _selectFirst: function() {
            this.fire('unloadContent');
            var _this = this
              , tabParam = this._getTabParam()
              , selectedTabId = this._getTabId(tabParam);

            //Trigger click on first tab navigation link
            setTimeout(function() {
                $(".nav-tabs a[data-tabid='" + selectedTabId + "']", _this.$ctx).click();
            }, 10);
            this.$ctx.removeClass('hidden');
        },

        _getParamString: function(tabParam) {
            var queryParams = Tc.Utils.WCM.getParsedQueryString()
              , params = [];

            tabParam = tabParam.replace("dynamictab", "") || "";
            queryParams.dyn_tab = tabParam;
            for (var p in queryParams) {
                params.push(p.replace("dyn_", "") + "=" + queryParams[p]);
            }
            params = params.join("&") || "";
            return params;
        },

        _getTabId: function(tabParam) {
            if (tabParam && !isNaN(tabParam)) {
                return "dynamictab" + tabParam;
            } else {
                return "dynamictab0";
            }
        },

        _getTabParam: function() {
            var params = Tc.Utils.WCM.getParsedQueryString();
            return params.dyn_tab;
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Themes module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Themes
	 * @extends Tc.Module
	 */
    Tc.Module.Themes = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this;
            callback();
            // !do not remove

            $(".tabs-list a", _this.$ctx).click(function(event) {
                $('.enhanced_select_scroll.is_open').removeClass('is_open').hide().click();
            });

            // Trigger tab when themes select field open
            $(".yamm--select").on("show.bs.dropdown", function() {
                $('li:not(.disabled) a[data-toggle="tab"]:first', _this.$ctx).tab('show').trigger('show.bs.tab');
            });
            _this._initTabEvents();
            _this._initSelectEvents();

        },

        after: function() {
            var _this = this;
        },
        _initTabEvents: function() {
            var _this = this;

            $('a[data-toggle="tab"]', _this.$ctx).unbind('show.bs.tab').on('show.bs.tab', function(e) {
                var url = $(e.target).data("url")
                  , tabId = $(e.target).attr("href");
                url = url || "";

                if ($("body").hasClass("skin-layout-author")) {
                    url += url.indexOf('?') == -1 ? "?wcmmode=disabled" : "&wcmmode=disabled";
                }

                //console.log("e.target: ", e.target); // newly activated tab
                //console.log("e.relatedTarget: ", e.relatedTarget); // previous active tab

                //check if content is already loaded
                if ($(tabId).children().length === 0) {
                    var modules = [];
                    $(tabId).addClass("loading");
                    $.ajax(url).done(function(data) {
                        //alert( "success" );
                        $(tabId).html(data);
                        modules = _this.sandbox.addModules($(tabId));
                        _this._initTabEvents();
                        _this._initSelectEvents();

                    }).fail(function() {//alert( "error" );
                    }).always(function() {
                        //alert( "complete" );
                        $(tabId).removeClass("loading");
                    });
                }
            });
        },
        _initSelectEvents: function() {
            var _this = this
              , $select = $("select:not(.no-chosen)", _this.$ctx);
            $select.chosen({
                disable_search_threshold: 10
            });
            $select.unbind('change').change(function() {
                var url = $("option:selected", this).val();
                url = url || "";
                if ($("body").hasClass("skin-layout-author")) {
                    url += url.indexOf('?') == -1 ? "?wcmmode=disabled" : "&wcmmode=disabled";
                }
                var modules = [];
                var $dContent = $(".tab-pane:visible", _this.$ctx).find(".dynamic-content");
                $dContent.addClass("loading");
                $.ajax(url).done(function(data) {
                    //alert( "success" );
                    $dContent.html(data);
                    modules = _this.sandbox.addModules($dContent);
                }).fail(function() {//alert( "error" );
                }).always(function() {
                    //alert( "complete" );
                    $dContent.removeClass("loading");
                });
            });

        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Form module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Form
	 * @extends Tc.Module
	 */
    Tc.Module.Form = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this;
            _this.bindAll("_formatErrors");

            _this._checkForErrors();
            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;
        },

        _checkForErrors: function() {
            var _this = this
              , $errors = this.$$('.form_error');
            if ($errors.length) {
                $errors.each(_this._formatErrors);
            }
        },

        _formatErrors: function(i, el) {
            var _this = this;

            $(el).closest(".form-group").addClass("has-warning");
            if (i === 0) {
                _this._focusError(el)
            }
        },

        _focusError: function(el) {
            $(el).closest(".form-group").find('input,select').focus();
        }

    });
}(Tc.$));

(function($) {
    'use strict';
    /**
	 * Teaserfocus module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author Ilario Engler <ilario.engler@namics.com>
	 * @namespace Tc.Module
	 * @class Teaserfocus
	 * @extends Tc.Module
	 */
    Tc.Module.Teaserfocus = Tc.Module.extend({

        selectors: {
            tabPane: '.tab-pane.js-equalheight-element',
            tabContainer: '.tab-content'
        },

        on: function(callback) {
            this.$ctx.imagesLoaded(this.setHeight.bind(this));

            callback();
            // !do not remove
        },

        setHeight: function() {
            var that = this;
            var max = 0;

            $(this.selectors.tabPane, this.$ctx).each(function(index, item) {
                var itemHeight = parseInt(getComputedStyle(item).getPropertyValue('height'), 10);

                if (max < itemHeight) {
                    max = itemHeight;
                }
            });
            this.$ctx.find(that.selectors.tabContainer).height(max);
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Mainnavigation module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Mainnavigation
	 * @extends Tc.Module
	 */
    Tc.Module.Mainnavigation = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this;

            //load flyout content a bit after the page was loaded
            setTimeout(function() {
                _this.loadFlyoutContent();
            }, 100);

            callback();
            // !do not remove
        },

        after: function() {
        },
        /**
		 * on page load, trigger ajax requests to fetch the flyout content and prepare the html structure
		 * on click, initialize the modules through the sandbox
		 */
        loadFlyoutContent: function() {
            var _this = this;
            $("li.dropdown", this.$ctx).each(function() {
                var $li = $(this)
                  , $a = $li.children("a.dropdown-toggle").first()
                  , dataUrl = $a.data("url") || ""
                  , $container = $("ul.dropdown-menu > li > div.yamm-content > div.row > div.main-column", $li);

                if (dataUrl && $container.children().length === 0) {
                    var $fluidContainer = _this.$ctx.find('.container-fluid')
                      , loadingClass = 'loading'
                      , initializedClass = 'initialized';

                    if ($("body").hasClass("skin-layout-author")) {
                        dataUrl += dataUrl.indexOf('?') == -1 ? "?wcmmode=disabled" : "&wcmmode=disabled";
                    }

                    $fluidContainer.addClass(loadingClass);

                    $.ajax({
                        type: "GET",
                        url: dataUrl,
                        success: function(data) {
                            //store ajax content into the correct position
                            $container.html(data);

                            //wait for click to trigger modules sandbox init
                            $li.on('click', function() {
                                if (!$container.hasClass(initializedClass)) {
                                    $container.addClass(initializedClass);

                                    //wait for the menu to open, then init modules
                                    setTimeout(function() {
                                        $fluidContainer.removeClass(loadingClass);
                                        _this.sandbox.addModules($container);

                                    }, 0);

                                }
                            });

                        },
                        error: function() {
                            $fluidContainer.removeClass(loadingClass);
                        }
                    });
                }
            });
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * FormWebcode module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class FormWebcode
	 * @extends Tc.Module
	 */
    Tc.Module.FormWebcode = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            this.bindAll("onFormSubmit");
            this.$$("form").on("submit", this.onFormSubmit);
            callback();
            // !do not remove
        },

        after: function() {
        },

        onFormSubmit: function(ev) {
            var _this = this
              , $body = $("body")
              , $form = $(ev.currentTarget)
              , formData = $form.serialize()
              , formError = $form.data("error")
              , url = $form.attr("action") + "?" + formData;
            ev.preventDefault();
            _this.showError("", $form);
            $.ajax({
                url: url + ($body.is(".cq-wcm-edit") || $body.is(".cq-wcm-preview") ? "&wcmmode=disabled" : ""),
                type: 'GET',
                beforeSend: function(xhr) {
                    xhr.overrideMimeType('text/plain; charset=UTF-8');
                },
                success: function() {
                    $("p.error", _this.$ctx).remove();
                    window.location = url;
                },
                error: function() {
                    _this.showError(formError, $form);
                }
            });
        },

        showError: function(formError, $form) {
            var _this = this;

            if ($("p.error", _this.$ctx).length !== 0) {
                $("p.error", _this.$ctx).html(formError);
            } else {
                $form.after("<p class='error'>" + formError + "</p>");
            }
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * OneLineForm module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author sbartels <sebastian.bartels@namics.com>
	 * @contributors tmay <thomas.may@namics.com>
	 * @namespace Tc.Module
	 * @class OneLineForm
	 * @extends Tc.Module
	 */
    Tc.Module.OneLineForm = Tc.Module.extend({
        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            this.bindAll("onSubmitForm", "onChangeQuantity");
            this.initForm();
            callback();
            // !do not remove
        },
        initForm: function() {
            if ($("html").hasClass("no-localstorage")) {
                this.$$(".js-form-group").html(this.template(this.$$("#onelineform-error-template").html(), {
                    "error": "no-cookie"
                }));
            } else {
                this.$quantity = this.$$(".js-quantity");
                this.sandbox.subscribe("Basket", this);
                this.$quantity.val(this.$quantity.attr("min"));
                this.$quantity.on("change", this.onChangeQuantity).trigger("change");
                this.$$(".js-one-line-form").on("submit", this.onSubmitForm);
            }
        },
        onSubmitForm: function(ev) {
            ev.preventDefault();
            this.amountValidation();
            if (!$(ev.currentTarget).is(":disabled")) {
                this.saveDataStorage();
                $.toast({
                    text: this.getCartToastMessage() + this.amount + 'x ' + this.$$("input[name='productTitle']").val(),
                    showHideTransition: 'slide',
                    hideAfter: 5000,
                    position: 'bottom-right',
                    textColor: '#3c763d',
                    loaderBg: "#3c763d",
                    loader: true,
                    icon: 'success',
                    class: 'alert alert-success'
                })
                this.fire("ShowDataStorage");
            }
        },
        getCartToastMessage: function() {
            var defaultMessage = 'The item has been added to the shopping cart:';
            var i18nMessage = Granite.I18n.get("core.components.content.wsdetails.cart.toast.message");
            if (i18nMessage) {
                return i18nMessage;
            } else {
                return defaultMessage;
            }
        },
        onChangeQuantity: function(ev) {
            this.amount = parseInt($(ev.currentTarget).val());
            this.amountValidation();
        },
        amountValidation: function() {
            var minAmount = parseInt(this.$quantity.attr("min"), 10)
              , maxAmount = parseInt(this.$quantity.attr("max"), 10);

            if (this.amount < minAmount && this.amount < maxAmount) {
                this.$$(".js-error-container").html(this.template(this.$$("#onelineform-error-template").html(), {
                    "error": "min"
                }));
                this.$$("#submit").prop("disabled", true);
            } else if (this.amount > maxAmount) {
                this.$$(".js-error-container").html(this.template(this.$$("#onelineform-error-template").html(), {
                    "error": "max"
                }));
                this.$$("#submit").prop("disabled", true);
            } else {
                this.$$(".js-error-container").empty();
                this.$$("#submit").prop("disabled", false);
            }
        },
        saveDataStorage: function() {
            // Find Product Text for the Storage and add to a Array
            var product = this.$$("input[name='productTitle']").val(), productIntern = this.$$("input[name='productId']").val(), orderForm = JSON.parse(localStorage.getItem("orderForm")) || [], isProcuctInList = false, i;

            for (i = 0; i < orderForm.length; i++) {
                // Check if current product already is in order list
                if (orderForm[i].productId === productIntern) {
                    orderForm[i].quantity = parseInt(orderForm[i].quantity) + parseInt(this.amount);
                    isProcuctInList = true;
                    break;
                }
            }
            // Add the product as new item if it is not in list
            if (!isProcuctInList) {
                orderForm.push({
                    "id": (orderForm.length + 1),
                    "productTitle": product,
                    "productId": productIntern,
                    quantity: this.amount
                });
            }

            // Read Localstorage with key orderForm
            localStorage.orderForm = JSON.stringify(orderForm);
        },
        after: function() {}
    });
}(Tc.$));

var Relaunch = Relaunch || {}
Relaunch.onlinepayment = Relaunch.onlinepayment || {}

Relaunch.onlinepayment.SETTINGS = {
    'logoUrl': 'https://www.ejpd.admin.ch/etc/clientlibs/relaunch/resources/styleguide/build/img/logo-CH.svg',
    'enableLog': false,
    'enableTrace': false,
    'resetElementFocus': true
};

Relaunch.onlinepayment.doPayment = function(endpointUrl, data, shopId, logoUrl, enableThreadMetrix) {
    $.extend(Relaunch.onlinepayment.SETTINGS, {
        'logoUrl': logoUrl,
        'enableThreadMetrix': enableThreadMetrix
    });

    var onlinePayment = new OnlinePayment(shopId,endpointUrl,this.SETTINGS);

    onlinePayment.payInOverlay(data).then(Relaunch.onlinepayment.finishPayment).catch(function(reason) {
        Relaunch.onlinepayment.setFormResolved();
        if (Relaunch.onlinepayment.SETTINGS.enableLog) {
            console.log('Promise rejected: ' + JSON.stringify(reason));
        }
    });
}
;

Relaunch.onlinepayment.finishPayment = function(response) {
    if (Relaunch.onlinepayment.SETTINGS.enableLog) {
        Relaunch.onlinepayment.printErrorLog(response);
    }

    switch (response.errorCode) {
    case 0:
        // Everything is ok, payment done. No error happened.
        Relaunch.onlinepayment.setPaymentComplete();
        Relaunch.onlinepayment.continueFillingForm();
        break;
    case 12:
        // Everything is ok, user just aborted payment process.
        Relaunch.onlinepayment.cancelProcess();
        Relaunch.onlinepayment.continueFillingForm();
        break;
    default:
        // All others are system errors. Always needed for analyzing.
        Relaunch.onlinepayment.printErrorLog(response);
        Relaunch.onlinepayment.systemError();
    }
    $('.alert').get(0).scrollIntoView();
}
;

Relaunch.onlinepayment.printErrorLog = function(response) {
    console.log('Onlinepayment promise resolved');
    console.log('The errorCode:          ' + response.errorCode);
    console.log('The base64 encoded XML: ' + response.data);
    console.log('The decoded XML: ' + atob(response.data));
    console.log('The errorMessage:       ' + response.errorMessage);
    console.log('FullResult:             ' + JSON.stringify(response));
    Relaunch.onlinepayment.errorCodeMessage(response.errorCode);
}
;

Relaunch.onlinepayment.errorCodeMessage = function(errorCode) {
    if (errorCode === 55) {
        console.log('Onlinepayment error 55: Redirect URLs not defined in OSGi configuration.');
    }
}
;

Relaunch.onlinepayment.cancelProcess = function() {
    Relaunch.onlinepayment.setFormResolved();
    $(".mod-onlinepayment").append(Relaunch.onlinepayment.specialBox('danger', Relaunch.onlinepayment.translations.transactionAbortedTitle, Relaunch.onlinepayment.translations.transactionAbortedContent));
}
;

Relaunch.onlinepayment.systemError = function() {
    Relaunch.onlinepayment.setFormResolved();
    $(".mod-onlinepayment").append(Relaunch.onlinepayment.specialBox('danger', Relaunch.onlinepayment.translations.transactionErrorTitle, Relaunch.onlinepayment.translations.transactionErrorContent));
}
;

//type success:warning:danger:crysis
Relaunch.onlinepayment.specialBox = function(type, title, content) {
    return '<div class="specialbox section"><div class="alert alert-' + type + ' clearfix" role="alert"><div class="col-sm-3"><span class="title">' + title + '</span></div><div class="col-sm-9">' + content + '</div></div></div>';
}
;

Relaunch.onlinepayment.payNow = function() {
    var getTime = new Date().getTime();
    $.get(Relaunch.onlinepayment.paymentData.nodePath + '.orderid.html?t=' + getTime, function(orderId) {
        $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " [name=" + Relaunch.onlinepayment.associatedfields.associatedOrderIdField + "]").val(orderId);
        $.get(Relaunch.onlinepayment.paymentData.nodePath + '.data.html?orderId=' + $.trim(orderId), function(xml) {
            xml = $.trim(xml);
            var encodedData = btoa(xml);

            if (Relaunch.onlinepayment.SETTINGS.enableLog) {
                console.log('OrderID: ' + orderId);
                console.log('XML data: ' + xml);
                console.log('Base64 encoded data: ' + encodedData);
            }

            Relaunch.onlinepayment.doPayment(Relaunch.onlinepayment.paymentData.endpointUrl, encodedData, Relaunch.onlinepayment.paymentData.shopId, Relaunch.onlinepayment.paymentData.logoUrl, Relaunch.onlinepayment.paymentData.enableThreatMetrix);
        });
    });
}
;

/**
 * Used when purchase was cancelled or payment was done.
 */
Relaunch.onlinepayment.continueFillingForm = function() {
    if (!Relaunch.onlinepayment.isPaymentComplete()) {
        // Payment aborted.
        $('#' + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier).trigger('formFieldChangedEvent', null);
        return;
    }
    Relaunch.onlinepayment.viewPaymentDone();
    $(".mod-onlinepayment").append(Relaunch.onlinepayment.specialBox('success', Relaunch.onlinepayment.translations.transactionSuccessTitle, Relaunch.onlinepayment.translations.transactionSuccessContent));
}
;

/**
 * Default view when no purchase was done previously and/or the user has opened the page the first time.
 */
Relaunch.onlinepayment.viewFormEditing = function() {
    // Enable Form elements
    $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " input").prop("disabled", false);

    // Disable Payment button
    var formId = 'onlinepayment-form-' + Relaunch.onlinepayment.associatedfields.formId;
    $('#' + formId + ' .opcheckout').prop('disabled', true);

    // Disable Submit button
    $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " input[type=submit]").prop('disabled', true);
}

/**
 * The view when the payment was done. Now activate the form submit button.
 */
Relaunch.onlinepayment.viewPaymentDone = function() {
    // Disable Payment button
    var formId = 'onlinepayment-form-' + Relaunch.onlinepayment.associatedfields.formId;
    $('#' + formId + ' .opcheckout').prop('disabled', true);

    // Enable Submit button
    $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " input[type=submit]").prop('disabled', false);
}

Relaunch.onlinepayment.setPaymentComplete = function() {
    localStorage = window.localStorage;
    localStorage.setItem("onlinepayment", $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " [name=" + Relaunch.onlinepayment.associatedfields.associatedOrderIdField + "]").val());
    localStorage.setItem("onlinepaymentDate", "" + new Date().getTime())
}

Relaunch.onlinepayment.getPaymentComplete = function() {
    localStorage = window.localStorage;

    //localstorage is only valid for 24 hours
    now = new Date().getTime();
    onlinePaymentDate = localStorage.getItem("onlinepaymentDate");
    if (onlinePaymentDate == null || now - onlinePaymentDate > 24 * 60 * 60 * 1000) {
        //if the payment is more than 24hours in the past, ignore it
        Relaunch.onlinepayment.setFormResolved();
    }
    return localStorage.getItem("onlinepayment");
}

Relaunch.onlinepayment.isPaymentComplete = function() {
    localStorage = window.localStorage;
    return Relaunch.onlinepayment.getPaymentComplete() != null;
}

Relaunch.onlinepayment.setFormResolved = function() {
    localStorage = window.localStorage;
    localStorage.removeItem("onlinepayment");
    localStorage.removeItem("onlinepaymentDate");
}

/**
 * Attach on each form input field an event dispatcher to detect when a field was changed by the user.
 */
Relaunch.onlinepayment.addFormFieldsValidator = function() {
    Relaunch.onlinepayment.associatedfields.fedpolInputFields.each(function(index, element) {
        // Add missing required field, needed for browser validation feature. Thanks to Foundation framework for not adding it.
        element.required = true;

        if (element.type === "text" || element.type === "number") {
            if (element.name.includes("email")) {
                // Email field detected, add special browser validation for it. Thanks to Foundation framework for not adding it.
                element.type = "email";
            }

            element.addEventListener('keyup', function() {
                $('#' + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier).trigger('formFieldChangedEvent', [element]);
            });
        } else if (element.type === "file") {
            element.addEventListener('change', function() {
                $('#' + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier).trigger('formFieldChangedEvent', [element]);
            });
        }
    })
}

/**
 * Catch event formFieldChangedEvent which is fired everytime a form input field has changed.
 * Enable/disable payment button.
 */
Relaunch.onlinepayment.registerFormFieldChangedListener = function() {
    let formId = 'onlinepayment-form-' + Relaunch.onlinepayment.associatedfields.formId;
    $('#' + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier).on('formFieldChangedEvent', function() {
        if ('true' === Relaunch.onlinepayment.checkFormFieldsValid().toString() && !Relaunch.onlinepayment.isPaymentComplete()) {
            // All fields are valid and there was no previous purchase. Show the purchase button.
            $('#' + formId + ' .opcheckout').prop('disabled', false);
        } else {
            // All evil, don't show purchase button.
            $('#' + formId + ' .opcheckout').prop('disabled', true);
        }
    });
}

/**
 * Check that all form input fields are valid.
 * @returns {boolean}
 */
Relaunch.onlinepayment.checkFormFieldsValid = function() {
    let result = true;
    Relaunch.onlinepayment.associatedfields.fedpolInputFields.each(function(index, element) {
        if (element.checkValidity().toString() === 'false') {
            result = false;
            // One wrong field is enough to indicate that the form is not valid.
            return false;
        }
    })
    return result;
}

Relaunch.onlinepayment.init = function() {
    var fedpolFields = $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " input");
    var fedpolOrderId = $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " [name=" + Relaunch.onlinepayment.associatedfields.associatedOrderIdField + "]");
    Relaunch.onlinepayment.associatedfields.fedpolInputFields = $("#" + Relaunch.onlinepayment.associatedfields.associatedFormIdentifier + " input.form-control");

    if (fedpolFields.length > 0 || fedpolOrderId.length > 0) {
        Relaunch.onlinepayment.registerFormFieldChangedListener();
        Relaunch.onlinepayment.addFormFieldsValidator();

        //Enable Form on page load if payment is not done
        if (Relaunch.onlinepayment.isPaymentComplete()) {
            fedpolOrderId.val(Relaunch.onlinepayment.getPaymentComplete());
            Relaunch.onlinepayment.viewPaymentDone();
        } else {
            Relaunch.onlinepayment.viewFormEditing();
        }
    } else {
        var alerts = [];
        fedpolFields.length > 0 || alerts.push("could not find the form with the configured identifier");
        fedpolOrderId.length > 0 || alerts.push("could not find the form field oderId");
        fedpolOrderId.attr("type") === "hidden" || alerts.push("oderId field should be hidden");

        $(".mod-onlinepayment").append(Relaunch.onlinepayment.specialBox('danger', "Configuration error", alerts.join("<br>")));
        $('.alert').get(0).scrollIntoView();
    }
}

//add polyfill for IE11 and below, docs see: https://polyfill.io/
if (window._usePolyfill_io === undefined) {
    var isIE = !!window.MSInputMethodContext && !!document.documentMode;
    window._usePolyfill_io = isIE;
}
if (window._usePolyfill_io) {
    var x = document.createElement('script');
    x.src = 'https://cdn.polyfill.io/v2/polyfill.min.js';
    document.getElementsByTagName("head")[0].appendChild(x);
}
//source: https://github.com/taylorhakes/promise-polyfill
!function(e) {
    function n() {}
    function t(e, n) {
        return function() {
            e.apply(n, arguments)
        }
    }
    function o(e) {
        if ("object" != typeof this)
            throw new TypeError("Promises must be constructed via new");
        if ("function" != typeof e)
            throw new TypeError("not a function");
        this._state = 0,
        this._handled = !1,
        this._value = void 0,
        this._deferreds = [],
        s(e, this)
    }
    function i(e, n) {
        for (; 3 === e._state; )
            e = e._value;
        return 0 === e._state ? void e._deferreds.push(n) : (e._handled = !0,
        void o._immediateFn(function() {
            var t = 1 === e._state ? n.onFulfilled : n.onRejected;
            if (null === t)
                return void (1 === e._state ? r : u)(n.promise, e._value);
            var o;
            try {
                o = t(e._value)
            } catch (i) {
                return void u(n.promise, i)
            }
            r(n.promise, o)
        }))
    }
    function r(e, n) {
        try {
            if (n === e)
                throw new TypeError("A promise cannot be resolved with itself.");
            if (n && ("object" == typeof n || "function" == typeof n)) {
                var i = n.then;
                if (n instanceof o)
                    return e._state = 3,
                    e._value = n,
                    void f(e);
                if ("function" == typeof i)
                    return void s(t(i, n), e)
            }
            e._state = 1,
            e._value = n,
            f(e)
        } catch (r) {
            u(e, r)
        }
    }
    function u(e, n) {
        e._state = 2,
        e._value = n,
        f(e)
    }
    function f(e) {
        2 === e._state && 0 === e._deferreds.length && o._immediateFn(function() {
            e._handled || o._unhandledRejectionFn(e._value)
        });
        for (var n = 0, t = e._deferreds.length; n < t; n++)
            i(e, e._deferreds[n]);
        e._deferreds = null
    }
    function c(e, n, t) {
        this.onFulfilled = "function" == typeof e ? e : null,
        this.onRejected = "function" == typeof n ? n : null,
        this.promise = t
    }
    function s(e, n) {
        var t = !1;
        try {
            e(function(e) {
                t || (t = !0,
                r(n, e))
            }, function(e) {
                t || (t = !0,
                u(n, e))
            })
        } catch (o) {
            if (t)
                return;
            t = !0,
            u(n, o)
        }
    }
    var a = setTimeout;
    o.prototype["catch"] = function(e) {
        return this.then(null, e)
    }
    ,
    o.prototype.then = function(e, t) {
        var o = new this.constructor(n);
        return i(this, new c(e,t,o)),
        o
    }
    ,
    o.all = function(e) {
        var n = Array.prototype.slice.call(e);
        return new o(function(e, t) {
            function o(r, u) {
                try {
                    if (u && ("object" == typeof u || "function" == typeof u)) {
                        var f = u.then;
                        if ("function" == typeof f)
                            return void f.call(u, function(e) {
                                o(r, e)
                            }, t)
                    }
                    n[r] = u,
                    0 === --i && e(n)
                } catch (c) {
                    t(c)
                }
            }
            if (0 === n.length)
                return e([]);
            for (var i = n.length, r = 0; r < n.length; r++)
                o(r, n[r])
        }
        )
    }
    ,
    o.resolve = function(e) {
        return e && "object" == typeof e && e.constructor === o ? e : new o(function(n) {
            n(e)
        }
        )
    }
    ,
    o.reject = function(e) {
        return new o(function(n, t) {
            t(e)
        }
        )
    }
    ,
    o.race = function(e) {
        return new o(function(n, t) {
            for (var o = 0, i = e.length; o < i; o++)
                e[o].then(n, t)
        }
        )
    }
    ,
    o._immediateFn = "function" == typeof setImmediate && function(e) {
        setImmediate(e)
    }
    || function(e) {
        a(e, 0)
    }
    ,
    o._unhandledRejectionFn = function(e) {
        "undefined" != typeof console && console && console.warn("Possible Unhandled Promise Rejection:", e)
    }
    ,
    o._setImmediateFn = function(e) {
        o._immediateFn = e
    }
    ,
    o._setUnhandledRejectionFn = function(e) {
        o._unhandledRejectionFn = e
    }
    ,
    "undefined" != typeof module && module.exports ? module.exports = o : e.Promise || (e.Promise = o)
}(this);
//https://github.com/WICG/inert/blob/
(function(global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory() : typeof define === 'function' && define.amd ? define(factory) : (factory());
}(this, (function() {
    'use strict';
    //does not work in ie11 because of Symbol.interator is not defined
    var isIE = !!window.MSInputMethodContext && !!document.documentMode;
    if (isIE) {
        return;
    }

    /**
 * Determine if a DOM element matches a CSS selector
 *
 * @param {Element} elem
 * @param {String} selector
 * @return {Boolean}
 * @api public
 */

    function matches(elem, selector) {
        // Vendor-specific implementations of `Element.prototype.matches()`.
        var proto = window.Element.prototype;
        var nativeMatches = proto.matches || proto.mozMatchesSelector || proto.msMatchesSelector || proto.oMatchesSelector || proto.webkitMatchesSelector;

        if (!elem || elem.nodeType !== 1) {
            return false;
        }

        var parentElem = elem.parentNode;

        // use native 'matches'
        if (nativeMatches) {
            return nativeMatches.call(elem, selector);
        }

        // native support for `matches` is missing and a fallback is required
        var nodes = parentElem.querySelectorAll(selector);
        var len = nodes.length;

        for (var i = 0; i < len; i++) {
            if (nodes[i] === elem) {
                return true;
            }
        }

        return false;
    }

    /**
 * Expose `matches`
 */

    var index = matches;

    var classCallCheck = function(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
            throw new TypeError("Cannot call a class as a function");
        }
    };

    var createClass = function() {
        function defineProperties(target, props) {
            for (var i = 0; i < props.length; i++) {
                var descriptor = props[i];
                descriptor.enumerable = descriptor.enumerable || false;
                descriptor.configurable = true;
                if ("value"in descriptor)
                    descriptor.writable = true;
                Object.defineProperty(target, descriptor.key, descriptor);
            }
        }

        return function(Constructor, protoProps, staticProps) {
            if (protoProps)
                defineProperties(Constructor.prototype, protoProps);
            if (staticProps)
                defineProperties(Constructor, staticProps);
            return Constructor;
        }
        ;
    }();

    /**
 * This work is licensed under the W3C Software and Document License
 * (http://www.w3.org/Consortium/Legal/2015/copyright-software-and-document).
 */

    (function(document) {
        // Convenience function for converting NodeLists.
        /** @type {function(number,number):Array} */
        var slice = Array.prototype.slice;

        /** @type {string} */
        var _focusableElementsString = ['a[href]', 'area[href]', 'input:not([disabled])', 'select:not([disabled])', 'textarea:not([disabled])', 'button:not([disabled])', 'iframe', 'object', 'embed', '[contenteditable]'].join(',');

        /**
         * `InertRoot` manages a single inert subtree, i.e. a DOM subtree whose root element has an `inert`
         * attribute.
         *
         * Its main functions are:
         *
         * - to create and maintain a set of managed `InertNode`s, including when mutations occur in the
         *   subtree. The `makeSubtreeUnfocusable()` method handles collecting `InertNode`s via registering
         *   each focusable node in the subtree with the singleton `InertManager` which manages all known
         *   focusable nodes within inert subtrees. `InertManager` ensures that a single `InertNode`
         *   instance exists for each focusable node which has at least one inert root as an ancestor.
         *
         * - to notify all managed `InertNode`s when this subtree stops being inert (i.e. when the `inert`
         *   attribute is removed from the root node). This is handled in the destructor, which calls the
         *   `deregister` method on `InertManager` for each managed inert node.
         */

        var InertRoot = function() {
            /**
             * @param {Element} rootElement The Element at the root of the inert subtree.
             * @param {InertManager} inertManager The global singleton InertManager object.
             */
            function InertRoot(rootElement, inertManager) {
                classCallCheck(this, InertRoot);

                /** @type {InertManager} */
                this._inertManager = inertManager;

                /** @type {Element} */
                this._rootElement = rootElement;

                /**
                 * @type {Set<Node>}
                 * All managed focusable nodes in this InertRoot's subtree.
                 */
                this._managedNodes = new Set([]);

                // Make the subtree hidden from assistive technology
                if (this._rootElement.hasAttribute('aria-hidden')) {
                    this._savedAriaHidden = this._rootElement.getAttribute('aria-hidden');
                }
                this._rootElement.setAttribute('aria-hidden', 'true');

                // Make all focusable elements in the subtree unfocusable and add them to _managedNodes
                this._makeSubtreeUnfocusable(this._rootElement);

                // Watch for:
                // - any additions in the subtree: make them unfocusable too
                // - any removals from the subtree: remove them from this inert root's managed nodes
                // - attribute changes: if `tabindex` is added, or removed from an intrinsically focusable
                //   element, make that node a managed node.
                this._observer = new MutationObserver(this._onMutation.bind(this));
                this._observer.observe(this._rootElement, {
                    attributes: true,
                    childList: true,
                    subtree: true
                });
            }

            /**
             * Call this whenever this object is about to become obsolete.  This unwinds all of the state
             * stored in this object and updates the state of all of the managed nodes.
             */

            createClass(InertRoot, [{
                key: 'destructor',
                value: function destructor() {
                    this._observer.disconnect();
                    this._observer = null;

                    if (this._rootElement) {
                        if (this.hasSavedAriaHidden) {
                            this._rootElement.setAttribute('aria-hidden', this.savedAriaHidden);
                        } else {
                            this._rootElement.removeAttribute('aria-hidden');
                        }
                    }
                    this._rootElement = null;

                    var _iteratorNormalCompletion = true;
                    var _didIteratorError = false;
                    var _iteratorError = undefined;

                    try {
                        for (var _iterator = this._managedNodes[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
                            var inertNode = _step.value;

                            this._unmanageNode(inertNode.node);
                        }
                    } catch (err) {
                        _didIteratorError = true;
                        _iteratorError = err;
                    } finally {
                        try {
                            if (!_iteratorNormalCompletion && _iterator.return) {
                                _iterator.return();
                            }
                        } finally {
                            if (_didIteratorError) {
                                throw _iteratorError;
                            }
                        }
                    }

                    this._managedNodes = null;

                    this._inertManager = null;
                }
                /**
                 * @return {Set<InertNode>} A copy of this InertRoot's managed nodes set.
                 */

            }, {
                key: '_makeSubtreeUnfocusable',

                /**
                 * @param {Node} startNode
                 */
                value: function _makeSubtreeUnfocusable(startNode) {
                    var _this = this;

                    composedTreeWalk(startNode, function(node) {
                        return _this._visitNode(node);
                    });

                    var activeElement = document.activeElement;
                    if (!contains(document.body, startNode)) {
                        // startNode may be in shadow DOM, so find its nearest shadowRoot to get the activeElement.
                        var node = startNode;
                        var root = undefined;
                        while (node) {
                            if (node.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
                                root = node;
                                break;
                            }
                            node = node.parentNode;
                        }
                        if (root) {
                            activeElement = root.activeElement;
                        }
                    }
                    if (contains(startNode, activeElement)) {
                        activeElement.blur();
                    }
                }
                /**
                 * @param {Node} node
                 */

            }, {
                key: '_visitNode',
                value: function _visitNode(node) {
                    if (node.nodeType !== Node.ELEMENT_NODE) {
                        return;
                    }

                    // If a descendant inert root becomes un-inert, its descendants will still be inert because of
                    // this inert root, so all of its managed nodes need to be adopted by this InertRoot.
                    if (node !== this._rootElement && node.hasAttribute('inert')) {
                        this._adoptInertRoot(node);
                    }

                    if (index(node, _focusableElementsString) || node.hasAttribute('tabindex')) {
                        this._manageNode(node);
                    }
                }
                /**
                 * Register the given node with this InertRoot and with InertManager.
                 * @param {Node} node
                 */

            }, {
                key: '_manageNode',
                value: function _manageNode(node) {
                    var inertNode = this._inertManager.register(node, this);
                    this._managedNodes.add(inertNode);
                }
                /**
                 * Unregister the given node with this InertRoot and with InertManager.
                 * @param {Node} node
                 */

            }, {
                key: '_unmanageNode',
                value: function _unmanageNode(node) {
                    var inertNode = this._inertManager.deregister(node, this);
                    if (inertNode) {
                        this._managedNodes.delete(inertNode);
                    }
                }
                /**
                 * Unregister the entire subtree starting at `startNode`.
                 * @param {Node} startNode
                 */

            }, {
                key: '_unmanageSubtree',
                value: function _unmanageSubtree(startNode) {
                    var _this2 = this;

                    composedTreeWalk(startNode, function(node) {
                        return _this2._unmanageNode(node);
                    });
                }
                /**
                 * If a descendant node is found with an `inert` attribute, adopt its managed nodes.
                 * @param {Node} node
                 */

            }, {
                key: '_adoptInertRoot',
                value: function _adoptInertRoot(node) {
                    var inertSubroot = this._inertManager.getInertRoot(node);

                    // During initialisation this inert root may not have been registered yet,
                    // so register it now if need be.
                    if (!inertSubroot) {
                        this._inertManager.setInert(node, true);
                        inertSubroot = this._inertManager.getInertRoot(node);
                    }

                    var _iteratorNormalCompletion2 = true;
                    var _didIteratorError2 = false;
                    var _iteratorError2 = undefined;

                    try {
                        for (var _iterator2 = inertSubroot.managedNodes[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
                            var savedInertNode = _step2.value;

                            this._manageNode(savedInertNode.node);
                        }
                    } catch (err) {
                        _didIteratorError2 = true;
                        _iteratorError2 = err;
                    } finally {
                        try {
                            if (!_iteratorNormalCompletion2 && _iterator2.return) {
                                _iterator2.return();
                            }
                        } finally {
                            if (_didIteratorError2) {
                                throw _iteratorError2;
                            }
                        }
                    }
                }
                /**
                 * Callback used when mutation observer detects subtree additions, removals, or attribute changes.
                 * @param {MutationRecord} records
                 * @param {MutationObserver} self
                 */

            }, {
                key: '_onMutation',
                value: function _onMutation(records, self) {
                    var _iteratorNormalCompletion3 = true;
                    var _didIteratorError3 = false;
                    var _iteratorError3 = undefined;

                    try {
                        for (var _iterator3 = records[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
                            var record = _step3.value;

                            var target = record.target;
                            if (record.type === 'childList') {
                                // Manage added nodes
                                var _iteratorNormalCompletion4 = true;
                                var _didIteratorError4 = false;
                                var _iteratorError4 = undefined;

                                try {
                                    for (var _iterator4 = slice.call(record.addedNodes)[Symbol.iterator](), _step4; !(_iteratorNormalCompletion4 = (_step4 = _iterator4.next()).done); _iteratorNormalCompletion4 = true) {
                                        var node = _step4.value;

                                        this._makeSubtreeUnfocusable(node);
                                    }

                                    // Un-manage removed nodes
                                } catch (err) {
                                    _didIteratorError4 = true;
                                    _iteratorError4 = err;
                                } finally {
                                    try {
                                        if (!_iteratorNormalCompletion4 && _iterator4.return) {
                                            _iterator4.return();
                                        }
                                    } finally {
                                        if (_didIteratorError4) {
                                            throw _iteratorError4;
                                        }
                                    }
                                }

                                var _iteratorNormalCompletion5 = true;
                                var _didIteratorError5 = false;
                                var _iteratorError5 = undefined;

                                try {
                                    for (var _iterator5 = slice.call(record.removedNodes)[Symbol.iterator](), _step5; !(_iteratorNormalCompletion5 = (_step5 = _iterator5.next()).done); _iteratorNormalCompletion5 = true) {
                                        var _node = _step5.value;

                                        this._unmanageSubtree(_node);
                                    }
                                } catch (err) {
                                    _didIteratorError5 = true;
                                    _iteratorError5 = err;
                                } finally {
                                    try {
                                        if (!_iteratorNormalCompletion5 && _iterator5.return) {
                                            _iterator5.return();
                                        }
                                    } finally {
                                        if (_didIteratorError5) {
                                            throw _iteratorError5;
                                        }
                                    }
                                }
                            } else if (record.type === 'attributes') {
                                if (record.attributeName === 'tabindex') {
                                    // Re-initialise inert node if tabindex changes
                                    this._manageNode(target);
                                } else if (target !== this._rootElement && record.attributeName === 'inert' && target.hasAttribute('inert')) {
                                    // If a new inert root is added, adopt its managed nodes and make sure it knows about the
                                    // already managed nodes from this inert subroot.
                                    this._adoptInertRoot(target);
                                    var inertSubroot = this._inertManager.getInertRoot(target);
                                    var _iteratorNormalCompletion6 = true;
                                    var _didIteratorError6 = false;
                                    var _iteratorError6 = undefined;

                                    try {
                                        for (var _iterator6 = this._managedNodes[Symbol.iterator](), _step6; !(_iteratorNormalCompletion6 = (_step6 = _iterator6.next()).done); _iteratorNormalCompletion6 = true) {
                                            var managedNode = _step6.value;

                                            if (contains(target, managedNode.node)) {
                                                inertSubroot._manageNode(managedNode.node);
                                            }
                                        }
                                    } catch (err) {
                                        _didIteratorError6 = true;
                                        _iteratorError6 = err;
                                    } finally {
                                        try {
                                            if (!_iteratorNormalCompletion6 && _iterator6.return) {
                                                _iterator6.return();
                                            }
                                        } finally {
                                            if (_didIteratorError6) {
                                                throw _iteratorError6;
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    } catch (err) {
                        _didIteratorError3 = true;
                        _iteratorError3 = err;
                    } finally {
                        try {
                            if (!_iteratorNormalCompletion3 && _iterator3.return) {
                                _iterator3.return();
                            }
                        } finally {
                            if (_didIteratorError3) {
                                throw _iteratorError3;
                            }
                        }
                    }
                }
            }, {
                key: 'managedNodes',
                get: function get$$1() {
                    return new Set(this._managedNodes);
                }
                /** @return {boolean} */

            }, {
                key: 'hasSavedAriaHidden',
                get: function get$$1() {
                    return '_savedAriaHidden'in this;
                }
                /** @param {string} ariaHidden */

            }, {
                key: 'savedAriaHidden',
                set: function set$$1(ariaHidden) {
                    this._savedAriaHidden = ariaHidden;
                }
                /** @return {string} */
                ,
                get: function get$$1() {
                    return this._savedAriaHidden;
                }
            }]);
            return InertRoot;
        }();

        /**
         * `InertNode` initialises and manages a single inert node.
         * A node is inert if it is a descendant of one or more inert root elements.
         *
         * On construction, `InertNode` saves the existing `tabindex` value for the node, if any, and
         * either removes the `tabindex` attribute or sets it to `-1`, depending on whether the element
         * is intrinsically focusable or not.
         *
         * `InertNode` maintains a set of `InertRoot`s which are descendants of this `InertNode`. When an
         * `InertRoot` is destroyed, and calls `InertManager.deregister()`, the `InertManager` notifies the
         * `InertNode` via `removeInertRoot()`, which in turn destroys the `InertNode` if no `InertRoot`s
         * remain in the set. On destruction, `InertNode` reinstates the stored `tabindex` if one exists,
         * or removes the `tabindex` attribute if the element is intrinsically focusable.
         */

        var InertNode = function() {
            /**
             * @param {Node} node A focusable element to be made inert.
             * @param {InertRoot} inertRoot The inert root element associated with this inert node.
             */
            function InertNode(node, inertRoot) {
                classCallCheck(this, InertNode);

                /** @type {Node} */
                this._node = node;

                /** @type {boolean} */
                this._overrodeFocusMethod = false;

                /**
                 * @type {Set<InertRoot>} The set of descendant inert roots.
                 *    If and only if this set becomes empty, this node is no longer inert.
                 */
                this._inertRoots = new Set([inertRoot]);

                /** @type {boolean} */
                this._destroyed = false;

                // Save any prior tabindex info and make this node untabbable
                this.ensureUntabbable();
            }

            /**
             * Call this whenever this object is about to become obsolete.
             * This makes the managed node focusable again and deletes all of the previously stored state.
             */

            createClass(InertNode, [{
                key: 'destructor',
                value: function destructor() {
                    this._throwIfDestroyed();

                    if (this._node) {
                        if (this.hasSavedTabIndex) {
                            this._node.setAttribute('tabindex', this.savedTabIndex);
                        } else {
                            this._node.removeAttribute('tabindex');
                        }

                        // Use `delete` to restore native focus method.
                        if (this._overrodeFocusMethod) {
                            delete this._node.focus;
                        }
                    }
                    this._node = null;
                    this._inertRoots = null;

                    this._destroyed = true;
                }
                /**
                 * @type {boolean} Whether this object is obsolete because the managed node is no longer inert.
                 * If the object has been destroyed, any attempt to access it will cause an exception.
                 */

            }, {
                key: '_throwIfDestroyed',

                /**
                 * Throw if user tries to access destroyed InertNode.
                 */
                value: function _throwIfDestroyed() {
                    if (this.destroyed) {
                        throw new Error('Trying to access destroyed InertNode');
                    }
                }
                /** @return {boolean} */

            }, {
                key: 'ensureUntabbable',

                /** Save the existing tabindex value and make the node untabbable and unfocusable */
                value: function ensureUntabbable() {
                    var node = this.node;
                    if (index(node, _focusableElementsString)) {
                        if (node.tabIndex === -1 && this.hasSavedTabIndex) {
                            return;
                        }

                        if (node.hasAttribute('tabindex')) {
                            this._savedTabIndex = node.tabIndex;
                        }
                        node.setAttribute('tabindex', '-1');
                        if (node.nodeType === Node.ELEMENT_NODE) {
                            node.focus = function() {}
                            ;
                            this._overrodeFocusMethod = true;
                        }
                    } else if (node.hasAttribute('tabindex')) {
                        this._savedTabIndex = node.tabIndex;
                        node.removeAttribute('tabindex');
                    }
                }
                /**
                 * Add another inert root to this inert node's set of managing inert roots.
                 * @param {InertRoot} inertRoot
                 */

            }, {
                key: 'addInertRoot',
                value: function addInertRoot(inertRoot) {
                    this._throwIfDestroyed();
                    this._inertRoots.add(inertRoot);
                }
                /**
                 * Remove the given inert root from this inert node's set of managing inert roots.
                 * If the set of managing inert roots becomes empty, this node is no longer inert,
                 * so the object should be destroyed.
                 * @param {InertRoot} inertRoot
                 */

            }, {
                key: 'removeInertRoot',
                value: function removeInertRoot(inertRoot) {
                    this._throwIfDestroyed();
                    this._inertRoots.delete(inertRoot);
                    if (this._inertRoots.size === 0) {
                        this.destructor();
                    }
                }
            }, {
                key: 'destroyed',
                get: function get$$1() {
                    return this._destroyed;
                }
            }, {
                key: 'hasSavedTabIndex',
                get: function get$$1() {
                    return '_savedTabIndex'in this;
                }
                /** @return {Node} */

            }, {
                key: 'node',
                get: function get$$1() {
                    this._throwIfDestroyed();
                    return this._node;
                }
                /** @param {number} tabIndex */

            }, {
                key: 'savedTabIndex',
                set: function set$$1(tabIndex) {
                    this._throwIfDestroyed();
                    this._savedTabIndex = tabIndex;
                }
                /** @return {number} */
                ,
                get: function get$$1() {
                    this._throwIfDestroyed();
                    return this._savedTabIndex;
                }
            }]);
            return InertNode;
        }();

        /**
         * InertManager is a per-document singleton object which manages all inert roots and nodes.
         *
         * When an element becomes an inert root by having an `inert` attribute set and/or its `inert`
         * property set to `true`, the `setInert` method creates an `InertRoot` object for the element.
         * The `InertRoot` in turn registers itself as managing all of the element's focusable descendant
         * nodes via the `register()` method. The `InertManager` ensures that a single `InertNode` instance
         * is created for each such node, via the `_managedNodes` map.
         */

        var InertManager = function() {
            /**
             * @param {Document} document
             */
            function InertManager(document) {
                classCallCheck(this, InertManager);

                if (!document) {
                    throw new Error('Missing required argument; InertManager needs to wrap a document.');
                }

                /** @type {Document} */
                this._document = document;

                /**
                 * All managed nodes known to this InertManager. In a map to allow looking up by Node.
                 * @type {Map<Node, InertNode>}
                 */
                this._managedNodes = new Map();

                /**
                 * All inert roots known to this InertManager. In a map to allow looking up by Node.
                 * @type {Map<Node, InertRoot>}
                 */
                this._inertRoots = new Map();

                /**
                 * Observer for mutations on `document.body`.
                 * @type {MutationObserver}
                 */
                this._observer = new MutationObserver(this._watchForInert.bind(this));

                // Add inert style.
                addInertStyle(document.head || document.body || document.documentElement);

                // Wait for document to be loaded.
                if (document.readyState === 'loading') {
                    document.addEventListener('DOMContentLoaded', this._onDocumentLoaded.bind(this));
                } else {
                    this._onDocumentLoaded();
                }
            }

            /**
             * Set whether the given element should be an inert root or not.
             * @param {Element} root
             * @param {boolean} inert
             */

            createClass(InertManager, [{
                key: 'setInert',
                value: function setInert(root, inert) {
                    if (inert) {
                        if (this._inertRoots.has(root)) {
                            // element is already inert
                            return;
                        }

                        var inertRoot = new InertRoot(root,this);
                        root.setAttribute('inert', '');
                        this._inertRoots.set(root, inertRoot);
                        // If not contained in the document, it must be in a shadowRoot.
                        // Ensure inert styles are added there.
                        if (!contains(this._document.body, root)) {
                            var parent = root.parentNode;
                            while (parent) {
                                if (parent.nodeType === 11) {
                                    addInertStyle(parent);
                                }
                                parent = parent.parentNode;
                            }
                        }
                    } else {
                        if (!this._inertRoots.has(root)) {
                            // element is already non-inert
                            return;
                        }

                        var _inertRoot = this._inertRoots.get(root);
                        _inertRoot.destructor();
                        this._inertRoots.delete(root);
                        root.removeAttribute('inert');
                    }
                }
                /**
                 * Get the InertRoot object corresponding to the given inert root element, if any.
                 * @param {Element} element
                 * @return {InertRoot?}
                 */

            }, {
                key: 'getInertRoot',
                value: function getInertRoot(element) {
                    return this._inertRoots.get(element);
                }
                /**
                 * Register the given InertRoot as managing the given node.
                 * In the case where the node has a previously existing inert root, this inert root will
                 * be added to its set of inert roots.
                 * @param {Node} node
                 * @param {InertRoot} inertRoot
                 * @return {InertNode} inertNode
                 */

            }, {
                key: 'register',
                value: function register(node, inertRoot) {
                    var inertNode = this._managedNodes.get(node);
                    if (inertNode !== undefined) {
                        // node was already in an inert subtree
                        inertNode.addInertRoot(inertRoot);
                        // Update saved tabindex value if necessary
                        inertNode.ensureUntabbable();
                    } else {
                        inertNode = new InertNode(node,inertRoot);
                    }

                    this._managedNodes.set(node, inertNode);

                    return inertNode;
                }
                /**
                 * De-register the given InertRoot as managing the given inert node.
                 * Removes the inert root from the InertNode's set of managing inert roots, and remove the inert
                 * node from the InertManager's set of managed nodes if it is destroyed.
                 * If the node is not currently managed, this is essentially a no-op.
                 * @param {Node} node
                 * @param {InertRoot} inertRoot
                 * @return {InertNode?} The potentially destroyed InertNode associated with this node, if any.
                 */

            }, {
                key: 'deregister',
                value: function deregister(node, inertRoot) {
                    var inertNode = this._managedNodes.get(node);
                    if (!inertNode) {
                        return null;
                    }

                    inertNode.removeInertRoot(inertRoot);
                    if (inertNode.destroyed) {
                        this._managedNodes.delete(node);
                    }

                    return inertNode;
                }
                /**
                 * Callback used when document has finished loading.
                 */

            }, {
                key: '_onDocumentLoaded',
                value: function _onDocumentLoaded() {
                    // Find all inert roots in document and make them actually inert.
                    var inertElements = slice.call(this._document.querySelectorAll('[inert]'));
                    var _iteratorNormalCompletion7 = true;
                    var _didIteratorError7 = false;
                    var _iteratorError7 = undefined;

                    try {
                        for (var _iterator7 = inertElements[Symbol.iterator](), _step7; !(_iteratorNormalCompletion7 = (_step7 = _iterator7.next()).done); _iteratorNormalCompletion7 = true) {
                            var inertElement = _step7.value;

                            this.setInert(inertElement, true);
                        }

                        // Comment this out to use programmatic API only.
                    } catch (err) {
                        _didIteratorError7 = true;
                        _iteratorError7 = err;
                    } finally {
                        try {
                            if (!_iteratorNormalCompletion7 && _iterator7.return) {
                                _iterator7.return();
                            }
                        } finally {
                            if (_didIteratorError7) {
                                throw _iteratorError7;
                            }
                        }
                    }

                    this._observer.observe(this._document.body, {
                        attributes: true,
                        subtree: true,
                        childList: true
                    });
                }
                /**
                 * Callback used when mutation observer detects attribute changes.
                 * @param {MutationRecord} records
                 * @param {MutationObserver} self
                 */

            }, {
                key: '_watchForInert',
                value: function _watchForInert(records, self) {
                    var _iteratorNormalCompletion8 = true;
                    var _didIteratorError8 = false;
                    var _iteratorError8 = undefined;

                    try {
                        for (var _iterator8 = records[Symbol.iterator](), _step8; !(_iteratorNormalCompletion8 = (_step8 = _iterator8.next()).done); _iteratorNormalCompletion8 = true) {
                            var record = _step8.value;

                            switch (record.type) {
                            case 'childList':
                                var _iteratorNormalCompletion9 = true;
                                var _didIteratorError9 = false;
                                var _iteratorError9 = undefined;

                                try {
                                    for (var _iterator9 = slice.call(record.addedNodes)[Symbol.iterator](), _step9; !(_iteratorNormalCompletion9 = (_step9 = _iterator9.next()).done); _iteratorNormalCompletion9 = true) {
                                        var node = _step9.value;

                                        if (node.nodeType !== Node.ELEMENT_NODE) {
                                            continue;
                                        }
                                        var inertElements = slice.call(node.querySelectorAll('[inert]'));
                                        if (index(node, '[inert]')) {
                                            inertElements.unshift(node);
                                        }
                                        var _iteratorNormalCompletion10 = true;
                                        var _didIteratorError10 = false;
                                        var _iteratorError10 = undefined;

                                        try {
                                            for (var _iterator10 = inertElements[Symbol.iterator](), _step10; !(_iteratorNormalCompletion10 = (_step10 = _iterator10.next()).done); _iteratorNormalCompletion10 = true) {
                                                var inertElement = _step10.value;

                                                this.setInert(inertElement, true);
                                            }
                                        } catch (err) {
                                            _didIteratorError10 = true;
                                            _iteratorError10 = err;
                                        } finally {
                                            try {
                                                if (!_iteratorNormalCompletion10 && _iterator10.return) {
                                                    _iterator10.return();
                                                }
                                            } finally {
                                                if (_didIteratorError10) {
                                                    throw _iteratorError10;
                                                }
                                            }
                                        }
                                    }
                                } catch (err) {
                                    _didIteratorError9 = true;
                                    _iteratorError9 = err;
                                } finally {
                                    try {
                                        if (!_iteratorNormalCompletion9 && _iterator9.return) {
                                            _iterator9.return();
                                        }
                                    } finally {
                                        if (_didIteratorError9) {
                                            throw _iteratorError9;
                                        }
                                    }
                                }

                                break;
                            case 'attributes':
                                if (record.attributeName !== 'inert') {
                                    continue;
                                }
                                var target = record.target;
                                var inert = target.hasAttribute('inert');
                                this.setInert(target, inert);
                                break;
                            }
                        }
                    } catch (err) {
                        _didIteratorError8 = true;
                        _iteratorError8 = err;
                    } finally {
                        try {
                            if (!_iteratorNormalCompletion8 && _iterator8.return) {
                                _iterator8.return();
                            }
                        } finally {
                            if (_didIteratorError8) {
                                throw _iteratorError8;
                            }
                        }
                    }
                }
            }]);
            return InertManager;
        }();

        /**
         * Recursively walk the composed tree from |node|.
         * @param {Node} node
         * @param {(function (Element))=} callback Callback to be called for each element traversed,
         *     before descending into child nodes.
         * @param {ShadowRoot=} shadowRootAncestor The nearest ShadowRoot ancestor, if any.
         */

        function composedTreeWalk(node, callback, shadowRootAncestor) {
            if (node.nodeType == Node.ELEMENT_NODE) {
                var element = /** @type {Element} */
                node;
                if (callback) {
                    callback(element);
                }

                // Descend into node:
                // If it has a ShadowRoot, ignore all child elements - these will be picked
                // up by the <content> or <shadow> elements. Descend straight into the
                // ShadowRoot.
                var shadowRoot = element.shadowRoot || element.webkitShadowRoot;
                if (shadowRoot) {
                    composedTreeWalk(shadowRoot, callback, shadowRoot);
                    return;
                }

                // If it is a <content> element, descend into distributed elements - these
                // are elements from outside the shadow root which are rendered inside the
                // shadow DOM.
                if (element.localName == 'content') {
                    var content = /** @type {HTMLContentElement} */
                    element;
                    // Verifies if ShadowDom v0 is supported.
                    var distributedNodes = content.getDistributedNodes ? content.getDistributedNodes() : [];
                    for (var i = 0; i < distributedNodes.length; i++) {
                        composedTreeWalk(distributedNodes[i], callback, shadowRootAncestor);
                    }
                    return;
                }

                // If it is a <slot> element, descend into assigned nodes - these
                // are elements from outside the shadow root which are rendered inside the
                // shadow DOM.
                if (element.localName == 'slot') {
                    var slot = /** @type {HTMLSlotElement} */
                    element;
                    // Verify if ShadowDom v1 is supported.
                    var _distributedNodes = slot.assignedNodes ? slot.assignedNodes({
                        flatten: true
                    }) : [];
                    for (var _i = 0; _i < _distributedNodes.length; _i++) {
                        composedTreeWalk(_distributedNodes[_i], callback, shadowRootAncestor);
                    }
                    return;
                }
            }

            // If it is neither the parent of a ShadowRoot, a <content> element, a <slot>
            // element, nor a <shadow> element recurse normally.
            var child = node.firstChild;
            while (child != null) {
                composedTreeWalk(child, callback, shadowRootAncestor);
                child = child.nextSibling;
            }
        }

        /**
         * Adds a style element to the node containing the inert specific styles
         * @param {Node} node
         */
        function addInertStyle(node) {
            if (node.querySelector('style#inert-style')) {
                return;
            }
            var style = document.createElement('style');
            style.setAttribute('id', 'inert-style');
            style.textContent = '\n' + '[inert] {\n' + '  pointer-events: none;\n' + '  cursor: default;\n' + '}\n' + '\n' + '[inert], [inert] * {\n' + '  user-select: none;\n' + '  -webkit-user-select: none;\n' + '  -moz-user-select: none;\n' + '  -ms-user-select: none;\n' + '}\n';
            node.appendChild(style);
        }

        /**
         * `Node#contains()` polyfill.
         *
         * See: http://compatibility.shwups-cms.ch/en/polyfills/?&id=1
         *
         * @param {Node} node
         * @param {Node} other
         * @return {Boolean}
         * @public
         */
        function contains(node, other) {
            return other && (node === other || !!(node.compareDocumentPosition(other) & 16));
        }

        var inertManager = new InertManager(document);

        Object.defineProperty(Element.prototype, 'inert', {
            enumerable: true,
            get: function get$$1() {
                return this.hasAttribute('inert');
            },
            set: function set$$1(inert) {
                inertManager.setInert(this, inert);
            }
        });
    }
    )(document);

}
)));

var OnlinePayment = function(_shopId, _targetUrl, _shopSettings) {
    var jsLibVersion = "1.0.4";
    var isIE11 = !!window.MSInputMethodContext && !!document.documentMode;

    if (!_shopId)
        throw "No shopId is provided";

    if (!_targetUrl)
        throw "No targetUrl is provided";

    //if not already appended by shop, add shopId
    if (_targetUrl.toLowerCase().indexOf('shopid=') === -1) {
        var appendor = _targetUrl.indexOf('?') === -1 ? '?' : '&';
        _targetUrl = _targetUrl + appendor + "ShopId=" + _shopId;
    }

    var settings = {
        enableLog: true,
        enableTrace: false,
        inertElements: true,
        resetElementFocus: true,
        dialogNotLoadedTimeout: 30 * 1000,
        logoUrl: "",
        shopColor: "#ffcc00"
    };

    if (_shopSettings) {
        for (var attrname in _shopSettings) {
            if (_shopSettings.hasOwnProperty(attrname)) {
                settings[attrname] = _shopSettings[attrname];
            }
        }
    }

    //with ie11 inert set works, but reset throws an error and the site is blocked.
    if (isIE11) {
        settings.inertElements = false;
    }

    var overlayPopupHelper = function() {

        var paymentPromise = {};
        var previousActiveElement = null;
        var noAnswerTimeout = null;

        function handleEvent(eventType, data) {
            log('got event: ' + eventType, data);
            if (eventType === "closeOverlay") {
                closeOverlay();
                paymentPromise.resolve(data);
            } else if (eventType === "resizeMask") {
                document.getElementById("OnlinePayment_modalContainer").style["max-width"] = data + "px";
            } else if (eventType === "overlayLoaded") {
                removeNode('OnlinePayment_LoadingId');
                window.clearTimeout(noAnswerTimeout);
            } else {
                var errorMessage = 'unhandled event ' + eventType;
                log(errorMessage);
                paymentPromise.reject(new Error({
                    eventType: eventType,
                    data: data,
                    errorMessage: errorMessage
                }));
            }
        }

        var eventHandler = function() {
            var receiveMessage = function(event) {
                var pathArray = _targetUrl.split("/");
                var protocol = pathArray[0];
                var host = pathArray[2];
                var targetUrlOrigin = protocol + '//' + host;

                // For Chrome, the origin property is in the event.originalEvent object.
                var origin = event.origin || event.originalEvent.origin;
                if (origin !== targetUrlOrigin)
                    return;

                var eventType = (event.data && event.data.message) || 'noEventTypeDefined';

                if (eventType === "noEventTypeDefined") {
                    trace('got no paymentonline event skip it. ' + eventType, event.data);
                    return;
                }

                handleEvent(eventType, event.data.data);
            };

            var urlChangedEvent = function(evt) {
                trace(urlChangedEvent, evt);
                return closeOverlayPrompt('Url Changed');
            }

            var removeWindowListeners = function() {
                if (window.removeEventListener) {
                    window.removeEventListener('message', receiveMessage);
                } else if (window.detachEvent) {
                    window.detachEvent('onmessage', receiveMessage);
                }
                document.removeEventListener('beforeunload', urlChangedEvent);

            }

            var addWindowListeners = function() {
                if (window.addEventListener) {
                    window.addEventListener("message", receiveMessage);
                } else if (window.attachEvent) {
                    window.attachEvent("onmessage", receiveMessage);
                }

                document.addEventListener('beforeunload', urlChangedEvent);
            };

            return {
                register: addWindowListeners,
                remove: removeWindowListeners
            };
        }();

        function appendCssStyleTag() {
            trace('appendCssStyleTag');
            try {
                var scrollingLockStyle = document.createElement("style");
                scrollingLockStyle.innerHTML = "html.shOverlayLocked {" + " width: 100%; height: 100%; overflow: hidden; }" + " html.shOverlayLocked body {width: 100%; height: 100%; overflow: visible; position: fixed; }" + "#OnlinePayment_modalContainer {z-index: 9999;position: absolute;top: 0px;overflow: hidden;left: 50%;transform: translate(-50%,0%);min-width: 320px;width: 100%;max-width: 450px;height: 100%;}" + "#OnlinePayment_iframeOverlay {border: 0; margin: 0; padding: 0; width: 100%;height: 100%;-webkit - transform:ranslate3d(0, 0, 0); overflow: hidden; position: relative}" + "#OnlinePayment_modalMask { z-index:9998;position:fixed;opacity:0.8;bottom:0;left:0;top:0;width:4800px;height:6073px;background-color:#A0A0A0;display:block";
                document.getElementsByTagName('head')[0].appendChild(scrollingLockStyle);
            } catch (errorInitOverlay) {
                log('error appendCssStyleTag', errorInitOverlay);
            }
        }

        function closeOverlayPrompt(type) {
            if (!confirm(OnlinePayment.Lang().CancelPayment)) {
                return;
            }
            handleEvent('closeOverlay', {
                'errorCode': 12,
                'errorMessage': 'aborted by user (' + type + ')',
                'result': 'cancel'
            });
        }

        function closeOverlay() {
            removeNode('OnlinePayment_LoadingId');
            var isOverlayOpened = document.getElementById('OnlinePayment_modalMask');
            if (!isOverlayOpened) {
                return;
            }

            toggleLockHostpage(false);
            removeNode('OnlinePayment_LoadingId');
            removeNode('OnlinePayment_modalMask');
            removeNode('OnlinePayment_modalContainer');
            eventHandler.remove();
            inertOtherElements(false);
            //does not work on ie11, the gui freezes
            if (!isIE11) {
                settings.resetElementFocus && previousActiveElement && previousActiveElement.focus();
            }
            previousActiveElement = null;
        }

        function removeNode(elementId) {
            var node = document.getElementById(elementId);
            if (node && node.parentNode) {
                node.parentNode.removeChild(node);
            }
        }

        function createPaymentFrame(paymentForm) {

            var paymentFrameWrapper = document.createElement("div");
            paymentFrameWrapper.id = 'OnlinePayment_modalContainer';
            paymentFrameWrapper.setAttribute("role", "dialog");
            paymentFrameWrapper.setAttribute("aira-labelledby", "dialogTitle");

            var dialogTitle = document.createElement("div");
            dialogTitle.id = "dialogTitle";
            dialogTitle.style.display = "none";
            dialogTitle.innerText = OnlinePayment.Lang().DialogTitle;
            paymentFrameWrapper.appendChild(dialogTitle);

            var iFrame = document.createElement("iframe");
            iFrame.id = 'OnlinePayment_iframeOverlay';
            iFrame.name = "iframeOverlay";
            iFrame.frameBorder = 0;
            iFrame.allowtransparency = "true";
            iFrame.appendChild(paymentForm);
            paymentFrameWrapper.appendChild(iFrame);

            return paymentFrameWrapper;
        }

        function toggleLockHostpage(doActivate) {
            var html = document.getElementsByTagName("html")[0];
            var body = document.getElementsByTagName("body")[0];
            var scrollPos = body.scrollTop;

            doActivate = (doActivate == null ? (html.classList.contains("shOverlayLocked") ? false : true) : !!doActivate);

            if (doActivate) {

                html.classList.add("shOverlayLocked");
                body.style.top = scrollPos * -1;
            } else {
                scrollPos = (parseInt(body.style.top, 10) * -1);
                html.classList.remove("shOverlayLocked");
                body.style.top = 0;
                body.scrollTop = scrollPos;
            }
        }

        function inertOtherElements(boolInerted) {
            if (!settings.inertElements) {
                trace("inertElements is disabled");
                return;
            }
            //works with the inert.min.js polyfill
            //https://github.com/WICG/inert/blob/master/dist/inert.min.js
            for (var i = 0; i < document.body.children.length; i++) {
                var child = document.body.children[i];
                if (child.id.indexOf("OnlinePayment_") === -1) {
                    child.inert = boolInerted;
                }
            }
        }

        var displayOverlay = function(b64EncodedXml) {

            function dialogNotLoaded() {
                log('overlay not loaded within ' + settings.dialogNotLoadedTimeout / 1000 + ' seconds, we close it.');
                handleEvent('closeOverlay', {
                    'errorCode': 99,
                    'errorMessage': 'aborted by timeout',
                    'result': 'error'
                });
            }

            if (_targetUrl.indexOf('http://') === 0 && location.protocol === 'http:') {
                log('WARN: your browser may deny the OnlinePayment request, since you run your site with http and OnlinePayment only runs with https://');
            }
            var promiseToReturn = new Promise(function(resolve, reject) {
                paymentPromise = {
                    resolve: resolve,
                    reject: reject
                };
            }
            );

            toggleLockHostpage(true);
            inertOtherElements(true);
            previousActiveElement = document.activeElement;

            var paymentForm = createPaymentFormular(b64EncodedXml, true, 'iframeOverlay');
            var paymentFrameWrapper = createPaymentFrame(paymentForm);

            var exposeMask = document.createElement("div");
            exposeMask.id = 'OnlinePayment_modalMask';

            var body = document.getElementsByTagName("body")[0];
            body.appendChild(paymentFrameWrapper);
            body.appendChild(exposeMask);
            body.appendChild(OnlinePayment.Spinner.getSpinner(settings.shopColor));

            eventHandler.register(_targetUrl);
            paymentForm.submit();
            noAnswerTimeout = setTimeout(dialogNotLoaded, settings.dialogNotLoadedTimeout);
            document.getElementById('OnlinePayment_iframeOverlay').contentWindow.focus();

            return promiseToReturn;
        };

        appendCssStyleTag();
        return {
            displayOverlay: displayOverlay
        };
    }();

    function log(errorMessage, obj) {
        settings.enableLog && console.log(errorMessage, obj);
    }

    function trace(errorMessage, obj) {
        settings.enableTrace && console.log(errorMessage, obj);
    }

    var createPaymentFormular = function(b64EncodedXml, isOverlay, formTarget) {

        function hiddenInput(id, data) {
            var obj = document.createElement('input');
            obj.id = id;
            obj.name = id;
            obj.value = data;
            return obj;
        }

        var paymentForm = document.createElement("form");
        paymentForm.id = 'paymentOverlayForm1';
        paymentForm.style.display = "none";
        paymentForm.method = "post";
        paymentForm.action = _targetUrl;
        if (formTarget) {
            paymentForm.target = formTarget;
        }

        var createElement = function(elementName, content) {
            return "<" + elementName + ">" + content + "</" + elementName + ">";
        }

        var a = document.createElement("ShopGuiSettings");
        var logoUrl = document.createElement("LogoUrl");
        logoUrl.innerText = settings.logoUrl;
        a.appendChild(logoUrl);

        var content = createElement("LogoUrl", encodeURI(settings.logoUrl));
        content += createElement("ShopColor", settings.shopColor);
        content += createElement("IsOverlay", !!isOverlay);
        content += createElement("JsLibVersion", jsLibVersion);

        var shopGuiSettings = createElement("ShopGuiSettings", content);

        var base64Settings = btoa('<?xml version="1.0" encoding="utf-8"?>' + shopGuiSettings);

        paymentForm.appendChild(hiddenInput("shopid", _shopId));
        paymentForm.appendChild(hiddenInput("data", b64EncodedXml));
        paymentForm.appendChild(hiddenInput("guiSettings", base64Settings));
        return paymentForm;
    };

    var payInIFrame = function(b64EncodedXml, iframeId) {
        var iFrame = document.getElementById(iframeId);
        if (!iFrame) {
            throw "iFrame " + iFrame + "not found"
        }
        ;iFrame = iFrame.documentElement || iFrame.contentWindow.document || iFrame.contentDocument;
        if (!iFrame) {
            throw "iFrame " + iFrame + "has no documentElement / contentWindow.document property."
        }
        ;
        var frameHtml = document.createElement("html");
        var frameBody = document.createElement("body");
        frameBody.setAttribute('onload', 'document.getElementById("paymentOverlayForm1").submit();');
        var form = createPaymentFormular(b64EncodedXml, false);
        frameBody.appendChild(form);
        frameHtml.appendChild(frameBody);
        iFrame.open();
        iFrame.write(frameHtml.outerHTML);
        iFrame.close();
    }

    var payWithRedirect = function(b64EncodedXml) {
        var body = document.getElementsByTagName("body")[0];
        var submitForm = createPaymentFormular(b64EncodedXml, false);
        body.appendChild(submitForm);
        submitForm.submit();
    }

    var payInOverlay = function(b64EncodedXml) {
        return overlayPopupHelper.displayOverlay(b64EncodedXml);
    }

    return {
        payInOverlay: payInOverlay,
        payWithRedirect: payWithRedirect,
        payInIFrame: payInIFrame
    };
};

OnlinePayment.Lang = function(lang) {
    var userLang = lang || navigator.language || navigator.userLanguage;
    var dict = {
        de: {
            DialogTitle: "Dialog für den Zahlvorgang",
            CancelPayment: "Möchten Sie den Zahlvorgang wirklick abbrechen?"
        },
        en: {
            DialogTitle: "Dialog of the payment process",
            CancelPayment: "Do you really want to abort the payment process?"
        }
    };
    return dict[userLang] ? dict[userLang] : dict["en"];
}

var OnlinePayment = OnlinePayment || {};

OnlinePayment.Spinner = function() {

    function initSpinner() {
        var spinnerStyle = document.createElement("style");
        spinnerStyle.innerHTML = ".onlinePaymentSpinner" + "{" + "border: 16px solid #f3f3f3;" + "border-radius: 50%;" + "width: 120px;" + "height: 120px;" + "-webkit-animation: onlinePaymentSpinnerKF 2s linear infinite;" + "animation: onlinePaymentSpinnerKF 2s linear infinite;" + "}" + ".onlinePaymentSpinnerContainer{" + "z-index: 9999;" + "position:fixed;" + "top:50%;" + "left:50%;" + "transform:translate(-50%, -50%);" + "}" + "@-webkit-keyframes onlinePaymentSpinnerKF {" + "    0% { -webkit-transform: rotate(0deg); }" + "    100% { -webkit-transform: rotate(360deg); }" + "    }" + "" + "@keyframes onlinePaymentSpinnerKF {" + "    0% { transform: rotate(0deg); }" + "    100% { transform: rotate(360deg); }" + "}";
        document.getElementsByTagName('head')[0].appendChild(spinnerStyle);
    }

    var getSpinner = function(color) {
        var onlinePaymentSpinnerContainer = document.createElement('div');
        onlinePaymentSpinnerContainer.classList.add('onlinePaymentSpinnerContainer');
        onlinePaymentSpinnerContainer.id = "OnlinePayment_LoadingId";

        var spinner = document.createElement('div');
        spinner.classList.add('onlinePaymentSpinner');
        spinner.style["border-top"] = "16px solid " + color;

        onlinePaymentSpinnerContainer.appendChild(spinner);
        return onlinePaymentSpinnerContainer;
    }

    initSpinner();
    return {
        getSpinner: getSpinner
    };
}();
(function($) {
    "use strict";
    /**
	 * Basket module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author sbartels <sebastian.bartels@namics.com>
	 * @namespace Tc.Module
	 * @class Basket
	 * @extends Tc.Module
	 */
    Tc.Module.Basket = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            // Trigger Connect
            this.sandbox.subscribe("Basket", this);
            this.isBasket();
            this.showDataStorage();
            this.deleteDataStorage();
            this.showBasketLink();
            callback();
            // !do not remove
        },
        isBasket: function() {
            if ($(".mod-orderform").length && !this.$ctx.parent().hasClass("mod-orderform")) {
                this.$ctx.hide();
            }
            if (this.$ctx.parent().hasClass("mod-orderform")) {
                this.$ctx.parent(".mod-orderform").parents("form").submit(function() {
                    localStorage.basket = window.location.href;
                });
            }
        },
        onShowDataStorage: function() {
            this.showDataStorage();
            this.showBasketLink();
        },
        showDataStorage: function() {
            var dataStorage = localStorage.orderForm, data;
            if (dataStorage) {
                data = {
                    "data": $.parseJSON(localStorage.getItem("orderForm"))
                };
                this.$ctx.find('.js-order-list').html(this.template(this.$ctx.find('#order-list-template').html(), data));
            }
            this.writeGenericField();
        },
        writeGenericField: function() {
            var mod = this, xallAttributes = "", $el;
            if (mod.$ctx.parent().hasClass("mod-orderform")) {
                $(".order-list input[name^='order_']", mod.$ctx).each(function(i, el) {
                    $el = $(el);
                    xallAttributes += $el.attr("name") + "=" + $el.attr("value") + ",";
                });
                xallAttributes = xallAttributes.slice(0, -1);
                if ($("input.orderform-input", mod.$ctx).length) {
                    $("input.orderform-input", mod.$ctx).val(xallAttributes);
                } else {
                    mod.$ctx.append("<input name='orderform' class='orderform-input' type='hidden' value='" + xallAttributes + "'>");
                }
            }
            this.updateCartCount();
        },
        showBasketLink: function() {
            var mod = this
              , basketLink = $(".basket-link", mod.$ctx)
              , error = $(".error.empty", mod.$ctx);
            if ($(".order-list", mod.$ctx).find("li").length) {
                if (mod.$ctx.parent().hasClass("mod-orderform")) {
                    mod.$ctx.show();
                } else if (!$(".mod-orderform").length) {
                    mod.$ctx.show();
                }
                basketLink.show();
                if (mod.$ctx.parent().hasClass("mod-orderform")) {
                    error.hide();
                }
            } else {
                basketLink.hide();
                if (mod.$ctx.parent().hasClass("mod-orderform")) {
                    error.show();
                } else {
                    mod.$ctx.hide();
                }
            }
        },
        deleteDataStorage: function() {
            var mod = this, i, domId, json, $el;
            $(".order-list", mod.$ctx).on("click", ".storage-delete", function(e) {
                e.preventDefault();
                $el = $(this);
                domId = $el.closest("li").attr("id");
                json = JSON.parse(localStorage.orderForm);
                $el.closest("li").remove();

                for (i = 0; i < json.length; i++) {
                    if (json[i].id === parseInt(domId)) {
                        json.splice(i, 1);
                    }
                }
                localStorage.orderForm = JSON.stringify(json);
                mod.showBasketLink();
                mod.writeGenericField();
            });

        },
        updateCartCount: function() {
            var orderForm = JSON.parse(localStorage.getItem("orderForm"));
            if (orderForm) {
                var cartCount = 0;
                for (var i = 0; i < orderForm.length; i++) {
                    cartCount += parseInt(orderForm[i].quantity);
                }
                if ($(".shopping-cart-count") && $(".shopping-cart-count").length) {
                    $(".shopping-cart-count").text(cartCount);

                    if (cartCount > 0) {
                        $(".shopping-cart-count").show();
                    } else {
                        $(".shopping-cart-count").hide();
                    }
                }
            }
        },
        after: function() {}
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Externalcontent module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Externalcontent
	 * @extends Tc.Module
	 */
    Tc.Module.Externalcontent = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this
              , $ctx = _this.$ctx;

            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;
            _this.dataUrl = $(".content", _this.$ctx).data("url");
            _this.loadContent(_this.dataUrl);

        },
        loadContent: function(url, attributes, type) {
            url = url || "";
            var _this = this
              , aUrl = attributes !== undefined && attributes !== null ? url + attributes : url;

            type = type !== undefined ? type : "GET";

            if ($("body").hasClass("skin-layout-author")) {
                aUrl += aUrl.indexOf('?') == -1 ? "?wcmmode=disabled" : "&wcmmode=disabled";
            }

            if (aUrl) {
                $.ajax({
                    type: type,
                    url: aUrl
                }).done(function(data) {
                    $(".content", _this.$ctx).html(data);
                    _this._bindClickEvents();
                });
            }

        },
        _bindClickEvents: function() {
            var _this = this;
            $("a", _this.$ctx).unbind("click").on("click", function(event) {
                var url = $(this).attr("href");
                //only load normal links via ajax. let downloads through
                if (_this.isNormalLink(url) && !$(this).is('[download]')) {
                    _this.loadContent(url);
                    event.preventDefault();
                }
            });
            $("form", _this.$ctx).unbind("submit").on("submit", function(event) {
                var method = $(this).attr("method");
                var url = $(this).attr("action")
                  , parameter = $(this).serialize();
                _this.loadContent(url + "?" + parameter, null, method);
                event.preventDefault();
            });
        },
        stringContains: function(str, search) {
            return str.indexOf(search) !== -1;
        },
        stringContainsAny: function(str, strArray) {
            for (var i = 0; i < strArray.length; i++) {
                if (this.stringContains(str, strArray[i])) {
                    return true;
                }
            }
            return false;
        },
        stringEndsWith: function(str, suffix) {
            return str.indexOf(suffix, str.length - suffix.length) !== -1;
        },
        isNormalLink: function(url) {
            return this.stringEndsWith(url, ".html");
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Search module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Search
	 * @extends Tc.Module
	 */
    Tc.Module.Search = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            var _this = this;
            this._super($ctx, sandbox, modId);

            this.$searchbox = $("form.search-box", _this.$ctx);
            this.dataUrl = this.$searchbox.data("url");
            this.content = this.$searchbox.data("content");
            this.documents = this.$searchbox.data("documents");
            this.validationMsg = "";
            this.minLength = 0;
            this.breakpointS = false;

            // add jquery rule for detecting double whitespaces
            // prevents user from using whitespaces to make an empty search
            jQuery.validator.addMethod("noDoubleSpace", function(value, element) {
                return value.indexOf("  ") < 0;
            });
        },

        on: function(callback) {
            var _this = this;
            _this._readHash();

            _this.$searchbox.submit(function(event) {
                event.preventDefault();
            });

            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;
            _this._initValidation();
            _this._setCurrentDate();
            //$( ".form", _this.$ctx).submit();

        },

        buildQueryUrl: function(searchGetParam, content, dataUrl) {
            var dateFrom = $("#realmsearch_datefrom").val() || "";
            var dateTo = $("#realmsearch_dateto").val() || "";

            return dataUrl + "." + content + ".html?_charset_=UTF-8&search=" + encodeURIComponent(searchGetParam) + "&datefrom=" + dateFrom + "&dateto=" + dateTo;
        },

        startAjaxRequest: function(type) {
            var _this = this;
            var search = $("#inputSearch", _this.$ctx).val();
            var hiddenResultContainer = '.search-results .collapsify + .tab-content';
            var countTabPane = $('.search-results .tab-pane').length;
            $(_this.$ctx).addClass("loading");
            $('#totalResults', _this.$ctx).html("0");
            _this.breakpointS = false;

            // mobile actions start
            if (window.innerWidth < 769) {
                _this.breakpointS = true;
            }
            // mobile actions end

            var contentPagePromise;
            var contentPagesDynamicContent = $("#contentPagesDynamicContent", _this.$ctx);
            if (contentPagesDynamicContent.length > 0) {
                $("#contentPagesDynamicContent", _this.$ctx).addClass("loading");
                contentPagePromise = $.get(this.buildQueryUrl(search, _this.content, _this.dataUrl), function(data) {
                    contentPagesDynamicContent.html(data).removeClass("loading");
                });
            } else {
                // if the element is not present in the dom, we just return a resolved deferred. "do nothing"
                contentPagePromise = $.Deferred().resolve();
            }

            var documentPromise;
            var documentsDynamicContent = $("#documentsDynamicContent", _this.$ctx);
            if (documentsDynamicContent.length > 0) {
                $("#documentsDynamicContent", _this.$ctx).addClass("loading");
                documentPromise = $.get(this.buildQueryUrl(search, _this.documents, _this.dataUrl), function(data) {
                    documentsDynamicContent.html(data).removeClass("loading");
                });
            } else {
                // if the element is not present in the dom, we just return a resolved deferred. "do nothing"
                documentPromise = $.Deferred().resolve();
            }

            $.when(contentPagePromise, documentPromise).then(function() {

                if (_this.breakpointS && countTabPane < 2) {
                    $(hiddenResultContainer).show().addClass('noTabLinkOnTop');
                }

                _this._resetFilter();
                _this._initPagination();
                _this._initFilter();
                _this._writeHash(search);
                _this._countTotalResults();
                $(_this.$ctx).removeClass("loading");
                $("#searchResults", _this.$ctx).show();
                _this._initMatomoSearchTracker(search, false, false);
            });
        },

        loadPage: function(page, type, attributes) {
            var _this = this
              , search = $(".tab-pane.active .dynamic-content input[name='search']").val() || $("#inputSearch", _this.$ctx).val()
              , nAttributes = attributes !== undefined ? "&" + attributes : ""
              , bottomPaging = attributes.indexOf('pgbtm=1') > 0
              , offsetTop = _this.$ctx.offset().top - 100;

            var paginationPortion = "&page=" + page + nAttributes;

            if (type === 'contentPages') {
                $("#contentPagesDynamicContent", _this.$ctx).addClass("loading");
                $.get(this.buildQueryUrl(search, _this.content, _this.dataUrl) + paginationPortion, function(data) {
                    $("#contentPagesDynamicContent", _this.$ctx).html(data).removeClass("loading");
                    if (bottomPaging) {
                        $('html, body').stop().animate({
                            'scrollTop': offsetTop
                        }, 200);
                    }
                    _this._initPagination();
                    _this._initFilter();
                    _this._countTotalResults();
                });
            }
            if (type === 'documentsPages') {
                $("#documentsDynamicContent", _this.$ctx).addClass("loading");
                $.get(this.buildQueryUrl(search, _this.documents, _this.dataUrl) + paginationPortion, function(data) {
                    $("#documentsDynamicContent", _this.$ctx).html(data).removeClass("loading");
                    if (bottomPaging) {
                        $('html, body').stop().animate({
                            'scrollTop': offsetTop
                        }, 200);
                    }
                    _this._initPagination();
                    _this._initFilter();
                    _this._countTotalResults();
                });
            }
        },

        _initValidation: function() {

            var _this = this;
            var search = $("form #inputSearch", _this.$ctx);
            _this.validationMsg = search.data("message");
            _this.minLength = search.data("minlength");

            $("form", _this.$ctx).validate({
                errorContainer: "#errorMessage",
                errorLabelContainer: "#errorMessage ul",
                wrapper: "li",
                submitHandler: function(form) {
                    _this.startAjaxRequest(0, '');
                },
                rules: {
                    searchField: {
                        noDoubleSpace: true,
                        required: true,
                        minlength: _this.minLength
                    }
                },
                messages: {
                    searchField: {
                        noDoubleSpace: _this.validationMsg,
                        required: _this.validationMsg,
                        minlength: _this.validationMsg
                    }
                }
            });
        },
        _initFilter: function() {
            var _this = this;
            $(".search-filter-form button", _this.$ctx).unbind("click").on("click", function(event) {
                var loadPage = 0
                  , type = $(this).closest(".tab-pane").attr("id")
                  , attributes = $(this).closest(".search-filter-form").serialize();

                if (type === undefined) {
                    var dynamicType = $(this).closest(".search-filter-form").attr("id");
                    if (dynamicType === "pagesForm") {
                        type = "contentPages";
                    }
                    if (dynamicType === "documentsForm") {
                        type = "documentsPages";
                    }
                }
                _this.loadPage(loadPage, type, attributes);
                event.preventDefault();
            });

        },
        _initPagination: function() {
            var _this = this;
            $(".pagination li:not(.active) a", _this.$ctx).unbind("click").on("click", function(event) {
                var loadPage = $(this).data("loadpage")
                  , type = $(this).closest(".tab-pane").attr("id");

                if (type === undefined) {
                    var dynamicType = $(this).closest(".dynamic-content").attr("id");
                    if (dynamicType === "contentPagesDynamicContent") {
                        type = "contentPages";
                    }
                    if (dynamicType === "documentsDynamicContent") {
                        type = "documentsPages";
                    }
                }

                // if bottom-pagination is clicked, say this to the loadPage-method
                var pagingBottom = ($(event.currentTarget).closest('.pg-bottom').length == 1) ? "&pgbtm=1" : "";

                _this.hiddenAttributes = "";
                $(this).closest(".tab-pane").find("input[type=hidden]").each(function() {
                    _this.hiddenAttributes += ($(this).attr("name") + "=" + $(this).attr("value") + "&");
                });
                $(this).closest(".collapse").find("input[type=hidden]").each(function() {
                    _this.hiddenAttributes += ($(this).attr("name") + "=" + $(this).attr("value") + "&");
                });
                _this.hiddenAttributes = _this.hiddenAttributes.slice(0, -1);
                var attributes = _this.hiddenAttributes !== undefined || _this.hiddenAttributes !== "" ? _this.hiddenAttributes : undefined;
                _this.loadPage(loadPage, type, attributes + pagingBottom);
                event.preventDefault();
            });
        },
        _writeHash: function(searchString) {
            var encodeSearchString = encodeURIComponent(searchString);
            if (window.location.hash !== encodeSearchString) {
                window.location.hash = encodeSearchString;
            }
        },
        _readHash: function() {
            var _this = this
              , searchString = window.location.hash.replace("#", "")
              , decodeSearchString = decodeURIComponent(searchString);

            if (decodeSearchString && decodeSearchString !== "contentPages" && decodeSearchString !== "documentsPages") {
                var url = _this.dataUrl + ".results.html?_charset_=UTF-8&search=" + decodeSearchString;
                $.get(url, function() {
                    $("#inputSearch", _this.$ctx).val(decodeSearchString);
                    _this.$searchbox.submit();
                });
            }
        },
        _setCurrentDate: function() {
            var _this = this
              , today = new Date()
              , dd = today.getDate()
              , mm = today.getMonth() + 1 //January is 0!
              , yyyy = today.getFullYear();

            if (dd < 10) {
                dd = '0' + dd;
            }

            if (mm < 10) {
                mm = '0' + mm;
            }

            today = yyyy + "-" + mm + "-" + dd;
            $("#newsForm input[name=endDate]", _this.$ctx).val(today);
        },

        _countTotalResults: function() {
            var _this = this;
            var totalResults = 0;
            var pgBottomSelector = $('.dynamic-content .pg-bottom');
            var pgBottomSelectorA, pgBottomSelectorB = '';

            if (_this.breakpointS) {
                // mobile
                pgBottomSelectorA = $('.search-results .collapsify .pg-bottom', _this.$ctx);
                // google search mobile
                pgBottomSelectorB = $('.search-results .collapsify + .tab-content .pg-bottom', _this.$ctx);
                // site search mobile
                pgBottomSelector = (pgBottomSelectorB.length > pgBottomSelectorA.length) ? pgBottomSelectorB : pgBottomSelectorA;
            }

            pgBottomSelector.each(function(i, v) {
                totalResults += $(v).data("results");
            });

            $("#totalResults", _this.$ctx).html(totalResults);
        },

        _resetFilter: function() {
            var _this = this
              , $startDate = $("#newsForm input[name=startDate]", _this.$ctx)
              , $datepicker = $("#newsForm input[name=startDate]", _this.$ctx).next(".is-datepicker");
            $(".search-filter-form", _this.$ctx).each(function() {
                $("input[type='radio']", this).first().trigger("click");
            });
            $startDate.val($startDate.data("initial-value"));
            //is datepicker plugin in use (firefox/internet explorer)
            if ($datepicker.length !== 0) {
                $datepicker.datepicker('setDates', new Date($startDate.data("initial-value")));
            }
            _this._setCurrentDate();
            $(".nav-tabs li:first a", _this.$ctx).trigger("click");
        },

        /**
		 * Initialize the matomo search tracker for the website
		 *
 		 * @param keyword Search keyword searched for
		 * @param category Search category selected in your search engine. If you do not need this, set to false
		 * @param numberOfResults Number of results on the Search results page. Zero indicates a 'No Result Search Keyword'. Set to false if you don't know
		 * @private
		 */
        _initMatomoSearchTracker: function(keyword, category, numberOfResults) {
            if (window._paq) {
                _paq.push(['trackSiteSearch', keyword, category, numberOfResults]);
            }
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Slideshow module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  tmay <thomas.may@namics.com>
	 * @namespace Tc.Module
	 * @class Slideshow
	 * @extends Tc.Module
	 */
    Tc.Module.Slideshow = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            this.setSlideshowOptions(this.$$(".carousel"));
            callback();
            // !do not remove
        },

        after: function() {
        },

        setSlideshowOptions: function($el) {
            $el.carousel({
                interval: $el.data("time")
            });
        }

    });
}(Tc.$));
(function($) {
    "use strict";
    /**
	 * Gallery module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Imagegallery
	 * @extends Tc.Module
	 */
    Tc.Module.Imagegallery = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            /*this.bindAll(
				"_positionMagnifier"
			);*/
            var _this = this;
            this.images = [];
            this.activeImage = -1;
            this.activeURL = null;
            this.prevImage = null;
            this.nextImage = null;

            this.preload = {};

            //DOM ELEMENTS
            this.$singleContainer = $(".single-view", this.$ctx);
            this.$prevPager = $(".pager_prev a", _this.$singleContainer);
            this.$nextPager = $(".pager_next a", _this.$singleContainer);
            this.$image = $("img", _this.$singleContainer);
            this.$description = $(".description", _this.$singleContainer);
            this.$closer = $("a.close-sv", _this.$singleContainer);

            _this._initGallery();

            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;
            //$(window).on("resize", $.debounce(200, _this._positionMagnifier));
        },

        _initGallery: function() {
            var _this = this;
            _this._linkMapper();
            //_this._positionMagnifier();
            _this._lightboxNavigation();
            //_this.$ctx.imagesLoaded(_this._positionMagnifier);
        },
        _lightboxNavigation: function() {
            var _this = this;
            $(".pagination .disabled a", _this.$ctx).click(function(e) {
                e.preventDefault();
            });
            $(".modal .carousel-control", _this.$ctx).click(function(e) {
                var currentIndex = $(this).closest(".col-sm-3").index(), imagesCount = $(this).closest(".row").children(".col-sm-3").length, isLeft = $(this).hasClass("left"), isRight = $(this).hasClass("right"), newIndex, $currentModal = $(this).closest(".modal"), parentWrapper;
                if (isLeft) {
                    //current image = first image?
                    if (currentIndex === 0) {
                        newIndex = imagesCount - 1;
                    } else {
                        newIndex = currentIndex - 1;
                    }

                } else if (isRight) {
                    //current image last image?
                    if (currentIndex + 1 === imagesCount) {
                        newIndex = 0;
                    } else {
                        newIndex = currentIndex + 1;
                    }
                }
                $currentModal.removeClass("fade").modal('hide').addClass("fade");
                parentWrapper = $(".gallery-view .col-sm-3", _this.$ctx).get(newIndex);
                $(parentWrapper).find(".modal").removeClass("fade").modal('show').addClass("fade");

                e.preventDefault();
            });

        },
        _positionMagnifier: function() {
            this.$$(".thumbnail").each(function() {
                var $img = $("img", this)
                  , $icon = $(".fa.fa-search", this)
                  , $figure = $("figure", this)
                  , bottom = $figure.height() - $img.height()
                  , right = $figure.width() - $img.width();

                if ($icon.length) {
                    if (bottom !== 0) {
                        bottom = (bottom / 2) - 1;
                    }
                    if (right !== 0) {
                        right = (right / 2) - 1;
                    }
                    $icon.css({
                        bottom: bottom,
                        right: right
                    });
                }
            });
        },

        _linkMapper: function() {
            var _this = this
              , $galleryEl = $(".gallery-single", _this.$ctx);

            $galleryEl.each(function() {
                var $modal = $(this).siblings(".modal")
                  , description = $(".modal-body .lbContent", $modal).html()
                  , link = $modal.find("img").attr("src")
                  , title = $modal.find(".modal-title").html()
                  , alt = $modal.find("img").attr("alt");
                _this.images.push([link, title, description, alt]);
            });

            $galleryEl.unbind("click").click(function(e) {
                var elementIndex = $galleryEl.index(this);
                return _this.changeImage(elementIndex);
            });
        },
        changeImage: function(imageIndex) {
            var _this = this;
            if (imageIndex >= 0) {
                _this.activeImage = imageIndex;
                _this.activeURL = _this.images[_this.activeImage][0];
                _this.prevImage = (_this.activeImage || 0) - 1;
                _this.nextImage = ((_this.activeImage + 1) % _this.images.length) || -1;

                _this.preload = new Image();
                _this.preload.onload = _this._adjustMarkup();
                _this.preload.src = _this.activeURL;

            }

            return false;
        },
        next: function() {
            return this.changeImage(this.nextImage);
        },
        prev: function() {
            return this.changeImage(this.prevImage);
        },
        close: function() {
            var _this = this;
            if ($(_this.$ctx).hasClass("detail-view")) {
                $(_this.$ctx).removeClass("detail-view");
                //				_this.$ctx.height("auto");
            }
        },

        _adjustMarkup: function() {

            //			this.$ctx.height("auto");

            var _this = this
              , el = _this.images[_this.activeImage]
              , singleViewHeight = _this.$singleContainer.height()
              , headline = el[1] !== undefined ? "<h3>" + el[1] + "</h3>" : "";
            //			_this.$ctx.height(singleViewHeight);
            _this.$image.attr("src", _this.activeURL).attr("alt", el[3]);
            _this.$description.html(headline).append(el[2]);

            if (_this.prevImage === -1) {
                _this.$prevPager.parent("li").hide();
            } else {
                _this.$prevPager.parent("li").show();
            }
            if (_this.nextImage === -1) {
                _this.$nextPager.parent("li").hide();
            } else {
                _this.$nextPager.parent("li").show();
            }
            _this.$prevPager.unbind("click").click(function() {
                return _this.prev();
            });
            _this.$nextPager.unbind("click").click(function() {
                return _this.next();
            });
            _this.$closer.unbind("click").click(function(event) {
                _this.close();
                event.preventDefault();
            });

            if (!$(_this.$ctx).hasClass("detail-view")) {
                $(_this.$ctx).addClass("detail-view");
            }

        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
     * Searchfield module implementation.
     * https://gitlab.namics.com/snippets/2
     *
     * @author  <>
     * @namespace Tc.Module
     * @class Searchfield
     * @extends Tc.Module
     */
    Tc.Module.Searchfield = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var _this = this;
            this.$$('.form-search .search-field').typeahead('destroy');
            $("form", _this.$ctx).submit(function(event) {
                var actionUrl = $("form", _this.$ctx).attr("action")
                  , searchString = $("input[name='query']", _this.$ctx).val()
                  , isAdminSearch = $("#admin-directory-search-field", _this.$ctx).length > 0
                  , encodeSearchString = encodeURIComponent(searchString)
                  , adminUrl = ''
                  , $adminDirSearchStringInput = null
                  , $adminDirSearchLanguageInput = null
                  , $adminDirSearchCharsetInput = null
                  , $adminDirSearchFormResourceIdentifierInput = null;

                if (!isAdminSearch) {
                    if ($(".mod-search").length !== 0 && $(".realsmsearch-form").length === 0) {
                        // go to search result page if the current page contains a realm search
                        $(".mod-search #inputSearch").val(searchString).focus();
                        $(".mod-search form.search-box").submit();
                        if ($(".nav-mobile").hasClass("nav-open")) {
                            $("#search-field-mobile").closest(".nav-mobile-menu.dropdown-toggle").click();
                        }

                    } else {
                        window.location = actionUrl + "#" + encodeSearchString;

                    }
                } else {
                    $adminDirSearchStringInput = $("input.js-admin-dir-search-field", _this.$ctx);
                    $adminDirSearchLanguageInput = $("input.js-admin-dir-language-field", _this.$ctx);
                    $adminDirSearchFormResourceIdentifierInput = $('input.js-admin-dir-form-resource-identifier-field', _this.$ctx);
                    $adminDirSearchCharsetInput = $('input.js-admin-dir-charset-field', _this.$ctx);

                    var queryString = $.grep([_this.addParam($adminDirSearchFormResourceIdentifierInput), _this.addParam($adminDirSearchCharsetInput), _this.addParam($adminDirSearchStringInput), _this.addParam($adminDirSearchLanguageInput)], Boolean).join('&');

                    adminUrl = actionUrl + '?' + queryString;

                    window.open(adminUrl, "_blank");

                }
                event.preventDefault();
                event.stopImmediatePropagation();
            });
            callback();
            // !do not remove
        },

        addParam: function(formField) {
            return (formField && formField.val()) ? formField.attr('name') + '=' + encodeURI(formField.val()) : '';
        },

        after: function() {
            var _this = this;
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Dynamic module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Dynamic
	 * @extends Tc.Module
	 */
    Tc.Module.Dynamic = Tc.Module.extend({

        isDynamicContentLoaded: false,
        freshPageLoad: false,
        $dynForm: $('#content form').eq(0),

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

            this.bindAll('contentLoaded', 'onPaginationClick', 'onAccordionClick'
            );

            this.currentWindow = Tc.Utils.WCM.getCurrentWindow();
            this.dataUrl = null;
            // offer helper
            this.initUrlParamHelper();
            this.getQueryVars();

        },

        getQueryVars: function() {

            var that = this
              , $ctx = that.$ctx
              , urlParamData = null
              , valueArray = new Array
              , formValueFromUrl = '';

            that.$dynForm.find('.form-group input[data-url-param][type=text], .form-group select[data-url-param]').each(function(i, v) {
                valueArray.length = 0;
                urlParamData = $(v).data('urlParam');

                if (urlParamData !== undefined && $.urlParam(urlParamData) !== '') {
                    formValueFromUrl = that._decodingValue($.urlParam(urlParamData));
                    if (v.nodeName === 'SELECT') {
                        if (v.multiple) {
                            $(v).val(formValueFromUrl.split(','));
                        } else {
                            $(v).val(formValueFromUrl);
                        }
                        $(v).trigger("chosen:updated");

                    } else {
                        // value to simple field
                        $(v).val(formValueFromUrl);
                    }

                }

            });

        },
        initUrlParamHelper: function() {

            var that = this;

            // offer url-param-return
            $.urlParam = function(name, url) {
                if (!url) {
                    url = that.currentWindow.location.href;
                }
                var results = new RegExp('[\\?&]' + name + '=([^&#]*)').exec(url);
                if (!results) {
                    return '';
                }
                return results[1] || undefined;
            }
        },

        on: function(callback) {
            var _this = this;
            callback();
            // !do not remove
        },

        after: function() {
            this.dataUrl = this.$ctx.data("url");
            this.dataInterval = this.$ctx.data("interval");
            this.pageIndexParam = this.$ctx.data('pageindex-param') || 'pageIndex';

            if (this.$ctx.data("initialload") !== 0) {
                // page allready loaded, only call data-url
                this.loadContent(this.dataUrl, null, true);
                if (this.dataInterval) {
                    var that = this;
                    setInterval(function() {
                        that.loadContent(that.dataUrl, null, true)
                    }, this.dataInterval * 1000);
                }
            } else {
                // run initial methodes
                this._initEvents();
            }
            $(window).trigger("resize");
        },

        // replacestate of history , add url-variables without reloading the page
        // MODIMPL-114
        updateUrl: function(newHtml) {
            var that = this
              , $addParams = $(newHtml).find('.add-param')
              , urlParamName = ''
              , urlParamValue = ''
              , authorModeString = 'wcmmode'
              , arrQueryString = new Array();

            if ($.urlParam(authorModeString)) {
                arrQueryString.push('wcmmode=disabled')
            }

            $addParams.each(function(i, v) {

                urlParamName = $(v).data('urlParam');
                urlParamValue = '';

                if (urlParamName) {
                    if ($.urlParam(urlParamName) && that.freshPageLoad) {
                        urlParamValue = $.urlParam(urlParamName);
                    } else {
                        if ($(v).val().length > 0)
                            urlParamValue = that._encodingValue($(v).val());
                    }

                    if (urlParamValue.length > 0) {
                        if (!(urlParamName == 'dyn_pageIndex' && urlParamValue == 0)) {
                            arrQueryString.push(urlParamName + '=' + urlParamValue);
                        }
                    }

                }
            });

            that._replaceStateHelper(arrQueryString);

        },

        _decodingValue: function(decodeString) {
            var decodedString = '';
            decodedString = decodeURIComponent(decodeString);
            return decodedString
        },

        _encodingValue: function(encodeString) {
            var encodedString = '';
            encodedString = encodeURIComponent(encodeString);
            return encodedString
        },

        _replaceStateHelper: function(rsQueryArray) {
            var _rsFilename = location.pathname.substring(location.pathname.lastIndexOf('/') + 1)
              , _rsQueryArray = rsQueryArray.join('&') || '';

            if (_rsQueryArray.length > 0) {
                history.replaceState({}, '', _rsFilename + '?' + _rsQueryArray);
            }
        },

        _initEvents: function() {
            this.$ctx.addClass("done");
            this._initPagination();
            this._initAccordion();
        },

        loadContent: function(originalUrl, attributes, pageload) {
            var that = this
              , url = originalUrl || ""
              , qsAttributes = that.checkQueryStrings() || ''
              , formAttributesCharset = '_charset_=UTF-8&';

            that.freshPageLoad = pageload;

            url = url.replace("{currentPath}", that.currentWindow.location.pathname + that.currentWindow.location.search);
            url = url.replace("{currentParams}", that.currentWindow.location.search.replace("?", "").replace(/(^|&)dyn_/g, "$1"));

            if ($("body").hasClass("skin-layout-author")) {
                url += url.indexOf('?') == -1 ? "?wcmmode=disabled" : "&wcmmode=disabled";

                if (that.freshPageLoad && !Tc.Utils.Lib.stringIsBlank(qsAttributes)) {
                    url += '&' + formAttributesCharset + qsAttributes;
                }
            } else {
                if (that.freshPageLoad && !Tc.Utils.Lib.stringIsBlank(qsAttributes)) {
                    url += '?' + formAttributesCharset + qsAttributes;
                }
            }

            this.$ctx.removeClass("done").addClass("loading");
            $.ajax({
                type: "GET",
                url: url,
                data: attributes,
                success: this.contentLoaded
            });
        },

        onLoadContent: function(param) {
            var that = this;

            if (param.url === undefined) {
                param.url = this.dataUrl;
            }
            // form post
            this.loadContent(param.url, param.attributes, false);
        },

        onInitialLoadContent: function(param) {
            if (!this.isDynamicContentLoaded) {
                this.onLoadContent(param);
            }
        },

        onUnloadContent: function() {
            this.isDynamicContentLoaded = false;
        },

        contentLoaded: function(data) {
            this.isDynamicContentLoaded = true;
            this.$ctx.html(data);
            this._initEvents();
            this.fire('contentDone');
            this.updateUrl(data);
            this.$ctx.removeClass("loading");
            this.sandbox.addModules(this.$ctx);
        },

        _initPagination: function() {
            this.$$("ul.pagination li:not(.active) a").off("click.pagination").on("click.pagination", this.onPaginationClick);
        },

        onPaginationClick: function(ev) {
            ev.preventDefault();
            var loadPage = $(ev.currentTarget).data("loadpage")
              , // 1
            $addParams = this.$$('.add-param[name!="' + this.pageIndexParam + '"]')
              , //
            // CNGIMPL-2943 - performance issue call paging without url-variable to enable caching
            attributes = ($addParams.length !== null && $addParams.length > 0) ? this.pageIndexParam + "=" + loadPage + "&" + $addParams.serialize() : ""
              , replaceddataUrl = decodeURI(this.dataUrl).replace("{}", loadPage);

            this.loadContent(replaceddataUrl, attributes, false);
        },

        _initAccordion: function() {
            if (!this.isDynamicContentLoaded && this.$ctx.data("accordion") === 1) {
                this.$ctx.prev("a.collapsed").on("click.accordion", this.onAccordionClick);
            }
        },

        onAccordionClick: function() {
            this.loadContent(this.dataUrl, null, true);
            this.$ctx.prev("a.collapsed").off("click.accordion");
        },

        checkQueryStrings: function() {
            var that = this
              , docLocSearch = that.currentWindow.location.search
              , queryString = docLocSearch.replace('?', '')
              , strKeyValue = null
              , formFieldName = ''
              , returnString = '';

            var querystringKeyValueArray = queryString.split('&');
            for (var iq = 0; iq < querystringKeyValueArray.length; iq++) {
                strKeyValue = querystringKeyValueArray[iq];
                querystringKeyValueArray[iq] = strKeyValue.split('=');

                if (that.$dynForm.find("[data-url-param='" + querystringKeyValueArray[iq][0] + "']").length > 0) {

                    formFieldName = that.$dynForm.find("[data-url-param='" + querystringKeyValueArray[iq][0] + "']").attr('name');
                    returnString += '&' + formFieldName + '=' + querystringKeyValueArray[iq][1];
                }
            }

            return returnString.substring(1);

        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Nsbabonnement module implementation.
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Nsbabonnement
	 * @extends Tc.Module
	 */
    Tc.Module.Nsbabonnement = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            Tc.Utils.Lib.initValidation(this.$$('form'));
            callback();
            // !do not remove
        },

        after: function() {
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Newslist module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <stefan.baur@namics.com>
	 * @namespace Tc.Module
	 * @class Newslist
	 * @extends Tc.Module
	 */
    Tc.Module.Newslist = Tc.Module.extend({
        on: function(callback) {
            var _this = this;

            //this causes those callbacks to be executed in the scope of the module, not the scope of the bound element
            _this.bindAll("onSearchFormSubmit");

            callback();
            // !do not remove
        },

        after: function() {
            this.$$(".newslist-form").on('submit', this.onSearchFormSubmit);
        },

        onSearchFormSubmit: function(ev) {
            ev.preventDefault();
            var $searchForm = $(ev.currentTarget)
              , attributes = $searchForm.length !== null ? "pageIndex=" + 0 + "&" + $searchForm.serialize() : "pageIndex=" + 0;
            this.fire('loadContent', {
                attributes: attributes
            });
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Nsbnewslist module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Nsbnewslist
	 * @extends Tc.Module
	 */
    Tc.Module.Nsbnewslist = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            var _this = this;
            _this.bindAll("_onSubmit", "onNewsFilterSubmit");
            callback();
            // !do not remove
        },

        after: function() {
            var _this = this;
            _this._initNewsFilter();
            _this._setEnddate();
            _this.$$("form").on("submit", _this._onSubmit);
        },

        _setEnddate: function() {
            // AEMDEV-652 - set end date to today if empty
            var _this = this
              , date = new Date()
              , enddate = ('0' + date.getDate()).slice(-2) + '.' + ('0' + (date.getMonth() + 1)).slice(-2) + '.' + date.getFullYear();

            if ($("input[name='endDate']", _this.$ctx).attr("value") === '') {
                $("input[name='endDate']", _this.$ctx).attr("value", enddate);
            }
        },

        _initNewsFilter: function() {
            this.$$("#newsFilter").on('submit', this.onNewsFilterSubmit);
        },

        onNewsFilterSubmit: function(ev) {
            ev.preventDefault();
            var $newsFilter = $(ev.currentTarget)
              , attributes = $newsFilter.length !== null ? "pageIndex=" + 0 + "&" + $newsFilter.serialize() : "pageIndex=" + 0;
            this.fire('loadContent', {
                attributes: attributes
            });
        },

        _onSubmit: function() {
            var _this = this, submit = true, $datepicker = _this.$$(".is-datepicker"), $el;
            if ($datepicker.length) {
                $datepicker.each(function(i, el) {
                    $el = $(el);
                    if (!$el.val()) {
                        submit = false;
                        $el.focus();
                    }
                    return false;
                });
            }
            return submit;
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Teaserlist module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Teaserlist
	 * @extends Tc.Module
	 */
    Tc.Module.Teaserlist = Tc.Module.extend({
        on: function(callback) {
            var _this = this;

            _this._loadDots();
            $(window).on("viewportchange", $.debounce(500, function(e) {
                _this._loadDots(true);
            }));

            //this causes those callbacks to be executed in the scope of the module, not the scope of the bound element
            _this.bindAll("_onReset", "onSearchFormSubmit");

            callback();
            // !do not remove
        },

        after: function() {
            this.$$("#teaserlist-form").on('submit', this.onSearchFormSubmit);
            this.$$("button[type='reset']").on("click", this._onReset);
            this.$$('.chosen-container').width('100%');
        },

        _onReset: function(ev) {
            var _this = this;
            setTimeout(function() {
                if (_this.$('.pagination').size() === 0) {
                    window.location.assign(window.location.pathname);
                } else {
                    _this.$('.tags').trigger('chosen:updated');
                    _this.$('.child-tags').trigger('chosen:updated');
                }
            }, 10);
        },

        onSearchFormSubmit: function(ev) {
            var usePaging = this.$ctx.data('paging-enabled');
            if (usePaging) {
                ev.preventDefault();
                var $searchForm = $(ev.currentTarget)
                  , attributes = $searchForm.length !== null ? "pageIndex=" + 0 + "&" + $searchForm.serialize() : "pageIndex=" + 0;
                this.fire('loadContent', {
                    attributes: attributes
                });
            }
        },

        _loadDots: function(isInitialized) {
            // var to = null;
            var _this = this
              , $ctx = _this.$ctx
              , $wrapper = $ctx.find('.wrapper')
              , minHeight = 1000;

            // if function is called the first time and any teaser contains an
            // image wait until all images are fully loaded, then call function again
            // with flag isInitialized
            if (!isInitialized) {
                var $images = $ctx.find(".row").find("img");
                if ($images.length) {
                    $images.load(function() {
                        //						if(to != null) {
                        //							clearTimeout(to);
                        //							to = null;

                        setTimeout(_this._loadDots(true), 100);
                        //							_this._loadDots(true);
                        //						}
                    });

                    // execute dotdotdot function after 1500 ms if image load event fails
                    //					to = setTimeout(function() {_this._loadDots(true);}, 1500);
                    return;
                }
            }

            // skip dotdot if there are images with height 0
            $.each($ctx.find(".row").find("img"), function() {
                if ($(this).height() === 0) {
                    return;
                }
            });

            // reset timeout function
            //			to = null;
            var execDot = false;

            // find all teaser within a row and set the height to lowest height
            $ctx.find(".row").each(function() {
                var $this = $(this);
                var $teaser = $this.find(".mod-teaser");
                var minimumHeight = 0;
                if ($teaser.length) {
                    $teaser.each(function() {
                        var $this = $(this);
                        minHeight = Math.min(minHeight, $this.height());
                        minimumHeight = Math.max(minimumHeight, $this.find("img").height());
                        var $img = $this.find("img");
                        if ($img.length && $img.height() == 0) {
                            //minimumHeight = Math.max (minimumHeight, 71);
                            return;
                        }
                        var $wrapper = $this.find(".wrapper");
                        if ($wrapper.length) {
                            execDot = true;
                            var textMinHeight = $wrapper.position().top + Math.min(71, $wrapper.height() + 5);
                            minimumHeight = Math.max(minimumHeight, textMinHeight);
                        }

                        minHeight = Math.max(minHeight, minimumHeight);
                    });
                }
                $this.find(".wrapper").each(function() {
                    var $this = $(this);
                    $this.css("height", minHeight - $this.position().top);
                });
            });

            // execute elipsis function
            if (execDot) {
                // AEM-282 Prevent text shortening when dialog checkbox is activated for it.
                $wrapper.find('p:not([data-prevent])').dotdotdot({
                    elipsis: '...',
                    wrap: 'word',
                    fallbackToLetter: true,
                    height: minHeight // set height to fix bug on smallest viewport -> dotdotdot plugin removes <p> from DOM without a height
                });
            }

            if ($ctx.parents("ul.dropdown-menu").length) {
                $ctx.find(".row").each(function() {
                    var $this = $(this);
                    var height = $this.height();
                    $this.children().css("height", height);
                });
            }
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Login module implementation.
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Nsbabonnement
	 * @extends Tc.Module
	 */
    Tc.Module.Login = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {
            Tc.Utils.Lib.initValidation(this.$$('form'));
            callback();
            // !do not remove
        },

        after: function() {
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Video module implementation.
	 *
	 * @author
	 * @namespace Tc.Module
	 * @class Video
	 * @extends Tc.Module
	 */
    Tc.Module.Video = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);

        },

        on: function(callback) {
            var $iframe = this.$ctx.find("iframe");
            $iframe.css("height", $iframe.width() * 9 / 16);

            $(window).on("resize viewportchange", function() {
                $iframe.css("height", $iframe.width() * 9 / 16);
            });

            callback();
            // !do not remove
        },

        after: function() {}
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Linklist module implementation.
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Linklist
	 * @extends Tc.Module
	 */
    Tc.Module.Linklist = Tc.Module.extend({
        on: function(callback) {
            var _this = this
              , $ctx = _this.$ctx;

            $ctx.on('change', '.js-linklist-select', function() {
                var opt = $(this).find(':selected')
                  , url = opt.data('url') || ''
                  , target = opt.data('target') || '_blank';

                if (url) {
                    if (Tc.Utils.Extensions && Tc.Utils.Extensions.isMobileDevice()) {
                        window.location.href = url;
                        // fix for mobile devices
                    } else {
                        window.open(url, target);
                    }
                }
            });

            callback();
            // !do not remove
        }
    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Twitterstream module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author  <>
	 * @namespace Tc.Module
	 * @class Twitterstream
	 * @extends Tc.Module
	 */
    Tc.Module.Twitterstream = Tc.Module.extend({
        on: function(callback) {
            var _this = this
              , $ctx = _this.$ctx;

            _this._loadTweets();
            _this._refreshTweets();

            callback();
            // !do not remove
        },

        _loadTweets: function() {
            var _this = this
              , $ctx = _this.$ctx
              , $timeline = $('.js-twitter-timeline', $ctx)
              , $url = $timeline.data('url');
            $url = $url || "";

            if ($("body").hasClass("skin-layout-author")) {
                $url += $url.indexOf('?') == -1 ? "?wcmmode=disabled" : "&wcmmode=disabled";
            }

            // Load HTML of tweets
            $.ajax({
                type: 'GET',
                url: $url
            }).done(function(data) {
                if ($timeline.html() !== data) {
                    $timeline.html(data);
                }
            });
        },

        _refreshTweets: function() {
            var _this = this
              , $ctx = _this.$ctx
              , $interval = $('.js-twitter-timeline', $ctx).data('time-interval') || 300000;

            // Refresh tweets every 5 minutes
            setInterval(function() {
                _this._loadTweets();
            }, $interval);
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * Iframe module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author Elena Wessolek <elena.wessolek@namics.com>
	 * @contributors Thomas May <thomas.may@namics.com>
	 * @namespace Tc.Module
	 * @class Iframe
	 * @extends Tc.Module
	 */
    Tc.Module.Iframe = Tc.Module.extend({
        on: function(callback) {
            this.bindAll("setCustomHeight");

            $(window).on("load resize", $.debounce(200, this.setCustomHeight));

            callback();
            // !do not remove
        },

        setCustomHeight: function(ev) {
            var $win = $(ev.currentTarget)
              , $iframe = this.$$("iframe")
              , heightDesktop = $iframe.data("custom-height-desktop")
              , heightTablet = $iframe.data("custom-height-tablet")
              , heightPhone = $iframe.data("custom-height-phone");

            if (heightDesktop && $win.width() >= 992) {
                $iframe.height(heightDesktop);
            } else if (heightTablet && $win.width() >= 767 && $win.width() <= 991) {
                $iframe.height(heightTablet);
            } else if (heightPhone && $win.width() <= 767) {
                $iframe.height(heightPhone);
            }
        }

    });
}(Tc.$));
(function($) {
    "use strict";
    /**
	 * Eventlist module implementation.
	 * https://gitlab.namics.com/snippets/2
	 *
	 * @author Dino Ziermann <dino.ziermann@namics.com>
	 * @namespace Tc.Module
	 * @class Eventlist
	 * @extends Tc.Module
	 */
    Tc.Module.Eventlist = Tc.Module.extend({

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
            this.bindAll("onSubmit");
        },

        on: function(callback) {
            this.$$("#eventsFilter").on("submit", this.onSubmit);
            callback();
            // !do not remove
        },

        onSubmit: function(ev) {
            ev.preventDefault();
            var $eventsFilter = $(ev.currentTarget)
              , attributes = $eventsFilter.serialize();
            this.fire('loadContent', {
                attributes: attributes
            });
        },

        after: function() {}

    });
}(Tc.$));
(function($) {
    "use strict";
    /**
     * Singlepageapplication module implementation copied from the Iframe module
     * https://gitlab.namics.com/snippets/2
     *
     * @author Raphael Hänni <raphael.haenni@bit.admin.ch>
     * @contributors Matthias Schwarz <matthias.schwarz@bit.admin.ch>
     * @contributors Elena Wessolek <elena.wessolek@namics.com>
     * @contributors Thomas May <thomas.may@namics.com>
     * @contributors Stefan Baur <sbaur@namics.com>
     * @namespace Tc.Module
     * @class Singlepageapplication
     * @extends Tc.Module
     */
    Tc.Module.Singlepageapplication = Tc.Module.extend({

        DEBUG: Tc.Utils.Lib.getUrlParameter("debugSinglePageApplication") == "true",

        UNDEFINED_CUSTOM_HEIGHT: -1,
        // Pseudo Constant

        on: function(callback) {
            this.$window = $(window);
            this.$iframe = this.$("iframe");

            this.bindAll("updateIframeHeight");

            callback();
            // !do not remove
        },

        after: function() {
            //initially set iframe height to 0
            this.$iframe.height(0);
            this.updateIframeHeight();

            this.$window.on("resize", $.debounce(200, this.updateIframeHeight));
            this.$iframe.on("load", this.updateIframeHeight);
        },

        updateIframeHeight: function(event) {
            this.log("updateIframeHeight - event", event);

            var height = this.determineCustomHeight();

            this.log("updateIframeHeight - custom height", height);

            if (height == this.UNDEFINED_CUSTOM_HEIGHT) {
                height = this.calculateIframeContentHeight();
                this.log("updateIframeHeight - calculated height", height);
            }

            this.log("updateIframeHeight - final height", height);

            this.$iframe.height(height);
        },

        calculateIframeContentHeight: function() {
            var content = this.$iframe.contents()
              , contentHeight = 0;

            try {
                contentHeight = content.height();
            } catch (err) {
                this.log("calculateIframeContentHeight - Could not calculate height due to error: ", err.message);
            }

            this.log("calculateIframeContentHeight - content", content);
            this.log("calculateIframeContentHeight - contentHeight", contentHeight);

            return contentHeight;
        },

        determineCustomHeight: function() {
            var windowWidth = this.$window.width()
              , heightDesktop = parseInt(this.$iframe.data('custom-height-desktop'), 10)
              , heightTablet = parseInt(this.$iframe.data('custom-height-tablet'), 10)
              , heightPhone = parseInt(this.$iframe.data('custom-height-phone'), 10)
              , height = this.UNDEFINED_CUSTOM_HEIGHT;

            this.log("determineCustomHeight - windowWidth", windowWidth);
            this.log("determineCustomHeight - heightDesktop", heightDesktop);
            this.log("determineCustomHeight - heightTablet", heightTablet);
            this.log("determineCustomHeight - heightPhone", heightPhone);

            if (heightDesktop && windowWidth >= 992) {
                height = heightDesktop;
            } else if (heightTablet && windowWidth >= 767 && windowWidth <= 991) {
                height = heightTablet;
            } else if (heightPhone && windowWidth <= 767) {
                height = heightPhone;
            }

            this.log("determineCustomHeight - height", height);

            return height;
        },

        log: function() {
            if (window.console) {
                if (this.DEBUG) {
                    console.log.apply(console, arguments);
                }
            }
        }

    });
}(Tc.$));

(function($) {
    "use strict";
    /**
	 * The Joblist modules fetches a joblist page from jobs.admin.ch
	 *
	 * @author Matthias Schwarz <matthias.schwarz@bit.admin.ch>
	 * @namespace Tc.Module
	 * @class Joblist
	 * @extends Tc.Module
	 */
    Tc.Module.Joblist = Tc.Module.extend({
        DEBUG: Tc.Utils.Lib.getUrlParameter("debugJoblistApplication") == "true",

        gotSizeFromFrame: false,
        lastFrameHeight: 0,

        init: function($ctx, sandbox, modId) {
            this._super($ctx, sandbox, modId);
        },

        on: function(callback) {

            var _this = this;
            var $ctx = _this.$ctx;

            this.$window = $(window);
            this.$div = this.$("div.jobListContent");
            this.$iframe;

            this.bindAll("loadContentToDiv", "iframeFallback", "updateIframeHeight", "log");
            this.authorMode = this.$div.hasClass("authorMode");
            this.gotSizeFromFrame = false;

            callback();
            // !do not remove
        },

        after: function() {

            var dataUrl = this.$div.data('url');
            this.loadContentToDiv(dataUrl, this.$div[0]);

            this.log("at the end of after()");
        },

        loadContentToDiv: function(url, targetElement) {

            url = url || "";
            var _this = this;
            var _targetElement = targetElement;
            // we need a var for the closure

            if (url) {
                $.ajax({
                    type: 'GET',
                    url: url

                }).done(function(data) {

                    var extractBody = /^[\s\S]*<body>([\s\S]+)<\/body>[\s\S]*$/;
                    var match = extractBody.exec(data);

                    // we skip documents with head but without resp. with an empty body
                    if (match || data.indexOf('<head>') == -1) {
                        var jobListDom = match ? $(match[1]) : $(data);
                        jobListDom.find('script').remove();
                        jobListDom.appendTo(_targetElement);
                    } else {
                        _this.log("loadContentToDiv(): skipping response from " + url + " because we got a head without a body!");
                    }

                }).error(function(request, status, errorThrown) {

                    var errorTxt = "loadContentToDiv() could NOT load " + url + " Status:" + status;
                    _this.log(errorTxt);
                    _this.iframeFallback(errorTxt);
                })
            }
        },

        iframeFallback: function(errorText) {

            if (this.authorMode) {

                this.log("iframeFallback() does work on author --> we display an error message");

                this.$div.text(errorText);

            } else {
                // we running on a publish instance

                this.log("use an iframe with document.domain='admin.ch' to display the joblist");

                var idNumber = this.$div[0].id.replace(/^.*[^0-9]([0-9]+)$/, '$1');
                try {
                    var iframeId = "generated-iframe-" + idNumber;
                    this.$iframe = this.$div.append("<iframe id='" + iframeId + "'/>");

                    this.$iframe = $("#" + iframeId);
                    this.$iframe.attr('height', 10).attr('scrolling', 'no').attr('src', this.$div.data('url'));
                    this.$iframe.css('width', '100%').css('border', 0).css('padding', 0).css('margin', 0);

                    document.domain = 'admin.ch';
                    this.$window.on("resize", $.debounce(200, this.updateIframeHeight));
                    this.$iframe.on("load", this.updateIframeHeight);
                    this.updateIframeHeight();

                } catch (err) {
                    // we provide a link, if iframes are not supported by the browser

                    var linkHref = this.$div.data('url');
                    var linkText = $('#joblist-title-' + idNumber).text();
                    linkText = linkText ? linkText : linkHref;
                    this.$div.append("<a href='" + linkHref + "'>" + linkText + '</a>');
                }
            }
        },

        updateIframeHeight: function(event) {

            try {
                var height = this.$iframe.contents().height();
                this.log("updateIframeHeight - setting height " + height);
                this.$iframe.height(height);
            } catch (err) {
                this.log("updateIframeHeight - Could not determine height due to error: " + err.message);
            }
        },

        log: function() {
            if (window.console) {
                if (this.DEBUG) {
                    console.log.apply(console, arguments);
                }
            }
        }
    });
}(Tc.$));
